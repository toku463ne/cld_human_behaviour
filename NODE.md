# ノードのルール

ノード（＝1人の人間）が従うルールの全体像です。**数値は `PARAMETERS.md`**、崩してはいけない原則は `CLAUDE.md`、なぜそう決めたかは `HISTORY.md` の設計判断ログ、操作方法は `README.md` にあります。ここには「何が起きるか」だけを書きます。

実装は `engine/` にあり、この文書はその要約です。食い違いがあればコードが正しいので、この文書を直してください。

---

## 1. ノードとは

1人 = 1ノード。持っているものは3種類です（詳細は `PARAMETERS.md`）。

- **遺伝子9本** — 攻撃力・防御力・最大体力・移動速度・回避率・記憶容量・理性・頭の良さ・魅力。生まれたときに決まり、生きている間は変わらない。子に受け継がれる。**9本すべてに役割がつきました**（防御力・回避率は段階8、記憶容量は段階9）。**体力上限と移動速度は「基準値 × 遺伝子 / 50.5」**なので、真ん中の遺伝子がちょうど基準の身体になる
  - **9本は1つの予算を分け合う。** 合計がその個体の予算で、何かを高くするには別の何かを削るしかない。**初期個体だけ**が予算（`GeneBudgetMean` ± `GeneBudgetStd`）と配分（ディリクレ分布、`GeneInitAlpha`）を抽選し、以後は継承で決まる。1本あたりの範囲は1〜100で、範囲に収めたあと予算に合うよう配分を詰め直す
  - **まれに「天才」が生まれる。** 出生あたり `GeniusRate`（約1.7%）で予算が `GeniusBudget` に、`GreatGeniusRate`（0.17%）で `GreatGeniusBudget` に**置き換わる**。親から継いだ額の倍率ではなく、水準そのものへの跳躍。その子はまたそこから継ぐので、当たりは一代では消えない
  - **子の予算は「どちらか一方の親の予算 ＋ 正規乱数（`BudgetInheritSpread`）」。両親の平均は取らない**（平均は毎世代ごとに分散を半分にするため）。`BudgetHeritability` を 0 にすると毎回抽選に戻る
  - **子は「配分を親から、予算を親から」別々に継ぐ。** 遺伝子ごとに引いた値を、継いだ予算に合うようまとめてスケールする。この結果、**突然変異は配分を動かすだけで、合計（予算）を増やさない**
- **状態3つ** — 体力・空腹度・**成熟度**（`Maturity`。食事で 0→1 に進み、能力の実効値と繁殖の可否を決める。11節）。**空腹の進み方は身体の大きさ（予算）に比例して速い**（`BudgetUpkeep`）。体力の上限は個体ごとに違う（上の体力遺伝子）。**求愛の体力条件と子の初期体力は、その個体の上限に対する割合**で決まる。生きている間に変わる。遺伝しない
- **他者についての記憶** — 相手ごとに「自分がその相手に失った体力」「その相手にしてもらったこと（好意）」「その相手の強さの推定」。**覚えていられる人数と忘れる速さは記憶遺伝子で決まる**（8節）

このほかに**寿命の残量**を持ちますが、これはノード自身が知る情報ではありません（2節を参照）。ノードが自分について知っている・使えるのは上の3種類だけです。

食料は**持ち物ではありません**。世界に落ちていて、食べるとその場で空腹度が下がります。食料には2種類あります。

- **植物** — 世界に湧く。人間だけが食べる
- **肉** — 死体。**どの種族の死体かを覚えていて、自分と同じ種族の肉は食べません**（共食い不可）。倒した者（直前に殴った者）が一定時間だけ独占でき、そのあとは誰でも取れる。放置すると腐って消える

死体の肉の量は**その個体が何でできていたか（予算）に比例**します。大きな相手を倒すほど多く落ちる、というのが「1人では割に合わないが集団なら割に合う」を世界の側の事実として作る唯一の仕掛けです。

ノードには**性別**があります。異性としかペアになれません。性別は生まれたときにコイン投げで決まり、能力には影響しません。

ノードには**種族**（`Agent.Species`）があります。**human と enemy の2種族で、どちらもまったく同じ構造・同じ意思決定エンジンで動きます。** 違うのは2点だけです。

- **予算の抽選範囲**（enemy のほうが大きい＝強く、倒したときに落ちる肉も多い）
- **食べられるもの**（human は植物と他種族の肉、enemy は肉だけ。どちらも自分の種族の肉は食べない）

種族は隠しパラメータではありません（見れば分かる）。**求愛は同じ種族の間だけ**で、異種族は「食料になりうる相手」として効用式に載ります（`NODE.md` 7節）。

---

## 2. 3つの状態量と、その一方向の結合

```
食べる  →  空腹度 ↓  →（満腹かつ無理をしていなければ）→  体力 ↑
動く / 戦う  →  体力 ↓
放置  →  空腹度 ↑  →（一定を超えると）→  体力 ↓  →（0で）→  死
```

- **空腹度**は毎tick一定量ずつ上がる。食べると `FoodNutrition` だけ下がる
- **体力**は、空腹度が `StarveHunger` を超えると減り始める。減る速さは空腹度に比例して立ち上がる
- **体力**は、空腹度が `SatiatedHunger` を下回っていて、かつその tick に力を使っていなければ回復する。回復量は `RegenRate × (1 − そのtickのeffort)` なので、**全力で動いている限り満腹でも回復しない**
- 体力が0になると死ぬ

この結合の向きが決まっているおかげで、「満腹で無理をしなければ回復する」という**回復経路が必ず存在**します。これが無いと全員が単調に減って餓死するだけの世界になります。

**死因は餓死・戦闘・寿命消費の3つです。**

寿命消費（老化死）は、体力・空腹度とは別枠の**寿命の残量**（`Agent.Lifespan`）が尽きたときに起きます。これは完全にバックグラウンドの機械的な処理で、ノードの意思決定には一切関与しません。

- ノードは自分の寿命を知りません。**`Perception` に寿命は載らず**、効用式にも「寿命を延ばす」というゴール項はありません
- 減るのは2つの場合だけです。**過小**＝空腹度が `StarveHunger` を超えている間（既存の体力の飢餓ダメージと同じ条件）。**過度**＝空腹度が `OverfedHunger`（「腹八分目」＝満腹度80%）を下回っている間、つまり食べ過ぎているとき
- どちらの条件にも当てはまらない範囲（`OverfedHunger` 〜 `StarveHunger` の間）で食べている限り、寿命は減りません
- 寿命が0になると、体力が0になったときと同じように死にますが、死因としては区別されます（`Stats().AgingDeaths`）
- 選択圧がかかるとすれば、それは「寿命を意識して行動を変えた」からではなく、**結果的に長生きした個体が結果的に多くの子を残した**という、世代をまたいだ差次的生存だけを通じてです

---

**同じものばかり食べると損をします**（段階16、`engine/diet.go`）。直近に食べた種類ほど、次の一口の栄養が目減りします（最大35%）。

- **新しい状態量ではありません。** 状態量は今も3つで、「最近何を食べたか」は**殴られた相手を覚えているのと同じ種類の帳簿**です。他のすべての減衰量と同じく、読み出し時に遅延減衰します
- **隠しません。** 寿命は「ノードが知りようのない沈黙の集計」という意図的な例外ですが、これは普通のほうで、`Perception` にも効用式にも空腹度と同じように載ります（**動物は自分が何かに飽きたことを当然知っている**）
- **割引はゼロに届きません。** 飽きることは「他を探す理由」であって「食料の隣で餓死する理由」ではありません
- 効果は小さいのですが（主食が事実上植物1種類なので）、**これが群れ狩りを初めて有意に増やしました**——植物に飽きたノードにとって、肉は**植物では取れない価値**を持つからです


## 3. 能力の役割

役割が重ならないように分けてあります（防御力・回避率は7節、魅力は10節）。

| 能力 | 何の能力か | 効き方 |
| --- | --- | --- |
| **パワー** | 体力あたりの戦闘効率 | 与えるダメージが `パワー / 50.5` 倍になる。体力そのものは増えない |
| **理性** | 世界を**正しく読む**力 | 他者の強さ・魅力の推定に乗るノイズが `(100 − 理性)/100 × JudgementNoise` に縮む。また比較検討の期間が長くなる |
| **頭の良さ** | 読んだ上で**良い手を選ぶ**力 | 各候補の効用の評価に `(100 − 頭の良さ)/100 × ChoiceNoise` の正規ノイズが乗る |
| **記憶容量** | 何人を、どれだけ長く覚えていられるか | 覚えていられる相手の数（`MemoryCapacity × 記憶/50.5`）と、1tickに取り込める数、忘却の速さ `(100 − 記憶)/49.5` 倍（8節） |

理性と頭の良さの違いは「見誤る」と「選び誤る」です。理性が低い個体は強い相手を弱いと思い込み、頭の良さが低い個体は正しく見えているのに損な手を選びます。

頭の良さの「選択品質」を**確率ではなくノイズ**として実装しているのは意図的です。「一定確率で最善手を外して一様ランダムに選ぶ」方式だと、外したときにしばしば自殺的な攻撃を選んでしまい、世界が暴力的になりすぎました。ノイズ方式なら、間違えるときも**近い評価の候補**に間違えます。

---

## 4. 1tickに起きること（処理順）

順序は結果を変えるので固定されています（`world.go:207`）。

1. **食料が湧く** — `FoodSpawnRate` の分だけ（小数部は次tickに繰り越す）
2. **考える** — 再判断のトリガーが立っているノードだけが `Decide()` を呼ぶ。**ペア中のノードは考えない**
3. **動く** — 全ノードが自分の行動を1tick分実行する。攻撃はここでは当たらず、キューに積まれるだけ
4. **打撃が同時に当たる** — 積まれた攻撃をまとめて適用する
5. **代謝** — 空腹度が上がり、体力が増減し、寿命の残量が減る（過小・過度のときだけ）、体力0または寿命0のノードが死ぬ
6. **出産と死体の除去** — 生まれた子を追加し、死んだノードを取り除く

**2で全員が「動く前の世界」を見て、4で打撃が同時に当たる**のが要点です。そうしないと、配列の先に並んでいるノードが有利になります。

---

## 5. 意思決定

### 5-1. いつ考えるか（トリガー方式）

毎tick考えるのではなく、**何かが起きたときだけ**考えます。理由は2つ。毎tick考えるのは一番高い処理であること、そして毎tick考え直す個体はどんな計画も最後までやり遂げられないこと。

考え直すきっかけは次のとおりです。

| トリガー | いつ |
| --- | --- |
| `spawned` | 世界に登場した |
| `attacked` | 誰かに殴られた |
| `vitality drop` | 前回の決定から `TriggerVitalityDrop` 以上の体力が減った |
| `target lost` | 狙っていた food/相手が消えた、取られた、条件を満たさなくなった |
| `goal reached` | 行動が完了した（食べ終わった、観察し終わった、逃げ切った） |
| `idle` | `TriggerIdleTicks` の間なにも起きていない |
| `food in sight` | 休息中か移動中に食料が視界に入った |
| `bond ended` | ペアが解消された（出産、または相手の死） |
| `controller set` | 操作するコントローラが差し替わった（人間が乗り移った・AIに戻した） |
| `requested` | 理由を言わずに要求された（人間プレイヤーが「今すぐ考え直せ」と押した。5-5） |

**「体力が減った」は「逃げろ」ではなく「考え直せ」です。** 逃走用のハードコードされた閾値はどこにもありません。逃げるかどうかは、あくまで下の効用比較の結果として決まります。

### 5-2. 何を見て考えるか（知覚）

`Decide()` に渡されるのは `Perception` だけで、そこには**他者の真の能力は一切載りません**。載るのは:

- **自分について** — 位置・性別・体力・空腹度・能力3つ・繁殖可能か・今誰に殴られているか・周囲の食料希少度、**配偶者としての自分の価値（`MateValue`）と今の要求（`MateBar`）**、**直前の求愛がどうなったか（`LastCourt`）**
- **視界内の食料** — 位置・距離、そして**その食料に一番近い他個体との距離**（＝競走の勝ち目）
- **視界内の他個体** — 位置・距離・性別・体力・ペア済みか・求愛中に見えるか・**自分を攻撃しに来ているか／自分を口説きに来ているか**、そして**自分の推定**（強さの推定値／その不確かさ／その相手に失った体力の累積／魅力の推定）

推定値には**自分の理性に応じたノイズが既にかかった状態**で渡されます。「相手の真の戦闘力は隠しパラメータ」という設計を、型のレベルで保証しています。

**視界は「自分が立っているセル＋その周囲1セル」のブロック**です（段階13、`engine/sight.go`。`SightGrid = false` で以前の半径130の円に戻せます）。外のものは存在しないのと同じです。

円との違いは、単に粗いか細かいかではありません。

- **円はどの方向にも同じだけ見え、どこに立っているかに依存しない**
- **ブロックは両方に依存する。** セルに入ったばかりの個体は来た方向をよく見通し、進む方向はほとんど見えません。**近いのに見えない相手**（セル境界のすぐ向こう）と、**遠いのに見える相手**（対角セルの隅、最大約177）が同時に存在します
- **「近い」ことが「見える」ことを意味しなくなりました。** これがこの段階が世界に入れた変化の本体で、実測でも殺しが減っています（近くても見えない相手は攻撃の候補に挙がらないため）

セルの大きさは**円と同じ面積になるよう較正**してあります（(2N+1)²·size² = π·radius²）。合わせないと「見える量が増えた／減った」が混ざって、形の違いと分離できません。

**視力（リング数）は遺伝子ではありません。** 0リング（自セルのみ）は世界が壊れ、2リングはやや悪い、という**離散で極端に非線形**な量なので、遺伝子にすると連続量である予算に対する固い閾値になります（`StrategyDepthUnlock` と同じ轍）。個体差を入れるなら、リング数ではなくセルの大きさを連続に振るべきです。

### 5-3. どう選ぶか（効用比較）

考えられる手を**すべて**同じ式で採点し、一番高いものを選びます。

```
効用 = Σ(目的の価値 × その目的を達成できる確率)
       − 過去にその相手に払わされた体力（リスク）
       − 体力コスト
       − 時間コスト
```

目的は6種類あり、行動によってどれが立つかが違います。

| 目的 | 意味 | どの行動に立つか |
| --- | --- | --- |
| `life` | 死ぬ確率がどれだけ下がるか | 休む・食べる・逃げる・攻撃する |
| `stake` | 今争っている食料を確保できる価値 | 攻撃する（その相手が食料の競争相手のとき） |
| `rival` | 将来その食料を食べるはずの口を減らす価値 | 攻撃する |
| `offspring` | 子が得られる価値 | 求愛する |
| `info` | 相手の強さを知る価値 | 観察する |
| `explore` | 見えていない食料を見つける価値 | さまよう |

**優先順位は「生命 > 子孫」だけ**で、時間は目的ではなくコストです。生命が子孫を上回るのは、`ReproHunger` / `ReproVitality` の条件（空腹でなく体力が十分）を満たさないと求愛が候補に挙がらないという形で担保されています。

死ぬ確率の見積もりは2つの合成です。

- **餓死までのカウントダウン** — 今の消耗ペースで `PlanHorizon` 以内に体力が尽きるか
- **消耗していること自体の危険**（`ShockRisk`）— 体力が減っていれば、次に何が起きても耐えられない

2つ目が無いと、空腹でない個体にとって体力の消費がタダに見えて、世界が殴り合いになります。

### 5-3b. 式の中の仮定は、個体ごとの値（段階12a）

上の式に出てくる数値のうち5つは、**全ノード共通の定数ではなく個体ごとの値**です（`engine/lore.go`）。2種類あり、**どこから来られるか**が違います。

| 値 | 種類 | 出どころ |
| --- | --- | --- |
| 殴り返される度合い（`retaliation`） | **世界の事実** | 経験から学ぶ。正解が世界の側にある |
| 求愛が受け入れられる確率（`accept`） | **世界の事実** | 同上 |
| リスクの重み（`RiskWeight`） | **好み** | 親から継ぐ＋突然変異。正解が無い |
| 競合排除の価値（`CompetitionWeight`） | **好み** | 同上 |
| 消耗の危険（`ShockRisk`） | **好み** | 同上 |

- **事実は間違えられる。** 強さ推定と同じ走る平均で更新し、証拠の数には上限があります（上限が無いと、老いた個体は世界が変わったことに気づけなくなる。記憶が薄れるのと同じ理由）。
- **好みは間違えられない。** より成功するかしないかがあるだけなので、粒子的遺伝（親のどちらかからそのまま）＋突然変異で、選択に任せます。世界の値の4倍を上限にクリップします（上限が無いと歯止めの無いランダムウォークになる）。
- **殴り返しの観測は「1回の戦い＝1票」。** 1打撃ごとに数えると、長い一方的な袋叩きが世界の答えを支配してしまうため、engagement が始まって5tick後に1度だけ数えます（相手が殴り返すには考え直す1tickが要るので、初撃で数えると必ず「殴り返さない」になる）。**攻撃した側と傍観者が学び、殴られた側は学びません**（自分の選択は世界についての証拠ではない）。
- **求愛の答えは「相手が承諾したか」だけ**を数えます。自分が思い直したことは、世界がどう答えるかについて何も教えないからです。

**既定では学習は止めてあります（`LearningRate = 0`）。** 効かないからではなく、効くからです。詳しくは `PARAMETERS.md` C-5b と `HISTORY.md`。

### 5-3c. 仮定は交換できる（段階12b）

上の5つは**他個体と交換できます**。専用の行動は作らず、既存の `observe`（観察）に相乗りします——観察はすでに体力と時間を払っており、**1度に1人しか観察できない**という排他性も持っているので、追加のコスト設計が要りません。

- **一方向の「学ぶ」ではなく、双方向・同時の歩み寄り。** 値が違えば互いに相手の値へ同じ量だけ近づきます（`LoreExchangeRate`）。**取るだけで与えないことはできません。** 観察を選んだのは片方だけですが、見られた側も変わります。
- **1回の歩み寄りは小さい**（既定 0.02＝差の2%）。中間で会う（0.5）ようにすると、**1回の実行に交換が約19000回起きるので集団の値が数世代で1点に収束し**、段階12a が作った「好みの多様性」が消えます（消えると個体数が半減する。`HISTORY.md`）。
- **好意の加点量は、交換で実際に動いた値の大きさに比例します。** すでに値が近い相手との交換は自動的にほぼ無価値になります（そのための判定は書いていません）。
- **交換が走った時点で、差がゼロで不成立でも減衰の起点は更新されます**（#22）。ただし**すでに覚えている相手だけ**——並んで立って全部意見が一致しただけの他人を、覚える枠を潰してまで記憶しません。
- **垂直伝承（親→子）と水平伝承は同じ経路です。** 親子は育児期間ぶん自動的に共在するので、同じ交換が多く走るだけで、家族についての規則はどこにもありません。
- **好意が効用の正の項になりました**（`LoreValue`）。「好意のある相手からは学べる」＝観察の価値に「その相手への好意」が乗ります。**他個体が生きていて近くにいることが自分の得になる2本目の経路**で、集団の**持続**を説明するのはこちらです（1本目は段階11 の共同狩猟）。相手が何を信じているかは見えないので、判断材料は**過去に何かを得られた相手かどうか**だけです。

### 5-3d. ヒント＝言い伝え（段階12c）

**効用式に書かれていない関係**を、設計者ではなく選択に見つけさせる枠です。1つのヒントは「**状況特徴量 × 対象行動 × 重み**」の3つ組で、上限4本。

- **ヒントは行動を決めません。** 既存の効用比較に**加点・減点として入るだけ**で、ヒント由来の分岐はどこにもありません。最大重みのヒントでも、致命的な手を選ばせることはできません（テストで固定）。
- **知能・理性でゲートしません**（`StrategyDepthUnlock` と同じ轍を踏まないため）。ヒントが要求するのは**予算**だけです。
- **枠は遺伝子予算を食います**（`HintSlotCost`）。**空の枠でも払います**——払っているのは「入れ物」であって中身ではありません。だから「賢い手口を増やすには身体の何かを削る」になります。
- **突然変異は二段階。** 重みの微調整＝通常の突然変異。**何についてのヒントか（状況・行動）が変わるのは、既存の天才イベントのときだけ**です。新しい概念は作らず、「新しい発想」と「桁違いの身体」を同じ稀な出来事として扱っています。
- **継承は染色体式を枠単位に拡張。** 枠ごとに親のどちらかから丸ごと引きます。親の枠数が子より少なければ、子は**空の枠**を持って生まれます。
- **交換ではヒントは歩み寄りません。コピーされます。** 「腹が減ったら大物を狙え」の半分は、弱い版ではなく無です。**空の枠を持つ相手にだけ**入ります＝これが文化伝播で、血統を通らずに手口が広まる唯一の経路です。同じ（状況・行動）についてのヒントを既に持っていれば、逆符号でも上書きしません（それは意見の相違で、決着は生死がつけます）。

### 5-4. 頭の良さによる揺らぎ

採点し終わった各候補のスコアに、`(100 − 頭の良さ)/100 × ChoiceNoise` の正規ノイズを足してから比較します。頭の良さ71の個体でも標準偏差は5.8あるので、**僅差の候補どうしはしばしば入れ替わります**。差の大きい候補は入れ替わりません。

---

### 5-5. 誰が操作しているか（AIと人間・段階19）

ノードは `Controller` に「どうする？」と訊かれて答えます。答える側がAI（効用比較）でも人間でも、**engine は区別しません**。人間が操作するときのルールは3つだけです。

- **人間に見せてよいのは、そのノードが知っていることだけ。** 画面に出すのは `Perception` の中身に限ります（推定の強さ・不確かさ・見た目・好意まで。**真の攻撃力・防御力・回避率は出しません**）。AIが読めないものを人に見せると、遊んでいるゲームがこの世界とは別物になるからです。
- **狙えるのも、そのノードが見えているものだけ。** 視界の外の相手を指定する命令は**拒否されます**（`World.OrderHuman` が断る）。画面には世界じゅうが描かれていますが、動くのはノードです。**判定するのは engine（世界）であって、コントローラが持っている知覚の写しではありません**——写しは「最後に訊かれたとき」のもので、プレーヤーが見ている画面は「今」なので、そのあいだに視界に入ったものを「見えていない」と断ってしまうためです。判定に使うのは視界の規則そのもの（`canSee` と、食料なら `canEat`）で、乱数も記憶も動かしません。
- **命令は「次に訊かれたときの答え」です。** engine はトリガーが立ったときにしか訊かないので、押した瞬間に動くとは限りません。すぐ動かしたいときは「**今すぐ考え直せ**」を要求します（トリガー `requested`）。これは新しい経路ではなく、既存のトリガー機構をそのまま通ります。

命令が白紙に戻る場合が3つあります。**流れるのが2つ、遂行されて使い切られるのが1つ**で、これ以外に人間の命令を engine が上書きすることはありません。

- **狙っていたものが消えたとき**（`target lost` で訊かれたとき）。AIは訊かれれば全候補を採点し直すので勝手に立ち直りますが、人間の命令は据え置きなので、消えた相手を永遠に指し続けてしまいます。そこで命令は白紙（`rest`）に戻ります。
- **身体が変わったとき**（血統の乗り換え）。**命令は与えられた身体のものです。** 継いだ子は白紙から始めます（親が狙っていた相手は、子には見えてすらいないかもしれません）。
- **遂行されたとき**（`goal reached` で訊かれ、その命令が誰か・何かを狙っていたとき）。**食料は食べ終わった、相手は見定めた、追っ手は振り切った**——頼んだことが起きたので、命令は使い切られます。**これが無いと、自分で食べたことが「食料を取られた」として記録されます**（命令が据え置きなので同じ「それを食べろ」を返し、次のtickで「そんなものは無い」と流れるため）。**「取られた」と「食べ終わった」は世界の側が区別できています**——訊きにきたトリガーがそれです。

**血統の乗り換え**は、同じコントローラを子に付け替えるだけです。付け替えられるのは**成人した子**（`Agent.IsAdult` ＝ `Maturity >= ReproMaturity`。求愛できるかどうかと同じ線）に限ります。engine 側には「プレイヤー」も「ゲームオーバー」もありません——誰を後継にするか、後継がいなければどうなるかは、操作している側（今は `cmd/devview`）が決めます。

---

### 5-6. 訊かれて答える（段階23）

5-5 は「人がノードを操縦する」形でした。**もう1つの形**があり、こちらが遊ぶときの既定です: **ノードは自分で決めて動き、転機のときだけ人に訊きます**（`GuidedController`）。

- **AIは留守番ではありません。** そのノードを動かしているのは常に効用比較で、人が答えるのはそのうちの一部です。誰も答えなければ、そのノードは**純粋なAIノードと1ビットも違わない**生涯を送ります（テストで固定）。
- **訊かれるのは転機だけです。** 既定は「殴られた・狙いが消えた・配偶者を見つけた・破局した・体力が急に減った」。**実測で 112 tick に1回**（全トリガーは 14.5 tick に1回なので8分の1）＝**一生で20〜30回**。**戦闘は1戦で1回**にまとめます（毎tick訊くのは何も訊いていないのと同じ）。
- **選択肢は、そのノードが実際に採点した候補です。** 人のために別のメニューを書きません。同じ（行動, 対象）は effort 違いをまとめて1つにし、**そのノード自身の評価ノイズ（頭の良さ）を乗せたスコア順**で最大12件まで渡します——ノイズ抜きの正しい順位を使うと、その身体が買っていない賢さを人に渡すことになります。
- **候補を絞るときに、行動の種類ごと1つは必ず残します。** 単純に上位N件で切ると、**求愛と食事が真っ先に消えます**（満腹なノードはどちらも安く見積もるため）。**そこはまさに人が口を出したい場面**なので、切り落としません。
- **そこから何をどう見せるかは engine の外です。** 12件から最大5件を選び（**食事と求愛は専用の枠**、残りを「払うものが一番少ない／一番早く片がつく／一番大きく賭ける」の3つで）、**合計スコアは見せない**、というのが今の `cmd/devview` の判断です。**片方に「最善」と書いてある選択は選択ではない**ためで、engine 側は場を渡すだけです。
- **答えるのは1手であって、据え置きの命令ではありません。** 取り上げられた瞬間に使い切られ、次の判断からはまたノードが自分で採点します。だから 5-5 の「命令が流れる場合」がここには要りません。
- **古い質問には答えられません**（既定60tick）。遅れて答えるのは、もう無い世界に対して行動することだからです。
- **人の側から訊くこともできます**（`World.RequestDecision` → トリガー `requested`）。これも転機の1つとして扱います——**転機はわざと稀にしてあるので、これが無いと転機と転機の間にできることが「見ている」だけになります**。世界がこのトリガーを出すことはありません（人だけが出せます）。
- **狙えるのは視界の中だけ**という約束は同じで、検査するのも同じ場所（世界）、返るエラーも同じものです。

### 5-6b. 主人公への贈与（段階27）

**engine には1か所だけ、継承も獲得もしていないものを個体に渡す口があります**（`World.Endow`）。**シミュレーションの側からは誰も呼びません**——規則も、コントローラも、出生も。呼ぶのは「ゲームを走らせている側」だけです。

理由は操作性です。**予算は固定で配分は遺伝する**ので、渡された身体が速さにほとんど使っていないと、**人が動かす世界は糖蜜になります**。ここで**世界を速くすると、測定した世界と遊んでいる世界が別物になる**ので、代わりに**主人公に良い身体を渡します**。

- **新しい予算で払います。** 他の遺伝子から取ると、プレーヤーが同意していない取引になり、主人公が静かに脆くなります
- **1本ずつで、足した量を返します。** 黙って調整すると「世界が気前がいい」のと区別がつきません
- **贈与は血統に残ります。** 予算は遺伝するので、主人公の子は増えた予算を継ぎます。避けるには「誰の予算が本物か」という第2の規則が要り、そのほうが悪いので受け入れています

### 5-7. 節目に決める「性格」（段階23）

**一生に数回、その個体が何を求めるかを人が決められます。** 決めるのは効用式の**好み3つ**（`RiskWeight` ＝ 過去に払わされた相手をどれだけ避けるか／`CompetitionWeight` ＝ 競合を1人減らすことの価値／`ShockRisk` ＝ 体力が尽きかけていることの怖さ）。

**なぜこの3つか**: 段階12a が、効用式の仮定を「**世界の事実**（殴り返される度合い・求愛が通る確率＝正解が世界にあるので学べる）」と「**好み**（正解が無いので学べない）」に分けました。好みの側は**学習で決まらない＝人が決めても何も上書きしていない**唯一の量です。

**縛りは1つだけ**: **合計は親から継いだもので、変えられるのは配分だけ**です。3つは単位が違うので「世界の値の何倍か」で読み、その倍率の合計を固定します。つまり**用心深くするぶんは、競争心か、命への執着から出すしかありません**。自由に設定できると最適値を見つけた時点で毎回同じ設定になり、選択が形骸化します（遺伝子9本が1つの予算を分け合うのと同じ形です）。上限（世界の値の4倍）はそのまま生きています。

**どの変化を「節目」と数えるかは engine の外**です（今は `cmd/devview` が 乗り換え・成人・ペア成立・出産 の4つを見ています）。engine が守るのは合計と上限だけで、それ以外は遊ぶ側の決めごとです。

---

## 6. 行動の語彙

AIも人間プレイヤーも、同じ7つの行動しか出せません（人間操作は段階19 で実装済み。5-5）。engine は誰が操作しているかを知りません。

| 行動 | 対象 | 中身 |
| --- | --- | --- |
| `rest` | — | 何もしない。満腹なら回復する。**どこで休むかは無料ではない**（8-1） |
| `move` | 方向 | その方向へ1tick進む。さまようときに使う |
| `eat` | 食料 | `GrabRadius` まで近づいて食べる。届かなければ近づく |
| `attack` | 他個体 | `CombatRadius` まで近づいて殴り続ける。届かなければ近づく |
| `flee` | 他個体 | 相手と反対方向へ走る。相手が視界から消えたら完了 |
| `observe` | 他個体 | 視界内に留まって `observeTicks` の間見る。完了すると強さの推定が更新される |
| `court` | 他個体 | `GrabRadius` まで近づいて求愛する |

すべての行動が **effort（0〜1）** を持ちます。effort が高いほど速く・強くなりますが、体力の消費が増えます。速度は `√effort` に比例し、コストは `effort` に比例するので、**急ぐほど距離あたりが高くつきます**。

全力を出すことのリスク（体力が空＝無防備）は、専用のルールではなく効用式のリスク項が自動的に抑制します。

現在 effort の選択肢は **0.4 と 1.0 の2段階だけ**です（`effortLevels`）。

---

## 7. 戦闘

### 7-0. 3つのチャネル（構え）

戦う行動（攻撃・逃走）には**構え**があり、注ぎ込んだ effort を「殴る・守る・かわす」の3チャネルに配分します。選べるのは3種類のプリセットだけです（自由に配分させると候補が立方体に増えるため）。

| 構え | 殴る | 守る | かわす |
| --- | --- | --- | --- |
| 攻め寄り | 1.0 | 0.1 | 0.0 |
| 守り寄り | 0.5 | 0.9 | 0.1 |
| 逃げ寄り | 0.15 | 0.3 | 0.9 |

- **守る** — 受けるダメージが `DefenceCap × 防御遺伝子/100 × 使用率` の割合だけ減る。**同じtickの自分の打撃も同じ配分ぶんしか出ない**（＝取引であって只の改善ではない）
- **かわす** — 確率で打撃が**丸ごと無効**になる。回避遺伝子と**移動速度**の両方に依存する
- **体力消費** — Σ(チャネルごとの単価 × 使用率)。使っているチャネルは、その tick に役に立たなくても払う
- **構えを持つのは戦闘系の行動だけ。** 食事中・休息中の個体は守りもかわしもしない＝**無防備**

**相手の防御力・回避率は見えません**（他の能力と同じ隠しパラメータ）。効用式は自分の守りだけを計算に入れ、相手の守りは「殴ってみて驚く」形になります。

### 7-1. 継続ダメージ方式

決着がつくまで毎tick殴り合います。1tickのダメージは:

```
ダメージ = AttackDamage × effort × 殴る側のパワー / 50.5
```

終わり方は3つ。どちらかが逃走を選ぶ、どちらかの体力が0になる、または攻撃側が別の手に切り替える。**「何ラウンドで決着」といった仕組みはありません。**

### 7-2. 攻撃側有利

殴る側が払うのは `AttackCost × effort`、殴られる側が失うのは上のダメージで、**殴る側のほうが安い**（0.30 対 1.15）。ここから次が自動的に出てきます。

- 反撃してこない相手を殴るのが、体力効率でいちばん得（不意打ち、逃げる相手の追撃）
- 殴り合いは双方に高くつく

**注意**: この非対称と先制排除が噛み合うと世界が過度に暴力的になり、個体数が崩壊しえます。`CompetitionWeight` と `SkirmishTicks` はこの意味で敏感です。

### 7-3. 逃走と追撃

逃走は専用の閾値ではなく効用比較から出ます。逃げることだけが「今受けているダメージを止める」唯一の手なので、**受けているダメージこそが死因になりつつあるときだけ**逃走が勝ちます。それ以外のときは負けます。

逃げる側は `fleeExposureTicks` の間はまだ間合いに残っている前提で値付けされます（＝逃走はタダではない）。追う側から見れば、逃げる相手は反撃してこないので最も安い獲物です。追撃するかどうかも効用比較で決まります。

### 7-4. 打撃の同時適用

その tick の全打撃をキューに積んでから、まとめて適用します。スライス上の並び順が有利不利にならないようにするためです。

---

### 7-5. 外すと隙ができる（段階24）

**打撃がかわされたとき、外した側に隙ができます。** そのあと `OpeningTicks` のあいだ、**外した個体自身の守りとかわしが `OpeningGuard` 倍**になります（体が柔らかくなるのではなく、次の一撃に応えられない、という形です）。

- **世界で唯一「外すこと」に値段をつけている規則**です。それまで、空振りは effort を払うだけで、リスクではありませんでした
- **誰も事前に読めません。** 相手の回避率は隠しパラメータなので、「こいつは当たらない」と分かってから殴らないという判断はできません。**変わるのは結果だけで、値付けをするのは選択です**
- `Perception` にも効用式にも載りません（隙は数tickしか続かず、判断はトリガー方式で数十tickに1回なので、一過性の状態を「この戦いのあいだずっと続くもの」として見積もることになるためです）
- **入れて分かったのは、この規則が効く余地が小さいこと**です。**打撃のうちかわされるのは 5% しかない**ので、隙が立つ機会自体が稀で、実測では回避遺伝子への選択圧はまったく動きませんでした（数値と外れた予想は `HISTORY.md`）

## 8. 記憶と推定

相手ごとに**1レコード**を持ち、その中に3種類が同居します。答えている問いが違うので中身は分けてありますが、**枠は共有**です（好意だけ別枠にすると、好意はタダになり、全員が全員と結ばれて集団が世界に1個になります）。

**レコードの数には上限があります（記憶容量）。** 上限は記憶遺伝子から決まり（`MemoryCapacity × 記憶/50.5`）、いっぱいの個体が新しい相手を覚えるには**誰かを1人忘れる**必要があります。忘却が「時間で消える」だけでなく「枠の取り合い」になるのはこのためです。捨てられるのは**価値の最も低いレコード**（＝リスクも好意もゼロの、一度見ただけの相手）で、**価値の残っている相手は、群衆の中の一人に押し出されることはありません**。ただし**世界の側から来る出来事**（殴られた・ペアが成立した・子が生まれた）だけは別で、これは容量にも帯域にも関係なく必ず記録されます（殴られたことを「覚えないでおく」という選択は個体には無いため）。

**1tickに取り込める量にも上限があります（帯域）。** 帯域は容量から導出します（`容量 × MemoryBandwidthShare`）——専用の遺伝子は作りません（「どれだけ持てるか」と「どれだけ取り込めるか」は同じ器官の2つの面で、遺伝子を2本にすると同じものを予算で2度買うことになる）。群衆に入っても、その場で全員を覚えることはできません。

**忘却の速さも個体ごと**です。世界の減衰率に `(100 − 記憶)/49.5`（0〜2倍）を掛けます。記憶が平均の個体はちょうど世界の率で忘れ、記憶に何も割かなかった個体は倍の速さで忘れます。

**接触は記憶を新鮮に保ちます。** 相手を視界に入れると、**値は変えずに減衰の起点だけ**が今に更新されます（帯域を消費します）。式は変えていません。一度痛い目に遭った相手が近くにいる限り警戒は薄れず、いなくなれば薄れていく、というだけのことです。

**(A) リスク記憶 — 「この相手に自分はいくら払わされたか」**

その相手との戦いで失った体力の累積です。読み出すたびに `exp(−減衰率 × 経過tick)` で減衰します。忘却が無いと、長生きした個体が全員を最大脅威として記憶し、誰も誰にも近づかない総すくみの世界になります。

**(A') 好意 — 「この相手は自分の側の人間か」**

記憶に入る**初めての正の項**です。リスク記憶と同じレコード・同じ形の減衰で、減衰は少しゆっくりです（してもらったことは、されたことより長く残る）。

発生源は**実際に起きた出来事だけ**で、今のところ3つあります。**共在時間による加点はしません**（隣に立っていることは関係ではなく、それを関係と呼ぶと混雑した個体は全員と結ばれます）。

- **ペア成立（`AffinityPairBond`）と出産（`AffinityBirth`）** — 双方に入る
- **血縁（`AffinityKin`）** — **直系の親子1ホップのみ**。相手のレコードを作るときに初期値として入る。祖父母・孫・きょうだいは対象外で、世代をまたぐ減衰係数も持ちません
- **共同で仕留めた（`AffinityHunt`）** — 死体の分配に**共に与った相手**（＝`HuntCreditTicks` 以内に打撃を入れ、かつその肉を食べられる者）どうしに、互いへ入る。**血縁でもなく交尾もしていない相手から得られる唯一の好意**です。誰が数えられるかは新しいルールではなく**既存の分配ルールそのまま**なので、（a）近くにいただけの傍観者は入らず、（b）自分が食べられない相手を殴っても入りません。参加者が1人の狩りでは誰も何も得ません

**敵意は新設していません。** リスク記憶がそのまま敵意です。

好意が今のところ効くのは**休息のリスク割引1つだけ**です（下の 8-1）。効用の**正の項**として効かせるのは段階12（伝承）と対にします。

**(B) 強さ推定 — 「まだ戦っていないこの相手と戦ったらどうなるか」**

推定値と分散を持つベイズ更新です。更新されるのは2つの経路。

- **直接戦う** — `spectateInterval` ごとに、戦っている2人が互いを観測する（分散 `CombatObsVariance`）
- **観戦する** — 戦いが視界に入っている第三者も、両者を観測する（分散はその `SpectateObsFactor` 倍＝価値が低い）。`observe` 行動で意図的に見ることもできる

観測1回のノイズは `(100 − 理性)/100 × JudgementNoise` なので、**理性が高いほど正確**。観測数が増えるほど分散が縮み、**その相手について驚かなくなります**。

初対面の相手の推定は、**その個体が見た目から自分で学んだ線**です（下の 8-2）。不確かさは `PriorVariance` で、これは学習しても変わりません（当たりをつけただけで、確かめたわけではないため）。**記憶に空きが無くて覚えられなかった相手も同じ扱い**です——覚えられない相手は、何度会っても見た目で判断され続け、誰にもなりません。

### 8-1. 休むことは無防備であること

`rest` の効用には**露出のリスク**が入ります。中身は「周囲にいる、好意を持っていない相手の推定の強さ × 近さ」の合計に `RestExposureWeight` を掛けたもので、「休んでいる間これだけ削られ続けるとしたら」という**仮定のダメージ率**として死亡確率の見積もりに乗ります（`PlanHorizon` / `ShockRisk` と同じ「分岐しない1本の軌道」の形です）。

**実際の体力は1も減りません。** これは効用式の中だけの見積もりで、世界の側（打撃の適用・代謝）は何も変わっていません。体力が減るのは今までどおり**本当に殴られたとき**だけで、襲われなければ罰もありません。既存の `incoming`（今まさに受けているダメージを「今後も続く」と仮定して同じ式に入れる）や `ShockRisk`（まだ起きていない「次の一撃」の危険）とまったく同じ扱いです。

- 材料は**既にある推定だけ**です。新しい状態も新しい探索も足していません（`Decide` の先頭で近傍を1度走査し、被攻撃ダメージと露出量を同時に作ります）
- **好意のある相手は露出に数えません**（`AffinityTrust` で完全に信用しきるまで連続に割り引く）。配偶者や子のそばでは休み、よそ者のそばでは休まない、が閾値なしで出てきます
- `RestExposureWeight = 0` で完全に切れます。**重みが小さいのには理由があります。** 露出は `recoverable()` の中で `RegenRate`（0.09）から引かれますが、隣人1人ぶんの推定脅威は 1.14/tick と**12倍**あるので、重みが大きいと**視界に1人いるだけで休息の価値が丸ごとゼロ**になります（勾配ではなく崖）。そうなると消耗死が増え、`PairBondDuration` の途中で片方が死んでペアが壊れ、個体数が落ちます（測定値は `HISTORY.md`）

**休むのに向いた時刻もあります**（段階18、`engine/clock.go`）。世界は1000tickの周期を持ち、ノードごとに「眠りやすい時刻」が違います。

- **時刻で変わるのは休息の回復量だけ**です。視界も食料の湧きも変わりません（それらは全員に同時に効く世界の変化で、「個体どうしが違うことが何を生むか」という問いとは別）
- **体内時計は予算遺伝子ではありません。** 9本は「量」（多く買えば強く、予算が止める）ですが、時計は**円周上の位相**で、0.9 は 0.1 より大きいのではなく隣です。予算に入れると「夜型であることが殴る力を削る」という取引を主張することになります。好み（`lore.go`）と同じく、親から丸ごと継いで変異し、何も食いません
- **回復はゼロには届きません。** 自分の時刻でなくても、少なく回復します（「回復経路を必ず残す」は世界が捨てられない唯一のルール）
- **眠っている相手は見張れません。** 「信頼できる相手は休息の危険から差し引く」に条件を1つ足しただけで、新しい項ではありません。**これが「たまたま寝る時刻が違う」を「誰かが見張っている」に変えます**——見張り役はルールのどこにも存在しないまま。実測で個体数 +12.6 \*、この条件を外すと利益が丸ごと消えます

**休むのに向いた場所と、向かない場所があります**（段階14、`engine/region.go`）。世界は粗い**地域**に分かれていて、地域ごとに「露出の係数」を1つ持ちます（0.4〜1.6、1が普通）。上の露出の見積もりにこれが掛かるだけで、**式も状態も増えていません**。

- **地域は壁ではありません。** 出ていくのを止めるものは無く、**どの地域にいるかをノードに教える規則もありません**（地域IDを読むルールはゼロ）
- **ノードは「あそこへ行って休もう」を計画できません。** 知覚に載るのは**今立っている場所**の係数だけです。したがって起きるのは計画ではなく**選別**——安全な地域にいるノードは休息が安く済むので留まり、露出した地域にいるノードは休まず動き続けるので出ていきます
- 効果は**弱いが本物**（休んでいるノードの下の地面は集団平均より2%良い。差を大きくすると比例して伸びる）。**場所の差を強く効かせるには「他所を知る」経路が要る**という形をしています
- 地域は1種類だけで、食料の豊かさ（段階15a）も将来の地域リセットも**同じ地域の上に乗ります**

**地面の豊かさは覚えられ、伝えられます**（段階15b・15c、`engine/regionlore.go`）。

- **自分が立った場所しか覚えられません。** 行ったことのない場所は**人から聞く以外に知りようがなく**、それが伝承に価値を与えています
- 読み取るのは「そこにいる間に見えた食料の数」だけ（**新しい感覚は作っていません**）。誤読は他のすべてと同じく `(100−理性)/100` に比例し、鮮度は既存の遅延減衰＋接触リセットをそのまま使います
- **人の記憶の枠は1つも食いません**（場所は人ではない）。記憶遺伝子が買うのは「country をどれだけよく保てるか」です
- 覚えたら、**そこへ向かう選択肢が1つ増えます**。これが段階14 に欠けていた「選べる」で、経路探索も目的地への commit もありません（1歩ぶんの方角を毎回作り直すだけ）
- 伝承は既存の等価交換に相乗りします。**両方が行ったことのある場所は歩み寄り、片方しか知らない場所はそのまま渡ります**（平均できないので）。**「教える」行動はありません**——親子は育児期間ぶん共在するので、同じ交換が多く走るだけです
- **集団を1か所に集めているのはこれです。** 自分の経験だけでは集まらず、**伝聞が入って初めて**個体間距離が 140 → 100 に縮みます

### 8-2. 見た目からの学習

**見えるのは「体つき」だけ**です。`Appearance` ＝ 体力遺伝子と移動速度遺伝子の平均で、**実効値**なので子どもは小さく見え、老いれば小さく見えます。**攻撃力は入っていません**（見た目に答えが入っていたら、それは推定ではなくただの遅い観測です）。観測ノイズは `(100 − 理性)/100 × AppearanceNoise` で、**強さのノイズ（`JudgementNoise`）よりずっと小さい**（体格は強さより見て分かる、という設定です）。

**「その体つきが何を意味するか」は engine が持たず、各個体が自分の経験から学びます。**

- 強さの観測（直接戦う・観戦する）は、そのまま「**この体つきの相手は、これくらい殴った**」という1点のデータでもある
- 個体はその点の集まりに**直線を1本**当てる（最小二乗。合計値だけ持つので観測を貯めない）。次に会った知らない相手には、その直線を当てて当たりをつける
- **傾きは「体つきは何も言わない」側へ引き戻される**（`AppearanceSlopePrior` 回ぶんの疑い）。個体が持てる観測数は数十なので、素の傾きはほとんどノイズで、**ノイズに引いた直線は直線を引かないより悪い**（実測。`HISTORY.md`）
- `AppearanceMinReads` 回の観測を積むまでは直線を使わず、`PriorStrength` に戻る
- **この学習は記憶容量を消費しません。** 誰かを覚える枠が無くて記録を作れなかった観察からも、印象だけは残ります（「誰か」と「その手のものは何か」は別の問いだからです）
- **記憶されるのは個体の経験だけ**で、親から受け継がれも、他人から教わりもしません（伝承は段階12）

`LearnFromLooks = 0` で切ると、初対面は全員 `PriorStrength` に戻ります（段階10 前の世界）。

---

## 9. 復讐について

**「攻撃してきた相手を恨む」という感情の状態はありません。** 攻撃は効用式の一手であって、報復ではありません。

攻撃が選ばれる理由は2つだけです。

- **今争っている食料を取る**（`stake`）
- **将来その食料を食べる口を今のうちに減らす**（`rival`）

2つ目の値付けは素朴な代理指標です — `CompetitionWeight × LifeValue × 周囲の食料希少度`。食料が潤沢なら希少度が下がり、**先制攻撃の動機は自然に消えます**。

一方 `Risk`（過去に払わされた体力）は効用から**引かれる**側にあります。つまり過去にひどい目に遭った相手は「恨む対象」ではなく「避ける対象」です。

---

## 10. 繁殖

### 10-1. 求愛の条件（段階25で「できない」と「今はしない」に分けた）

**2種類あり、答えるルールが違います。**

**(1) 身体にできないこと＝拒否される。**

- **成人していること**（`Maturity >= ReproMaturity`）。子ども期は世界の時間として実在し、その代金は世代時間です
- **出産の代金を払っても生きていられること**（体力 > `BirthVitalityCost / 2`）。両親が折半するので、これがその個体の負担分です
- **出産直後のクールダウンが切れていること**

これらは判断ではないので、**候補にすら挙がりません**（人が操作していても選べません）。

**(2) 今は割に合わないこと＝効用式が決める。**

腹が減っていること、傷ついていることは**求愛しない理由**であって、求愛を不可能にするものではありません。**理由は効用比較の中にあるべきで、候補を消す閾値であってはいけません**（`CLAUDE.md`「ハードコードされた行動閾値を作らない」）。

そのために **`addCourt` は出産の代金を効用に載せます**——`BirthVitalityCost/2 × 求愛が通る確率`ぶんの体力と、**それを失うことで死ぬ確率がどれだけ上がるか**（`Life` の負の項）。**段階25 まで、求愛は「そこまで歩く代金」しか払っていませんでした**。抜けていた項の代わりに置かれていたのが、`Hunger < ReproHunger` という閾値です。

代金が入った今、**餓死しかけた個体は閾値なしで求愛を選びません**（テストで固定）。`CourtNeedsSurplus = true` で昔の閾値に戻せます（測定の対のために残してあります）。

**相手の側の条件**: 同種・異性・未ペア・最近断られていない・視界の中にいること。相手も (1) を満たしている必要があります。

### 10-2. 相手選びと、待つこと（段階26で「待てば誰でもよくなる」をやめた）

**求愛は双方が納得して初めて成立します。** それぞれが自分の物差しで相手の**魅力**（`fitness` ＝ 魅力遺伝子 ×0.65 ＋ 体調 ×0.35。理性に応じたノイズが乗る）を見て決めます。

**「待つ」が数えているのは、その個体が配偶者を探し始めてからの時間です**（求愛できるようになった時点から）。**求愛のたびに振り出しに戻るのではありません**——段階28 まではそうなっていて、**断られると患者が最初からになるので我慢が尽きることがなく、誰も誰にも妥協しませんでした**。直すと個体数 +16.79 \*\*、出生 +48.00 \*\*\*、世代 +1.75 \*\*\*。`CourtClockResets = true` で以前の挙動に戻せます。

**待つことは、要求を下げます。下げ切りはしません。**

- 比較中は `CommitFitness`（78）以上の「一目でわかる当たり」だけを受ける
- 我慢が尽きる（`PatienceBase` ＋ 理性 × `PatienceRationality` tick）と、要求は `CommitFloor`（38）まで下がる
- **`CommitFloor` より下は、いくら待っても受けません**

**段階26 まで、我慢が尽きると要求は下がるのではなく消えていました**（誰でも受ける）。**そこが、相手の体調が効かなくなる唯一の場所でした**——体調は魅力の3分の1を占めているのに、実際の受諾はほとんど「我慢が尽きたかどうか」で決まっていたためです（`CommitFitness` 78 は集団の上位1割より上なので、一目でわかる当たりはめったにいません）。

**床は体調についての新しい規則ではなく、すでにあった規則を、待っている個体にも届かせるためのものです。**

**近づいてくる相手が何をしに来たかは見えます**（`AgentView.AttackingMe` / `CourtingMe`。2026-09-06 に後者を足しました）。**同じ種類の事実**——**まっすぐこちらへ歩いてくることは目に見える振る舞い**であって、隠しパラメータでも、まだ下していない決定でもありません。**誰も狙っていない個体はどちらの印も付きません**（何も決めていないので見せるものが無い）。`Seeking`（配偶者を探している）は「誰を」を含まないので、この2つとは別物です。

**どちら側が断ったかは、両方が覚えています**（`Perception.Self.LastCourt`。2026-09-06）。**新しい知識ではありません**——世界はもともと「相手が受けたか」を求愛した側に教えており（それが `AcceptChance` の学習源）、自分の答えは自分のものだからです。**規則は1つも読みません**（効用式にも入りません）。入れた理由は見ている側で、**devview が「#12 に断られた」と表示していたのは嘘のことがあった**（断ったのが自分の側でも同じ文が出ていた）ためです。

**そして、両方が出産の代金を払えなければ成立しません**（10-1 の (1)）。**近づいた側も、着いた時点で改めて確認されます**——そこまで歩くのにも体力を使うためです。**払えない相手と結ばれても子は生まれず、その間の時間は両方が失います。**

**測定**: 床38 は**個体数を1つも払いません**（−1.71 ±5.06 ＝ 誤差内）。買えたのは `killShare` −0.05 \*\*\*、子の死亡率 −0.01 \*、寿命 +1.38 \*\*、集団の持続 +7.45 \*、そして**魅力遺伝子への初めての選択圧**（`shLooks` +0.01）。床50（中央値より上）は個体数を −9.25 \* 払います。

### 10-3. 出産と遺伝

ペア成立から `PairBondDuration` tick後に子が1人生まれます。その間、2人は一緒に移動します。**子は小さく生まれ、育児期間のあいだ親のそばに留まります**（11節）。

- 両親は `BirthVitalityCost` を折半して払う。払えるだけの体力が無ければ出産は起きない
- **走り終えたペアは必ず1人産みます**（2026-09-06 に直したバグ。それまでは「先に処理されたのが番号の小さいほうだったとき」しか産まず、**実測でおよそ半分のペアが何も残さずに解散していました**）。ペアが子を残さないのは、**どちらかが死んだか、出産の代金を払えないときだけ**です（実測96%が子を残す）。`BirthNeedsLowerIDFirst = true` で以前の世界に戻せます——**2026-09-06 より前の全測定はその世界のものです**
- 子の能力は **能力ごとにどちらかの親の値をそのまま引き継ぐ**。**どちらの親から引くかは能力ごとに独立**（父の力と母の理性、という組み合わせが起きる）
- **突然変異はまれで大きい。** 能力ごとに `MutationRate`（既定2%）の確率でだけ起き、起きたときは標準偏差 `MutationStd`（既定40）の正規乱数が乗る。範囲は1〜100にクリップ。**98%の遺伝子は親の値がそのまま残る**
- 子の性別はコイン投げ
- 子は `ChildVitality` / `ChildHunger` で、両親の中間地点付近に生まれる
- `ParentIDs` と `ChildIDs` に血統が記録される

**遺伝は粒子的です（どちらかの親の値を丸ごと引く）。** ただし連鎖群も、性に紐づく染色体も、対立遺伝子の優劣もありません。能力ごとにコインを1回投げるだけです。

以前は混合遺伝（両親の平均）でしたが、**平均は毎世代ごとに分散を半分にする**ので、数世代で「突然変異が今入れたぶん」しか多様性が残らず、選択の余地がほとんどありませんでした（2026-09-01。実測値は `HISTORY.md`）。

突然変異も同じ日に「毎回小さく」から「まれに大きく」へ変えました。**毎回少しずつ乗せると、どの親の値も子には残らない**ので、粒子的遺伝にした意味が消えてしまうためです。

出産後、両親は `MatingCooldown` の間、求愛できません。

---

## 11. 成長・老い・育児

### 11-1. 年齢補正は1本の曲線

**遺伝子の実効値 ＝ 遺伝的素質 × 年齢補正**です。掛け算をしているのは `Agent.Ability` の1か所だけで、`Gene()`（遺伝的素質そのもの）は年齢に触られません。**子に伝わるのも、実験が測るのも遺伝的素質のほう**です（選択は「継いだもの」にかかるのであって、持ち主の年齢にかかるのではない）。

曲線には両端があります。

- **若い端＝成長**: 生まれたときは `ChildAbilityShare`（既定0.6）しか出せず、`Maturity` が 0→1 に進むにつれて全部出せるようになる
- **老いた端＝衰え**: `SenescenceYears`（既定12年）を過ぎると年ごとに `SenescenceRate` ずつ落ち、`SenescenceFloor`（既定0.4）で止まる

**成長も衰えも、9本すべての遺伝子に同じように効きます。** 子は小さく（体力上限が低い）、遅く、弱く、覚えられる人数も少ない。死体から取れる肉も体の大きさぶんだけです。

### 11-2. 成長は食事で進む

**新しい行動はありません。** 食べれば進み、飢えていれば止まります。進む速さは満腹度に比例し、**空腹度0（満腹）で最大**です。`ChildhoodYears`（既定2年）ぶん十分に食べれば成人します。

ここに**2つの別々の閾値**があります。**成長は満腹（空腹度0）まで効き続ける**のに対し、**過度な食事による寿命の消費は `OverfedHunger`（空腹度20＝腹八分目）から始まります**。したがって最後のひと口は、子には得があり大人には損しかありません。

**ただしノードはこの差を知りません。** 成長も寿命も `Perception` に載らないので、**食べ方は子も大人も同じ**です。変わるのは結果（消耗死の多寡）だけで、行動ではありません。

**成人（`ReproMaturity`、既定0.9）に達するまで求愛は候補に挙がりません。** これが子ども期の最大のコストで、世代時間に直接乗ります。

### 11-3. 慢性的な低体力は寿命を削る

体力が `FrailVitalityShare`（既定20%）を下回った状態が `FrailGraceTicks` 続くと、寿命が毎tick減ります。**20%を超えた時点でカウントはリセット**されるので、これは「長い不調」のコストであって「悪かったtickの総計」ではありません。

**効用式に載る `ShockRisk`（今感じる危険）とは別物**です。こちらはノードが一切知らない静かな長期コストです。

### 11-4. 育児期間

出産後 `ChildRearingTicks`（既定1000＝2年）の間、子は親から `RearingRadius`（既定130＝視界と同じ）以上離れると戻ります。

- **給餌はありません。** 子は自分で食べます
- **親側に負担はありません。** 親は何も知らず、何も変えません。子の生存が親の効用に入ることもありません
- **半径は視界と同じでなければなりません。** 短くすると、子は食料を追うたびに引き戻されて**かえって死にます**（実測: 半径45で成人到達率0.46、紐なしで0.54、半径130で0.72）
- **実測された効き方は「保護」ではなく「凝集」でした。** 子の生存率は育児の有無で動かず、動いたのは死亡率と個体間距離です。家族が同じ場所に留まることで世界がばらけにくくなる、という形で効いています
- **親は何も与えません**（`ParentFeedShare = 0`）。`ParentFeedShare` を上げると**親の一口の一部が、そばにいる育児中の子に回ります**（新しい行動ではなく、`eat` の分配だけ。誰も「食べさせる」という選択をしません）。**分けるのは一口であって食料そのものではない**ので、誰が食料を所有し・競走し・奪い合うかは何も変わりません。**親は自分の食事が半分になったことを知ります**（`Perception` の栄養価に載る＝知らせないと効用式が一番敏感な量について嘘をつく）。**子は毒も分け前の割合で受けます。** 測定は2度しています——**出産のバグ（下記10-3）を直す前は「個体数を払って子の生存を買う取引」でしたが、直した世界では費用が消えました**: 0.5 で個体数 +5.08（誤差内）・`gen` +0.48 \*\*・`grewUp` +0.04 \*\*\*・子の死亡割合 −0.02 \*\*\*・`starveRate` −0.04 \*。**費用だけを取り出した対照（`ParentFeedWasted`）は今も −7.54 \*** なので、**親への課税は実在し、給餌がそれを上回って返している**。既定は0のままです
- **紐の終わりは「時計」であって「独り立ち」ではありません**（`RearingUntilGrown = false`）。成長は食事で買うので、この2つは同じ長さではありません。`RearingUntilGrown = true` にすると紐は成熟度が1になるまで続きますが、**世界は良くなりません**（2026-09-06 の測定。`HISTORY.md`）——伸びるのは中央値ではなく**裾**で（実測: 中央値1000→1180 に対し p90 は 1000→2000〜2800）、**長く縛られるのは飢えている子だけ**です。飢えている子ほど食べに行くのを妨げられます。**出産のバグを直した世界で測り直すと、代価はさらに大きくなります**（個体数 −10.62 \*、出生 −41.08 \*\*\*、`gen` −0.58 \*。買えるのは `clumping` +0.08 \* だけで、`grewUp` は誤差内）

---

## 12. 今の単純化（わかっている限界）

正しさのために、意図的に単純化してあるところを列挙します。

- **老いは「衰え」であって「寿命で死ぬこと」ではありません。** 年齢は能力の実効値を下げる（11節）だけで、年齢そのものが死因になることはありません。死因としての「消耗死」は寿命の残量が尽きたときで、それは年齢ではなく**暮らしぶり**（飢え・食べすぎ・長期の低体力）で減ります
- **血統は記録するだけで、判断に使われていない。** 自分の子か他人かをノードは区別しません
- **「仲間」という概念が無い。** 効用式で他個体について語る項は `rival`（消えると得）だけで、**他個体が生きていることが自分の得になる経路は1本もありません**
- **食料の争奪は純粋な距離の競走として値付けされている。** 「着いた先で殴られる」ぶんは食料の効用に入っていません（相手の強さは攻撃の効用側で別に見ます）
- **効用式の中の仮定が全ノード共通の固定値。** 「相手は7割の力で殴り返してくる」「求愛は6割で受け入れられる」といった仮定は、個体差も学習もありません
- **体格差が無い。** 体力の上限・速度・視界はすべてのノードで同じです。違うのは能力3つだけ
- **effort が2段階しかない。** 0.4 と 1.0 の間の選択肢はありません
- **観察できる他者の行動は「自分を攻撃しているか」「求愛中に見えるか」の2つだけ。** 「あの個体が今なにをしているか」は一般には見えません（協力を入れるならここに手が要ります）
