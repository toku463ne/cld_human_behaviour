# 外敵のルール

> **実装済みのルールは3節「確定したルール」にあります**（2026-09-01 に外敵が入り、2026-09-03 に共同狩猟の好意とサイズ分布で区切りました）。**2026-08-14 の時点では全部が未実装で、この文書はその頃の仕様案の置き場として始まっています。**
>
> ここに書くのは「これから何を作るか」の仕様案と、**まだ決まっていないこと**です。実装が入るたびに、確定した部分を「確定したルール」へ移し、未決の項目を消していきます。数値が決まったら `PARAMETERS.md` にも行を足します。**これから作る多様化（段階58〜64・2026-09-12 追加）は8節**にあります。

## 1. なぜ外敵を入れるのか

目的は**ノード同士が集まる理由を世界の側に作ること**です。最終的に目指しているのは、「同じ集団のメンバーを殺さないほうが有利」という判断が、ルールとしてではなく**効用比較の結果として**現れる状態です。

現状ではそれが起こりえません。効用式で他個体について語る項は `rival`（その相手が消えると自分が得）だけで、**他個体が生きていることが自分の得になる経路が1本もない**からです。外敵と、集団でしか取れない食料は、その経路を世界の側の事実として作るための道具立てです。

大事な点として、**「協力」という項を効用式に足すのが目的ではありません。** 世界の側で「隣人が生きていると自分の生存率が上がる」が事実になれば、既存の効用式がそれを勝手に計算します。新しい目的項を足すのは、それでも足りないと分かってからです。

## 2. 2種類の外敵と、それぞれが生むもの

想定している外敵は役割が異なる2種類です。**同じものの強弱ではなく、生む行動が違います。**

### (a) 捕食者 — 「利己的な群れ」を生む

ノードを襲う存在。ノードから見た最適応答は「他個体の近くにいて、かつ自分が捕食者に一番近くならないこと」で、これは**協力ではなく利己的な群れ**です。

- 集まって見えるが、中身は「他人を盾にする」
- 新しい効用項は要らない。既存の `life`（死ぬ確率）だけで表現できる
- **副産物として「隣人を殺す動機が下がる」**。盾は生きていないと役に立たない

実装が軽く、効果が `cmd/devview` で目に見えて、`clumping` 指標で測れます。**最初に試すのはこれ**です。

### (b) 大型の獲物 — 共同作業を生む（が、フリーライドが立ちはだかる）

1人では倒せず、複数人で攻撃して初めて倒れる食料源。倒すと大きな栄養が得られます。

これは本命ですが、**分配ルールが結果のすべてを決めます**。

| 分配ルール | 予想される結果 |
| --- | --- |
| 近くにいる全員が食べられる | **崩壊する。** 殴らずに待って死体に走るのが最適手になり、誰も狩らない |
| 攻撃に参加した者だけ | 動く。engine が「誰が何ダメージ入れたか」を記録する必要がある |
| 貢献量に比例して分配 | 最も自然で、互恵性の土台にもなる。機構は一番重い |

**最初は「参加した者だけ」から始める**方針です。1番目を選ぶと「なぜか誰も狩らない」で終わります。

### (c) 植物 — 外敵ではないが、ここで一緒に扱う

その場に生えて再生する食料は、**集団を有利にしません**。有利になるのは縄張りと排除で、むしろ内部の争いを増やす方向です。

意味が出るとすれば「一群れが守れるパッチ」にして**群れ対群れ**の競争にしたときですが、それは「群れ」を明示的に定義する必要を生みます（第4節を参照）。**当面は実装しません。**

## 3. 確定したルール

**2026-09-01 に実装したもの**（`NODE.md` と `PARAMETERS.md` にも反映済み）。

- **外敵は「別の種類のもの」ではない。** 人間とまったく同じ `Agent`・同じ遺伝子・同じ `AIController` で動く。違うのは**予算の抽選範囲**（外敵のほうが大きい）と**食性**（外敵は肉のみ、人間は植物と他種族の肉）の2点だけ
- **4-2 の選択肢は「3（ノードと同じコントローラ）」に決まった**（`PLAN.md` 段階11 の方針。当初の「まずは1（動く危険地帯）」からの変更）
- **4-3 も決まった**: 種族は隠さない（見れば分かる）。強さは他個体と同じく推定のみ。`AgentView` に `Species` / `Prey` / `Meat` を載せる
- **分配は「参加した者だけ」**（2-b の表のとおり）。倒した直後に殴っていた者だけが一定時間その死体を食べられる。**ただし食べられない種族には権利を渡さない**（人間が人間を殺した死体を人間が独占しても、腐るだけで誰の得にもならないため）
- **大きさ＝予算。** 死体から出る肉の数は死んだ個体の予算に比例する。「1人では割に合わないが集団なら割に合う」を作る唯一の仕掛けで、**効用式には「協力」も「狩り」も出てこない**（倒せば肉になる相手、という事実が `stake` 項に入るだけ）
- **外敵は地図の外からも入ってくる**（`EnemySpawnTicks`）。繁殖もするが、この広さの世界では捕食者だけでは獲物を食い尽くして共倒れになるため

**2026-09-03 に足したもの**（段階11 の最後の1項目）。

- **共同で仕留めたことが記憶に残る（`AffinityHunt`）。** 死体の分配に共に与った者どうしが、互いに好意を得る。**誰が数えられるかは分配ルールそのまま**なので、新しい観測も新しい行動も要らない（4-1 が求めていた「一緒に獲物を倒した相手」が、ここで初めて記憶の中に現れる）
- **これは種族を問わない。** 外敵も同じルールで動くので、人間を仕留めた外敵どうしにも好意が入る（種族ごとの分岐は書いていない）
- **外敵のサイズ分布は既定（`EnemyBudgetMean` 520・`EnemyBudgetStd` 90）で確定した。** 700 に上げても 400 に下げても、標準偏差を倍にしても**参加人数は 1.22〜1.25 で動かない**（96シード）。大きい獲物は群れ狩りを呼ばず、人間の個体数を動かすだけだった（700 で −7.6、400 で +12.7）。**群れ狩りが現れない理由はサイズ分布ではない**

**2026-09-12 に足したもの**（段階58）。

- **湧く場所は地域の重みで引く**（`EnemySpread`。`pickFoodRegion` と同型の重みつき抽選で、**湧く総数は変えず位置だけを変える**＝段階15a の論法。**0 なら乱数を1つも引かない**）。**人間の湧きには触っていない**（人間は出産でしか増えないので、重みを掛ける先が無い）
- **既定は 0＝地図の作者が入れるもの。** そして**入れても「危険地帯」にはならない**というのが、この段階で分かったこと——**規則は発火した瞬間には完璧に効く**（`prowlArrive` +0.25 \*\*\*）のに、**外敵が実際に立っている場所にはその6%しか残らない**（`prowlGain` ±0.01）
- **消える理由は2つとも数えた**: **新しい外敵の 38〜41% は世界の中で生まれる**（親のいた場所から始まるので、地図の重みは最初から触れていない）／**出発点からの平均距離は 270 で、地域の幅は 200**（一生のうちに地域1つぶんより遠くへ行く）
- **偏らせると外敵はむしろ散る**（`enemyCrowd` −0.20 \*\*）——**既定の世界で外敵が固まって見えるのは人間を追っているから**で、**湧きを人間の集まる場所から引き剥がすと追跡の集中が薄まる**
- **したがって段階64（湧いた地域から離れるコスト）は「危険地帯」の付け足しではなく前提**であり、**8節の残り（59〜63）より先に通す**

**同日に足したもの**（段階59）。

- **外敵の種類は `Config.EnemyKinds` の1行**（`engine/enemykind.go`）。**`Species` は2値のまま**で、**1行が持つのは4つだけ**——名前・湧きに占める割合・予算の範囲・**`Homing`**（段階58 の地図の重みにどれだけ従うか。1で完全に従い、0でどこにでも）
- **空の表＝1種類＝段階58 までの外敵**で、**選ぶものが無いときは乱数を1つも引かない**
- **種類を増やすのは1行**。**どの規則も行数を数えず、空欄は世界の既定に落ち、割合は正規化される**。**意思決定エンジンには種類の分岐が1つも無い**——「この種類はどうするか」を訊きたくなったら、**答えは行の上の数字にする**（3行の世界が1行のコードも足さずに動くことをテストで固定）
- **種類は種ではない。** **求愛は種の中でするが種類を見ない**ので、**2つの種類は交配する**。**子の種類は予算と同じコイン**（#2＝どちらの親からか50%）で、**親が同じ種類なら乱数を引かない**
- **`Homing` を「種類ごとの地図」にはしなかった**——**危険な土地は1つ**で、2つの種類が別々の地図を持てば土地が2つになる
- **測定で分かったこと**: **表は効く**（重い行と軽い行の予算比 1.57）。**`Homing` は効かない**（段階58 の「湧きの偏りは一生のうちに散る」がそのまま）。**そして混ぜ方が世界の選択を変える**——**重いのが4分の1なら殺しとパワー**（`killRate` +0.20 \*\*、`dPower` +3.69 \*）、**半分なら狩りと凝集、ただし頭の良さを払う**（`hunts` +8.15 \*、`halfLife` +6.45 \*、`dIntelligence` −3.98 \*\*）。**`packSize` はどちらでも動かない**＝**大きい獲物は群れ狩りを呼ばない**（2026-09-03 の結論が保たれた）

**同日に足したもの**（段階64。**これで「危険地帯」が初めて成立した**）。

- **外敵は、世界に入ってきた地域から離れるほど高い代価を払う**（`EnemyHomeCost`。**1tickあたり、遠いほど高い**＝段階34 の `Hazard` と同じ形。**自分の地域の中は無料**）。**種類ごとに `Homely` で強弱をつけられる**（段階59 の行の上の数字）
- **引き戻しではない。** 育児の紐と同じ形は採らない——**外敵は人間と同じ意思決定エンジンで動く**ので、強制は閾値そのものであり、**境界の際で振る舞いが不自然に切れる**（段階13 が避けた形）。**十分に良い手があれば遠くでも選ぶ**（テストで固定）
- **帰り道は候補を1つ足すだけで、値打ちを持たない**——**全候補が「その手がどこへ連れて行くか」で課金される**ので、**帰り道は安いから勝つ**
- **`HomeRegion` は固定参照**（湧いた／生まれた地域。移住も更新もしない＝`GuardianID` と同じ立場）
- **測定で分かったこと**: **段階58 の湧きの偏りが初めて居場所に残った**（`prowlArrive` 0.11 のうち **6% → 45%**）。**そして危険地帯が噛む**（`prowlBite` +0.21 \*\*\*＝外敵の多く湧く地域ほど死因に占める殺しが多い）。**距離は用量に応じて縮む**（`enemyAway` 1.24 → 0.64、`enemyAtHome` 0.26 → 0.53）
- **効いているのはコストであって帰り道ではない**（帰り道が選ばれるのは決定の1%）
- **代価は外敵が獲物を見つけられなくなること**——`hunts` −56 \*\*\*、`rareShare` 0.15 → 0.10、餓死 +20 \*\*\*、`packSize` −0.38 \*\*\*。**外敵は肉しか食べないので、動かないことは食べないこと**。**人間の個体数は +8.94 \***・`killShare` −0.04 \*\*\* ＝**「危険地帯を作る」と「世界を穏やかにする」は同じ操作の両面**

**同日に足したもの**（段階60）。

- **何を消化できるかは種類の行**（`EnemyKind.CanEatPlants`）。**3節の「種族を分けるのは予算の抽選範囲と食性だけ」を、種族の中でも引けるようにしただけ**で、**触ったのは `canEat` の1か所**（そこを通る食事・運搬・贈与・売買・知覚は自動でついてくる）
- **どれだけ食べたいかは `PlantAppetite`**（0〜1）＝**`dietValue` の中の掛け算1つ**。**見積もりと一口の両方が通る唯一の倍率**なので、**身体が自分について嘘をつかれない**。**確率のコインフリップは書いていない**——**できないことは候補から消し、割に合わないことは効用比較に任せる**（段階25 の2階建て）
- **測定で分かったこと**: **外敵の数を決めているのは湧きの設定ではなく餌**だった。**植物を食べられるようにしただけで外敵は12.4 → 49.0（+36.67 \*\*\*）**——`MaxEnemies` も `EnemySpawnTicks` も触っていない。**人間は −6.17 \***、`hunts` +46.98 \*\*\*、`enemyKillShare` 0.59 → 0.80、`humansByEnemy` 0.76 → 0.81
- **発火機会はこの群で一番大きい**（`plantSeenByEnemy` 0.68＝**外敵の3分の2が視界に植物を持っている**。段階45 の石は 0.13 だった）
- **読み方の注意**: **`pop` は2種族の合計**なので、**片方だけを養う規則はそれでは測れない**（`pop` +34.54 \*\*\* のすべてが外敵だった）。**`humans` / `enemies` の列で読むこと**

**まだ実装していないもの**: 2-a の捕食者（人間だけを狙う存在）と 2-c の植物パッチ。今の外敵は「大きな獲物」（2-b）に近い。

## 4. 決まっていないこと

### 4-1. 「仲間」をどう定義するか（最重要）

**明示的なグループID（所属フィールド）は持たせない方針です。** 「殺さないほうが有利」を進化の結果として出したいのに、所属を engine が書き込んでしまうと創発ではなく前提になります。

代わりに使える材料は既に全部あります。

- **血縁** — `ParentIDs` / `ChildIDs`（現在は記録のみで判断に使われていない）
- **個体別の記憶** — `opinions` マップ（誰が自分にいくら払わせたか、強さの推定）
- **第三者の観戦** — 戦いを見ている個体も推定を更新する仕組みが既にある
- **空間構造** — 近傍だけが相互作用する世界に既になっている

「仲間」は定義せず、**「長く隣にいて、自分を傷つけたことがなく、一緒に獲物を倒した相手」**という既存データの合成として扱うのが今の想定です。ただしこれをどう効用に載せるかは未定。

### 4-2. 外敵は意思決定を持つか【決着済み・3を採用。上の3節を参照】

3つの案があります。

1. **持たない**（動く危険地帯）— 決められた動きをするだけ。実装が最も軽い。**まずはこれ**
2. **簡易な規則**（一番近い／一番弱いノードを追う）— 中間
3. **ノードと同じ `Controller` を使う** — engine が2種類のエージェントを扱うことになり、`Perception` / `Action` の語彙を共有するか分けるかの判断が要る

3が必要になるのは、外敵自身に進化させたいときだけです。当面その予定はありません。ただし**ゲーム化のときにプレイヤーが外敵を操作する可能性**を考えると、いずれ語彙を揃える判断が要るかもしれません。

### 4-3. ノードから外敵はどう見えるか【決着済み。上の3節を参照】

`Perception` に外敵を載せる必要がありますが、**何を見せて何を隠すかの原則が未定**です。ノードに対しては「能力＝隠す（推定のみ）、行動＝一部だけ見える」という線引きになっています。外敵についても同じ扱いにするか（強さは推定、位置は見える）を決める必要があります。

関連して、**協力には「あの個体が今なにをしているか」の観測が必須**です。現在 `AgentView` が漏らす他者の行動は `AttackingMe` と `Seeking` の2つだけで、「あの個体が獲物を攻撃している」は見えません。共同狩りを入れるならここに項目が増えます。

### 4-4. 数値

外敵の強さ・速さ・数・湧き方、獲物に必要な人数、栄養量、いずれも未定です。決めたら `PARAMETERS.md` に足します。

### 4-5. 外敵タイプのプリセットの本数と性格【決着・2026-09-12】

**測定用は2種類**にしました（**軽い・どこにでも**／**重い・危険な土地に居つく**）。**そして「今後は増やしたい」という要件のほうを設計の中心に置いてあります**——**増やすのは表に1行足すだけで、コードは1行も要りません**（3行の世界が動くことをテストで固定）。**指標も表の長さに依りません**（`kinds` / `kindMix` / `kindGap` / `kindHomed`）。**残っているのは「どんな性格を足すか」だけ**で、それは段階60（食料を狙う）・62（対抗スキル）・63（水中）が行に数字を足していきます。

## 5. 測定の計画

**外敵を入れる前に指標だけ先に作り、ベースラインを取りました**（`cmd/experiment`、2026-08-14）。

| 指標 | 意味 | 現在のベースライン |
| --- | --- | --- |
| `clumping` | 同じ個体数を一様にばらまいた場合と比べて、どれだけ集まっているか | 1.19 |
| `neighbours` | 平均的な個体の視界内にいる他個体の数 | 18.1 |
| `nearest` | 最も近い他個体までの平均距離 | 21.2 |
| `killShare` | 死因に占める「殺された」の割合 | 0.61 |

`clumping` が (a) 捕食者の効果、`killShare` が最終目標の効果を測る指標です。

### 実験設計上の注意

**「殺しが減った」を協力の証拠にしてはいけません。** 対照条件が必ず要ります。

| 条件 | 中身 |
| --- | --- |
| `baseline` | 今の世界 |
| `morefood` | 単独で取れる食料を増やす |
| （今後）`group-prey` | 同じだけのカロリーを、集団でないと取れない獲物として供給する |

`group-prey` と `morefood` の**差**が協力の効果です。

なお当初は「食料を増やせば殺しは減るはずなので混同しやすい」と考えていましたが、実測は違いました。食料供給を 0.12〜0.30 の範囲で振っても `killShare` は 0.59〜0.64 でほとんど動かず、**殺しの絶対数はむしろ食料が増えるほど増えます**（個体数が増えて遭遇が増えるため）。したがって `killShare` は食料供給と混同しにくい、使いやすい指標です。

## 6. 進める順序

各段階で `cmd/experiment` の数字が動くことを確認しながら進めます。

| 段階 | やること | 見る指標 | 状態 |
| --- | --- | --- | --- |
| A | 指標を追加してベースラインを取る | 全部 | **完了**（2026-08-14） |
| B | 捕食者を1種類（意思決定を持たない） | `clumping`、`nearest` | 未着手 |
| C | 大型の獲物、分配は参加者のみ | 狩りの成立数、参加人数 | 未着手 |
| D | 分配ルールのA/B（参加者のみ／全員／貢献比例） | 狩りの成立数、フリーライド率 | 未着手 |
| E | `rival` の対になる項（近くの仲間が将来の狩りの成功率を上げる価値）を効用式に入れる | `killShare`、`morefood` との差 | 未着手 |
| F | 血縁を効用に入れる（包括適応度） | 血縁間と非血縁間の殺し率の差 | 未着手 |

B と C は独立なので順序は入れ替えられます。

## 7. 予期しているリスク

- **世界が平和になって選択圧が消える。** 集団狩りの獲物は食料供給を増やす方向なので、`FoodSpawnRate` を同時に絞らないと誰も争わなくなり、能力に選択圧がかからなくなります（2026-08-12 の設計判断と同じ罠）
- **個体数が崩壊する。** 捕食者は死因を増やします。`extinct` 指標が0のままであることを毎回確認します
- **`Decide()` が重くなる。** 候補に外敵と獲物が加わるぶん、効用の採点が増えます。深さゲートを外した時点で `BenchmarkDecide` は 5.8µs → 8.0µs になっており、余裕は大きくありません
- **フリーライドで何も起きない。** 分配ルールを間違えると「実装したのに誰も狩らない」になります。段階Dはそれを検出するためにあります

## 8. これから作る多様化（段階58〜64・仕様案・2026-09-12 追加）

**まだ1行も実装していません。** 中身と順序と予想は `PLAN.md` の「マップの性格（段階56〜64）」、決定事項は #90〜#96。ここには**外敵の側から見た仕様案**だけを置き、確定したら3節へ移します。

**この群が答えようとしているのは「マップの各地点に住む外敵の性質を、マップの側が決められるようにする」**です。

**【2026-09-12 の追記】8-2（湧きを地域に紐付ける）は実装・測定して3節へ移しました。** **そして残り（8-1・8-3〜8-4）の土台が現状では成り立たないことも分かりました**——**外敵は湧いた地域に留まらない**ので、**8-5（行動範囲＝段階64）を先に通します**。

### 8-1. 種族は増やさない（#91）【実装済み・3節へ移した】

> **2026-09-12 に実装・測定しました。結論は3節にあります。** **以下は当時の仕様案で、実装との違いは2つ**——**1行が持つのは4つだけ**（食性・対抗スキル・水のフラグは、それを使う段階が来たときに足す）**と、種類は交配します**（求愛は種類を見ないので、そもそも分けようが無かった）。

**`Species` は2値（human / enemy）のまま**で、**タイプは名前付きプリセット表の1行**として表します（器は `engine/edit.go` の `Difficulties` と同じ形）。**3節が決めた「同じノード構造・同じ意思決定エンジンで、違うのは予算の抽選範囲と食性だけ」をそのまま拡張するだけ**で、意思決定エンジンにはタイプ別の分岐を1つも作りません。

| 1行が持つもの | 何のためか | 段階 |
| --- | --- | --- |
| 予算の平均と分散 | 今の `EnemyBudgetMean` / `EnemyBudgetStd` と同じ形。**強さの違い** | 59 |
| 地域重み | **どの地域に多く湧くか**（省略時は一様＝今の外敵） | 58・59 |
| `CanEatPlants` / `PlantAppetite` | **人間の食料を狙うか、どれだけ狙うか** | 60 |
| 対抗スキルのタグ | **このタイプに強くなれるスキルがあるか** | 62 |
| 水セルからのみ湧くか | **水中の脅威**（地域重みではなく地形で引く1行） | 63 |

**`Agent` に足すのはタグ1つだけ**（湧いたときに決まってあとは変わらない参照＝`GuardianID` と同じ立場）。**既定のテーブルは1行＝今の外敵そのもの**で、**そのときは今と同じ順序で同じ乱数を引きます**（指紋テスト無変更）。

**最初に用意したのは2種類**（**軽い・どこにでも**／**重い・危険な土地に居つく**）。**増やすのは1行です**（4-5）。

### 8-2. 湧く場所を地域に紐付ける（#90・段階58）【実装済み・3節へ移した】

> **2026-09-12 に実装・測定しました。結論は3節にあります**——**規則は正しく働き、世界が一生のうちに消します**（残るのは6%）。**以下は当時の仕様案です。**

**`region` にスカラーを1本足し、`pickFoodRegion` と同型の重みつき抽選で湧く地域を選びます**（新しい抽選の仕組みは作らない）。**人間の湧きには触りません**——人間は出産でしか増えないので、効くのは初期配置と `EnemySpawnTicks` の追加湧きだけです。

**湧きの総量は変えず、位置だけを変えます**（段階15a の食料と同じ論法）。したがって**世界全体の個体数は動かない見込み**で、**動いたらその理由のほうが結果**です。

**そしてこの段階で、地域単位で外敵密度を読む測定を作ります**——**58〜61 の効果は世界平均では埋もれる**ので、物差しが無ければ何も言えません。

### 8-3. 人間の食料を狙う（#92・段階60）【実装済み・3節へ移した】

> **2026-09-12 に実装・測定しました。結論は3節にあります**——**外敵の数を決めているのは餌でした**（4倍になった）。**以下は当時の仕様案です。**

**今の種族間の動機は「倒せば肉になる」だけ**（`AgentView.Prey` / `Meat`）で、**外敵の予算は人間より44%大きい**（520 対 360）ので、**人間から見ると避けるほうが得**に寄りがちです。**植物を巡る競合なら、この世界で一番強い攻撃動機（食料競合の先制排除）をそのまま種族間に広げられます**。

**形は段階25 の分離（身体にできないこと／今は割に合わないこと）そのまま**:

- **消化できるか**＝身体の硬い事実。`eatsPlants(Species)` を上の表の `CanEatPlants` に一般化する（**既定 false ＝今と同じ**）
- **どれだけ食べたいか**＝`PlantAppetite` による栄養の評価値の割引。**確率のコインフリップにしない**——**滅多に選ばれないことは効用比較の結果として出させる**（「ハードコードされた行動閾値を作らない」）

**警戒すること**: **植物の総量は不変のまま消費者を1つ増やす**ので、**段階39（肉の量）・段階42（食料が水へ移る）で2度出た「総量不変のプールに新しい負荷をかけると非線形に効く」が再演される**見込みです。既定値は小さくし、用量を掃引します。

### 8-4. 対抗スキルと水中の脅威（#94・#95・段階62・63）

- **対抗スキルは汎用の戦闘力バフではなく `SkillPoison` の形**（そのタイプから受けるダメージの割引＋そのタイプへの恐れの値段の割引）。**天井遺伝子は防御力を出発点**にし、**出生地の種は「そのタイプがその地域にどれだけ湧きやすいか」から読みます**（新しい地図の読み方を発明しない）
- **水中の脅威は、魚を生き物にすることではありません**（#78 の保留は保留のまま）。**テーブルに「水セルからのみ湧く行」を1つ足すだけ**で、**既存の食料魚（`fish.go`）には1行も触らず、横取りもしません**。**溺死と水域外敵の死因は排他のバケツに分けます**（段階34 の理由）

### 8-5. 行動範囲は壁ではなくコスト（#96・段階64）【実装済み・3節へ移した】

> **2026-09-12 に実装・測定しました。結論は3節にあります**——**この群で唯一、狙った量が狙ったとおりに動いた段階**で、**段階58 の湧きの偏りがここで初めて居場所に残りました**。**以下は当時の仕様案です。**

**「地域は壁ではない」という `region.go` の原則は破りません。** 育児の紐（`RearingRadius` の強制的な引き戻し）と同じ形は採りません——**外敵は人間と同じ `AIController` で動く完全な意思決定主体**なので、**強制は閾値そのものであり、地域境界の際で振る舞いを不自然に切ります**（段階13 の視界グリッドが避けた形）。

代わりに、**湧いた地域を `HomeRegion` として1つ記録し**（固定参照・移住も更新もしない）、**そこから離れるほど大きくなるコストを既存の `Hazard` 項と同じ場所に足します**（段階34 の「見込みtick数 × 確率 × `LifeValue`」と同じ形）。**短い深追いは効用が許せば起き、深追いしすぎると割に合わなくなって自然に引き返します。**

**コストが強すぎると逃げた獲物を追わなくなり、「危険地帯」が却って静かになります**——強さと追跡距離の関係は掃引して確かめます。

### 8-6. この節が5節・6節に対して意味すること

**6節の表の B（捕食者を1種類）は、59・60 で初めて作れるようになります**——「人間を狙う弱いタイプ」はテーブルの1行で、**意思決定を持たない特別な存在を作る必要はありません**（4-2 で採った「3（ノードと同じコントローラ）」のまま）。**ただし測るときの注意は5節のまま**です: **「殺しが減った／増えた」を規則の効果として読むには対照が要り**、**58〜61 についてはそれが地域単位の集計でなければ埋もれます**。
