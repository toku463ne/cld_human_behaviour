# 実装計画（段階6.5以降）

**この文書はチェックリストです。** 段階を上から順に進め、終わったら `[x]` を入れます。全部終わったら、この文書は空になるか次の計画に置き換わります。

- 「なぜそう決めたか」は `HISTORY.md` の設計判断ログ（2026-08-18・2026-08-19・2026-08-30 の項）
- 実装後のルールの姿は `NODE.md` / `ENEMY.md`、数値は `PARAMETERS.md`
- 崩してはいけない原則は `CLAUDE.md`

---

## あるべき状態（受け入れ基準・2026-08-19）

**これは仕様ではなく受け入れ基準です。** 一定tick（当面 50000tick を想定）を回したあとの世界が、次の状態になっていることを目指します。ここを目指す理由は、**将来プレイヤーが1ノードを操作して他の集団を制圧していくゲームにしたいから**で、制圧する対象＝集団が世界に自然に存在していないとゲームが成立しません。

| # | あるべき状態 | 測り方（段階6.5 で用意した。指標名は `cmd/experiment` の列） | 判定する世界（2026-09-12 追加） |
| --- | --- | --- | --- |
| A | 異なる種族がそれぞれ一定数存在する | 種族ごとの個体数が、窓（直近5000tick）の平均で帯の中に収まる。**振動は許容する**ので見るのは振れ幅ではなく**谷**（`rareShare` / `rareTrough`。`engine.CensusTracker`） | **平坦な校正世界**（段階6.5 のベースラインと同じ既定条件） |
| B | human は一定数いて、**集団がいくつか**存在する | クラスタ数とサイズ分布（`clusters` / `clusterSize` / `grouped`。`World.Clusters`、連結距離30）。`largestShare` が1に近いときは世界が1つの塊につながっただけなので他の3つを読まない | **平坦な校正世界**（段階6.5 のベースラインと同じ既定条件） |
| C | 集団の中では戦闘率が低い | `fightRatio`。**素直な「集団内戦闘率 ÷ 集団間戦闘率」は原理的に測れない**（追記16）ので、遭遇あたりの「仲間 ÷ よそ者」に置き換えてある。1を下回るほど良い | **平坦な校正世界**（段階6.5 のベースラインと同じ既定条件） |
| D | 集団は行動を共にする | membership half-life（同じ顔ぶれが一緒にいる時間）。たまたま近くにいるだけの塊と区別する（`halfLife` / `together`。`engine.MembershipTracker`、組の共在生存曲線） | **平坦な校正世界**（段階6.5 のベースラインと同じ既定条件） |
| E | 異なる集団のノード同士は、戦闘になるか近づかない | 距離側は `gap` / `gapP10` / `gapRel`（`World.ClusterGaps`）、戦闘側は C と同じ `fightRatio`。**`gap` は「どれだけ離れているか」、`gapRel` は「その数の集団としては離れているか」で、2026-09-05 の測定では両者が逆を向いた**（下の表のE） | **平坦な校正世界**（段階6.5 のベースラインと同じ既定条件） |
| **F** | **通貨が流通し、売買が適度に行われている**（2026-09-11 追加） | **流通**＝`coinsHeld` / (`coinsHeld` + `coinsLying`)（手にあるコインの割合。地面に転がっているだけの通貨は流通していない）。**売買の量**＝`sales` と `saleRate`（一生あたり）。**成立のしやすさ**＝`salesRefused` ÷ `sales`（**1 を超えるほど、足りないのは買い手ではなく売り手**＝段階51 が見つけた形）。**3つ目の目的になっていないこと**＝`starveRate` がコインの無い対照より上がらない（#73） | **`coins` の腕**（遊ぶ地図＋コイン60枚＋発信）。**既定の世界では全部ゼロになるだけなので測れない**（#73） |
| **G** | **探索はプレイヤーの仕事**（2026-09-12 追加） | **AIノードが良い地域を探して歩くことは測定対象にしない。** `richGain` / `shelterGain` / `drawShare` が測ってきたのは段階14・15 の実測どおり**選別**（今いる場所が良ければ留まる）であって探索ではなく、**探索の余地はマップの構造（世界の広さと視界の比・地域ごとの性格の持続）が生むもの**＝**マップ作者の仕事**。したがってここで見るのは指標ではなく、**そのマップに「まだ見ていない良い場所」が実在するか**（`cmd/devview` の目視と、地域ごとの `Food` / `Shelter` の分布） | **将来の複数マップ全般** |
| **H** | **マップごとの性格は測定対象ではなく設計対象**（2026-09-12 追加） | **地域ごとの食料・外敵の分布・強さ・`RegionBias` などのマップ知識について、平坦な校正世界で確認するのは「既定オフ／既定値ゼロで指紋テストに影響しないこと」だけ。** 実際の難易度・性格は個別に検証する（`cmd/devview` の目視と、必要なら地域単位の `CensusTracker`）。**A〜F の帯をマップごとに満たすことは求めない** | **個々のマップ** |

**「判定する世界」の列は 2026-09-12（段階56）に足しました。** **A〜E は段階6.5「計測の先行整備」で、外敵も地形も物も無い世界の土台を測るために作ったもの**で、**以後の全マップが満たすべき契約として設計したわけではありません**。ところが段階37 以降、**同じ規則が地図によって逆符号になる例が5つたまりました**——運搬（既定 +1.25 の誤差内／遊ぶ地図 −8.90 \*）、贈与（+9.35 \*／−11.27 \*\*）、魚（−16.7 \*\*\*／−5.3 の誤差内）、特産と石は地図の作者が入れるもの、そして調理は**食料に触る規則で両方の地図が同符号になった初めての例**としてわざわざ書いてある。**この列が言うのは「その行の数値がどの腕のものか」だけ**で、**基準そのものは1つも変えていません**（#88）。

**G と H も同じ日に足しました。** **G は「AIが良い地域を探すこと」を測定対象から外すもの**——段階14・15・29・35・36・55a が6度にわたって示したのは、**世界が居場所を動かすのは食料の分布だけ**で、**個体ができるのは選別であって探索ではない**ということでした。**H は「マップごとの難易度・性格を A〜F の帯で縛らない」こと**を書いたものです。**帯で縛れない代わりに縛るものが「マップ共通の土台」**（`CLAUDE.md`）で、**閾値を作らないという原則があるからこそ、極端なマップ設定も degrade にしかならず「構造的に生存不可能」という壊れ方をしません**。

**下の目標数値の表は A〜F だけです。** G と H は**数値目標を持ちません**——G はマップの構造についての設計方針、H は「測らないと決めたこと」そのものなので、**数字を入れると H が自分自身に反することになります**。

**F だけは条件が違います**（2026-09-11 追加）。A〜E は**既定の世界で社会の規則から創発するか**を問うもので、**世界を建てる側が何も置かなくても測れます**。**F は通貨が世界に置かれていなければ存在しません**（`Coins` は既定 0＝#73 の「コインは地図の作者が撒くもの」）ので、**測るのは `coins` の腕**（遊ぶ地図＋コイン60枚＋発信）であり、**既定の世界で F を測ろうとしてはいけません**（全部ゼロになるだけです）。**そしてもう1つ違うのは、F には「作れば満たせてしまう」逃げ道が無いこと**——#10 が集団IDを禁じたのと違い、**売買の回数を直接増やす規則は #73 が既に禁じています**（値打ちは「それが買う食事」だけで、独立した3つ目の目的にはしない）ので、**F を満たす道は「売り手が売る気になる世界にすること」しかありません**。

**A〜E の5つとも 2026-09-01 に揃いました。** 「集団が無い世界」のベースライン（既定条件・24シード × 50000tick）は `HISTORY.md` の同日の項にあります。

**目標数値（2026-09-01・段階11 の実測から記入）**

| # | 実測（段階11 時点） | 目標 |
| --- | --- | --- |
| A | 種族2つが全24シードで存続。外敵は個体数の 8%、谷は平均の 0.37 | **達成。** 帯は「希少なほうの種族が個体数の5〜20%、谷が平均の0.3以上」とする |
| B | クラスタ11.1個・平均3.1体・所属率0.56 | 未達（塊はあるが集団ではない。下のD参照） |
| C | `fightRatio` 0.87 ± 0.11 | 未達。**目標は 0.6 以下**（仲間との争いが明確に少ない） |
| D | `halfLife` 30.6tick | 未達。**目標は 150tick 以上**（ペア結合の長さを超えて顔ぶれが持続する） |
| **F** | **2026-09-11 の実測**（`coins` の腕、48シード × 20000tick）: **売買 5.12 回／実行**、**断り 45.60 回＝売買の 8.9 倍**、`saleRate` 0.58（一生あたり）、**コインは60枚中 13.8 が手にある（0.23）**、保有者 0.27。**断りは段階51 の 6.17 から 45.60 へ、売買の1.9倍から8.9倍に増えている**——**間に入ったのは調理**（段階52）で、**治る食事を持っている身体はそれを手放さない**。**買い手は増え、売り手はいっそう渋くなった** | **未達。** 目標は4つとも同時に: **(1) 断り ÷ 売買 < 1**（売り手が足りている。**今は 8.9 で、ここが一番遠い**）／**(2) 手にあるコインの割合 0.5 以上**（今 0.23＝**過半が地面に転がっている**）／**(3) `saleRate` 1.0 以上**（**一生に一度は取引する**。今 0.58）／**(4) `starveRate` がコインの無い対照より上がらない**（＝通貨が食料の枠と手を奪っていない。#73）。**上限を「回数」ではなく「断りの少なさ」と「餓死しないこと」で書いたのは、売買の回数そのものを目標にすると #73 が禁じた「お金を3つ目の目的にする」で満たせてしまうから** |
| E | `gapRel` 0.98 → **0.83 ± 0.03（2026-09-05 の再判定）** | 未達。**目標は 1.2 以上**（集団どうしが偶然より離れている）。**目盛りは正しい**（無構造な配置 0.98・格子状に避け合った配置 1.92。`TestClusterGapsRelativeOfAnAvoidingLayout`）が、**世界の側がこの帯から出ない**——段階14・15 の地域差でも、唯一の「避ける」規則（`RestExposureWeight`）を4倍にしても 0.78〜0.87。**個体は確かに避けるようになる**（`nearest` +8.34 \*、`gap` +12.79 \*）が、**避けると集団の数も減る**ので密度で割った値は動かない。**`gap` と `gapRel` は別のことを言っている**ので、E は距離側を `gap` / `gapP10`（絶対）と `gapRel`（配置）に分けて読む |

**地理は B〜E の欄に書きません**（2026-09-08・段階37）。川で B〜E を満たしたなら、それは「地理が集団を作った」のであって「社会が集団を作った」ではないので、**達成した場合でも社会の欄とは別に書く**と先に決めてありました。**実際には川では何も満たしませんでした**（`gapRel` 0.76〜0.86・`halfLife` 33〜38。視界より広い川でも同じ）ので、表は段階12 の値のままです。

**B〜E の目標値は「段階12 のあとに判定する」ものとして置きました。** 決定事項#17 は段階11 のあとに数値を入れる想定でしたが、**好意（段階9）と伝承（段階12）が未実装のままでは、共同で狩ったことが記憶に残らず、集団化の経路が世界に存在しません**（段階11 の実測でも群れ狩りは現れませんでした）。

**目標の数値はまだ書きません。** 今の世界のクラスタ数も戦闘率比も測ったことがないので、根拠のない数字になります。測定インフラは予定どおり段階6.5 で先に作りますが、**表に数値を書き込むのは段階11（外敵）を実装したあと**です（決定事項#17・2026-08-30）。

**段階11 より前は、集団が形成されなくてよい。** 今の世界（human だけ）には他個体が生きていることが自分の得になる経路が1本もなく（`NODE.md` 11節）、原理的に集団化しません。実際の人類の社会性も外部の脅威に強く駆動されたと考えられており、この単純化には裏付けがあります。したがって段階6.5 の時点で B〜E に目標値を入れるのは早すぎます。段階6.5 では**現状の値を「集団が無い世界のベースライン」として記録するだけ**にし、目標値は外敵が入って共同戦闘という正の項が生まれたあとの世界を見てから決めます。

**達成の手段は「機構を直接書くこと」ではありません。** 集団IDや所属行動を作れば B〜E は明日にでも満たせますが、それは決定事項#10 と同じ理由で意味がない（世界がそうなったのではなく、我々がそう書いただけ）。B〜E は**好意（正の記憶）と伝承**から創発させ、達成できなかった場合にだけ明示的な機構を再検討します — #10 と同じ条件つきの構えです。

**プレイヤーに集団を見せるための「集団の識別」は view 層の仕事です。** クラスタ検出は読み取り専用の解析なので、engine に集団IDを持たせる理由にはなりません。

## 決定事項（2026-08-18 ／ 2026-08-19 ／ 2026-08-30 ／ 2026-08-31 ／ 2026-09-03 追加）

段階0〜6の完了後に出た要望を検討して決めたことです（予算の可変化・天才の2段階・理性/頭の良さを予算に入れる件は同日の追加決定）。**#12〜#16 は 2026-08-19 の追加**（魅力の遺伝子化・集団の創発・伝承・あるべき状態の位置づけ）、**#17〜#32 は 2026-08-30 の追加**（集団化の前提の見直し・好意の発生源・伝承の等価交換・ヒント・初期配分の引き方・魅力と実能力の相関・好意の記憶容量・血縁ブーストの範囲・**成長／老い／寿命の前倒し**・**休息の無防備さ**）、**#33〜#35 は 2026-08-31 の追加**（**慢性的な低体力による寿命コスト**・**数手先の見積もりの役割分担**・**能力による深さゲートの禁止**）で、各行に日付を入れてあります。**#14 と #16 は 2026-08-30 に内容を差し替え、#4 の分配方式も同日「遺伝子ごとの独立分配」で確定しました。** **#36〜#46 は 2026-09-03 の追加**（**環境と食料生態系**＝休憩場所・睡眠を作らないこと・地域という語彙の一本化・地域差の測り方・地域の豊かさの知覚と伝承・地域の記憶と記憶容量の関係・植物の遺伝・毒と警告信号・種子散布・栄養バランス・体内時計）で、これらは段階14〜18 として下に節を立ててあります。 **#47〜#50 は 2026-09-05 の追加**（**ゲーム化とマップエディタ**＝人間操作コントローラ・保存/読込の一本化・地形をエディタより先に測ること・パラメータUIの絞り込み）で、これらは段階19〜22 として下に節を立ててあります。 **#53〜#57 は 2026-09-06 の追加**（地形の信念・高台の安全性・目撃した殺害・協力の誘い・地形と食料の相関）で、段階29〜33 に対応します。**#58〜#64 は 2026-09-07 の追加**（**水辺の危険と伝承**＝溺死の形・場所の危険という2本目の信念・目撃者スキャンの共通化の時期・集落分化は測定であること・水辺の豊かさ、および**スキル**＝ヒント枠に乗せること・採取はイールドであること）で、これらは段階34〜38 として下に節を立ててあります。 **#65〜#76 は 2026-09-09 の追加**（**物を持つ世界と経済圏**＝所持品という5つ目の状態量・運搬の対価・運搬の的・肉の量と回復量・魚・特産食料と座標知識を作らないこと・遠隔攻撃・弾・通貨よりバーターを先に測ること・発信は見える事実であること・倉庫知識は地域信念であること・多tickの拘束）で、これらは段階39〜51 として下に節を立ててあります。 **#77 は 2026-09-09 の追加**（運搬中・販売中の腐敗）。 **#78 は 2026-09-10 の追加**（魚は本来「攻撃力の小さい種族」だったこと＝保留）。 **#79〜#84 は 2026-09-11 の追加**（**今しか見ていないこと**＝段階50・51 が独立に当たった壁の診断・調理と分業・調理の独占を時間で区切ること・性差は発現時の掛け算であること・感情は評価ノイズの構造であること・孤立は条件つきであること）で、これらは段階52〜55 として下に節を立ててあります。 **#85〜#87 も同日の追加**（性差の配分は役割から逆算すること・ガーディアンを母にしたら父の給餌経路を同じ回で開けること・孤立への対処は方角1つの帳簿から試すこと）で、段階53・55 の中身を決めたものです。 **#88〜#96 は 2026-09-12 の追加**（**マップの性格**＝あるべき状態を判定する世界で分けること・非可搬の地域補正・外敵の湧きを地域に紐付けること・`Species` を増やさないプリセット表・消化可否と食欲の2階建て・食料の再正規化のトグル・対抗スキル・水中の脅威・行動範囲の緩やかな制限）で、これらは**段階56〜64** として下に節を立ててあります。 **#97〜#99 も同日の追加**（**伝わる機会**＝父からの伝承には何もしないこと・交配のときの配偶者間の伝承・育児中の母の速度低下）で、**段階65〜66** として下に節を立ててあります。 **#100〜#112 は 2026-09-14 の追加**（**自給自足の世界で経済を成り立たせる**＝問題は配管ではなく自給自足の優位であること・作る前に数える2つ・手の枠の既定の測り直し・売り手の物差しの一本化・先読みが食料について知っていること・売買を前後差に一本化すること・値段を変数にすること・コインの天井の配分・飾り物の作り方とその位置づけ・購入価格のアンカーという一線・保留3つ）で、これらは**段階75〜83** として下に節を立ててあります。 **#113〜#119 は 2026-09-15 の追加**（欲しがり方が身体ごとに違うこと・気候の3つの決め・市場が動かない理由の診断・コインの請求先・防寒具の需要を繰り返させる候補・財の多様性・`addGive` の値打ちの出どころ）で、これらは**段階84〜89b** に対応します。 **#120〜#125 は 2026-09-16 の追加**（**贈与・速度・多様性**＝贈与の代償はすでに半分測ってあること・血縁という直交チャンネル・先読みが読むのは速度であること・欲求を予算の外の形質にすること・揺れの振幅を形質にすること・好意への欲求）で、これらは**段階91〜96** として下に節を立ててあります。

**#1〜#129 は `docs/history/decision/table-1.md` と `table-2.md` へ移した**（2026-09-21。**この文書が 150,000 文字の上限に達したため**——**表の 64,000 文字のほぼ全部が、もう作り終えた段階についての決定だった**）。**行は1文字も変えていない。** **番号で引くときはそちらを `grep -rn docs/history/decision/` する。**

**ここに残してあるのは、まだ作っていないものについての決定（#130〜#132）だけ。** **#133 以降は表に行が無く、下の各節の中に「決めたこと」として書いてある**（#133〜#135 は地図、#136〜#139 は押す・覚える・好意）。

| # | 決めたこと |
| --- | --- |
| 130 | **ゲームの方向性は「王朝もの」——複数拠点への定住（2026-09-19。ユーザの提起。`PLAN.md` の同名の節）。** **クリア条件は「指定した複数の拠点に、同時に自分の血統が定住していること」**、**ゲームオーバーは「すべての拠点で継承ラインが途絶えたとき」**。**拠点は新しい地物ではなく、豊かさグリッドで食料を集中させた区画**（P16 の 1・2 が前提）。**プレイヤーは生きている間は転移できず、死後10年相当を裏で進めてから、継承候補のいる拠点を選んで転移する**（**継承ラインは拠点ごとに独立＝各拠点の直系の最年長者**。転移した子孫は**既存の天才化**〈`Inspire`〉で生まれる）。**engine には「プレイヤー」も「ゲームオーバー」も置かない**（段階19 の線。ゲーム側に置き、engine が足すとしたら血統タグのような操作者と無関係な状態だけ）。**先に読むべき測定が4つある**——**防寒着は既に段階87a〜88 で「作られて85%が人手に渡り、その全部が贈与で売買は不動」**（値段を付けると交換そのものが消える）／**市場が開かない理由は `CoinValue` ではなく手が1つであること**（段階79・81。`CarrySlotsWeigh` を入れた世界で探す）／**豊かさと定住の相関は6割**（段階15a。`richGain` 0.05、社会的動機を切ると 0.03）／**寒さは居場所を動かさない**（段階86。**動かせたのは段階100 の「一歩先を読む」だけ**なので、寒さも同じ形にできるかを先に考える） |

| 131 | **クライアントはブラウザで動かし、`engine` を直接 import する。サーバは当面作らない（2026-09-19。ユーザの提起。`PLAN.md` の同名の節）。** **2026-08-11 に決めた「サーバ・クライアント型のネイティブアプリ」＋ `protocol` / `server` / `client` の3点セットは、ここで形が変わった**——**ebiten を WASM 出力してブラウザ（Android Chrome / Safari）で動かし、世界はプレイ中しか進まず、開発環境で育てたスナップショットを配って続きから始める**（背景での時間経過は無し）。**後から足すのは「セーブのアップロード」程度の薄い API だけで、tick ごとの配信ではない**。**この方針だと `cmd/devview` が踏み台ではなく本番の形になる**（2026-08-11 に「`client` が揃った時点で削除するか開発用に残すか決める」と書いたものの答え）。**そして段階21（保存/読込）がそのための土台として既にある**——**`World.Save` は `io.Writer` を取り、乱数は「種と引いた回数」で保存されるので、どの実行環境でも同じ続きになる**。**描画は円からタイルへ**（1ノード1〜3タイル・大型外敵5タイル・透過・歩行/食事/戦闘のアニメーション・個体差は `ColorScale`・向きは左右反転・ハッシュ付きファイル名と manifest でキャッシュしプリロード）、**操作はタップで選択→2回目で既定行動（食べ物 `eat`／外敵 `attack`／地面 `move`）、長押しで行動メニュー**。**崩してはいけない線は3つ**——**`engine` は描画も通信も知らない**／**段階19 の「見せるのは `Perception` の中身だけ・狙えるのは視界の中だけ」**（タップも同じ線に乗る）／**決定性**（WASM でも同じでなければ、育てた世界の続きが別物になる） |

| 132 | **開発機と端末で「続き」が違うことは許容する（2026-09-19。ユーザの判断。決定性の要件）。** **実測で分かっていたのは2つ**——**同じ `.wasm` なら Chrome・Edge・node は1ビット同じ**（2万tick 後の digest が一致）**で、食い違うのは「ネイティブ実行 対 WASM 実行」の1本だけ**（`math.Exp` と `math.Pow` が amd64 と wasm で 1 ULP 違う）。**したがって問題は「プレイヤーどうしで世界が違う」ではなく「開発機で確かめた続きが、プレイヤーの端末では別の続きになる」だった**。**理由**: **この世界はプレイヤーが動かすので、そもそも続きはプレイヤー次第でいくらでも変わる**——**「同じ続き」は守る価値のある性質ではない**。**したがって engine は直さない**（`decay` と `survives()` の自前実装も、arm64 の FMA 融合を止める作業も、やらない）。**測定（`cmd/experiment`）は今までどおりネイティブ**で、**記録は無傷**。**必要になったときの逃げ道は記録してある**——**配るスナップショットを WASM 側で育てる／試す**（`node wasm_exec_node.js` で回る。engine は1行も変えない） |

---

## 未決（実装前に決める）

- [x] **`BudgetInheritSpread` = 30**（2026-09-02 決定）。**「幅が広いほどドリフトが速くなる」という上の見立ては外れました**——7.5 と 60 の8倍差でも `dBudget` は 43／52／54 と動きません。予算の上昇は拡散ではなく**選択**によるもので、幅を広げて実際に起きるのは親から遠い予算の子が死ぬこと（個体数 −15.2 \*）です
- [x] **価格は維持コスト（`BudgetUpkeep = 1`）で確定**（2026-09-02）。`noupkeep` との対で `dBudget` +30.0 \*\*\*・`deathRate` +0.84 \*\*\*・個体数 −12.1 \*。**段階7c 当時「効くが止まらない」だった価格が効くようになったのは、段階8・9 で餓死が本物の死因に戻ったから**
- [x] **出生あたりの確率で確定**（2026-09-02）。**「個体数によってずれる」は実測で3倍ありました**——同じ設定で 20000tick なら33年に一度、60000tick なら11年に一度（個体数が増えて出生が増えるため）
- [ ] **`TicksPerYear` の値**（**段階7d で本決めにします**。2026-08-30 の方針転換・#28。「老い実装待ちの暫定値」ではなくなりました）。**現実の秒数（365×24×3600）は根拠にしません** — このエンジンの tick は現実の秒と対応しておらず、秒換算だと天才イベントが実験中に一度も発火しません。既存の tick スケール（`SkirmishTicks = 30` / `PairBondDuration = 150` / `PlanHorizon = 700` / 実行長 20000〜60000tick）から逆算し、**数百程度（200〜1000、出発点は500）**から始めます
  - 満たすべき条件は3つ: (1) **100年に一度（大天才）が実験の実行長と同程度かやや大きい**、(2) **自立期間3年＋想定生涯が、標準実行長（20000〜60000tick）の中で複数世代分観測できる**、(3) **老いが実行時間内に一度も発火しない／老化死が支配的で他の死因が見えなくなる、のどちらにも転ばない**。(2)(3) は段階7d の測定で確認します
  - 段階7c（天才の発生間隔）と段階7d（寿命）は**同じ1つの `TicksPerYear`** を使うので、7c では出発点の 500 を置き、**7d で確定したら 7c の測定をやり直します**
- [x] **`GeneInitAlpha` = 0.8**（2026-09-02 決定）。**α は「初期の」多様性ではなく、その実行を通しての多様性でした**（変異がまれで継承が粒子的なので founders の配分が遺伝子プールそのものになる）。2.0 では末尾の能力の分散が 30.0 → 23.8（−6.3 \*\*\*）、`killShare` +0.07 \*\*\*、知能への選択圧 −9.6 \*\*
- [ ] **記憶の「単位時間に記憶できる量」と「忘却曲線」の扱い。** 現在の案は、容量だけが予算内、帯域は容量から導出（遺伝子を増やさない）、忘却曲線は予算外の別遺伝子（量ではなく形なので）
- [x] 予算まわりの数値は出発点のまま据え置き。**天才2段階の発生率（0.017 / 0.0017）の確定だけは段階7d に移送**します（`TicksPerYear` が動くと「10年に一度」の意味が変わるため）。正しく集計すると現状は 11.1年／144年 で狙いどおり
- [x] **外敵のサイズ分布は既定（520 ± 90）で確定**（2026-09-03）。`EnemyBudgetMean` を 400／700 に振っても `EnemyBudgetStd` を倍にしても、**参加人数は 1.22〜1.25 で動きません**（96シード）。動くのは人間の個体数だけ（+12.7 \*\*\* / −7.6 \*\*\* / −3.0 \*）。**「1食分の価値が小さいから群れ狩りが現れない」は主因ではなかった**ということです
- [x] **`FitnessConditionWeight` = 0.35 で確定**（2026-09-02。`world.go` の package const から `Config` へ移した）。0 は個体数 −18.2 \*\*、0.7 は個体数 +15.8 \* ですが**採りません**——0.7 では配偶者選びが「今の健康状態」だけになり、遺伝する装飾という独立の選択軸（決定事項#25）が消えるため。**個体数で約2割を払って軸を生かす、という設計上の選択**です
- [ ] **血縁の初期好意ブーストの大きさ**（**範囲は決定事項#27 で「直系の親子のみ・世代減衰なし」と確定**。残るのは値だけ）。初期値を仮置きし、`cmd/experiment` で調整します。大きすぎると家族だけで集団が閉じ、小さすぎると好意の種が育つ前に減衰します
- [ ] **育児期間の長さと、親側の負担。** 子は自由に決定できるのか、親の移動・体力にコストが乗るのか。餓死率が直接動きます
- [ ] **等価交換の歩み寄り率**（1回の交換で差の何割を詰めるか）と、**動いた量を好意に換算する係数**。`observe` 1回で何項目まで交換できるかも決める
- [ ] **ヒント枠の上限本数（案: 3〜5本）と、枠1つあたりの予算消費量。** 未定。実装時にまず粗い値を置き、`cmd/experiment` で調整します
- [ ] **ヒントの状況特徴量として何を許すか。** `Perception` から取れるものに限る、という制約は決まっていますが、具体的な語彙（相手の推定の強さ・自分の空腹度・相手の種族…）をどこまで並べるかは実装時に決めます
- [ ] **深い伝承に相互占有（お互いを一定期間占有し合う状態）を追加するか。** 被参照数の分布を実測してから判断します（決定事項#20）。先に入れない
- [ ] **接触による起点リセット（#22）の「接触」の定義。** 視界内での遭遇でよいか、`observe` による交換のチェックが走ったときだけか
- [ ] **あるべき状態の目標数値**（集団数・戦闘率比・持続時間・種族ごとの個体数の帯）。段階6.5 で現状を測り、**段階11 の後**に書きます（決定事項#17）
- [ ] **寿命消費の既定値**（`MaxLifespan` / `StarveLifespanRate` / `OverfedLifespanRate`）。2026-08-31 に実装した時点の値では標準実行長（20000〜60000tick）でほぼ発火しません（下の段階7d・寿命の項を参照）。年齢補正・成長・`TicksPerYear` が決まってから、`cmd/experiment` で改めて調整します
- [ ] **休憩場所の割引率の幅**（#36）。地域ブロックごとに `RestExposureWeight` へ掛ける係数の下限・上限をどこに置くか。**露出は `RegenRate`（0.09）の12倍の単位で入る**ので、割引を強くしすぎると「安全な地域では休息が常に最善」、弱すぎると何も変わらない。段階9 の測り直し（重み 0.10 で崖に入った件）と同じ注意が要る
- [ ] **地域ブロックの粗さ**（#38）。グリッドを何セル束ねて1地域にするか。**世界は 800×600・視界 130・個体数 111** なので、地形のパッチと同じく「大きく取りすぎると局所適応の前に地域が絶滅する」制約がかかる。出発点は 2〜3 × 2〜3 ブロック
- [ ] **地域ごとの `FoodSpawnRate` の差の付け方**（段階15）。倍率の幅と、地域間で総量を保存するか（豊かな地域を作るぶん乏しい地域を削るか、世界全体の食料を増やすか）。**総量を変えると個体数が動いて条件が交絡する**ので、まずは総量保存で入れる
- [ ] **地域の豊かさのベクトルの持ち方**（#40・#41）。地域IDごとに1つのスカラー（推定の豊かさ）でよいか、分散も持つか（強さ推定と同じベイズ更新にするか）。**記憶容量遺伝子が精度・更新速度にどう効くか**の式もここで決める
- [ ] **毒のコストの置き場所**（#43）。体力への直接損害にするか、その場の栄養価の低下にするか。**前者は死因が1つ増える**（`cmd/experiment` の死因の内訳に列が増える）ので、増やすかどうかを先に決める
- [ ] **摂食散布の確率と遅延**（#44）。食べたとき種が生き残る確率と、何tick後にどこへ生えるか（食べた個体の現在位置か、移動の軌跡上か）。**人間の採食効率に直接効く**ので、他の変更と混ぜずに単独で測る
- [ ] **栄養バランスの割引の強さと履歴の長さ**（#45）。直近何回ぶんの消費を見るか、割引の下限をどこに置くか。**今の世界では効果がほぼ出ないと予想している**（下の段階16 を参照）ので、値を詰めるのは食料源が複数になってから
- [ ] **時間帯の周期と、時間帯によって何が変わるか**（#46）。段階18 に着手するときに詳細化する。先に決めない
- [ ] **人間の入力粒度と、トリガー方式との噛み合わせ**（段階19・#47）。engine は `shouldDecide` が立ったときにしか `Decide` を呼ばないが、人間の入力は非同期に来る。「次に訊かれたら答える内容」を置く形にするか、入力のたびに再判断を要求するか。**人間のためのハードコード分岐は作らない**（既存のトリガーの語彙で表す）
- [ ] **子への転移の規則**（段階19）。後継の指定はいつでもできるのか、成人（`ReproMaturity`）した子が複数いるとき何を基準に選ばせるか、後継が未指定のまま操作ノードが死んだらどうなるか（ゲームオーバーか、最も近い子孫へ自動で移るか）
- [ ] **高台の多層構造をどう表現するか**（段階20）。スロープを通ってしか上れない・高台の上にさらに高台を作れる、を、今の `(x, y)` 平面に**高さの軸**を足して表すのか、地域のような**別レイヤー**にするのか。**着手時に決める**（先に決めない）
- [ ] **`Perception` に地形を載せるか**（段階20）。今のコントローラは地形が入っても平坦だと思って計画する設計。人間操作ノード（段階19）が地形を意味のある形で扱うには、少なくとも人間側の知覚には要る可能性がある。**AI の閉じた式をそのまま使うか、AI と人間で知覚の中身を分けるか**を地形本体の着手時に決める。**分けるなら #47（人間にも `Perception` の外は見せない）との整合を先に取る**
- [ ] **保存形式と、乱数の状態を保存するか**（段階21・#48）。`World` が持つ `*rand.Rand` 1本が決定性の根拠なので、保存しないなら「読み込んだ世界は決定的に再現しない」ことを明示する。形式（gob / json）もここで決める
- [ ] **「育ったパラメータ」としてどこまで持ち込むか**（段階21）。予算・遺伝子・好み・言い伝え・ヒントは持ち込めるが、**記憶は相手のIDを含むので世界をまたげない**。血統リンクも同様。どこで切るかを決める
- [ ] **UI に出すパラメータの範囲と、警告の出し方**（段階22・#50）。安全域をどこに書くか（`PARAMETERS.md` の値をコードに持つか、文書のままにするか）
- [ ] **調理の回復倍率と、調理にかかる時間**（段階52・#80）。**倍率は生食との差が測定に出る大きさで、かつ「調理しない身体が餓死する」崖にならない幅**（段階9 の `RestExposureWeight` と同じ注意）。**時間は `CallTicks`（30）と `SkirmishTicks`（30）が既存の目盛り**で、長くすると拘束そのものの機会費用（#76）と混ざるので**短い側から始める**
- [ ] **`CookClaimTicks` の長さ**（段階52・#81）。**肉の `MeatClaimTicks`（400tick）が既存の目盛り**。**0（フリーライド）と長すぎ（手渡しが減る）の両端が悪く、中間が良いはず**（段階41 と同じ形）なので、**両端を対照として必ず回す**
- [ ] **`SkillCook` の適性遺伝子（天井）と、出生地の種の引き方**（段階52b・#63）。**天井は「その行いが元から属している遺伝子」という2度の選び方に従う**。**出生地の項は、持たせない選択肢もある**——持たせなければ差は純粋に遺伝子予算の競合から出るので、**「スキルは居場所を動かさない」という5度の測定を初めて回避した設計**になる
- [x] **感情バイアスの大きさと、余韻の減衰率**（段階54・#83）。**決着（2026-09-11）: 重み 0.5、半減期 150、利得は恐れ 30 / 満ち足り 3。** **帯は細くないが下限がある**——**好みを0.5% しか動かさない用量では符号を逆にしても結果が同じ**で、**数%動かして初めて構造が分かれる**。**利得を2つに分けたのは実測から**（満ち足りが恐れの6倍）。以下は当時のメモ: **`ChoiceNoise` = 20 が目盛り**で、**小さすぎると埋もれ、大きくすると 2026-08-14 が却下した「選択の上書き」に漸近する**。**この帯が細ければ、それがこの世界に感情が要らないことの答え**（段階51 の割引率と同じ形）。**減衰率は `Opinion` の既存の率を流用するか、独自に持つかも決める**
- [ ] **性差をどの遺伝子に、どちらの性へ、どの程度振るか**（段階53・#82）。**この決定が無いまま実装に入らない**（`TODO.md` の P9 で 52・54 より後ろに置いてある唯一の理由）。**魅力遺伝子に掛けるなら「見え方の性差」、実能力に掛けるなら「強さの性差」**で意味が違うので、**どちらの意味かを先に決める**（段階26 の「`fitness` に実能力が入らない」という線に触れるため）
- [ ] **孤立ペナルティを `ShockRisk` の乗数にするか、独立した項にするか**（段階55・#84）。**着手すると決まってから決める**

---

## 終わった段階の設計は `docs/history/plan/` にある（2026-09-21 に切り出した）

**段階6.5〜100 の設計・実装前の数え上げ・予想・完了条件は、この文書から `docs/history/plan/1.md`〜`9.md` へ移した。** **理由は2つ**——**この文書は「これから作るもの」の置き場で、終わった段階はもう「これから」ではないこと**（`CLAUDE.md` の文書の分担）、**そして 309,000 文字に育って上限の2倍を超えていたこと**。**移すときに文章は1文字も変えていない**（`docs/features/` の分割と同じやり方。**9つの本文をつないだものが、この文書から消えた範囲と1文字も違わないことを機械で確かめてある**）。

| ファイル | 中身 | 入っている段階 |
|---|---|---|
| [plan/1.md](docs/history/plan/1.md) | 計測の先行整備、予算制と成長、記憶と好意、伝承とヒント、環境と食料生態系、地形の下ごしらえ | 6.5・7a〜7d・8〜18 |
| [plan/2.md](docs/history/plan/2.md) | ゲーム化とマップエディタ、地形本体、選択を要求されるゲーム、地形の続き、出来事の伝播、溺死 | 19〜34（29 の次に 33、その次に 30） |
| [plan/3.md](docs/history/plan/3.md) | 溺死の周知と水辺の豊かさ、スキル、物を持つ世界（肉・運搬・魚・特産・石・投擲）、バーター | 35〜48 |
| [plan/4.md](docs/history/plan/4.md) | 発信・倉庫・通貨、調理とスキル分業、報酬と恐れ、性差、孤立、あるべき状態を土台と性格に分ける | 49〜56（52d と、54 の次に 53） |
| [plan/5.md](docs/history/plan/5.md) | マップの性格（`RegionBias`・外敵の湧きと種類と対抗スキルと行動範囲・水中の外敵）、伝わる機会 | 57〜66（61 の次に 60） |
| [plan/6.md](docs/history/plan/6.md) | 効用式の先読みと、その先にある経済（本・コイン・「置く」・手の枠・2つ目の窓） | 67〜75（69 の次に 51a） |
| [plan/7.md](docs/history/plan/7.md) | 自給自足の世界で経済を成り立たせる（値段・コインの天井・飾り物・欲しがり方・気候の下ごしらえ） | 76〜85（80a・84） |
| [plan/8.md](docs/history/plan/8.md) | 寒さと寒さに答える品、飾り物は満たされる、贈与・速度・多様性 | 86〜95（86b・89b・83 を含む） |
| [plan/9.md](docs/history/plan/9.md) | 好意への欲求、行動の拘束時間、水の中の遅さと消耗、寒冷地の罰、材料と加工、一歩先の地面 | 96〜100 |

**この文書に残っているのは、あるべき状態・決定事項・未決と、まだ作っていないもの（アイデアと保留）だけ。** **測定値と理由は `docs/history/work/` と `docs/history/decision/`（索引は `HISTORY.md`）、段階ごとの結論は `docs/stages.md`、次にやることは `TODO.md`。**

---

## 地図を作者に開く（Tiled・豊かさグリッド・外敵の種別・巣。ユーザの提起）【アイデア・2026-09-19 に立てた。**設計は実装前の数え上げで決める**】

**ユーザからの4項目**（**2026-09-20 に 5 を足したので5項目**。ユーザの「植物の種類・外敵の種類を今後増やしていきたい」から）**。** **2026-09-19 に `TODO.md` からここへ移した**（`TODO.md` は作業の順とチェックリストだけを持つ）——**この節は「何をしたいか」と「既に何があるか」「何を壊してはいけないか」「着手前に何を数えるか」だけ**で、**形は数え上げが決めます**（この project の作法。段階98 は分布を見てカーブの形が変わり、段階100 は候補の重みを見て効かせる先が変わりました）。

**この群より段階100（一歩先の地面を読む）が先**（2026-09-19・ユーザの判断）。**理由は、段階100 の結果がこの群の 1 と 2 の値打ちを決めるから**——**段階35 は「信念と真値の相関が 0.98 まで上がっても行動は変わらない」と測っており**（粒度9倍・108地域でも）、**その理由がもし「地形について行き先を値付ける項が効用式に無いこと」なら、段階100 がそれを入れた瞬間に、細かい地図は初めて効く先を持つ**。**逆に段階100 でも動かなければ、段階35 の不発は粒度の問題ではないと確定し、1 と 2 は「見た目のための地図」になる**（それでも作る価値はあるが、測定で効くものとしては期待しない）。**注意して読むべき先行の測定が1つ**——**地域の信念のうち、段階29（移動コスト）は荒れ地に立つ割合を 0.48 → 0.42 と動かしている**のに、**段階35（危険）は `onWater` を動かせなかった**。**同じ「良い地域へ行く」候補に乗っているのに符号が割れている**ので、**段階100 を測るときはこの2つの差も一緒に説明できるかを見る**。

**この群の中の順序の提案**: **5 → 3 → 1 → 2 → 4**（**5 は 2026-09-20 に足した「名前の取り決め」で、実装が要らず、先に決めておかないと 2 と 3 のあとに地図を描き直すことになる**）。**3 は既存の表に列を足すだけで新しいデータ構造が要らず、単独で測れる**。**1 は 2 と 4 の運び手**（1 が入れば 2 も 4 も「地図に1レイヤー足す」になる）。**1 だけは世界の規則を1つも変えない基盤なので、測るものが無い**——**受け入れ条件は段階21 と同じ形**（読み込んだ世界が、同じ設定を手で書いた世界と**1ビットも違わない**）。

### 1. Tiled(.tmj/JSON)からワールドを生成する【完了・2026-09-19。**2026-09-20 に地域を塗る経路を足した**】

**実装済み**（`engine/tiled.go` / `engine/regionshape.go` / `cmd/devview -tiled`）。**数値と決めたことは `HISTORY.md` の 2026-09-19（3項目目）、ルールは `NODE.md` 11-4b、現在の姿は `docs/features.md`。** **数えて分かった一番大きいことは「守るべきものはここではなかった」**——**`regionIndexAt` は1tickに3〜10回しか呼ばれず**、**地域数に比例するのは `worthOfRegion` と1体あたりの記憶**。**そして「地域を増やすと世界が壊れる」は1シードの幻だった**（6シードで差が読めない）。

**2026-09-20 の追加（ユーザの問い「地域レイヤーが『オブジェクト層』である必要は？」から）**: **地域はタイル層でも描けます**（`Config.RegionMap` ＝1セル1文字、凡例は `RegionShapes` の `Key`。Tiled では `region` プロパティを持つタイルで塗った層）。**前日に挙げた「オブジェクト層である理由」3つのうち2つは間違いか弱い理由でした**——**「地域1つにタイル1枚要る」は Tiled では普通のこと**、**「何百もの地域になりかねない」は間違い**（**地域の数は塗ったタイルの種類の数**）、**「索引が要る」は反論になっていない**（**200×200 の表はすでにあり、タイル層はそれを直接埋める**）。**本当の違いは数字がどこに付くかだけ**（矩形は1つ1つが自分の数字を持てる／タイルは種類に付く）で、**代わりに塗りは矩形にできない形になれます**（L字・分かれた谷）。**両方残し、同じセルについて両方が言っていたら塗りが勝ちます。** **タイル層の役割も、順番ではなく中身で決めるようにしました**（地形だけが最初の層）。**理由と測定は `HISTORY.md` の 2026-09-20。**

**当初の計画（そのまま残す）**

- **やりたいこと**: 地形レイヤーをタイルの custom property（種別 flat/rough/water/slope/high と高さ）で表し、**地域（region）はオブジェクトレイヤーの矩形で作者が自由な形に**定義する（**今の固定グリッド前提から脱却**）
- **既にあるもの**: `Config.TerrainMap`（1文字1セル）・`ClimateMap`（段階85）・`engine/save.go` の JSON 入出力（段階21）・`engine/edit.go` の `SetTerrain` / `SetRegion`（段階22）。**`RegionCols` / `RegionRows` は既に `Config` にある**（既定 4×3）ので、**「格子のまま細かくする」だけなら今日できる**
- **壊してはいけないもの**: **外部依存を増やさない**（`.tmj` は JSON なので `encoding/json` で足りる）／**読み込みは乱数を1つも引かない**（`SetTerrain` と同じ立場＝段階22）／**決定性**（同じファイル＋同じ種なら同じ実行。段階21 の受け入れ条件）／**地形と地域は別の地図**（段階22 で確認した設計。片方を塗っても他方は動かない）
- **自由形状にすると何が変わるか（ここが設計の焦点）**: **`regionIndexAt` は今 O(1) の割り算2つ**で、**知覚が毎決定で全地域を読む**（`worthOfRegion`）。**自由形状にすると当たり判定と索引が要る**
- **着手前に数えること**: **`regionIndexAt` と `worthOfRegion` が1tickに何回呼ばれるか**（ホットパス。段階15 で `Step` が2.4倍になった前例がある）／**地域を増やしたときの `a.regions` の実メモリと伝承の交換コスト**（個体ごとに1地域1レコード。記憶の枠は食わない＝#41）／**そもそも細かくして意味があるか**——**段階35 が粒度9倍（108地域）を試して「信念の相関は 0.98 まで上がるのに行動は変わらない」と測っている**

### 2. 豊かさグリッドを細かくし、食料の種類ごとに分ける

- **やりたいこと**: **地域（信念用の粗い区画）と豊かさグリッド（成長確率に直接効く実データ）を分離**し、グリッドを Tiled のタイル／矩形で自由な粒度で塗る。**植物と魚で独立したグリッドと独立した取り分プール**を持たせる。**魚グリッドは水セルにしか意味を持たない**（陸への指定は無視）。**肉は狩り由来なので対象外**
- **既にあるもの**: `regions[i].Food`（段階15a の地域ごとの重み）＋ `pickFoodRegion`（重みつき抽選）＋ `FoodRenormalize` / `FoodTotalFromMap`（段階61）。**魚は `FishShare` の確率で、水セルから一様に引いている**（`spawnFish`）＝**魚には豊かさの概念が今まったく無い**ので、**2 の魚の側は純粋な新規**
- **壊してはいけないもの**: **総量保存**（段階15a。**各グリッドの中で保つ**というユーザの指定は原則と整合）／**魚は植物の代わりに湧く**（段階42。**独立プールにすると総量の関係が変わる**ので、**そこは意図的な変更として明記し、段階42・43・36 の数値を読み直す必要がある**）／**`FoodTotalFromMap` が「総量保存を意図的に緩める唯一の場所」**（段階61）という位置づけ
- **着手前に数えること**: **今の地図で地域の重みが実際にどれだけ散らばっているか**（段階61 で「重みは比としてしか使われない」と分かっている）／**細かくしたときに食料の分布が実際に変わるか**（`richGain`・`rareTrough` で見る）／**魚の豊かさに応答する余地があるか**

#### 実装前の予想（2026-09-20・**測定前に書いた**）

**数え方**: **粒度を変える腕はもう `cmd/experiment` にある**（`fineregions` ＝48区画、`manyregions` ＝108区画、既定は4×3＝12区画）。**新しいコードを1行も書かずに「細かくすると何が動くか」を先に測れる。** **段階35 が同じことを測っているが、その後ベースラインが2度リセットされている**（先読み・一歩先の地面）ので、**数値は引き直す。**

- **`richGain`（人間が立っている地面の豊かさ − 世界平均）は、細かくすると下がる。** **12 > 48 > 108 の順**で、**誤差より大きく**。**理由**: 108区画では1区画が 67×67 で **`PerceptionRadius` 130 より小さい**——**「豊かな区画に居る」ことが視界より細かい単位になり、区画単位の豊かさが身体にとって意味を失う。**
- **`pop` は動かない**（総量保存。分布が変わっても世界が育てる量は同じ）。**誤差内。**
- **`collapsed` も動かない。**
- **見た目**（`cmd/devview`）: **食料の散らばりが細かい斑になり、「食料の厚い一角」が消える。**

#### 実装前に数えた（2026-09-20・12シード × 20000tick・既定条件）

**まず区画の大きさを数えた**（`FoodSpread` 0.6、12シード）:

| 格子 | 区画数 | 1区画 | 視界半径130 に対して | 重みの max/min |
|---|---:|---|---:|---:|
| 4×3（既定） | 12 | 200×200 | **1.54倍** | 3.0 |
| 8×6 | 48 | 100×100 | 0.77倍 | 3.8 |
| 12×9 | 108 | 67×67 | **0.51倍** | 3.9 |

**重みの散らばりは粒度でほとんど変わらない**（同じ `U[0.4,1.6]` から引く回数が増えるだけ）。**変わるのは「1区画が視界より大きいか小さいか」**で、**既定の12区画だけが視界より大きい。**

**そして世界を回した結果**（`baseline` / `fineregions`＝48 / `manyregions`＝108）:

| | 12区画 | 48区画 | 108区画 | 対の差（48） | 対の差（108） |
|---|---:|---:|---:|---:|---:|
| `richGain` | **0.07** | 0.03 | 0.04 | **−0.04 \*\*** | **−0.04 \*\*** |
| `humanRich` | 1.12 | 1.04 | 1.02 | −0.08 \*\* | −0.11 \*\*\* |
| `pop` | 143.8 | 142.2 | 126.4 | −1.7 ± 18.9 | −17.4 ± 18.7 |
| `collapsed` | 0.00 | 0.00 | 0.00 | 0 | 0 |
| `rareTrough` | 0.68 | 0.66 | 0.66 | −0.02 ± 0.07 | −0.01 ± 0.05 |

**予想は向きも大きさも当たり、順序だけ外れた。** **`richGain` は半分になる**（0.07 → 0.03〜0.04）。**個体数も `collapsed` も動かない**（総量保存どおり）。**外れたのは「12 > 48 > 108 の順に下がる」のほう**——**48 と 108 で区別がつかない**（どちらも −0.04）。**つまり効果は連続に薄まるのではなく、区画が視界より小さくなった時点で失われ、それ以上細かくしても変わらない。**

**この項目の形が、数え上げで変わった。** **「豊かさグリッドを細かくする」は、測定上は逆向きの操作である。** **豊かさが身体の居場所を動かすには、区画が視界より大きくなければならない**——**細かくすると、どこに立っても複数の区画が視界に入り、身体が経験する豊かさが世界平均に近づく。** **したがってこの項目で作る値打ちがあるのは「細かさ」ではなく「作者が塗れること」**（任意の形・任意の大きさ・地域＝信念の区画とは独立に）。**既定の粒度は変えない。**

**（測定前に書いた予想は上。以下はその続きで、書いたまま残す。）**

#### 魚に独立した取り分プールを与える前の予想（2026-09-20・**測定前に書いた**）

**腕は2つ**——`fishshare`（**段階42 の世界。魚は植物の代わりに湧き、1つの上限を分け合う**）と `fishpool`（**水が自分の割合と自分の上限を持ち、陸の取り分を取らない**）。**魚の量はどちらも同じくらいになるように置いた**ので、**違うのは「陸から取ったか、足したか」だけ。**

- **`pop` は上がる。** **誤差より大きく。** **これは総量保存を意図的に緩めた結果であって、副作用ではない**——`fishpool` の世界は単純に食料が多い。
- **`starved` は下がる。**
- **`onWater`（水の中に立っている割合）は上がる。** 魚が増えれば水に居る理由が増える。
- **`drowned` は上がる**（水にいる時間が増えるぶん）。
- **`collapsed` は下がるか変わらない。**

**外れたら何がわかるか**: **`pop` が動かなければ、この世界の個体数を決めているのは食料の総量ではなく別のもの**（`MaxPopulation` の天井か、出会いの頻度）。**2026-09-20 に「200000tick では12シード全部が上限240に張り付く」と測っているので、20000tick でも天井が近ければ食料を足しても出ない。**

**そしてこの測定は、この項目そのものの値打ちを決める。** **`richGain` が動かないなら、「豊かさグリッドを細かくする」ことに測定上の値打ちは無い**——**段階35 の結論（粒度9倍でも行動は変わらない）がリセット後も成り立つことになる**。**その場合この項目に残るのは「植物の種類」と「魚の独立プール」だけで、細かいグリッドは見た目と地図の作者のための機能になる**（それでも作る価値はあるが、測定で効くものとしては期待しない）。——**段階42 が `onWater` を 0.13 → 0.24 と動かした唯一の規則**なので、**ここは効く見込みがある数少ない場所**

### 3. 外敵の種別（type）を広げる

- **やりたいこと**: 種別ごとに**移動能力フラグ（泳げる／飛べる／高台を登れる、を独立に）**・既存ステータス・**倒したときのドロップ**
- **既にあるもの（重要）**: **`EnemyKind` の表は段階59 で入っている**——`Name` / `Share` / `BudgetMean` / `BudgetStd` / `Homing`（段階58 の湧きの偏りへの従い方）/ `CanEatPlants` / `PlantAppetite`（段階60）/ **`Water`（段階63）** / `Ward`（段階62）/ `Homely`（段階64）。**「1行足すだけで増える」形も測定済み**（3行の世界が1行のコードも足さずに動くことをテストで固定）
- **したがって新しいのは2つだけ**: **移動能力フラグ**（`Water` は既に「溺れない・遅くならない・（段階99）消耗しない」を1つの事実として束ねている＝**新しいフラグも「その地形の何を無視するのか」を4つ〈コスト・危険・速度・目減り〉について明示する**）と、**ドロップ**（今は `MeatPerBudget × 予算` で全種別同じ）
- **壊してはいけないもの**: **渡れない地形は作らない**（段階20）——**飛行型が地形を完全に無視すると「地形がゲートになる」前提が崩れる**というユーザの指摘はこの原則と同じ側。**残す制約の候補は「コストは無視するが高くつく」「速度は上がるが体力の目減りが大きい」「水の上では狩れない」など**／**種類は種ではない**（段階59。求愛は種類を見ないので交配する）
- **「魚（水中限定の外敵）」について先に読むこと**: **`EnemyKind.Water` がすでにそれ**（段階63）で、**測定は「川に住んで狩るものを置いても人は川から出ない」**でした。**そして「魚を生き物にする」は 2026-09-13 に「作らない」で決着しています**（`PLAN.md` の保留。理由は段階42・43・34・36 の記録が全部引き直しになること）。**この項目で新しいのは種別の表に名前とドロップが付くことで、挙動は段階63 の再確認になる**——**そこを承知で入れるなら安い**
- **着手前に数えること**: **段階62 の上限の式**（保有率 × 実効値 × 的の大きさ）**をドロップに当てはめる**——**人間の食事に占める肉の割合**が的の大きさ／**飛行型が実際に地形をまたぐ頻度**（段階58 が「湧きの偏りは一生のうちに消える」と測っている）

#### 実装前に数えた（2026-09-20・12シード × 20000tick、一部は 200000tick）

**2つ数えて、2つの結論が逆になった。**

**(1) ドロップには的がある。** **`meatShare`（食事のうち肉だったもの）は 0.16 ± 0.01**（平坦・20000tick）、**0.19**（plateau）、**0.15〜0.16**（river）、**そして 0.30 ± 0.01**（平坦・200000tick）。**`meatEatenShare`（落ちた肉のうち食べられたもの）は 0.67〜0.80。** **つまり肉は食事の1.6〜3割を占めていて、しかも余っていない**——**増やせば食べられる**。**段階62 の式に当てはめて、ドロップを種類ごとに変える余地は実際にある。**

**(2) 移動能力フラグの的は小さい。** **地形は今、居場所を面積比までしか動かしていない**: **plateau の地図で `onHigh` 0.33 ± 0.02**（高台は面積の約28%）、**river で `onDear` 0.13 / `onWater` 0.13**（川は面積の約12.5%）。**身体は高いところにも水の上にも面積なりに居る**——**plateau は「2本のランプでしか登れない高台」なのに、避けられていない。** **だから「またげること」の値打ちは、地形が誰かを閉じ込めている地図でしか出ない。**

**この2つから、この項目の順序が決まった**——**ドロップは測れる的があるので先に作り、移動能力フラグは「そういう地図を描けるようにするため」に作る**（今ある地図では測っても何も出ない見込み、と承知のうえで）。

### 4. 外敵の巣（湧き地点）と徘徊

- **やりたいこと**: Tiled のオブジェクトレイヤーの**点**で巣を配置（`enemy_type` と `roam_radius`）。**外敵は巣を中心に徘徊し、範囲を超えたら巣へ戻る**
- **既にあるもの**: **段階64 がほぼこれ**——`Agent.HomeRegion`（湧いた／生まれた地域を固定参照）＋ `EnemyHomeCost`（**離れるほど高い代価。`Utility.Roam` として別の列**）＋ `EnemyKind.Homely`。**「引き戻し」ではなく「代価」にしたのは意図的**で、**強制は閾値そのものになり境界の際で振る舞いが不自然に切れるから**（段階13 が避けた形）。**帰り道は候補を1つ足すだけで値打ちを持たず、安いから勝つ**
- **したがって新しいのは「地域」から「点＋半径」への差し替え**と、**地物として地図に置けること**（倉庫＝段階50 と同じ「世界を敷く側が置くもの」の系列＝ユーザの指摘どおり）
- **壊してはいけないもの**: **閾値を作らない**（`roam_radius` を「超えたら強制的に戻す」にすると、この project が全段階で守ってきた線を初めて破る。**段階64 と同じく「半径を超えるほど高い代価」にすれば、十分に良い手があれば遠くでも選ぶ**——テストで固定済みの性質）／**湧きの総数は変えない**（段階58 の論法）
#### 実装前に数えた（2026-09-20・12シード × 20000tick）

**湧きの偏りは、居場所に1つも残っていない。**

| | `prowl` | `homebound` |
|---|---:|---:|
| `prowlArrive`（湧きの偏り） | 0.12 | 0.10 |
| **`prowlGain`（居場所に残る偏り）** | **0.00 ± 0.02** | **0.01 ± 0.01** |
| `prowlKept`（残った割合） | 0.12 ± 0.20 | 0.22 ± 0.21 |
| `enemyBorn`（世界の中で生まれた割合） | **0.38** | 0.20 |

**段階64 が記録した `prowlGain` 6% → 45% は、2度のベースラインのリセットを挟んだ今の世界では再現しない**——**今は 0.00**。**`prowlKept` も誤差が大きすぎて0と区別がつかない。**

**原因は家の決め方にある。** **`HomeRegion` は `addAgent` で位置から決まる**ので、**外から来た個体はそこ、世界の中で生まれた子は生まれた場所**が家になる。**そして新しい外敵の 38% は世界の中で生まれる**（`prowl` の測定値。段階58 の 38〜41% と一致）。**つまり偏りは1世代で薄まり、数世代で消える。** **巣を地図に置いても、置いた意味が残らない。**

#### 予想（**測定前に書いた**。2026-09-20）

**変えるのは2つ**——**家を「地域」から「点＋半径」にすること**と、**子が親の巣を継ぐこと**。

- **`prowlGain` は 0.00 から誤差より大きく上がる。** **偏りが1世代で消えなくなるから。**
- **`prowlKept` も上がる**（同じことを別の目盛りで見ている）。
- **`pop` は動かない。** **外敵の数も湧きの総数も変えない。**
- **`enemyBorn` は動かない。** **生まれる数は変えず、生まれた子の家の決め方だけを変える。**
- **見た目**（`cmd/devview`）: **外敵が地図の巣のまわりに固まって見えるようになる**（今は世界じゅうに散っている）。

**外れたら何がわかるか**: **`prowlGain` が動かなければ、偏りを消しているのは出生ではなく徘徊そのもの**——**段階58 が測った「出発点から平均270 離れる（地域の幅は200）」のほうが効いている**ことになり、**巣を置くだけでは足りず、離れる代価（`EnemyHomeCost`）を既定にするかどうかの話になる。**

#### 測定でこうなった（24シード × 20000tick・2026-09-20。**予想は外れた**）

| | `nestsbirth`（生まれた場所） | `nests`（親の巣を継ぐ） | 対の差 |
|---|---:|---:|---:|
| `prowlArrive`（湧きの偏り） | 0.09 | 0.10 | +0.01 \* |
| **`prowlGain`** | **0.01 ± 0.02** | **0.01 ± 0.02** | **−0.00 ± 0.02** |
| `prowlKept` | 0.10 ± 0.24 | 0.13 ± 0.19 | +0.03 ± 0.32 |
| `pop` | 132.7 | 129.7 | −3.00 ± 10.93 |
| **`killShare`** | 0.77 | 0.81 | **+0.04 ± 0.02 \*\*** |

**12シードでは `prowlGain` +0.03 \* が出ていたが、24シードで消えた**（−0.00 ± 0.02）。**12シードの側がノイズだった。**

**そして測定前に書いておいた「外れたら何がわかるか」がそのまま答えになった**——**偏りを消しているのは出生ではなく徘徊**。**段階58 が「外敵は出発点から平均270 離れる（地域の幅は200）」と測っているので、偏りは1つの生涯のうちに消えており、次の世代が受け継ぐものが残っていない。** **巣を継がせても、継ぐものが無い。**

**片方だけ良くなっていないかの確認で、1つ悪くなった**: **`killShare` +0.04 \*\***。**この段階では説明がついていない。** **`prowlGain` が動いていない以上「外敵が巣に固まって人間とよくぶつかる」では説明できない。**

**したがって既定にしなかった**（`Config.NestInherited` は false）。**点＋半径の形は残す**（地図の作者が半径を言えるようになり、測定上は中立）。**残っている問いは、離れる代価（`EnemyHomeCost`、既定オフ）を既定にするかどうかのほう。**

- **着手前に数えること**: **段階58 が測った「外敵は出発点から平均270 離れる（地域の幅は200）」と「新しい外敵の 38〜41% は世界の中で生まれる」**——**点の巣にしても、生まれた個体の巣をどう決めるかで同じ問題が出る**（親の巣を継ぐのか、生まれた場所か）／**段階64 の `prowlGain`（湧きの偏りが居場所に残る割合。6% → 45%）が点＋半径でどう動くか**

### 5. 植物の種類と外敵の種類を増やす前提で、タイルの語彙を先に決める（2026-09-20・ユーザの指定）【アイデア】

**ユーザの指定**: **今後、植物の種類と外敵の種類を増やしていきたい。その前提で今タイルを作りたい。** **この項目は 2 と 3 の前提になる「名前の取り決め」だけで、単独では世界の規則を1つも変えません**——**目的は、今日描いたタイルが種類の増えたあとに描き直しにならないこと**。

**決めたこと（決定事項#133。2026-09-20）**

- **植物の種類で `FoodKind` を増やさない。** **種類は `Food` のフィールド**にする（`Special`＝段階44 / `Cooked`＝段階52 / `Style`＝段階84 / `Made`＝段階82 と同じ立場）。**理由は `cook.go` が #80 で既に名指ししている**——**`NumEdibleKinds` は `dietVariety` の目盛り**なので、**4つ目の「食べられる kind」を足すと段階42 以降に記録した数値が全部「別の測定」になる**。**種類がいくつ増えても「植物を食べた」は1つの出来事のまま**にしておけば、過去の測定はそのまま読めます。
- **外敵の種類は `EnemyKinds` の行を増やすだけ**（段階59 の形。**1行足すだけで増えることはテストで固定済み**）。**種ではなくタグ**——**求愛は種類を見ないので交配する**。
- **総量は増えない。** **植物の種類が増えても `FoodSpawnRate` は変わらず、種類は同じ取り分を分け合う**（段階15a の原則）。**外敵も同じ**（段階58 の論法＝湧きの総数は変えない）。
- **魚は外敵の一種**（2026-09-20・ユーザの指定）。**したがって種類の表は「植物」と「外敵」の2つだけで、魚のための3つ目は作りません**——**水の生き物はすでに `EnemyKind.Water` の行**（段階63。`cmd/devview -lurkers` がその例）。**タイルに書く名前も `enemy` で、`fish` という語は予約しません。**
- **ただし `spawn: fish` はこれとは別のものなので、そのまま残ります**——**あれは「食料アイテムの魚（`FoodFish`）が湧く場所」**であって、生き物の話ではありません。**「魚を生き物にする」は 2026-09-13 に「作らない」で決着しており**（この文書の「保留」。段階42・43・34・36 の記録が全部引き直しになるため）、**上の指定はそれを覆すものとしては読んでいません**——**覆すなら別の判断として記録します。**
- **地図は名前で参照するだけ。** **種類の中身**（予算・移動能力・栄養・ドロップ・毒の初期値）**は `Config` の表に置く**（`EnemyKinds`、将来の `PlantKinds`）。**タイルが持つのは名前1つ。** **理由は文書の分担と同じ**——**数値を地図と `Config` の両方に置くと、必ず片方が古くなる**。**表そのものを地図に載せるか**（Tiled のマップ単位のカスタムプロパティに JSON で置く手はある）**は未決**で、**数え上げのときに決めます**。

**タイルの語彙（今日から使える形）**

- **engine が今読む語（予約済み）**: **地形** `kind`（string: flat / rough / water / high / slope）・`height`（int: 1〜9）／**発生** `spawn`（string: plant / fish / enemy / food / all / none）／**地域** `region`（string: 名前）・`shelter`・`food`・`special`・`enemies`（float: 0〜2）・`goal`（bool）
- **将来のために予約する語（今は黙って無視される＝エラーにならない）**: **`plant`**（植物の種類の名前）・**`enemy`**（外敵の種類の名前＝`EnemyKinds[].Name` と一致させる。**魚もこれ**）・**`nest`**（巣＝4）・**`roam`**（徘徊半径＝4）・**`rich`**（豊かさの重み＝2）
- **`kind` は地形専用。** **植物や外敵の名前に `kind` を使ってはいけません**——**`groundChar` は知らない値を拒否するので、地図全体が読み込みエラーになります**。**`food` も地域の豊かさで予約済み**なので、食べ物の種類名には使いません。
- **知らないプロパティ名が無視されることは実装で確認済み**（`propString` / `propNumber` は名前で引くだけで、余分なプロパティを見ない）。**だから今日 `plant: "berry"` と書いたタイルは、今は「ただの植物」として動き、2 を実装した日に意味を持ちます。**

**着手前に数えること**

- **植物の種類がいくつ要るか。** **`dietVariety` が今いくつで、種類を増やしたときに何が動くはずか**を先に書く。**段階42（魚）が `onWater` を 0.13 → 0.24 と動かした唯一の例**なので、**「種類が増えると居場所が動く」が期待できる形は「その種類がそこにしか無い」ことだけ**——**どこにでもある2種類目は、たぶん何も動かしません**。
- ~~**種類と `plantGenes` の関係。**~~ **決着（2026-09-20・ユーザの指定＝決定事項#134）: 束でよい。** **植物の種類＝`plantGenes`（`Spread` / `Regrow` / `Poison` / `Signal`）の初期値の束**で、**新しい軸は作らない**。要るのは **`Food` のフィールド1本と `Config` の表1つ**だけ。**着手時に確かめることが3つ残る**:
  - **名前の衝突**: **`Food.Kind` は `FoodKind`（植物／肉／魚／石／コイン／本／飾り物）で埋まっている**ので、**種類を指すフィールドは別の名前にする**。
  - **束は、植物の遺伝を既定オンにしないと世界に現れない。** **`PlantGenetics` も `PlantDefence` も既定オフ**で、オフのとき `drawPlantGenes` は**全植物に同じ `{Spread: PlantSpread, Regrow: 1}` を配る**。**つまり「種類を足す」は「植物の遺伝を既定にする」とセットでしか意味を持たない**——**どちらを既定にするかは測ってから決める**。
  - **種類のタグを子に継ぐかどうか。** **束は初期値なので、`seedFrom` → `inheritPlantGenes` で親から継いだ株は、もとの種類の値から離れていく**。**タグを継がせないと、地図に `plant: berry` と塗った意味は最初の一株にしか残らない。** **継ぐのは1フィールドのコピーで安い**ので継ぐほうに倒すのが自然だが、**「種類は遺伝子の束」という決定の外側の話**なので、着手時に確認する。
- **外敵の側は 3 の数え上げをそのまま使う**（段階62 の上限の式＝保有率 × 実効値 × 的の大きさ／飛行型が地形をまたぐ頻度）。

**壊してはいけない線**

- **`FoodKind` を増やさない**（#80。増やすと段階42 以降の `dietVariety` が全部読み直し）
- **総量保存**（段階15a）——**種類は取り分を分けるものであって、増やすものではない**
- **種類は種ではない**（段階59）
- **能力の役割を重ねない**（`CLAUDE.md` の原則）——**種類は「予算の振り方」であって、新しい能力ではありません**

---

### 6. 気候を地域から切り離し、自分の粒度の地図にする（決定事項#135。2026-09-20・ユーザの指定）【実装済み】

**ユーザの指定**: 「**地域とは別のレイヤーにするほうがいい。地域は広くなりすぎる可能性がある。特に初期ステージは地域を少なくすることで難易度を簡単にする予定。**」

**決めたこと（決定事項#135）**

- **気候は地域の性質をやめ、自分の粒度の2枚目の地図にする。** **`ClimateMap` の1文字＝1セル**で、**セルの大きさは絵そのものから決まる**（地形と同じ。3×3 の絵なら3×3、40×30 の絵なら40×30）。**`region.Weather` は消した。**
- **これは #114 の撤回。** #114 が気候を地域に置いた理由は「**この世界が場所について学んで伝える仕組みは全部地域に乗っている**」だったが、**段階86 がその前提を潰していた**——**身体が地域について持つ信念は豊かさ・行きにくさ・危険の3本で、天気はどれでもない**。**天気を学ぶものも伝えるものも、この世界には1つも無い。** 地域に置く理由が実際には残っていなかった。
- **そして前提そのものが変わった。** **地域の数が難易度のつまみになった**（#130 の初期ステージ）ので、**地域が3つの地図は気温を3段階しか持てない**。**粒度が難易度と結びついているのは設計の誤り。**
- **数は地図に書かない**（決定#133 のまま）。**タイルが持つのは「一番寒い所を1としたときの割合」（`chill: 1..9` / `heat: 1..9`）で、その一番寒い所がいくら取るかは `Config.ChillDrain` / `HeatDrain`**。**同じ絵が、置かれる世界によって穏やかな冬にも殺す冬にもなる**——**初期ステージは絵を描き直さずに用量だけ下げられる。**
- **タイルは専用のレイヤーに塗る**（`chill` / `heat` プロパティ）。**ただし engine はレイヤーの名前も番号も見ない**——**「そのタイルが天気を言っているか」だけを見る**ので、地形タイルが天気を兼ねても読める（豊かさと同じ扱い）。
- **一歩先を読む経路に載せた**（`ChillAheadSeen`、既定オン）。**これが粒度を細かくする唯一の意味**——**段階86 が「寒さは居場所を動かさない」と測ったときの理由は「足元の寒さは全候補を等しく押し上げるので約分される」**で、**段階100（一歩先の地面を読む）はこの project で居場所を動かせた唯一の規則**。**隣のセルとの差は約分されない。** 課金は段階99 の drain とまったく同じ場所・同じ単位（`moveCostDir` の中で、そのオプションが掛かる tick 数ぶん）。
- **段階86b（`ChillGradient`）の歩幅も気候のセル1つに変えた**（以前は地域の幅。**絵より細かい歩幅は同じセルを2度読んで「世界は平ら」と答える**）。
- **`RegionView.Chill` は残すが要約**（その地域のセルの平均）。**規則は1つもこれを読まない**——読むのは測定器と編集画面だけ。

**実装前に数えたこと（そして数えて設計が変わった点）**

- **粒度の取引は一方向ではない。** **細かくすると一歩あたりの差が薄まる**——40列なら1セル20幅（1決定が運ぶのとほぼ同じ）だが、一歩の寒さ差は範囲の1/39。**4列ならセル200幅で、世界の大半が東西とも同じセル＝何も読めない**（段階86 の再現）。**だから腕を3つにした**（4列 / 10列 / 40列）。**単調ではないはず**、というのが測定前の予想。

**測定（2026-09-20。24シード × 20000tick、粒度ごとに対照を1つずつ）**

- **効くのは一番粗い絵だけ**: **4列で `pop` 48.4 → 94.4（+46.00 \*\*\*）、`peak` +37.79 \*\*\***。**10列・40列はどちらも誤差の中。**
- **予想は外れた**（細かいほど効くと予想した）。**理由は段数**——**絵の文字は9段しかないので、40列に1→9の傾斜を描くと隣同士が同じ文字になり、一歩先を読んでも同じ値が返る**（東西の読みの差: 4列 0.0278 / 10列 0.0056 / 40列 **0.0000**）。
- **効き方も予想と違った**: **`onCold` / `coldGain` は動かない**（+0.01 ± 0.04）。**動いたのは1体あたりが払う寒さで、424 → 266（−37%）**。**集団の平均位置は食料が決める**（段階86 以来7度）が、**個体が寒さの奥へ迷い込む頻度は変わった**＝**居場所を動かさずに死亡率を動かした初めての規則**。
- **測定設計を1度やり直した**——最初は対照が1つで、どの腕も「絵」と「読むこと」が同時に違っていた。**そして平均の寒さを揃えるだけでは足りない**（4列は世界の1/4 が一番穏やかな段、40列は 1/40。**非線形なら揃えるべきは分布**）。
- **地図の作者への含意**: **天気は「何セルおきに段が変わるか」で描く。** なだらかに描いても隣が同じ文字なら身体には読めない。

**壊してはいけない線**

- **気候と地面は別の地図**（#128 の線）。**`terrain.Drain`（濡れたセル）と天気は足し算せず、最後まで別々に運ぶ**——`GroundRead` も `Drain` と `Chill` の2本を持つ
- **数は地図に載せない**（#133）
- **既定は天気なし**（絵を描くのは地図の作者）。**`ChillAheadSeen` は天気の無い世界では1つも仕事をしない**

---

## 王朝もの——複数拠点への定住を目指すゲーム（#130。ユーザの提起）【アイデア・2026-09-19 に立てた。**設計とパラメータは実装前の数え上げで決める**】

**段階19・23 で「1ノードを動かす」「転機に訊かれる」まで作ったが、勝ち負けが無かった。** ここで決めるのは**遊びの形**——**指定した複数の拠点に、同時に自分の民族（血統ベース）が定住していることをクリア条件にする**。

**この節はアイデアの記録で、まだ段階番号を振っていない。** **形・罰の強さ・パラメータはどれも「実装前に数える」で決める**（段階98 は分布を見てカーブの形が変わり、段階100 は候補の重みを見て効かせる先が変わった）。

### 決めたこと（ユーザの指定）

- **クリア条件**: **指定した複数の拠点に、同時に自分の血統が定住していること**
- **拠点**: **新しい地物タイプは作らない**——**豊かさグリッドで食料を意図的に集中させた区画＝拠点**（P16 の 1・2 が前提）
- **民族の定義は血統ベース**（開始時の血統をタグとして子に継承。開始時点のスナップショットに好感度クラスタを使うのは可だが、以降は血統で追跡）
- **プレイヤーは生きている間は転移できない。** **死後、一定期間（例: 10年相当）は裏でシミュレーションを進め、プレイヤーには見せない**（ダイジェストは任意）
- **継承ラインは拠点ごとに独立**（各拠点にいる直系の最年長者を継承候補とする＝拠点単位で完結させ、血統全体を探索しない）
- **期間経過後、継承候補がいる拠点の中からプレイヤーが選んで転移する**（どこに生まれ変わるかが戦略選択）
- **転移した子孫は天才として生まれる**（既存の天才化を転移演出に流用）
- **ゲームオーバー**: **すべての拠点で継承ラインが途絶えたとき**
- **地形スキルは推奨であって必須ではない**（罰は「無いと詰む」ではなく「あるほうが有利」）
- **寒冷地形**: 防寒着を持たない個体は定住しにくい。**罰の強さは実装前に数えて決める**
- **防寒着は「作って売る物」**（材料と加工の2段階。材料は**外敵討伐のドロップ**）
- **「身内には贈与・他人には売買」は新しい分岐を作らない**——**既存の効用比較のパラメータ（材料の希少性・価値・`CoinValue` など）のチューニングの結果として立ち上がることを狙う**。立ち上がらなければ、その時点で何が足りないかを見て最小限の調整を検討する

### ゴールエリアの渡し方（2026-09-19 に決めて、印だけ実装した）

**クリア条件のエリアは「地域に印を付ける」形で渡します。** **Tiled で `goal` プロパティを付けると、その地域が印つきになります**（`RegionShape.Goal`）——**オブジェクト層の矩形に付けても、地域を塗るタイルに付けても同じ**（2026-09-20。**ゴールが L字や入り江の形をしていてよい**のは塗りの側だけです）。

- **engine はこの印を1度も読みません。** **`Endow` や `SetTerrain` と同じ立場**——**世界が運ぶデータであって、シミュレーションが消費するものではない**。**読み出しは `World.GoalRegions() []int` の1本だけ**で、**クリア判定はゲーム側**（`cmd/devview`、将来のブラウザ）＝**段階19 と決定#130 の線をそのまま守る**
- **なぜ別の矩形ではなく地域に印を付けるのか**: **(1)「そのエリアに誰がいるか」が既にタダ**（位置→地域は O(1) の索引なので、数えるだけ）／**(2) 定住の測定（`TODO.md` の5番）はどのみち地域単位**（拠点＝豊かさグリッドの高食料区画、と決めてある）／**(3) 身体の信念も地域単位**なので、将来「その血統はその土地を知っているか」まで言える
- **配布したスナップショットが自分のゴールを連れて行きます**（`RegionShapes` は `Config` の一部なので保存・読込に自動で乗る）＝**地図とゴールが1つのファイル**で、別ファイルの定義と食い違う事故が起きない
- **まだ決まっていないのは「同時に住んでいる」の定義**（人数か、滞在の割合か、連続tickか）。**`TODO.md` の5番で、実装前に数えてから決める**——**`richGain` は 0.05、社会的動機を切ると 0.03 ＝拠点が引き寄せる力の実測値は 0.02 の幅しかない**ので、**厳しすぎる定義にすると誰も達成できない**
- **地域より細かいゴールが要るとき**は、別のオブジェクト層の矩形を `Config` が素通しで持つ形にもできますが、**その場合「そこに住んでいるか」の判定はゲーム側で毎回全個体を走査**することになります。**地域に印を付けるほうが安い**

### 既にあるもの（作らなくてよいもの）

- **血統リンク**（`ParentIDs` / `ChildIDs`、段階0）と**乗り換え**（`World.Heirs` ＝生きている成人の子。段階19）。**`cmd/devview` は死亡時に後継を選ばせる UI をすでに持つ**（成人前の子も候補に出す）
- **天才化**（`World.Inspire`。段階22。**枠を1つ増やし、すでに持っているスキルを跳ね上げる。乱数を引かない**）＝**転移演出にそのまま使える**
- **寒冷地形と防寒着はすでにある**（段階85〜88）——**気候は地域の性質**（`ClimateMap` / `ChillDrain`）、**防寒具は飾り物の一種として作られ**（段階87a、`Wards`）、**1着で足りる**（`wardsOff` は手の中の最大値）
- **スキルの継承・模倣**（段階38a。出生地・継承・観察の3経路）
- **調理・贈与・売買・掲示・倉庫・コイン**（段階48〜52・68・80）

### 先に読むべき測定（この案の前提に直接あたるもの）

- **防寒着は「売る物」にならなかった**。**段階87a: 770着作られ85%が人手に渡り、その全部が贈与で、売買は不動**。**段階88（天気を増やす）: 贈与に値段を付けると贈与が半減し売買も半減する**（−878 \*\*\* / −0.80 \*\*\*）＝**交換が売買に化けるのではなく交換そのものが消える**。**#116（コインの請求先を広げる）: コインの値打ちを上げると持たれるだけで使われない**
- **市場が開かない理由は `CoinValue` ではない**（段階79・81）。**売り手の `coin/food` の比は構造で `CoinValue` に固定されており**（並の1食なら状態によらず）、**釣り合うのはコイン2枚**。**ところが手が1つなので買い手は1枚しか持てない**（段階80a `CarrySlotsWeigh` で 2.88枚になり、段階80 の的が10倍になった）。**したがって「材料市場が回る `CoinValue` の水域」は、まず `CarrySlotsWeigh` を入れた世界で探す**
- **豊かさと定住の相関は6割**（段階15a。**社会的動機を全部切ると `richGain` 0.05 → 0.03**）＝**豊かさグリッドを整えるだけでは定住は保証されない**というユーザの留意事項は、この測定と一致する
- **寒さは居場所を動かさない**（段階86。**寒い側にいる割合は偶然どおりで、「感じない」対照と区別がつかない**）。**したがって「寒冷地に定住しにくい」は、罰を強くしても出ない可能性が高い**——**2026-09-19 に既定になった「一歩先の地面を読む」（段階100）が、地形について居場所を動かした唯一の経路**なので、**寒さも同じ形（行き先の寒さを読む）にできるかを先に考える**
- **外敵のドロップは今は肉だけ**（`dropMeat` ＝ `MeatPerBudget × 予算`）。**種別ごとのドロップは P16 の 3**

### 崩してはいけない線

- **engine に「プレイヤー」も「ゲームオーバー」も無い**（段階19 の線）。**クリア条件・拠点の指定・継承ラインの追跡・転移の待ち時間は、ゲーム側（`cmd/devview` か将来の `client`）に置く**。**engine が足すとしたら「血統タグ」のような、誰が操作しているかと無関係な状態だけ**
- **閾値を作らない**（「防寒着が無ければ入れない」は書かない。**あるほうが有利**にとどめる＝ユーザの指定と一致）
- **新しい分岐で「身内／他人」を切らない**（既存の効用比較＝好意・血縁・信頼の飽和で立ち上がるかを見る。**段階92 が `AffinityKin` = 22 > `AffinityTrust` = 20 の飽和を見つけている**ので、**身内への贈与が候補に挙がる形になっているかを先に確かめる**）
- **保存則**（食料の総量・予算の継承総量・死体の claim）。**拠点を作ることは食料を増やすことではない**（段階15a の取り分の原則）

### 実装前に数えた（2026-09-20・12シード × 20000tick・既定条件12地域）

**血統タグ（`Agent.Lineage`）を入れて、最初の問い——「複数拠点に同時に居ることが偶然どれだけ起きるか」——を測った。**

| | |
|---|---:|
| `lines`（生き残っている血統の数） | **4.67 ± 0.48**（始まりは70） |
| `lineBiggest`（最大の血統が占める割合） | **0.55 ± 0.07** |
| `lineRegions`（1血統が同時に立っている地域数） | **7.00 ± 0.35**（12地域中） |
| `lineInTwo`（2地域以上にいる血統の割合） | **0.92 ± 0.03** |
| `lineInThree`（3地域以上） | **0.85 ± 0.05** |

**答え: 偶然ほぼ常に起きる。** **20000tick で残るのは約5血統だけ**（70本のうち）**で、そのうち92%が2地域以上、85%が3地域以上に同時にいる**。**平均で12地域のうち7地域。**

**これはクリア条件の設計に直接効く。** **「複数の拠点に同時に自分の民族がいること」をそのままクリア条件にすると、何もしなくても達成される。** **したがって「定住」は「立っている」よりずっと強い意味でなければならない**——**滞在の割合か、出生地か、顔ぶれの持続か**（`TODO.md` の5番がその定義を決める項目）。**拠点の数も、12地域のうち3つでは足りない。**

**血統の集約も速い**: **70本が20000tick で5本になり、最大の1本が人口の55%を占める。** **「自分の民族」を名乗れる単位は、思っていたよりずっと粗い。**

### 継承則を変えたら、両極端に割れた（2026-09-20・ユーザの問い「長子にのみ継承するルールにしても状況は変わらないか」から）

**12シード × 20000tick。** **世界はまったく同じ**（`pop` / `gen` / `births` が完全一致＝**タグは engine の誰にも読まれていない**）。**変わるのはタグの意味だけ。**

| | 木（全員が継ぐ・既定） | 鎖（長子のみ、死んだら次） | 対の差 |
|---|---:|---:|---:|
| 生き残っている血統 | 4.67 | **0.25 ± 0.13** | −4.42 \*\*\* |
| 1血統が立っている地域数 | 7.00 | 0.25 | −6.75 \*\*\* |
| **2地域以上にいる割合** | **0.92** | **0.00 ± 0.00** | −0.92 \*\*\* |
| 3地域以上 | 0.85 | 0.00 | −0.85 \*\*\* |

**答え: 変わる。両極端に割れる。**

- **木では「複数拠点に同時にいる」が偶然ほぼ常に起きる**（0.92）。
- **鎖では1度も起きない**（0.00 ± 0.00）。**担い手が親と長子の2人しかおらず、その2人は家族なので同じ場所に立っている。**
- **そして鎖は絶える。** **70本で始めて、20000tick 後に残るのは 0.25本**——**12シード中9シードで全滅**。**これは数学のほうの話でもある**: **1人にしか継がせない継承は臨界の分枝過程で、臨界の分枝過程は確率1で絶える。**

**クリア条件の設計に対する答え**: **どちらもそのままでは使えない。** **木は緩すぎて何もしなくても達成され、鎖は「同時に複数拠点」が原理的に不可能なうえ、40年で4回に3回は家が絶える。** **「自分の民族」は、担い手が1人の家系でも全員が継ぐ木でもない何か**——**たとえば「その血を引く者すべて」（＝木）を民族とし、クリア条件のほうを「立っている」より強い定住にする**、という組み合わせが残る。

**そして6番（継承と転移）が数えたがっていた「ゲームオーバーの頻度」の答えもここにある**: **1人にしか継がせないなら、40年で75%が詰む。**

### 3つ目の継承則——地域ごとに1人（2026-09-20・ユーザの提案「プレイヤーは各地域を旅して子孫を残す。長子／次子は地域ごとに決める」）

**`LineageRegionChain`**: **生まれた子は、その子が生まれた区画に自分の血統の担い手が（母以外に）1人もいないときだけ継ぐ。** **区画ごとに担い手が1人になり、その1人が死ねばその区画で次に生まれた子が継ぐ。** **母を数えないのは、母は定義上その場にいるから**——**訊いているのは「この国に跡継ぎを残していくのか、もう居るのか」。**

| | 木（全員） | 鎖（1人） | **地域ごと** |
|---|---:|---:|---:|
| 生き残っている血統 | 4.67 | 0.25 | **1.83 ± 0.47** |
| 1血統が立っている地域数 | 7.00 | 0.25 | **2.12 ± 0.45** |
| **2地域以上にいる割合** | 0.92 | 0.00 | **0.58 ± 0.13** |
| **3地域以上** | 0.85 | 0.00 | **0.40 ± 0.12** |
| `pop` / `gen` / `births` | 同一 | 同一 | 同一 |

**これが遊びの形と噛み合う唯一の継承則だった。**

- **血統は絶えにくい**（1.83本。鎖の7倍）——**40年で家が絶える確率がずっと下がる。**
- **「複数拠点に同時に」は起きるが、当たり前ではない**（2地域以上 0.58、3地域以上 0.40）。**木の「ほぼ確実」と鎖の「不可能」の中間で、初めて「狙えて、失敗もする」条件になった。**
- **そして担い手を増やす方法が、プレイヤーの行動そのもの**——**新しい国へ行って子を残すことが、その国に跡継ぎを作ること。** **規則は1つも増えていない**（engine はタグを読まない）**のに、遊びの目的と世界の動きが同じ向きを向く。**

**残っている選択**: **「民族＝血を引く者すべて（木）」と「跡継ぎ＝地域ごとに1人」は別の概念**で、**ゲームは両方を欲しがるかもしれない**（「自分の血筋はまだ世界に残っているか」と「どの国に跡継ぎがいるか」）。**今は1つのタグしか無いので、どちらか一方を選ぶ。** **両方が要ると分かったら、2本目の欄を足す**——**それは6番で決める。**

**難度の調整つまみは2つ**: **ゴールエリアの数**（3地域なら偶然0.40、4地域以上なら下がる）と、**5番が決める「定住」の強さ**（今の 0.58 / 0.40 は「立っている」だけの数字）。

### 「定住」を決めて測った（2026-09-20・12シード × 20000tick）

**定義**: **身体は、直近40読み（25tick ごと＝1000tick＝2年）の6割以上を1つの区画で過ごしていればそこに定住している。** **半分より上なので、同時に2か所へ定住することはない。** **3候補のうち「滞在の割合」を採った**——**出生地は変わらないので動いた血統と動かない血統を区別できず**、**顔ぶれの持続は `MembershipTracker` が別の問いに答えている。**

| | 木・立っている | 木・住んでいる | 地域ごと・立っている | **地域ごと・住んでいる** |
|---|---:|---:|---:|---:|
| 血統数 | 4.67 | 4.42 | 1.83 | 1.42 |
| 地域数 | 7.00 | 5.09 | 2.12 | 1.26 |
| 2地域以上 | 0.92 | 0.82 | 0.58 | **0.30** |
| 3地域以上 | 0.85 | 0.67 | 0.40 | **0.13** |

**そして `settled` 0.44**——**生きている身体のうち、どこかに定住しているのは半分以下。残りはいつも流浪している。**

**これでクリア条件が書ける。** **「地域ごとに跡継ぎ」×「定住」で、3拠点同時は偶然 0.13**——**8回に1回**。**木のままだと 0.67**（3回に2回）**で緩すぎ、「立っている」だけだと 0.85**（ほぼ確実）。**3つのうち2つを厳しくして、初めて狙う値打ちのある条件になる。**

**難度のつまみは3つになった**: **継承則**（木／地域ごと）・**「住んでいる」の窓と割合**（今は 1000tick の6割）・**ゴールエリアの数**（今の数字は3拠点のもの）。

### 実装前に数えること（形とパラメータはこれで決まる）

- **拠点に「定住」しているとはどう測るのか**（滞在の割合か、出生地か、`MembershipTracker` の顔ぶれか）。**段階15a の `richGain` は 0.05 で、社会的動機を切ると 0.03**——**この0.02 の幅が「拠点が引き寄せる力」の実測値**
- **1つの血統が20000tick でいくつの地域に散るか**（段階57c は「一生に12地域のうち 5.9 に立つ」と数えた）＝**複数拠点に同時に居ることが偶然どれだけ起きるか**が、クリア条件の難易度の基準線
- **拠点ごとの継承候補（直系の最年長者）が存在する確率**——**血統が拠点で途絶える頻度**がゲームオーバーの頻度そのもの
- **10年（5000tick）裏で進めると、選んだ血統が何%残るか**
- **防寒着の材料が「外敵のドロップ」で足りるのか**（段階62 の上限の式＝保有率 × 実効値 × 的の大きさ。**外敵の死は1実行に何回で、そのうち人間が回収できるのは何回か**）
- **`CarrySlotsWeigh` を入れた世界での売買の成立率**（段階80 の的は 0.0159 → 0.1571 になった）

### 順序（前提が2つある）

1. **P16 の 1・2**（Tiled と豊かさグリッド）——**拠点がそこに乗る**
2. **P16 の 3**（外敵の種別とドロップ）——**防寒着の材料がそこに乗る**
3. **そのうえで、この節を段階に分ける**（血統タグ → 拠点の定義と定住の測定 → 継承と転移 → 寒冷地の罰 → 材料と加工）

---

## ブラウザで遊ぶクライアント（#131。ユーザの提起）【アイデア・2026-09-19 に立てた。**設計とパラメータは実装前の数え上げで決める**】

**`protocol` / `server` / `client` の3点セットは 2026-08-11 からずっと「未着手」のまま残っていたが、ここで形が変わった**——**クライアントはブラウザ（Android Chrome / Safari）で動かし、`engine` を直接 import する**。**サーバは当面作らない。**

### 決めたこと（ユーザの指定）

- **ブラウザで動かす**（Android Chrome / Safari）。**ebiten を WASM 出力する**
- **世界はプレイ中しか進まない**。**開発環境で育てたスナップショットを配布し、そこから始める**（**背景での時間経過は無し**）
- **クライアントは `engine` を直接 import する**。**`protocol` / `server` は当面不要**で、**後から「セーブのアップロード」程度の薄い API だけ足せればよい**
- **描画は円からタイル画像へ**。**ノードは1〜3タイル、大型外敵は5タイル程度、透過必須、簡単な状態アニメーション（歩行・食事・戦闘）**、**個体差は `ColorScale` で色味を変える**、**向きは左右反転**
- **画像アセットはハッシュ付きファイル名＋ manifest でキャッシュし、プリロードしてから開始する**
- **操作はタップで対象選択 → 2回目のタップで既定行動**（食べ物→ `eat`、外敵→ `attack`、地面→ `move`）、**長押しで行動メニュー**

### 既にあるもの（作らなくてよいもの）

- **`cmd/devview` が `engine` を直接 import する ebiten アプリ**（2026-08-11 に「踏み台」として作ったもの）。**この方針だと、踏み台がそのまま本番の形になる**——**`client` は devview の子孫であって、別物として作り直す必要がない**
- **スナップショットの配布は段階21 がそのために作られている**（`World.Save` は `io.Writer` を取るので、ブラウザでもそのまま動く）。**段階21 が挙げた3つの用途の2つ目がまさに「進めた世界の配布」**。**乱数は「種と引いた回数」で保存されるので、どの実行環境でも同じ続きになる**
- **遊び方は段階19・23 で入っている**（人間コントローラ／訊かれて答えるモード／節目の性格／死亡時の後継選択）。**engine は誰が操作しているかを知らない**ので、入力の形が変わっても engine は1行も変わらない
- **ebiten は v2.9.9**（`go.mod`）。**WASM 出力と `ColorScale`（`DrawImageOptions`）と `TouchIDs` はこのバージョンにある**

### 崩してはいけない線

- **`engine` は描画も通信も知らない**（2026-08-11 の設計。**タイルも manifest もアニメーションもクライアント側**）
- **段階19 の2つの約束**: **見せるのは `Perception` の中身だけ**／**狙えるのは視界の中だけ**。**「タップで対象選択」も同じ線に乗る**（視界の外はタップできない。**画面に描いてある＝ノードが知っている、ではない**ので、何を描いて何をタップ可能にするかは別に決める）
- **決定性**（同じ `Config`・同じ `Seed` なら同じ結果）。**WASM でも同じでなければ、開発環境で育てたスナップショットの続きが別物になる**
- **保存形式は段階21 のまま**（エディタ専用形式も、ブラウザ専用形式も作らない）
- **測定は `cmd/experiment` のまま**（ヘッドレスのネイティブ。ブラウザは遊ぶ側で、測る側ではない）

### 実装前に数えること

- **WASM の `Step` のコスト**（ネイティブは既定の世界で 750µs／1tick、遊ぶ地図ではもっと）。**スマートフォンで1フレームに何tick進められるかが、遊びの速度そのもの**
- **WASM バイナリの大きさと起動時間**（Go の WASM は素で10MB 超になりうる。**`gzip`／`brotli` 後の実寸と、初回ロードの秒数**）
- **育てた世界のセーブの大きさ**（段階21 の JSON は**全個体の記憶・信念・ヒント・地域の知識**を持つ。**個体数100 で何MB か**——**配布するファイルなので、ここが大きければ「何を持ち込むか」を段階21 の `Node`（身体だけ）に寄せる判断が要る**）
- **1画面に何個描くことになるか**（遊ぶ地図で人間100・外敵12・食料150 前後。**タイル化すると1ノード1〜3枚なので、描画は数百枚／フレーム**）
- **タップで答える頻度**（段階23 が測った「訊かれるのは 112 tick に1回＝一生で20〜30回」。**長押しメニューが必要な頻度はこれで決まる**）

### (1)(2) をやった（2026-09-19）——**ビルドは通り、速度も大きさも実用域。ただし決定性はプラットフォームをまたがない**

**(3) の確認でわかった手順の2つの罠（2026-09-20）**: **`--virtual-time-budget` を使うとフレームがほとんど来ない**（40秒の仮想時間で4〜6フレーム＝合成した押下を1フレームも見ずに終わる。**外すと 3.6秒で136フレーム**）。**しかし外すと Chrome の `--screenshot` は読み込み直後に撮ってしまう**ので、**ページ自身に撮らせる**——**`requestAnimationFrame` の中で `canvas.toDataURL()` を呼び、ローカルのサーバへ POST する**（**ebiten は `preserveDrawingBuffer` を付けていないので、フレームの外で読むと空**）。**合成タッチは `new TouchEvent(..., {targetTouches:[new Touch({...})]})` を canvas に投げる**（ebiten が読むのは `targetTouches`）。**`go.argv` をページから渡せる**ので、ブラウザでも `-play` などの起動オプションが使える。

**(1) WASM ビルドと実行**: **`engine` も `cmd/devview` も、コードを1行も変えずに `GOOS=js GOARCH=wasm` でビルドでき、実際のブラウザで動いた**。**確認の仕方**——WSL 側で wasm と `wasm_exec.js` と読み込み用の HTML を配信し、**Windows 側の Chrome（headless）から `localhost` で開いて、ページから `fetch` で結果を報告させ、`--screenshot` で描画を撮った**。**結果: WASM のインスタンス化 27〜28ms、ebiten が canvas を1つ作り（ウィンドウに合わせて 1169×705 にリサイズ）、世界が進み、地形・ノード・HUD・右パネルがすべて描かれた**（撮った絵では tick 46・個体数69・川の帯・求愛や戦闘の線まで出ている）。**エラーは1つも出ていない**。**Android Chrome は同じエンジンなので、残る不確かさは端末の速度だけ**。**この手順は (3)(4) の目視にもそのまま使える**。

**(2) 測った5つ**（遊ぶ地図・シード5・20000tick まで育てた世界。native は amd64、wasm は node/V8）:

| | native | wasm（node/V8） |
| --- | --- | --- |
| 育てるとき | 216 µs/tick | 586 µs/tick |
| **育った世界での定常** | **400 µs/tick** | **614 µs/tick（1.54倍）** |
| **16.7ms のフレームに入る tick 数** | 41.8 | **27.2** |
| 画面の中身 | 人間153・外敵14・食料130 | （別の世界になる。下記） |
| セーブ | **0.90 MB**（gzip 0.15 MB） | 0.75 MB |
| 身体だけ（`SaveNodes`） | **0.09 MB** | 0.08 MB |

- **バイナリ**: **`devview.wasm` は 14.2 MB（gzip 3.4 MB）**。**engine だけの小さな実行ファイルで 3.7 MB**、**何もしない Go プログラムでも 2.3 MB** ＝**Go ランタイムの下駄が大半**。**起動は node で 0.32 秒**（何もしない Go プログラムで 0.06 秒）
- **速度**: **wasm は native の 1.5倍遅いだけ**。**16.7ms に 27tick** なので、**`cmd/devview` の「normal」（1フレーム2tick）と「fast」（8tick）は余裕で回り、一番速い設定（60tick）だけが入らない**。**ただし node/V8 はこの PC での数字で、スマートフォンはさらに 2〜4倍遅いと見ておく**
- **セーブ**: **育った世界まるごとで 0.90 MB・gzip で 0.15 MB** ＝**配布に何の問題も無い**。**身体だけなら 0.09 MB**だが、**丸ごとでも十分小さいので「`Node` に寄せる」判断は要らなかった**
- **描画枚数**: **人間153・外敵14・食料130 ＝ 約300 個**。**1体1〜3タイルなら 300〜500 枚／フレーム**
- **タップで訊かれる頻度**: 段階23 の実測が **112 tick に1回**。**devview の「normal」は1フレーム2tick なので、60fps なら約0.9秒に1回**——**長押しメニューはその間隔で使われる**

### 決定性はプラットフォームをまたがない（この測定の一番大きい発見）

**同じ `Config`・同じシードで、native と wasm は違う世界になった**（遊ぶ地図で 20000tick 後に 153人 対 140人）。**原因は突き止めた**——**`math.Exp` と `math.Pow` が amd64 と wasm で 1 ULP 違う**（`math.Log` / `math.Sqrt` / `math.Atan2` / `math.Hypot` は一致した。8点 × 3関数の bit 比較で `Pow` 5件・`Exp` 2件が相違）。**engine の使用箇所は3つ**——`memory.go` の `decay`（記憶とリスクの減衰。**常に走る**）、`controller.go` の `survives()`（段階73。**既定オン**）、`genome.go` のガンマ抽選（世界を作るとき）。

- **指紋テストは wasm でも通る**（平坦な既定の世界・5000tick）。**しかしそれは運**——**その入力では両者が一致したというだけ**で、**地形のある世界を長く回すと必ずずれる**（遊ぶ地図では 1000〜2000tick の間に、段階100 の読みを切っても 15000tick 付近で）
- **段階100 の `Atan2` は犯人ではない**（bit 一致）。**読みを入れるとずれが早く出るのは、決定の数が増えて `Exp`/`Pow` を引く回数が増えるから**
- **P18 にとって何が壊れて何が壊れないか**: **壊れない**——**配布したスナップショットの「状態」は完全に復元される**（JSON の float64 は往復で一致、乱数は種と引いた回数）。**プレイヤーの端末の中では決定的**。**壊れる**——**「開発環境で見た続き」と「プレイヤーの端末での続き」は同じにならない**。**別の端末どうしでも同じにならない**（同じアーキテクチャなら同じ）
- **ブラウザどうしは一致する（2026-09-19 に実測）**: **同じ `.wasm` を Chrome・Edge・node で走らせると、2万tick 後の digest が1ビット違わない**（`4d15b34ae4a43ba8`・71体。ネイティブは `9f92ffa23d070c9a`・166体）。**つまり食い違いは「ブラウザごと」ではなく「ネイティブ実行 対 WASM 実行」の1本だけ**。**WASM の f64 演算は仕様で厳密に決まっており、`Exp` / `Pow` は Go の純 Go 実装がそのまま `.wasm` に入る**ので、**どのエンジンでも同じ命令列が走る**。**ただし測ったのは V8 系（Chrome・Edge・node）だけ**で、**Safari / Firefox は未確認**（Android Chrome は V8 なので、想定する端末は測った側）
- **決着（2026-09-19・ユーザの判断＝決定事項#132）**: **開発機と端末で続きが違うことは許容する**。**理由は「この世界はプレイヤーが動かすので、続きはどのみちプレイヤー次第でいくらでも変わる」**——**「同じ続き」は守る価値のある性質ではない**。**engine は直さない。** 以下はその判断の材料として残す
- **したがって決めることは「ブラウザ差をどうするか」ではなかった**——**「開発機で見た続き」と「プレイヤーの端末での続き」を一致させるか**の1点。**プレイヤーどうしは（同じ `.wasm` なら）すでに一致している**
- **安いほうの直し方が1つある**: **engine を直さず、配るスナップショットを「WASM 側で」育てる／試す**（`node wasm_exec_node.js` で回せることは確認済み）。**そうすれば開発で見た続きがそのままプレイヤーの続きになる**。**測定（`cmd/experiment`）は分布を見るものなので、今までどおりネイティブでよい**
- **高いほうの直し方**: **`decay` と `survives` の2つを、自前の実装（IEEE-754 の四則だけで書いたもの）に置き換える**。**ただし arm64 では Go が FMA に融合するので、そこも `float64(x*y) + z` の形で止める必要がある**——**「全端末で同じ続きになる」を要件にするなら、その作業がまるごと要る**。**要件にしないなら、決定性は「1つの端末の中でだけ」と明記して進める**（**測定は今までどおり native の `cmd/experiment` なので、記録は無傷**）

### 順序

1. ~~**まず WASM で `cmd/devview` がそのまま動くか**~~ **（2026-09-19・完了。ビルドも、Windows 側の Chrome での実行も、コードを変えずに通った）**（`engine` は変えない。**動かないのはファイル I/O だけのはず**＝保存／読込のところ）
2. **測る**（上の5つ）
3. ~~**入力をタップ/長押しに**~~ **（2026-09-20・完了。`cmd/devview/touch.go`。キーとマウスは残っている）**——**タップで狙い、2度目で既定の行動、長押しでメニュー**。**engine は1行も変えていない**。**メニューは4つの質問（後継・求愛の返事・訊かれるモード・節目）にも使った**ので、**数字キーだけだったものが指で答えられる**。**判定は時間**（フレーム数だと遅い端末で長押しが1.5秒になり、実測でブラウザが1秒に4フレームしか描かない状況があった）
4. **描画をタイルに**（`ColorScale` と左右反転と manifest）
5. **「セーブのアップロード」の薄い API**（ここで初めて `server` の話が出るが、**今の `protocol` の設計とは別物**——**スナップショット1個の受け渡しであって、tick ごとの配信ではない**）

---

## 殴られて押される・死に方を覚える・仲間を殺した者への目・押すことを選ぶ（#136〜#139。ユーザの提起）【アイデア・2026-09-20 に立てた。**設計は実装前の数え上げで決める**】

> **2026-09-20 に、この節の3つの未決にユーザが答えました**（下の各項の「**決まったこと**」）。**13 は崖落ちを作る／15 は学習の置き場を新しい遺伝スロットにする／14 は「避ける」ではなく「戦いを動かす」側に置く**。**14 は答えの中身が項目そのものを大きくしたので、下の 3 を書き換えてあります。**

**ユーザからの3項目**（**1. 被弾ノックバック**／**2. 死因の記憶と知識伝承**／**3. 好感度の連動**）。**ユーザの「設計上の注意」をそのまま枠にする**——**既存の hunch・好意・伝承の拡張として作り、新しい推論エンジンや記憶システムを別建てで作らない**（これが決定事項#136〜#138 に共通する前提）。**この節は「何をしたいか」「既に何があるか」「壊してはいけないもの」「着手前に何を数えるか」だけ**で、**形は数え上げが決めます**（段階98 は分布を見てカーブの形が変わり、段階100 は候補の重みを見て効かせる先が変わりました）。

**この群の中の順序の提案: 1 → 3 → 4 → 2。**（**4 は 2026-09-21 に加わった4つ目＝「押すことを選ぶ」（#139）。1 を選択に上げるもので、2 が学ぶべき鍵を1つ実在させます。理由は下の 4 の節**）

- **1 が先なのは、2 が学びたいパターンを世界に作るのが 1 だから。** **「殴られている最中に水に入ると死ぬ」は、今の世界では規則性ではありません**——**水に入るのは自分で決めたことで、殴られていることとは無関係**。**ノックバックが入って初めて、その2つが相関のある出来事になります。** **1 を入れずに 2 を作ると、2 は「無い規則性を探す仕組み」になります。**
- **3 が 2 より先なのは、3 の半分がもう世界にあるから**（3-1 ＝段階31b。下記）。**実装ではなく測定から始められるので安く、しかも 2 と同じ「目撃の走査」（`engine/witness.go`）を触ります。**
- **2 が最後なのは、この群で一番大きく、一番「情報だけを足す規則」に近いから**（下記の前例3つ）。
- **2026-09-20 の3つの判断を受けても、この順序は変わりません**——**ただし 3 が一番大きい項目になりました**（「避ける理由1本」から「好意が誰と誰が戦うかを決める」へ）。**そして 1 と 3 は噛み合います**: **複数に囲まれた身体は押される回数も増えるので、水際・崖際での死は 3 が入ると増えるはず**。**測るときは 1 と 3 を別々に入れて、対で読むこと**（同時に入れると、どちらが個体数を動かしたか分かりません）。

### コードを読んで確かめたこと（2026-09-20。**着手前の数え上げとは別で、これは「今どうなっているか」**）

- **殴打は全員が動いたあとに解決され、溺死の判定はそのあと**（`engine/world.go` の `Step`: `perform` → `resolveAttacks` → `metabolise` → `drownings`）。**押し込んだ先の水は、同じ tick のうちに効きます**——**新しい順序を作らなくてよい。**
- **`resolveAttacks` はループの中で空間インデックスに訊く**（20tick に1回、`exchangeReadings` → `appendAgentsInSight`）。**`moveDir` は `invalidateIndex()` を呼ぶ**ので、**押す処理を素直にそのループの中に書くと、そのtickは殴打ごとにグリッドを作り直します**（`CLAUDE.md` のコーディング規約が名指ししている形）。**押し先を溜めてループの外で一度に適用する**のが素直で、**攻撃の解決順に結果が依存しなくなる副次効果もあります。**
- **`canStep` は段差を拒む**（`engine/terrain.go`。**下りも `from.Slope` が無ければ不可**）。**したがって「崖から落ちる」はノックバックが `canStep` を無視しないと起きません。** **水は段差ではないので、押し込むほうは素直に通ります**——**1 の本命は水で、崖は付け足し。**
- **hunch は生涯学習しない。** 経路は3つだけ——`drawHints`（建国者の抽選）・`inheritHints`（親から枠ごと。天才誕生でだけ1枠まるごと差し替わる）・`exchangeHints`（**空き枠にだけコピー**）。**「自分の経験から新しい hunch を作る」経路はこの世界に1つもありません。** **2-2 はその1本目になります。**
- **hunch の状況（`HintFeature`）は8つで、全部 `Perception` から来る。** **「水の上にいる」も「殴られている最中」もありません。** **そして特徴を1つ足すと `drawHint` の抽選が変わるので、規則をオフにしても世界が変わり、指紋テストの数値は全部動きます**（`CLAUDE.md` の `numActionKinds` と同じ罠）。**足すなら意図した変更として、同じコミットで指紋を更新する。**
- **好意は「負」を受け付けない。** `addAffinity`（`engine/memory.go`）は **`amount <= 0` を黙って捨てます**——**`Opinion` の設計そのもの**（Risk ＝「この相手に何を払わされたか」、Affinity ＝「これは自分の側か」）。**3-2 はここに正面から当たります。**
- **3-1（外敵討伐を見た者の好意）はもう実装されている**——段階31b の `witnessKill`（`engine/witness.go`）で、**分岐は「自分と同じ種族の者が、別の種族を殺したのを見た」**、重みは `AffinityWitnessKill`（既定 2）、計数は `avengeSeen`、腕は `noavenge` と `avengehigh`。
- **死の目撃という走査も、もう2つ乗っている**（`forEachWitness`）——**殺し**（段階31）と**溺死**（段階35 の `DrownWitnessLooks` 20 ＝**既定オン**）。**「死亡イベントが視界内に発信される」は新規ではありません。**
- **死の直前の状況は、死んだ身体がもう持っている**——`attackerID` / `lastAttackTick`（殴られた相手といつ）・`drowned`・立っている座標（＝そこの地形）。**したがって「殴られている最中に、水で死んだ」という鍵は、誰にも輪バッファを持たせずに `kill` の時点で組み立てられます**（2 の設計で一番効く逃げ道。下記）。

### すぐ数えられたこと（2026-09-20・**4シード × 20000tick**。**本番の数え上げは12シードで引き直す**）

| 指標 | baseline（平坦） | `river` | `countryskill`（遊ぶ地図） | 何に効くか |
|---|---:|---:|---:|---|
| `hintSlots` / `hintsHeld` | 1.82 / **1.81** | 2.04 / 1.98 | 2.31 / **2.31** | **2**（枠は埋まっている） |
| `hintCopyRate` | 0.96 | — | — | 2（今の伝承の発火） |
| `avengeSeen`（3-1 の発火数） | 12.67 | 5.59 | 4.05 | **3-1 の的の大きさ** |
| `killed` | 279 | 187 | 167 | 3-2 の的の上限 |
| `fights`（殴った tick の数） | 76699 | 42924 | 35250 | **1 の発火回数の上限** |
| `onWater` | 0.00 | 0.14 | 0.08 | 1（押し込む先がある世界か） |
| `drowned` / `drownShare` | 0 / 0 | 27.5 / 0.10 | 17.0 / 0.07 | **1 で動くはずの指標** |
| `drownSeen` | 0 | 8.80 | 8.27 | 2（目撃はすでに桁で少ない） |

**この表から先に言えること2つ。**

- **1 は平坦な世界では1つも仕事をしません**（`onWater` 0.00）。**測るのは水のある腕**（`river` / `countryskill`）**で、遊ぶ地図は 20000tick で崩壊することがある**（`TODO.md` の現在地）ので、**崩壊率と対で読む**。
- **2 の枠は空いていません**（買った枠 1.81〜2.31 に対し、埋まっている数がほぼ同じ）。**`exchangeHints` と同じ「空き枠にだけ入る」規則にすると、2-2 はほぼ一度も発火しません。**

---

### 1. 被弾ノックバック（#136。戦闘×地形の連動）

- **やりたいこと**（ユーザ原文の要約）: **殴られたら、打撃の重さ（攻撃力）に応じて攻撃者→被攻撃者の向きへ強制移動させられる。** **重い身体ほど押される距離が短い。** 狙いは「**殴られて水に押し込まれる → 溺死**」「**崖際で殴られて落ちる**」という戦術的な地形利用。
- **既にあるもの**: 殴打の解決と溺死判定の順序（上記）／水が「死ぬ場所・遅い場所・消耗する場所」であること（段階34・97・99）／**押された身体が考え直すトリガー**（殴られた・体力が減った）／**押された先の危険を効用式で読む経路**（`Perception.Self.Drown` → `Utility.Hazard`）。**つまり、押したあとに必要なものは全部ある。**
- **壊してはいけないもの**:
  - **移動ループの中でインデックスに訊かない**（上記。**押し先を溜めて、ループの外で一度に適用する**）
  - **効用式に「押されるかもしれない」という項を足さない。** **ノックバックは選べない結果として身体に起きること**（段階24 の隙・段階54 の怯えと同じ立場）。**避ける判断は、押されたあとに既存の `Hazard` が行う。**
  - **攻撃側有利を保つ**（`CLAUDE.md`）。**向きは同じ**（殴られる側のコストが上がる）**が、距離を取らせることは逃走の補助でもある**ので、**`fights` が減る向きにも働きうる**。両方見る。
  - **渡れない地形は作らない（段階20）の裏返し**——**押しが `canStep` を無視すると、歩いて行けない所へ暴力で行けるようになります**。**下りだけ許すのは筋が通るが、決定として書くこと。**
  - **ペアと子**（`PartnerID != 0` は `stepPaired` で連れ立ち、子は `keepToGuardian` で親の近くに留められる）。**押しがその拘束と矛盾したときの扱いを決める。**
- **着手前に数えること**:
  - **的の大きさ**（段階62 の式＝保有率 × 実効値 × 的の大きさ）＝**「押した先が水になる位置」で起きた殴打の割合**。**今の指標には無いので、計数を1本足して先に数える。** **`onWater` 0.08〜0.14 は上限の目安にしかならない**——**すでに水の中にいる身体を押しても新しいことは起きない。**
  - **押す距離の見当**: **1tick の歩幅（`speedAt`）と水域の幅の比**。**歩幅より短い押しは次の tick に歩いて戻れるので、たぶん何も変えません。**
  - **予想を書く**（測定前に）: `drowned` / `drownShare` が `river` と `countryskill` で上がる／`onWater` はほとんど動かない／**平坦な世界では1ビットも動かない**／`fights` の向き。
**決まったこと（2026-09-20・ユーザの判断＝決定事項#136）**

- **崖落ちを作る。** **押しは下りに限って `canStep` を無視する。** **上りは無視しません**——**暴力で高台に上がれるようにはしない**（段階20 の「渡れない地形は作らない」を、逆向きに壊さないため）。**落ちるのは一方通行で、戻るには坂を探すことになります。**
- **落ちたら追加ダメージ。** **落差（`height` の段数）に比例させ、殴打と同じ tick に、殴打の続きとして課金する。**
- **これで死因のバケツは1つも増えません。** `kill` は **`a.lastAttackTick >= w.tick-1` を戦闘死の条件にしている**ので、**落下で死んだ身体は自動的に戦闘死に入り**、`recentAttackers` に押した者がいるので **claim（段階41）も `witnessKill`（段階31）も既存のまま動きます。** **`CLAUDE.md` の「死因は排他」はそのまま。**
- **対して、押して溺れさせた死は `drowned` のバケツのまま**で、**`kills` にも `witnessKill` にも入りません**（`kill` は `drowned` を先に見る）。**つまり「押して溺れさせた者」は、14 の好意の連鎖には乗りません。** **乗せるかどうかは着手時に決める**（乗せるなら、溺死の witness 経路に「押した者」を渡す1行）。
- **落下ダメージの強さは「即死装置にしない」ことで決める。** **1発で死ぬ大きさにすると、崖は地形をやめて扉になります**（段階20 の線。飛行型に `FlyCost` を必ず払わせたのと同じ理由）。**目安は1打撃（`damagePerTick`）の何倍か**で、**落差1段ぶんが1打撃を大きく超えないところから始める。**

**追加で数えること（崖の側）**: **高台のあるマップ**（`country` / `countryskill`）**で、高台の縁からノックバック距離の内側で起きた殴打の割合**／**落差の分布**（1段が大半なら、段数に比例させる意味は薄い）。

#### 実装前に数えた（2026-09-20・4シード × 20000tick・`country` の地図。**この数え上げがこの段階の形を変えました**）

**数え方**: 一時のテストで、1tickごとに解決された殴打すべてについて、**殴られた身体を「攻撃者→被攻撃者」の向きに d だけ押したらどこに着くか**を引き、**そこが水か／歩いては降りられない段差の下か**を数えた（`canStep` が拒む降りだけを「落ちる」と数えた）。**押す距離は決め打ちの4通り**。

| | 1実行あたり | 殴打に対する割合 |
|---|---:|---:|
| 殴打（解決された打撃） | 12195 | — |
| 殴られた時点で既に水の中 | 404 | 0.033 |
| **押した先が水**（d=5 / 10 / 20 / 40） | **16 / 30 / 80 / 250** | **0.0013 / 0.0024 / 0.0066 / 0.0205** |
| **押した先が崖の下**（同上） | **774 / 1002 / 1327 / 1781** | **0.063 / 0.082 / 0.109 / 0.146** |
| 落差の分布（d=10） | **全部1段** | — |
| 1tickの歩幅 / セルの大きさ | **1.94 / 50×50** | — |

**この数え上げで、この段階の本命が入れ替わりました。** **崖は水の25〜50倍の的**です。**理由は地図の形**——**水は幅2セルの帯1本で、身体はそれを避けて暮らす**（水の中にいるのは殴打の3.3%）**のに対し、高台は食料と結びついた広い面で、その縁は長く、人はその上で暮らしている**。**「押して溺れさせる」は絵としては分かりやすいが、世界の中ではほとんど起きません。**

**そしてもう1つ、水のほうは的が小さいだけでなく、効き目も無い**: **溺死は tick あたり 0.0002**（`DrownChancePerTick`）なので、**押し込まれた身体が10tick 水にいても死ぬ確率は 0.2%**。**1実行に16〜250回の押し込みでは、増える溺死は0.03〜0.5体**で、**元から17〜27体ある溺死の中では見えません**。**つまりこの段階が世界を動かすなら、それは落下ダメージの側からです。**

**数えて決めたこと**: **`KnockbackDist` の既定の目安は 5**（**`CombatRadius` 15 の3分の1＝押しても取っ組み合いは続く**。**歩幅1.94 の2.5歩ぶん**）。**20 を超える押しは間合いを切る**ので、**別の腕（`knockfar`）として測る。**

#### 実装後の予想（2026-09-20・**測定前に書いた**）

**腕は4つ**——`knock`（`country` ＋ 押し5）／`knockfar`（同 ＋ 押し20）／`knocknofall`（同 ＋ 押し5・落下ダメージ0）／`knockflat`（平坦な世界 ＋ 押し5）。**対照は `country` と `baseline`。**

- **`knocked` は殴打とほぼ同数**（`knockShare` > 0.9）。**押しが効かないのは「重なって立っている」ときだけ。**
- **`knockFell` は1実行に 400〜800**（**上の数え上げの 774 より少ないはず**——**実際の押しは「landed damage ÷ AttackDamage」で割り引かれ、守りに入っている身体には満額入らない**）。**`knockWet` は 10〜20。**
- **`drowned` は動かない**（上の計算。**誤差の中**）。**これが外れて溺死が増えたら、押し込まれた身体が水から出られていないということ**で、そのときは `wetTicks` を見る。
- **`killed` は上がる**（落下が1実行に 1600 前後の体力を余分に取る＝殴打の総ダメージの2割ほど）。**`pop` は下がる**が、**誤差に隠れる可能性がある。** **`collapsed` は変わらない。**
- **`knocknofall` は `country` と区別がつかない**はず——**5の押し（2.5歩）は、食料のレースにも戦いの間合いにもほとんど影響しない**。**これが対照の役目で、もし差が出たら「効いているのは落下ではなく位置の乱れ」という別の話になる。**
- **`knockfar` は `fights` を1割以上減らす**（**押し20 > `CombatRadius` 15 ＝毎回間合いが切れ、攻撃側は歩いて戻る**）。**`killShare` は下がる。** **落下は増えるので、`pop` への符号は読めない**——**ここは予想を立てない。**
- **`knockflat` は誤差の中**（**押し込む先の地形が無いので、残るのは位置の乱れだけ**）。**これが外れたら、位置の乱れそのものが世界を動かしているということ。**
- **見た目**（`cmd/devview`）: **殴り合いが少しずつ場所を移していく**。**高台の縁での戦いは、片方が落ちて終わる。**

#### 測定（2026-09-20・12シード × 20000tick）——**予想は向きからして外れ、この項目は地形の規則ではなくなった**

| | `country` | `knock`（押し5） | `knocknofall` | `knockfar`（押し20） |
|---|---:|---:|---:|---:|
| `pop` | 52.0 | **114.6（+62.6 \*\*\*）** | 124.8 | 123.1 |
| `collapsed` | 0.33 | **0.00** | 0.08 | 0.00 |
| `killShare` | 0.64 | 0.53（−0.11 \*\*\*） | 0.47 | **0.33** |
| `knockFell` / `knockWet` | 0 / 0 | **112 / 44** | 100 / 41 | 157 / 133 |
| `dPower` | −1.2 | **−17.4（−16.2 \*\*）** | −19.3 | −22.5 |

**平坦な世界（水も崖も無い）**: `killShare` 0.81 → **0.68 \*\*\***、`fightRate` 524.9 → **382.0 \*\*\***、`pop` +19.2 \*、**`knockWet` も `knockFell` も 0**。

- ❌ **「個体数は下がる」**——**倍以上になった**（崩壊した実行が3分の1あったのが0に）。
- ❌ **「効いているのは落下」**——**落下ダメージを0にした対照と区別がつかない**。崖落ちは1実行に112回しかない。
- ❌ **「溺死は動かない」**——**増えたが、押し込みのせいではない**（`knockWet` 44回では説明がつかず、`onWater` は不動。**個体数が2.2倍になったぶん**）。
- ✅ **`knockShare` は 0.95〜0.96**（効かないのは重なって立っているときだけ）。

**起きていたのは「殴られた側が毎tick 間合いの外へ出て、攻撃側が詰め直しに体力を払う」ことで、これは攻撃側有利を削る**（押された側は移動コストを払わない）。**したがって `NODE.md` にも戦闘の節（7-4c）として書いた。** **既定はオフのまま**——**個体数を倍にする規則を既定にすると、ベースラインのリセット4度目になる。** **詳細と数値は `HISTORY.md` の 2026-09-20。**

- **決めること（未決）**: 押しが `canStep` を無視するか（崖落ち）／**投擲（`at.thrown`）で押すか**（**離れた一撃なので既定では押さないのが素直**）／**溺死を誰の手柄にするか**（`killed` と `drowned` は排他なので、**押して溺れさせた死をどちらに計上するかで `killShare` の意味が変わる**）／**押された身体が移動コストを払うか**（**払わないのが素直**——自分で動いていない）／**重さの代理変数**（**`MaxVitality` の実効値**が素直。**`CLAUDE.md` の作法どおり予算比で読む**）。

---

### 2. 死に方を覚える（#137。エピソード → パターン → 伝承）

- **やりたいこと**（ユーザ原文の要約）: **(2-1)** 死亡イベントが視界内に発信され、目撃者は死の直前 N tick の出来事列を記録する／**(2-2)** **同じパターンを2回以上見たら、エピソードからパターンへ昇格して hunch 枠へ**（鍵は上位2〜3要素。「誰に」は捨てる）／**(2-3)** 既存の伝承経路で拡散／**(2-4)** 既存の hunch → 効用の加点でのみ働く（**新しい推論系は作らない**）。
- **既にあるもの**: **目撃の走査**（`forEachWitness`。殺しと溺死の2つが既に乗っている）／**伝承経路**（`exchangeHints` が `observe` に相乗り）／**加点だけで働く規則**（`hintFeatures.score`。**分岐はどこにも無い**＝2-4 はこの線をそのまま守っている）。
- **この項目の設計の中心は「置き場」です。** **hunch の枠は買ったぶんが全部埋まっており**（上の表）、**追い出しの論理は hunch には無い**（あるのは相手ごとの記憶のほう＝`engine/memory.go`）。**ユーザの「使われない記憶は既存の枠追い出しロジックで自然に風化」は、hunch についてはまだ無いものを指しています。** **選択肢は3つ**——**(a) hunch に追い出しを作る**（**何を基準に捨てるか＝この世界が一度も決めていないこと**。相手ごとの記憶は「リスクも好意もゼロのレコード」を捨てる）／**(b) 学習した規則に別の枠を持たせる**（**予算を食わせるなら保存則は守れる**が、**「枠が2種類ある」という構造が増える**）／**(c) 空き枠にだけ入れる**（**今の `exchangeHints` と同じ。ほぼ発火しないと分かっている**）。
- **常時メモリは要らないかもしれない（一番効く逃げ道）**: **死んだ身体は、死の直前の状況をもう持っています**（`attackerID` / `lastAttackTick` / `drowned` / 立っている座標＝地形）。**「殴られている最中に、水で死んだ」という鍵は `kill` の時点で組み立てられる**ので、**全員に輪バッファを持たせる必要はありません。** **2-1 の「出来事列」を作る前に、この形で足りるかを先に確かめる**（足りないのは「Aに殴られた**あとで自分から**水へ移動した」のような、**死んだ身体自身の行動の並び**だけ）。
- **先に読むべき前例（この項目が一番踏みやすい罠）**:
  - **段階35**——**溺死の目撃 → 地域の危険の信念は既定オンで入っており、`onWater` を1つも動かさなかった**。**「危ないと学べる」と「行動が変わる」は別。**
  - **段階31・49**——**ただの情報は世界を動かさない**（`docs/features/` と `HISTORY.md`）。**先に的を数え、「情報だけが無い」対照を対で回す。**
  - **段階12c**——**hunch は何も決めない。** **2-4 はこれを守っているので、そこは安全。**
- **着手前に数えること**:
  - **的の大きさ**: **1実行に死は何回・そのうち目撃されたのは何回・同じ鍵が同一個体の生涯に2回以上起きるのは何回か。** **`killed` 167〜279・`drowned` 17〜27.5・`starved` 49〜63 に対し、目撃は `drownSeen` 8.3〜8.8・`avengeSeen` 4〜13**——**すでに桁で少ない。** **「2回目で昇格」はさらにその中の話なので、ここを数えないと規則を書く値打ちが決まりません。**
  - **鍵の数**: **`hintKinds` は今 25 前後**（集団全体で生きている〈状況×行動〉の組の数）。**鍵を増やすと、同じ枠数でばらけて、どの鍵も証拠が溜まりません。**
  - **エピソード列を持たせる場合の値段**: **1体あたりのバイト数と `BenchmarkEngineStep` を前後で。**
- **壊してはいけないもの**: **hunch は何も決めない**（加点のみ）／**予算の保存則**（**枠を増やすなら予算を食う**。**学習でただで枠が増えるのは `Endow` / `Inspire` と同じ例外になる**）／**記憶容量の役割を重ねない**（**相手ごとの記憶と同じ枠から取るのか別に買わせるのか**。**#41 が「地域の信念は人の記憶の枠を食わない」と決めた前例がある**）／**指紋**（**`HintFeature` を足すと、規則をオフにしても動く**）。
**決まったこと（2026-09-20・ユーザの判断＝決定事項#137）**

- **学習したパターンは hunch の枠と共用しません。** **新しい枠を作り、その枠数は予算から買う遺伝のスロットにします**（ユーザの指定＝「遺伝子に記憶力という新たなスロットにしたい」）。**上の (a)(b)(c) のうち (b) で決着**——**hunch に追い出しを作る必要はなくなり、`exchangeHints` も1行も変わりません。**
- **形の前例は `hintSlots` です。** **9遺伝子の外にある整数で、`fitBudget(genome, budget - hintCost(slots))` で同じ予算を食い、親から枠ごと継がれ、天才誕生でだけ1つ増える。** **同じ形をもう1本作ればよく、新しい概念は1つも要りません。**
- **名前は分けます。** **`GeneMemory`（記憶容量）が既にあり**、**それは「覚えていられる人数と、取り込み・忘却の速さ」**（`NODE.md`）。**`CLAUDE.md` の「能力の役割を重ねない」があるので、新しい枠は「人についての記憶」ではなく「出来事から作った規則の置き場」と定義し、コード上の名前も別にします**（日本語で両方を「記憶力」と呼ぶのは文書の用語の問題なので、`PARAMETERS.md` で呼び分けを1行書く）。
- **既定は0枠。** **枠を買えばそのぶん9遺伝子が痩せるので、既定を0にしないと指紋が動きます。** **0で入れて、測ってから既定を決める**（`HintSlots` が 4 なのと同じ扱い）。
- **予算の保存則は守られます**——**枠は買うものなので、学習でただで能力が増えることにはなりません。**

**追加で数えること（枠の側）**: **枠1つの値段**（`HintSlotCost` 5 が前例。**高すぎると誰も買わず、安すぎると全員が買って選択にならない**）／**枠を買った個体の割合と、その枠が実際に埋まる割合**（**hunch は買った枠がほぼ全部埋まっている**＝1.81/1.82 なので、**同じことが起きるなら「昇格の条件（2回）」のほうが効いていない**という読みになる）。

- **決めること（未決）**: 学習した hunch の置き場（上の (a)(b)(c)）／**`HintFeature` を足すか**（足すなら指紋のリセットを伴う）／**エピソード列を全員に持たせるか、死の瞬間に組み立てるか**。

---

### 3. 好感度が戦いを動かす（#138。討伐で上がり、身内殺しで下がり、**好意が誰と誰が戦うかを決める**）

- **3-1（外敵討伐による好感度上昇）はもう世界にあります。** 段階31b の `witnessKill`、重みは `AffinityWitnessKill`（既定 2）、計数は `avengeSeen`、腕は `noavenge` / `avengehigh`。**したがってここに残っているのは実装ではなく測定と調整**で、**発火は1実行に 4〜13 回**（`killed` 167〜279 に対して）。**効きを強めたいなら、増やすべきは重みではなく発火回数**——**それは「人間が外敵を殺す回数」の話なので、この項目ではなく別の段階の仕事です。**
- **3-2（身内を殺した者への好感度の大幅下落）は構造に当たります。** **`addAffinity` は `amount <= 0` を捨てる**ので、**既存の台帳では表現できません**。**選択肢は3つ**:
  - **(a) 好意を負にできるようにする。** **一番素直に読めるが、一番広く当たる**——`HintTrust` は `Affinity/AffinityTrust` を 0..1 にクランプして読み、贈与・売買・伝承・血縁（`AffinityKin` 22）が同じ数を読んでいます。**負の好意が何を意味するかを、その全部について決めることになります。**
  - **(b) `Risk` に書く（避ける理由）。** **`CLAUDE.md` の「恨みの状態を持たない」が名指しで許しているのはこの形**——「過去に払わされた体力は『避ける理由』として効用から引く側にある」。**ただし `Risk` の単位は体力**なので、**「死んだ相手への好意 × 係数」を体力に換算する根拠を先に書くこと**（段階77 の教訓＝**物差しは1本にする**）。
  - **(c) `Opinion` に3本目を足す。** **「3つで足りている」と明記してある**ので、**覆す判断として記録する。**
- **どれを選んでも復讐動機にはなりません。** **効用式に「その相手を殺すと良いことがある」という項は無く**（攻撃は食料競合の先制排除としてのみ値付けられている）、**(b) は「避ける」を強めるだけ**です。**ユーザの「復讐動機の下地」という位置づけはそのとおり**——**下地であって、動機そのものはまだ世界にありません。** **動機まで作るなら、それは `CLAUDE.md` の「恨みの状態を持たない」を覆す判断**で、別に記録します。
- **着手前に数えること**: **「自分が好意を持っている相手が、見ている前で殺される」は1実行に何回起きるか**（**今どの指標も数えていません**。`avengeSeen` 4〜13 と同じ桁なら、この規則は書いても世界を動かしません）／**好意の分布**（`friends` と、段階92 が見つけた飽和＝`AffinityKin` 22 > `AffinityTrust` 20 なので**親子には `trustBought` がゼロを返す**）／**対照**（**同じ殺しを見て、被害者に好意が無い場合**との対＝**比例していることの確認**）。
- **壊してはいけないもの**: **好意の発生源は実イベントだけ**（#19。**3-2 はその条件を満たしている**）／**恨みの状態を持たない**（**(b) なら守れる。(a)(c) も「避ける以上のことをさせない」なら守れる**）／**記憶は有限**（**加害者を覚えるのに枠が1つ要る**。**空きが無ければ何も学ばないのが既存の作法**＝`rememberAffinityIfRoom`）。

**決まったこと（2026-09-20・ユーザの判断＝決定事項#138）。この判断は項目の形を変えました**——**「避ける理由を1本足す」から、「好意が誰と誰が戦うかを決める」へ。**

**ユーザの指定（4つ）**

- **(i)** **好感度の低い相手が、自分の狙っている食べ物を狙っていたら、戦いになりやすくする。**
- **(ii)** **好感度の高いノードが好感度の低いノードと戦っていれば、その戦いに加担する。**
- **(iii)** **加担してくれたら好感度が上がる。**
- **(iv)** **加担する理由は、先読みによる好感度狙い**（＝「敵の敵だから」ではなく「加担すると好意が買えるから」）。
- **(v) 狙う帰結**: **好感度が下がり切ったノードは、複数との戦いになりやすくなる。**
- **(vi)** **自分が狙っていた食べ物を他人に取られたら、その相手への好意が下がる**（2026-09-20・同日の追加。**ユーザの「好感度が一定より高いノードが自分と同じ食べ物をとったときは好感度が下がる」を、閾値なしの形に直したもの**）。**そして下がった先は 0 では止まりません**——**同日の判断で好意はマイナスを許します。**

**この項目で入れる規則の一覧（6つ。**既定はすべてオフ＝0 から始める**）**

| | 規則 | どこに書くか | 既にあるか |
|---|---|---|---|
| (i) | **殴ることは好意を失うこと** | `scoreFight` の項を1本（`trustBought` の鏡＝`trustLost`） | **無い**（`scoreFight` に好意は1つも入っていない） |
| (ii) | **好きな相手の戦いに加担する** | `help()` が既に勝ち目に効いている。**足すのは動機**＝(iv) | **機械は在る**（段階32） |
| (iii) | **加担すると好意が上がる** | `AffinityAlly` を1本（`AffinityHunt` 6 と同じ桁から） | **無い**（`AffinityHunt` は死体を分けた者だけ） |
| (iv) | **加担の動機は買える好意** | `scoreFight` の `Lore` 項に「いまその相手を殴っている友1人ごとの `trustBought`」 | **形は在る**（段階96 の `AllyValue`、既定0） |
| (vi) | **取られたら好意が下がる** | `eat` の中で1回。**狙っていた者にだけ、乱数を引かず** | **無い**（今は「損をさせられた」を好意側に書く経路が1つも無い） |
| (a) | **好意はマイナスを許す** | `addAffinity` の `amount <= 0` ガードと、上の「継ぎ目3つ」 | **無い**（負は黙って捨てられている） |

**(vi) の形（2026-09-20 に決めたところまで）**

- **起点は実イベントだけ**（#19）。**「近くにいる」「レースになっている」では減らしません**——**それだと豊かで混んだ場所では全員の好意が減り続けます**（`RivalID` は「その品に一番近い他人」で、**狙っているかどうかは見ていない**＝下の「問い1」の答え）。
- **素直な起点は「自分が `ActEat` の対象にしていた品を、他人が食べた瞬間」。** **`eat` の中で1回**、**その品を `Action.TargetID` にしていた生きている身体にだけ**、**乱数を引かずに**。**`Action` はもう世界が持っているので、新しい状態は1つも要りません。**
- **量は「その食事がその身体にとって何だったか」に比例させるのが素直**（**空腹の身体ほど深く恨む**）。**ただし単位が違うので、まずは定数1本から始めて、比例は測ってから**（段階77＝物差しは1本にする、の側で慎重に）。
- **(i) と噛み合って、ユーザの狙い（v）が自動的に出ます**: **取られるたびに好意が減る → 0 を割る → 好意を失うコストが無くなる（負なら、殴ることが好意の面でも損でなくなる）→ 同じ相手との同じレースが、今度は戦う値打ちを持つ → しかも (ii) で友が加担する。** **袋叩きは規則ではなく、3本の量の帰結として現れます。**
- **「盗られ得」にならないこと**——**(vi) だけを入れると、好意が下がるだけで何も起きません**（**段階35・49 が5度測った「ただの情報は世界を動かさない」の形**）。**(vi) は (i) と対で入れ、対で測ること。**

**(vi) の着手前に数えること（新しい計数が1本要ります）**

- **「自分が `ActEat` の対象にしていた品を、他人に食べられた」は1実行に何回あるか。** **今どの指標も数えていません。** **`fights` は平坦な世界で 76699 あるので、レース自体は絶えず起きているはずですが、「狙いを定めていた品を横から取られた」はその部分集合**で、**ここが小さければ (vi) は世界を動かしません。**
- **そのうち相手が「好意のある相手」だった割合。** **`friends` は1体あたり 2.73**（`remembered` 3.59）なので、**大半は他人が相手のはず**——**その場合 (vi) が実際に減らすのは「もともと0の好意」で、負へ押し下げる働きのほうが本体になります。**
- **1回あたりの減りの目安**: **`AffinityGift` 6 / `AffinityHunt` 6 / `AffinityWitnessKill` 2 / `AffinityTrust` 20** と並べて、**「何回取られたら、贈り物1回ぶんが帳消しになるか」で決める。**

**(vi) の予想（測定前に書くこと）**

- **`friends` は下がる**（好意が減る経路が1本増えるので）。**そして 0 を割った相手が出る**ので、**新しい指標が要ります**——**負の好意を持たれている身体の割合**と、**1体あたりの一番低い好意**。
- **`fightRatio`（仲間 ÷ よそ者）は上がる**（**(vi) 単独では、仲間がよそ者に変わっていくだけ**なので）。**(i) と一緒に入れると下がるはず**——**この符号の反転が、2本が噛み合っている証拠になります。**
- **`pop` は (vi) 単独ではほとんど動かない**（**好意は下がるが、それを読む項が (i) しかない**）。**動いたら、それは休息の安心・贈与・加担のどれかが下側のクランプ越しに効いているということなので、そのときは継ぎ目の3つを疑う。**

**これが前回の「復讐動機が無い」への答えになっています。** **恨みという状態を1つも作らずに、`CLAUDE.md` の「攻撃は食料競合の先制排除」という値付けのまま、「みんなに嫌われた身体は袋叩きに遭う」が出てくる形**——**動機は全部、既存の量（好意・食料の取り合い・勝ち目）です。**

**既にあるもの（4つのうち、新しいのは2つ半だけ）**

- **(ii) の機械はもう世界にあります**（段階32）。`allyForce` / `AgentView.DeclaredFor` / `help()` / `hoped()` / `AllyTrustWeight`（既定 1 ＝**オン**）／`AllyPreyOnly`（既定 false ＝**同種どうしの喧嘩でも味方は数えられる**）。**「友が戦っている相手を殴ると勝ち目が上がる」は、すでに `scoreFight` の `pWin` と `exchange` に入っています。** **足りないのは動機（iv）と報酬（iii）。**
- **(iv) の形も段階96 が持っています**——`AllyValue` / `AllyFlat`（**既定はどちらも0**）。**「好意そのものを欲しがる」を、`LoreValue + AllyValue/(1 + 近くの信頼)` という減衰つきの形で入れてある**ので、**加担の効用に載せるのはこの上。**
- **(iii) の隣も既にあります**——`AffinityHunt` 6 は**一緒に死体を倒した者どうしに払われる**。**喧嘩には払われません。** **(iii) はその喧嘩版1本。**
- **新しいのは (i) です。** **`scoreFight` には好意が1つも入っていません**（Life / Stake / Rival / Risk / コスト。**抑止は `Risk` ＝「この相手に払わされた体力」だけ**）。**つまり今の世界は、仲の良い相手とも同じ確率で食料を争います。**

**形の候補（着手時に1つ選ぶ。物差しは1本にする＝段階77）**

- **(i) は「失う好意」で払わせるのが素直です。** **贈与が `trustBought` で「買える信頼」を得ているのの裏返しで、殴れば信頼を失う**——**同じ物差し（`AffinityTrust` で割った 0..1）を、符号を変えて使うだけ。** **`CompetitionWeight` や `Rival` の係数をいじるより、足す場所が1つで済みます。**
- **(iii) は `AffinityAlly` を1本足す**（`AffinityHunt` と同じ桁から始める）。**払う相手は「助けられた側 → 助けた側」の一方向か、双方向か**を決める（`AffinityGift` は両方向、`AffinityHunt` も両方向）。
- **(iv) は `scoreFight` の `Lore` 項に、`trustBought(その友の好意, AffinityAlly)` × 既存の goodwill の値打ちを足す**——**その相手をいま殴っている友1人ごとに。** **`hoped()` が「まだ頼んでいない者」を数えているのと対で、こちらは「もう殴っている者」を数えます。**

**着手前に数えること（2026-09-20 に一部は数えた。4シード × 20000tick）**

| 指標 | baseline（平坦） | `countryskill`（遊ぶ地図） | 何に効くか |
|---|---:|---:|---|
| `killShare`（死のうち戦闘死） | **0.82** | 0.67 | **(i)(ii) を入れる前に、この世界は既に暴力的** |
| `packSize` | 2.33 | 1.54 | (ii) が動かすはずの数 |
| `callShare` / `joinShare` | 0.04 / 0.03 | 0.03 / 0.02 | **加担の機械が今どれだけ発火しているか** |
| `fightCompanion` / `fightStranger` | 9.6 / 11.8 | 20.1 / 16.5 | (i) が動かすはずの比（`fightRatio` 0.81 / 0.99） |
| `friends` / `remembered` | 2.73 / 3.59 | 2.77 / 3.80 | 好意を持つ相手の数＝(i)(ii) の的 |
| `memFull` | **0.99** | 0.85 | **記憶はほぼ常に満杯**——(iii) の報酬を受け取る枠が無い |
| `pop` / `collapsed` | 127.5 / 0.00 | 90.5 / 0.25 | **崩壊しないことの確認** |

**まだ数えていない、一番大事な1つ**: **「好意の高い相手が、好意の低い相手と戦っているのが見えている」決定は、1実行に何回あるか。** **`joinShare` 0.02〜0.03 はそれに近いが、好意の条件が入っていません。** **ここが小さければ、(ii)(iii)(iv) は書いても世界を動かしません**（段階24 の教訓＝**発火回数が説明できることの天井**）。

**予想を先に書くこと（測定前に）**: `fightRatio`（仲間 ÷ よそ者）は**下がる**はず——**(i) が仲間との喧嘩を減らし、(ii) がよそ者への喧嘩を増やす**ので、**両側から同じ向きに動きます**。**`packSize` は上がる。** **`pop` は下がる**（暴力が増えるので）——**どれだけ下がるかがこの項目の可否**。

**壊してはいけない線**

- **個体数。** **`CLAUDE.md` が名指ししているのはまさにこの組み合わせです**——「**攻撃側有利を保つ。ただしこれと先制排除が噛み合うと世界が過度に暴力的になり個体数が崩壊しうる**」。**(i) と (ii) はその両方を増やす向き**で、**`killShare` は平坦な世界で既に 0.82**。**`pop` / `collapsed` / `peak` を、能力の指標より先に読む。**
- **閾値を作らない。** **「好感度が X 以下なら殴る」ではなく、効用の係数として入れる**（**そうすれば「嫌いな相手でも、腹が満ちていて勝ち目が薄ければ殴らない」が自動的に出ます**）。
- **恨みの状態を持たない。** **(i)〜(iv) はどれも恨みではありません**（好意という既存の正の量と、先読みの利得）。**(iv) の理由づけを「加担すると好意が買えるから」に保つこと**——**「敵の敵だから」にした瞬間、それは恨みの裏返しになり、この原則を破ります。**
- **好意の発生源は実イベントだけ**（#19）。**(vi) はその原則を負の側へ対称に延ばすもの**——**「取られた」という出来事が起点で、近くにいることや競合していることでは減りません。** **#19 が禁じたのは「共在時間で加点する」ことで、(vi) はその鏡像には当たりません。**
- **記憶は有限**（`memFull` 0.99）。**(iii) の報酬を受け取るには枠が1つ要る**ので、**満杯の身体は加担しても何も得ません**。**`rememberAffinityIfRoom`（空きが無ければ諦める）と `rememberAffinity`（追い出してでも記録する）のどちらを使うかで、この規則の発火回数が変わります。**

~~**残る1点（未決。ユーザの判断が要ります）**~~ **決着（2026-09-20・同日・ユーザの判断）: (a) ——好意はマイナスを許す。**

**追記（2026-09-20・ユーザの3つの問いから）**

**問い1「『仲の良い相手とも同じ確率で食料を争う』とはどういう意味か。同じ食料を狙っていることを検知して戦いになるのか」への答え——検知しているのは意図ではなく距離です。**

- **`FoodView.RivalID` は「その品に一番近い、視界内の他人」**（`engine/perception.go`）。**その相手がその品を狙っているかどうかは見ていません**——**相手の意図は隠しパラメータなので、見えるのは位置だけ。**
- **そこから `pGet = raceChance(距離の比)` が出て、`gap = その食事の値打ち × (1 − pGet)`**＝**「レースに負けるぶん」**。**これが `bestFoodGap` で、その相手を殴る候補の `Stake` に載ります**（Chance は `pWin`）。
- **戦いになるかどうかは自動ではありません。** **上がるのは「殴る」候補の点だけ**で、**Life（自分が失う体力）・移動と戦闘のコスト・`Risk`（その相手に払わされた体力の記憶）と比べて勝ったときにだけ殴ります。** **閾値はどこにもありません。**
- **そして `scoreFight` には好意が1つも入っていません。** **だから「同じ距離・同じ強さ・同じ飢え」なら、友でも他人でも点は1ビット同じ**——これが「同じ確率で争う」の意味です。**今の世界で友を殴りにくくしている唯一のものは `Risk`**（＝**過去に殴り合ったことがある相手**）で、**仲が良いことそのものは1つも効いていません。**

**問い2「先読みで、下がる好感と得られる延命を天秤にかけられないか」への答え——かけられます。しかも半分はもう在ります。**

- **効用式は既に「この手が将来買う好意」を値付けています**——`ActObserve` の `Lore` 項（`LoreValue × trust`）と、贈与・売買の `trustBought`（**贈ったときに買える信頼の増分**）。**同じ物差しの負の側を `scoreFight` に足せばよく、新しい概念は要りません**（`trustBought` の鏡＝`trustLost`）。
- **`Stake`（勝てば食べられるぶん）と同じ Utility の中で引き算されるので、天秤はそのまま成立します。** **飢えていれば食事が勝ち、満ちていれば好意が勝つ**——**これが「閾値を作らない」の形。**
- **1つだけ補足**: **2段目の窓（`LookaheadHorizons`）に好意は載っていません**（持ち越すのは体力と空腹だけ＝段階67・72）。**なので天秤は「同じ1回の採点の中」で取ります。** **好意を2段目に持ち込むのは別の話で、今は要りません。**

**問い3「好感度が一定より高いノードが自分と同じ食べ物を取ったときは、好感度は下がるが戦いにはならない、とするとどうなるか」への答え——狙いは正しく、形だけが原則に当たります。**

- **そのままだと閾値が2つ入ります**（「一定より高い」「戦いにはならない」）。**`CLAUDE.md` の「ハードコードされた行動閾値を作らない」に真正面から当たり**、**しかも段階25 で同じ形を1度剥がしています**（「腹が減っていたら求愛しない」という閾値は、**効用式から抜けていた項の代わりだった**）。
- **同じ振る舞いは、問い2 の形で閾値なしに出ます。** **好意を失うコストが食事の値打ちを上回っているあいだ、その相手とは戦いになりません**——**「一定より高い」は閾値ではなく、2つの量の大小として現れます。**
- **そして「取られたら好感度が下がる」のほうは、独立した1本として入れる値打ちがあります**——**今の世界に「この相手のせいで損をした」を好意の側に書く経路が1つも無い**（`Risk` は殴られたときだけ）ためで、**これが (a)〈マイナスを許す〉の主な使い道になります**。**規則 (vi) として上の一覧に入れました**（起点・量・数えること・予想はそちら）。

**マイナスを許すときの継ぎ目（コードを読んで見つけた3つ。どれも小さいが、放っておくと規則が逆に働きます）**

- **記憶の追い出しが、恨んでいる相手を真っ先に捨てます。** `weakestOpinion` は **`decay(Risk) + decay(Affinity)` を「価値」として一番小さいものを捨てる**ので、**好意が負のレコードは価値が負＝最優先で捨てられる**（しかも `worstWeight > 0` の判定を通るので、任意の記録として捨ててよいと見なされます）。**直しは「絶対値で量る」1行**ですが、**意図して決めること**——**恨みは薄れて忘れられるべきか、覚えているべきか。**
- **`trustBought` の下側のクランプ。** **`clamp(aff/T, 0, 1)` なので、好意が負の相手には贈り物が「信頼を1つも買わない」と評価されます**（実際には `Affinity` は加算されるのに）。**つまり嫌われた身体は贈与で関係を戻せません**（正確には、**誰もその贈与を選びません**）。**仲直りの経路を残すなら、ここの下端を負まで開ける。**
- **`addAffinity` の `amount <= 0` ガード**（**負を捨てている当の1行**）と、**記録の取り方**。**`memFull` は 0.99** なので、**`rememberAffinityIfRoom`（空きが無ければ諦める）で書くと、恨みはほとんど記録されません**。**殴られたときと同じ `forced` の側にするのが素直**です（**「殴られたことを、覚えないという選択はできない」**という既存の理屈がそのまま当てはまります）。
- **一方で、負を許すこと自体は世界を1ビットも変えません**——**今ある読み手は全部 `clamp(…, 0, 1)` で下を止めている**（`HintTrust`・味方の信頼・休息の安心・`trustBought`・`saleGoodwill`）。**変わるのは「負の側を読む項を足したとき」だけ**なので、**(a) を入れる変更と、それを読む変更は別のコミットにでき、前者は指紋を動かしません。**


---

### 4. 押すことを選ぶ（#139。ノックバックを行動の語彙に上げる）【アイデア・2026-09-21 に立てた。**形は実装前の数え上げで決める**】

- **やりたいこと（ユーザ原文の要約・2026-09-21）**: **ノックバックを「起きること」から「選ぶこと」に上げる。** 動機は2つ——**(a) 逃げる確率を上げたいとき**（押して間合いを切る）／**(b) 相手の背後が水や崖で、押せば大きな損を与えられると知っているとき**。**そして (c) その「知っている」ことが、好意を生みながらノードからノードへ伝わること。**

**決まったこと（2026-09-21・ユーザの判断＝決定事項#139）。この判断は項目を小さくしました**——**新しい語彙を1つも作らずに済みます。**

- **(1) `ActShove` は作らず、`Stance` の4つ目にする。** **帰結として、この手はヒントで指せません。**
- **(2) 押した結果は覚える価値がある**（ユーザ原文「押して逃げれたとか押されて逃げられたは記憶として価値がある」）。**したがって学習の置き場はヒントではなく `lore`**（`engine/lore.go` の belief。`retaliation` / `accept` と同じ形＝**実イベントから更新される、上限つきの走る平均**）。**そしてこれは (c)〈知識が好意を生みながら伝わる〉を、ヒントより素直に満たします**——**`exchangeLore` は既定オンで、`moved × AffinityLore`（30）が双方に入る**。**「押しは効く」という知識は、教えると好意になる。**
- **(3) 押しにも防御と回避がきく。** **受動ノックバック（13番）では既にそうなっています**——`noteShove` に渡る damage は**回避の判定を通り抜けたあと**、**防御（`to.defence`×落ち着き）と `wardAgainst` を引いたあとの値**。**選べる押しも同じ2つの関門を通す**: **回避＝そもそも押されない**、**防御＝押される距離が減る。**

**この3つで設計がどう変わるか（実装前に効いてくる4点）**

- **語彙は増えず、`numActionKinds` は19のまま**＝**`drawHint` の抽選が動かない**（ヒント側の指紋の罠が消える）。**代わりに `NumStances` が 3→4。** **ただし `pick` は候補1つにつき乱数を1つ引く**ので、**4つ目を常に採点すると規則がオフの世界でも指紋が全部動きます**——**`ShovePush > 0` のときだけ候補に足す**。
- **押す力は damage とは別のチャネルにする。** **押しの構えは殴るを捨てるので、距離を「入ったダメージ」に比例させたままだと押しの構えほど押せない**という逆向きの式になります。**`channels` に4本目（押す）を足し、距離は `ShovePush ×（その構えの押しの使用率）×（1 − 相手の防御 × 相手の構えの受け）× 基準の大きさ ÷ 相手の最大体力`**。**回避は damage と同じく「そもそも当たらない」側。**
- **既存の3つの構えは押しを0にする**——**受動ノックバック（`KnockbackDist`）と選べる押し（`ShovePush`）が別の規則として並び、段階13 の既定オフを動かさずに済みます。**

**(2) のために新しく決めることと数えること**

- **「押して離れられた」の定義を先に決める**（決めないと belief が何の割合なのか言えない）。**候補は2つ**: **押したあと N tick、距離が `CombatRadius` を超えたままだった**／**押した側が次の決定で戦い以外の手を選べた**。
- **belief に証拠が入る回数を数える。** **`retaliation` は1つの交戦につき1回しか観測しない**（長い一方的な殴打から答えを取らないため）。**押すたびに1回にすると `LoreMemory`（上限）の効き方が変わります。**
- **両側から同じ1つの数に証拠が入る**（押した側＝離れられたか／押された側＝相手に離れられたか）。**2つの入口を持つ belief はこれが初めてなので、片側だけの腕を対照に取る。**

**先に3つ、コードを読んで確かめたこと（2026-09-21）**

- **(c) はもう全部あります。** `exchangeHints` が写したヒントの数は `HintTradeWorth`（0.5）倍で `exchangeLore` の `moved` に入り、**`moved × AffinityLore`（30）が双方に好意として記録されます**（`engine/lore.go`。**既定オン**）。**「知識が伝わる」も「教えると好意が得られる」も、新しく作るものは1つもありません。** **しかも `AffinityLore` は1〜30でスイープ済みで、個体数も友の数も雑音を超えて動きません**——**効くのは1回の値打ちではなく回数**（`config.go` のコメント）。**したがって (c) に足すべきものは好意の経路ではなく、伝える中身の語彙だけです。**
- **押す機構もあります**（`engine/knockback.go` ＝13番）。`noteShove` / `applyShoves` / `canBeShoved`（**下りだけ段差を無視**）。**足りないのは「誰がいつ押すか」だけ。**
- **ヒントは行動の種類にしか付きません**（`hintFeatures.score(hints, kind)`）。**押しを `Stance` の4つ目にすると安く済みますが、その瞬間に (c) が死にます**——**stance は指せないので、「背中が崖なら押せ」は学習も伝承もできない。**

**本命は水でも崖でもなく、間合い**

**段階13 の実測がそう言っています**——**水も崖も1つも無い平坦な世界で `killShare` 0.81 → 0.68 \*\*\*・`fightRate` −27%**、**崖落ちは1実行に112回で落下ダメージ0の対照と区別がつかず**、**水は溺死率 0.0002/tick なので250回押し込んで0.5体**。**つまり「押して大きな損を与える」は、今の世界では嘘です。** **知識としてそれを学ばせる前に、賞金が実在するかを数える**（下記）。**実在しないなら、学ばれるべき知識は「押して間合いを切ると damage が止む」のほう**で、**これは `TODO.md` 15 の申し送り**（「15 が学ぶべき鍵は水ではなく崖か間合いのほうかもしれない」）**に直接答えます。**

**押す向きは選べない**

`noteShove` の向きは**攻撃者→被攻撃者の直線**で、**幾何が決めます**。**「崖の前にいる相手を崖へ押す」には崖の反対側へ回り込む必要があり、今の効用式には「そこに立つと次に何ができるか」を値付ける項が1つもありません**（居場所についての6度の測定と、段階100 が触ったのはその手前）。**したがって第一歩は「今の立ち位置がたまたま有利なときだけ押す」に限ります。** **的は小さくありません**——**段階13 の数え上げで、殴打の 6.3〜14.6% は押した先が崖の下**（水は 0.13〜2.05%）。

**どれだけ押せるかは推定で、外れる**

距離は「入ったダメージ ÷ `AttackDamage` × `MaxVitality` ÷ 相手の最大体力」ですが、**相手の最大体力＝重さは隠しパラメータで `AgentView` にありません**（見えるのは今の体力だけ）。**原則（相手の真の能力は隠す）を守ったまま、「押し切れると思って押したら足りなかった」が自然に起きます**——**学習に意味を与えるのはこの誤差です。**

**決めるべきこと（未決。数え上げの前に形だけ選ぶ）**

1. ~~**`ActShove` を20番目の行動にするか、`Stance` の4つ目にするか。**~~ **決着（2026-09-21・ユーザの判断）: 構えにする。** **推奨は行動だった**（ヒントが指せないと (c) が死ぬため）が、**(2) で学習の置き場が lore になったので、その理由が消えました**。**`numActionKinds` は19のままで、`drawHint` の抽選は動きません。**
2. ~~**`HintFeature` を1つ足すか**（「相手の向こう側が危ない」）。~~ **決着（2026-09-21）: 足さない。** **学習は lore の belief 1本で持つ**ので、**`NumHintFeatures` は8のまま＝180通りへの薄まりも指紋の罠も消えます。** **状況（相手の背後に何があるか）は毎回知覚から読み、効き目（押すと本当に離れられるか）だけを覚える**——**この分担が (1)(2) の帰結。**
3. **知覚をどう足すか。** **「相手の向こうのセル」は今どこからも読めません**——`SelfView.Around` は `Cost`/`Drain`/`Chill` だけで `Drown` も `Height` も持たず（**コメントが明示的に「次の川は入って知る」と書いてある**）、`AgentView` が持つ地形は `Uphill` 1本だけ。**素直なのは `AgentView` に1本足すこと**（`Uphill` の前例）。**理性の誤差を乗せ**（`judgementScale` + `noise`）、**規則がオフのときは乱数を1つも引かない**（引くと指紋が動く）。
4. **用量は `KnockbackDist` と分けるか。** **分ける**（`ShoveDist` など。既定0）。**段階13 の既定オフを動かさないため**——**動かすとベースラインのリセット4度目**になります。
5. **逃げるほうの形。** **「押して逃げる」は `addFlee` とは別物**で、**その場に留まったまま、相手が詰め直すあいだ入ってくるダメージを止める手**。**閾値を1つも書かずに済みます**——`scoreFight` の `after` が使う incoming を、**押した距離 ÷ 相手の速度**ぶん減らすだけ。**そしてここが (2) の belief の効き場所**——**「押すと離れられる割合」が、その減らし方の確からしさとして掛かる**（`retaliation` が「殴り返される割合」として反撃の見積もりに掛かるのと同じ場所）。

**実装前に数えること（この案の可否を決める）**

- **決定の時点で、見えている相手のうち「私→相手の延長線上、押せる距離の内側」に落差か水があるのは何%か**（`country` / `river` / `countryskill`）。**段階13 の 6.3〜14.6% / 0.13〜2.05% は殴打あたりの下限**で、**決定あたりで数え直す。**
- **押しが捨てるダメージと、押しが買うもの（落差 2.3 × 段数 ＋ 相手が戻ってくるまでの tick）の比。** **1を割るなら、崖の知識は学ばれません**（段階24 の教訓＝発火しない規則は何も説明しない）。
- **「押して間合いが切れた」あと、相手が詰め直すのに実際に何 tick かかるか。** **これが (a) の賞金の本体**（`CombatRadius` 15 を超える押しは接触が切れる＝`knockfar` の腕）。
- **ヒント1枠が「背中×押す」になる確率と、実際に持っている個体数**（180通りへの薄まり）。**`hintsCopied` は既にあるので、その知識が何回コピーされたかは追加の計数なしで読めます。**
- **「知識だけが無い」対照**（押しは選べるが、その特徴のヒントだけを引かせない腕）。**段階35・49 が5度測った「ただの情報は世界を動かさない」を踏まないため。**

**壊してはいけない線**

- **効用式に「押されるかもしれない」という項を足さない**（段階13 の線。**足すのは押す側の選択だけで、押される側は今のまま**）
- **攻撃側有利を保つ**（`CLAUDE.md`）。**選べる押しは、負けている側が一番得をする道具**なので、**段階13 が既に削った攻撃側有利をさらに削ります**。**`pop` / `collapsed` / `killShare` / `fightRate` を能力の指標より先に、対で読む。**
- **渡れない地形は作らない**（`canBeShoved` のまま。**上りには押さない**）
- **能力の役割を重ねない**（**押す専用の遺伝子を作らない**。距離はダメージと重さから）
- **恨みの状態を持たない**（**押す理由は「今この手が買うもの」だけ**）
- **指紋**（語彙を足したら、同じコミットで golden を更新する）

**順序**: **14 → 16 → 15**（`TODO.md` の番号）。**16 が 15 より先なのは、15 が学ぶべき鍵を1つ実在させるから**——**段階13 の測定は「鍵は水ではなく間合いかもしれない」と言ったまま止まっています**。**14 と同時には入れません**（**どちらも `pop` を動かす向きなので、対で読めなくなる**）。

---

## 保留（条件つきで復活）

- ~~**明示的な「協力」**（相互選択の `ActCooperate` ＋ 協力性遺伝子）。段階11 まで進めて、それでも群れ狩りがまったく現れない場合にだけ再検討する~~ → **2026-09-06 に保留から出した（段階32）。** **再検討の条件は満たされている**: 段階11 で群れ狩りは現れず（`packSize` 1.43 → 1.46、動機を切っても動かない）、**獲物を大きくしても現れず**（外敵のサイズを 400〜700 に振っても参加人数は不動）、段階16 の栄養バランスで初めて有意に増えたときも 18.5 → 23.2 \*\* にとどまった。**段階11 の完了時に名指しした残りの欠けもの（「狙いを合わせる手段が無い」）の前提だった段階12（伝承）は 2026-09-04 に通っている。** ただし**#10 が却下したものをそのまま作るのではない**——協力性遺伝子は作らず、**行動語彙を1つ足し、受けるかどうかは既存の効用比較に採点させる**（打算は効用式にやらせる、という #10 の骨格は保つ）
- **明示的な「集団」**（集団ID・所属行動・集団間の敵対フラグ）。段階12 まで進めて、それでも**あるべき状態 B〜E** が満たせない場合にだけ再検討する。#10 と同じ条件つきの構え
- **`protocol` / `server` / `client`**。engine 側をどこまで進めてから着手するかは未定（`CLAUDE.md` の未決事項）。**2026-09-05 に一部決着**: スナップショット形式は段階21（保存/読込）を実際に使った上で設計する（#48）
- **配偶者を巡る同性間の戦い**（**地形は保留から外し、環境と食料生態系の最後に置いた**。2026-09-03。**2026-09-05 に段階20 へ移した**（ゲーム化の群の中で、エディタより前））。**成長・老い・自然死は保留から外し、段階7d に前倒しした**（2026-08-30・#28）
- **激しい活動（求愛・戦闘）による寿命の消費。** disposable soma 説の骨格としては筋が通るが、**今回は実装しない**（#31 は食事量だけを見る簡易近似）。寿命という状態が入るので、後から項を足すだけで済む
- **独立した睡眠機構**（疲労度という状態量・強制的な `ActSleep`）。**今回は実装しない**（#37）。狙っていた「休まざるを得ないが休むと危ない」という緊張は、段階9 の `RestExposureWeight` ですでに出ている（休まない世界では消耗死 +45%）。**今後の測定でこの緊張が弱すぎると分かった場合にだけ**復活させる
- **動物付着散布（毛に引っ付く種）と水散布。** 前者は「付着」という新しい状態を人間・外敵の側に足す必要があるので採らない（#44）。後者の条件（川・水場）は**段階20 で満たされた**（`~` のセルが川）が、**植物の遺伝そのもの（段階17）が世界を壊すので既定オフのまま**なので、復活は17 の復活が先。**2026-09-07 の追記**: 段階34〜36 で水は「死ぬ場所であり豊かな場所」になるので、**復活したときに水散布が運ぶ意味は段階20 当時より大きい**
- ~~**水辺に独自の豊かさを与えること**（現実の河畔植生のような個性）~~ → **2026-09-07 に保留から出した（段階36）。** 保留の理由は「一般式（段階33）と両立しない特別扱いになる」ことだったが、**段階33 が通って一般式そのものが世界に入った**ので、**その式に独立した項を1本足す形**で入れられるようになった（#62）。**コストの項には混ぜない**——水は「渡りにくいから避けたい」と「近くにいると豊か」を同時に満たす必要があり、1本の倍率に混ぜると符号が矛盾する
- ~~**肉に「植物では取れない栄養」という独自の価値をつけること。**~~ → **2026-09-09 に保留から出した（段階39）。** 段階16 の完了条件に条件つきで置き、「一般則（同じものばかりは損）が専用の仕掛けの弱い版として働いた」で一度は閉じていたが、**`dietVariety` 0.12 ＝人間はほぼ植物しか食べていない**という実測が残っていた。**形は「量」と「回復量」の2つのレバー**で、**後者は食料が体力に直接触る初めての経路**（今の食事は空腹度しか動かさない）。**群れ狩りへの5度目の挑戦を、初めて獲物の側から行うことでもある**（#68）
- **他人の持ち物を奪う行動。** 段階40（運搬）で「持っている」が世界に入り、段階49（発信）が「荷物を持った身体がここにいる」を暗に知らせるので、**作れば発信は諸刃になる**（買い手を呼ぶが、奪う側も呼ぶ）。**今は作らない**——今の世界の暴力は食料競合の先制排除として値付けされていて、**「所有物を奪う」は恨みや報復と同じく、効用式に無い動機を1つ増やす**（`CLAUDE.md` の「恨みの状態を持たない」に近い線）。**復活の条件は、段階48（バーター）で他人どうしの交換が実際に起き、持ち物が意味のある量になったとき**
- ~~**魚を生き物にすること（攻撃力の小さい第3の種族）。**~~ **2026-09-13 に「作らない」で決着した**（ユーザの判断）。**これが段階42・43 の本来の意図**だったが、実装は食料アイテムであり（#78）、**「寄ってきた魚を捕る」と「魚に寄って捕る」の違いは今のコードに存在しない**（どちらも動かない的への成功率の掛け算）。**それを直す代価が見合わない**——**決めることが多く**（魚は何を食べるか／繁殖するか／死体は `FoodFish` か `FoodMeat` か／`hunts`・`killShare`・種族ごとの個体数・`dietVariety` の意味が全部変わる）、**段階42・43・34・36 の記録が全部引き直しになる**うえ、**2026-09-13 に先読みが既定になってベースラインが一度リセットされたばかり**なので、**同じ記録を短期間に二度引き直すことになる**。**水の中の脅威は段階63 が別の形で入れた**（`EnemyKind.Water` ＝ 種類の表に1行。#95）ので、**この項目が担っていた目的のうち残っているのは「2つの釣りスキルの区別」だけ**で、それは**スキル1本に畳んでも失われるものが測定に出ていない***——#78 が並べたとおり決めることが多く、段階42・43・34・36 の測定は全部引き直しになる
- **座標レベルの知識（POI のリストを覚え、伝え、見えない座標へ向かうこと）。** 段階44 で代替案（地域＋スキル）を先に試すと決めた（#70）。**復活の条件は、地域単位の粒度が実際に足りないと測定で示されたとき**——今のところ「岸に沿った帯」（段階36・37）が唯一の実例で、それも `crossIndex` / `crossDry` という**測る側の指標**で足りている
- **危険側の先読み**（2026-09-14・#112(a)）。`stoneWorth` は `Chance: 1` の一点推定で、**「未来に喧嘩があるか」を一切見ていない**。**段階67 が `extra` を2窓目に持ち込まないと決めた理由（全候補が体力ゼロに収束して選べなくなる）がそのまま壁**になる。**段階81 の重みづけ（今の環境の危なさを重みとしてだけ使う）で迂回できる**ので、**単独で解くのは後回し**。**復活の条件は、段階81 で `w_stone` の側が実際に発火し、しかも一点推定では足りないと測定で示されたとき**
- **相場（過去の取引価格を参照点にする）**（2026-09-14・#112(b)）。**段階68・69 の教訓**——**社会的に伝わる仕組み（好意・本）は10段階以上かけてほとんど世界を動かさず、動いたのは構造的な制約（調理・地形・先読み）だった**。**相場は伝承に近い性質なので同じ罠にはまりうる**。**復活の条件は、段階80 で価格が実際に散らばると測定で示されたとき**（毎回同じ値に収束するなら、統計を足しても参照するものが無い）
- **お金それ自体への欲求**（2026-09-14・#112(c)）。現状コインへの欲求は `coinWorth` 1本のみで、**コメントにも「これはお金を欲しがる2つ目の理由ではない」と明記されている**（#73）。**理由なき欲求を足すと、貯め込みすぎて流通しなくなるという新しい罠になりうる**。**復活の条件は、段階79〜81 を試してなお「売り手が売らない」が残ったとき**
