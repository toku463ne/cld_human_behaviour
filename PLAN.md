# 実装計画（段階7以降）

**この文書はチェックリストです。** 段階を上から順に進め、終わったら `[x]` を入れます。全部終わったら、この文書は空になるか次の計画に置き換わります。

- 「なぜそう決めたか」は `HISTORY.md` の設計判断ログ（2026-08-18 の項）
- 実装後のルールの姿は `NODE.md` / `ENEMY.md`、数値は `PARAMETERS.md`
- 崩してはいけない原則は `CLAUDE.md`

---

## 決定事項（2026-08-18）

段階0〜6の完了後に出た要望を検討して決めたことです（予算の可変化・天才の2段階・理性/頭の良さを予算に入れる件は同日の追加決定）。

| # | 決めたこと |
| --- | --- |
| 1 | **遺伝子は予算制。8種。** 攻撃力・防御力・最大体力・移動速度・回避率・記憶容量・**理性・頭の良さ**。強くなるには別の何かを削る |
| 2 | **予算（合計値）は固定ではなく、出生ごとに幅をもって抽選する**（`GeneBudgetMean` ± `GeneBudgetStd`）。同世代でも「単純に恵まれた個体」が存在する |
| 3 | **天才は2段階。** 稀に（10年に一度）、さらに稀に（100年に一度）異常に大きな予算を引く。維持コストは課さない（予算が遺伝しないので、放っておいても水準は張り付かない） |
| 4 | **継承は染色体式＋幅。** 遺伝子ごとに親のどちらかの値を引き継ぎ、正規乱数を足す。突然変異は `MutationRate`（既定1%）でだけ起き、そのときの幅は大きい（`MutationStd ≈ 40`） |
| 5 | **ラマルク継承を採用。ただし `LamarckRate` で切替可能にする。** 0＝ダーウィン（生まれたときの値を継ぐ）、1＝完全ラマルク（学習後の値を継ぐ）。両方回せないと、測っているのが選択圧なのか学習速度なのか分からなくなるため |
| 6 | **防御と回避は払う資源を変える。** 防御＝ダメージ乗数を下げる／**体力**で払う。回避＝一定確率で完全無効／**行動機会**で払う（回避したtickは攻撃・食事ができない）。同じ資源で払わせると2つを分離して測れない |
| 7 | **effort の配分はプリセット3種**（攻め寄り／守り寄り／逃げ寄り）。連続値にすると候補数が爆発する。戦闘系の行動にだけ掛ける |
| 8 | **記憶容量を有限にする。** 容量は遺伝子（予算の配分の一つ）。容量が有限でないと忘却曲線がただの損になり、0に進化して終わる |
| 9 | **見た目からの推定は各ノードが学習する。** `AgentView` に真値は載せず、遺伝子から作った観測可能な相関値（`Appearance`）＋観測ノイズだけを載せる。「見た目→能力」の写像は engine が持たず、各ノードが自分の経験から学習する |
| 10 | **明示的な「協力」は却下。** 協力という行動も協力性遺伝子も作らない。外敵と「倒した者だけがドロップを得る」という世界側の事実だけを置き、打算は既存の効用式にやらせる。**進化が進んでも一向に協力が現れない場合にだけ、明示的な機構の導入を再検討する** |
| 11 | **空間グリッドを入れる。** ただし「広域探索の高速化（挙動不変）」と「視界をグリッド単位にする（挙動変更）」は別段階に分ける |

### 予算制の帰結（実装時に効いてくる点）

- **遺伝するのは「配分」、予算は「水準」で、水準は遺伝しない。** 子は遺伝子ごとに親のどちらかの値を引き、そのあと**自分が引いた予算**に合うようスケールする。つまり天才の子は、親の**配分の型**は継ぐが**予算の大きさは継がない**。予算を遺伝させると、予算そのものが単調に上がるだけの遺伝子になり（価格が無いので）、予算制にした意味が消える
- **だから維持コスト（基礎代謝）は要らない。** 予算に選択圧がかからないので、水準が張り付くことがない
- **選択圧は「配分」で測る。** 絶対値で測ると予算抽選のばらつきを拾ってしまうので、`cmd/experiment` の指標は各遺伝子の**予算に対する比率**にする。ここは既存の測り方からの明確な変更点
- **理性の弱さがそのまま読めるようになる。** 今の未決事項「理性への選択圧が弱い（+1.47。パワー +6.59 に比べて一桁小さい）」は、予算制では「理性に配分するくらいなら攻撃力に回す」という形で直接現れる。理性の配分比が世代とともに下がり続けるなら、理性の利得が価格に見合っていないことがはっきりする
- **クリップと予算の上限に注意。** 遺伝子は1本あたり1〜100なので、予算が 8×100 = 800 に近づくと全部が100に張り付き、配分という概念が消える。天才の予算も**800よりかなり下**（1本あたり70程度まで）に抑える
- **「10年に一度」は tick に翻訳が要る。** 世界に暦が無いので `TicksPerYear` を決める。ただし発生率は「出生あたりの確率」で持つほうがよい（世界側で発生間隔を管理すると engine が世界レベルの状態を持つことになる）。**副作用として、100年に一度の天才は 20000tick の実験にはまず現れません。** 効果を測るときは発生率を上げた条件を別に用意し、本番の頻度は演出として戻す
- **稀な天才は「進化の駆動力」ではなく「物語」として扱う。** 世界を動かすのは配分の遺伝のほうで、100年に一度の個体が集団の平均を動かすことはない。期待する役割を取り違えないこと

### 予算まわりの数値の出発点（要調整）

| 値 | 案 | 根拠 |
| --- | --- | --- |
| 遺伝子の本数 | 8 | 上の8種 |
| 1本あたりの範囲 | 1〜100 | 現行の `MinAbility` / `MaxAbility` のまま |
| `GeneBudgetMean` | 320（1本平均40） | 現行は3能力×平均50＝150。本数が増えるぶん1本あたりは下がる |
| `GeneBudgetStd` | 30 | 通常個体はおよそ 240〜400 に収まる |
| 天才（10年に一度） | 予算 440 前後（1本平均55） | 通常の上限を明確に超えるが、まだ配分の余地がある |
| 大天才（100年に一度） | 予算 560 前後（1本平均70） | 800（全部100）の手前で止める |

---

## 未決（実装前に決める）

- [ ] **「染色体」は性染色体か、遺伝子ごとの独立分配か。** 現在は後者（遺伝子ごとに独立にどちらの親から引くか決める＝メンデル的独立分配）を前提に書いてあります。性染色体にすると能力が性で偏る別の世界になります
- [ ] **予算をどの程度まで遺伝させるか。** 現在は「遺伝しない（出生ごとに抽選）」を前提に書いています。`BudgetHeritability`（0=毎回抽選、1=完全遺伝）を用意して0を既定にするのが素直ですが、「天才の子は凡人」でよいかは決めていません
- [ ] **`TicksPerYear` をいくつにするか、天才の発生率を出生あたりの確率で持つか世界側の間隔で持つか。** 出生あたりの確率にすると「10年に一度」は個体数によってずれます
- [ ] **初期個体の配分をどう引くか**（ディリクレ分布か、一様乱数を正規化するか）。初期の偏りの強さがそのまま初期の多様性になります
- [ ] **記憶の「単位時間に記憶できる量」と「忘却曲線」の扱い。** 現在の案は、容量だけが予算内、帯域は容量から導出（遺伝子を増やさない）、忘却曲線は予算外の別遺伝子（量ではなく形なので）
- [ ] 予算まわりの数値（上の表は出発点の案であって、決定ではありません）と、天才2段階の発生率
- [ ] 外敵のサイズ分布（「ドロップ数 ÷ 参加人数 > 単独で同じ時間に得られる期待値」が成り立つサイズが十分に現れる分布でないと、協力は原理的に現れない）

---

## 段階7a：空間グリッド（挙動を変えない高速化）

**目的**: 以降の全段階の土台。`perceive()` が全ノードを線形に走査している状態（実質 O(n²)）を潰す。段階9（地域ごとの世界リセット）の地域割りもここから取れる。

> 注意: `CLAUDE.md` に「既存の空間分割グリッドを粗くまとめたブロックを流用」とありますが、**空間分割はまだ存在しません**。この段階で初めて作ります。

- [ ] `World` に一様グリッドを持たせ、ノードと食料をセルに登録する（tickごとに再構築 or 移動時に更新）
- [ ] `perceive()` の近傍探索をグリッド経由にする。**円の視界判定はそのまま残す**（グリッドは広域探索の絞り込みだけに使う）
- [ ] 食料ごとの「最も近い他個体」の計算もグリッド経由にする
- [ ] **完了条件**: 同じシードで、グリッド導入前と**統計値が完全に一致する**ことをテストで確認（挙動不変）
- [ ] `BenchmarkEngineStep` / `BenchmarkEngineStepCrowded` を前後で測り、`HISTORY.md` に数値を残す
- [ ] 触る文書: `HISTORY.md`（性能の数値）

## 段階7b：継承則の入れ替え（染色体式＋幅）

**目的**: 混合遺伝（両親の平均）をやめる。混合遺伝は毎世代ごとに分散を約半分にするので、今は `MutationStd` だけが多様性の源になっている。粒子的遺伝にすると分散が維持され、選択圧の測定がやりやすくなる。

- [ ] `tryBirth()` を「遺伝子ごとに親のどちらかを引く＋正規乱数」に変更（能力3つのまま。遺伝子の追加は 7c）
- [ ] `Config` に `MutationRate`（既定 0.01）を追加。`MutationStd` を再較正する（毎回 std=4 の分散注入は 16。1% でこれに合わせるなら std ≈ 40）
- [ ] `LamarckRate` を `Config` に用意する（この段階ではまだ学習パラメータが無いので効かない。枠だけ先に置く）
- [ ] **完了条件**: 継承則の前後で `cmd/experiment` を回し、能力の分散と選択圧の変化を `HISTORY.md` に残す（**過去の測定値と直接比較できなくなる区切り**なので、ここに新しいベースラインを記録する）
- [ ] 触る文書: `NODE.md`（10-3 の遺伝）、`PARAMETERS.md`（C-8）、`HISTORY.md`

## 段階7c：遺伝子ベクトル化と予算制

**目的**: 能力3つを `Genome []float64` に置き換え、8種の遺伝子を予算制（予算は出生ごとに抽選）で導入する。

- [ ] `Agent.Power/Rationality/Intelligence` を `Genome []float64` に置き換える（インデックスは定数で名前を付ける）
- [ ] 遺伝子8種にする: 攻撃力・防御力・最大体力・移動速度・回避率・記憶容量・理性・頭の良さ
- [ ] 予算を出生ごとに抽選する（`GeneBudgetMean` / `GeneBudgetStd`）。予算は遺伝しない（`BudgetHeritability = 0`）
- [ ] 継承時の正規化（親から引いた値を、子が引いた予算に合うようスケール）
- [ ] 天才2段階（`GeniusRate` / `GreatGeniusRate` と、それぞれの予算）。`TicksPerYear` を決めて「10年に一度」を出生あたりの確率に換算する
- [ ] 既存の `Power` の役割（`damagePerTick`）を攻撃力に、`MaxVitality`・`MaxSpeed` を遺伝子に置き換える
- [ ] `cmd/experiment` の能力指標を**絶対値から予算に対する比率へ変更する**（予算抽選のばらつきを拾わないため）
- [ ] **完了条件**: devview で「大きくて遅い個体」「小さくて速い個体」が並存するのが見える。experiment で配分の分布を出し、一点に収束していないことを確認。天才の発生間隔が狙いどおりかも出す
- [ ] 触る文書: `NODE.md`（1・3節）、`PARAMETERS.md`（A・C-2）、`HISTORY.md`

## 段階8：攻撃・防御・回避のチャネル化

**目的**: 要望「攻撃力、防御力、移動、回避の使い方によって体力の減りが決まる」。

**この段階が一番危険です。** 攻撃側有利の非対称量が動くので、`CompetitionWeight` / `SkirmishTicks` と同じく個体数を崩しうる。

- [ ] effort をプリセット3種（攻め寄り／守り寄り／逃げ寄り）に拡張し、戦闘系の行動にだけ掛ける
- [ ] 防御＝ダメージ乗数を下げる／体力で払う。同tickの攻撃出力も下がる
- [ ] 回避＝確率で完全無効／行動機会で払う（回避したtickは攻撃・食事ができない）。移動速度に依存させる
- [ ] 体力消費 = Σ(チャネルごとの 最大値 × 使用率 × 単価)
- [ ] **完了条件**: `BenchmarkDecide`（現在 8.0µs、候補約40件）が候補約70件でどれだけ増えるかを測る。experiment で**個体数・死因の内訳を必ず確認**（防御と回避への選択圧が別方向に出ているかも見る）
- [ ] 触る文書: `NODE.md`（6・7節）、`PARAMETERS.md`（C-3）、`HISTORY.md`

## 段階9：記憶の容量・帯域・忘却曲線

**目的**: `opinions` を有限容量にし、忘却を「容量の取り合い」にする。副産物として記憶が O(個体数) から O(容量) に落ちる。

- [ ] `opinions` を容量つきに変更（あふれたら重要度の低いものから捨てる）
- [ ] 単位時間に更新できる `Opinion` 数の上限（帯域）
- [ ] 忘却曲線を遺伝子にする（今の `RiskDecayPerTick` はグローバル定数）
- [ ] **完了条件**: devview で、容量の小さい個体が「知っているはずの相手を忘れている」のが見える。experiment で記憶容量への選択圧が正か負かを測る
- [ ] 触る文書: `NODE.md`（8節）、`PARAMETERS.md`（C-4）、`HISTORY.md`

## 段階10：見た目からの学習

**目的**: 初対面の相手に `PriorStrength = 50` を当てはめるのをやめる。

- [ ] 遺伝子から観測可能な相関値 `Appearance` を作る（体格・動きの速さなど）＋観測ノイズ
- [ ] `AgentView` に `Appearance` を載せる。**真値は載せない**（型による保証を維持）
- [ ] 各ノードが「見た目→能力」の写像を自分の戦闘・観戦から学習する（線形1本で十分）。engine は写像を持たない
- [ ] **完了条件**: 世代が進むにつれて**初対面の相手に対する推定誤差が下がる**ことを experiment で示す。devview のパネルで、まだ会っていない相手にも推定値が出るのが見える
- [ ] 触る文書: `NODE.md`（8節）、`PARAMETERS.md`、`HISTORY.md`

## 段階11：種族の汎用化と外敵（協力の土俵）

**目的**: 「隣人が生きていると自分が得」を世界側の事実として置く。協力という言葉は engine に登場させない。

- [ ] 人間・外敵で同じノード構造・同じ意思決定エンジンを共有する（種族ごとのパラメータ範囲＋種族固有遺伝子で差を出す）
- [ ] 外敵に「大きさ」を持たせ、**倒したときのドロップ数を大きさで決める**
- [ ] 分配は「攻撃に参加した者だけ」（`ENEMY.md` の方針。「近くにいる全員」は誰も狩らなくなるので選ばない）
- [ ] 食料3種（共通食料A/B＋死体からの肉）。人間は人間由来の肉を食べない、外敵は種族を問わず食べる
- [ ] **完了条件**: 群れ狩りが**現れるか現れないか**を測る（`Spacing()` の `Clumping`、外敵の討伐数、参加人数の分布）。**現れなくてもこの段階は成功**です。現れなかったという事実が、明示的な協力機構を再検討する根拠になります
- [ ] 触る文書: `ENEMY.md`（「確定したルール」へ移す）、`NODE.md`、`PARAMETERS.md`、`HISTORY.md`

## 段階12：傾向の遺伝と学習（ラマルク）

**目的**: 今グローバル定数になっている効用式の仮定を、個体ごとの値にして学習させる。

- [ ] 対象: `retaliation = 0.7`（相手は7割で殴り返す）／`pAccept = 0.6`（求愛は6割で受け入れられる）／`RiskWeight` / `CompetitionWeight` / `ShockRisk`
- [ ] **世界の事実**（`retaliation`, `pAccept`）は正解が世界側にあるので、強さ推定と同じベイズ更新で収束させる
- [ ] **好み**（`RiskWeight`, `CompetitionWeight`）は正解が無いので、遺伝＋突然変異＋近傍からの模倣に任せる
- [ ] `LamarckRate` を有効化（学習後の値をどれだけ子に伝えるか）
- [ ] `LearningRate = 0` で全部止められること
- [ ] **完了条件**: experiment に「平均」ではなく**分散と空間相関**の指標を足し、`LamarckRate` 0 と 1 で比較する。文化のクラスタ化は平均を見ても絶対に見えません
- [ ] 触る文書: `NODE.md`（5節）、`PARAMETERS.md`（D節から C 節へ移動する項目が出る）、`HISTORY.md`

## 段階13：視界をグリッド単位にする（挙動変更）

**目的**: 要望「視力によって見えるグリッド数（周囲1グリッド）」。7a と分けるのは、こちらが**挙動の変更**だからです。

- [ ] 視界を円から「自セル＋周囲Nグリッド」に変更する
- [ ] 視力（N）を遺伝子にするかどうかを決める（するなら予算の配分に加える）
- [ ] **完了条件**: 角の方向だけ遠くまで見える異方性が入るので、逃走成功率と食料への競走が変わります。experiment を必ず前後で回す
- [ ] 触る文書: `NODE.md`（5-2）、`PARAMETERS.md`、`HISTORY.md`

---

## 保留（条件つきで復活）

- **明示的な「協力」**（相互選択の `ActCooperate` ＋ 協力性遺伝子）。段階11 まで進めて、それでも群れ狩りがまったく現れない場合にだけ再検討する
- **`protocol` / `server` / `client`**。engine 側をどこまで進めてから着手するかは未定（`CLAUDE.md` の未決事項）
- **成長・老い・自然死**、**配偶者を巡る同性間の戦い**、**地形**（`CLAUDE.md` の「将来実装」）
