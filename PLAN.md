# 実装計画（段階6.5以降）

**この文書はチェックリストです。** 段階を上から順に進め、終わったら `[x]` を入れます。全部終わったら、この文書は空になるか次の計画に置き換わります。

- 「なぜそう決めたか」は `HISTORY.md` の設計判断ログ（2026-08-18・2026-08-19・2026-08-30 の項）
- 実装後のルールの姿は `NODE.md` / `ENEMY.md`、数値は `PARAMETERS.md`
- 崩してはいけない原則は `CLAUDE.md`

---

## あるべき状態（受け入れ基準・2026-08-19）

**これは仕様ではなく受け入れ基準です。** 一定tick（当面 50000tick を想定）を回したあとの世界が、次の状態になっていることを目指します。ここを目指す理由は、**将来プレイヤーが1ノードを操作して他の集団を制圧していくゲームにしたいから**で、制圧する対象＝集団が世界に自然に存在していないとゲームが成立しません。

| # | あるべき状態 | 測り方（段階6.5 で用意した。指標名は `cmd/experiment` の列） |
| --- | --- | --- |
| A | 異なる種族がそれぞれ一定数存在する | 種族ごとの個体数が、窓（直近5000tick）の平均で帯の中に収まる。**振動は許容する**ので見るのは振れ幅ではなく**谷**（`rareShare` / `rareTrough`。`engine.CensusTracker`） |
| B | human は一定数いて、**集団がいくつか**存在する | クラスタ数とサイズ分布（`clusters` / `clusterSize` / `grouped`。`World.Clusters`、連結距離30）。`largestShare` が1に近いときは世界が1つの塊につながっただけなので他の3つを読まない |
| C | 集団の中では戦闘率が低い | `fightRatio`。**素直な「集団内戦闘率 ÷ 集団間戦闘率」は原理的に測れない**（追記16）ので、遭遇あたりの「仲間 ÷ よそ者」に置き換えてある。1を下回るほど良い |
| D | 集団は行動を共にする | membership half-life（同じ顔ぶれが一緒にいる時間）。たまたま近くにいるだけの塊と区別する（`halfLife` / `together`。`engine.MembershipTracker`、組の共在生存曲線） |
| E | 異なる集団のノード同士は、戦闘になるか近づかない | 距離側は `gap` / `gapP10` / `gapRel`（`World.ClusterGaps`）、戦闘側は C と同じ `fightRatio` |

**5つとも 2026-09-01 に揃いました。** 「集団が無い世界」のベースライン（既定条件・24シード × 50000tick）は `HISTORY.md` の同日の項にあります。

**目標数値（2026-09-01・段階11 の実測から記入）**

| # | 実測（段階11 時点） | 目標 |
| --- | --- | --- |
| A | 種族2つが全24シードで存続。外敵は個体数の 8%、谷は平均の 0.37 | **達成。** 帯は「希少なほうの種族が個体数の5〜20%、谷が平均の0.3以上」とする |
| B | クラスタ11.1個・平均3.1体・所属率0.56 | 未達（塊はあるが集団ではない。下のD参照） |
| C | `fightRatio` 0.87 ± 0.11 | 未達。**目標は 0.6 以下**（仲間との争いが明確に少ない） |
| D | `halfLife` 30.6tick | 未達。**目標は 150tick 以上**（ペア結合の長さを超えて顔ぶれが持続する） |
| E | `gapRel` 0.98 | 未達。**目標は 1.2 以上**（集団どうしが偶然より離れている） |

**B〜E の目標値は「段階12 のあとに判定する」ものとして置きました。** 決定事項#17 は段階11 のあとに数値を入れる想定でしたが、**好意（段階9）と伝承（段階12）が未実装のままでは、共同で狩ったことが記憶に残らず、集団化の経路が世界に存在しません**（段階11 の実測でも群れ狩りは現れませんでした）。

**目標の数値はまだ書きません。** 今の世界のクラスタ数も戦闘率比も測ったことがないので、根拠のない数字になります。測定インフラは予定どおり段階6.5 で先に作りますが、**表に数値を書き込むのは段階11（外敵）を実装したあと**です（決定事項#17・2026-08-30）。

**段階11 より前は、集団が形成されなくてよい。** 今の世界（human だけ）には他個体が生きていることが自分の得になる経路が1本もなく（`NODE.md` 11節）、原理的に集団化しません。実際の人類の社会性も外部の脅威に強く駆動されたと考えられており、この単純化には裏付けがあります。したがって段階6.5 の時点で B〜E に目標値を入れるのは早すぎます。段階6.5 では**現状の値を「集団が無い世界のベースライン」として記録するだけ**にし、目標値は外敵が入って共同戦闘という正の項が生まれたあとの世界を見てから決めます。

**達成の手段は「機構を直接書くこと」ではありません。** 集団IDや所属行動を作れば B〜E は明日にでも満たせますが、それは決定事項#10 と同じ理由で意味がない（世界がそうなったのではなく、我々がそう書いただけ）。B〜E は**好意（正の記憶）と伝承**から創発させ、達成できなかった場合にだけ明示的な機構を再検討します — #10 と同じ条件つきの構えです。

**プレイヤーに集団を見せるための「集団の識別」は view 層の仕事です。** クラスタ検出は読み取り専用の解析なので、engine に集団IDを持たせる理由にはなりません。

## 決定事項（2026-08-18 ／ 2026-08-19 ／ 2026-08-30 ／ 2026-08-31 ／ 2026-09-03 追加）

段階0〜6の完了後に出た要望を検討して決めたことです（予算の可変化・天才の2段階・理性/頭の良さを予算に入れる件は同日の追加決定）。**#12〜#16 は 2026-08-19 の追加**（魅力の遺伝子化・集団の創発・伝承・あるべき状態の位置づけ）、**#17〜#32 は 2026-08-30 の追加**（集団化の前提の見直し・好意の発生源・伝承の等価交換・ヒント・初期配分の引き方・魅力と実能力の相関・好意の記憶容量・血縁ブーストの範囲・**成長／老い／寿命の前倒し**・**休息の無防備さ**）、**#33〜#35 は 2026-08-31 の追加**（**慢性的な低体力による寿命コスト**・**数手先の見積もりの役割分担**・**能力による深さゲートの禁止**）で、各行に日付を入れてあります。**#14 と #16 は 2026-08-30 に内容を差し替え、#4 の分配方式も同日「遺伝子ごとの独立分配」で確定しました。** **#36〜#46 は 2026-09-03 の追加**（**環境と食料生態系**＝休憩場所・睡眠を作らないこと・地域という語彙の一本化・地域差の測り方・地域の豊かさの知覚と伝承・地域の記憶と記憶容量の関係・植物の遺伝・毒と警告信号・種子散布・栄養バランス・体内時計）で、これらは段階14〜18 として下に節を立ててあります。

| # | 決めたこと |
| --- | --- |
| 1 | **遺伝子は予算制。9種。** 攻撃力・防御力・最大体力・移動速度・回避率・記憶容量・理性・頭の良さ・**魅力**。強くなるには別の何かを削る |
| 2 | **予算（合計値）は親から継ぐ（2026-08-30 確定）。** 子の予算＝**親の予算 ＋ 正規乱数**（平均0・標準偏差 `BudgetInheritSpread`）。**どちらの親の予算から引くかは 50% のランダム**（両親の平均は取らない。2026-08-30 確定）。初期個体だけ `GeneBudgetMean` ± `GeneBudgetStd` で抽選する。同世代でも「単純に恵まれた個体」が存在し、その恵まれ方が子にも受け継がれる。**当初の「出生ごとに抽選＝予算は遺伝しない」（2026-08-18）から変更**。切替は `BudgetHeritability`（0＝毎回抽選＝旧仕様、1＝親から継ぐ＝既定）で残し、両方回せるようにする |
| 3 | **天才は2段階。** 稀に（10年に一度）、さらに稀に（100年に一度）異常に大きな予算を引く。**#2 の変更により、天才の予算は子孫にも部分的に残る**（親の予算 ± 幅で継がれるため）。維持コスト（価格）を課すかどうかは、**予算の平均が世代とともに単調に上がるかを測ってから決める**（下の帰結と未決を参照）|
| 4 | **継承は染色体式＋幅。** 遺伝子ごとに親のどちらかの値を引き継ぎ、正規乱数を足す。突然変異は `MutationRate`（既定1%）でだけ起き、そのときの幅は大きい（`MutationStd ≈ 40`）。**分配は遺伝子ごとの独立分配（メンデル的独立分配）で確定。性染色体にはしない**（2026-08-30。連鎖群も作らない＝遺伝子どうしは相関しない） |
| 5 | **ラマルク継承を採用。ただし `LamarckRate` で切替可能にする。** 0＝ダーウィン（生まれたときの値を継ぐ）、1＝完全ラマルク（学習後の値を継ぐ）。両方回せないと、測っているのが選択圧なのか学習速度なのか分からなくなるため |
| 6 | **防御と回避は払う資源を変える。** 防御＝ダメージ乗数を下げる／**体力**で払う。回避＝一定確率で完全無効／**行動機会**で払う（回避したtickは攻撃・食事ができない）。同じ資源で払わせると2つを分離して測れない |
| 7 | **effort の配分はプリセット3種**（攻め寄り／守り寄り／逃げ寄り）。連続値にすると候補数が爆発する。戦闘系の行動にだけ掛ける |
| 8 | **記憶容量を有限にする。** 容量は遺伝子（予算の配分の一つ）。容量が有限でないと忘却曲線がただの損になり、0に進化して終わる |
| 9 | **見た目からの推定は各ノードが学習する。** `AgentView` に真値は載せず、遺伝子から作った観測可能な相関値（`Appearance`）＋観測ノイズだけを載せる。「見た目→能力」の写像は engine が持たず、各ノードが自分の経験から学習する |
| 10 | **明示的な「協力」は却下。** 協力という行動も協力性遺伝子も作らない。外敵と「倒した者だけがドロップを得る」という世界側の事実だけを置き、打算は既存の効用式にやらせる。**進化が進んでも一向に協力が現れない場合にだけ、明示的な機構の導入を再検討する** |
| 11 | **空間グリッドを入れる。** ただし「広域探索の高速化（挙動不変）」と「視界をグリッド単位にする（挙動変更）」は別段階に分ける |
| 12 | **魅力を遺伝子にする（2026-08-19）。** 予算を食うが生存には効かない純粋な装飾。**既存の `fitness`（パワー35%＋理性15%＋知能15%＋体力35%）を置き換える**（能力に2役持たせない）。実能力とは**相関させない**（見た目→能力の写像は段階10 で各ノードが学習する。engine が先に答えを書かない）|
| 13 | **「魅力の高い相手に近づく」は行動として書かない（2026-08-19）。** 求愛の効用は既に `OffspringValue × 相手の推定魅力/100` なので、魅力が上がれば接近しやすくなるのは今の効用式のまま起きる。足すのは**再判断トリガー1本**（「魅力の高い個体が視界に入った」）だけ |
| 14 | **集団は「好意」と「伝承」から創発させる（2026-08-19 ／ 2026-08-30 改訂）。** 集団IDも所属行動も作らない。**好意（正の記憶）**を持たせ、そこから学べる（伝承）ことを効用の正の項にする。**集団間の敵対は「よそ者には正の項が無い」ことだけから出す**（憎悪パラメータは作らない＝原則「恨みの状態を持たない」を守る）。**当初あった「無害に共在した時間を好意にする」は 2026-08-30 に不採用**（#19）|
| 15 | **伝えるのは effort そのものではなく、effort を決める前提（2026-08-19）。** effort はプリセット3種＝効用比較の結果（#7）なので、配分を直接コピーさせると効用式と競合する第二の意思決定系ができる。段階12 の学習対象（`retaliation` / `pAccept` / `RiskWeight` / `CompetitionWeight`）を伝承の対象にすれば、「あの集団は守り寄りの effort を選ぶ」は結果として現れる |
| 16 | **「あるべき状態」は受け入れ基準であって仕様ではない（2026-08-19 ／ 2026-08-30 改訂）。** 目標を engine の機構として直接実装したら（集団IDを持たせる等）、「集団があるのは我々がそう書いたから」になり、#10 と同じ理由で世界を測る意味が消える。**目標の数値は段階6.5 で現状を測ったうえで、段階11（外敵）の実装後に書く**（#17）|
| 17 | **段階11 より前は集団が形成されなくてよい（2026-08-30）。** 今の世界には他個体の存在そのものが自分の得になる経路が無く、原理的に集団化しない。よって段階6.5 で目標数値を決めるのは早すぎる。**測定インフラは段階6.5 で先に作り、目標数値の記入は段階11 の後に回す** |
| 18 | **敵意（脅威記憶）は新設しない（2026-08-30）。** 距離・接近ベースの新しい敵意計算は採用しない。**既存のリスク記憶**（自分がその相手に払わされた体力の累積・遅延減衰つき）**をそのまま敵意として扱う**。新しい状態を増やさない |
| 19 | **好意は実イベントからのみ生まれる（2026-08-30）。** 距離・滞在時間ベースの好意計算（視界にいる時間に比例して加点する案）は**不採用**（#17 により急ぐ理由が無くなったため）。発生源は4つだけ: **ペア形成・出産**（既存の `PairBondDuration` 等で成立する関係そのもの）／**血縁**（`ParentIDs` / `ChildIDs` を使った親子の初期ブースト）／**共同戦闘**（段階11。集団化の主エンジン）／**継承**（出産・模倣のどちらでも、実際に何かを受け取ったときに加点）|
| 20 | **深い伝承は既存の `observe` に相乗りさせる（2026-08-30）。** 新しい観察の仕組みは作らない方針を維持する。`observe` が既に持つ体力コストと時間コスト（`observeTicks`）をそのまま流用でき、追加のコスト設計は要らない。排他性はまず `observe` の自然な排他性（1度に1人しか観察できない＝**発信側**の帯域制限）だけに任せる。**受信側（人気者）のハブ化はこれでは防げない**ので、被参照数の分布を測ってから相互占有の追加を判断する |
| 21 | **伝承は等価交換（2026-08-30）。** 一方向の「学ぶ」ではなく**双方向・同時の歩み寄り**にする。学習対象ごとに、値が違えば互いに相手の値へ少しずつ近づく。**好意の加点量は実際に動いた値の大きさに比例**させる。すでに値が近い相手との交換は自動的にほぼ無価値になる（特別な判定を足さない）。副次効果として、学び尽くした相手は交換価値を失うのでハブ化が自然に緩む |
| 22 | **接触だけで減衰の起点をリセットする（2026-08-30）。** 減衰式 `exp(−減衰率×経過tick)` 自体は変えず、**「経過tick」の起点（最後にいつ更新されたか）の扱いだけを変える**。値が動いたときに加えて、**接触した事実だけでも起点を更新する**（等価交換のチェックが走れば、差がゼロで不成立でも更新）。**好意・リスク記憶の両方に適用**する。新しいトリガーは要らない |
| 23 | **伝承される中身は固定パラメータではなく「ヒント」（2026-08-30）。** `retaliation` のような固定名のスカラーだけに限定せず、**個体ごとに可変なヒントの集合**へ拡張する。ヒントは〈状況特徴量 × 対象行動 × 重み〉の3つ組で、**行動を直接決めず、既存の効用比較への加点／減点としてのみ働く**（原則を壊さないことが必須条件）。**枠は遺伝子予算を消費する。** 重みの微調整＝通常の突然変異、構造の変化＝既存の天才イベントの流用 |
| 24 | **初期個体の配分はディリクレ分布で引く（2026-08-30）。** α ≈ 0.5〜1（多少の偏りが出る設定）を出発点にし、値は `cmd/experiment` で振って調整する。**一様乱数を正規化する方式は不採用** — あらゆる配分が等確率にはならず、**1本に予算が集中するような極端な配分が出にくい方向にバイアスがかかる**（中央に寄る）。α で偏りの強さを直接いじれるディリクレのほうが「多少の偏りも生まれる初期集団」に合う |
| 25 | **魅力と実能力は無相関のままにする（2026-08-30 確定）。** ハンディキャップ原理（強い個体しか高い魅力を維持できない）は**今は導入しない**。無相関にしておく狙いは #12 のとおりで、「装飾は嘘をつけるのか」を段階10 の学習に答えさせるため。engine が先に相関を書いてしまうと、その問いの答えを engine が与えたことになる。**ただし性選択圧は `CompetitionWeight` と同格の危険パラメータとして扱う**（下の帰結を参照）。実装後に魅力への配分が暴走して他の遺伝子を圧迫する／個体数が崩れるなら、そこで初めて (a) ハンディキャップ原理側の設計に切り替える か (b) 魅力の効き方（`pAccept` の重み）を弱める、のどちらかを検討する |
| 26 | **好意は既存の「意見」レコードの1フィールドにする。専用の枠は作らない（2026-08-30 確定）。** 記憶容量は**相手ごとに1レコード**（`Opinions(id)` が返す既存のレコード＝リスク記憶＋強さ推定）という単位で管理し、好意はそこへ**新しいフィールドとして足す**。「その相手を覚えている」こと自体が1枠を消費し、リスク記憶・強さ推定・好意はその1枠を共有する（**脅威として覚えても味方として覚えても、消費する枠は同じ1つ**）。容量は既存の9遺伝子の**記憶容量**でそのまま管理し、**好意専用の遺伝子もパラメータも追加しない**。したがって**1ノードが同時に好意を持てる人数＝集団の実質的な最大メンバー数は、記憶容量の遺伝子値がそのまま決める**。好意と脅威を別々の容量で管理する別枠案は、遺伝子とパラメータが増えて測定も複雑になるため不採用。**実測で集団サイズが明らかに不自然（小さすぎる／大きすぎる）と分かった場合にだけ、分離を検討する** |
| 27 | **血縁ブーストは直系の親子（1ホップ）だけ。世代をまたぐ減衰ルールは作らない（2026-08-30 確定）。** `ParentIDs` / `ChildIDs` を1ホップだけ辿り、親子の双方に初期好意を与える。**祖父母・孫・きょうだいは血縁として扱わない** — それらの関係は他の発生源（共同戦闘・継承／模倣・接触による起点リセット）に委ねる。血統を何ホップも辿る処理も、ホップ数に応じた減衰係数も持たない（**新しい状態も新しいパラメータも増やさない**）。近縁者が結果として結びつくなら、それは育児期間ぶん近くにいて実イベントが多く走るからで、engine が血統距離を計算したからではない。**ブーストの大きさは初期値を仮置きし、`cmd/experiment` で調整する** |
| 28 | **成長・老い・自然死を前倒しで実装する。`TicksPerYear` は本決めにする（2026-08-30 の方針転換）。** `CLAUDE.md` では「将来実装・年齢補正は当面つねに1」として先送りしていたが、**育児期間（段階7d）の設計に成長の概念が不可欠**なため、ここで前倒しする。実装の形は当初方針どおり「**遺伝的素質 × 年齢補正**」で、**成長（0→1に伸びる）と老い（1→減衰する）は同じ1本の関数の両端**として扱う（別々の仕組みにしない）。あわせて `TicksPerYear` を「老い実装待ちの暫定プレースホルダー」から**本決めの値**に切り替える（#31 の自立期間と実行長の制約から決める） |
| 29 | **子育て中の食糧確保は「保護」だけ。給餌は作らない（2026-08-30）。** 親が子に食料を渡す新しい仕組み（給餌アクション・食料の受け渡し）は**実装しない**。子は自分で食料を得ようとするが弱いので、**親が近くにいて、既存の戦闘・先制排除の仕組みで競合相手を排除する**という間接支援だけにする。新しい行動もルールも増やさない。**これで足りるかは実装後に測る**（子の餓死率・自立到達率）。不足するようなら、そのとき初めて給餌の追加を検討する |
| 30 | **成長は食事で進む。既存の `eat` に相乗りさせる（2026-08-30）。** 「成長する」という新しいアクションは作らない。食料を摂ると年齢補正の成長側が進み、**満腹（空腹度0＝100%）に達するとそれ以上は成長に乗らない**（頭打ち） |
| 31 | **寿命は過小・過度の両側で削れる。閾値は意図的に別の値にする（2026-08-30）。** **過小**＝空腹が一定時間続くと、既存どおり体力を削るのに加えて**寿命も削る**。**過度**＝摂取が「満腹」ではなく**「腹八分目」（80%程度）を超えた時点**から寿命を削り始める。**成長の頭打ち（満腹＝100%）と過度コストの開始点（腹八分目＝80%）をずらすことが設計の要**で、この差分から「**成長中の個体は多少寿命を削ってでも満腹近くまで食べるほうが得、成長を終えた個体は腹八分目で止めるほうが得**」という行動差が、**特別な場合分けを書かずに効用比較から出る**。「満腹に近いのになぜ食べるのか」も新しい判断閾値ではなく**既存の `rival` の項**（競合相手に渡さないために寿命コストを払ってでも食べる）で説明する。理論的な位置づけは **disposable soma 説の簡易近似**（下の帰結を参照） |
| 32 | **休息を無防備にする。よそ者の近さを効用のリスク項に入れる（2026-08-30）。** 今の `ActRest` は Life の項だけを持ち、リスクがゼロ（＝いつどこで休んでも同じ）。ここに「**対象の近くにいる、好意を持っていない他個体の存在**」によるリスクを足す。**好意は既存の相手ごとの記憶レコード（強さ推定・リスク記憶と同じ枠。#26）を参照するだけ**で、新しい状態も新しい強さ評価器も追加しない（リスクの大きさは既存の `EstStrength` とリスク記憶と距離から作る）。**好意のある個体が近くにいる場合はこのリスクを乗せない、または割り引く。** 実装は**既存の候補生成の走査（`Decide` が `p.Others` を1度なめる処理）に相乗り**させ、**追加の探索コストを発生させない**。重みは `Config` に出して **0 で完全に切れる**こと（今の挙動に戻せる＝A/B の対が取れる） |
| 33 | **慢性的な低体力も寿命を削る（2026-08-31）。** 体力が20%を下回った状態が一定時間続いたら、既存の空腹による寿命コスト（#31 の過小側）とは別に寿命を削る。**`ShockRisk`（今感じられる危険。効用式に載り意思決定に使われる）とは別物**として扱い、この寿命コストは**意思決定に一切関与しない静かな長期コスト**にする。カウントは**体力が20%を上回った時点でリセットする単純な版から始め**、生涯の累積コスト（リセットしない版）にするかは実装後の測定で判断する。**攻撃側有利・休息の無防備さ（#32）と組み合わさると、弱い個体への不利が複合しうる**ため、導入時は必ず `cmd/experiment` で個体数を確認する |
| 34 | **数手先の見積もりはミニマックス探索ではなく役割分担で行う（2026-08-31）。** 将棋のような分岐探索（候補ごとに数手先まで枝を広げる方式）は不採用。理由は2つ：候補数（現状約40）が手数ごとに掛け算で増え `BenchmarkDecide`（現状約8.0µs）が桁違いに重くなること、そしてこの世界は相手も適応的に動く・相手の真の能力は隠しパラメータという二重の不確実性があり、深い事前計画より**トリガー方式による頻繁な考え直し**のほうが動的な多主体環境では有効なことが多いこと。代わりに (a) **閉じた式による数tick先の見積もり**（既存の `PlanHorizon`／`ShockRisk`／`SkirmishTicks`／`fleeExposureTicks`／`recoverable` と同じパターン＝代替案に分岐せず「今の状態がこのまま続いたら数tick先どうなるか」を1本の軌道として見積もる）を、設計者が式を書き下せる新しい場面（休息の無防備化の被害見積もり、育児中に子から離れるリスク、好意のない他個体への接近リスクなど）に広げるのと、(b) **設計者が式を書き下せない・気づいていない関係はヒント（#23）に発見させる**（評価は個体1回の決定ではなく世代を超えた集団規模の生存競争が行う＝探索コストを「今この瞬間の1個体」から「時間をかけた進化」に移す）の2つを役割分担させる。**寿命はこの見積もりの対象に含めない**（ノードは寿命を知覚せず、効用式にも寿命の項を作らないという既存方針を優先する）。両方とも同じ `Utility.Total()` に加算される項として実装し、アーキテクチャを分けない |
| 35 | **深さ・ヒント枠を能力でゲートしない（2026-08-31）。** 頭の良さ・理性が高い個体だけに、より深い見積もり（#34 の(a)）やより多くのヒント枠（#23・#34 の(b)）を解禁する設計はしない。2026-08-14 の測定で `StrategyDepthUnlock`（知能が高いほど戦略の選択肢が広がるゲート）が逆効果（愚かな個体を損な手から保護してしまい、知能への選択圧をむしろ下げた）と分かり既定で無効化した経緯があり（`CLAUDE.md`・`HISTORY.md`）、#34 を実装するときも同じ罠を繰り返さない |
| 36 | **休憩場所は既存の休息露出に相乗りさせる（2026-09-03）。** 新しい仕組みは作らない。実装・較正済みの `RestExposureWeight`（休息中の被害見積もり・既定 0.05）に、**場所ごとの割引率を1つ掛けるだけ**にする。区画は #38 の地域ブロックをそのまま使い、休憩場所専用の区画分けは新設しない。#34 の (a)「閉じた式による数tick先の見積もりを必要な場面に広げる」の延長として扱う |
| 37 | **睡眠を独立した状態として実装しない（2026-09-03）。** 睡眠欲求（疲労度）のような新しい状態量も、強制的な `ActSleep` も作らない。休息はすでに体力回復のために事実上避けられない行為で、`RestExposureWeight` との組で「**休まざるを得ないが休むと危ない**」という緊張はすでに生まれている（段階9 の実測＝休まない世界では消耗死 +45%・`PairBondDuration` の途中でペアが壊れる。`HISTORY.md` 追記26）。**今後の測定でこの緊張が弱すぎると分かった場合にだけ**再検討する（保留欄） |
| 38 | **「地域」は空間分割グリッドを粗くまとめたブロック1種類だけにする（2026-09-03）。** 休憩場所（#36）・植物食料の豊かさ（段階15）・将来の「地域ごとの世界リセット」が、**すべて同じ「地域」という語彙の上に乗る**形にする。用途ごとに別の区画分けを作らない |
| 39 | **「豊かな地域は集団で守られる」という結果を仮定しない（2026-09-03）。** `rival`（先制排除の動機）は周囲の食料希少度に比例するので、**豊かな地域ほど希少度が下がって先制排除の動機が消える**可能性がある。その場合に出てくるのは資源防衛理論どおりの「**内側は平和で、乏しい地域との境界が一番荒れる**」構造で、これも成果として受け取る。判定は段階11 の群れ狩り検証（`PreyValue=0` の対照条件）と同じ要領＝**集団形成に関わる動機だけを切った対照条件**との比較で行う |
| 40 | **地域の豊かさに、新しい知覚も新しい記憶の鮮度管理も作らない（2026-09-03）。** 推定は既存の強さ推定・魅力推定と同じ `(100−理性)/100 × ノイズ`（推定する対象が1つ増えるだけ）。植物の分布は動的になりうる（段階17）ので**静的な地図にはせず**、既存の「読み出し時の遅延減衰」＋「接触で起点をリセット」（#22）をそのまま使う。伝承は等価交換（#21）に乗せ、`retaliation` / `pAccept` / `RiskWeight` / `CompetitionWeight` と並ぶ**学習パラメータの1つ**として地域IDごとのベクトルを足すだけにする。**垂直伝承に「教える」行動は作らない**——育児期間（`RearingRadius` / `ChildRearingTicks`）の共在がそのまま経路になる。子の初期値は粒子的遺伝と同じ形で親の一方からコピーし、そこから先は自分の経験と模倣で更新する |
| 41 | **地域の豊かさの記憶は、相手ごとの記憶容量の枠には入れない。ただし遺伝子は共有する（2026-09-03）。** 相手の人数は理屈上いくらでも増えうるので容量で頭打ちにする意味があるが、**地域の数は地図の広さで決まる小さな固定数**なので同じ理由が当てはまらない。別の小さな構造として持たせる。そのうえで**同じ記憶容量遺伝子が、地域の豊かさの推定精度・更新速度にも効く**ようにする（枠数は共有しないが遺伝子は共有する）。段階9 で記憶容量への選択圧が出なかった（`shMemory` 0.07〜0.08）ので、**この経路が記憶容量への初めての実利的な選択圧になりうる** |
| 42 | **植物は軽量な遺伝子だけを持ち、`Decide()` を持たない（2026-09-03）。** 人間・外敵が共有する9本の予算制アーキテクチャは、戦闘も知覚も意思決定も無い植物には過剰なので**持ち込まない**。繁殖は個体の決定ではなく**既存の食料湧きと同じ確率的なイベント**として扱い、そこに既存の「粒子的遺伝＋まれで大きい変異」だけを相乗りさせる |
| 43 | **毒と警告信号は独立した2遺伝子にし、相関させない（2026-09-03）。** 匂いと目立ちやすさは別々の遺伝子にせず「**警告信号の強さ**」1本にまとめる（現実の apparency theory でも、見つかりやすさ・警告の強さ・防御への投資はセットで語られ、3つの独立した軸として扱われていない）。毒と信号を無相関にするのは #25（魅力と実能力を無相関にした判断）と同じ構造で、**正直な警告が進化するかを世界の側に決めさせる**ため。独立させておけば、毒を持たないのに毒があるふりをする個体（ベイツ型擬態）が本物の毒個体の評判に便乗する余地が開く。**これを狙って作り込まず、独立遺伝子にするだけで可能性を残す。** 毒を食べた側のコストは体力への直接的な損害またはその場での栄養価の低下、毒の有無は**隠しパラメータ**で、食べる側は信号から `(100−理性)/100 × ノイズ` で推定する（理性が高いほど信号を正しく読める） |
| 44 | **種子散布は風散布と動物摂食散布の2つだけ採用する（2026-09-03）。** 現実の5系統（風・動物付着・動物摂食・自己・水）のうち、**風散布**は他個体の行動と無関係に実装できるので最小構成として先に入れる。**動物摂食散布**（食べられて消化管を通り別の場所で出る）は既存の `eat` にそのまま相乗りできる（食べたとき一定確率で種が生き残り、食べた個体が移動した先で新しい株が生える**遅延効果**）ので採り、**動物付着散布は採らない**（「付着」という新しい状態を人間・外敵の側に足す必要があるため。現実の分類でも摂食散布のほうがメジャー）。水散布は川・水場が無いので**地形が入るまで見送る**。摂食散布は段階15・16 とも噛み合う——**人間がよく通う場所ほどその植物の種が運ばれる**ので、行動と分布が互いに影響し合う循環が新しいルール無しに生まれる |
| 45 | **栄養バランスに新しい状態量を作らない。そして隠さない（2026-09-03）。** 栄養素ごとの貯蔵量のような状態は作らず、**直近同じ種類の食料ばかり食べていると、その食料の `FoodNutrition` の効果が一時的に目減りする**という、自分の消費履歴だけを使う軽い割引として実装する。寿命（背景処理・`Perception` に載せない）とは逆に、これは**主観的に感じられる**もの（同じものばかりで飽きる）なので、既存の `Hunger` / `ShockRisk` と同じ「自分についてすでに知っていること」の延長として `Perception` にも効用式にも普通に載せてよい |
| 46 | **夜型・朝型（体内時計）は独立した段階にし、段階12 より後に置く（2026-09-03）。** 狙いは、個体ごとに活動しやすい時間帯が違えば**集団の中で常に誰かが起きている**状態が自然にでき、明示的な見張り役の割り当て無しに夜通しの警戒が生まれること（ハッザ族の睡眠研究＝個体ごとの体内時計のばらつきにより、集団全員が同時に眠っている瞬間がほとんど無かったという報告に根拠がある）。ただし**世界に周期的な時間帯という概念自体が無く、これを新設する必要がある**ので休憩場所・地形よりコストが大きい。休憩場所（#36）や地域差（段階15）とは**束ねない**。何が時間帯によって変わるか・遺伝子をどう割り当てるかは、この段階に着手するときに詳細化する |

### 予算制の帰結（実装時に効いてくる点）

- **配分も水準（予算）も遺伝する（2026-08-30 の #2 変更）。** 子は遺伝子ごとに親のどちらかの値を引き、そのあと**自分が継いだ予算**（親の予算 ± 一定幅）に合うようスケールする。**天才の子は、配分の型も予算の大きさも部分的に継ぐ**
- **予算に価格が無いまま遺伝させると、予算は上へドリフトする。** 予算は大きいほど無条件に有利なので、選択は常に上向きに働き、乱数の幅は上下に等しく振れる（復元力が無い）。放っておくと世代とともに平均予算が上がり、**最後は全遺伝子が上限100に張り付いて「配分」という概念が消える**。これは予算制にした目的（配分への選択圧を測ること）が失われる状態で、2026-08-18 に予算を遺伝させなかった理由そのもの
- **なので「予算の平均の推移」を段階7c の必須の測定項目にする。** 世代ごとの平均予算を `cmd/experiment` に出し、**単調に上がり続けるなら価格を戻す**。戻し方の候補は3つ: (a) 維持コスト（予算に比例する基礎代謝＝体力の恒常的な減り）、(b) 親の予算ではなく「親の予算と `GeneBudgetMean` の中間」から引く（平均への回帰。`BudgetHeritability` を 0〜1 の連続値にすればそのまま表現できる）、(c) 上限クリップだけで済ませる（張り付くので非推奨）。**先に入れず、測ってから入れる**
- **`BudgetHeritability` は捨てずに残す。** 0（毎回抽選＝旧仕様）と 1（親から継ぐ＝新既定）を同じバイナリで回せないと、「予算が遺伝することで何が変わったか」を同一シードの対で測れない（「ルールの切替は `Config` の書き換えで表現する」の適用）
- **選択圧は「配分」で測る。** 絶対値で測ると予算のばらつきを拾ってしまうので、`cmd/experiment` の指標は各遺伝子の**予算に対する比率**にする。ここは既存の測り方からの明確な変更点。**予算が遺伝するようになったぶん、絶対値と比率の乖離は前より大きくなる**（水準そのものが世代とともに動くため）ので、比率で測ることの重要性が上がった
- **理性の弱さがそのまま読めるようになる。** 今の未決事項「理性への選択圧が弱い（+1.47。パワー +6.59 に比べて一桁小さい）」は、予算制では「理性に配分するくらいなら攻撃力に回す」という形で直接現れる。理性の配分比が世代とともに下がり続けるなら、理性の利得が価格に見合っていないことがはっきりする
- **クリップと予算の上限に注意。** 遺伝子は1本あたり1〜100なので、予算が 9×100 = 900 に近づくと全部が100に張り付き、配分という概念が消える。天才の予算も**900よりかなり下**（1本あたり70程度まで）に抑える
- **「10年に一度」は tick に翻訳が要る。** 世界に暦が無いので `TicksPerYear` を決める。ただし発生率は「出生あたりの確率」で持つほうがよい（世界側で発生間隔を管理すると engine が世界レベルの状態を持つことになる）
- **`TicksPerYear` を現実の秒数（365×24×3600）から決めてはいけない（2026-08-30）。** このエンジンの tick は現実の秒と対応していない。既存の tick 基準の値は `SkirmishTicks = 30`、`PairBondDuration = 150`、`PlanHorizon = 700`、`cmd/experiment` の標準実行長 20000〜60000tick と、**すべて数十〜数万のオーダー**に収まっている。秒換算すると1年が3000万tickになり、「10年に一度」が実験の実行長の何千倍になって、**天才イベントが一度も発火しない**。既存の tick スケールから逆算し、**数百程度（200〜1000、出発点は500）**から始める。目安は「**100年に一度が実験の実行長（数万tick）と同程度かやや大きい**」（500 なら 100年＝50000tick）
- **`TicksPerYear` は段階7d で本決めにする（2026-08-30 の方針転換・#28）。** 「老いを実装するまでの暫定プレースホルダー」という位置づけは**やめた** — 老いを前倒しで実装するので、待つ相手がいなくなった。決め方は #31 の自立期間（3年）と `cmd/experiment` の標準実行長（20000〜60000tick）の両方から挟む（下の「成長・老い・寿命の帰結」を参照）。段階7c（天才の発生間隔）で使う値と段階7d で決める値は**同じ1つの `TicksPerYear`** なので、7c の時点では出発点の 500 を置き、**7d で確定させたら 7c の測定をやり直す**
- **稀な天才は「進化の駆動力」ではなく「物語」として扱う。** 世界を動かすのは配分の遺伝のほうで、100年に一度の個体が集団の平均を動かすことはない。期待する役割を取り違えないこと。**ただし #2 の変更で天才の予算は子孫に残るので、「1個体の物語」だったものが「恵まれた血統」になりうる。** これも予算の平均の推移で見える

### 魅力・好意・伝承の帰結（実装時に効いてくる点）

- **性選択は戦闘由来の圧よりたいてい強い。** 出生のたびに働くので、全員が魅力に予算を寄せて戦闘力が崩れる暴走が起こりうる。`cmd/experiment` の配分比で必ず前後を見る（`CompetitionWeight` と同じ扱いの危険パラメータ）
- **無相関で進めるぶん、暴走の検出は測定に頼る（#25）。** engine 側にブレーキ（ハンディキャップ原理）を置かないと決めたので、**魅力への選択圧（配分比の変化量＝`dAttractiveness` 的な指標）と個体数を `cmd/experiment` の必須項目にする**。魅力の配分比が単調に上がり続けて他の遺伝子を圧迫する、または個体数が崩れる場合が切替の合図で、そのときの選択肢は「ハンディキャップ原理側の設計に変える」か「`pAccept` の重みを弱める」
- **魅力の推定ノイズは強さより小さくてよい。** 強さは隠しパラメータだが装飾は「見せるためのもの」。今も魅力の推定ノイズは `JudgementNoise` の半分になっている
- **今の世界では集団化は原理的に起きない。** `NODE.md` 11節のとおり、効用式で他個体について語る項は `rival`（消えると得）だけで、**他個体が生きていることが自分の得になる経路が1本もない**。tick をいくら回しても近接は食料への競走の副産物にしかならない。あるべき状態の鍵は魅力でも外敵でもなく、**正の項を1本足すこと**で、**それが入るのは段階11（共同戦闘）と段階12（伝承）**。それより前に集団を探しても見つからないのが正しい（決定事項#17）
- **共同戦闘が集団化の主エンジン、伝承が持続の説明。** 外敵ドロップ（段階11）は「攻撃に参加した者だけ」に配られるので、一緒に殴った相手への好意という強い加点源になる。ただし狩りの最中しか効かない。「好意のある相手からは学べる」（段階12）は常時効くので、集団の**持続**を説明できるのはこちら
- **好意の種は実イベントだけで、ゼロから積み上がる経路は作らない（#19）。** 共在時間で加点する案を捨てたので、最初の種はペア・出産と血縁ブーストから来る。**`ParentIDs` / `ChildIDs` は今は記録しているだけ**なので、ここで初めて使う
- **血縁は1ホップだけ（#27）。** 血統を辿る処理も世代ごとの減衰係数も持たないので、`ParentIDs` / `ChildIDs` の使い道は「親子に初期好意を置く」1箇所で終わる。**祖父母と孫が結びつくかどうかは、育児期間ぶん近くにいて実イベント（接触・模倣）が走るかどうかで決まる** — engine が血縁として先に書かない。#10・#25 と同じ、答えを engine に書かせない構え
- **好意のある相手＝学習源として価値がある → 離れたくない → 群れる。見知らぬ相手＝価値ゼロで `rival` の項だけが残る → 攻撃するか避ける。** 集団間の敵対がこれだけで出る（憎悪を実装しない）
- **敵意は作らない（#18）。** 既存のリスク記憶がそのまま敵意なので、記憶に足すのは好意の1項だけ。状態を増やさない
- **好意は記憶容量を食わせる（段階9）。** ただでいくらでも覚えられるなら全員と好意で結ばれ、集団は世界に1個しかできない。**枠は相手ごとに1つで、リスク記憶・強さ推定と共有する（#26）。** 集団の最大サイズは記憶容量の遺伝子値がそのまま決めるので、A/B で容量を振ればそのまま集団サイズを振れる
- **脅威を多く覚えている個体ほど、味方を覚える枠が削られる（#26 の副産物）。** 枠を敵味方で共有させたことから自動的に出るトレードオフで、そのために新しいルールは書かない。「能力の役割を重ねない」原則にも沿う — 記憶容量という1つの能力の中で敵味方の情報が競合するだけで、選択圧の軸は増えない
- **休息が「ただ」でなくなると、好意が初めて空間に効く（#32）。** 今の `ActRest` はリスク項がゼロなので、どこで休んでも同じ。よそ者の近さをリスクにすると、**同じ休息でも「知らない奴の隣」が高くつく**ようになり、居場所の選び方に好意が効き始める
- **ただしこれは正の項ではなく、よそ者に対する負の項（#32）。** 好意のある相手が近くにいても、**単独でいるのがいちばん安いまま**（リスクを乗せない／割り引くだけで、マイナスにはしない）。したがって**集団化の主エンジンは段階11（共同戦闘）と段階12（伝承）のまま**で、#32 が足すのは「よそ者から離れて休む」という**分離の側**の力。集団ができたことの説明に #32 を使わないこと
- **`ActRest` にリスクを載せると「回復経路を必ず残す」原則に触れる（#32）。** 休息は満腹時の唯一の回復手段なので、重みが強すぎると誰も休めなくなり、世界が単調に消耗する。**重み 0 との対で、餓死率・個体数・平均体力を必ず見る**。原則を壊しかけたら重みを下げる
- **等価交換は「学び尽くした相手の価値が下がる」を無料で手に入れる（#21）。** 加点を「実際に動いた量」に比例させるだけで、値が近い相手との交換は自動的に無価値になる。特定個体への学習の集中（ハブ化）がこれで緩む見込みだが、**見込みであって測定ではない**ので、被参照数の分布を必ず出す
- **接触で減衰の起点が戻る（#22）ので、記憶の寿命が距離で決まるようになる。** 近くにいて頻繁に顔を合わせる相手（家族・配偶者・同じ集団）の好意もリスクもほとんど減衰せず、遠方でめったに会わない相手は素直に消えていく。「一度痛い目に遭った相手を見かけるたびに警戒が新鮮に保たれる」も同じ仕組みから出る
- **空間局在は拡散を遅らせるだけで、止めはしない（#17 の議論とは別問題）。** 伝承も好意も `PerceptionRadius` / `GrabRadius` によって元々近距離限定だが、**近距離のイベントが連鎖すれば、時間をかけて世界全体が1つの文化に均質化しうる**。これを止めるのは距離ではなく、**記憶容量の有限化（段階9）と、突然変異・出生による地域ごとの多様性の継続的な注入**。段階7a のグリッドと有限の記憶帯域は、クラスタが分かれるための必要条件であって十分条件ではない
- **ヒント（#23）は効用式の外に第二の意思決定系を作らない形でだけ入れる。** ヒントは行動を選ばず、候補の効用に加点／減点するだけ。`Utility.Total()` が効用式の唯一の実装であること、トレースが式とズレようがないことを維持する。ここを崩す実装になったら設計をやり直す
- **ヒント枠が予算を食うので、能力の水準が下がる。** 「賢い手口を持つには何かを削る」という予算制の経済にそのまま乗るが、段階7c で取ったベースラインとは水準が変わる。段階12 の測定は**枠 0 本の条件と対にして**読む
- **育児期間は `ChildRearingTicks = 0` で完全に切れる形にする。** 空間分布を直接動かすので、集団化のどれだけが育児由来かを A/B で分離できないと「集団ができた」の解釈ができない
- **垂直伝承（親→子）と水平伝承（模倣）は同じ経路で扱う（#19・#21）。** 親子だから特別、という分岐を書かない。親子は育児期間ぶん共在するので、同じ等価交換が自然に多く走るだけ。伝承の仕組みを2つ持たない
- **多種共存は band（帯）で書く。** 捕食-被食は本質的に振動する（Lotka-Volterra）ので固定値で書くと永久に達成できない目標になる。ニッチ分離の軸には計画済みの食料3種を使う

### 成長・老い・寿命の帰結（実装時に効いてくる点・2026-08-30）

- **年齢補正は1本の関数の両端で書く（#28）。** 成長（0→1）と老い（1→減衰）を別々の仕組みにすると、能力の実効値を決める場所が2つになる。「遺伝的素質 × 年齢補正」の掛け算1箇所に閉じておけば、devview のパネルにも1つの値として出せる
- **閾値をずらしたこと自体が設計（#31）。** 成長の頭打ち＝満腹（100%）、過度コストの開始＝腹八分目（80%）。**この差分だけで「成長中は多少寿命を削っても満腹まで食べる／成長後は腹八分目で止める」という年齢による行動差が出る。** 場合分けも新しい判断閾値も書かない（「ハードコードされた行動閾値を作らない」原則）。**逆に言うと、2つの閾値を同じ値にすると行動差は消える** — A/B で片方を動かして、行動差がこの差分から来ていることを示す
- **満腹に近いのに食べる理由は `rival` が持つ（#31）。** 「食べると寿命が減る」だけなら満腹手前で止まるが、既存の `rival`（その食料が競合相手に渡ると将来自分が損をする）が正の側にあるので、**寿命コストを払ってでも先に食べる**が候補として残る。新しい項を足さないこと
- **子を守る動機も既存の効用式から出す（#29）。** 給餌を作らないので、親ができるのは「近くにいて競合を排除する」だけ。これは既存の先制排除（`CompetitionWeight`）がそのまま働く形で、**子の生存が親の効用にどう入るか**（子の `OffspringValue` を通すのか、育児期間の共在だけで済むのか）は実装時に決める。**足りているかは子の餓死率と自立到達率で測り、不足なら初めて給餌を検討する**
- **死因が3つになるので、既存の A/B のベースラインが動く。** 今は餓死と戦闘の2つ（`NODE.md`）。老化死が加わると、餓死率・戦闘死率の比率が変わり、**段階7c までに取った測定値とは直接比べられなくなる**。段階7d の前後で同一シードの対を取り直す
- **`TicksPerYear` が実験の設計値になる（#28）。** 自立期間3年・想定生涯（3年 × 何倍か）・標準実行長（20000〜60000tick）の3つを同時に満たす値を選ぶ。**老いが一度も発火しない**（生涯が長すぎ）ことも、**老化死が支配的で餓死と戦闘が見えなくなる**（生涯が短すぎ）ことも、どちらも測定が壊れる。段階7d の完了条件で死因の内訳と年齢分布を必ず出す
- **disposable soma 説の簡易近似として文書化する（#31）。** 有限の資源を成長・繁殖に回すか、維持・修復に回すかの配分の結果として老化が生じる、という老化の進化理論の1つ。今回は**食事量だけ**を見るいちばん単純な形にとどめるが、**将来は求愛・戦闘などの激しい活動も寿命消費に効かせられる**（骨格としてはそちらが本来の disposable soma）。**今回は実装しない** — 寿命という状態さえ入っていれば、後から項を足すだけで済む
- **慢性的な低体力コストは `ShockRisk` と役割を分ける（#33）。** `ShockRisk` は「今の体力の低さが、次に何が起きても耐えられないという意思決定上の危険」で、効用式に載って行動選択を変える。**#33 はそれとは別に、体力が低い状態が続いたこと自体が寿命という別の状態量を静かに削る**もので、どの効用の項にも足さない。1つの「体力が低い」という事実から、意思決定用の指標（`ShockRisk`）と長期コストの指標（寿命の慢性コスト）の**2本を別々に読む**形になる
- **弱い個体ほど不利が複合しうる（#33）。** 攻撃側有利（殴る側のコスト＜殴られる側のコスト）と休息の無防備さ（#32）は、どちらも「体力が低い個体はさらに体力を削られやすい」方向に働く。そこに**低体力の継続そのものが寿命を削る**#33 が重なると、弱い個体の不利が二重・三重になりうる。**#33 を有効にする前後で、同一シードの対を `cmd/experiment` にかけ、個体数・餓死率・老化死の内訳が崩れていないかを必ず確認する**（原則「攻撃側有利を保つ」の注意書きと同じ理由）

### 数手先の見積もりとヒントの役割分担（探索の代替・2026-08-31・#34・#35）

- **導入順序が非対称。** (a) 閉じた式は各機能（休息の無防備化、育児の保護など）を実装するたびに、その機能と一緒に追加していけばよい。(b) ヒントは制度自体（段階12）が実装されるまで効果が出ない。**段階12 が入るまでは (a) だけが唯一の手段**になる
- **役割分担の基準は「設計者が式を書き下せるかどうか」。** 設計者が継続の数式を書き下せる場面は (a)、複雑すぎる・気づいていない関係は (b) に任せる。どちらも同じ `Utility.Total()` に加算される項として実装し、アーキテクチャを分けない
- **ヒントの評価は個体ではなく世代規模で行われる。** ヒントは実行時には状況特徴量から行動への単純な参照（O(1)）でしかなく、そのヒントが良いかどうかの評価は個体1回の決定ではなく、**世代を超えた集団規模の生存競争そのもの**が行う。探索のコストを「今この瞬間の1個体」から「時間をかけた進化」に移す設計になっている
- **同じ罠（`StrategyDepthUnlock`）を繰り返さない（#35）。** 2026-08-14 の測定で、知能が高いほど戦略の選択肢が広がるゲートは逆効果（愚かな個体を損な手から保護し、知能への選択圧をむしろ下げた）と分かっている。#34 の (a)(b) をどの能力にも紐づけてゲートしない
- **ヒントの多様性は、多様性そのものを定量化してはじめて意味を持つ。** 段階12 の完了条件にある「地域ごとの言い伝えの分布」に加え、**集団内で実際に使われているヒントの種類数、またはエントロピーのような指標**もあわせて測定する。バラけているだけでは不十分で、**それぞれのヒントが実際に生存率に効いているか**まで確認して初めて意味のある多様性と言える
- **多様性の測定は、地域差が十分ついてから行うほうが良い結果が期待できる。** ヒントの多様性は、好意・集団・外敵などによる世界の地域差が十分あって初めて意味を持つので、これらの仕組みが機能してから測るほうを優先する

### 予算まわりの数値の出発点（要調整）

| 値 | 案 | 根拠 |
| --- | --- | --- |
| 初期配分の分布 | ディリクレ α ≈ 0.5〜1 | 決定事項#24。α が小さいほど極端な配分（1本に集中）が出る |
| 遺伝子の本数 | 9 | 上の9種（魅力を含む） |
| 1本あたりの範囲 | 1〜100 | 現行の `MinAbility` / `MaxAbility` のまま |
| `GeneBudgetMean` | 360（1本平均40） | 現行は3能力×平均50＝150。本数が増えるぶん1本あたりは下がる |
| `GeneBudgetStd` | 30 | 通常個体はおよそ 270〜450 に収まる（**初期個体だけ**に使う） |
| `BudgetInheritSpread` | 30（正規乱数の標準偏差） | 1世代あたりの揺れを初期分布の幅（`GeneBudgetStd`）と同じにしておき、そこから詰める。血統の予算は世代数 n に対して `30√n` で広がる（10世代で ±95 程度）ので、**ここが広すぎると予算のドリフトが速く、配分への選択圧より先に水準が動いて読めなくなる** |
| `TicksPerYear` | 500（段階7d で確定させる） | 既存の tick スケールから逆算。100年＝50000tick で実験の実行長と同程度。**段階7d の自立期間（3年）・寿命・実行長の制約と突き合わせて本決めにする**（#28） |
| 天才（10年に一度） | 予算 495 前後（1本平均55） | 通常の上限を明確に超えるが、まだ配分の余地がある。10年＝5000tick |
| 大天才（100年に一度） | 予算 630 前後（1本平均70） | 900（全部100）の手前で止める |

---

## 未決（実装前に決める）

- [x] **`BudgetInheritSpread` = 30**（2026-09-02 決定）。**「幅が広いほどドリフトが速くなる」という上の見立ては外れました**——7.5 と 60 の8倍差でも `dBudget` は 43／52／54 と動きません。予算の上昇は拡散ではなく**選択**によるもので、幅を広げて実際に起きるのは親から遠い予算の子が死ぬこと（個体数 −15.2 \*）です
- [x] **価格は維持コスト（`BudgetUpkeep = 1`）で確定**（2026-09-02）。`noupkeep` との対で `dBudget` +30.0 \*\*\*・`deathRate` +0.84 \*\*\*・個体数 −12.1 \*。**段階7c 当時「効くが止まらない」だった価格が効くようになったのは、段階8・9 で餓死が本物の死因に戻ったから**
- [x] **出生あたりの確率で確定**（2026-09-02）。**「個体数によってずれる」は実測で3倍ありました**——同じ設定で 20000tick なら33年に一度、60000tick なら11年に一度（個体数が増えて出生が増えるため）
- [ ] **`TicksPerYear` の値**（**段階7d で本決めにします**。2026-08-30 の方針転換・#28。「老い実装待ちの暫定値」ではなくなりました）。**現実の秒数（365×24×3600）は根拠にしません** — このエンジンの tick は現実の秒と対応しておらず、秒換算だと天才イベントが実験中に一度も発火しません。既存の tick スケール（`SkirmishTicks = 30` / `PairBondDuration = 150` / `PlanHorizon = 700` / 実行長 20000〜60000tick）から逆算し、**数百程度（200〜1000、出発点は500）**から始めます
  - 満たすべき条件は3つ: (1) **100年に一度（大天才）が実験の実行長と同程度かやや大きい**、(2) **自立期間3年＋想定生涯が、標準実行長（20000〜60000tick）の中で複数世代分観測できる**、(3) **老いが実行時間内に一度も発火しない／老化死が支配的で他の死因が見えなくなる、のどちらにも転ばない**。(2)(3) は段階7d の測定で確認します
  - 段階7c（天才の発生間隔）と段階7d（寿命）は**同じ1つの `TicksPerYear`** を使うので、7c では出発点の 500 を置き、**7d で確定したら 7c の測定をやり直します**
- [x] **`GeneInitAlpha` = 0.8**（2026-09-02 決定）。**α は「初期の」多様性ではなく、その実行を通しての多様性でした**（変異がまれで継承が粒子的なので founders の配分が遺伝子プールそのものになる）。2.0 では末尾の能力の分散が 30.0 → 23.8（−6.3 \*\*\*）、`killShare` +0.07 \*\*\*、知能への選択圧 −9.6 \*\*
- [ ] **記憶の「単位時間に記憶できる量」と「忘却曲線」の扱い。** 現在の案は、容量だけが予算内、帯域は容量から導出（遺伝子を増やさない）、忘却曲線は予算外の別遺伝子（量ではなく形なので）
- [x] 予算まわりの数値は出発点のまま据え置き。**天才2段階の発生率（0.017 / 0.0017）の確定だけは段階7d に移送**します（`TicksPerYear` が動くと「10年に一度」の意味が変わるため）。正しく集計すると現状は 11.1年／144年 で狙いどおり
- [x] **外敵のサイズ分布は既定（520 ± 90）で確定**（2026-09-03）。`EnemyBudgetMean` を 400／700 に振っても `EnemyBudgetStd` を倍にしても、**参加人数は 1.22〜1.25 で動きません**（96シード）。動くのは人間の個体数だけ（+12.7 \*\*\* / −7.6 \*\*\* / −3.0 \*）。**「1食分の価値が小さいから群れ狩りが現れない」は主因ではなかった**ということです
- [x] **`FitnessConditionWeight` = 0.35 で確定**（2026-09-02。`world.go` の package const から `Config` へ移した）。0 は個体数 −18.2 \*\*、0.7 は個体数 +15.8 \* ですが**採りません**——0.7 では配偶者選びが「今の健康状態」だけになり、遺伝する装飾という独立の選択軸（決定事項#25）が消えるため。**個体数で約2割を払って軸を生かす、という設計上の選択**です
- [ ] **血縁の初期好意ブーストの大きさ**（**範囲は決定事項#27 で「直系の親子のみ・世代減衰なし」と確定**。残るのは値だけ）。初期値を仮置きし、`cmd/experiment` で調整します。大きすぎると家族だけで集団が閉じ、小さすぎると好意の種が育つ前に減衰します
- [ ] **育児期間の長さと、親側の負担。** 子は自由に決定できるのか、親の移動・体力にコストが乗るのか。餓死率が直接動きます
- [ ] **等価交換の歩み寄り率**（1回の交換で差の何割を詰めるか）と、**動いた量を好意に換算する係数**。`observe` 1回で何項目まで交換できるかも決める
- [ ] **ヒント枠の上限本数（案: 3〜5本）と、枠1つあたりの予算消費量。** 未定。実装時にまず粗い値を置き、`cmd/experiment` で調整します
- [ ] **ヒントの状況特徴量として何を許すか。** `Perception` から取れるものに限る、という制約は決まっていますが、具体的な語彙（相手の推定の強さ・自分の空腹度・相手の種族…）をどこまで並べるかは実装時に決めます
- [ ] **深い伝承に相互占有（お互いを一定期間占有し合う状態）を追加するか。** 被参照数の分布を実測してから判断します（決定事項#20）。先に入れない
- [ ] **接触による起点リセット（#22）の「接触」の定義。** 視界内での遭遇でよいか、`observe` による交換のチェックが走ったときだけか
- [ ] **あるべき状態の目標数値**（集団数・戦闘率比・持続時間・種族ごとの個体数の帯）。段階6.5 で現状を測り、**段階11 の後**に書きます（決定事項#17）
- [ ] **寿命消費の既定値**（`MaxLifespan` / `StarveLifespanRate` / `OverfedLifespanRate`）。2026-08-31 に実装した時点の値では標準実行長（20000〜60000tick）でほぼ発火しません（下の段階7d・寿命の項を参照）。年齢補正・成長・`TicksPerYear` が決まってから、`cmd/experiment` で改めて調整します
- [ ] **休憩場所の割引率の幅**（#36）。地域ブロックごとに `RestExposureWeight` へ掛ける係数の下限・上限をどこに置くか。**露出は `RegenRate`（0.09）の12倍の単位で入る**ので、割引を強くしすぎると「安全な地域では休息が常に最善」、弱すぎると何も変わらない。段階9 の測り直し（重み 0.10 で崖に入った件）と同じ注意が要る
- [ ] **地域ブロックの粗さ**（#38）。グリッドを何セル束ねて1地域にするか。**世界は 800×600・視界 130・個体数 111** なので、地形のパッチと同じく「大きく取りすぎると局所適応の前に地域が絶滅する」制約がかかる。出発点は 2〜3 × 2〜3 ブロック
- [ ] **地域ごとの `FoodSpawnRate` の差の付け方**（段階15）。倍率の幅と、地域間で総量を保存するか（豊かな地域を作るぶん乏しい地域を削るか、世界全体の食料を増やすか）。**総量を変えると個体数が動いて条件が交絡する**ので、まずは総量保存で入れる
- [ ] **地域の豊かさのベクトルの持ち方**（#40・#41）。地域IDごとに1つのスカラー（推定の豊かさ）でよいか、分散も持つか（強さ推定と同じベイズ更新にするか）。**記憶容量遺伝子が精度・更新速度にどう効くか**の式もここで決める
- [ ] **毒のコストの置き場所**（#43）。体力への直接損害にするか、その場の栄養価の低下にするか。**前者は死因が1つ増える**（`cmd/experiment` の死因の内訳に列が増える）ので、増やすかどうかを先に決める
- [ ] **摂食散布の確率と遅延**（#44）。食べたとき種が生き残る確率と、何tick後にどこへ生えるか（食べた個体の現在位置か、移動の軌跡上か）。**人間の採食効率に直接効く**ので、他の変更と混ぜずに単独で測る
- [ ] **栄養バランスの割引の強さと履歴の長さ**（#45）。直近何回ぶんの消費を見るか、割引の下限をどこに置くか。**今の世界では効果がほぼ出ないと予想している**（下の段階16 を参照）ので、値を詰めるのは食料源が複数になってから
- [ ] **時間帯の周期と、時間帯によって何が変わるか**（#46）。段階18 に着手するときに詳細化する。先に決めない

---

## 段階6.5：計測の先行整備（あるべき状態を測れるようにする）

**目的**: 「あるべき状態」の目標値を、憶測ではなく現状の測定値から決める。指標はすべて読み取り専用なので安く作れて、以降の全段階の効果測定にも使い回せる。

- [x] クラスタ検出（グリッド上の単連結でよい。段階7a のグリッドが入るまでは素朴な実装で構わない）→ **クラスタ数・サイズ分布**（2026-08-31。`engine.World.Clusters(linkDist)` と `Clustering`。連結距離は `engine.DefaultClusterLinkDist = 30` に固定＝**距離が違う測定値どうしは比べられない**ため。`cmd/experiment` に `clusters` / `clusterSize` / `grouped` / `largestShare` を追加。値の決め方と現状のベースラインは `HISTORY.md` の同日の項）
- [x] **membership half-life**（同じ顔ぶれが一緒に居続ける時間）。「集団」と「たまたま近くにいる塊」を区別する唯一の指標（2026-08-31。`engine.MembershipTracker`。**組（ペア）の共在生存曲線**として測る＝クラスタは合体・分裂するので「同じクラスタ、のちほど」は定義できないため。片方が死んだ組は離れたと数えない。`cmd/experiment` に `halfLife` / `together` / `censored`。現状のベースラインと曲線の形は `HISTORY.md` の同日の項）
- [x] **集団内戦闘率 ÷ 集団間戦闘率**（2026-08-31。`engine.FightTracker`。**書かれたとおりには測れない**ことが実測で分かったので——打撃の届く `CombatRadius`(15) が連結距離(30) より短く、殴り合う2体はつねに同じクラスタにいる——「同じ遭遇を、200tick前から一緒にいた組（仲間）かそうでない組（よそ者）かで分ける」形にした。`cmd/experiment` に `fightCompanion` / `fightStranger` / `fightRatio`。理由と現状の値は `HISTORY.md` の同日の項）
- [x] 集団間の距離分布（2026-08-31。`engine.World.ClusterGaps(linkDist)`。各集団から最も近い別の集団までの最短距離。`cmd/experiment` に `gap` / `gapP10` / `gapRel`。相対値は「構造なしの配置＝1」に較正済み。現状の値は `HISTORY.md` の同日の項）
- [x] 種族ごとの個体数（2026-09-01。`engine.CensusTracker`。**1tickの数え上げでは答えにならない**——基準Aが窓の平均で書かれているのはそのため——ので直近5000tickの窓で持つ。**振動は許容されるので振れ幅は単独では何も言わず、読むのは谷（窓の最小 ÷ 平均）**＝0にどれだけ近づいたか。移入が無い以上0は吸収状態なので、**絶滅は窓ではなく実行全体で覚える**。`cmd/experiment` に `species` / `rareShare` / `rareTrough` / `rareSwing`。今は human だけなので測っているのは人間の個体数の振動そのもので、それが段階11 の比較対象になる。理由と現状の値は `HISTORY.md` の同日の項）
- [x] `cmd/experiment` の出力に上記を追加する（`Spacing()` と同じく O(n²) なので、頻度を落として測る）
- [x] **完了条件**: 現状の世界を 50000tick 回して上記を測り、**数値を `HISTORY.md` に残す**。ここでは目標値を書き込まない（決定事項#17。段階11 より前は集団が形成されなくて当然なので、今の測定値は「集団が無い世界のベースライン」として残す）— 2026-09-01、既定条件・24シード × 50000tick。**8割の個体が塊の中にいるのに集団は無い**（顔ぶれの半減期60tick／`fightRatio` 0.93＝仲間もよそ者もほぼ同じ／`gapRel` 0.80＝集団どうしは避けるどころか寄っている）
- [x] **目標値の記入は段階11 の完了条件に移す。** 外敵が入って共同戦闘という正の項が生まれたあとに同じ指標を測り、その値をもとに「あるべき状態」の表を埋める
- [x] 触る文書: `HISTORY.md`（現状の数値）

**段階6.5 完了（2026-09-01）。** 段階11・12 まで意味を保つのは配置系の B〜E で、能力の値は段階7b・7c の継承則・遺伝子表現の入れ替えで一度リセットされる。

## 段階7a：空間グリッド（挙動を変えない高速化）

**目的**: 以降の全段階の土台。`perceive()` が全ノードを線形に走査している状態（実質 O(n²)）を潰す。段階9（地域ごとの世界リセット）の地域割りもここから取れる。

> 注意: `CLAUDE.md` に「既存の空間分割グリッドを粗くまとめたブロックを流用」とありますが、**空間分割はまだ存在しません**。この段階で初めて作ります。

- [x] `World` に一様グリッドを持たせ、ノードと食料をセルに登録する（2026-09-01。**tickごとの再構築でも移動時の更新でもなく、「古い」印をつけて次の問い合わせまで遅らせる**形にした＝結果は1tickあたり実質2回の再構築。セル幅は `PerceptionRadius` から導出し `Config` には入れない。候補は**添字の昇順**で返す。理由は `HISTORY.md` の同日の項）
- [x] `perceive()` の近傍探索をグリッド経由にする。**円の視界判定はそのまま残す**（グリッドは広域探索の絞り込みだけに使う）— 2026-09-01。近傍を訊く3か所（`perceive()`・`nearestFoodInSight`・`exchangeReadings`）すべて
- [x] 食料ごとの「最も近い他個体」の計算もグリッド経由にする — 2026-09-01。ただし**食料ごとに問い合わせる形にはできない**（ライバルは「観測者に見えている個体」に限られるので、食料の周りを訊くと見えていない個体まで拾って答えが変わる）。他個体の走査に相乗りしているので、上の項目で自動的にグリッド経由になっている
- [x] **完了条件**: 同じシードで、グリッド導入前と**統計値が完全に一致する**ことをテストで確認（挙動不変）— `cmd/experiment` の出力（2条件 × 8シード × 20000tick）がバイト単位で一致。加えて `engine/golden_test.go` に**導入前のエンジンが出した数値**を焼き付けた（同じバイナリ内の比較では「両方まちがっている」が検出できないため）
- [x] `BenchmarkEngineStep` / `BenchmarkEngineStepCrowded` を前後で測り、`HISTORY.md` に数値を残す — 2026-09-01。**数百体では全走査のほうが速く、効き始めるのは500〜600体から**。`BenchmarkEngineStepLarge`（同密度で9倍の広さ）を追加
- [x] 触る文書: `HISTORY.md`（性能の数値）

**段階7a 完了（2026-09-01）。** 得たものは速度そのものより「以降の段階が乗る土台」で、既定の個体数（約146）ではまだ数%の持ち出し。

## 段階7b：継承則の入れ替え（染色体式＋幅）

**目的**: 混合遺伝（両親の平均）をやめる。混合遺伝は毎世代ごとに分散を約半分にするので、今は `MutationStd` だけが多様性の源になっている。粒子的遺伝にすると分散が維持され、選択圧の測定がやりやすくなる。

- [x] `tryBirth()` を「遺伝子ごとに親のどちらかを引く＋正規乱数」に変更（能力3つのまま。遺伝子の追加は 7c）— 2026-09-01
- [x] 引き方は**遺伝子ごとに独立**（決定事項#4）。遺伝子をまとめて引く連鎖群も、性に紐づく染色体も作らない。実装は「遺伝子ごとに 50% でどちらの親から引くか決める」だけ — `World.inheritGene`
- [x] `Config` に `MutationRate` を追加。`MutationStd` を再較正する（2026-09-01。**既定は 0.01 ではなく 0.02 × std 40 にした**。「毎回 std=4 の分散注入 16 に名目を合わせる」＝1% × 40 で測ると、集団に残る分散が15%足りない——大きな跳躍は端（1と100）でクリップされて一部が捨てられるため。確率を2倍にして実測で合わせた。`cmd/experiment` に `jump1` / `jump5` / `jitter` / `nomutation` の各条件を残してある）
- [x] `LamarckRate` を `Config` に用意する（この段階ではまだ学習パラメータが無いので効かない。枠だけ先に置く）— 2026-09-01
- [x] **完了条件**: 継承則の前後で `cmd/experiment` を回し、能力の分散と選択圧の変化を `HISTORY.md` に残す（**過去の測定値と直接比較できなくなる区切り**なので、ここに新しいベースラインを記録する）— 2026-09-01。分散 5.6〜6.1 → 11.0〜14.5、選択圧 2〜3倍、個体数 146 → 170
- [x] 触る文書: `NODE.md`（10-3 の遺伝）、`PARAMETERS.md`（C-8）、`HISTORY.md`

**段階7b 完了（2026-09-01）。** 注意: 個体数が2割増えたので**集団の指標は継承則の効果と交絡**している。能力の平均は上限（100）に押しつけられているので、**絶対値として読めるのは段階7c の予算制まで**。

## 段階7c：遺伝子ベクトル化と予算制

**目的**: 能力3つを `Genome []float64` に置き換え、9種の遺伝子を予算制（予算は親から継ぐ）で導入する。**魅力もここで入れる**（後から遺伝子を足すと、予算も測定のベースラインも取り直しになるため）。

- [x] `Agent.Power/Rationality/Intelligence` を `Genome []float64` に置き換える（インデックスは定数で名前を付ける）
- [x] 遺伝子9種にする: 攻撃力・防御力・最大体力・移動速度・回避率・記憶容量・理性・頭の良さ・**魅力**
- [x] **初期個体の予算だけ抽選する**（`GeneBudgetMean` / `GeneBudgetStd`）
- [x] **初期個体の配分はディリクレ分布から引く**（`GeneInitAlpha` ≈ 0.5〜1。決定事項#24）。α を `Config` に出し、`cmd/experiment` で振れるようにする
  - [x] 標準ライブラリにガンマ分布のサンプラは無いので自前で書く（外部依存を増やさない方針）。Marsaglia–Tsang 法（`NormFloat64` + `Float64` だけで書ける）＋ α<1 用のブースト（`U^(1/α)` を掛ける）で足りる。**乱数は `World` の `*rand.Rand` 1本を使う**
  - [x] **一様乱数の正規化は使わない**（#24）。ちなみに一様の正規化はディリクレ α=1 と等価ではない（α=1 が欲しければ指数乱数を正規化する）
  - [x] 引いた配分を予算に掛けたあと、**遺伝子1本あたりの範囲 1〜100 でクリップして再正規化する**。α が小さいほど極端な配分が出るので、クリップで予算が食い違わないようにする（クリップ後の合計が予算とズレたままだと「予算に対する比率」の測定が狂う）
- [x] **子の予算＝親の予算 ＋ 正規乱数**（平均0・標準偏差 `BudgetInheritSpread`。決定事項#2）。**どちらの親から引くかは 50% のランダム**で、遺伝子の独立分配（#4）と同じ引き方にする。**両親の平均は取らない**（平均は毎世代ごとに予算の分散を半分にするので、混合遺伝をやめた 7b の判断と逆向きになる）
- [x] `BudgetHeritability` を残し、0（毎回抽選＝旧仕様）と 1（親から継ぐ＝既定）を同じバイナリで切り替えられること。0〜1 の連続値にすると「平均への回帰」もこの1本で表現できる
- [x] 継承時の正規化（親から引いた値を、子が継いだ予算に合うようスケール）
- [x] 天才2段階（`GeniusRate` / `GreatGeniusRate` と、それぞれの予算）。`TicksPerYear`（出発点 500。**秒換算は使わない**。**値の確定は段階7d**。上の未決を参照）で「10年に一度」を出生あたりの確率に換算する
- [x] 既存の `Power` の役割（`damagePerTick`）を攻撃力に、`MaxVitality`・`MaxSpeed` を遺伝子に置き換える
- [x] **`fitness` を魅力遺伝子で置き換える**（`world.go:11` の `fitnessPowerWeight` ほか4つを廃止し、`fitness = 魅力遺伝子 × 体調` にする）。パワーに「戦闘に勝つ」と「配偶者を得る」の2役を持たせない
- [x] 魅力は実能力と**相関させない**（決定事項#25。ハンディキャップ原理は入れない）。推定ノイズは強さより小さいまま（装飾は見せるためのもの）
- [x] 再判断トリガーに「魅力の高い個体が視界に入った」を追加する（**行動は足さない**。求愛の効用式は今のまま）
- [x] `cmd/experiment` の能力指標を**絶対値から予算に対する比率へ変更する**（予算抽選のばらつきを拾わないため）
- [x] **段階12 のヒント枠（決定事項#23）が後から同じ予算を食うことを見込んでおく。** 遺伝子9本の配分で予算をぴったり使い切る前提に較正すると、ヒント枠を足したときに全体の水準が下がって過去の測定と比較できなくなる。枠の予算消費量は段階12 で決めてよいが、**`Genome` の表現は「9本＋後から足す可変長のヒント枠」を受け入れられる形**にしておく
- [x] **`cmd/experiment` に「世代ごとの平均予算」と「予算の分布」を出す**（予算が遺伝するようになったので必須。上の帰結を参照）
- [x] **完了条件（2026-09-01・段階8 のあとに判定）**: 「大きくて遅い」「小さくて速い」の並存は**起きない**（各1%・4%。体力と移動速度はどちらも攻撃力に対抗して買われる同じ側で、互いに競合しない）。**ただし異なる種類の個体の並存という条件の狙いは達成**——「殴る対 考える」−0.309、「殴る対 頑丈」−0.215、「殴る対 守る」−0.191（割合どうしの基準線は −0.125）。**完了条件に「どの2つが対立するか」まで書いたのが誤りだった**（`HISTORY.md` 追記25）。**目視の半分も 2026-09-01 に済ませた**（devview: 輪の大きさ＝体力上限・塗り＝残り体力・尾＝速さ・輪の太さ＝攻撃力、パネルに遺伝子9本と予算比）。 experiment で配分の分布を出し、一点に収束していないことを確認。天才の発生間隔が狙いどおりかも出す。**魅力の配分比（`dAttractiveness` 的な指標）と個体数を必ず見る**（性選択が暴走して戦闘力が崩れていないか。崩れていたら #25 の切替を検討する）
- [x] **完了条件（予算のドリフト）**: `BudgetHeritability` 0 と 1 を同一シードの対で回し、**平均予算が単調に上がり続けていないか、遺伝子が上限100に張り付いていないかを確認する**。上がり続けるなら価格（維持コスト or 平均への回帰）を入れて再測定し、数値を `HISTORY.md` に残す
- [x] 触る文書: `NODE.md`（1・3節）、`PARAMETERS.md`（A・C-2）、`HISTORY.md`


**段階7c 完了（2026-09-02）。** 実装とチェックリストは 2026-09-01、**残っていた「実装前に決める」6項目に数値をつけたのが 2026-09-02**（下の未決の節を参照）。**段階8 への申し送りは解決済み**（`killShare` 0.92 → 0.79、個体数 63 → 111）。**予算はもう暴走せず**（20000tickで +51、そこから60000tickまでで +31 と減速して442）、**遺伝子の上限張り付きは19〜27%だが予算は最大900に対して432**——縛っているのは1本あたりの上限ではなく予算のほう（特化であって飽和ではない）。数値と外した予想は `HISTORY.md` の同日の項と追記27。

## 段階7d：成長・老い・寿命と、育児期間（親子の共在）

**目的**: 伝承（段階12）の土台。親と子が一定時間近くにいるようにして、親から子への伝承が起きやすい状況を作る。**この段階だけでは学習は起きません**（伝えるものがまだ無い）。空間分布が先に変わるので、単独で測って切り分けておく。

**2026-09-02 完了。** 決めた値・外した予想3つ・新しいベースラインは `HISTORY.md` の同日の項と追記28。以下のチェックリストは実装時に決めたことを反映してあります。

**成長・老い・自然死をここで前倒しします**（決定事項#28。2026-08-30 の方針転換。`CLAUDE.md` では「将来実装・年齢補正は当面つねに1」として先送りしていました）。**育児期間の設計に成長の概念が不可欠**だからです（「子はまだ弱い」を書けないと、親の保護に意味が無い）。あわせて `TicksPerYear` を暫定値から**本決め**にします。

### 年齢補正（成長と老いを1本の関数で）

- [x] 能力の実効値を「**遺伝的素質 × 年齢補正**」にする（`CLAUDE.md` の当初方針どおりの形。「年齢補正をつねに1にしておく」段階は飛ばす）
- [x] **成長（0→1に伸びる）と老い（1→減衰する）は同じ1本の関数の両端**として実装する（別々の仕組みにしない。#28）
- [x] 掛け算は1箇所に閉じる（実効値を決める場所を2つにしない）。devview のパネルに年齢補正の現在値を出す

### 成長（食事で進む・#30）

- [x] 成長は**食料摂取で進む**。**新しいアクションは作らず、既存の `eat` に相乗り**させる
- [x] **満腹（空腹度0＝100%）に達すると、成長への効果はそれ以上乗らない**（頭打ち）
- [x] 自立期間は**2年**（3年では世界が壊れた。誕生時の実効値も 0.35 → 0.6 に上げた）。自立前の個体は能力が低い＝弱い

**寿命消費（過小・過度）だけを、段階7d の残り（年齢補正・成長・育児期間）より先に実装しました**（2026-08-31）。理由は明示の指示で、寿命の残量・変化を `Perception` に一切含めず、効用式にも新しいゴール項を足さず、完全にバックグラウンドの機械的処理として切り出すこと。ノードは自分の寿命を知らず、死因としての老化も理解しません。選択圧は世代を超えた差次的生存だけを通じてかかります。実装は `engine/world.go` の `World.spendLifespan`（`World.metabolise` から呼ばれる）で、`Agent.Lifespan` が唯一の新しい状態です。

- [x] 寿命を状態として持つ（`Age` は既にある。寿命の残量は別の量＝`Agent.Lifespan`）
- [x] **過小**: 空腹が一定時間続くと、**既存どおり体力を削るのに加えて寿命も削る**（`StarveHunger` を超えている間、`StarveLifespanRate` を毎tick引く）
- [x] **過度**: 摂取が**「腹八分目」（80%程度）を超えた時点**から寿命を削り始める（満腹＝100% ではない。`OverfedHunger`＝空腹度20＝満腹度80%を下回っている間、`OverfedLifespanRate` を毎tick引く）
- [x] **2つの閾値を別の値にする**（成長の頭打ち＝100%、過度コストの開始＝80%）。**行動差は原理的に出ないことが分かった**（成長も寿命も `Perception` に載せないので、ノードに差が見えない。変わるのは結果だけ: `OverfedHunger = 0` との対で `aged` −5.58 \*\*\* に対し `grewUp` +0.02 \*）。当初のメモ——`OverfedHunger` 自体は `SatiatedHunger`（40）より下の20に置いてあり、成長が無くても「無条件に満腹まで食べるのが得」にはならない下地だけは用意した
- [x] 「満腹に近いのになぜ食べるのか」に**新しい判断閾値を足さない**。効用式・`Perception` は無改変（`rival` の項もそのまま）
- [x] 閾値・削り方の係数はすべて `Config` に出し、**過度・過小それぞれ 0 で切れる**こと（`StarveLifespanRate` / `OverfedLifespanRate` を個別に0にできる。A/B のため）
- [x] **激しい活動（求愛・戦闘）による寿命消費は今回は実装しない**（#31 の帰結。将来の拡張余地としてだけ残す）
- [x] `cmd/experiment` に死因の内訳として `aged` / `agedShare` を追加。**既定値（`MaxLifespan=6000`、両rate=0.2）では20000〜60000tickの標準実行長でほぼ発火しない**（8シード×20000tick、6シード×60000tickとも `aged` = 0）。既存個体が寿命を使い切るより先に餓死・戦闘で死ぬため。`brittlelifespan`（`MaxLifespan=1500`）で発火を確認済み（8シード×20000tickで死因の約4%）。**既定値のチューニングは今回は行わない**（未決事項に追加）
- [x] 年齢補正・成長・育児期間・`TicksPerYear` の確定は未着手（下のセクションのまま）
- [x] **慢性的な低体力も寿命を削る（#33。まだ未実装）**: 体力が20%を下回った状態が一定時間続いたら、既存の空腹による寿命コスト（過小側）とは別に寿命を削る
- [x] **既存の `ShockRisk`（今感じられる危険。効用式に載り意思決定に使われる）とは別物として実装する。** この寿命コストは**意思決定に一切関与しない静かな長期コスト**（効用式のどの項にも足さない）
- [x] カウントは**体力が20%を上回った時点でリセットする単純な版**にした。個体数への影響は測定範囲内（`nofrailty` との差は有意でない）ので、累積版の検討は保留
- [x] 閾値（20%）・削り方の係数はすべて `Config` に出し、**0 で切れる**こと（過小・過度と同様）
- [x] **攻撃側有利・休息の無防備さ（#32）と組み合わさると、弱い個体への不利が複合しうる。導入時は必ず `cmd/experiment` で個体数を確認する**（同一シードの有効/無効の対で、個体数・餓死率・老化死の内訳を見る）

### 育児期間

- [x] 出産後、子が `ChildRearingTicks` の間は親の近くに留まる（`PairBondDuration` の「2人が一緒に動く」実装の延長）
- [x] `ChildRearingTicks = 0` で**完全に切れる**こと（集団化のどれだけが育児由来かを A/B で分離するため）
- [x] **子育て中の食糧確保は「保護」だけ（決定事項#29）。給餌の仕組みは作りません。** 子は自分で食料を得ようとするが弱く、親は近くにいて**既存の戦闘・先制排除**で競合相手を排除する、という間接支援だけ
- [x] 親側の負担は**入れない**と決めた（子が留まる側だけで成立する。親は何も知らない）
- [x] 子の生存は**親の効用に入れない**と決めた（共在だけで済ませる。効用式に新しい項を足さない方針どおり）

### `TicksPerYear` の確定（#28）

- [x] 「老い実装待ちの暫定プレースホルダー」をやめ、**本決めの値**にする
- [x] 満たすべき条件: (1) 大天才（100年に一度）が実行長と同程度かやや大きい、(2) **自立期間3年＋想定生涯が、標準実行長（20000〜60000tick）の中で複数世代分観測できる**、(3) **老いが実行時間内に一度も発火しない／老化死が支配的で他の死因が見えなくなる、のどちらにも転ばない**
- [x] 確定したら、**段階7c の天才の発生間隔の測定をやり直す**（同じ1つの値を使っているため）

### 完了条件

- [x] **死因の内訳に「老衰」が加わった**（戦闘62%・餓死34%・消耗4%）。 餓死・戦闘死・老化死の3つを `cmd/experiment` に出し、**どれか1つが支配的になっていないこと**を確認する（`TicksPerYear` の条件(3)）
- [x] **年齢分布と世代数**を出し、標準実行長の中で複数世代が回っていることを確認する
- [ ] ~~**成長中と成長後で食べ方が変わることを目視**~~ — **この条件は満たせない。** 成長も寿命も `Perception` に載せないという同じ段階の設計と矛盾しており、ノードに見えない差が行動を変えることはない。実測でも変わるのは結果だけ。**完了条件を「行動差」ではなく「結果の差」で書くべきだった**
- [x] **子の餓死率と自立到達率**を出す。保護だけで足りているかの判断材料にする（不足なら給餌の追加を検討・#29）
- [x] `ChildRearingTicks = 0` との対: `deathRate` +0.71 \*・`starveRate` +0.57 \*・`nearest` +19.5 \*・`childDeathShare` +0.01 \*。個体数とクラスタ数は有意に動かない。**子の生存率（`grewUp`）は動かないので、効いているのは保護ではなく凝集**
- [x] **段階7c までに取った測定値と直接比べない**（死因が増えてベースラインが動くため）。同一シードの対を取り直す
- [x] **#33（慢性的な低体力による寿命コスト）を有効/無効の同一シード対で `cmd/experiment` にかけ、個体数・死因の内訳（餓死／戦闘／老衰）を確認する。** 攻撃側有利・休息の無防備さ（#32）との複合で弱い個体の不利が増幅していないかを見る
- [x] 触る文書: `NODE.md`（1・4・10-3節と「今は無いもの」の老いの項）、`PARAMETERS.md`（`Age` の行を含む）、`CLAUDE.md`（将来実装から外す）、`HISTORY.md`

## 段階8：攻撃・防御・回避のチャネル化

**目的**: 要望「攻撃力、防御力、移動、回避の使い方によって体力の減りが決まる」。

**この段階が一番危険です。** 攻撃側有利の非対称量が動くので、`CompetitionWeight` / `SkirmishTicks` と同じく個体数を崩しうる。

- [x] effort をプリセット3種（攻め寄り／守り寄り／逃げ寄り）に拡張し、戦闘系の行動にだけ掛ける
- [x] 防御＝ダメージ乗数を下げる／体力で払う。同tickの攻撃出力も下がる
- [x] 回避＝確率で完全無効／行動機会で払う（回避したtickは攻撃・食事ができない）。移動速度に依存させる
- [x] 体力消費 = Σ(チャネルごとの 最大値 × 使用率 × 単価)
- [x] **完了条件**: `BenchmarkDecide`（現在 8.0µs、候補約40件）が候補約70件でどれだけ増えるかを測る。experiment で**個体数・死因の内訳を必ず確認**（防御と回避への選択圧が別方向に出ているかも見る）
- [x] 触る文書: `NODE.md`（6・7節）、`PARAMETERS.md`（C-3）、`HISTORY.md`

**段階8 実装完了（2026-09-01）。「一番危険」という上の見立ては外れた**——個体数は 86 → 111 に増え、`killShare` は 0.92 → 0.79 に下がった。危険だったのは段階7c の予算制を防御の選択肢なしで入れることのほうだった。数値は `HISTORY.md` の同日の項。

## 段階9：記憶の容量・帯域・忘却曲線と、好意

**目的**: `opinions` を有限容量にし、忘却を「容量の取り合い」にする。副産物として記憶が O(個体数) から O(容量) に落ちる。あわせて、記憶に**初めての正の項（好意）**を入れる。

**この段階で好意が効用に効くのは #32（休息の無防備さ）の割引だけです。** 正の項として効かせるのは段階12（伝承）と対にします。

**敵意は新設しません**（決定事項#18）。既存のリスク記憶（その相手に失った体力の累積・遅延減衰つき）を**そのまま敵意として扱います**。この段階で記憶に足すのは好意の側だけです。

**2026-09-02 実装完了。** 下のチェックリストは実装時に決めたことを反映してあります（測定値と、外した予想は `HISTORY.md` の同日の項と追記26）。

### 容量・帯域・忘却曲線

- [x] `opinions` を容量つきに変更 — 捨てるのは**減衰後のリスク＋好意が最小**のレコード。ただし**価値の残っているレコードは、群衆の一人には押し出されない**（押し出せるのは殴られた・ペア成立・出産という世界の側の出来事だけ）
- [x] 単位時間に更新できる `Opinion` 数の上限（帯域）— **容量から導出**（`MemoryBandwidthShare`）。専用の遺伝子は作らなかった
- [x] 忘却曲線を遺伝子にする — 減衰率に `(100 − 記憶)/49.5`（0〜2倍）。**記憶が平均の個体はちょうど従来の率**

### 好意（正の記憶）

- [x] **好意**を `Opinion` の**新しいフィールド**として足す（リスク記憶と同じく遅延減衰）。**専用の別枠は作りません**（決定事項#26）。相手ごとに1レコード＝1枠で、リスク記憶・強さ推定・好意がその枠を共有する
- [x] 容量は既存の**記憶容量の遺伝子**でそのまま管理する。**好意専用の遺伝子もパラメータも足しません**（ただなら全員と好意で結ばれ、集団は世界に1個になる）
- [x] 好意の発生源は**実イベントだけ**（決定事項#19）。**共在時間による加点は実装しません**（当初案。2026-08-30 に不採用）
- [x] この段階で入る発生源は2つ:
  - [x] **ペア形成・出産** — 既存の `PairBondDuration` 等で成立する関係そのものを、両者にとって最初の好意の種にする
  - [x] **血縁** — `ParentIDs` / `ChildIDs`（**現状は記録しているだけで未使用**）を使い、親子に初期好意ブーストを与える。ゼロから積み上げなくても種ができるようにする
  - [x] 血縁は**直系の親子（1ホップ）のみ**（決定事項#27）。**祖父母・孫・きょうだいは対象にせず、世代をまたぐ減衰ルールも作りません**。血統を辿る処理も減衰係数も持たない
  - [x] ブーストの大きさは**初期値を仮置き**し、`cmd/experiment` で調整する — `AffinityKin = 22`、ペア成立・出産は各 18。`noaffinity` 条件で全部 0 にできる
- [ ] 残る2つの発生源はあとの段階で足す: **共同戦闘**は段階11、**継承（出産・伝承で実際に何かを受け取ったとき）**は段階12（**この2つはまだ入っていない**）
- [x] 好意が効用に効くのは、この段階では**#32（休息の無防備さ）の割引だけ**。**正の項として効かせるのは段階12（伝承）と対にする**。どちらも重みを `Config` で 0 にできること

### 休息を無防備にする（決定事項#32）

- [x] `ActRest` の `Utility` に**リスク項**を足す。中身は「**近くにいる、好意を持っていない他個体の存在**」。今の `addRest` は `Life` の項だけで、リスクがゼロ（どこで休んでも同じ）。**これは #34(a) の「閉じた式による数tick先の見積もり」パターンの実例**（`PlanHorizon`/`ShockRisk` と同じ形で、分岐せず今の状態が続いたときの被害を1本の軌道として見積もる）
- [x] リスクの大きさは**既存の推定だけ**から作る（相手ごとの `EstStrength`・リスク記憶と、距離）。**新しい状態も新しい強さ評価器も追加しません**
- [x] **好意のある個体が近くにいる場合は、このリスクを乗せない（または割り引く）。** 好意は #26 で決めた**既存の記憶レコードのフィールドを参照するだけ**
- [x] **既存の走査に相乗りさせ、追加の探索コストを出さない。** 今は `addRest` が先に走り（`incoming(p)` が `p.Others` をなめる）、そのあと `addAgents` が同じスライスをなめている。**`Decide` の先頭で `p.Others` を1度だけ走査して「被攻撃ダメージ」と「よそ者の露出量」を同時に作る**形にまとめ、走査の回数を増やさない
- [x] `BenchmarkDecide` を変更の前後で取る。**走査が1本増えていたら実装が間違っている** — 走査は減った（`incoming()` の重複が消えた）のに **10.0 → 11.2µs**。犯人は走査ではなく**遅延減衰の `exp`**（容量あふれの判定が全レコードの読み出しになる）。詳細は `HISTORY.md` 追記26
- [x] 重みは `Config` に出し、**0 で完全に切れる**こと（`RestExposureWeight`。既定 0.05）
- [x] **原則「回復経路を必ず残す」の確認を完了条件に入れる**（下）。**壊れ方は「誰も休めない」で合っていた**が、そこから個体数に効く経路が違った（消耗死 +45% → `PairBondDuration` の途中で片方が死んでペアが壊れる）
- [x] 今回の対象は `ActRest` だけにする。`eat` や `observe` にも同じ無防備さを広げるかは、休息で効き方を見てから決める

### 接触による減衰起点のリセット（決定事項#22）

- [x] 減衰式 `exp(−減衰率×経過tick)` は**変えない**。変えるのは**「経過tick」の起点（最後にいつ更新されたか）の扱いだけ**
- [x] 今は「値そのものが動いたとき」だけ起点が更新される。これに加えて、**その相手と接触したという事実だけでも起点を更新する**
- [x] **好意・リスク記憶の両方に適用**する（一度痛い目に遭った相手を見かけるたび、新しい被害が無くても警戒が新鮮に保たれる）
- [x] この段階では「接触」＝視界内での遭遇でよい。段階12 で**等価交換のチェックに相乗り**させる（差がゼロで交換が不成立でも起点は更新する）。新しいトリガーは追加しない
- [x] 帯域（上の項）を食うかどうかを決める — **食わせた**

### 完了条件

- [x] **#32 の重み 0／有効の同一シード対で、餓死率・個体数・平均体力を確認する** — 重みを 0.35 → **0.05** に下げた上で、末尾の個体数は動かず（−0.2 ± 6.9）。**0.35 が個体数を半分にした理由は、避け合いではなく「露出が `RegenRate` の12倍の単位で入るので休息の価値が丸ごとゼロになる崖」**（測り直しは `HISTORY.md`）
- [x] **#32 が効いていることを devview で目視**／experiment で `restNear` を見る — 6.5 → 2.0（\*\*\*）
- [x] **1ノードが同時に好意を持てる相手の人数が、記憶容量で頭打ちになる** — 容量 6/12/24/∞ で `friends` 1.01/2.16/2.55/3.88
- [x] devview で、容量の小さい個体が「知っているはずの相手を忘れている」のが見える（容量2の個体が自分の子を忘れている）。パネルに `memory n/上限  forgets at x倍` と好意の列を追加した
- [x] **記憶容量への選択圧を測った** — **正でも負でもなく、出ない**（`shMemory` はどの条件でも 0.07〜0.08）。段階12 で再測定する
- [x] 触る文書: `NODE.md`（8節）、`PARAMETERS.md`（A / B / C-4 / C-5 / E）、`HISTORY.md`

## 段階10：見た目からの学習

**目的**: 初対面の相手に `PriorStrength = 50` を当てはめるのをやめる。

- [x] 遺伝子から観測可能な相関値 `Appearance` を作る（体力遺伝子と移動速度遺伝子の平均 × 年齢補正）＋観測ノイズ（`AppearanceNoise` = 6。**強さの 40 よりずっと小さい**）
- [x] `AgentView` に `Appearance` を載せる。**真値は載せない**（型による保証を維持）
- [x] 各ノードが「見た目→能力」の写像を自分の戦闘・観戦から学習する（最小二乗の直線1本）。engine は写像を持たない。**傾きは0へ引き戻す**（`AppearanceSlopePrior` = 60。素の最小二乗は観測数が数十では損）
- [x] **完了条件**（2026-09-03）: **測り方を変えた。** 条件どうしの比較では出会う相手の顔ぶれが違って交絡するので、**同じ実行の同じ遭遇を3つの推定器で採点**する形にした。学習の利得は **+3.04 \*\*\***（20000tick）・**+8.07 \*\*\***（50000tick）、経験のある観察者は 26.6 に対し無い観察者は 33.5。devview のパネルで、まだ会っていない相手（`seen 0`）の推定が相手ごとに違う値になっているのを目視で確認した
- [x] **分かったこと**: **利得の89%は「世界の平均を知ったこと」で、体つきを読んだぶんは11%（+0.34）。** 体つきと攻撃力の相関は +0.22 しかない（予算制では、大きい体は予算が大きい＝正の相関と、体に使った予算は殴りに使えない＝負の相関が打ち消し合う）
- [x] **個体数は 22.8 → 14.8 に落ちる。原因は学習ではなく水準**——`PriorStrength` を 50 から実測平均の 36 に下げるだけで同じだけ落ちる（`lowprior` 条件）。50 という「暫定値」がよそ者への警戒を通じて世界を支えていた
- [x] 触る文書: `NODE.md`（8-2）、`PARAMETERS.md`、`HISTORY.md`

**段階10 は 2026-09-03 に完了。** 申し送り: **`rareTrough`（希少種の谷）が 0.33 → 0.22 に下がり、あるべき状態A の帯（0.3以上）を割った**（96シードで絶滅は0）。段階12 のあとに測り直す。

## 段階11：種族の汎用化と外敵（協力の土俵）

**目的**: 「隣人が生きていると自分が得」を世界側の事実として置く。協力という言葉は engine に登場させない。

- [x] 人間・外敵で同じノード構造・同じ意思決定エンジンを共有する（種族ごとのパラメータ範囲＋種族固有遺伝子で差を出す）
- [x] 外敵に「大きさ」を持たせ、**倒したときのドロップ数を大きさで決める**
- [x] 分配は「攻撃に参加した者だけ」（`ENEMY.md` の方針。「近くにいる全員」は誰も狩らなくなるので選ばない）
- [x] **共同戦闘を好意の加点源にする**（2026-09-03。決定事項#19）。分配ルールで共に参加した相手に好意を加点する（`AffinityHunt` = 6）。**「主要な」加点源にはなりませんでした**——群れ狩りが現れない世界では発火が 20000tick に13.7回しかなく、集団の指標（`halfLife` / `fightRatio` / `gapRel`）はどの値でも動きません。**入れた意味は段階12 の前提（非血縁の好意）を作ったことのほうにあります**。数値と外した予想は `HISTORY.md` の同日の項
- [x] 食料3種（共通食料A/B＋死体からの肉）。人間は人間由来の肉を食べない、外敵は種族を問わず食べる
- [x] **完了条件**: 群れ狩りが**現れるか現れないか**を測る（`Spacing()` の `Clumping`、外敵の討伐数、参加人数の分布）。**現れなくてもこの段階は成功**です。現れなかったという事実が、明示的な協力機構を再検討する根拠になります。あわせて**あるべき状態 A（種族の共存）**を段階6.5 の指標で判定する（帯で見る。振動は許容）
- [x] **完了条件（追加・決定事項#17）**: 段階6.5 の指標をここでもう一度全部測り、**その値をもとに「あるべき状態」の表に目標値を書き込む**。ここが、集団が形成されうる世界になった最初の時点なので、B〜E の目標値を書けるのもここから
- [x] 触る文書に `PLAN.md`（あるべき状態の表に数値を入れる）を追加する
- [x] 触る文書: `ENEMY.md`（「確定したルール」へ移す）、`NODE.md`、`PARAMETERS.md`、`HISTORY.md`

**段階11 完了（実装 2026-09-01、残り1項目と未決の決着 2026-09-03）。群れ狩りは現れなかった**（対照条件つきで確認）。詳細は `HISTORY.md` の両日の項、確定したルールは `ENEMY.md` 3節。**外敵のサイズ分布は既定で確定**（400／700／標準偏差180 のどれでも参加人数は 1.22〜1.25 で動かず、動くのは人間の個体数だけ）。

## 段階12：伝承（等価交換）とヒントの進化

**目的**: 今グローバル定数になっている効用式の仮定を個体ごとの値にして学習させ、**それを他個体と交換できるようにする（伝承）**。さらに、伝わる中身を固定名のスカラーに留めず、**個体ごとに可変な「ヒント」**へ広げる（決定事項#23）。あるべき状態 B〜E はここで決まります。

**伝えるのは effort の配分そのものではなく、effort を決める前提**（決定事項#15）。前提が伝われば「あの集団は守り寄りの effort を選ぶ」は結果として現れます。

**12c のヒントは、数手先を探索する代わりの手段（決定事項#34）です。** 設計者が閉じた式を書き下せる場面は既存の `PlanHorizon`/`ShockRisk`/`SkirmishTicks` などのパターンをそのまま広げ（#34 の(a)）、書き下せない・気づいていない関係だけをヒントに発見させます（#34 の(b)）。ヒントの評価は個体の1回の決定ではなく世代を超えた生存競争が行うので、実装（12c）が入るまでは (a) だけが唯一の手段です。**知能・理性でヒント枠や見積もりの深さをゲートしないこと**（決定事項#35。`StrategyDepthUnlock` と同じ轍を踏まない）。

### 12a：学習対象を個体ごとの値にする 【完了・2026-09-04】

- [x] 対象: `retaliation = 0.7`（相手は7割で殴り返す）／`pAccept = 0.6`（求愛は6割で受け入れられる）／`RiskWeight` / `CompetitionWeight` / `ShockRisk` — `engine/lore.go`。5つとも `SelfView` から読む形になり、`controller.go` の package const は消えた
- [x] **世界の事実**（`retaliation`, `pAccept`）は正解が世界側にあるので、強さ推定と同じベイズ更新で収束させる — 走る平均＋証拠の数の上限（`LoreMemory`）。上限が無いと老いた個体が世界の変化に気づけない
- [x] **好み**（`RiskWeight`, `CompetitionWeight`, `ShockRisk`）は正解が無いので、遺伝＋突然変異＋伝承に任せる — 粒子的遺伝（親のどちらかから丸ごと）＋比例の突然変異、世界の値の4倍でクリップ
- [x] 材料は**成功体験と失敗体験、観察由来を含む**。**新しい観察の仕組みは作らない** — 殴り返しは攻撃側と傍観者が、求愛の答えは求愛した側だけが学ぶ。**ただし単位は打撃ではなく engagement**（`retaliationSampleTicks` = 5）。1打撃ごとに数えると長い戦い1つが世界の答えを支配する
- [x] `LearningRate = 0` で全部止められること — **そして既定が 0 になった**。効かないからではなく効くから: 世界の真の殴り返し率は 0.15 で、`0.7` という思い込みが世界を支えていた（個体数 19.0 → 10.1、増えるのは殺しではなく餓死）。好みの側は既定オン（`LoreInitSpread` = 0.15、較正済み）
- [x] **申し送り**: 信念の学習は段階12b で世界に本物の抑止力が入ってから測り直す。A/B の腕（`nolore` / `onepreference` / `learning` / `lamarck` / `spread10` / `spread25` / `retal45` / `retal20`）と12の指標は `cmd/experiment` に入れてある
- [x] **副産物**: 好みが可変になるだけで**理性への選択圧が −6.95 → −0.83 \*** に戻る。全員が同じものを欲しがる世界は、世界を正確に読むことに報いていなかった

### 12b：伝承＝等価交換（決定事項#20・#21・#22）【完了・2026-09-04】

- [x] 深い伝承は**既存の `ActObserve` に相乗り**させる。`observe` が既に持つ体力コストと時間コスト（`observeTicks`）をそのまま流用し、**追加のコスト設計はしない**
- [x] 排他性は、まず `observe` が持つ自然な排他性（**1度に1人しか観察できない＝発信側の帯域制限**）だけに任せる — 足す必要は無かった
- [x] 更新は一方向の「学ぶ」ではなく、**双方向・同時の歩み寄り**にする — ただし**1回あたりは差の2%**。「中間で会う」0.5 は段階12a が作った多様性を消して個体数を半減させる（交換は1実行に約19000回起きる）
- [x] **好意の加点量は、交換で実際に動いた値の大きさに比例** — 判定は書かず、算術の結果として出る
- [x] 相手が持っていて自分が持っていないヒント（12c）は、歩み寄りではなく**そのままコピーする** — 空の枠を持つ相手にだけ入る
- [x] 交換のチェックが走った時点で、**差がゼロで不成立でも減衰の起点を更新する** — ただし**すでに覚えている相手だけ**（意見が一致しただけの他人のために記憶枠を潰さない）
- [x] **垂直伝承（親→子）と水平伝承（模倣）は同じ経路で扱い、特別扱いしない** — 家族についての規則はどこにも無い
- [x] **好意を効用の正の項にする**（`LoreValue` = 9。0 で切れる）
- [x] `LamarckRate` を有効化 — 段階12a で済んでいた
- [x] **測る**: 1個体あたりの**被参照数の分布** — 上位5分の1が 0.48。**較正してから読んだ**（完全均等 0.20、無作為の雑音の底 0.22）。偏りは実在するが「数人の教師と群衆」ではないので、**相互占有は足さない**

### 12c：ヒント（進化し続ける言い伝え）【完了・2026-09-04】

**目的**: 伝承の中身を、固定名の少数のスカラーから、**個体ごとに可変な「ヒント」の集合**へ広げる。

**必須条件: ヒントは行動を直接決めない。** 既存の効用比較に対する**加点／減点としてのみ**働く。

- [x] 1個体は上限4本の**ヒント枠**を持つ。各枠は 状況特徴量 × 対象候補行動 × 重み の3つ組
  - [x] 状況特徴量は既存の `Perception` から取れるものに限る（8種）。すべて 0..1 に正規化し、対象のいない候補では対象由来の4つは0になる
  - [x] 対象候補行動は既存の7行動のどれか
- [x] **ヒント枠は遺伝子予算を消費する** — **空の枠でも払う**（払っているのは入れ物）。値段は較正した（15 では割に合わず、5 で対照に勝つ）
- [x] 突然変異は二段階（重みの微調整＝通常の変異／**構造の変化＝天才イベントの流用**）
- [x] 継承は既存の**染色体式継承を枠単位に拡張**
- [x] トレースに「**ヒント由来の加点**」を出す — `Utility.Hint`、`Total()` の唯一の実装は崩していない。devview では `hunch +2.30`
- [x] **知能・理性でゲートしない**（決定事項#35）
- [ ] **申し送り**: ヒントは**頭の良さへの選択圧を下げる**（`dIntelligence` がどのヒント条件でも既定より低い）。安い経験則があると手を丁寧に評価する見返りが減る、という筋の通った副作用だが、狙ったものではない

### 完了条件

- [x] experiment に**分散**の指標を足した（`sdRiskWeight` / `sdCompetition` / `sdShock`）。**これが無ければ段階12b が多様性を消していたことに気づけませんでした**（平均は動かない）
- [ ] **どのヒントがどの地域で定着しているか**（地域ごとの言い伝えの分布）— **地域という語彙がまだ無いので段階14 待ち**。今は集団全体の種類数とエントロピーだけ出している
- [x] **多様性そのものを定量化する指標**（`hintKinds` / `hintEntropy`）。**それぞれのヒントが生存率に効いているかは `deadhints`（枠も値段も同じで重みだけ0）との対で確認した**（40000tickで個体数 22.0 対 14.8）。ただし**多様性そのものを作っているのは重みではなく模倣**だった
- [x] 好意の重み 0／有効の A/B（`noexchange` / `notrust`）
- [x] **あるべき状態 B〜E を段階6.5 の指標で判定した**（40000tick）: `fightRatio` 0.43（目標0.6以下）**達成**／`halfLife` 227（目標150）**達成だが標準誤差±85で下限値**／`gapRel` 0.34（目標1.2以上）**未達**。**集団は一緒にいるようになったが、集団どうしが避け合う段階には入っていない**
- [x] **被参照数の分布**を確認した（上位5分の1が0.49、雑音の底0.22）。ハブ化していないので**相互占有は足さない**
- [x] **個体数と死因の内訳を確認した**。**正の項は暴力度を下げる、という予測は当たらなかった**——`killShare` は動かず、動いたのは個体数（40000tickで −12.7 \*）と餓死率。**20000tick では見えず、実行長を倍にして初めて出ました**
- [x] 触る文書: `NODE.md`（5-3c・5-3d）、`PARAMETERS.md`（C-5c）、`README.md`、`HISTORY.md`

## 段階13：視界をグリッド単位にする（挙動変更）

**目的**: 要望「視力によって見えるグリッド数（周囲1グリッド）」。7a と分けるのは、こちらが**挙動の変更**だからです。

- [x] 視界を円から「自セル＋周囲1グリッド」に変更した（2026-09-04、`engine/sight.go`）。**セルの大きさは円と同じ面積になるよう較正**（76.8）——合わせないと「見える量」と「見える形」の変化が分離できない。`SightGrid = false` で円に戻る
- [x] **視力（N）は遺伝子にしない。** 0リングは致死（個体数 4.4）・1が最適・2はやや悪い、という**離散で極端に非線形**な量なので、遺伝子にすると連続量である予算に対する固い閾値になる（`StrategyDepthUnlock` と同じ轍）。しかも 0 が致死なので 1 に張り付いて動かない。**個体差を入れるならリング数ではなくセルの大きさを連続に振る**（10本目の遺伝子になるので必要になってから）
- [x] **完了条件**: 逃走成功率の指標（`flees` / `escapeShare`）を新設して前後で測った。**逃走成功率は動かなかった**（+0.02 ± 0.04）——面積を合わせると逃げ切るまでの距離の平均まで揃い、**異方性は分散を増やすだけで平均をずらさない**。動いたのは殺し（`killRate` −0.13 \*、`killShare` −0.02 \*）で、機序は「**近いのに見えない相手**は攻撃の候補に挙がらない」。`neighbours`（円の物差しで測った近傍数）が不動なのが較正の検算になっている
- [x] 触る文書: `NODE.md`（5-2）、`PARAMETERS.md`（C-5e）、`README.md`、`HISTORY.md`
- [ ] **申し送り**: `Step` が +28%（345 → 442µs）。ブロックは隅の方向に円より遠く届くので問い合わせ範囲が広い。途中で「半径ではなく矩形で問い合わせる」「インデックスのセル幅をブロックの幅の半分から導く」の2つを直して 520 → 442µs まで戻した

---

## 環境と食料生態系（段階14〜18・2026-09-03 追加）

**この5つは「世界の側」を作る段階です。** ここまでの段階が個体（遺伝子・記憶・意思決定）を作ってきたのに対し、ここからは**個体が置かれている場所に差をつける**段階になります。段階番号は 2026-09-03 の追加時に振ったもので、**着手は段階12（伝承）と並行でも構いませんが、段階15 の伝承部分だけは段階12b が入っていないと載る先がありません。**

**導入順序**（この順を守る。理由は各節に書いた）

| 順 | 段階 | 何を入れるか | なぜこの位置か |
| --- | --- | --- | --- |
| 1 | 段階14 | 休憩場所 | 既存の `RestExposureWeight` に係数を1つ掛けるだけで一番安い。地域という語彙（#38）をここで作る |
| 2 | 段階15 | 植物食料の地域差（**固定版**）＋ 地域の豊かさの認識・伝承 | 地域が「食べ物の量が違う場所」になって初めて、地域を覚える意味と伝える意味が出る |
| 3 | 段階16 | 食料の栄養バランス | **効果がほとんど出ないことを先に確認しておく**ためのベースライン（下記） |
| 4 | 段階17 | 植物の遺伝と種子散布（段階15 の固定版を**置き換える**） | 同時に入れると、地域の記憶の効果なのか植物生態系の変動そのものの効果なのかが切り分けられない |
| 5 | 段階18 | 夜型・朝型（体内時計） | 世界に時間帯という概念を新設する必要があり、**段階12 より後**（#46） |
| 6 | — | 地形全般（川・高低差など） | `CLAUDE.md` の「将来実装」のまま。下ごしらえ2つは下の節 |

**いずれも「1変更＝1つの `Config` トグル」にします。** `CLAUDE.md` の測定手順（**実装前に期待する数値変化を書く** → `cmd/experiment` と `cmd/devview` の両方で確認する → **予想が外れた場合も含めて** `HISTORY.md` に記録する）に従います。

---

## 段階14：休憩場所（既存の休息露出への相乗り）

**目的**: 「休むのに向いた場所」と「向かない場所」を世界に作る。**新しい仕組みは作らない**（#36）。

- [x] **地域ブロック**を `World` に持たせた（2026-09-04、`engine/region.go`。4×3＝12ブロック）。休憩場所・食料の豊かさ・将来の地域リセットが全部これを使う。**壁ではなく、地域IDを読むルールはゼロ**
- [x] 地域ごとの「休息のしやすさ」の係数を `RestExposureWeight` に掛けた。**掛け算が1か所増えただけ**
- [x] `Config` トグル（`ShelterSpread = 0`）: **全地域1.0になり、かつ乱数を1つも消費しない**ので、段階14 前と1ビットも違わない実行になる（テストで固定）
- [x] `cmd/devview`: 地域の境界と係数を描いた（緑がかったブロックほど休むのに向いた地面）。右パネルに「今立っている地面の係数」も出る
- [x] **予想を先に書いた**（`shelterGain` が正／`nearest` が縮む／`restNear` が上がる／個体数不変／細かい地域では薄まる）
- [x] **完了条件**: **5つ中2つしか当たらなかった。** 当たり＝**選別は起きる**（`shelterGain` +0.02 \*\*、差を±1.0にすると +0.06 \*\* と**比例する**＝機序が本物である証拠）と**個体数不変**。外れ＝**凝集しない**（`nearest`・`clumping` とも誤差内）、`restNear` 動かず、**細かい地域でも薄まらない**。目視でも地域への偏りははっきりとは見えない
- [x] 触る文書: `NODE.md`（8-1）、`PARAMETERS.md`（C-5f）、`README.md`、`HISTORY.md`
- [ ] **申し送り**: **効果が小さい理由は「個体が場所を選べない」こと。** 感じられるのは今いる場所だけなので、働くのは弱い選別だけ。**強度を上げるより「他所を知る」経路（段階15b の地域の豊かさの推定）のほうが効く見込み**で、今回の小ささはその必要性の証拠。強度±1.0 は効果3倍だが出生率 −0.20 \* を払う

---

## 段階15：植物食料の地域差（固定版）と、地域の豊かさの認識・伝承

**目的**: 場所によって食べ物の量が違う世界にし、それを**覚えて・伝えられる**ようにする。段階17 で植物が動的になる前の、**固定の**地域差版。

### 15a：地域差（固定版）

- [x] 地域ブロックごとに植物の生えやすさの倍率を持たせた（2026-09-04）
- [x] `Config` トグル（`FoodSpread = 0`）: **乱数も消費せず1ビットも違わない**（テストで固定）
- [x] **総量は1つも変えず、どこに生えるかだけを変えた**（テストで固定）。`killShare` は既定では動かず、**強い差（±1.0）では逆に下がる**——#39 が予告していた「豊かな地域は希少度が下がって先制排除の動機が消える」筋
- [x] **対照条件（`richnosocial`＝社会的動機を全部切る）を用意した。** 答えは二択ではなく比率で、**`richGain` 0.05 → 0.03＝6割は食料、4割は社会的**
- [x] 種族ごとの分布を測定項目に追加（`humanRich` / `enemyRich`）。**予想と違い、外敵は追ってこない**（`enemyRichGain` +0.01 は誤差内）
- [x] **予想外の成果**: 世界が良くなった。`rareTrough` 0.25 → 0.40 \*（**段階10 で割ったあるべき状態A の帯に復帰**）、`nearest` −19.5 \*、`clumping` +0.10 \*、餓死 −0.66 \*。**段階14 で起きなかった凝集が、食料でなら起きる**

### 15b：地域の豊かさの認識（#40）

- [x] 地域ごとの豊かさの推定を持たせた（2026-09-04、`engine/regionlore.go`）。**自分が立った地面しか覚えられない**——それが15c に価値を与える
- [x] 鮮度は既存の遅延減衰＋接触リセットをそのまま使った
- [x] **相手ごとの記憶容量の枠には入れていない。記憶遺伝子は「country をどれだけよく保てるか」を買う**（保持する観測回数の上限と忘却の速さ）
- [x] **予想は外れた。`shMemory` は 0.08 で動かない。** ただし `PLAN.md` が書いていた疑い先が当たり——**平均 8.18 / 12 地域を知っている＝12個では容量が制約になっていない**。容量に選択圧をかけたいなら地域を増やすか忘却を速めるかが要る

### 15c：伝承（段階12b が前提）

- [x] 等価交換に地域の見解を足した（**専用の伝承経路は作らず** `exchangeLore` に相乗り）。行ったことのある場所どうしは歩み寄り、**片方しか知らなければそのまま渡す**（平均できないので）
- [x] **「教える」行動は作っていない。** 親子は育児期間ぶん共在するので同じ交換が多く走るだけ
- [x] **完了条件**: `RegionToldCount = 0`（伝承だけ切る）との対で分離した。**知識の3分の2は伝聞**（知っている地域 8.18 対 2.70）。地域ごとの推定値のばらつき（`regionSpread`）も出せる——**そして伝承の有無でほとんど変わらない**（伝えることは知識を広めるが一致はさせない）
- [x] **一番大きな成果**: **集団を1か所に集めたのは伝承だった。** 自分の経験だけでは `nearest` は動かず（+0.5）、伝承を入れると 140 → 100（−40.7 \*\*）
- [x] 触る文書: `NODE.md`、`PARAMETERS.md`（C-5g）、`README.md`、`HISTORY.md`

---

## 段階16：食料の栄養バランス

**目的**: 同じものばかり食べることに小さな罰を置く（#45）。**新しい状態量は作らない。**

- [x] 直近の消費履歴による割引を入れた（2026-09-04、`engine/diet.go`）。**新しい状態量は作っていない**（帳簿であって状態軸ではない）。**割引はゼロに届かない**（飽きは他を探す理由であって、食料の隣で餓死する理由ではない）
- [x] **隠していない。** `Perception` にも効用式にも空腹度と同じように載る
- [x] **単体で測った**（他の変更と混ぜていない）
- [x] **予想の検証**: 個体数・`killShare`・死因はいずれも誤差の範囲——**`PLAN.md` の事前予想が当たり**、実装中に上乗せした「個体数はやや下がる」のほうが外れた
- [x] **ただし「ほとんど出ない」ではなかった。** `dietVariety` 0.13 \*\*\*、そして**群れ狩りが初めて有意に増えた**（共同での仕留め 18.5 → 23.2 \*\*、参加人数 +0.03 \*）
- [x] **完了条件の (a) は、この一般則が弱い版として果たしていた。** 植物に飽きた個体にとって肉は「植物では取れない価値」を持つ——絶対的な栄養としてではなく**飽きの相対的な差として**。専用の仕掛けを書く前に一般則がその場所を埋めた形。(b)（段階17）はこれから
- [x] **強い罰（0.8）は逆効果**（群れ狩りの増加が消え、個体数 −10.3 \*）。世界が痩せて狩りに出る余裕が消えるため
- [x] 触る文書: `NODE.md`（食事）、`PARAMETERS.md`（C-5h）、`README.md`、`HISTORY.md`

---

## 段階17：植物の遺伝と種子散布

**目的**: 段階15 の**固定の**地域差を、**植物自身の進化から生まれる**動的な分布に置き換える（#42・#43・#44）。

**先に段階15（固定版）を実装・測定してから、この動的版に置き換えます。** 同時に入れると、地域の記憶（15b・15c）の効果なのか植物生態系の変動そのものの効果なのかが切り分けられません。

### 17a：風散布（最小構成）

- [x] 植物に軽量な遺伝子2本（散布半径・再生速度）を持たせた（2026-09-04、`engine/plant.go`）。**予算制は持ち込んでいない**
- [x] `Decide()` は作っていない。繁殖は既存の食料湧きに粒子的遺伝＋まれで大きい変異を相乗り
- [x] `Config` トグル（`PlantGenetics = false`）: **乱数も消費せず、指紋テストが無変更で通る**
- [x] **既定はオフにした。** 進化は起きる（`plantRegrow` 1.00 → 1.57 \*\*\*）が**個体数が3分の1**（44.2 → 14.9）。**逃げ道の無い2つの失敗**——短い散布は地域を空にし（吸収状態）、長い散布は `richGain` を 0.05 → 0.01 にして段階15b・15c が学ぶ対象を失わせる。**段階15 の固定版は置き換えられる下地ではなく土台だった**
- [x] **散布半径は予想と逆に上がった**（90 → 138 \*\*）。親の近くはすでに食べ尽くされているため

### 17b：毒と警告信号

- [ ] 遺伝子を2本足す: **毒の強さ**と**警告信号の強さ**（匂いと目立ちやすさは1本にまとめる・#43）
- [ ] **2本を相関させない。** 正直な警告が進化するかは世界の側に決めさせる
- [ ] 毒の有無は**隠しパラメータ**。食べる側は信号から `(100−理性)/100 × ノイズ` で推定する（理性が高いほど正しく読める）
- [ ] **実装前に書く予想**: `shRationality`（理性の予算比）が上がるはず——理性に「信号を読む」という**食料側の**役割が初めて付くため。あわせて、**毒を持たないのに信号だけ強い個体**（ベイツ型擬態）が出現しうるので、毒と信号の相関を世代ごとに出して観察する。**出なくても失敗ではない**（可能性を開いただけで、狙って作り込まない）

### 17c：動物摂食散布

- [x] 既存の `eat` に相乗りさせた（新しい行動も「付着」状態も無い）。運ばれた種は次の湧きを**置き換える**（総量不変）
- [x] **切り出して測った**（`noseedcarry` との対、および遺伝なし条件での単独効果）
- [x] **予想は外れ、救済にもならなかった。** 空の地域の割合は 0.55（あり）対 0.53（なし）で**不変**。しかも**単独で個体数を3分の1減らす**（44.2 → 26.0）——**運ばれた種が落ちるのは「動物がいま立っている場所」＝「たった今食べ尽くした場所」**なので、食料を「もう無い場所」に植え直している。**既定はオフ**

### 監視（3段階に共通）

- [x] **植物の局所絶滅を監視対象に加えた**（`plantEmpty`＝植物のいない地域の割合）。**着手前に監視項目を決めておいたおかげで、失敗が診断として返ってきた**——0.53 まで上がり、なぜ壊れたのかがその場で分かった
- [ ] 触る文書: `NODE.md`（食料）、`PARAMETERS.md`、`HISTORY.md`。植物が「食べられるだけの物」でなくなるので、**食料のルールをどの文書に書くか**（`NODE.md` の中か、独立した `PLANT.md` か）は実装時に決める

---

## 段階18：夜型・朝型（体内時計）

**目的**: 個体ごとに活動しやすい時間帯が違えば、集団の中で**常に誰かが起きている**状態が自然にでき、明示的な見張り役の割り当て無しに夜通しの警戒が生まれる（#46）。

- [ ] **世界に周期的な時間帯（昼夜のようなもの）という概念を新設する**。ここが一番のコストで、休憩場所・地形より大きい
- [ ] 個体ごとの体内時計を遺伝子として持たせる
- [ ] 何が時間帯によって変わるか（視界・食料の湧き・休息の回復量・露出のどれか）は、**この段階に着手するときに詳細化する**（先に決めない）
- [ ] **段階14・15 とは束ねない。段階12 より後**（#46）
- [ ] **完了条件**: 「集団の全員が同時に休んでいる瞬間の割合」を測り、体内時計のばらつきを 0 にした対照条件と比べる

---

## 地形の下ごしらえ（段階番号なし・いつでもよい）

**目的**: 速さが「体力と取引する」形になる唯一の見込みが地形なので、その前の下ごしらえを2つ済ませておく。

**地形全般（川・高低差など）は環境と食料生態系（段階14〜18）の最後**に置きます（2026-09-03）。地形が入るまで水散布は見送り（#44）、`cmd/devview` の地域ブロック（#38）は段階14 で先に入るので、**地形のパッチは地域ブロックとは別物**（地形はコストの地図、地域は食料と休息と将来のリセットの単位）である点に注意してください。

段階7c の完了条件（「大きくて遅い個体」と「小さくて速い個体」の並存）は 2026-09-01 の実測で**起きないと分かりました**。速さと体力はどちらも「殴る」に対抗して買われる**同じ側**の遺伝子で、互いに競合しないためです（相関 +0.147、割合どうしの基準線 −0.125。`HISTORY.md` 追記25）。**速さは効いていないのではなく、9本の中で一番買われています**（予算比 0.230）。足りないのは「速さが有利な場所と不利な場所」で、それを作れるのが地形です。

- [x] **食料レースの勝率に速さを入れた**（2026-09-04）。到達時間の比に置き換え、相手の速さは隠しパラメータなので世界の基準速度を仮定する（`RaceOnDistance` で旧ルール）。**両者は同じ effort で動くと仮定する**ので effort は約分され、残るのは脚の差だけ。**予想（`shSpeed` が上がる）は外れ、動かなかった**——速さはすでに到達tick数を通じて移動コスト・時間コストに効いており、`pGet` は二次的な経路だった。**この空振りは下の表の仮説（足りないのは場所差）を裏づける。** 値段はほぼゼロ（個体数 +1.4 誤差内、餓死率 +0.59 \*）で、**地形が入れば距離と時間は本当に食い違う**ので先に通した。**1回目の実装（自分の effort 対 相手の全力）は個体数を3分の1にした**（`HISTORY.md`）
- [x] **移動コストを地形照会関数経由にした**（2026-09-04、`engine/terrain.go`）。`w.moveCostOn(x, y, effort)` → `w.terrainAt(x, y)`。世界じゅうが `flatGround` を返すので**挙動は完全に不変**で、指紋テストが無変更で通ることがその証拠。**倍率は速度ではなくコストに掛ける**（速度を落とすのは effort がすでにやっていることで、二重にすると測定で区別できない。コストに掛ければ「荒れ地を速く渡る」が速さと体力の取引そのものになる）。**位置を引数に取る**——引数の無い版は、地形が入る日に全呼び出し元を直すことになるため。**コントローラは今も平坦だと思って計画する**（`Perception` に地形は無い）ので、その食い違いを埋めるかは地形本体の段階で決める

### 地形の種類と、それぞれが効かせるもの（2026-09-01 の見込み）

**どれを入れるかで効く能力が変わります。「地形を入れる」と一括りにしない。**

| 入れるもの | 何の価値が上がるか | 速さ × 体力の並存への効き |
| --- | --- | --- |
| **移動コストの地域差**（坂・ぬかるみ） | 速さ**の場所依存** | **本命。** 開けた場所は速い個体、荒れた場所は頑丈な個体という住み分け（ニッチ）ができ、集団全体で見たとき速さと体力の相関が初めて負になる見込み |
| **障害物**（通れない） | **頭の良さ**（経路選択） | 間接的。距離がユークリッドでなくなるので、直線距離で移動時間を見積もっている今の効用式がズレる。障害物は「速さ」より「知能」を効かせる変更 |
| **隘路・チョークポイント** | **防御力** | 逆方向。逃げ場が無い場所では回避が効かず、守りが強くなる |
| **遮蔽**（見えなくなる） | **回避率・知覚** | 視界が円で固定なので、**段階13（視界のグリッド化）とセットでないと効かない** |

### ニッチが成立する条件（入れる前に確認すること）

- **混ざりすぎないこと。** 局所適応は、個体が自由に動き回って混ざると洗い流される。今の世界は顔ぶれの半減期が約30tickで全員が動き回っている。**地形はコストと同時に「混ざりにくさ」も作る**ので地形自身が解決する見込みはあるが、確認は要る
- **1パッチあたりの個体数が足りること。** 世界は 800×600・視界130・個体数111（2026-09-01）。**パッチを大きく取りすぎると局所適応の前にパッチが絶滅する。** 出発点はパッチ2〜3個
- **測り方が変わる。** 相関を集団全体ではなく**パッチごとに測って比べる**。全体の相関が負になるだけでは不十分で、**「開けた場所の平均速さ ＞ 荒れた場所の平均速さ」**まで出て初めて「ニッチによる並存」と言える（全体の相関だけでは単なる分散の増加と区別がつかない）

---

## 保留（条件つきで復活）

- **明示的な「協力」**（相互選択の `ActCooperate` ＋ 協力性遺伝子）。段階11 まで進めて、それでも群れ狩りがまったく現れない場合にだけ再検討する
- **明示的な「集団」**（集団ID・所属行動・集団間の敵対フラグ）。段階12 まで進めて、それでも**あるべき状態 B〜E** が満たせない場合にだけ再検討する。#10 と同じ条件つきの構え
- **`protocol` / `server` / `client`**。engine 側をどこまで進めてから着手するかは未定（`CLAUDE.md` の未決事項）
- **配偶者を巡る同性間の戦い**（**地形は保留から外し、環境と食料生態系の最後に置いた**。2026-09-03）。**成長・老い・自然死は保留から外し、段階7d に前倒しした**（2026-08-30・#28）
- **激しい活動（求愛・戦闘）による寿命の消費。** disposable soma 説の骨格としては筋が通るが、**今回は実装しない**（#31 は食事量だけを見る簡易近似）。寿命という状態が入るので、後から項を足すだけで済む
- **独立した睡眠機構**（疲労度という状態量・強制的な `ActSleep`）。**今回は実装しない**（#37）。狙っていた「休まざるを得ないが休むと危ない」という緊張は、段階9 の `RestExposureWeight` ですでに出ている（休まない世界では消耗死 +45%）。**今後の測定でこの緊張が弱すぎると分かった場合にだけ**復活させる
- **動物付着散布（毛に引っ付く種）と水散布。** 前者は「付着」という新しい状態を人間・外敵の側に足す必要があるので採らない（#44）。後者は**川・水場が入ってから**（地形の下ごしらえの先）
- **肉に「植物では取れない栄養」という独自の価値をつけること。** 段階16 の完了条件に条件つきで置いた。栄養バランスの効果が測れるようになる前提の一つで、段階11 で群れ狩りが現れなかった一因を補う可能性がある
