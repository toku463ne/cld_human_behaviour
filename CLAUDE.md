# CLAUDE.md

## このファイルについて（メンテナンス方針）

- 各セクションは独立して追記・更新できるように分けてある。新機能を実装したら「現在の機能」に1行足すだけでよい。アーキテクチャや設計方針そのものを変えるときだけ、該当セクションを書き換える。
- 大きな決定（言語選定・通信方式など）は「設計判断ログ」に日付つきで残し、理由も一緒に書く。同じ議論を繰り返さないため。
- ルールは「確定」と「設計中（未確定）」を分けて書く。実装が追いついたら、該当項目を設計中から確定へ移す。
- このファイルが肥大化してきたら `docs/` 配下に分割し、ここからリンクする（例: `docs/architecture.md`, `docs/simulation-rules.md`）。目安は、1セクションが数十行を超えて読みにくくなったとき。
- 迷ったら「小さく追記」を優先する。既存の文章を大きく書き直す必要がある変更は、それ自体が設計判断ログに残すべき変更である可能性が高い。

## 文書の分担

どの文書に何を書くかの規則。**同じことを2箇所に書かない**（必ず片方が古くなるため）。

- `README.md` — **今の操作方法だけ。** ビルド・テスト・ベンチの回し方、`cmd/devview` の起動オプションとキー操作、画面とパネルの読み方。操作が変わったら**上書きする**。「以前は〜だった」「〜は廃止した」といった経緯は書かない
- `HISTORY.md` — **履歴。** いつ何が変わったか。README.md から落とした古い記述（廃止した操作、変わったコマンド、以前の既定値）はここに移す。**末尾に追記**し、過去の項目は書き換えない
- `CLAUDE.md`（この文書）— **設計と規約。** アーキテクチャ、実装ロードマップ、未決事項、崩してはいけない原則、そして**なぜそう決めたか**の設計判断ログ。ルールの詳細も操作手順も書かず、下の文書を指す
- `NODE.md` — **ノードが従うルール。** 状態量・能力・意思決定・行動・戦闘・記憶・繁殖が「何をするか」。数値は書かず `PARAMETERS.md` を指す。ルールを変えたら**上書きする**
- `ENEMY.md` — **外敵が従うルール。** 2026-08-14 時点で全部が未実装なので、仕様案と未決事項の置き場。確定したら「確定したルール」節へ移す
- `PARAMETERS.md` — **パラメータの一覧。** 世界のパラメータとノード固有の値の区別、既定値、`Config` に無いマジックナンバー、測定済みの感度。値を変えたら上書きする

使い分けの目安: 「どう動かすか」は README.md、「いつ変わったか」は HISTORY.md、「なぜそうなっているか」は CLAUDE.md。操作が変わる変更を入れたら、README.md を書き換えたうえで HISTORY.md に一項目足す。

## プロジェクト概要

人間の振る舞い・適応・相対的能力・資源・進化をテーマにした、ゲーム要素のあるシミュレーション。人間は単純化して1人 = 1ノードとして表現する。

将来的に目指す最終形はゲーム：ワールド内のノードの1つをユーザが操作し、子孫を残して、親が死んだらその子に操作を切り替えながら世界を生き延びる。まずは全ノードをAIが動かす観察型シミュレーションとして作り、人間操作は後から差し込む（後述の「意思決定の差し込み口」を参照）。

方針：最初はシンプルに作り、少しずつ機能を追加していく。追加のたびに重くなることを見込んで実行速度を重視する。将来的には自分以外の人にも使ってもらうことを想定している。

v0としてブラウザで動くHTML/JSのプロトタイプ（`human_evolution_sim.html`）が既に存在する。これはルールの参照実装（「仕様がわからなくなったら挙動を見て確認する」用）として扱い、以後の実装はGoに移行する。

## シミュレーションのルール

**実装済みのルールの現在の姿は `NODE.md`（ノード）と `ENEMY.md`（外敵・未実装）にある。** 数値は `PARAMETERS.md`。ここには重複して書かず、**壊してはいけない設計上の原則**と、まだ実装していない項目だけを残す。

### 崩してはいけない原則

ルールを足すときに、これらを壊していないか確認する。どれも一度壊して世界が破綻したか、破綻すると分かっているもの。

- **回復経路を必ず残す。** 「満腹かつ無理をしなければ回復する」が無いと、全員が単調に減って餓死するだけの世界になる。
- **優先順位は 生命 > 子孫 だけ。** 時間は目的ではなくコスト。子孫の行動は、生命の条件が満たされるまで候補にすら挙げない。
- **相手の真の能力は隠しパラメータ。** コントローラが見るのは自分の推定値だけ。`Perception` に真値を載せない（型で保証している）。
- **ハードコードされた行動閾値を作らない。** 「体力がこれ以下なら逃げる」のような条件は書かない。逃走も追撃も先制攻撃も、効用比較の結果として現れさせる。再判断のトリガーは「考え直せ」であって「こうしろ」ではない。
- **恨みの状態を持たない。** 攻撃は感情ではなく、食料競合の先制排除という効用式の一手として扱う。過去に払わされた体力は「避ける理由」として効用から引く側にある。
- **攻撃側有利を保つ。** 殴る側のコスト < 殴られる側のコスト。ただしこれと先制排除が噛み合うと世界が過度に暴力的になり個体数が崩壊しうるので、`CompetitionWeight` と `SkirmishTicks` を触ったら必ず個体数を確認する。
- **能力の役割を重ねない。** パワー＝戦闘効率、理性＝世界を読む精度、頭の良さ＝手を選ぶ質。1つの能力が2つの役割を持つと、選択圧がどちらから来ているのか測れなくなる。

### 将来実装（今はやらない）

- **成長・老い・自然死。** 入れるときは「遺伝的素質 × 年齢補正」の形にし、年齢補正を当面は常に1にしておく（後から補正関数を差し込むだけで足せるように）。`Age` は既に数えてある。
- **見た目からの相手分類。** 体格などから当たりをつける仕組みは、決め打ちではなく「学習の結果として得られるもの」として実装したい。
- **配偶者を巡る同性間の戦い**（戦闘と求愛の接続）。
- **仲間・協力・外敵。** 方針と段階は `ENEMY.md`。
- **効用式の中の仮定を個体が経験から学ぶこと。** 「相手は7割で殴り返す」「求愛は6割で受け入れられる」などが今は全ノード共通の固定値になっている（一覧は `PARAMETERS.md` の D 節）。

## アーキテクチャ（現時点の方針）

- 言語: Go（速度、既存のGo実績、ビルド・テストループの速さを理由に採用）。
- 構成: サーバ・クライアント型のネイティブアプリ。
  - `engine`: シミュレーション本体。状態とtickごとの更新ロジックのみを持ち、描画・通信のことは一切知らない。単体でテストできる形を保つ。
  - `protocol`: クライアント・サーバ間でやり取りするメッセージ型（ワールドのスナップショット、クライアントからのコマンドなど）を定義する共有パッケージ。
  - `server`: `engine` を保持してtickを回し、一定間隔でスナップショットを接続中の全クライアントに配信する。クライアントからのコマンド（パラメータ変更・一時停止など）を受けて `engine` に反映する。
  - `client`: ネイティブGUI（ebiten想定）。サーバに接続し、受信したスナップショットを描画するだけの薄い層。UI操作はコマンドとしてサーバに送る。
- 通信方式: 未確定。第一候補は `net.Conn` + `encoding/gob`（または json）による自前の軽量フレームプロトコルで、外部依存を増やさない。将来Web版が必要になればWebSocket/gRPCへの切替を検討する。

### 意思決定の差し込み口（ゲーム化への布石・早期に敷く）

- 各ノードに「コントローラ」を持たせ、`engine` は毎トリガー `controller.Decide(知覚) → 行動` を呼ぶだけにする。行動は `NODE.md` の「行動の語彙」に統一する。
- 今は全ノードがAIコントローラ（効用式で判断）。将来、1ノードだけを人間コントローラに差し替え、その個体が死んだら指定した子のコントローラをAI→人間に切り替えることで「子に乗り換える」を実現する。AIも人間も同じ行動語彙を出すので、`engine` は誰が操作しているかを知らなくてよい。
- 血統リンク（親→子のIDを記録）も早期に入れておく。将来「自分の子孫に乗り換える」ために必要で、今入れておけば安い。
- この境界（コントローラ／知覚／行動）と血統記録は、UIを作らない今のうちに敷く。後付けは大きな書き換えになるため。

## 開発中の目視確認

- `protocol`/`server`/`client` が揃うのを待たずに、機能を作るたびにその場で目視確認できるよう `cmd/devview` という開発用ツールを用意する。`engine` を直接importし（ネットワークなし）、ebitenでその場に描画するだけの、動作確認専用のツール。
- 本番の `client`（`server` 経由でネットワーク越しに描画する）とは別物。engineの実装が先行している間、挙動を手早く目視確認するための踏み台として使う。`client` が揃った時点で、`cmd/devview` は削除するか開発用モードとして残すかをその時点で判断する。
- 使い方（起動オプションとキー操作の一覧）は `README.md` にある。
- 目視には2つのモードがある。**全体を眺める**（世界が破綻していないか）のと、**1ノードを追う**（なぜその行動を選んだか）。後者は速度を落とすか一時停止して、ノードをクリックし、右のパネルで意思決定トレースを読む。
- 見た目は凝らなくてよい（`human_evolution_sim.html` と似た程度のシンプルな円・色分けで十分）。目的はロジックの目視確認であって、UIの作り込みではない。
- engineに新しい状態や行動を追加したときは、対応するテストに加えて `cmd/devview` で一度動きを目視確認する。
- シミュレーションの内部tick速度と、クライアントへの配信・描画速度は独立させる（サーバは好きな速さで内部tickを回し、配信は一定間隔に間引く）。

## ルール変更の測定（cmd/experiment）

目視で分かるのは「世界が破綻していないか」までで、「このルール変更で能力への選択圧がどちらに動いたか」は見えない。それを測るためのヘッドレスのランナーが `cmd/experiment`。使い方（オプションと出力の読み方）は `README.md` にある。

方針:

- **ルールの切替は `engine` の分岐ではなく `Config` の書き換えで表現する。** 両方の条件が同じバイナリに入っていないと、同じシードで対にして比べられない。ルールを1つ無効化できる形で `Config` に持たせるという既存の方針（2026-08-12の判断）が、そのままA/Bの前提にもなっている。
- **比較は必ず同一シードの対で行う（paired）。** シード間のばらつきは条件差よりずっと大きく、対にしないと数十シードでも埋もれる。
- **能力の値は最後の1tickではなく末尾2割の平均で読む。** 個体数が数百なので、数体の生死で平均が1回分の選択圧より大きく動く。
- 選択圧の指標は「20000tickでの平均能力の変化量」。副次的に個体数・世代数・戦闘数・死因の内訳（餓死／戦闘）も見る。**片方だけ良くなっていないか**を必ず確認する（例: 知能への選択圧は上がったが世界が暴力的になりすぎて個体数が崩れた、など）。
- 選択圧に触れる変更を入れたときは、この測定を変更の前後で回し、数値を `HISTORY.md` に残す。

## 実装ロードマップ（段階）

各段階は小さく、`cmd/devview` で新しい挙動が必ず目視できる粒度にする。上から順に進める。

**段階0〜6は 2026-08-12 に完了**（`engine` に実装、`cmd/devview` で目視確認済み）。次にやるのは `protocol`/`server`/`client`。

- 段階0：意思決定の差し込み口（コントローラ／知覚／行動の境界）と血統記録を先に敷く。この上に以降を載せる。【完了】
- 段階1：状態モデルを 体力＋空腹度＋食料 に置換。餓死と回復が目視できること。【完了】
- 段階2：移動の体力コストと effort。動き回ると減り、満腹で休むと回復するのが目視できること。【完了】
- 段階3：継続戦闘と逃走。食料を巡る戦い、劣勢側の逃走、攻撃側有利が目視できること。【完了】
- 段階4：強さ推定と記憶（(A)リスク記憶＋減衰、(B)観戦ベースの強さ推定）。ノードをクリックすると他者への推定が見え、対戦・観戦で収束するのを確認。【完了】
- 段階5：リスク判断と先制排除。強い個体を避ける、競合圧に応じた先制攻撃、もつれが目視できること。【完了】
- 段階6：頭の良さによる戦略選択。賢い血統と愚かな血統で生存率を比較できること。【完了。ただし下記の注意を参照】

段階6の注意：`Intelligence` は「戦略の幅（`StrategyDepthUnlock` による深さゲート）」と「選択品質（`ChoiceNoise`）」の2経路で効くように実装したが、**2026-08-14 のA/B測定で、深さゲートの側は機能していないことが分かったため既定で無効にした**（下記の設計判断ログを参照）。現在の頭の良さは評価ノイズだけを通して効く。理性はシードによってばらつく（下記「未決事項」を参照）。

## 未決事項

- 単一インスタンス運用（1人1サーバ+1クライアント）か、複数人が同じサーバに同時接続して同じワールドを見る共有型にするか。まずは前者で作りつつ、サーバ側は最初から複数クライアントへの配信を前提にした作りにしておく。
- 通信プロトコルの詳細（フレーミング方式、コマンド体系、後方互換の持たせ方）。
- 複数クライアント接続時、誰がパラメータを操作できるかのルール（今のところ未検討）。
- バランス調整用の数値（体力/空腹度のレート、effort の逓減カーブ、リスク記憶の減衰率、競合圧の代理指標の重み、攻撃側有利の非対称量など）。素朴に作ってから `cmd/devview` とベンチで詰める。一通りの値は入れたが、下記の3点は特に暫定。
  - 理性（`Rationality`）の選択圧が弱い。24シード × 20000tick で平均 +1.47 ± 0.53 と、パワー（+6.59）や知能（ゲートを切った条件で +6.85）に比べて一桁小さい。理性の利得（推定精度）に対して `PatienceRationality`（比較検討が長引くコスト）が効きすぎている疑いは残っている。**次にA/Bで詰める対象。**
  - 世界の暴力度は `CompetitionWeight` と `SkirmishTicks` に非常に敏感。設計メモの警告どおり、上げると個体数が崩れる方向に振れる。
  - 個体数は食料（`FoodSpawnRate`）で頭打ちになるよう調整してある。`MaxPopulation` は安全網であって、通常は張り付かない想定。
- 効用式の再計算コスト。まず素朴に全候補評価で作り、ベンチで重くなってからトリガーの間引きや候補の枝刈りで最適化する（方針(b)）。現状は1決定あたり約5.3µs（`BenchmarkDecide`）。

## 設計判断ログ

日付は決定した会話のタイミング。あとから追記するときは末尾に追加する（過去の行は書き換えない）。

- 2026-08-11: 実装言語をGoに決定。理由: 速度、開発者の既存Go実績（goLogAnalyzer等）、ビルド・テストループの速さ。
- 2026-08-11: サーバ・クライアント型を採用。理由: 将来的に複数人に使ってもらうことを想定。
- 2026-08-11: ネイティブアプリを採用し、ブラウザ版は当面見送り。
- 2026-08-11: コード（識別子・コメント・ログ／エラーメッセージ）はすべて英語で統一することに決定。ドキュメント（このCLAUDE.mdなど）は日本語のままでよい。
- 2026-08-11: パフォーマンスに影響しうる変更（ホットパス）のときは、都度Goの軽量ベンチマーク（`go test -bench`）を実行して確認する運用を採用。CIで強制する重いゲートにはせず、開発者自身の習慣として運用する。
- 2026-08-11: `protocol`/`server`/`client` が揃う前でも機能ごとに目視確認できるよう、`cmd/devview`（engineを直接importする簡易ebitenビューア、ネットワークなし）を導入する方針を追加。本番の`client`とは別物として扱う。
- 2026-08-12: モジュール名を `github.com/toku463ne/cld_human_behaviour` に決定。
- 2026-08-12: シミュレーションの定数を package const ではなく `engine.Config` のフィールドとして持つことに決定。理由: テストでルールを1つずつ無効化して（`MutationStd = 0` など）他のルールから切り離して検証できるようにするため。`Config` が実質的な仕様の一覧にもなる。
- 2026-08-12: 乱数は `World` が持つ `*rand.Rand` 1本に統一し、固定シードでの再現性を担保する。乱数源のinterface化は見送った（ホットパスに間接呼び出しを増やしたくないため）。確率が絡むルールのテストは「決定的にできるものは条件を作って決定的に検証」「ゆらぎ自体が本質のものは固定シードでN回試行して割合を比較」の2本立てで書く。
- 2026-08-12: engineの近傍探索は当面 v0 と同じ線形走査のままにする。理由: 数百ノードでは十分速く、まずシンプルさを優先するため。`BenchmarkEngineStep` の初回ベースライン（300体で約343µs/op）はその判断材料として残す。
- 2026-08-12: 死亡個体は v0（30tickごとにまとめて削除）と違い、毎tickの最後にその場で取り除くことにした。理由: `Agents()` が常に生存個体だけを返すほうが、devviewや将来の `protocol` のスナップショットにとって扱いやすいため。出産で生まれた子も同様にtick末にまとめて追加する（ループ中にsliceへappendするとバッキング配列が再確保されうるため）。
- 2026-08-12: 最終形をゲーム（1ノードを操作し子に乗り換えて生き延びる）と定め、そのために「意思決定の差し込み口（コントローラ／知覚／行動の共通境界）」と血統記録を早期に敷く方針を採用。成長・老い・自然死は将来実装。
- 2026-08-12: 状態量を 体力・空腹度・食料 の3軸に確定（一方向結合で回復経路を保証）。能力を パワー（体力あたりの戦闘効率）／理性（推定精度）／頭の良さ（戦略の幅と選択品質）の3種に役割分離。
- 2026-08-12: 意思決定はトリガー方式（毎tickではない）。逃走用のハードコード閾値は設けず、逃走・追撃は効用比較の結果として創発させる。優先順位は 生命>子孫、時間はコスト。
- 2026-08-12: 戦闘は継続ダメージ方式で攻撃側有利（殴る側 < 殴られる側のコスト）。相手のリスク記憶＝自分が失った体力の累積（ゆるく減衰）。復讐は感情ではなく食料競合の先制排除として効用式に載せ、専用の恨み系状態は持たない。
- 2026-08-12: 相手の強さは (A)直接戦闘のリスク記憶 と (B)観戦ベースの強さ推定 の2系統で扱う（(B)も今段階から実装）。見た目からの分類は将来「学習で獲得」する形にし今はやらない。
- 2026-08-12: 実行の重さは方針(b)（素朴に作ってベンチで重くなってから最適化）で進める。

- 2026-08-12: 段階0〜6を実装。以下は実装時に決めたこと。
  - 知覚（`Perception`）には他個体の真の能力を一切載せない。コントローラが見るのは自分の推定値（理性に応じてぼかした値）だけ。これで「真の戦闘力は隠しパラメータ」を型で保証する。
  - 効用式の生存確率は「餓死までのカウントダウン」と「体力が減っていること自体の危険（`ShockRisk`）」の2つを合成する。後者がないと、空腹でない個体にとって体力の消費がタダに見えて、世界が殴り合いになる。
  - 「休む」「食べる」の見積もりでは、受けている被ダメージを差し引いたうえで回復量を計算する（`recoverable`）。これがないと「殴られながら休んで回復する」が最善手に見えて、逃走が絶対に選ばれない。
  - 「さまよう」の価値は空腹度だけで決める。全体の死亡リスクに比例させると、追い詰められた個体が逃げずに散歩に出る。
  - 頭の良さの「選択品質」は、最善手を選ぶ確率ではなく**効用の評価に乗るノイズ**として実装した。確率方式だと外したときに一様ランダムな行動（＝しばしば自殺的な攻撃）を選んでしまい、世界が暴力的になりすぎる。
  - 戦闘コストの見積もりは「相手を倒しきるまで」ではなく `SkirmishTicks` で頭打ちにする。倒しきる前提で価格をつけると1食を巡る小競り合いが常に割に合わず、誰も何も争わなくなる。
  - ペア期間の終了はその tick の移動より先に処理する。そうしないと、ループで先に処理された側だけが移動コストを払い、両親が非対称になる。
  - 老い・自然死は実装しない（設計どおり将来実装）。個体は餓死か戦闘でのみ死ぬ。そのぶんリスク記憶の減衰が効いてくる。
- 2026-08-12: 段階0〜6実装後のベンチ再測定（同じRyzen 5 5500U）。`BenchmarkEngineStep` 300体で約283µs/op・18 allocs/op（v0移植時は343µs/op）。混雑シナリオ `BenchmarkEngineStepCrowded`（300体を300x260に詰めて食料を絞る）で約633µs/op、`BenchmarkDecide`（効用比較1回）で約5.3µs/op・0 allocs。
- 2026-08-12: `DefaultConfig` は「食料が足りない世界」に振り直した（`FoodSpawnRate` 1.5→0.20 相当）。理由: 食料が余っていると争いも餓死も起きず、能力に選択圧がまったくかからないため（実際、最初の実装では戦闘が20000tickで0回だった）。
- 2026-08-13: 意思決定トレース（`engine/trace.go`）を追加。以下は実装時に決めたこと。
  - 効用の内訳（`Utility`）を「表示用の別計算」にはせず、**効用式そのものの唯一の実装**にした（`Utility.Total()` が各行動の点数を返す）。表示用に式を書き直すと、トレースが実際の判断とズレたときに気づけない。
  - 記録は追跡対象のノードだけ。全ノード常時記録は決定1回あたりのコストを跳ね上げるうえ、読む側も追えない。エージェント側は `trace *traceLog` ポインタを持ち、非追跡なら `nil` チェック1回で終わる。
  - トレースはノードの死後も残す（`World.traces` はIDキー）。死因を読み返すのが一番の用途のため。
  - 履歴はノードごとに直近16件のリングバッファで、`Options` のスライスを使い回す（追跡中に毎決定でallocしない）。読み出し時だけコピーを返す。
  - 再判断トリガーに名前を付け（`Trigger`）、`needsDecision = true` を立てていた各所を `requestDecision(理由)` に置き換えた。「なぜ考え直したか」はトレースの一行目として一番重要な情報で、旧実装はbool 1個で理由を捨てていた。
  - ベンチ再測定（同じRyzen 5 5500U、`-benchtime=3s -count=3`）: `BenchmarkDecide` 5.36µs→5.77µs（+7.5%）、`BenchmarkEngineStep` 276µs→283µs。原因は `Utility` 構造体（float64×17）の組み立てで、追跡していないノードでも払う。ポインタ渡し・トレース時のみ構造体を保持など数パターン測ったが、いずれも1〜2%しか戻らずAPIが汚くなるだけだったので、可読性を採った。同一シード5000tickで統計値が完全一致することは確認済み（リファクタで挙動は変わっていない）。
  - シナリオテストを書いていて、engineの側が正しく自分の想定のほうが間違っていたケースが2件あった（「格上に守られた食料は諦めて別を探すはず」→ 実際は勝率0.19でも命がかかった食事の期待値のほうが徘徊より高いので賭けに行く、が正しい）。トレースを見て初めて分かったので、この道具立て自体は当たりだった。なお、食料の争奪は純粋な**距離の競走**として値付けされていて、「着いた先で殴られる」ぶんは入っていない。今のところ意図どおり（相手の強さは戦闘の効用側で見る）だが、覚えておくべき単純化。

- 2026-08-14: ルール変更をA/Bで測る `cmd/experiment` を導入し、その最初の結果として**戦略の深さゲートを既定で無効にした**（`StrategyDepthUnlock` 16→0）。以下は判断の根拠と、実装時に決めたこと。
  - きっかけは「知能48以上は深さ軸で何も買えない」という指摘。閾値は `StrategyDepthUnlock` の整数倍（16/32/48）だが、初期能力は一様分布 `[25,75]`、20000tick後も概ね24〜73に収まる。つまり16の閾値は一度も発火せず、32もほとんど跨がず、**実質的に効いているのは48（先制排除）の1本だけ**だった。
  - 測定（24シード × 20000tick、同一シードの差分）: ゲートを切ると平均知能の上がり幅が **+4.52 ± 0.95** 増える。逆に閾値を20/40/60に詰めると **−3.87 ± 0.79**（知能への選択圧が負に反転する）。`ChoiceNoise = 0` にして評価ノイズの経路だけを潰すと、知能の上がり幅は +0.63 ± 0.51 でほぼ0。つまり**知能を選択しているのは評価ノイズの経路だけで、深さゲートはむしろ足を引っ張っていた**。60000tickでも向きは同じ。
  - なぜそうなるかの解釈: ゲートは愚かな個体を「損な手を思いつけない」形で保護する。先制攻撃と観察は、今のパラメータでは平均すると割に合わない手なので、それを賢い個体だけに解禁すると賢さが罰になる。ゲートを外すと全員が同じ土俵に立ち、あとは評価の正確さだけが効く。
  - ゲートの仕組み自体は消さずに `Config` に残した（`StrategyDepthUnlock = 0` が「ゲートなし」）。理由: 再設計の土台であり、A/Bの対照条件としても要る。既定を変えるだけで挙動が切り替わる形にしておくほうが、コードを消すより後戻りが安い。
  - 副作用として世界は暴力的になった（戦闘 +25%、殺害 +15%、餓死 −16%）が、個体数・世代数は変わらない（−2.0 ± 2.1）。設計メモの「攻撃側有利は個体数を崩しうる」という警告の範囲内には収まっている。
  - 速度: `BenchmarkDecide` 5.8µs→8.0µs（+36%）。ゲートが無くなり全個体が観察・攻撃まで採点するため。`BenchmarkEngineStep` は同時測定で差が測定ノイズの範囲だったので、まずは可読性と正しさを採って最適化しない。重くなったら候補の枝刈り（方針(b)）で対応する。
  - 測定の作法を「ルール変更の測定（cmd/experiment）」の節に切り出した。とくに **paired（同一シードの差分）でないと何も見えない** ことと、**能力は末尾2割の平均で読む** ことは、これを作る過程で分かった実務上の要点。

- 2026-08-14: 協力・外敵に着手する前段として、**集団化の指標を先に作ってベースラインを取った**（段階A）。あわせて文書を分割した。以下は判断の根拠。
  - 指標を先に作ったのは、「群れているように見える」は証拠にならないため。`Spacing()` の `Clumping` は個体数で割ってあるので、集団が増えただけで上がることがない。`Stats()` に入れなかったのは O(n^2) で、`Stats()` は devview が毎フレーム呼ぶため。
  - **当初の想定が測定で覆った件**: 「食料を増やせば殺しは減るので、協力の効果と混同する」と考えていたが、実測では `FoodSpawnRate` を 0.12〜0.30 で振っても `killShare`（死因に占める殺害の割合）は 0.59〜0.64 でほぼ動かず、殺害の**絶対数はむしろ食料が増えるほど増える**（個体数が増えて遭遇が増えるため）。したがって `killShare` は食料供給と交絡しにくく、協力の効果を測る指標として使える。`morefood` / `scarce` の条件は対照として残す。
  - 文書を `NODE.md` / `ENEMY.md` / `PARAMETERS.md` に分けたのは、この文書の冒頭に書いた「肥大化してきたら分割する」方針どおり。ルールの記述が CLAUDE.md と実装の両方にあると必ず片方が古くなる（実際、深さゲートを既定オフにした後も「戦略の幅と選択品質は頭の良さでゲートされる」という記述が残っていた）。CLAUDE.md 側には**崩してはいけない原則**だけを残し、「何が起きるか」は `NODE.md` を唯一の置き場にした。
  - `ENEMY.md` は中身が全部未実装だが先に作った。外敵は「何を作るか」より「何を決めていないか」のほうが多く、決めていないことを書き留める場所が要るため。

## コーディング規約

- `go build ./... && go vet ./... && go test -race ./...` が常に通ること。
- 標準ライブラリで完結できるところは外部依存を増やさない（過去のGoプロジェクトと同じ方針）。
- 新しいシミュレーションルールを追加したときは、対応するテストと、`NODE.md`（または `ENEMY.md`）の更新をセットで行う。パラメータが増えたら `PARAMETERS.md` にも行を足す。
- コードはすべて英語で書く。変数名・関数名・型名・コメント・ログメッセージ・エラーメッセージ・コミットメッセージを含む。日本語が混ざらないようにする（CLAUDE.mdや設計ドキュメントなど、コード外のプロジェクト文書は日本語のままでよい）。

## パフォーマンス計測（ベンチマーク）

- ホットパス（`engine` のtickループ、近傍探索、大量ノードを走査する処理など）に手を入れたときは、変更の前後で軽量なベンチマークを取り、大きな悪化がないか確認する。
- 回し方は `README.md` にある。Goのベンチマークはデフォルトで1ベンチマークあたり約1秒しか回さないので、変更のたびに実行しても十分速く終わる。
- 最初のベンチマークは「固定シード・固定エージェント数（当面は数百程度を想定）で `Step()` を繰り返し呼ぶだけ」のシンプルなもの（例: `BenchmarkEngineStep`）から始める。凝った計測は必要になってから足す。戦闘や食料の取り合いが多発する混雑シナリオもベンチに含めると、トリガー方式のバースト的コストを拾える。
- 毎回数値を記録する必要はないが、明らかな悪化（体感の目安としてns/opが1桁%以上悪化するようなレベル）に気づいたら、コミットメッセージか「設計判断ログ」に一言残す。
- 全PR・全コミットで強制するものではなく、「パフォーマンスに影響がありそうな変更のときだけ」実施する軽い習慣として扱う。

## 新機能を追加するときの流れ

1. `engine` にロジックを追加し、ユニットテストを書く。ホットパスに触れる変更なら、上記のベンチマークも変更前後で実行する。見た目・挙動に関わる変更は `cmd/devview` で目視確認する。
2. 必要なら `protocol` にメッセージ型の変更を加える（後方互換を意識する）。
3. `server` / `client` に反映する。
4. このファイルの「現在の機能」に1〜数行追記する。ルールの追加・変更なら `NODE.md` / `ENEMY.md` を、パラメータが増減したなら `PARAMETERS.md` を更新する。選択圧に触れる変更なら `cmd/experiment` を前後で回し、数値を `HISTORY.md` に残す。
5. アーキテクチャや通信方式など大きな設計判断があれば「設計判断ログ」に日付つきで残す。

## 現在の機能

Goモジュール名は `github.com/toku463ne/cld_human_behaviour`。現時点で存在するのは `engine` と `cmd/devview` のみで、**`protocol` / `server` / `client` はまだ未着手**（未決事項も参照）。実装ロードマップの段階0〜6は完了している。

- `engine`: シミュレーション本体。描画・通信を一切知らない。`NewWorld(Config)` で生成し、`Step()` で1tick進める。読み出しは `Agents()` / `Foods()` / `Stats()` / `Tick()` / `AgentByID()` / `Opinions(id)`。
- `engine.Config`: 全パラメータのフィールド化。テストはここを1つずつ潰して各ルールを分離して検証する（例: `MutationStd = 0` で突然変異を止める、`RiskDecayPerTick = 0` で忘却を止める）。
- 状態モデル: 体力（`Vitality`）・空腹度（`Hunger`）・世界に湧く食料の3軸。食べる→空腹度↓、満腹かつ無理をしなければ体力↑、動く/戦うと体力↓、空腹が進むと体力↓。死因は餓死と戦闘のみ（老い・自然死は未実装）。
- 能力3種: パワー（体力あたりの戦闘効率）／理性（推定精度＝世界の読みのノイズ）／頭の良さ（手の評価のノイズ。戦略の幅の経路は既定で無効、`StrategyDepthUnlock = 0`）。
- 意思決定の差し込み口: `Controller` interface（`Decide(*Perception) Action`）。`engine` はトリガー時にこれを呼ぶだけ。`World.SetController(id, c)` で1体だけ差し替えられる（将来の人間操作用）。`Perception` は他個体の真の能力を含まず、観測者自身の推定値だけを渡す。
- 行動の語彙: `ActRest` / `ActMove` / `ActEat` / `ActAttack` / `ActFlee` / `ActObserve` / `ActCourt`。すべて effort（体力の注ぎ具合）を持つ。
- 再判断トリガー: 攻撃された／体力が一定量減った／目標を見失った・到達した／一定tick何も起きない／視界に食料が入った。逃走用の閾値は存在しない。
- 効用比較（`AIController`）: 全候補を「生命の価値×生存確率の改善 ＋ 子孫の価値×成立確率 − 体力コスト − 時間コスト」で採点し、最良手を選ぶ。逃走・追撃・先制攻撃はすべてこの比較の結果として現れる。
- 戦闘: 毎tickの継続ダメージ。殴る側のコスト < 殴られる側のコスト（攻撃側有利）。打撃は全員分をまとめてから同時に適用するので、スライス上の並び順が有利不利にならない。
- 記憶: (A) リスク記憶＝その相手に失った体力の累積（読み出し時に遅延減衰）、(B) 強さ推定＝ベイズ更新の平均と分散。直接戦闘と第三者の戦いの観戦で更新され、理性が高いほど正確、観測数が増えるほど分散が縮む。
- 血統: 子は `ParentIDs`、親は `ChildIDs` を記録する。
- 乱数は `World` が持つ `*rand.Rand` 1本。同じ `Config`（同じ `Seed`）なら常に同じ結果になる。コントローラも `Perception.Rand` 経由でこれを使う。
- 意思決定トレース（`engine/trace.go`）: `Decide()` 1回分の記録。「どのトリガーで呼ばれたか（`Trigger`）」「比較した候補ごとの効用の内訳（`Utility`：目的ごとの `Goal{Value, Chance}`、リスク・体力コスト・時間コスト）」「頭の良さによる評価ノイズ（`Noise`）と実際に比較したスコア」「選ばれた行動」を持つ。効用の合計は `Utility.Total()` ＝ Σ(Value×Chance) − Risk − VitalityCost − TimeCost で、効用式の唯一の実装でもある（トレースが式とズレようがない）。
- トレースは**追跡対象に指定したノードだけ**記録する。`World.TrackDecisions(id, true/false)` で開始・停止、`DecisionTraces(id)`（古い順、直近16件）と `LastDecisionTrace(id)` で読み出す。記録はノードの死後も残る（死に際に何を考えていたかが一番読みたい）。指定していないノードは何も記録しない。
- 再判断トリガーは `Trigger` として名前が付く（spawned / target lost / goal reached / attacked / vitality drop / idle / food in sight / bond ended / controller set）。`AIController` 以外のコントローラ（将来の人間操作）でも、トリガーと最終行動だけは `engine` 側が記録する。
- テスト: `engine/world_test.go`（ルールごとの単体テストと、8000tick回す統合テスト）と `engine/decision_test.go`（シナリオテスト）。後者は状況を手で組み立てて `Decide()` を1回呼び、選ばれる行動をassertする（格上に守られた食料／格下との食料争い／逃げ場のない被攻撃／満腹で消耗している／餓死寸前で求愛を検討すらしない、など）。効用の内訳やそもそも候補に挙がったかまでトレース経由で確認しているものもある。
- ベンチマーク: `engine/bench_test.go` に `BenchmarkEngineStep` / `BenchmarkEngineStepCrowded` / `BenchmarkDecide`。
- `engine.Spacing()`: 個体の散らばり具合（視界内の他個体数・最近傍距離・一様分布と比べた集まり具合）。`Stats()` と別なのは O(n^2) だから。協力を入れる前後で「群れているか」を測るために用意した。
- `cmd/experiment`: ルール変更をA/Bで測るヘッドレスのランナー。条件（`Config` を書き換える関数）× シード × tick数を並列で回し、条件ごとの平均±標準誤差と、**同じシードどうしの差分**を出す。選択圧に触れる変更のときはこれを前後で回す。方針は「ルール変更の測定」の節、使い方は `README.md`。
- `cmd/devview`: ebitenで `engine` を直接描画する開発用ツール。全体を眺めるモードと、1ノードを追うモード（クリックで選択＝追跡、一時停止・1tick送り・低速再生、右パネルに意思決定トレースと他者への強さ推定）を持つ。**起動方法・キー操作・画面とパネルの読み方は `README.md`**（ここには書かない）。
