# CLAUDE.md

## このファイルについて（メンテナンス方針）

- 各セクションは独立して追記・更新できるように分けてある。**新機能を実装したら `docs/features.md` に項目を足す**（この文書の「現在の機能」は索引なので、骨組みが変わったときだけ触る）。アーキテクチャや設計方針そのものを変えるときだけ、該当セクションを書き換える。
- **この文書には「今も有効な結論」だけを書く。** 大きな決定（言語選定・通信方式など）の**経緯と理由**は `HISTORY.md` の「設計判断ログ」に日付つきで残す。同じ議論を繰り返さないため。ここに書くのは結論と、必要なら `HISTORY.md` への参照だけ。
- ルールは「確定」と「設計中（未確定）」を分けて書く。実装が追いついたら、該当項目を設計中から確定へ移す。
- **このファイルは 150,000 文字を超えてはならない。** 読み込みの上限で、**超えると末尾が黙って落ちる**（末尾にあるのは「現在の機能」＝一番よく読む節）。**目安は 40,000 文字以内に保つ**こと。**2026-09-19 に実際に上限へ達し**、「現在の機能」を `docs/features.md`、「実装ロードマップ」の完了記録を `docs/stages.md` へ切り出した。
- **肥大化の原因は「測定値と経緯をここに書いたこと」だった。** 追記は**1項目1〜3行**にとどめ、**数値は書かず `HISTORY.md` を指す**。ルールの詳細は `NODE.md` / `docs/features.md`、段階ごとの記録は `docs/stages.md` に書く。**「この規則は何を測ったか」をここに書き始めたら、それは行き先を間違えている。**
- 節が数十行を超えて読みにくくなったら `docs/` 配下に分割し、ここからリンクする。**切り出した文書も同じ上限を持つ**ので、`docs/features.md` が 100,000 文字を超えたらテーマで分ける（食料と交換／地形と気候／社会と伝承、など）。
- 迷ったら「小さく追記」を優先する。既存の文章を大きく書き直す必要がある変更は、それ自体が `HISTORY.md` の設計判断ログに残すべき変更である可能性が高い。

## 文書の分担

どの文書に何を書くかの規則。**同じことを2箇所に書かない**（必ず片方が古くなるため）。

- `README.md` — **今の操作方法だけ。** ビルド・テスト・ベンチの回し方、`cmd/devview` の起動オプションとキー操作、画面とパネルの読み方。操作が変わったら**上書きする**。「以前は〜だった」「〜は廃止した」といった経緯は書かない
- `HISTORY.md` — **履歴と設計判断ログ。** 前半が「いつ何が変わったか」、後半が「**なぜそう決めたか**」。README.md から落とした古い記述（廃止した操作、変わったコマンド、以前の既定値）はここに移す。大きな設計判断も日付つきでここに残す。どちらも**末尾に追記**し、過去の項目は書き換えない
- `CLAUDE.md`（この文書）— **設計と規約の現在形。** アーキテクチャ、実装ロードマップの現在地、未決事項、崩してはいけない原則。ルールの詳細も操作手順も判断の経緯も測定値も書かず、下の文書を指す。**上限 150,000 文字（上のメンテナンス方針）**
- `NODE.md` — **ノードが従うルール。** 状態量・能力・意思決定・行動・戦闘・記憶・繁殖が「何をするか」。数値は書かず `PARAMETERS.md` を指す。ルールを変えたら**上書きする**
- `ENEMY.md` — **外敵が従うルール。** 2026-08-14 時点で全部が未実装なので、仕様案と未決事項の置き場。確定したら「確定したルール」節へ移す
- `PLAN.md` — **これから作るもののチェックリスト。** 段階7以降の実装計画と、その前提として決めたこと・まだ決めていないこと。実装したらチェックを入れる。終わった段階の経緯は `HISTORY.md` へ移す
- `docs/features.md` — **ルール1つ1つの現在の姿。** 既定のオン/オフ・形・そのルールが測ったこと。**「現在の機能」の正はこちら**（CLAUDE.md にあるのは索引）。ルールを変えたら**上書きする**
- `docs/stages.md` — **段階ごとの完了記録と、閉じた将来実装。** 段階0〜98 が何をして何を測ったか。**末尾に追記**し、過去の項目は書き換えない
- `PARAMETERS.md` — **パラメータの一覧。** 世界のパラメータとノード固有の値の区別、既定値、`Config` に無いマジックナンバー、測定済みの感度。値を変えたら上書きする

使い分けの目安: 「どう動かすか」は README.md、「今どうなっているか」は CLAUDE.md（設計・規約）と NODE.md / ENEMY.md（ルール）と docs/features.md（ルールごとの現在の姿）と PARAMETERS.md（数値）、「いつ変わったか・なぜそう決めたか」は HISTORY.md。操作が変わる変更を入れたら、README.md を書き換えたうえで HISTORY.md に一項目足す。設計判断をしたら、結論を CLAUDE.md の該当節に反映し、理由は HISTORY.md の設計判断ログに残す。

## プロジェクト概要

人間の振る舞い・適応・相対的能力・資源・進化をテーマにした、ゲーム要素のあるシミュレーション。人間は単純化して1人 = 1ノードとして表現する。

将来的に目指す最終形はゲーム：ワールド内のノードの1つをユーザが操作し、子孫を残して、親が死んだらその子に操作を切り替えながら世界を生き延びる。まずは全ノードをAIが動かす観察型シミュレーションとして作り、人間操作は後から差し込む（後述の「意思決定の差し込み口」を参照）。

方針：最初はシンプルに作り、少しずつ機能を追加していく。追加のたびに重くなることを見込んで実行速度を重視する。将来的には自分以外の人にも使ってもらうことを想定している。

v0としてブラウザで動くHTML/JSのプロトタイプ（`human_evolution_sim.html`）が既に存在する。これはルールの参照実装（「仕様がわからなくなったら挙動を見て確認する」用）として扱い、以後の実装はGoに移行する。

## シミュレーションのルール

**実装済みのルールの現在の姿は `NODE.md`（ノード）と `ENEMY.md`（外敵・未実装）にある。** 数値は `PARAMETERS.md`。ここには重複して書かず、**壊してはいけない設計上の原則**と、まだ実装していない項目だけを残す。

### 崩してはいけない原則

ルールを足すときに、これらを壊していないか確認する。どれも一度壊して世界が破綻したか、破綻すると分かっているもの。

- **回復経路を必ず残す。** 「満腹かつ無理をしなければ回復する」が無いと、全員が単調に減って餓死するだけの世界になる。
- **優先順位は 生命 > 子孫 だけ。** 時間は目的ではなくコスト。**ただし「候補にすら挙げない」のは 2026-09-05（段階25）にやめた**——腹が減っているのは求愛しない**理由**であって、求愛を不可能にするものではない。今は `addCourt` が出産の代金（`BirthVitalityCost/2` × 受諾確率）とそれで死ぬ確率の上昇を効用に載せ、**餓死しかけた個体は閾値なしで求愛を選ばない**（テストで固定）。**候補から消えるのは身体にできないことだけ**（成人前・代金を払うと死ぬ・クールダウン中）。**閾値は、効用式から抜けていた項の代わりだった**
- **相手の真の能力は隠しパラメータ。** コントローラが見るのは自分の推定値だけ。`Perception` に真値を載せない（型で保証している）。
- **ハードコードされた行動閾値を作らない。** 「体力がこれ以下なら逃げる」のような条件は書かない。逃走も追撃も先制攻撃も、効用比較の結果として現れさせる。再判断のトリガーは「考え直せ」であって「こうしろ」ではない。
- **恨みの状態を持たない。** 攻撃は感情ではなく、食料競合の先制排除という効用式の一手として扱う。過去に払わされた体力は「避ける理由」として効用から引く側にある。
- **攻撃側有利を保つ。** 殴る側のコスト < 殴られる側のコスト。ただしこれと先制排除が噛み合うと世界が過度に暴力的になり個体数が崩壊しうるので、`CompetitionWeight` と `SkirmishTicks` を触ったら必ず個体数を確認する。
- **能力の役割を重ねない。** パワー＝戦闘効率、理性＝世界を読む精度、頭の良さ＝手を選ぶ質。1つの能力が2つの役割を持つと、選択圧がどちらから来ているのか測れなくなる。

### マップ共通の土台（2026-09-12・段階56）

**上の原則のうち、マップの内容にかかわらず常に成り立っていなければならないものを束ねたもの。新しいルールは1つも増えていない。** これを書いたのは、**受け入れ基準（`PLAN.md` の「あるべき状態」）を全マップに求めるのをやめたから**——**帯で縛らない代わりに、どのマップでも崩してはいけないものを名指しする必要がある**（#88）。

- **回復経路を必ず残す**（上と同じ）
- **ハードコードされた行動閾値を作らない**（上と同じ）。**そしてこれがマップごとに難易度を振ってよいと言える唯一の根拠**——**閾値が無いからこそ、どんなに極端なマップ設定も「生存確率が低い」という degrade にしかならず、「構造的に生存不可能」という壊れ方をしない**。閾値を1つ書いた瞬間に、その閾値の向こう側にあるマップは**遊べない世界ではなく動かない世界**になる
- **能力の役割を重ねない**（上と同じ）
- **保存則を保つ。** 3つある。**予算の継承総量**（子は親の予算を継ぎ、突然変異は配分を動かすだけで合計を増やさない。贈与＝`Endow` と `Inspire` だけが例外で、**それは世界の側が呼ばないと明記してある**）／**死体の claim という所有規則**（倒した者が先に食べる。段階41 が変えたのは claim が何個を覆うかであって、規則そのものではない）／**食料の総量が「どこから来てどこへ行くか」閉じていること**（世界が湧かせ、食べられるか腐って消える。地域差・地形・水辺・魚・調理はどれも**分布か値打ちを変えるだけで総量を変えない**＝段階15a の原則）。**唯一これを意図的に緩めるのが段階61**（`FoodTotalFromMap`＝**1tickに生える量が地図の重みの平均に従う**。**既定はオフ**）で、**緩めることはマップの作者の選択であって既定ではない**。**なお `FoodRenormalize` を外すことは緩めることではない**——**地域の重みは比としてしか使われない**ので、割り戻しても配分は動かない（2026-09-12 に実測。動くのはクランプに当たったときだけ）

### 将来実装（今はやらない）

- **配偶者を巡る同性間の戦い**（戦闘と求愛の接続）。
- **地域ごとの世界リセット（random immigrants）。** 個体数過多・遺伝的多様性の停滞を地域単位で検知し、適応度に応じた生存選抜と新規ランダム個体の投入を行う。地域は既存の空間分割グリッドを粗くまとめたブロックを流用し、移動の壁にはしない（隣接地域からの自然な再流入を狙う）。トリガー条件・選抜方式は未決（下記「未決事項」）。

**ここに並んでいた残りの項目は `docs/stages.md` の「閉じた将来実装」へ移した**（2026-09-19）——実装済みのもの（見た目からの相手分類・仲間と協力・効用式の仮定の学習・種族の汎用化・遺伝子ベクトルの拡張・地形）と、「作らない」で決着したもの（記憶容量に選択圧をかける経路・魚を生き物にする）。**復活の条件つきで閉じたものもそちらにある。**

## アーキテクチャ（現時点の方針）

- 言語: Go（速度、既存のGo実績、ビルド・テストループの速さを理由に採用）。
- 構成: サーバ・クライアント型のネイティブアプリ。
  - `engine`: シミュレーション本体。状態とtickごとの更新ロジックのみを持ち、描画・通信のことは一切知らない。単体でテストできる形を保つ。
  - `protocol`: クライアント・サーバ間でやり取りするメッセージ型（ワールドのスナップショット、クライアントからのコマンドなど）を定義する共有パッケージ。
  - `server`: `engine` を保持してtickを回し、一定間隔でスナップショットを接続中の全クライアントに配信する。クライアントからのコマンド（パラメータ変更・一時停止など）を受けて `engine` に反映する。
  - `client`: ネイティブGUI（ebiten想定）。サーバに接続し、受信したスナップショットを描画するだけの薄い層。UI操作はコマンドとしてサーバに送る。
- 通信方式: 未確定。第一候補は `net.Conn` + `encoding/gob`（または json）による自前の軽量フレームプロトコルで、外部依存を増やさない。将来Web版が必要になればWebSocket/gRPCへの切替を検討する。

### 意思決定の差し込み口（ゲーム化への布石・早期に敷く）

- 各ノードに「コントローラ」を持たせ、`engine` は毎トリガー `controller.Decide(知覚) → 行動` を呼ぶだけにする。行動は `NODE.md` の「行動の語彙」に統一する。
- 今は全ノードがAIコントローラ（効用式で判断）。将来、1ノードだけを人間コントローラに差し替え、その個体が死んだら指定した子のコントローラをAI→人間に切り替えることで「子に乗り換える」を実現する。AIも人間も同じ行動語彙を出すので、`engine` は誰が操作しているかを知らなくてよい。
- 血統リンク（親→子のIDを記録）も早期に入れておく。将来「自分の子孫に乗り換える」ために必要で、今入れておけば安い。
- この境界（コントローラ／知覚／行動）と血統記録は、UIを作らない今のうちに敷く。後付けは大きな書き換えになるため。

## 開発中の目視確認

- `protocol`/`server`/`client` が揃うのを待たずに、機能を作るたびにその場で目視確認できるよう `cmd/devview` という開発用ツールを用意する。`engine` を直接importし（ネットワークなし）、ebitenでその場に描画するだけの、動作確認専用のツール。
- 本番の `client`（`server` 経由でネットワーク越しに描画する）とは別物。engineの実装が先行している間、挙動を手早く目視確認するための踏み台として使う。`client` が揃った時点で、`cmd/devview` は削除するか開発用モードとして残すかをその時点で判断する。
- 使い方（起動オプションとキー操作の一覧）は `README.md` にある。
- 目視には2つのモードがある。**全体を眺める**（世界が破綻していないか）のと、**1ノードを追う**（なぜその行動を選んだか）。後者は速度を落とすか一時停止して、ノードをクリックし、右のパネルで意思決定トレースを読む。
- 見た目は凝らなくてよい（`human_evolution_sim.html` と似た程度のシンプルな円・色分けで十分）。目的はロジックの目視確認であって、UIの作り込みではない。
- engineに新しい状態や行動を追加したときは、対応するテストに加えて `cmd/devview` で一度動きを目視確認する。
- シミュレーションの内部tick速度と、クライアントへの配信・描画速度は独立させる（サーバは好きな速さで内部tickを回し、配信は一定間隔に間引く）。

## ルール変更の測定（cmd/experiment）

目視で分かるのは「世界が破綻していないか」までで、「このルール変更で能力への選択圧がどちらに動いたか」は見えない。それを測るためのヘッドレスのランナーが `cmd/experiment`。使い方（オプションと出力の読み方）は `README.md` にある。

方針:

- **ルールの切替は `engine` の分岐ではなく `Config` の書き換えで表現する。** 両方の条件が同じバイナリに入っていないと、同じシードで対にして比べられない。ルールを1つ無効化できる形で `Config` に持たせるという既存の方針（2026-08-12の判断、`HISTORY.md`）が、そのままA/Bの前提にもなっている。
- **比較は必ず同一シードの対で行う（paired）。** シード間のばらつきは条件差よりずっと大きく、対にしないと数十シードでも埋もれる。
- **能力の値は最後の1tickではなく末尾2割の平均で読む。** 個体数が数百なので、数体の生死で平均が1回分の選択圧より大きく動く。
- 選択圧の指標は「20000tickでの平均能力の変化量」。副次的に個体数・世代数・戦闘数・死因の内訳（餓死／戦闘）も見る。**片方だけ良くなっていないか**を必ず確認する（例: 知能への選択圧は上がったが世界が暴力的になりすぎて個体数が崩れた、など）。
- 選択圧に触れる変更を入れたときは、この測定を変更の前後で回し、数値を `HISTORY.md` に残す。
- **測定の前に予想を書く。** 選択圧に触れる変更を入れる前に、`cmd/experiment` のどの指標（`dPower`/`dRationality`/`dIntelligence`/`killShare`/`clumping`/`neighbours`/`nearest`/`starved`/`killed`/`extinct` など）が、どちら向きに、誤差より大きく動くはずかを先に書く。「良くなるはず」のような曖昧な予想は測定後にどうとでも解釈できてしまうので、具体的で反証可能な形にする。
- **予想が外れたときほど `HISTORY.md` に残す。** 変更前後の数値だけでなく、何を予想していてどう外れたかを書く。これまでの設計判断ログで一番価値があった記録は、想定が測定で覆ったとき（段階6の深さゲートが逆効果だった件、`killShare` が食料供給と交絡すると思っていたが実際は交絡しなかった件）で、当たった予想より外れた予想のほうが学びが大きい。
- **数値と見た目の両方で確認する。** `cmd/experiment` の数値が動いても、それが狙った挙動によるものかは `cmd/devview` での目視まで分からないことがある。予想には数値の動きに加えて「見た目としてどう変わるはずか」も書き、両方を確認してから変更を確定させる。

## 実装ロードマップ（段階）

各段階は小さく、`cmd/devview` で新しい挙動が必ず目視できる粒度にする。上から順に進める。

**段階0〜97 の一覧と、各段階が何をして何を測ったかは `docs/stages.md`**（2026-09-19 にここから切り出した）。**これから作るものは `PLAN.md`、次に何をやるかは `TODO.md`、測定値と理由は `HISTORY.md`。**

**現在地（2026-09-19）: 段階97（水の中では遅い）まで完了。次は段階98（泳げない身体にとっての水）。** `PLAN.md` に未着手のまま残っているのは段階98 と、保留の節の復活条件つきの項目だけ。**新しい群を立てるならユーザと相談してから。**

**測定のベースラインは2度リセットされている**——**段階7b（粒子的遺伝＝能力の分散が約2倍になった）**と、**2026-09-13 の「先読みを既定にした」**。**`HISTORY.md` の 2026-09-13 より前の数値は「窓が1つ」の世界のもの**で、`cmd/experiment` の `noahead` の腕と `cmd/devview -noahead` で再現できる。

段階6の注意：`Intelligence` は「戦略の幅（`StrategyDepthUnlock` による深さゲート）」と「選択品質（`ChoiceNoise`）」の2経路で効くように実装したが、**2026-08-14 のA/B測定で、深さゲートの側は機能していないことが分かったため既定で無効にした**（測定値と判断の根拠は `HISTORY.md`）。現在の頭の良さは評価ノイズだけを通して効く。理性はシードによってばらつく（下記「未決事項」を参照）。

## 未決事項

- 単一インスタンス運用（1人1サーバ+1クライアント）か、複数人が同じサーバに同時接続して同じワールドを見る共有型にするか。まずは前者で作りつつ、サーバ側は最初から複数クライアントへの配信を前提にした作りにしておく。
- 通信プロトコルの詳細（フレーミング方式、コマンド体系、後方互換の持たせ方）。
- 複数クライアント接続時、誰がパラメータを操作できるかのルール（今のところ未検討）。
- バランス調整用の数値（体力/空腹度のレート、effort の逓減カーブ、リスク記憶の減衰率、競合圧の代理指標の重み、攻撃側有利の非対称量など）。素朴に作ってから `cmd/devview` とベンチで詰める。一通りの値は入れたが、下記の3点は特に暫定。
  - 理性（`Rationality`）の選択圧の弱さは、**原因の大半が効用式ではなく継承則だった**（2026-09-01）。粒子的遺伝に変えただけで +2.55 → +10.54（24シード × 50000tick）。`PatienceRationality` が効きすぎている疑いは残るが、**優先度は下がった**。パワー（+20.57）・知能（+29.96）に比べればまだ小さいので、詰めるとしても段階7c（予算制）で能力が上限に張り付かなくなってから。
  - 世界の暴力度は `CompetitionWeight` と `SkirmishTicks` に非常に敏感。設計メモの警告どおり、上げると個体数が崩れる方向に振れる。
  - 個体数は食料（`FoodSpawnRate`）で頭打ちになるよう調整してある。`MaxPopulation` は安全網であって、通常は張り付かない想定。
- 効用式の再計算コスト。まず素朴に全候補評価で作り、ベンチで重くなってからトリガーの間引きや候補の枝刈りで最適化する（方針(b)）。現状は1決定あたり約8.0µs（`BenchmarkDecide`。深さゲートを既定オフにして全候補を採点するようになったぶん、5.8µsから増えている）。
- 地域リセットのトリガー条件（局所的な過密／遺伝的停滞のどちらか、または両方か）と、リセット時の生存個体の選び方（適応度順か、地理的多様性も守るハイブリッドか）。
- プレイヤーの出生地選択UI（ワールド概観からの地域選択）の具体的な見せ方。
- 種族の汎用化・遺伝子ベクトル拡張・地域リセット（上の「将来実装」段階7〜9）を、`protocol`/`server`/`client` より先にengine側でさらに進めるか、先に`protocol`/`server`/`client`を通すか。順序は未確定。 **2026-09-05 に一部決着**: 先に段階19〜22（ゲーム化とマップエディタ）を通し、`protocol` のスナップショット形式は段階21（保存/読込）を実際に使った上で設計する（決定事項#48）。残る未決は `protocol`/`server`/`client` そのものの着手時期。

## 設計判断ログ → `HISTORY.md`

「なぜそう決めたか」の記録は **`HISTORY.md` の後半（設計判断ログ）** にある。2026-08-11 以降の全項目をそこへ移した（2026-08-16）。

この文書には**今も有効な結論だけ**を書き、経緯は書かない。新しい設計判断をしたら、結論をこの文書の該当節に反映し、理由と測定値は `HISTORY.md` の末尾に日付つきで追記する。

## コーディング規約

- `go build ./... && go vet ./... && go test -race ./...` が常に通ること。
- 標準ライブラリで完結できるところは外部依存を増やさない（過去のGoプロジェクトと同じ方針）。
- **シミュレーションの定数は package const ではなく `engine.Config` のフィールドとして持つ。** テストでルールを1つずつ無効化して（`MutationStd = 0` など）他のルールから切り離して検証するため、そしてA/Bで条件を差し替えるため。`Config` が実質的な仕様の一覧にもなる。
- **乱数は `World` が持つ `*rand.Rand` 1本に統一する。** 同じ `Config`（同じ `Seed`）なら常に同じ結果になること。確率が絡むルールのテストは「決定的にできるものは条件を作って決定的に検証」「ゆらぎ自体が本質のものは固定シードでN回試行して割合を比較」の2本立てで書く。
- **空間インデックス（`engine/grid.go`）への問い合わせを、位置を動かすループの中に置かない。** インデックスは移動のたびに「古い」印がつくので、動かしながら訊くと1体ごとに作り直しが走り、置き換えたはずの全走査より遅くなる。訊くのは動かす前か、全員動かしたあと。
- 新しいシミュレーションルールを追加したときは、対応するテストと、`NODE.md`（または `ENEMY.md`）の更新をセットで行う。パラメータが増えたら `PARAMETERS.md` にも行を足す。
- コードはすべて英語で書く。変数名・関数名・型名・コメント・ログメッセージ・エラーメッセージ・コミットメッセージを含む。日本語が混ざらないようにする（CLAUDE.mdや設計ドキュメントなど、コード外のプロジェクト文書は日本語のままでよい）。

## コミットの方針

- **`main` に直接コミットする。ブランチは切らない。** 履歴は一本で保つ。理由は2つ。作業者が1人であること、そして**コミット番号を成果物に焼き込むことがある**こと（`cmd/snapshot` の画像は下帯にコミットを持つ）。ブランチを経由すると rebase / squash でハッシュが変わり、焼き込んだ番号が嘘になる。
- **コミットメッセージは英語で書く**（コーディング規約と同じ。コード外の設計文書だけが日本語）。
- **1つの機能・1つの測定 ＝ 1コミット。** 実装・テスト・文書の更新は同じコミットに入れる（`NODE.md` / `PARAMETERS.md` / `HISTORY.md` の追記まで含めて1つ）。
- **記録用の成果物（画像など）は、それを生成したコードとは別のコミットにする。** 順序は「コードを先にコミット → そのハッシュで成果物を生成 → 成果物を次のコミットに入れる」。こうすると成果物が指すコミットに、それを再生成するコードが必ず含まれている。
- **本文には「何を変えたか」だけでなく「なぜその形にしたか」の要点を書く。** 詳細と測定値は `HISTORY.md` にあるので、ここは要点だけでよい。特に**採らなかった素直な選択肢とその理由**は残す価値が高い（例: 連結距離に `PerceptionRadius` を使わなかった理由）。
- コミットするのは**ユーザから指示があったとき**。指示なく勝手にコミットしない。

## パフォーマンス計測（ベンチマーク）

- ホットパス（`engine` のtickループ、近傍探索、大量ノードを走査する処理など）に手を入れたときは、変更の前後で軽量なベンチマークを取り、大きな悪化がないか確認する。
- 回し方は `README.md` にある。Goのベンチマークはデフォルトで1ベンチマークあたり約1秒しか回さないので、変更のたびに実行しても十分速く終わる。
- **計測をテストの形で回すときは `-count=1` を付ける。** `go test` は同じバイナリ・同じ引数の結果をキャッシュするので、付けないと「2回測った」つもりの数値が1回目の使い回しになる（2026-09-01 に実際にやらかした。`HISTORY.md` 追記19）。
- 最初のベンチマークは「固定シード・固定エージェント数（当面は数百程度を想定）で `Step()` を繰り返し呼ぶだけ」のシンプルなもの（例: `BenchmarkEngineStep`）から始める。凝った計測は必要になってから足す。戦闘や食料の取り合いが多発する混雑シナリオもベンチに含めると、トリガー方式のバースト的コストを拾える。
- **世界を進めるベンチマークは、一定tickごとに世界を作り直す**（`engine/bench_test.go` の `benchSteps`。作り直しの間はタイマーを止める）。**1つの世界を `b.N` 回進める素朴な形は一定の仕事量を測っていない**——測っている間も世界が進むので、長く回すほど別の世界を測ることになり、同じコードで 82µs〜758µs までばらつく（2026-09-04 に実際に踏んだ。`HISTORY.md` 追記33）。
- **プロファイルは上位関数で止めず、`-peek` で呼び出し元まで降りる。** 「遅い関数」ではなく「**結果を使わない呼び出し**」が本体だったことがある（同上）。
- 毎回数値を記録する必要はないが、明らかな悪化（体感の目安としてns/opが1桁%以上悪化するようなレベル）に気づいたら、コミットメッセージか `HISTORY.md` に一言残す。
- 全PR・全コミットで強制するものではなく、「パフォーマンスに影響がありそうな変更のときだけ」実施する軽い習慣として扱う。

## 新機能を追加するときの流れ

1. `engine` にロジックを追加し、ユニットテストを書く。ホットパスに触れる変更なら、上記のベンチマークも変更前後で実行する。見た目・挙動に関わる変更は `cmd/devview` で目視確認する。
2. 必要なら `protocol` にメッセージ型の変更を加える（後方互換を意識する）。
3. `server` / `client` に反映する。
4. `docs/features.md` に項目を足す（**この文書の「現在の機能」は索引なので、骨組みが変わったときだけ触る**）。ルールの追加・変更なら `NODE.md` / `ENEMY.md` を、パラメータが増減したなら `PARAMETERS.md` を更新する。操作が変わったら `README.md` を上書きする。段階を1つ通したら `docs/stages.md` に1項目足す。
5. `HISTORY.md` の「変更の記録」に、何がどう変わったかを1項目足す（末尾に追記）。選択圧に触れる変更なら `cmd/experiment` を前後で回し、その数値もここに残す。
6. アーキテクチャや通信方式など大きな設計判断があれば、**結論**をこのファイルの該当節に反映し、**理由と経緯**を `HISTORY.md` の「設計判断ログ」に日付つきで残す（末尾に追記、過去の行は書き換えない）。

## 現在の機能

Goモジュール名は `github.com/toku463ne/cld_human_behaviour`。現時点で存在するのは `engine` と `cmd/devview` / `cmd/experiment` / `cmd/snapshot` で、**`protocol` / `server` / `client` はまだ未着手**（未決事項も参照）。

**ルール1つ1つの現在の姿——既定のオン/オフ・形・そのルールが測ったこと——は `docs/features.md` にある**（2026-09-19 にここから切り出した。**食い違ったらそちらが正**）。**この節はその索引で、毎回要る骨組みだけを置く。** ルールの説明は `NODE.md` / `ENEMY.md`、数値は `PARAMETERS.md`、経緯は `HISTORY.md`。`docs/features.md` は追記順に並んでいるので、**探すときは段階番号か `Config` のフィールド名で `grep` する**。

- `engine`: シミュレーション本体。描画・通信を一切知らない。`NewWorld(Config)` で生成し `Step()` で1tick進める。読み出しは `Agents()` / `Foods()` / `Stats()` / `Tick()` / `AgentByID()` / `Opinions(id)`。
- `engine.Config`: 全パラメータのフィールド化。テストはここを1つずつ潰して各ルールを分離して検証する。**既定でオフのルールが多い**（地図の作者が入れるもの／測って既定にしなかったもの）——どれがオフかは `docs/features.md` の各項目の先頭と `PARAMETERS.md`。
- 種族: human と enemy の2種族。**同じノード構造・同じ意思決定エンジン**で、違いは予算の抽選範囲と食性だけ。求愛は同種内のみ。**種類（`EnemyKinds`）は種ではなくタグ**で、種類どうしは交配する。
- 状態モデル: 体力・空腹度・成熟度と、世界に湧く食料（植物・魚・肉・飾り物・石・コイン・本）。死因は餓死・戦闘・消耗（寿命）・溺死で、どれも排他。
- 遺伝子: `Agent.Genome []float64` の9本（攻撃力・防御力・最大体力・移動速度・回避率・記憶容量・理性・頭の良さ・魅力）が**1つの予算を分け合う**。**指標は絶対値ではなく予算比で読む。** 実効値 ＝ 遺伝的素質 × 年齢補正 ×（性別補正）×（足元の地面）で、**掛け算は `Agent.Ability` の1か所だけ**。子は予算を親の一方から継ぎ、**突然変異は配分を動かすだけで合計を増やさない**。
- **予算の外にあるもの**（量ではないので予算を食わない）: 体内時計の位相・味の位相・`lore`（好み3つ・信念2つ・子の値打ち・揺れの振幅）。**ヒント枠とスキルは予算を食う**（枠が空でも払う）。
- 能力の役割: 攻撃力＝戦闘効率／理性＝世界を読む精度／頭の良さ＝手を選ぶ質（評価ノイズ。深さゲートは既定で無効）／記憶容量＝覚えていられる人数と取り込み・忘却の速さ。
- 行動の語彙（18）: `ActRest` / `ActMove` / `ActEat` / `ActAttack` / `ActFlee` / `ActObserve` / `ActCourt` / `ActInvite` / `ActTake` / `ActThrow` / `ActGive` / `ActOffer` / `ActStore` / `ActBuy` / `ActCook` / `ActWrite` / `ActRead` / `ActDrop`。すべて effort（体力の注ぎ具合）を持つ。**語を足すと「言い伝えが何についてのものか」の幅（`numActionKinds`）も広がるので、既定でオフの語でも指紋テストの数値は動く。**
- 効用比較（`AIController`）: 全候補を「生命の価値×生存確率の改善 ＋ 子孫の価値×成立確率 − 体力コスト − 時間コスト」で採点し、最良手を選ぶ。**逃走・追撃・先制攻撃・売買・協力はすべてこの比較の結果として現れる**（閾値はどこにも無い）。
- 先読み: **既定で1段**（`LookaheadHorizons` = 1。2026-09-13 のベースラインのリセット）。2段目は**その身体自身の代謝だけを進めた状態**で、**今入っている打撃は持ち込まない**。
- 意思決定の差し込み口: `Controller` interface（`Decide(*Perception) Action`）。実装は3つ——`AIController` / `HumanController`（段階19）/ `GuidedController`（段階23）。`World.SetController(id, c)` で1体だけ差し替えられる。**`Perception` は他個体の真の能力を含まず、観測者自身の推定値だけを渡す。**
- 再判断トリガー: 攻撃された／体力が一定量減った／目標を見失った・到達した／一定tick何も起きない／視界に食料や配偶者が**入った**（「ある」ではない）。**逃走用の閾値は存在しない。**
- 記憶（`engine/memory.go`）: 相手ごとに1レコード（リスク記憶・好意・強さ推定）。**有限**で、あふれたらリスクも好意もゼロのレコードを捨てる。**好意の発生源は実イベントだけ。**
- 伝承とヒント（`engine/lore.go` / `engine/hint.go`）: 効用式の中の仮定は個体ごとの値で、`observe` に相乗りして**双方向・同時に**交換される。ヒントは「状況×行動×重み」の3つ組で、**効用への加減点としてのみ働き、分岐はどこにも無い**。
- 乱数は `World` が持つ `*rand.Rand` 1本。同じ `Config`（同じ `Seed`）なら常に同じ結果になる。
- 意思決定トレース（`engine/trace.go`）: `Decide()` 1回分の記録（トリガー・候補ごとの効用の内訳・評価ノイズ・選ばれた行動）。**`Utility.Total()` が効用式の唯一の実装**なので、トレースが式とズレようがない。**追跡対象に指定したノードだけ**記録し、死後も残る。
- 挙動の指紋（`engine/golden_test.go`）: 既定条件・シード1〜3を5000tick回した結果を固定値で押さえたテスト。**「速くするだけの変更」が世界を変えていないことの唯一の証拠。** ルールを意図して変えたときだけ、その変更と同じコミットで数値を更新する（赤いテストを緑にするために書き換えない）。
- テスト: `engine/world_test.go`（ルールごとの単体テストと8000tickの統合テスト）と `engine/decision_test.go`（状況を手で組み立てて `Decide()` を1回呼ぶシナリオテスト）。ベンチマークは `engine/bench_test.go`。
- 空間インデックス（`engine/grid.go`、非公開）: 一様グリッド。**世界のルールを何も変えない**のが唯一の条件なので、セル幅は `Config` ではなく `PerceptionRadius` から導出する。**移動ループの途中で問い合わせない**（コーディング規約）。
- 測定器（どれも**読み取り専用で、世界に書き込まず乱数も引かない**）: `Clusters` / `ClusterGaps` / `MembershipTracker` / `FightTracker` / `CensusTracker` / `Spacing` / `Banks`（両岸）/ `Trade`（商売）/ `Wading`（水中の速度）/ `CarryUse`（手の塞がり）。**連結距離のような測る側の目盛りは `Config` に入れない**（世界のルールではないため。距離が違う測定値どうしは比べられない）。
- 世界を外から変える（`engine/edit.go` / `engine/endow.go`）: `SetTerrain` / `SetRegion` / `Tune` / `SetDifficulty` / `Inspire` / `Endow` は**シミュレーションの側からは誰も呼ばず、乱数を1つも引かない**。
- 保存と読込（`engine/save.go`）: 世界まるごとと個体群だけの2つを JSON で出し入れする。**乱数は「種と、いくつ引いたか」を保存して読込時に引き直す。** 個体群に持ち込めるのは「その身体が何であるか」だけ。
- ツール: `cmd/devview`（目視・人間操作・マップエディタ）／`cmd/experiment`（A/B測定）／`cmd/snapshot`（記録用の画像）。**使い方は `README.md`。**
