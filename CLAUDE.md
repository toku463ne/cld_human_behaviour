# CLAUDE.md

## このファイルについて（メンテナンス方針）

- 各セクションは独立して追記・更新できるように分けてある。新機能を実装したら「現在の機能」に1行足すだけでよい。アーキテクチャや設計方針そのものを変えるときだけ、該当セクションを書き換える。
- **この文書には「今も有効な結論」だけを書く。** 大きな決定（言語選定・通信方式など）の**経緯と理由**は `HISTORY.md` の「設計判断ログ」に日付つきで残す。同じ議論を繰り返さないため。ここに書くのは結論と、必要なら `HISTORY.md` への参照だけ。
- ルールは「確定」と「設計中（未確定）」を分けて書く。実装が追いついたら、該当項目を設計中から確定へ移す。
- このファイルが肥大化してきたら `docs/` 配下に分割し、ここからリンクする（例: `docs/architecture.md`, `docs/simulation-rules.md`）。目安は、1セクションが数十行を超えて読みにくくなったとき。
- 迷ったら「小さく追記」を優先する。既存の文章を大きく書き直す必要がある変更は、それ自体が `HISTORY.md` の設計判断ログに残すべき変更である可能性が高い。

## 文書の分担

どの文書に何を書くかの規則。**同じことを2箇所に書かない**（必ず片方が古くなるため）。

- `README.md` — **今の操作方法だけ。** ビルド・テスト・ベンチの回し方、`cmd/devview` の起動オプションとキー操作、画面とパネルの読み方。操作が変わったら**上書きする**。「以前は〜だった」「〜は廃止した」といった経緯は書かない
- `HISTORY.md` — **履歴と設計判断ログ。** 前半が「いつ何が変わったか」、後半が「**なぜそう決めたか**」。README.md から落とした古い記述（廃止した操作、変わったコマンド、以前の既定値）はここに移す。大きな設計判断も日付つきでここに残す。どちらも**末尾に追記**し、過去の項目は書き換えない
- `CLAUDE.md`（この文書）— **設計と規約の現在形。** アーキテクチャ、実装ロードマップ、未決事項、崩してはいけない原則。ルールの詳細も操作手順も判断の経緯も書かず、下の文書を指す
- `NODE.md` — **ノードが従うルール。** 状態量・能力・意思決定・行動・戦闘・記憶・繁殖が「何をするか」。数値は書かず `PARAMETERS.md` を指す。ルールを変えたら**上書きする**
- `ENEMY.md` — **外敵が従うルール。** 2026-08-14 時点で全部が未実装なので、仕様案と未決事項の置き場。確定したら「確定したルール」節へ移す
- `PLAN.md` — **これから作るもののチェックリスト。** 段階7以降の実装計画と、その前提として決めたこと・まだ決めていないこと。実装したらチェックを入れる。終わった段階の経緯は `HISTORY.md` へ移す
- `PARAMETERS.md` — **パラメータの一覧。** 世界のパラメータとノード固有の値の区別、既定値、`Config` に無いマジックナンバー、測定済みの感度。値を変えたら上書きする

使い分けの目安: 「どう動かすか」は README.md、「今どうなっているか」は CLAUDE.md（設計・規約）と NODE.md / ENEMY.md（ルール）と PARAMETERS.md（数値）、「いつ変わったか・なぜそう決めたか」は HISTORY.md。操作が変わる変更を入れたら、README.md を書き換えたうえで HISTORY.md に一項目足す。設計判断をしたら、結論を CLAUDE.md の該当節に反映し、理由は HISTORY.md の設計判断ログに残す。

## プロジェクト概要

人間の振る舞い・適応・相対的能力・資源・進化をテーマにした、ゲーム要素のあるシミュレーション。人間は単純化して1人 = 1ノードとして表現する。

将来的に目指す最終形はゲーム：ワールド内のノードの1つをユーザが操作し、子孫を残して、親が死んだらその子に操作を切り替えながら世界を生き延びる。まずは全ノードをAIが動かす観察型シミュレーションとして作り、人間操作は後から差し込む（後述の「意思決定の差し込み口」を参照）。

方針：最初はシンプルに作り、少しずつ機能を追加していく。追加のたびに重くなることを見込んで実行速度を重視する。将来的には自分以外の人にも使ってもらうことを想定している。

v0としてブラウザで動くHTML/JSのプロトタイプ（`human_evolution_sim.html`）が既に存在する。これはルールの参照実装（「仕様がわからなくなったら挙動を見て確認する」用）として扱い、以後の実装はGoに移行する。

## シミュレーションのルール

**実装済みのルールの現在の姿は `NODE.md`（ノード）と `ENEMY.md`（外敵・未実装）にある。** 数値は `PARAMETERS.md`。ここには重複して書かず、**壊してはいけない設計上の原則**と、まだ実装していない項目だけを残す。

### 崩してはいけない原則

ルールを足すときに、これらを壊していないか確認する。どれも一度壊して世界が破綻したか、破綻すると分かっているもの。

- **回復経路を必ず残す。** 「満腹かつ無理をしなければ回復する」が無いと、全員が単調に減って餓死するだけの世界になる。
- **優先順位は 生命 > 子孫 だけ。** 時間は目的ではなくコスト。子孫の行動は、生命の条件が満たされるまで候補にすら挙げない。
- **相手の真の能力は隠しパラメータ。** コントローラが見るのは自分の推定値だけ。`Perception` に真値を載せない（型で保証している）。
- **ハードコードされた行動閾値を作らない。** 「体力がこれ以下なら逃げる」のような条件は書かない。逃走も追撃も先制攻撃も、効用比較の結果として現れさせる。再判断のトリガーは「考え直せ」であって「こうしろ」ではない。
- **恨みの状態を持たない。** 攻撃は感情ではなく、食料競合の先制排除という効用式の一手として扱う。過去に払わされた体力は「避ける理由」として効用から引く側にある。
- **攻撃側有利を保つ。** 殴る側のコスト < 殴られる側のコスト。ただしこれと先制排除が噛み合うと世界が過度に暴力的になり個体数が崩壊しうるので、`CompetitionWeight` と `SkirmishTicks` を触ったら必ず個体数を確認する。
- **能力の役割を重ねない。** パワー＝戦闘効率、理性＝世界を読む精度、頭の良さ＝手を選ぶ質。1つの能力が2つの役割を持つと、選択圧がどちらから来ているのか測れなくなる。

### 将来実装（今はやらない）

- ~~**見た目からの相手分類。**~~ 2026-09-03 に実装（段階10。上記「現在の機能」を参照）。狙いどおり**決め打ちではなく学習の結果**として入ったが、**体つきという信号自体が弱い**（攻撃力との相関 +0.22）ので、当たりをつけられる幅は誤差30に対して0.3しかない。信号が強くなる余地があるのは、地形や種族が増えて「体つきが違えば暮らし方も違う」世界になったとき。
- **配偶者を巡る同性間の戦い**（戦闘と求愛の接続）。
- **仲間・協力。** 外敵は 2026-09-01 に実装済み（`ENEMY.md` 3節）。**群れ狩りは現れなかった**ので、明示的な協力機構の再検討条件は満たされているが、先に段階9（好意）と段階12（伝承）を通す。
- **記憶容量に選択圧をかける経路。** 段階9 で容量・帯域・忘却の速さに役割をつけたのに、`shMemory` は動かない（0.07〜0.08）。役割があることと得であることは別、という例。段階12（伝承）で記憶が「受け取れる知識の量」になったら再測定する。
- ~~**効用式の中の仮定を個体が経験から学ぶこと。**~~ 2026-09-04 に実装（段階12a。下記「現在の機能」を参照）。**仕組みは全部入ったが、信念の学習は既定でオフ**——世界の真の殴り返し率は 0.15 しかなく、`retaliation = 0.7` という思い込みが世界を支えていたため、真実を知ると個体数が半分になる。段階12b で本物の抑止力が入ってから測り直す。
- ~~**種族の汎用化・複数食料種・死体ドロップ。**~~ 2026-09-01 に実装（下記「現在の機能」を参照）。当初の想定と違ったのは、**共通食料A/Bの2種類は作らなかった**こと（AとBを区別するルールが無い）。以下は当時のメモ: 人間・外敵は同じ汎用ノード構造・同じ意思決定エンジンを共有し、パラメータの範囲（種族ごと）と種族固有遺伝子だけで種族差を表現する。食料は3種（共通食料A/B＋死体からの肉ドロップ）。人間は人間由来の肉を食べない（共食い不可）が外敵は種族を問わず肉を食べる。詳細は実装着手時に `NODE.md` / `ENEMY.md` / `PARAMETERS.md` へ書く。
- ~~**遺伝子ベクトルの拡張と学習パラメータ（文化）。**~~ 2026-09-04 に実装（段階12b・12c）。**双方向の等価交換もヒントも狙いどおり入った**が、**1回あたりの歩み寄りは想定よりずっと小さくないと世界が壊れる**（交換は1実行に19000回起きる）。以下は当時のメモ: 遺伝は `Genome []float64`、学習は別ベクトルとして分離する。文化は専用データ構造を作らず、近接個体間の模倣則によるパラメータのクラスタ化として創発させる。模倣は一方向の学習ではなく**双方向の等価交換**とし、伝わる中身は固定名のスカラーだけでなく**個体ごとに可変な「ヒント」**（状況特徴量 × 対象行動 × 重み。効用への加減点としてのみ働き、行動は直接決めない）も含む。「天才」は学習パラメータのどれか1つを通常の突然変異よりずっと大きく飛躍させる特殊イベントとして実装し、**ヒントの構造の変異にも同じ仕組みを流用する**（新しい概念は作らない）。詳細は `PLAN.md` の段階9・段階12。
- **地域ごとの世界リセット（random immigrants）。** 個体数過多・遺伝的多様性の停滞を地域単位で検知し、適応度に応じた生存選抜と新規ランダム個体の投入を行う。地域は既存の空間分割グリッドを粗くまとめたブロックを流用し、移動の壁にはしない（隣接地域からの自然な再流入を狙う）。トリガー条件・選抜方式は未決（下記「未決事項」）。
- **地形（川・高低差・スロープ・穴・崖）。** 移動コスト計算の**地形照会関数は 2026-09-04 に入れた**（`engine/terrain.go`。今は世界じゅうが平坦を返すので挙動は不変で、指紋テストが無変更で通ることがその証拠）。**地形の種類ごとに効く能力が違う**ので一括りにしない（`PLAN.md` の表）——**移動コストの地域差だけが速さに効き**、障害物は頭の良さ、隘路は防御力、遮蔽は回避率に効く。地形本体は段階14〜18 の最後。

## アーキテクチャ（現時点の方針）

- 言語: Go（速度、既存のGo実績、ビルド・テストループの速さを理由に採用）。
- 構成: サーバ・クライアント型のネイティブアプリ。
  - `engine`: シミュレーション本体。状態とtickごとの更新ロジックのみを持ち、描画・通信のことは一切知らない。単体でテストできる形を保つ。
  - `protocol`: クライアント・サーバ間でやり取りするメッセージ型（ワールドのスナップショット、クライアントからのコマンドなど）を定義する共有パッケージ。
  - `server`: `engine` を保持してtickを回し、一定間隔でスナップショットを接続中の全クライアントに配信する。クライアントからのコマンド（パラメータ変更・一時停止など）を受けて `engine` に反映する。
  - `client`: ネイティブGUI（ebiten想定）。サーバに接続し、受信したスナップショットを描画するだけの薄い層。UI操作はコマンドとしてサーバに送る。
- 通信方式: 未確定。第一候補は `net.Conn` + `encoding/gob`（または json）による自前の軽量フレームプロトコルで、外部依存を増やさない。将来Web版が必要になればWebSocket/gRPCへの切替を検討する。

### 意思決定の差し込み口（ゲーム化への布石・早期に敷く）

- 各ノードに「コントローラ」を持たせ、`engine` は毎トリガー `controller.Decide(知覚) → 行動` を呼ぶだけにする。行動は `NODE.md` の「行動の語彙」に統一する。
- 今は全ノードがAIコントローラ（効用式で判断）。将来、1ノードだけを人間コントローラに差し替え、その個体が死んだら指定した子のコントローラをAI→人間に切り替えることで「子に乗り換える」を実現する。AIも人間も同じ行動語彙を出すので、`engine` は誰が操作しているかを知らなくてよい。
- 血統リンク（親→子のIDを記録）も早期に入れておく。将来「自分の子孫に乗り換える」ために必要で、今入れておけば安い。
- この境界（コントローラ／知覚／行動）と血統記録は、UIを作らない今のうちに敷く。後付けは大きな書き換えになるため。

## 開発中の目視確認

- `protocol`/`server`/`client` が揃うのを待たずに、機能を作るたびにその場で目視確認できるよう `cmd/devview` という開発用ツールを用意する。`engine` を直接importし（ネットワークなし）、ebitenでその場に描画するだけの、動作確認専用のツール。
- 本番の `client`（`server` 経由でネットワーク越しに描画する）とは別物。engineの実装が先行している間、挙動を手早く目視確認するための踏み台として使う。`client` が揃った時点で、`cmd/devview` は削除するか開発用モードとして残すかをその時点で判断する。
- 使い方（起動オプションとキー操作の一覧）は `README.md` にある。
- 目視には2つのモードがある。**全体を眺める**（世界が破綻していないか）のと、**1ノードを追う**（なぜその行動を選んだか）。後者は速度を落とすか一時停止して、ノードをクリックし、右のパネルで意思決定トレースを読む。
- 見た目は凝らなくてよい（`human_evolution_sim.html` と似た程度のシンプルな円・色分けで十分）。目的はロジックの目視確認であって、UIの作り込みではない。
- engineに新しい状態や行動を追加したときは、対応するテストに加えて `cmd/devview` で一度動きを目視確認する。
- シミュレーションの内部tick速度と、クライアントへの配信・描画速度は独立させる（サーバは好きな速さで内部tickを回し、配信は一定間隔に間引く）。

## ルール変更の測定（cmd/experiment）

目視で分かるのは「世界が破綻していないか」までで、「このルール変更で能力への選択圧がどちらに動いたか」は見えない。それを測るためのヘッドレスのランナーが `cmd/experiment`。使い方（オプションと出力の読み方）は `README.md` にある。

方針:

- **ルールの切替は `engine` の分岐ではなく `Config` の書き換えで表現する。** 両方の条件が同じバイナリに入っていないと、同じシードで対にして比べられない。ルールを1つ無効化できる形で `Config` に持たせるという既存の方針（2026-08-12の判断、`HISTORY.md`）が、そのままA/Bの前提にもなっている。
- **比較は必ず同一シードの対で行う（paired）。** シード間のばらつきは条件差よりずっと大きく、対にしないと数十シードでも埋もれる。
- **能力の値は最後の1tickではなく末尾2割の平均で読む。** 個体数が数百なので、数体の生死で平均が1回分の選択圧より大きく動く。
- 選択圧の指標は「20000tickでの平均能力の変化量」。副次的に個体数・世代数・戦闘数・死因の内訳（餓死／戦闘）も見る。**片方だけ良くなっていないか**を必ず確認する（例: 知能への選択圧は上がったが世界が暴力的になりすぎて個体数が崩れた、など）。
- 選択圧に触れる変更を入れたときは、この測定を変更の前後で回し、数値を `HISTORY.md` に残す。
- **測定の前に予想を書く。** 選択圧に触れる変更を入れる前に、`cmd/experiment` のどの指標（`dPower`/`dRationality`/`dIntelligence`/`killShare`/`clumping`/`neighbours`/`nearest`/`starved`/`killed`/`extinct` など）が、どちら向きに、誤差より大きく動くはずかを先に書く。「良くなるはず」のような曖昧な予想は測定後にどうとでも解釈できてしまうので、具体的で反証可能な形にする。
- **予想が外れたときほど `HISTORY.md` に残す。** 変更前後の数値だけでなく、何を予想していてどう外れたかを書く。これまでの設計判断ログで一番価値があった記録は、想定が測定で覆ったとき（段階6の深さゲートが逆効果だった件、`killShare` が食料供給と交絡すると思っていたが実際は交絡しなかった件）で、当たった予想より外れた予想のほうが学びが大きい。
- **数値と見た目の両方で確認する。** `cmd/experiment` の数値が動いても、それが狙った挙動によるものかは `cmd/devview` での目視まで分からないことがある。予想には数値の動きに加えて「見た目としてどう変わるはずか」も書き、両方を確認してから変更を確定させる。

## 実装ロードマップ（段階）

各段階は小さく、`cmd/devview` で新しい挙動が必ず目視できる粒度にする。上から順に進める。

**段階0〜6は 2026-08-12 に完了**（`engine` に実装、`cmd/devview` で目視確認済み）。**段階6.5（計測の先行整備）は 2026-09-01 に完了**（5指標＝クラスタ・membership half-life・戦闘の相手・集団間距離・種族ごとの個体数。「集団が無い世界」のベースラインは `HISTORY.md` の同日の項）。**段階7a（空間グリッド）と段階7b（継承則の入れ替え）は 2026-09-01 に完了**（7a＝近傍探索をインデックス経由に、挙動不変。7b＝粒子的遺伝とまれで大きい変異。**測定のベースラインはここで一度リセットされている**——数値は `HISTORY.md`）。**段階11（種族の汎用化と外敵）は 2026-09-01 に実装、2026-09-03 に完了**（`PLAN.md` の順を飛ばした。**群れ狩りは現れなかった**——対照条件つきで確認。`HISTORY.md` 追記23。残っていた「共同戦闘を好意の加点源にする」と「外敵のサイズ分布」は 2026-09-03 に決着。`HISTORY.md` 同日の項）。**段階7c（遺伝子ベクトル化と予算制）は 2026-09-01 に実装完了**（9本の遺伝子が1つの予算を分け合う。**完了条件のうち「大きくて遅い／小さくて速い の並存」だけは段階8 のあとでないと判定できない**）。**段階7d（成長・老い・育児期間）は 2026-09-02 に完了**（`TicksPerYear = 500` を本決め、`MaxLifespan` を 3000 に較正。死因は戦闘62%・餓死34%・消耗4%）。**段階7c の残っていた決定（予算の継承幅・初期配分の α・天才の発生率・魅力の体調重み・予算の価格）は 2026-09-02 に数値をつけて完了**（`HISTORY.md` 同日の項と追記27。**天才2段階の発生率の確定だけは `TicksPerYear` と対なので段階7d へ移送**）。**段階8（チャネル化）は 2026-09-01 に完了**（`killShare` 0.92 → 0.79、個体数 63 → 111、選択圧がパワーから頭の良さへ移った）。**段階9（記憶の容量・帯域・忘却曲線と好意）は 2026-09-02 に完了**（記憶が有限になり、記憶に初めての正の項が入った。選択圧がさらに頭の良さへ寄った。**記憶容量への選択圧は出ない**——段階12 で再測定する）。**段階10（見た目からの学習）は 2026-09-03 に完了**（初対面の推定誤差が固定値より 3.0 小さい。ただし利得の9割は「世界の平均を学んだ」ぶんで、体つきを読んだぶんは1割）。**段階12a（効用式の仮定を個体ごとの値にする）は 2026-09-04 に完了**（好みは可変で既定オン、信念の学習は動くが既定オフ。理由は下記）。**段階12b（伝承）・12c（ヒント）も同日に完了**（等価交換とヒントの模倣。**あるべき状態 B〜E のうち B は達成、`halfLife` はほぼ、`gapRel` は未達**）。**段階13（視界のグリッド化）と地形の下ごしらえも 2026-09-04 に完了**。**残るのは 2026-09-03 に足した段階14〜18（環境と食料生態系）だけ**。`protocol`/`server`/`client` との順序は未確定。

- 段階0：意思決定の差し込み口（コントローラ／知覚／行動の境界）と血統記録を先に敷く。この上に以降を載せる。【完了】
- 段階1：状態モデルを 体力＋空腹度＋食料 に置換。餓死と回復が目視できること。【完了】
- 段階2：移動の体力コストと effort。動き回ると減り、満腹で休むと回復するのが目視できること。【完了】
- 段階3：継続戦闘と逃走。食料を巡る戦い、劣勢側の逃走、攻撃側有利が目視できること。【完了】
- 段階4：強さ推定と記憶（(A)リスク記憶＋減衰、(B)観戦ベースの強さ推定）。ノードをクリックすると他者への推定が見え、対戦・観戦で収束するのを確認。【完了】
- 段階5：リスク判断と先制排除。強い個体を避ける、競合圧に応じた先制攻撃、もつれが目視できること。【完了】
- 段階6：頭の良さによる戦略選択。賢い血統と愚かな血統で生存率を比較できること。【完了。ただし下記の注意を参照】
- 段階6.5：計測の先行整備（あるべき状態を測れるようにする）。【完了。2026-09-01】
- 段階7a：空間グリッド（挙動を変えない高速化）。【完了。2026-09-01。**既定の個体数では数%の持ち出しで、効き始めるのは500〜600体から**】
- 段階7b：継承則の入れ替え（粒子的遺伝＋まれで大きい変異）。【完了。2026-09-01。**能力の分散が約2倍になり、ここで測定のベースラインがリセットされた**】
- 段階7c：遺伝子ベクトル化と予算制。【完了。実装は 2026-09-01、残っていた6つの決定に数値をつけたのが 2026-09-02。**能力は9本の遺伝子が1つの予算を分け合う。指標は絶対値ではなく予算比で読む。** 予算はもう暴走しない（60000tickで442、最大900に対して）。**初期配分のディリクレ α は「初期条件」ではなくその実行の多様性そのもの**だと分かった】
- 段階8：攻撃・防御・回避のチャネル化。【完了。2026-09-01。**「一番危険」という当初の警告は逆だった**——危険だったのは入れないことのほうで、個体数は 63 → 111 に増えた】
- 段階7d：成長・老い・寿命と育児期間。【完了。2026-09-02。**実効値＝遺伝的素質 × 年齢補正（1本の曲線・掛け算は1か所）。** 子ども期は個体数の4割を払う——死亡率ではなく**世代時間**に乗るコスト。育児が効くのは保護ではなく**凝集**だった】
- 段階9：記憶の容量・帯域・忘却曲線と、好意。【完了。2026-09-02。**好意が入ったので段階12（伝承）の前提が揃った。** 休息の露出は重み 0.05——**露出は `RegenRate` の12倍の単位で入るので、大きくすると「視界に1人いれば休息の価値ゼロ」という崖になる**】
- 段階10：見た目からの学習。【完了。2026-09-03。**初対面の推定は各個体が自分で引いた直線から出る。** 利得は同じ遭遇で測って +3.0（誤差 33.4 → 30.3）だが、**そのうち体つきを読んだぶんは +0.34 だけ**——残りは「世界の平均を知ったこと」。**個体数は35%落ちる。原因は学習ではなく `PriorStrength` の水準**】
- 段階12a：効用式の仮定を個体ごとの値にする。【完了。2026-09-04。**事実（学べる）と好み（学べない）に分けた。** `Retaliation = 0.7` は世界の事実ではなく**実在しない報復への恐れという抑止力**で、真の値 0.15 を知ると個体数が半分になる——しかも増えるのは殺しではなく**餓死**。よって `LearningRate` の既定は 0。好みの側（広がり 0.15）は既定オンで、これだけで**理性への選択圧が負から0へ戻る**】
- 段階12b：伝承（等価交換）。【完了。2026-09-04。**`observe` に相乗りする双方向・同時の歩み寄り。** 好意が効用の正の項になり、他個体が生きていることが得になる2本目の経路ができた。**1回の歩み寄りは差の2%**——「中間で会う」0.5 は段階12a が作った多様性を消して個体数を半減させる。値段は個体数（40000tickで −12.7 \*）で、買えたのは好意のある相手の数（+0.90 \*\*）と集団の持続】
- 段階12c：ヒント（進化し続ける言い伝え）。【完了。2026-09-04。**状況×行動×重みの3つ組を上限4本。効用への加減点としてのみ働き、分岐はどこにもない。** 枠は空でも予算を食う。**枠も値段も同じで重みだけ0の対照に勝つ**（40000tickで個体数 22.0 対 14.8）。**手口の多様性を作っているのは重みではなく模倣**——重みを0にしても種類数は変わらず、模倣を切ると伝承なしの水準まで落ちる。**頭の良さへの選択圧は下がる**】
- 段階13：視界をグリッド単位にする。【完了。2026-09-04。**視界は「自セル＋周囲1セル」のブロック**（面積は円と同じになるよう較正）。**「近い」ことが「見える」ことを意味しなくなった**のが本体で、殺しが減る（`killRate` −0.13 \*）。**逃走成功率は動かない**——面積を合わせると逃げ切る距離の平均まで揃い、異方性は分散しか増やさない。**視力は遺伝子にしない**（0リングは致死・2リングはやや悪い＝離散で非線形なので `StrategyDepthUnlock` と同じ轍になる）】
- 地形の下ごしらえは 2026-09-04 に完了（上記「現在の機能」）。**残るのは段階14〜18（環境と食料生態系）**：`PLAN.md` にチェックリストとして分解済み。

段階6の注意：`Intelligence` は「戦略の幅（`StrategyDepthUnlock` による深さゲート）」と「選択品質（`ChoiceNoise`）」の2経路で効くように実装したが、**2026-08-14 のA/B測定で、深さゲートの側は機能していないことが分かったため既定で無効にした**（測定値と判断の根拠は `HISTORY.md`）。現在の頭の良さは評価ノイズだけを通して効く。理性はシードによってばらつく（下記「未決事項」を参照）。

## 未決事項

- 単一インスタンス運用（1人1サーバ+1クライアント）か、複数人が同じサーバに同時接続して同じワールドを見る共有型にするか。まずは前者で作りつつ、サーバ側は最初から複数クライアントへの配信を前提にした作りにしておく。
- 通信プロトコルの詳細（フレーミング方式、コマンド体系、後方互換の持たせ方）。
- 複数クライアント接続時、誰がパラメータを操作できるかのルール（今のところ未検討）。
- バランス調整用の数値（体力/空腹度のレート、effort の逓減カーブ、リスク記憶の減衰率、競合圧の代理指標の重み、攻撃側有利の非対称量など）。素朴に作ってから `cmd/devview` とベンチで詰める。一通りの値は入れたが、下記の3点は特に暫定。
  - 理性（`Rationality`）の選択圧の弱さは、**原因の大半が効用式ではなく継承則だった**（2026-09-01）。粒子的遺伝に変えただけで +2.55 → +10.54（24シード × 50000tick）。`PatienceRationality` が効きすぎている疑いは残るが、**優先度は下がった**。パワー（+20.57）・知能（+29.96）に比べればまだ小さいので、詰めるとしても段階7c（予算制）で能力が上限に張り付かなくなってから。
  - 世界の暴力度は `CompetitionWeight` と `SkirmishTicks` に非常に敏感。設計メモの警告どおり、上げると個体数が崩れる方向に振れる。
  - 個体数は食料（`FoodSpawnRate`）で頭打ちになるよう調整してある。`MaxPopulation` は安全網であって、通常は張り付かない想定。
- 効用式の再計算コスト。まず素朴に全候補評価で作り、ベンチで重くなってからトリガーの間引きや候補の枝刈りで最適化する（方針(b)）。現状は1決定あたり約8.0µs（`BenchmarkDecide`。深さゲートを既定オフにして全候補を採点するようになったぶん、5.8µsから増えている）。
- 地域リセットのトリガー条件（局所的な過密／遺伝的停滞のどちらか、または両方か）と、リセット時の生存個体の選び方（適応度順か、地理的多様性も守るハイブリッドか）。
- プレイヤーの出生地選択UI（ワールド概観からの地域選択）の具体的な見せ方。
- 種族の汎用化・遺伝子ベクトル拡張・地域リセット（上の「将来実装」段階7〜9）を、`protocol`/`server`/`client` より先にengine側でさらに進めるか、先に`protocol`/`server`/`client`を通すか。順序は未確定。

## 設計判断ログ → `HISTORY.md`

「なぜそう決めたか」の記録は **`HISTORY.md` の後半（設計判断ログ）** にある。2026-08-11 以降の全項目をそこへ移した（2026-08-16）。

この文書には**今も有効な結論だけ**を書き、経緯は書かない。新しい設計判断をしたら、結論をこの文書の該当節に反映し、理由と測定値は `HISTORY.md` の末尾に日付つきで追記する。

## コーディング規約

- `go build ./... && go vet ./... && go test -race ./...` が常に通ること。
- 標準ライブラリで完結できるところは外部依存を増やさない（過去のGoプロジェクトと同じ方針）。
- **シミュレーションの定数は package const ではなく `engine.Config` のフィールドとして持つ。** テストでルールを1つずつ無効化して（`MutationStd = 0` など）他のルールから切り離して検証するため、そしてA/Bで条件を差し替えるため。`Config` が実質的な仕様の一覧にもなる。
- **乱数は `World` が持つ `*rand.Rand` 1本に統一する。** 同じ `Config`（同じ `Seed`）なら常に同じ結果になること。確率が絡むルールのテストは「決定的にできるものは条件を作って決定的に検証」「ゆらぎ自体が本質のものは固定シードでN回試行して割合を比較」の2本立てで書く。
- **空間インデックス（`engine/grid.go`）への問い合わせを、位置を動かすループの中に置かない。** インデックスは移動のたびに「古い」印がつくので、動かしながら訊くと1体ごとに作り直しが走り、置き換えたはずの全走査より遅くなる。訊くのは動かす前か、全員動かしたあと。
- 新しいシミュレーションルールを追加したときは、対応するテストと、`NODE.md`（または `ENEMY.md`）の更新をセットで行う。パラメータが増えたら `PARAMETERS.md` にも行を足す。
- コードはすべて英語で書く。変数名・関数名・型名・コメント・ログメッセージ・エラーメッセージ・コミットメッセージを含む。日本語が混ざらないようにする（CLAUDE.mdや設計ドキュメントなど、コード外のプロジェクト文書は日本語のままでよい）。

## コミットの方針

- **`main` に直接コミットする。ブランチは切らない。** 履歴は一本で保つ。理由は2つ。作業者が1人であること、そして**コミット番号を成果物に焼き込むことがある**こと（`cmd/snapshot` の画像は下帯にコミットを持つ）。ブランチを経由すると rebase / squash でハッシュが変わり、焼き込んだ番号が嘘になる。
- **コミットメッセージは英語で書く**（コーディング規約と同じ。コード外の設計文書だけが日本語）。
- **1つの機能・1つの測定 ＝ 1コミット。** 実装・テスト・文書の更新は同じコミットに入れる（`NODE.md` / `PARAMETERS.md` / `HISTORY.md` の追記まで含めて1つ）。
- **記録用の成果物（画像など）は、それを生成したコードとは別のコミットにする。** 順序は「コードを先にコミット → そのハッシュで成果物を生成 → 成果物を次のコミットに入れる」。こうすると成果物が指すコミットに、それを再生成するコードが必ず含まれている。
- **本文には「何を変えたか」だけでなく「なぜその形にしたか」の要点を書く。** 詳細と測定値は `HISTORY.md` にあるので、ここは要点だけでよい。特に**採らなかった素直な選択肢とその理由**は残す価値が高い（例: 連結距離に `PerceptionRadius` を使わなかった理由）。
- コミットするのは**ユーザから指示があったとき**。指示なく勝手にコミットしない。

## パフォーマンス計測（ベンチマーク）

- ホットパス（`engine` のtickループ、近傍探索、大量ノードを走査する処理など）に手を入れたときは、変更の前後で軽量なベンチマークを取り、大きな悪化がないか確認する。
- 回し方は `README.md` にある。Goのベンチマークはデフォルトで1ベンチマークあたり約1秒しか回さないので、変更のたびに実行しても十分速く終わる。
- **計測をテストの形で回すときは `-count=1` を付ける。** `go test` は同じバイナリ・同じ引数の結果をキャッシュするので、付けないと「2回測った」つもりの数値が1回目の使い回しになる（2026-09-01 に実際にやらかした。`HISTORY.md` 追記19）。
- 最初のベンチマークは「固定シード・固定エージェント数（当面は数百程度を想定）で `Step()` を繰り返し呼ぶだけ」のシンプルなもの（例: `BenchmarkEngineStep`）から始める。凝った計測は必要になってから足す。戦闘や食料の取り合いが多発する混雑シナリオもベンチに含めると、トリガー方式のバースト的コストを拾える。
- **世界を進めるベンチマークは、一定tickごとに世界を作り直す**（`engine/bench_test.go` の `benchSteps`。作り直しの間はタイマーを止める）。**1つの世界を `b.N` 回進める素朴な形は一定の仕事量を測っていない**——測っている間も世界が進むので、長く回すほど別の世界を測ることになり、同じコードで 82µs〜758µs までばらつく（2026-09-04 に実際に踏んだ。`HISTORY.md` 追記33）。
- **プロファイルは上位関数で止めず、`-peek` で呼び出し元まで降りる。** 「遅い関数」ではなく「**結果を使わない呼び出し**」が本体だったことがある（同上）。
- 毎回数値を記録する必要はないが、明らかな悪化（体感の目安としてns/opが1桁%以上悪化するようなレベル）に気づいたら、コミットメッセージか `HISTORY.md` に一言残す。
- 全PR・全コミットで強制するものではなく、「パフォーマンスに影響がありそうな変更のときだけ」実施する軽い習慣として扱う。

## 新機能を追加するときの流れ

1. `engine` にロジックを追加し、ユニットテストを書く。ホットパスに触れる変更なら、上記のベンチマークも変更前後で実行する。見た目・挙動に関わる変更は `cmd/devview` で目視確認する。
2. 必要なら `protocol` にメッセージ型の変更を加える（後方互換を意識する）。
3. `server` / `client` に反映する。
4. このファイルの「現在の機能」に1〜数行追記する。ルールの追加・変更なら `NODE.md` / `ENEMY.md` を、パラメータが増減したなら `PARAMETERS.md` を更新する。操作が変わったら `README.md` を上書きする。
5. `HISTORY.md` の「変更の記録」に、何がどう変わったかを1項目足す（末尾に追記）。選択圧に触れる変更なら `cmd/experiment` を前後で回し、その数値もここに残す。
6. アーキテクチャや通信方式など大きな設計判断があれば、**結論**をこのファイルの該当節に反映し、**理由と経緯**を `HISTORY.md` の「設計判断ログ」に日付つきで残す（末尾に追記、過去の行は書き換えない）。

## 現在の機能

Goモジュール名は `github.com/toku463ne/cld_human_behaviour`。現時点で存在するのは `engine` と `cmd/devview` のみで、**`protocol` / `server` / `client` はまだ未着手**（未決事項も参照）。実装ロードマップは段階0〜6・6.5・7a〜7d・8・9・10・11 まで完了している（12・13 と、2026-09-03 に足した14〜18 が残り）。

- `engine`: シミュレーション本体。描画・通信を一切知らない。`NewWorld(Config)` で生成し、`Step()` で1tick進める。読み出しは `Agents()` / `Foods()` / `Stats()` / `Tick()` / `AgentByID()` / `Opinions(id)`。
- `engine.Config`: 全パラメータのフィールド化。テストはここを1つずつ潰して各ルールを分離して検証する（例: `MutationStd = 0` で突然変異を止める、`RiskDecayPerTick = 0` で忘却を止める）。
- 種族: human と enemy の2種族。**同じノード構造・同じ意思決定エンジン**で、違いは予算の抽選範囲と食性だけ（enemy のほうが大きく、肉しか食べない）。求愛は同種内のみ。異種族は効用式に「倒せば肉になる相手」として載る（`Perception` の `AgentView.Prey` / `Meat`）。外敵は繁殖もするが、この広さでは捕食者だけで安定しないので地図の外から一定間隔で入ってくる（`EnemySpawnTicks`）。
- 食料は2種類: 植物（世界に湧く。人間だけが食べる）と肉（死体。**自分の種族の肉は食べない**、倒した者が一定時間独占、放置すると腐る）。**肉の量は死んだ個体の予算に比例する**＝大きい相手ほど割に合う、というのが集団で狩る唯一の動機（**外敵のサイズを 400〜700 に振っても参加人数は動かない**ので、群れ狩りが現れない理由はサイズ分布ではない）。**分配に共に与った者どうしには好意が入る**（`AffinityHunt`）。植物と肉は**別枠の上限**を持つ（同じ枠だと肉が枠を埋めて植物が湧かなくなる）。
- 状態モデル: 体力（`Vitality`）・空腹度（`Hunger`）・**成熟度（`Maturity`）**・世界に湧く食料。食べる→空腹度↓、満腹かつ無理をしなければ体力↑、動く/戦うと体力↓、空腹が進むと体力↓。死因は餓死・戦闘・消耗（寿命）の3つで、既定でおよそ 34% / 62% / 4%。
- 成長と老い（`Agent.AgeFactor`）: **遺伝子の実効値 ＝ 遺伝的素質 × 年齢補正**で、掛け算は `Agent.Ability` の1か所だけ（`Gene()` は遺伝的素質のまま＝繁殖と測定はこちらを読む）。曲線は1本で、**若い端が成長**（`Maturity` が食事で 0→1、`ChildhoodYears` = 2年）、**老いた端が衰え**（`SenescenceYears` = 12年から `SenescenceFloor` まで）。**成人（`ReproMaturity`）まで求愛は候補に挙がらない**——これが子ども期のコストの本体で、死亡率ではなく世代時間に乗る。
- 育児期間: 出産後 `ChildRearingTicks`（1000＝2年）、子は親から `RearingRadius`（130＝**視界と同じ**）以上離れると戻る。**給餌なし・親側の負担なし・親の効用に子は入らない。** 半径を短くすると子を食料から引き離して殺す。実測された効き方は**保護ではなく凝集**（子の生存率は動かず、死亡率と個体間距離が動く）。
- 慢性的な低体力（#33）: 体力が `FrailVitalityShare` を下回る状態が続くと寿命が減る。**`ShockRisk`（効用式に載る今の危険）とは別物**で、`Perception` にも効用式にも出てこない。
- 寿命消費（背景処理・`World.spendLifespan`）: `Agent.Lifespan` が空腹度過多（`StarveHunger` 超）または過度な満腹（`OverfedHunger` 未満＝「腹八分目」超）の間だけ毎tick減り、0で死ぬ（`Stats().AgingDeaths`）。完全にバックグラウンドの機械的処理で、`Perception` に載らず効用式にもゴール項が無い。ノードは自分の寿命も、死因としての老化も知らない。選択圧は世代を超えた差次的生存だけを通じてかかる。既定値は 2026-09-02 に較正した（`MaxLifespan = 3000`。6000では標準実行長で一度も発火せず、1500では世界が壊れる）。
- 魅力（`fitness`）＝ 魅力遺伝子 ×(1−`FitnessConditionWeight`) ＋ 体調 × `FitnessConditionWeight`（既定 0.35）。**実能力（攻撃力・理性・頭の良さ）は一切入らない**（強さと「良い相手に見えること」を1つの値に兼ねさせない）。**体調の重みを上げると個体数は増えるが、配偶者選びが「今の健康状態」だけになって遺伝する装飾という選択軸が消えるので、上げていない**（値段は個体数で約2割。数値は `HISTORY.md` 2026-09-02）。
- 遺伝子は `Agent.Genome []float64`（`engine/genome.go`）。体力上限と移動速度は個体ごと（`a.MaxVitality(cfg)` / `a.MaxSpeed(cfg)` ＝ 基準値 × 遺伝子 / 50.5）で、**体力に関する閾値（求愛条件・子の初期体力）はその個体の上限に対する割合**で持つ（絶対値だと体力遺伝子の低い個体が「一生求愛できない」崖になる）。`Gene` 定数で名前を付けた9本＝攻撃力・防御力・最大体力・移動速度・回避率・記憶容量・理性・頭の良さ・魅力。**9本すべてに役割がついた**（防御力・回避率は段階8、記憶容量は段階9）。読むときは `a.Attack()` / `a.Rationality()` / `a.Intelligence()` か `a.Gene(g)`。**9本は1つの予算を分け合う**（`a.Budget()` ＝ 合計）。初期個体だけが予算（`GeneBudgetMean` ± `GeneBudgetStd`）と配分（ディリクレ、`GeneInitAlpha`。ガンマ分布のサンプラは `engine/genome.go` に自前で実装）を抽選し、**子は予算を親の一方から継ぎ（＋`BudgetInheritSpread` の正規乱数）、遺伝子ごとに継いだ値をその予算に合わせてスケールする**。したがって**突然変異は配分を動かすだけで合計を増やさない**。`BudgetHeritability = 0` で「予算が遺伝しない世界」に切り替えられる。**予算には維持費がある**（`BudgetUpkeep`。大きな身体ほど空腹が速く進む）——これが無いと予算は世代とともに際限なく上がり、予算が制約でなくなる。
- 能力の役割: 攻撃力（体力あたりの戦闘効率）／理性（推定精度＝世界の読みのノイズ）／頭の良さ（手の評価のノイズ。戦略の幅の経路は既定で無効、`StrategyDepthUnlock = 0`）／記憶容量（覚えていられる人数・取り込む速さ・忘れる速さ）。
- 意思決定の差し込み口: `Controller` interface（`Decide(*Perception) Action`）。`engine` はトリガー時にこれを呼ぶだけ。`World.SetController(id, c)` で1体だけ差し替えられる（将来の人間操作用）。`Perception` は他個体の真の能力を含まず、観測者自身の推定値だけを渡す。
- 行動の語彙: `ActRest` / `ActMove` / `ActEat` / `ActAttack` / `ActFlee` / `ActObserve` / `ActCourt`。すべて effort（体力の注ぎ具合）を持つ。
- 再判断トリガー: 攻撃された／体力が一定量減った／目標を見失った・到達した／一定tick何も起きない／視界に食料が入った。逃走用の閾値は存在しない。
- 視界（`engine/sight.go`）: **「自分が立っているセル＋周囲1セル」のブロック**（`SightGrid = false` で以前の半径130の円）。セルの大きさは**円と同じ面積になるよう較正**（76.8）——合わせないと「見える量」と「見える形」の変化が分離できない。**円との違いは粗さではなく、どこに立っているかに依存すること**: 近いのに見えない相手（セル境界のすぐ向こう）と遠いのに見える相手（対角セルの隅、最大177）が同時にいる。**「近い」が「見える」を意味しなくなった**のがこの段階の本体で、`neighbours`（円の物差し）が不動のまま `killRate` が −0.13 \* 下がる。**逃走成功率は動かない**（面積を合わせると逃げ切る距離の平均まで揃い、異方性は分散しか増やさない）。**視界のグリッドは空間インデックスとは別物**——インデックスは結果を変えてはいけない最適化なのでセル幅が `Config` に無く、視界は世界のルールなので `Config` にある。インデックスへの問い合わせは**半径ではなく矩形**（ブロックの中心に個体が立っていないので、半径で囲むと4倍近く舐める）。**視力は遺伝子ではない**。
- 地形の下ごしらえ（`engine/terrain.go`）: 移動の体力コストが `w.moveCostOn(x, y, effort)` → `w.terrainAt(x, y)` を通る。**今は世界じゅうが `flatGround`（コスト倍率1）を返すので挙動は完全に不変**（指紋テストが無変更で通る）。狙いは**速さに「有利な場所と不利な場所」を作ること**——速さは9本で一番買われている遺伝子（予算比0.230）なのに何とも取引していないので、「速くて脆い／遅くて頑丈」が並存しない。**倍率は速度ではなくコストに掛ける**（速度を落とすのは effort がすでにやっていることで、二重にすると測定で区別できなくなる。またコストにすれば、荒れ地を速く渡ることが速さと体力の取引そのものになる）。**地域ブロック（段階14）とは別物**——地域は「世界が何をくれるか」の単位、地形は「移動が何を要求するか」の地図。**コントローラは今も平坦だと思って計画する**（`Perception` に地形は載っていない）ので、この食い違いを埋めるかどうかは地形本体の段階で決める。
- 効用比較（`AIController`）: 全候補を「生命の価値×生存確率の改善 ＋ 子孫の価値×成立確率 − 体力コスト − 時間コスト」で採点し、最良手を選ぶ。逃走・追撃・先制攻撃はすべてこの比較の結果として現れる。**食料レースの勝率は到達時間の比**（`RaceOnDistance` で旧・距離比に戻せる）——相手の速さは隠しパラメータなので世界の基準速度を仮定し、**両者が同じ effort で動くと仮定する**ので effort は約分され、残るのは脚の差だけ。**速さの予算比は動かなかった**（速さはすでに到達tick数を通じて効いていた）が、地形が入れば距離と時間は食い違うので先に直してある。
- 戦闘の構え（`engine/stance.go`）: 戦闘系の行動は effort を「殴る・守る・かわす」の3チャネルに配分する。選べるのはプリセット3種（攻め寄り／守り寄り／逃げ寄り）だけ——自由配分にすると候補が立方体に増えるため。守りはダメージを減らすが同tickの打撃も減り、かわしは確率で丸ごと無効にする。**体力消費は Σ(チャネルの単価 × 使用率)** で、使っているチャネルは役に立たなくても払う。**食事中・休息中は無防備**（構えを持たない）。**相手の防御力・回避率は隠しパラメータ**（自分のものだけが効用式に入る）。
- 戦闘: 毎tickの継続ダメージ。殴る側のコスト < 殴られる側のコスト（攻撃側有利）。打撃は全員分をまとめてから同時に適用するので、スライス上の並び順が有利不利にならない。
- 見た目からの学習（`engine/appearance.go`）: 初対面の相手に固定値を当てはめるのをやめた。**見えるのは体つき（`Appearance` ＝ 体力遺伝子と移動速度遺伝子の平均 × 年齢補正）だけ**で、攻撃力は入らない。各個体は強さの観測（戦闘・観戦）を「この体つきはこれくらい殴った」の1点として貯め、**最小二乗の直線を1本**当てて次の初対面に使う。**engine は写像を持たない**（同じ世界を見た2体でも違う線を引く）。**傾きは0へ引き戻す**（`AppearanceSlopePrior`。観測数が数十では素の傾きはノイズで、引かないほうがマシになる）。**記憶容量は消費しない**（覚える枠が無くて記録できなかった観察からも印象は残る）。利得は同じ実行の同じ遭遇で測る（`learnGain` +3.0、うち体つきを読んだぶんは +0.34）。**個体数は 22.8 → 14.8 に落ちるが、原因は学習ではなく水準**——`PriorStrength` を 50 から実際の平均（36）へ下げるだけで同じだけ落ちる（＝よそ者の過大評価が世界を支えていた）。
- 効用式の中の仮定（`engine/lore.go`）: 効用式が使う5つの数値は**全ノード共通の定数ではなく個体ごとの値**。**2種類あり、どこから来られるかが違う**。**世界の事実**（殴り返される度合い・求愛が通る確率）は正解が世界にあるので走る平均で経験から学び、証拠の数には上限がある（上限が無いと老いた個体が世界の変化に気づけない）。**好み**（`RiskWeight` / `CompetitionWeight` / `ShockRisk`）は正解が無いので粒子的遺伝＋突然変異＋選択に任せ、世界の値の4倍でクリップする。**殴り返しの観測は「1回の戦い＝1票」**（engagement 開始5tick後に1度だけ。1打撃ごとに数えると長い戦いが答えを支配する）で、**学ぶのは殴った側と傍観者だけ**（殴られた側の選択は世界についての証拠ではない）。**信念の学習は既定でオフ**（`LearningRate = 0`）——動かないからではなく動くからで、真の殴り返し率 0.15 を知った世界は個体数が半分になる（増えるのは殺しではなく餓死）。**好みの側は既定オン**（`LoreInitSpread = 0.15`。0.25 は個体数を2割払うので較正して下げた）で、これだけで理性への選択圧が −6.95 → −0.83 に戻る。`World.Lore()` は集団の平均・広がりと**世界が実際にやっていること**（`TrueRetaliation` / `TrueAccept`）を返す読み取り専用の測定。
- 伝承（`engine/lore.go` の `exchangeLore`）: 上の5つの仮定は**他個体と交換できる**。専用の行動は作らず**既存の `observe` に相乗り**（観察はすでに体力と時間を払い、1度に1人しか観察できないという排他性も持つ）。**一方向の「学ぶ」ではなく双方向・同時の歩み寄り**で、観察を選んだのは片方でも見られた側も変わる。**1回の歩み寄りは差の2%**（`LoreExchangeRate`）——「中間で会う」0.5 にすると、交換が1実行に19000回起きるので集団の値が数世代で1点に収束し、段階12a が作った好みの多様性が消えて個体数が半減する。**好意の加点は実際に動いた量に比例**（すでに値が近い相手との交換は自動的に無価値。判定は書いていない）。**垂直伝承と水平伝承は同じ経路**（親子は育児期間ぶん共在するので同じ交換が多く走るだけ）。交換の記憶は**割り込まない枠**で取る（殴られたことは世界が押しつける出来事だが、通りすがりの交換はそうではない）。**好意が効用の正の項になった**（`LoreValue`。他個体が生きていて近くにいることが得になる2本目の経路で、集団の**持続**を説明するのはこちら）。**被参照数は上位5分の1が0.48**——完全均等 0.20・無作為の雑音の底 0.22 に対して偏りは実在するが、「数人の教師と群衆」ではない。
- ヒント（`engine/hint.go`）: **効用式に書かれていない関係**を選択に見つけさせる枠。「**状況特徴量 × 対象行動 × 重み**」の3つ組を上限4本。**ヒントは行動を決めない**——既存の効用比較への加減点としてのみ働き、ヒント由来の分岐はどこにも無い（最大重みでも致命的な手を選ばせられないことをテストで固定）。**知能・理性でゲートしない**（`StrategyDepthUnlock` の轍を踏まない）。要求するのは**予算**だけで、**空の枠でも払う**（払っているのは入れ物）。**突然変異は二段階**——重みは通常の変異、**何についてのヒントかが変わるのは天才イベントのときだけ**。**継承は染色体式を枠単位に拡張**。**交換ではヒントは歩み寄らずコピーされる**（「腹が減ったら大物を狙え」の半分は弱い版ではなく無だから）ので、**空の枠を持つ相手にだけ**入る＝血統を通らない唯一の経路。**手口の多様性を作っているのは重みではなく配られ方**——重みを0にしても種類数は変わらないのに、模倣を切ると 1個体が持つヒント 1.50 → 0.77、種類数 7.11 → 4.38 と伝承なしの水準まで落ちる。**頭の良さへの選択圧は下がる**（安い経験則が手に入ると、手を丁寧に評価する見返りが減る）。
- 記憶（`engine/memory.go`）: 相手ごとに1レコードで、中身は (A) リスク記憶＝その相手に失った体力の累積、(A') **好意**＝その相手にしてもらったことの累積、(B) 強さ推定＝ベイズ更新の平均と分散。(A)(A') は読み出し時に遅延減衰し、(B) は直接戦闘と観戦で更新され、理性が高いほど正確、観測数が増えるほど分散が縮む。
- **記憶は有限**: 覚えていられる人数（`MemoryCapacity × 記憶遺伝子/50.5`）、1tickに取り込める数（容量から導出。専用遺伝子は作らない）、忘却の速さ（`(100 − 記憶)/49.5` 倍）のすべてが記憶遺伝子から決まる。あふれたら**リスクも好意もゼロのレコード**を捨てる（価値の残るレコードは群衆の一人には押し出されない。押し出せるのは殴られた・ペア成立・出産・共同で仕留めたという**世界の側の出来事**だけ）。**視界に入れると、値は変えずに減衰の起点だけが更新される**（近くにいる相手のことは忘れない）。**覚えられなかった相手は何度会っても初対面のまま**（`PriorStrength` が当たる）。
- **好意**: 記憶に入る初めての正の項。発生源は**実イベントだけ**（ペア成立・出産・**直系の親子1ホップ**・**共同で仕留めたこと**）で、**共在時間による加点はしない**。共同狩猟の好意（`AffinityHunt`）は**血縁でも配偶者でもない相手から得られる唯一の好意**で、誰が数えられるかは新しいルールではなく**死体の分配ルールそのまま**（傍観者は入らない）。**ただし群れ狩りが現れない世界では発火機会が少なく**（20000tickで13.7回）、集団の指標は動かない——効くのは段階12 で好意が効用の正の項になってから。専用の枠を作らないので、**好意を持てる相手の人数は記憶容量で頭打ちになる**（＝集団の実質的な最大人数が、集団という概念を書かずに出てくる）。**敵意は新設していない**（リスク記憶がそのまま敵意）。今のところ効くのは休息の露出の割引だけで、効用の正の項にするのは段階12 と対にする。
- **休むことは無防備であること**（`RestExposureWeight`）: `rest` の効用に「近くの好意の無い相手の推定の強さ × 近さ」を、休んでいる間ずっと入り続けるダメージとして乗せる（`PlanHorizon`/`ShockRisk` と同じ「分岐しない1本の軌道」の形）。`Decide` の先頭で近傍を1度走査し、被攻撃ダメージと露出を同時に作る。**重みは 0.05 と小さい**——`exposed` は `recoverable()` の中で `RegenRate`（0.09）から引かれるのに、隣人1人ぶんの脅威は 1.14/tick と12倍あるので、大きくすると勾配ではなく崖になる（視界に1人いれば休息の価値ゼロ → 消耗死 → 出産前のペアが壊れる）。
- 遺伝: 子の能力は**能力ごとに親のどちらかの値をそのまま引き継ぐ**（粒子的遺伝）。引き方は能力ごとに独立で、連鎖群も性染色体も無い。混合遺伝（両親の平均）は分散を毎世代半分にするので 2026-09-01 にやめた。**突然変異はまれで大きい**（`MutationRate` 2% × `MutationStd` 40）＝毎回小さく乗せると親の値が子に残らず、粒子的遺伝にした意味が消えるため。
- 血統: 子は `ParentIDs`、親は `ChildIDs` を記録する。
- 乱数は `World` が持つ `*rand.Rand` 1本。同じ `Config`（同じ `Seed`）なら常に同じ結果になる。コントローラも `Perception.Rand` 経由でこれを使う。
- 意思決定トレース（`engine/trace.go`）: `Decide()` 1回分の記録。「どのトリガーで呼ばれたか（`Trigger`）」「比較した候補ごとの効用の内訳（`Utility`：目的ごとの `Goal{Value, Chance}`、リスク・体力コスト・時間コスト）」「頭の良さによる評価ノイズ（`Noise`）と実際に比較したスコア」「選ばれた行動」を持つ。効用の合計は `Utility.Total()` ＝ Σ(Value×Chance) − Risk − VitalityCost − TimeCost で、効用式の唯一の実装でもある（トレースが式とズレようがない）。
- トレースは**追跡対象に指定したノードだけ**記録する。`World.TrackDecisions(id, true/false)` で開始・停止、`DecisionTraces(id)`（古い順、直近16件）と `LastDecisionTrace(id)` で読み出す。記録はノードの死後も残る（死に際に何を考えていたかが一番読みたい）。指定していないノードは何も記録しない。
- 再判断トリガーは `Trigger` として名前が付く（spawned / target lost / goal reached / attacked / vitality drop / idle / food in sight / bond ended / controller set）。`AIController` 以外のコントローラ（将来の人間操作）でも、トリガーと最終行動だけは `engine` 側が記録する。
- 挙動の指紋（`engine/golden_test.go`）: 既定条件・シード1〜3を5000tick回したときの個体数・出生・死亡・戦闘数・世代数と平均値を固定値で押さえたテスト。**「速くするだけの変更」が世界を変えていないことを、同じバイナリ内の比較では証明できない**ために置いてある（2026-09-04 の記憶キャッシュがまさにこの用途で、**数値を1つも書き換えずに通ったことが挙動不変の証拠**になった）。ルールを意図して変えたときは、その変更と同じコミットで数値を更新する（赤いテストを緑にするために書き換えない）。
- テスト: `engine/world_test.go`（ルールごとの単体テストと、8000tick回す統合テスト）と `engine/decision_test.go`（シナリオテスト）。後者は状況を手で組み立てて `Decide()` を1回呼び、選ばれる行動をassertする（格上に守られた食料／格下との食料争い／逃げ場のない被攻撃／満腹で消耗している／餓死寸前で求愛を検討すらしない、など）。効用の内訳やそもそも候補に挙がったかまでトレース経由で確認しているものもある。
- ベンチマーク: `engine/bench_test.go` に `BenchmarkEngineStep` / `BenchmarkEngineStepCrowded` / `BenchmarkDecide`。
- 空間インデックス（`engine/grid.go`、非公開）: 一様グリッド。ノードと食料をセルに登録し、「この近くに誰がいるか」を全体走査せずに答える。近傍を訊く3か所（`perceive()` の他個体と食料、`nearestFoodInSight`、`exchangeReadings`）がこれを通る。**世界のルールを何も変えない**（同じシードなら1ビットも変わらないこと）のが唯一の条件なので、セル幅は `Config` ではなく `PerceptionRadius` から導出する。**インデックスは差分更新せず、移動・出生・死・食料の増減で「古い」印を付けて、次に問い合わせが来たときにまとめて作り直す**（1tickあたり実質2回）。返す候補は**必ず添字の昇順**——知覚は他個体ごとに乱数を引くので、走査順は同じシードが再現するものの一部だから。**移動ループの途中で問い合わせると毎回作り直しになる**ので、動かす前か後に訊く（実測で1tickあたり1.05回）。**個体数が数百のうちは全走査のほうが速く、効き始めるのは500〜600体から**（数値は `HISTORY.md`）。
- `engine.World.Clusters(linkDist)`: 集団の構造（単連結の連結成分。クラスタ数・サイズ分布・最大成分の割合・個体ごとの所属ラベル `Labels`）。読み取り専用の解析で、ノードは集団IDを持たない。連結距離は `engine.DefaultClusterLinkDist = 30` 固定（`Config` に入れない＝世界のルールではなく測る側の目盛りだから。距離が違う測定値どうしは比べられない）。`Spacing()` と同じく O(n²) なので間引いて測る。
- `engine.World.ClusterGaps(linkDist)`: 集団間の距離分布（各集団から最も近い別の集団までの最短距離。平均・中央値・下位1割・密度で割った相対値）。単独個体は集団としても対象としても数えない。**連結距離より短い値は原理的に出ない**（それ以内なら同じ集団になるため）。相対値は「構造なしの配置＝1」に較正済みなので、1未満＝偶然より寄り集まっている、1超＝避けている、と読める。
- `engine.MembershipTracker`: 同じ顔ぶれが一緒に居続ける時間（membership half-life）。一定間隔で `Observe(w)` を呼ぶと、同じクラスタにいた**2体の組**が何tick後まで同じクラスタに居続けるかの生存曲線を貯め、半減期を返す。クラスタは合体・分裂するので「同じクラスタ、のちほど」は定義できないが「この2体はまだ同じクラスタか」は定義できる、というのが組で測る理由。**片方が死んだ組は離れたと数えない**（在籍時間ではなく寿命を測ってしまうため）。世界には一切書き込まない。
- `engine.FightTracker`: 戦闘を「相手が誰か」で分ける。**素直な「集団内戦闘率 ÷ 集団間戦闘率」は測れない**（打撃が届く `CombatRadius` はクラスタの連結距離より短いので、殴り合う2体はつねに同じクラスタにいる＝集団間戦闘は稀ではなく不可能）。代わりに、同じ遭遇（`CombatRadius` 以内にいる組）を「`DefaultCompanionLag` tick前から同じクラスタにいた組（仲間）」と「そうでない組（よそ者）」に分け、**遭遇あたりの**戦闘率とその比を出す。比が1を下回るほど「仲間とは争わない」。世界には一切書き込まない。
- `engine.CensusTracker`: 種族ごとの個体数。一定間隔で `Observe(w)` を呼ぶと直近 `DefaultCensusWindow`（5000tick）の窓の中の**種族ごとの平均・最小・最大**を返す。1tickの数え上げでは共存しているかどうかは決まらない（あるべき状態Aが窓の平均で書かれているのはそのため）ので窓で持つ。読むのは振れ幅ではなく **Trough（窓の最小値 ÷ 平均）＝振動の谷が0にどれだけ近づいたか**。振動は許容されるが、谷が0に触れた種族だけは戻ってこない（移入が無いので0は吸収状態）。**絶滅は窓ではなく実行全体で覚える**（窓が死を通り過ぎると見えなくなるため）。種族の一覧は持たず、実際に現れたラベルを数えるだけなので、段階11 で種族が増えてもこのファイルは触らない。世界には一切書き込まない。
- `engine.Spacing()`: 個体の散らばり具合（視界内の他個体数・最近傍距離・一様分布と比べた集まり具合）。`Stats()` と別なのは O(n^2) だから。協力を入れる前後で「群れているか」を測るために用意した。
- `cmd/experiment`: ルール変更をA/Bで測るヘッドレスのランナー。条件（`Config` を書き換える関数）× シード × tick数を並列で回し、条件ごとの平均±標準誤差と、**同じシードどうしの差分**を出す。選択圧に触れる変更のときはこれを前後で回す。方針は「ルール変更の測定」の節、使い方は `README.md`。
- `cmd/snapshot`: 記録用の画像をPNGに描くヘッドレスの開発用ツール。`-mode world` は1瞬間の世界をクラスタごとに色分けして、`-mode curve` は membership の生存曲線を複数シードの平均で描き、`-mode genes` は能力の分布を**開始時の分布と重ねて**描く（継承則の変更は世界の絵には映らない＝2つの実行は「誰がどこに立っているか」が違うだけなので、分布でしか比べられない）。数値では分からない「その集団がどんな形か」「曲線がどんな形か」を記録に残すためのもの。決定的で、画像の下帯に条件と**コミット**を焼き込む（画像がリポジトリの外に出ても出所が分かるように）。出力は `docs/images/`。使い方は `README.md`。
- `cmd/devview`: ebitenで `engine` を直接描画する開発用ツール。**円は身体そのもの**（輪の大きさ＝体力上限＝体力遺伝子、塗り＝今残っている体力、後ろの尾＝移動速度、輪の太さ＝攻撃力）で、右パネルには遺伝子9本と予算比が出る。**予算制で「どんな身体を買ったか」が個体ごとに違うようになったので、体力そのものを大きさに使うのをやめた**（傷ついた大型と無傷の小型が同じに見えてしまうため）。全体を眺めるモードと、1ノードを追うモード（クリックで選択＝追跡、一時停止・1tick送り・低速再生、右パネルに意思決定トレースと他者への強さ推定）を持つ。**起動方法・キー操作・画面とパネルの読み方は `README.md`**（ここには書かない）。