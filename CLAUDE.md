# CLAUDE.md

## このファイルについて（メンテナンス方針）

- 各セクションは独立して追記・更新できるように分けてある。新機能を実装したら「現在の機能」に1行足すだけでよい。アーキテクチャや設計方針そのものを変えるときだけ、該当セクションを書き換える。
- **この文書には「今も有効な結論」だけを書く。** 大きな決定（言語選定・通信方式など）の**経緯と理由**は `HISTORY.md` の「設計判断ログ」に日付つきで残す。同じ議論を繰り返さないため。ここに書くのは結論と、必要なら `HISTORY.md` への参照だけ。
- ルールは「確定」と「設計中（未確定）」を分けて書く。実装が追いついたら、該当項目を設計中から確定へ移す。
- このファイルが肥大化してきたら `docs/` 配下に分割し、ここからリンクする（例: `docs/architecture.md`, `docs/simulation-rules.md`）。目安は、1セクションが数十行を超えて読みにくくなったとき。
- 迷ったら「小さく追記」を優先する。既存の文章を大きく書き直す必要がある変更は、それ自体が `HISTORY.md` の設計判断ログに残すべき変更である可能性が高い。

## 文書の分担

どの文書に何を書くかの規則。**同じことを2箇所に書かない**（必ず片方が古くなるため）。

- `README.md` — **今の操作方法だけ。** ビルド・テスト・ベンチの回し方、`cmd/devview` の起動オプションとキー操作、画面とパネルの読み方。操作が変わったら**上書きする**。「以前は〜だった」「〜は廃止した」といった経緯は書かない
- `HISTORY.md` — **履歴と設計判断ログ。** 前半が「いつ何が変わったか」、後半が「**なぜそう決めたか**」。README.md から落とした古い記述（廃止した操作、変わったコマンド、以前の既定値）はここに移す。大きな設計判断も日付つきでここに残す。どちらも**末尾に追記**し、過去の項目は書き換えない
- `CLAUDE.md`（この文書）— **設計と規約の現在形。** アーキテクチャ、実装ロードマップ、未決事項、崩してはいけない原則。ルールの詳細も操作手順も判断の経緯も書かず、下の文書を指す
- `NODE.md` — **ノードが従うルール。** 状態量・能力・意思決定・行動・戦闘・記憶・繁殖が「何をするか」。数値は書かず `PARAMETERS.md` を指す。ルールを変えたら**上書きする**
- `ENEMY.md` — **外敵が従うルール。** 2026-08-14 時点で全部が未実装なので、仕様案と未決事項の置き場。確定したら「確定したルール」節へ移す
- `PLAN.md` — **これから作るもののチェックリスト。** 段階7以降の実装計画と、その前提として決めたこと・まだ決めていないこと。実装したらチェックを入れる。終わった段階の経緯は `HISTORY.md` へ移す
- `PARAMETERS.md` — **パラメータの一覧。** 世界のパラメータとノード固有の値の区別、既定値、`Config` に無いマジックナンバー、測定済みの感度。値を変えたら上書きする

使い分けの目安: 「どう動かすか」は README.md、「今どうなっているか」は CLAUDE.md（設計・規約）と NODE.md / ENEMY.md（ルール）と PARAMETERS.md（数値）、「いつ変わったか・なぜそう決めたか」は HISTORY.md。操作が変わる変更を入れたら、README.md を書き換えたうえで HISTORY.md に一項目足す。設計判断をしたら、結論を CLAUDE.md の該当節に反映し、理由は HISTORY.md の設計判断ログに残す。

## プロジェクト概要

人間の振る舞い・適応・相対的能力・資源・進化をテーマにした、ゲーム要素のあるシミュレーション。人間は単純化して1人 = 1ノードとして表現する。

将来的に目指す最終形はゲーム：ワールド内のノードの1つをユーザが操作し、子孫を残して、親が死んだらその子に操作を切り替えながら世界を生き延びる。まずは全ノードをAIが動かす観察型シミュレーションとして作り、人間操作は後から差し込む（後述の「意思決定の差し込み口」を参照）。

方針：最初はシンプルに作り、少しずつ機能を追加していく。追加のたびに重くなることを見込んで実行速度を重視する。将来的には自分以外の人にも使ってもらうことを想定している。

v0としてブラウザで動くHTML/JSのプロトタイプ（`human_evolution_sim.html`）が既に存在する。これはルールの参照実装（「仕様がわからなくなったら挙動を見て確認する」用）として扱い、以後の実装はGoに移行する。

## シミュレーションのルール

**実装済みのルールの現在の姿は `NODE.md`（ノード）と `ENEMY.md`（外敵・未実装）にある。** 数値は `PARAMETERS.md`。ここには重複して書かず、**壊してはいけない設計上の原則**と、まだ実装していない項目だけを残す。

### 崩してはいけない原則

ルールを足すときに、これらを壊していないか確認する。どれも一度壊して世界が破綻したか、破綻すると分かっているもの。

- **回復経路を必ず残す。** 「満腹かつ無理をしなければ回復する」が無いと、全員が単調に減って餓死するだけの世界になる。
- **優先順位は 生命 > 子孫 だけ。** 時間は目的ではなくコスト。**ただし「候補にすら挙げない」のは 2026-09-05（段階25）にやめた**——腹が減っているのは求愛しない**理由**であって、求愛を不可能にするものではない。今は `addCourt` が出産の代金（`BirthVitalityCost/2` × 受諾確率）とそれで死ぬ確率の上昇を効用に載せ、**餓死しかけた個体は閾値なしで求愛を選ばない**（テストで固定）。**候補から消えるのは身体にできないことだけ**（成人前・代金を払うと死ぬ・クールダウン中）。**閾値は、効用式から抜けていた項の代わりだった**
- **相手の真の能力は隠しパラメータ。** コントローラが見るのは自分の推定値だけ。`Perception` に真値を載せない（型で保証している）。
- **ハードコードされた行動閾値を作らない。** 「体力がこれ以下なら逃げる」のような条件は書かない。逃走も追撃も先制攻撃も、効用比較の結果として現れさせる。再判断のトリガーは「考え直せ」であって「こうしろ」ではない。
- **恨みの状態を持たない。** 攻撃は感情ではなく、食料競合の先制排除という効用式の一手として扱う。過去に払わされた体力は「避ける理由」として効用から引く側にある。
- **攻撃側有利を保つ。** 殴る側のコスト < 殴られる側のコスト。ただしこれと先制排除が噛み合うと世界が過度に暴力的になり個体数が崩壊しうるので、`CompetitionWeight` と `SkirmishTicks` を触ったら必ず個体数を確認する。
- **能力の役割を重ねない。** パワー＝戦闘効率、理性＝世界を読む精度、頭の良さ＝手を選ぶ質。1つの能力が2つの役割を持つと、選択圧がどちらから来ているのか測れなくなる。

### 将来実装（今はやらない）

- ~~**見た目からの相手分類。**~~ 2026-09-03 に実装（段階10）。**2026-09-04 に申し送りを閉じた。** 学習そのものは6倍の価値になった（`learnGain` +3.04 → +18.00 \*\*\*）が、**体つきを読んだぶんは 11% → 0% に消えた**（相関 0.22 → 0.08）。「世界が豊かになれば信号が強くなる」という当時の期待は**逆に外れた**。**理由は `Appearance` の定義が予算制と噛み合っていないこと**——体力と速さは「攻撃力を見せないため」に選ばれたが、予算制ではその2本は**攻撃力と同じ財布を奪い合う**ので、隠すために選んだ変数がまさにそのために予測力を失っていた。**見えるものを「総量」にすれば信号は実在する**（相関 0.50、体つきの利得 1.24 \*\*\*。`LooksShowBulk` で試せる。総量は見せても配分は隠れるので攻撃力は隠しパラメータのまま）が、**推定の精度は上がらず個体数を 8.8 \* 払う**ので既定は変えない。
- **配偶者を巡る同性間の戦い**（戦闘と求愛の接続）。
- ~~**仲間・協力。**~~ **2026-09-06 に保留から出した（段階32）。** 外敵は 2026-09-01 に実装済み（`ENEMY.md` 3節）で、**群れ狩りは動機・獲物のサイズ・栄養価をどれだけ振っても現れず**（`packSize` 1.2〜1.5）、段階11 が名指しした残りの欠けもの（狙いを合わせる手段）の前提だった段階9・段階12 はどちらも通った。**作るのは協力性遺伝子ではなく、行動語彙1つ（「誘う」）と好意の使い道1つ**——受けるかどうかは**既存の効用比較が採点**し（閾値は置かない＝段階25 の原則）、**好意は参加ボーナスではなく「土壇場で離脱しない確からしさ」としてリスク見積もりを割り引く**（既存の `AffinityTrust` を戦闘へ広げるだけ）。**AIにも同じ語彙を持たせる**（AI同士で働いて初めて群れ狩りを測れる）。対になる **段階31＝目撃した殺害の伝播**（人間ならリスク推定、外敵なら好意という符号違いの1本。**重みは `ActObserve` 1回分より粗く**＝観察という選択肢を殺さないため）。**順序は 31 → 32 → 29 → 30 → 21 → 22**（`TODO.md`。決定事項#55・#56）。
- ~~**記憶容量に選択圧をかける経路。**~~ 2026-09-04 に決着し、**探すのをやめた**。段階9・12・15b と3度測って `shMemory` は 0.07〜0.08 で動かず、4度目に**掃引して形を見たら平らではなく右下がり**だった（記憶6人で個体数79.9、12人で77.7、24人で73.8、無制限で54.1）。**記憶がこの世界で主に生産するのは「友人」で、好意は休息を安全に見せるので net で損**——`shMemory` 0.07 はバグではなく**正しい値付け**。**「役割があること」と「得であること」は別**という段階9 の観察に、ここで理由がついた。なお段階15b が書いた診断（地域が少なすぎて容量が縛られない）は**決定事項#41 と矛盾しており**（地域の記憶は容量の枠に入れないので、地域を増やしても容量は縛られない）、実測でも否定された。
- ~~**効用式の中の仮定を個体が経験から学ぶこと。**~~ 2026-09-04 に実装（段階12a。下記「現在の機能」を参照）。**仕組みは全部入ったが、信念の学習は既定でオフ**——世界の真の殴り返し率は 0.15 しかなく、`retaliation = 0.7` という思い込みが世界を支えていたため、真実を知ると個体数が半分になる。段階12b で本物の抑止力が入ってから測り直す。
- ~~**種族の汎用化・複数食料種・死体ドロップ。**~~ 2026-09-01 に実装（下記「現在の機能」を参照）。当初の想定と違ったのは、**共通食料A/Bの2種類は作らなかった**こと（AとBを区別するルールが無い）。以下は当時のメモ: 人間・外敵は同じ汎用ノード構造・同じ意思決定エンジンを共有し、パラメータの範囲（種族ごと）と種族固有遺伝子だけで種族差を表現する。食料は3種（共通食料A/B＋死体からの肉ドロップ）。人間は人間由来の肉を食べない（共食い不可）が外敵は種族を問わず肉を食べる。詳細は実装着手時に `NODE.md` / `ENEMY.md` / `PARAMETERS.md` へ書く。
- ~~**遺伝子ベクトルの拡張と学習パラメータ（文化）。**~~ 2026-09-04 に実装（段階12b・12c）。**双方向の等価交換もヒントも狙いどおり入った**が、**1回あたりの歩み寄りは想定よりずっと小さくないと世界が壊れる**（交換は1実行に19000回起きる）。以下は当時のメモ: 遺伝は `Genome []float64`、学習は別ベクトルとして分離する。文化は専用データ構造を作らず、近接個体間の模倣則によるパラメータのクラスタ化として創発させる。模倣は一方向の学習ではなく**双方向の等価交換**とし、伝わる中身は固定名のスカラーだけでなく**個体ごとに可変な「ヒント」**（状況特徴量 × 対象行動 × 重み。効用への加減点としてのみ働き、行動は直接決めない）も含む。「天才」は学習パラメータのどれか1つを通常の突然変異よりずっと大きく飛躍させる特殊イベントとして実装し、**ヒントの構造の変異にも同じ仕組みを流用する**（新しい概念は作らない）。詳細は `PLAN.md` の段階9・段階12。
- **地域ごとの世界リセット（random immigrants）。** 個体数過多・遺伝的多様性の停滞を地域単位で検知し、適応度に応じた生存選抜と新規ランダム個体の投入を行う。地域は既存の空間分割グリッドを粗くまとめたブロックを流用し、移動の壁にはしない（隣接地域からの自然な再流入を狙う）。トリガー条件・選抜方式は未決（下記「未決事項」）。
- ~~**地形（川・高低差・スロープ・穴・崖）。**~~ 2026-09-06 に実装・測定（段階20）。**既定は平坦のまま**（`TerrainMap` が空）で、地形は世界を始める側が渡すもの。詳細は下記「現在の機能」。**続きを2つ立てた**（`PLAN.md`）: **段階29＝地形コストの認識と伝承**（段階15b/15c の地域ごとのベクトルに1本足すだけ。**減衰は0**＝地形は静的な事実。**粒度は12ブロックのまま**＝今のコントローラは経路計画を持たないので、そこが解像度の天井。決定事項#53）と、**段階30＝高台の遮蔽（回避率）**（**攻撃者との高さの差分**を見る初めての規則。**予防的な行動に要る「高い場所は安全」は `engine/lore.go` 側の世界の事実**で、地域ごとの信念とは別枠。**既定オンだと決めつけない**——段階12a の `retaliation` に近い性質だから。決定事項#54）。**順序は 29 → 30 → 21 → 22**（`TODO.md`）。

## アーキテクチャ（現時点の方針）

- 言語: Go（速度、既存のGo実績、ビルド・テストループの速さを理由に採用）。
- 構成: サーバ・クライアント型のネイティブアプリ。
  - `engine`: シミュレーション本体。状態とtickごとの更新ロジックのみを持ち、描画・通信のことは一切知らない。単体でテストできる形を保つ。
  - `protocol`: クライアント・サーバ間でやり取りするメッセージ型（ワールドのスナップショット、クライアントからのコマンドなど）を定義する共有パッケージ。
  - `server`: `engine` を保持してtickを回し、一定間隔でスナップショットを接続中の全クライアントに配信する。クライアントからのコマンド（パラメータ変更・一時停止など）を受けて `engine` に反映する。
  - `client`: ネイティブGUI（ebiten想定）。サーバに接続し、受信したスナップショットを描画するだけの薄い層。UI操作はコマンドとしてサーバに送る。
- 通信方式: 未確定。第一候補は `net.Conn` + `encoding/gob`（または json）による自前の軽量フレームプロトコルで、外部依存を増やさない。将来Web版が必要になればWebSocket/gRPCへの切替を検討する。

### 意思決定の差し込み口（ゲーム化への布石・早期に敷く）

- 各ノードに「コントローラ」を持たせ、`engine` は毎トリガー `controller.Decide(知覚) → 行動` を呼ぶだけにする。行動は `NODE.md` の「行動の語彙」に統一する。
- 今は全ノードがAIコントローラ（効用式で判断）。将来、1ノードだけを人間コントローラに差し替え、その個体が死んだら指定した子のコントローラをAI→人間に切り替えることで「子に乗り換える」を実現する。AIも人間も同じ行動語彙を出すので、`engine` は誰が操作しているかを知らなくてよい。
- 血統リンク（親→子のIDを記録）も早期に入れておく。将来「自分の子孫に乗り換える」ために必要で、今入れておけば安い。
- この境界（コントローラ／知覚／行動）と血統記録は、UIを作らない今のうちに敷く。後付けは大きな書き換えになるため。

## 開発中の目視確認

- `protocol`/`server`/`client` が揃うのを待たずに、機能を作るたびにその場で目視確認できるよう `cmd/devview` という開発用ツールを用意する。`engine` を直接importし（ネットワークなし）、ebitenでその場に描画するだけの、動作確認専用のツール。
- 本番の `client`（`server` 経由でネットワーク越しに描画する）とは別物。engineの実装が先行している間、挙動を手早く目視確認するための踏み台として使う。`client` が揃った時点で、`cmd/devview` は削除するか開発用モードとして残すかをその時点で判断する。
- 使い方（起動オプションとキー操作の一覧）は `README.md` にある。
- 目視には2つのモードがある。**全体を眺める**（世界が破綻していないか）のと、**1ノードを追う**（なぜその行動を選んだか）。後者は速度を落とすか一時停止して、ノードをクリックし、右のパネルで意思決定トレースを読む。
- 見た目は凝らなくてよい（`human_evolution_sim.html` と似た程度のシンプルな円・色分けで十分）。目的はロジックの目視確認であって、UIの作り込みではない。
- engineに新しい状態や行動を追加したときは、対応するテストに加えて `cmd/devview` で一度動きを目視確認する。
- シミュレーションの内部tick速度と、クライアントへの配信・描画速度は独立させる（サーバは好きな速さで内部tickを回し、配信は一定間隔に間引く）。

## ルール変更の測定（cmd/experiment）

目視で分かるのは「世界が破綻していないか」までで、「このルール変更で能力への選択圧がどちらに動いたか」は見えない。それを測るためのヘッドレスのランナーが `cmd/experiment`。使い方（オプションと出力の読み方）は `README.md` にある。

方針:

- **ルールの切替は `engine` の分岐ではなく `Config` の書き換えで表現する。** 両方の条件が同じバイナリに入っていないと、同じシードで対にして比べられない。ルールを1つ無効化できる形で `Config` に持たせるという既存の方針（2026-08-12の判断、`HISTORY.md`）が、そのままA/Bの前提にもなっている。
- **比較は必ず同一シードの対で行う（paired）。** シード間のばらつきは条件差よりずっと大きく、対にしないと数十シードでも埋もれる。
- **能力の値は最後の1tickではなく末尾2割の平均で読む。** 個体数が数百なので、数体の生死で平均が1回分の選択圧より大きく動く。
- 選択圧の指標は「20000tickでの平均能力の変化量」。副次的に個体数・世代数・戦闘数・死因の内訳（餓死／戦闘）も見る。**片方だけ良くなっていないか**を必ず確認する（例: 知能への選択圧は上がったが世界が暴力的になりすぎて個体数が崩れた、など）。
- 選択圧に触れる変更を入れたときは、この測定を変更の前後で回し、数値を `HISTORY.md` に残す。
- **測定の前に予想を書く。** 選択圧に触れる変更を入れる前に、`cmd/experiment` のどの指標（`dPower`/`dRationality`/`dIntelligence`/`killShare`/`clumping`/`neighbours`/`nearest`/`starved`/`killed`/`extinct` など）が、どちら向きに、誤差より大きく動くはずかを先に書く。「良くなるはず」のような曖昧な予想は測定後にどうとでも解釈できてしまうので、具体的で反証可能な形にする。
- **予想が外れたときほど `HISTORY.md` に残す。** 変更前後の数値だけでなく、何を予想していてどう外れたかを書く。これまでの設計判断ログで一番価値があった記録は、想定が測定で覆ったとき（段階6の深さゲートが逆効果だった件、`killShare` が食料供給と交絡すると思っていたが実際は交絡しなかった件）で、当たった予想より外れた予想のほうが学びが大きい。
- **数値と見た目の両方で確認する。** `cmd/experiment` の数値が動いても、それが狙った挙動によるものかは `cmd/devview` での目視まで分からないことがある。予想には数値の動きに加えて「見た目としてどう変わるはずか」も書き、両方を確認してから変更を確定させる。

## 実装ロードマップ（段階）

各段階は小さく、`cmd/devview` で新しい挙動が必ず目視できる粒度にする。上から順に進める。

**段階0〜6は 2026-08-12 に完了**（`engine` に実装、`cmd/devview` で目視確認済み）。**段階6.5（計測の先行整備）は 2026-09-01 に完了**（5指標＝クラスタ・membership half-life・戦闘の相手・集団間距離・種族ごとの個体数。「集団が無い世界」のベースラインは `HISTORY.md` の同日の項）。**段階7a（空間グリッド）と段階7b（継承則の入れ替え）は 2026-09-01 に完了**（7a＝近傍探索をインデックス経由に、挙動不変。7b＝粒子的遺伝とまれで大きい変異。**測定のベースラインはここで一度リセットされている**——数値は `HISTORY.md`）。**段階11（種族の汎用化と外敵）は 2026-09-01 に実装、2026-09-03 に完了**（`PLAN.md` の順を飛ばした。**群れ狩りは現れなかった**——対照条件つきで確認。`HISTORY.md` 追記23。残っていた「共同戦闘を好意の加点源にする」と「外敵のサイズ分布」は 2026-09-03 に決着。`HISTORY.md` 同日の項）。**段階7c（遺伝子ベクトル化と予算制）は 2026-09-01 に実装完了**（9本の遺伝子が1つの予算を分け合う。**完了条件のうち「大きくて遅い／小さくて速い の並存」だけは段階8 のあとでないと判定できない**）。**段階7d（成長・老い・育児期間）は 2026-09-02 に完了**（`TicksPerYear = 500` を本決め、`MaxLifespan` を 3000 に較正。死因は戦闘62%・餓死34%・消耗4%）。**段階7c の残っていた決定（予算の継承幅・初期配分の α・天才の発生率・魅力の体調重み・予算の価格）は 2026-09-02 に数値をつけて完了**（`HISTORY.md` 同日の項と追記27。**天才2段階の発生率の確定だけは `TicksPerYear` と対なので段階7d へ移送**）。**段階8（チャネル化）は 2026-09-01 に完了**（`killShare` 0.92 → 0.79、個体数 63 → 111、選択圧がパワーから頭の良さへ移った）。**段階9（記憶の容量・帯域・忘却曲線と好意）は 2026-09-02 に完了**（記憶が有限になり、記憶に初めての正の項が入った。選択圧がさらに頭の良さへ寄った。**記憶容量への選択圧は出ない**——段階12 で再測定する）。**段階10（見た目からの学習）は 2026-09-03 に完了**（初対面の推定誤差が固定値より 3.0 小さい。ただし利得の9割は「世界の平均を学んだ」ぶんで、体つきを読んだぶんは1割）。**段階12a（効用式の仮定を個体ごとの値にする）は 2026-09-04 に完了**（好みは可変で既定オン、信念の学習は動くが既定オフ。理由は下記）。**段階12b（伝承）・12c（ヒント）も同日に完了**（等価交換とヒントの模倣。**あるべき状態 B〜E のうち B は達成、`halfLife` はほぼ、`gapRel` は未達**）。**段階13（視界のグリッド化）・地形の下ごしらえ・段階14（休憩場所）も 2026-09-04 に完了**。**段階15・16・17・18 も 2026-09-04 に完了（17 は実装・測定のうえ既定オフ）。`PLAN.md` の段階は全部通した**——残っているのは `TODO.md` の宿題（再判断トリガーの性能）と、保留中の項目（`protocol`/`server`/`client`）だけ。**2026-09-08 に段階38b の1つ目（採取）も完了した**（**設計どおりに動き、ほとんど何も買わない**——学ばれ広がり天井も効く〈名目 0.55／実効 0.18／模倣なし 0.19〉のに `dietDiscount` は +0.00 \*。**打ち消す相手＝飽きの割引がそもそも5%**だから。**#64 が心配した独占は起きず**、**記憶は5度目も選択圧に応えなかった**）。**2026-09-08 に段階38a（最初のスキル＝荒れ地耐性）を完了した**（**既定はオフ**＝地図の作者が入れる。**知識は社会的に育つが居場所は変わらない**——名目習熟度は 0.80／模倣なし 0.54／跳躍もなし 0.50〈＝出生地の値そのもの、ばらつきゼロ〉で**跳躍が作り模倣が配る**が、`onDear` は +0.01 \* しか動かず**超過コストを全部消しても不動**。**段階33 の宿題「速さと体力の並存への別経路」への答えは「スキルでもなかった」**）。**同日に 10b（ヒントの既定）も閉じた**（消しても個体数は増えず `killShare` が +0.03 \*\*\* 上がるので**既定は今のまま**）。**2026-09-08 に段階32（協力の誘い）を完了した**（**群れ狩りは現れず、段階11 の診断が否定された**——狙いを合わせる手段を与えても `packSize` は 1.34 → 1.36 で、語彙なしの対照でも同じだけ出る。**代わりに個体数 +12.83 \*\***、そして**社会的な規則で予算比が動いた初めての例**〈`shSpeed` +0.01 \*\*〉。**保留#10 はこれで閉じた**）。**2026-09-08 に段階31（目撃した殺害が伝わる）を完了した**（**規則は絶えず発火し、世界の数字を1つも動かさない**——1件の殺害が 24.3 回の観測機会を作るのに取り込まれるのは 0.21 で、**上限は重みではなく記憶の枠**。**#55 が心配した「観察が死ぬ」も起きない**）。**2026-09-08 に段階37（川が世界を2つに分けるか）を完了した**（P6 の最後。**実装ではなく観測**——規則は1つも足さず、読み取り専用の測定を `engine/banks.go` に足しただけ。**川は視界を分けるが集団は分けない**: 乾いた地面の者どうしの遭遇は 0.17 → 0.01 に落ちるのに、両岸の予算比も地域信念もずれない。理由は**水の上に個体数の14%が立っていて両岸を同時に見ていること**と、**一生のうち両岸に立つ個体が半分いること**）。**2026-09-07 に段階34（溺死）・35（溺死の周知）・36（水辺の豊かさ）を完了した**（P6。**34＝規則は効くが効き目は全部「選別」**、**35＝知識は正確に育ち伝わるが、水から出る理由にはならない**、**36＝`onWater` を動かすのは食料の分布だけ**——3点（痩せた水辺 0.11／一様 0.14／豊かな水辺 0.15）が一直線に並ぶ。詳細は下記「現在の機能」）。**2026-09-06 に段階20（地形本体）・段階29（地形コストの認識と伝承）・段階33（食料を地形に結びつける）・段階30a（高台の遮蔽）を完了した**（30b は先に決めた2つのどちらかを取り消さないと動かないので宿題）（実装・測定。既定は平坦のまま＝地形は世界を始める側が渡すもの。**狙っていた速さのニッチは出ず、逆に速さが買われなくなった**——下記「現在の機能」）。**2026-09-05 に段階19〜22（ゲーム化とマップエディタ）を足し、同日 段階19 と段階23 を完了した**: 19＝人間操作コントローラと血統乗り換え【完了】、20＝地形本体（旧「地形全般」）、21＝世界・ノードの保存/読込、22＝マップエディタv0 とパラメータ調整UI、**23＝選択を要求されるゲーム【完了】**（遊び方そのもの。19 と同じく世界のルールを変えないので 20 の前に通した）、**24＝外すと隙ができる【完了】**（戦闘の規則。ゲーム化とは独立）、**25＝求愛の閾値を効用式に移した【完了】、26＝待っても下げない魅力の床【完了】、27＝直接操作と操作性【完了】、28＝求愛の待ち時間が数えるもの【完了】**。**順序は 19 → 20 → 21 → 22 で確定**（`TODO.md` の P5 に理由）。`protocol`/`server`/`client` の着手時期は未定のままだが、**スナップショット形式は段階21 の実地の上に設計する**（`PLAN.md` 決定事項#48）。

- 段階0：意思決定の差し込み口（コントローラ／知覚／行動の境界）と血統記録を先に敷く。この上に以降を載せる。【完了】
- 段階1：状態モデルを 体力＋空腹度＋食料 に置換。餓死と回復が目視できること。【完了】
- 段階2：移動の体力コストと effort。動き回ると減り、満腹で休むと回復するのが目視できること。【完了】
- 段階3：継続戦闘と逃走。食料を巡る戦い、劣勢側の逃走、攻撃側有利が目視できること。【完了】
- 段階4：強さ推定と記憶（(A)リスク記憶＋減衰、(B)観戦ベースの強さ推定）。ノードをクリックすると他者への推定が見え、対戦・観戦で収束するのを確認。【完了】
- 段階5：リスク判断と先制排除。強い個体を避ける、競合圧に応じた先制攻撃、もつれが目視できること。【完了】
- 段階6：頭の良さによる戦略選択。賢い血統と愚かな血統で生存率を比較できること。【完了。ただし下記の注意を参照】
- 段階6.5：計測の先行整備（あるべき状態を測れるようにする）。【完了。2026-09-01】
- 段階7a：空間グリッド（挙動を変えない高速化）。【完了。2026-09-01。**既定の個体数では数%の持ち出しで、効き始めるのは500〜600体から**】
- 段階7b：継承則の入れ替え（粒子的遺伝＋まれで大きい変異）。【完了。2026-09-01。**能力の分散が約2倍になり、ここで測定のベースラインがリセットされた**】
- 段階7c：遺伝子ベクトル化と予算制。【完了。実装は 2026-09-01、残っていた6つの決定に数値をつけたのが 2026-09-02。**能力は9本の遺伝子が1つの予算を分け合う。指標は絶対値ではなく予算比で読む。** 予算はもう暴走しない（60000tickで442、最大900に対して）。**初期配分のディリクレ α は「初期条件」ではなくその実行の多様性そのもの**だと分かった】
- 段階8：攻撃・防御・回避のチャネル化。【完了。2026-09-01。**「一番危険」という当初の警告は逆だった**——危険だったのは入れないことのほうで、個体数は 63 → 111 に増えた】
- 段階7d：成長・老い・寿命と育児期間。【完了。2026-09-02。**実効値＝遺伝的素質 × 年齢補正（1本の曲線・掛け算は1か所）。** 子ども期は個体数の4割を払う——死亡率ではなく**世代時間**に乗るコスト。育児が効くのは保護ではなく**凝集**だった】
- 段階9：記憶の容量・帯域・忘却曲線と、好意。【完了。2026-09-02。**好意が入ったので段階12（伝承）の前提が揃った。** 休息の露出は重み 0.05——**露出は `RegenRate` の12倍の単位で入るので、大きくすると「視界に1人いれば休息の価値ゼロ」という崖になる**】
- 段階10：見た目からの学習。【完了。2026-09-03。**初対面の推定は各個体が自分で引いた直線から出る。** 利得は同じ遭遇で測って +3.0（誤差 33.4 → 30.3）だが、**そのうち体つきを読んだぶんは +0.34 だけ**——残りは「世界の平均を知ったこと」。**個体数は35%落ちる。原因は学習ではなく `PriorStrength` の水準**】
- 段階12a：効用式の仮定を個体ごとの値にする。【完了。2026-09-04。**事実（学べる）と好み（学べない）に分けた。** `Retaliation = 0.7` は世界の事実ではなく**実在しない報復への恐れという抑止力**で、真の値 0.15 を知ると個体数が半分になる——しかも増えるのは殺しではなく**餓死**。よって `LearningRate` の既定は 0。好みの側（広がり 0.15）は既定オンで、これだけで**理性への選択圧が負から0へ戻る**】
- 段階12b：伝承（等価交換）。【完了。2026-09-04。**`observe` に相乗りする双方向・同時の歩み寄り。** 好意が効用の正の項になり、他個体が生きていることが得になる2本目の経路ができた。**1回の歩み寄りは差の2%**——「中間で会う」0.5 は段階12a が作った多様性を消して個体数を半減させる。値段は個体数（40000tickで −12.7 \*）で、買えたのは好意のある相手の数（+0.90 \*\*）と集団の持続】
- 段階12c：ヒント（進化し続ける言い伝え）。【完了。2026-09-04。**状況×行動×重みの3つ組を上限4本。効用への加減点としてのみ働き、分岐はどこにもない。** 枠は空でも予算を食う。**手口の多様性を作っているのは重みではなく模倣**——重みを0にしても種類数は変わらず、模倣を切ると伝承なしの水準まで落ちる。**2026-09-05 に再測定して2つ訂正した**: 「頭の良さへの選択圧は下がる」は**予算が下がっていただけ**（予算比では1標準誤差）、「重みだけ0の対照に勝つ」（22.0 対 14.8）は**穴の開いた世界の値で、直った世界では誤差内**（73.3 対 73.5）】
- 段階13：視界をグリッド単位にする。【完了。2026-09-04。**視界は「自セル＋周囲1セル」のブロック**（面積は円と同じになるよう較正）。**「近い」ことが「見える」ことを意味しなくなった**のが本体で、殺しが減る（`killRate` −0.13 \*）。**逃走成功率は動かない**——面積を合わせると逃げ切る距離の平均まで揃い、異方性は分散しか増やさない。**視力は遺伝子にしない**（0リングは致死・2リングはやや悪い＝離散で非線形なので `StrategyDepthUnlock` と同じ轍になる）】
- 段階18：夜型・朝型（体内時計）。【完了。2026-09-04。**時刻で変わるのは休息の回復量だけ。体内時計は予算遺伝子ではない**（量ではなく円周上の位相）。**「眠っている友人は見張れない」という条件1つ**が「たまたま寝る時刻が違う」を「誰かが見張っている」に変える——**見張り役はルールに存在しない**。**これまでで一番世界を良くした**（個体数 +12.6 \*）。**完了条件の指標は最初から飽和していた**】
- 段階17b：毒と警告信号。【実装・測定完了、**既定はオフ**。2026-09-04。**個体数が9分の1**。**`shRationality` が段階9 以来はじめて上がる（0.07 → 0.11 \*\*\*）が、信号を読めなくしても同じだけ上がる**＝上げているのは信号ではなく世界の厳しさ。**正直な警告は進化しない**（毒にも信号にもコストが無いので足場が無い）】
- 段階17a・17c：植物の遺伝と種子散布。【実装・測定完了、**既定はオフ**。2026-09-04。**動かないからではなく世界を壊すから**（個体数 44.2 → 14.9）。**短い散布は地域を空にし（吸収状態）、長い散布は地理を学べなくする**という逃げ道の無い2つの失敗で、パラメータでは解けない。**段階15 の固定版は土台だった。** 17b（毒と警告信号）は別途】
- 段階16：食料の栄養バランス。【完了。2026-09-04。**新しい状態量を作らず、隠さない。** 効果は予想どおり小さい（個体数・`killShare` は誤差内）が**ゼロではなく、群れ狩りを初めて有意に増やした**（+4.7 \*\*）——**一般則（同じものばかりは損）が、専用の仕掛け（肉に独自の栄養）の弱い版として働いた**。**強い罰は逆効果**】
- 段階15：植物食料の地域差と、地域の豊かさの認識・伝承。【完了。2026-09-04。**15a＝食料の総量は変えず分布だけ変える**（`rareTrough` があるべき状態A の帯に復帰、段階14 で起きなかった凝集が起きる）。**15b＝自分が立った地面だけを覚える**。**15c＝行ったことのない場所は聞いて知る**——**集団を1か所に集めたのはこれ**。**`shMemory` への正の選択圧は出なかった**（地域12個では容量が制約にならない、と `PLAN.md` が予告していたとおり）。**性能: `Step` が2.4倍**（原因は既存の再判断トリガーで、`TODO.md` に独立項目）】
- 段階19：人間操作コントローラと血統乗り換え。【完了。2026-09-05。**段階0 で敷いた差し込み口を使うだけで、engine のルールは1つも変わらない**（指紋テストが無変更で通る）。人間は `Controller` のもう1つの実装（`engine/human.go`）で、**命令は「次に訊かれたときの答え」**。**約束は2つ——見せるのは `Perception` の中身だけ、狙えるのは視界の中だけ。** 実際に作ったのは3つ目の規則で、**命令が流れる場合が2つある**（狙いが消えた／身体が変わった）——**どちらも「AIは訊かれれば採点し直すが、据え置きの命令は自力で立ち直れない」という非対称から出た**。血統の乗り換えは同じコントローラを成人した子に付け替えるだけ（`World.Heirs`）】
- 段階23：選択を要求されるゲーム（転機の一手と、節目の性格）。【完了。2026-09-05。**「時々選択を要求される」は新しい仕組みではなく、既存のトリガー機構の目盛りの取り方だった。** 設計の前に頻度を測ったのが要点——1体が訊かれるのは **14.5 tick に1回**（うち53%が「殴られた」）で `-slow` では0.6秒に1回。**トリガーを5つに絞り戦闘を1戦＝1回にまとめると 112 tick に1回＝一生で20〜30回**になり、それがそのままゲームの形になる。**AIは代役ではなく常に動かしている側**（だから誰も答えない誘導ノードは純粋なAIノードと1ビットも違わない）。**メニューは書かず、その個体が実際に採点した候補をそのまま出す**。**順位にはその個体自身の評価ノイズを乗せる**（ノイズ抜きは、その身体の頭の良さ遺伝子が買っていない賢さを渡すことになる）。**答えは1手であって据え置き命令ではない**ので、段階19 が要した「命令が流れる場合」が要らなくなった。節目に人が動かすのは**好みの配分だけで合計は継いだもの**——段階12a が「好みには正解が世界に無い」と切っていたので、人に渡しても何も上書きしていない。**`Config` は1つも増えていない**】
- 段階22：マップエディタv0 とパラメータ調整UI。【完了。2026-09-06。**`cmd/devview` の1つのモード**（`F3`）——別プログラムにしなかったのは、描画もカメラも世界も全部ここにあるから。**engine 側は `engine/edit.go` の4つの操作だけ**で、どれも `Endow` と同じ「シミュレーションからは呼ばれない」立場。**地形と地域は別の地図**（セル単位のコスト／ブロック単位の恵み）で、片方を塗っても他方は動かない。**食料は取り分**なので増やせない。**ルールは6つだけ出し、測定済み範囲を外れたら警告する**（止めない）。**難易度は予算だけ**。**エディタ専用の保存形式は作っていない**（段階21 のまま）】
- 段階21：世界・ノードの保存/読込。【完了。2026-09-06。**3つの要件（他所で育った個体群・進めた世界の配布・天才の配置）は同じ1つの機能**（#48）。**乱数は「種と引いた回数」で保存し、読込時に引き直す**——生成器を差し替えると記録済みの測定値が全部再現できなくなるため。**受け入れ条件は指紋テストと同じ形**（保存→読込→500tick が保存しなかった世界と1ビットも違わない）。**個体群が世界をまたげるのは「その身体が何であるか」だけ**で、他人の名前を含むものは置いていく】
- 段階14：休憩場所。【完了。2026-09-04。**世界に地域が入った**（1種類だけ。段階15 以降も同じ地域に乗る）。**新しい仕組みは作らず、`RestExposureWeight` への掛け算が1つ**。**「良い場所がある」だけでは弱い**（+2%）——個体は今いる場所しか感じられないので、働くのは計画ではなく選別。**凝集は起きなかった**】
- 地形の下ごしらえは 2026-09-04 に完了（上記「現在の機能」）。**残るのは段階15〜18（環境と食料生態系の残り）**：`PLAN.md` にチェックリストとして分解済み。

段階6の注意：`Intelligence` は「戦略の幅（`StrategyDepthUnlock` による深さゲート）」と「選択品質（`ChoiceNoise`）」の2経路で効くように実装したが、**2026-08-14 のA/B測定で、深さゲートの側は機能していないことが分かったため既定で無効にした**（測定値と判断の根拠は `HISTORY.md`）。現在の頭の良さは評価ノイズだけを通して効く。理性はシードによってばらつく（下記「未決事項」を参照）。

## 未決事項

- 単一インスタンス運用（1人1サーバ+1クライアント）か、複数人が同じサーバに同時接続して同じワールドを見る共有型にするか。まずは前者で作りつつ、サーバ側は最初から複数クライアントへの配信を前提にした作りにしておく。
- 通信プロトコルの詳細（フレーミング方式、コマンド体系、後方互換の持たせ方）。
- 複数クライアント接続時、誰がパラメータを操作できるかのルール（今のところ未検討）。
- バランス調整用の数値（体力/空腹度のレート、effort の逓減カーブ、リスク記憶の減衰率、競合圧の代理指標の重み、攻撃側有利の非対称量など）。素朴に作ってから `cmd/devview` とベンチで詰める。一通りの値は入れたが、下記の3点は特に暫定。
  - 理性（`Rationality`）の選択圧の弱さは、**原因の大半が効用式ではなく継承則だった**（2026-09-01）。粒子的遺伝に変えただけで +2.55 → +10.54（24シード × 50000tick）。`PatienceRationality` が効きすぎている疑いは残るが、**優先度は下がった**。パワー（+20.57）・知能（+29.96）に比べればまだ小さいので、詰めるとしても段階7c（予算制）で能力が上限に張り付かなくなってから。
  - 世界の暴力度は `CompetitionWeight` と `SkirmishTicks` に非常に敏感。設計メモの警告どおり、上げると個体数が崩れる方向に振れる。
  - 個体数は食料（`FoodSpawnRate`）で頭打ちになるよう調整してある。`MaxPopulation` は安全網であって、通常は張り付かない想定。
- 効用式の再計算コスト。まず素朴に全候補評価で作り、ベンチで重くなってからトリガーの間引きや候補の枝刈りで最適化する（方針(b)）。現状は1決定あたり約8.0µs（`BenchmarkDecide`。深さゲートを既定オフにして全候補を採点するようになったぶん、5.8µsから増えている）。
- 地域リセットのトリガー条件（局所的な過密／遺伝的停滞のどちらか、または両方か）と、リセット時の生存個体の選び方（適応度順か、地理的多様性も守るハイブリッドか）。
- プレイヤーの出生地選択UI（ワールド概観からの地域選択）の具体的な見せ方。
- 種族の汎用化・遺伝子ベクトル拡張・地域リセット（上の「将来実装」段階7〜9）を、`protocol`/`server`/`client` より先にengine側でさらに進めるか、先に`protocol`/`server`/`client`を通すか。順序は未確定。 **2026-09-05 に一部決着**: 先に段階19〜22（ゲーム化とマップエディタ）を通し、`protocol` のスナップショット形式は段階21（保存/読込）を実際に使った上で設計する（決定事項#48）。残る未決は `protocol`/`server`/`client` そのものの着手時期。

## 設計判断ログ → `HISTORY.md`

「なぜそう決めたか」の記録は **`HISTORY.md` の後半（設計判断ログ）** にある。2026-08-11 以降の全項目をそこへ移した（2026-08-16）。

この文書には**今も有効な結論だけ**を書き、経緯は書かない。新しい設計判断をしたら、結論をこの文書の該当節に反映し、理由と測定値は `HISTORY.md` の末尾に日付つきで追記する。

## コーディング規約

- `go build ./... && go vet ./... && go test -race ./...` が常に通ること。
- 標準ライブラリで完結できるところは外部依存を増やさない（過去のGoプロジェクトと同じ方針）。
- **シミュレーションの定数は package const ではなく `engine.Config` のフィールドとして持つ。** テストでルールを1つずつ無効化して（`MutationStd = 0` など）他のルールから切り離して検証するため、そしてA/Bで条件を差し替えるため。`Config` が実質的な仕様の一覧にもなる。
- **乱数は `World` が持つ `*rand.Rand` 1本に統一する。** 同じ `Config`（同じ `Seed`）なら常に同じ結果になること。確率が絡むルールのテストは「決定的にできるものは条件を作って決定的に検証」「ゆらぎ自体が本質のものは固定シードでN回試行して割合を比較」の2本立てで書く。
- **空間インデックス（`engine/grid.go`）への問い合わせを、位置を動かすループの中に置かない。** インデックスは移動のたびに「古い」印がつくので、動かしながら訊くと1体ごとに作り直しが走り、置き換えたはずの全走査より遅くなる。訊くのは動かす前か、全員動かしたあと。
- 新しいシミュレーションルールを追加したときは、対応するテストと、`NODE.md`（または `ENEMY.md`）の更新をセットで行う。パラメータが増えたら `PARAMETERS.md` にも行を足す。
- コードはすべて英語で書く。変数名・関数名・型名・コメント・ログメッセージ・エラーメッセージ・コミットメッセージを含む。日本語が混ざらないようにする（CLAUDE.mdや設計ドキュメントなど、コード外のプロジェクト文書は日本語のままでよい）。

## コミットの方針

- **`main` に直接コミットする。ブランチは切らない。** 履歴は一本で保つ。理由は2つ。作業者が1人であること、そして**コミット番号を成果物に焼き込むことがある**こと（`cmd/snapshot` の画像は下帯にコミットを持つ）。ブランチを経由すると rebase / squash でハッシュが変わり、焼き込んだ番号が嘘になる。
- **コミットメッセージは英語で書く**（コーディング規約と同じ。コード外の設計文書だけが日本語）。
- **1つの機能・1つの測定 ＝ 1コミット。** 実装・テスト・文書の更新は同じコミットに入れる（`NODE.md` / `PARAMETERS.md` / `HISTORY.md` の追記まで含めて1つ）。
- **記録用の成果物（画像など）は、それを生成したコードとは別のコミットにする。** 順序は「コードを先にコミット → そのハッシュで成果物を生成 → 成果物を次のコミットに入れる」。こうすると成果物が指すコミットに、それを再生成するコードが必ず含まれている。
- **本文には「何を変えたか」だけでなく「なぜその形にしたか」の要点を書く。** 詳細と測定値は `HISTORY.md` にあるので、ここは要点だけでよい。特に**採らなかった素直な選択肢とその理由**は残す価値が高い（例: 連結距離に `PerceptionRadius` を使わなかった理由）。
- コミットするのは**ユーザから指示があったとき**。指示なく勝手にコミットしない。

## パフォーマンス計測（ベンチマーク）

- ホットパス（`engine` のtickループ、近傍探索、大量ノードを走査する処理など）に手を入れたときは、変更の前後で軽量なベンチマークを取り、大きな悪化がないか確認する。
- 回し方は `README.md` にある。Goのベンチマークはデフォルトで1ベンチマークあたり約1秒しか回さないので、変更のたびに実行しても十分速く終わる。
- **計測をテストの形で回すときは `-count=1` を付ける。** `go test` は同じバイナリ・同じ引数の結果をキャッシュするので、付けないと「2回測った」つもりの数値が1回目の使い回しになる（2026-09-01 に実際にやらかした。`HISTORY.md` 追記19）。
- 最初のベンチマークは「固定シード・固定エージェント数（当面は数百程度を想定）で `Step()` を繰り返し呼ぶだけ」のシンプルなもの（例: `BenchmarkEngineStep`）から始める。凝った計測は必要になってから足す。戦闘や食料の取り合いが多発する混雑シナリオもベンチに含めると、トリガー方式のバースト的コストを拾える。
- **世界を進めるベンチマークは、一定tickごとに世界を作り直す**（`engine/bench_test.go` の `benchSteps`。作り直しの間はタイマーを止める）。**1つの世界を `b.N` 回進める素朴な形は一定の仕事量を測っていない**——測っている間も世界が進むので、長く回すほど別の世界を測ることになり、同じコードで 82µs〜758µs までばらつく（2026-09-04 に実際に踏んだ。`HISTORY.md` 追記33）。
- **プロファイルは上位関数で止めず、`-peek` で呼び出し元まで降りる。** 「遅い関数」ではなく「**結果を使わない呼び出し**」が本体だったことがある（同上）。
- 毎回数値を記録する必要はないが、明らかな悪化（体感の目安としてns/opが1桁%以上悪化するようなレベル）に気づいたら、コミットメッセージか `HISTORY.md` に一言残す。
- 全PR・全コミットで強制するものではなく、「パフォーマンスに影響がありそうな変更のときだけ」実施する軽い習慣として扱う。

## 新機能を追加するときの流れ

1. `engine` にロジックを追加し、ユニットテストを書く。ホットパスに触れる変更なら、上記のベンチマークも変更前後で実行する。見た目・挙動に関わる変更は `cmd/devview` で目視確認する。
2. 必要なら `protocol` にメッセージ型の変更を加える（後方互換を意識する）。
3. `server` / `client` に反映する。
4. このファイルの「現在の機能」に1〜数行追記する。ルールの追加・変更なら `NODE.md` / `ENEMY.md` を、パラメータが増減したなら `PARAMETERS.md` を更新する。操作が変わったら `README.md` を上書きする。
5. `HISTORY.md` の「変更の記録」に、何がどう変わったかを1項目足す（末尾に追記）。選択圧に触れる変更なら `cmd/experiment` を前後で回し、その数値もここに残す。
6. アーキテクチャや通信方式など大きな設計判断があれば、**結論**をこのファイルの該当節に反映し、**理由と経緯**を `HISTORY.md` の「設計判断ログ」に日付つきで残す（末尾に追記、過去の行は書き換えない）。

## 現在の機能

Goモジュール名は `github.com/toku463ne/cld_human_behaviour`。現時点で存在するのは `engine` と `cmd/devview` のみで、**`protocol` / `server` / `client` はまだ未着手**（未決事項も参照）。実装ロードマップは段階0〜6・6.5・7a〜7d・8・9・10・11 まで完了している（12・13 と、2026-09-03 に足した14〜18 が残り）。

- `engine`: シミュレーション本体。描画・通信を一切知らない。`NewWorld(Config)` で生成し、`Step()` で1tick進める。読み出しは `Agents()` / `Foods()` / `Stats()` / `Tick()` / `AgentByID()` / `Opinions(id)`。
- `engine.Config`: 全パラメータのフィールド化。テストはここを1つずつ潰して各ルールを分離して検証する（例: `MutationStd = 0` で突然変異を止める、`RiskDecayPerTick = 0` で忘却を止める）。
- 種族: human と enemy の2種族。**同じノード構造・同じ意思決定エンジン**で、違いは予算の抽選範囲と食性だけ（enemy のほうが大きく、肉しか食べない）。求愛は同種内のみ。異種族は効用式に「倒せば肉になる相手」として載る（`Perception` の `AgentView.Prey` / `Meat`）。外敵は繁殖もするが、この広さでは捕食者だけで安定しないので地図の外から一定間隔で入ってくる（`EnemySpawnTicks`）。
- 食料は2種類: 植物（世界に湧く。人間だけが食べる）と肉（死体。**自分の種族の肉は食べない**、倒した者が一定時間独占、放置すると腐る）。**肉の量は死んだ個体の予算に比例する**＝大きい相手ほど割に合う、というのが集団で狩る唯一の動機（**外敵のサイズを 400〜700 に振っても参加人数は動かない**ので、群れ狩りが現れない理由はサイズ分布ではない）。**分配に共に与った者どうしには好意が入る**（`AffinityHunt`）。植物と肉は**別枠の上限**を持つ（同じ枠だと肉が枠を埋めて植物が湧かなくなる）。
- 状態モデル: 体力（`Vitality`）・空腹度（`Hunger`）・**成熟度（`Maturity`）**・世界に湧く食料。食べる→空腹度↓、満腹かつ無理をしなければ体力↑、動く/戦うと体力↓、空腹が進むと体力↓。死因は餓死・戦闘・消耗（寿命）の3つで、既定でおよそ 34% / 62% / 4%。
- 成長と老い（`Agent.AgeFactor`）: **遺伝子の実効値 ＝ 遺伝的素質 × 年齢補正**で、掛け算は `Agent.Ability` の1か所だけ（`Gene()` は遺伝的素質のまま＝繁殖と測定はこちらを読む）。曲線は1本で、**若い端が成長**（`Maturity` が食事で 0→1、`ChildhoodYears` = 2年）、**老いた端が衰え**（`SenescenceYears` = 12年から `SenescenceFloor` まで）。**成人（`ReproMaturity`）まで求愛は候補に挙がらない**——これが子ども期のコストの本体で、死亡率ではなく世代時間に乗る。
- 育児期間: 出産後 `ChildRearingTicks`（1000＝2年）、子は親から `RearingRadius`（130＝**視界と同じ**）以上離れると戻る。**既定では給餌なし・親側の負担なし・親の効用に子は入らない**（`ParentFeedShare = 0`。2026-09-06 に給餌を実装して測ったが既定にはしなかった＝下記）。 半径を短くすると子を食料から引き離して殺す。実測された効き方は**保護ではなく凝集**（子の生存率は動かず、死亡率と個体間距離が動く）。**紐の終わりは時計であって「独り立ち」ではない**（`RearingUntilGrown = false`）——2026-09-06 に「成熟度が1になるまで親についていく」版を実装して測ったが**既定にはしなかった**。**伸びるのは中央値ではなく裾**（1000 → 中央値1180・p90 2000〜2800tick）で、**長く縛られるのは飢えている子だけ**＝成長は食事で買うので、紐が妨げるものと紐を解くものが同じ1つの資源。結果は `grewUp` −0.03 \*\*・`starveRate` +0.29 \*・`killRate` −0.35 \*\*\* で、**凝集は買えない**（`clumping` −0.03、誤差内。動いたのは密度依存の指標だけ＝個体数が減ったぶん）。**固定2500tickの対照（`longrearing`）は同じ向きにもっと強く出る**ので、効いているのは「規則の種類」ではなく「長さ」。**出産のバグを直した世界で測り直すと代価はさらに大きい**（個体数 −10.62 \*、出生 −41.08 \*\*\*。買えるのは `clumping` +0.08 \* だけ）。
- 給餌（`engine/provision.go`、`ParentFeedShare`）— **既定でオフ**: 親の一口の一部が、そばにいる育児中の子に回る。**世界で初めて「ある個体が別の個体のために払う」規則**だが、**新しい行動も新しい状態も判断も作らない**（`eat` の分配が変わるだけで、誰も「食べさせる」ことを選ばない）。**分けるのは一口であって食料そのものではない**（所有・競走・奪い合いは不変）。**親は知る**（`Perception` の栄養価に載る＝`ParentFeedKnown`）。**毒も分け前の割合で子が受ける。** **測定は2度した**——**出産のバグ（下記）を直す前は「個体数 −4.67 \* を払って `grewUp` +0.03 \*\*\* を買う取引」だったが、直した世界では費用が消えた**: 0.5 で個体数 +5.08（誤差内）・`gen` +0.48 \*\*・`grewUp` +0.04 \*\*\*・子の死亡割合 −0.02 \*\*\*・`starveRate` −0.04 \*（48シード）。**費用だけの対照（`ParentFeedWasted`）は今も −7.54 \*** ＝**親への課税は実在し、給餌がそれを上回って返している**。**既定は0のままだが、上げる根拠は測定の側にある。** **親の効用式には今も子の項が無い**——親は給餌を望むことも拒むこともできず、うまくやることで選択もされない。選択が見ているのは血統だけ。
- 慢性的な低体力（#33）: 体力が `FrailVitalityShare` を下回る状態が続くと寿命が減る。**`ShockRisk`（効用式に載る今の危険）とは別物**で、`Perception` にも効用式にも出てこない。
- 寿命消費（背景処理・`World.spendLifespan`）: `Agent.Lifespan` が空腹度過多（`StarveHunger` 超）または過度な満腹（`OverfedHunger` 未満＝「腹八分目」超）の間だけ毎tick減り、0で死ぬ（`Stats().AgingDeaths`）。完全にバックグラウンドの機械的処理で、`Perception` に載らず効用式にもゴール項が無い。ノードは自分の寿命も、死因としての老化も知らない。選択圧は世代を超えた差次的生存だけを通じてかかる。既定値は 2026-09-02 に較正した（`MaxLifespan = 3000`。6000では標準実行長で一度も発火せず、1500では世界が壊れる）。
- 出産（`stepPaired` → `tryBirth`）: **走り終えたペアは必ず1人産む。** 2026-09-06 まで、出産は「番号の小さいほうが実行する」というID条件に掛かっていて、**両者が同じ時計を減らすので先に0に達したのが番号の大きいほうだと、そのまま解散して何も産まれなかった**——**実測で完走したペアの約半分**。直すと**個体数 51.75 → 87.96（+36.21 \*\*\*）、出生 113 → 244（+130 \*\*\*）、世代 7.17 → 9.00（+1.83 \*\*\*）**。世界は混み合い荒れる（`killShare` +0.09 \*\*\*、`neighbours` +3.55 \*\*\*、`grewUp` −0.07 \*\*\*、`childShare` 0.25 → 0.42）が、**頭の良さへの選択圧が大きく上がる**（`dIntelligence` +15.93 \*\*\*）。**`BirthNeedsLowerIDFirst = true` で以前の世界に戻せる——2026-09-06 より前の測定値はすべてその世界のもので、個体数・出生に触れる結論は読み直す必要がある。**
- 求愛への答え（`engine/courtship.go`、2026-09-06）: **求愛は2人でするのに、訊かれるのは片方だけだった**——求愛者が歩いていくのは行動（人も選べる）だが、**着いた瞬間の答えは世界の規則**（`willCommit`）で、求愛された側は事後に知るだけだった。**答えをコントローラに出せるようにした。** 形は「engine は誰が後ろにいるかを知らない」といういつもの線: **`CourtAnswerer` を実装したコントローラにだけ訊き、実装しないコントローラ（AI全部）では1ビットも変わらない**（指紋テストが無変更で通る）。**待ち時間は `CourtAnswerTicks` = 60 で打ち切り、そのノード自身の規則が答える**——**誰も答えないノードはAIと同じ結末**になり、席を外した人が他人の一生を止められない。**`GuidedController` は既定で「答えない」**（`AnswerProposals(true)` を UI が呼ぶ）＝**段階23 の「誰も答えない誘導ノードは純粋なAIノードと1ビットも違わない」を守るため**（待たせること自体が違いになるので、既定で待たせない）。
- 求愛の可否（`Agent.CanReproduce`、段階25）: **「身体にできないこと」と「今は割に合わないこと」を分ける。** 前者（成人前・出産の代金 `BirthVitalityCost/2` を払うと死ぬ・クールダウン中）は**候補から消す**。後者（空腹・低体力）は**効用式が決める**——`addCourt` が代金と `Life` の負の項を載せるため。**閾値は効用式から抜けていた項の代わりだった**（求愛はそれまで「そこまで歩く代金」しか払っていなかった）。**代金を入れるだけで個体数 39.6 → 28.1、閾値を外すと 38.0 に戻る**＝2つはほぼ打ち消し合い、正味は個体数・出生ほぼ同じで `killShare` 0.65 → 0.60、餓死は増える。**予想の「pop は下がる」は外れ**（上がった）——閾値のほうが厳しすぎた。`CourtNeedsSurplus = true` で昔の挙動に戻せる。
- 相手選び（`willCommit`、段階26・28）: **「待つ」が数えるのはその個体が配偶者を探し始めてからの時間**（`CourtClockResets = false`、段階28）——**求愛のたびに振り出しに戻していたので、断られた個体は我慢が尽きることがなく、誰も誰にも妥協しなかった**。直すと個体数 **+16.79 \*\***・出生 **+48.00 \*\*\***・世代 **+1.75 \*\*\***（代償は `killShare` +0.10 \*\*\*）。**この修正で集団の100%が「待ち疲れ」になり、床が常に効くようになった**ので段階26 の較正を測り直した——**床0 にすると個体数 +6.96 \* だが `shLooks` が 0.16 → 0.10（−0.05 \*\*\*）＝魅力遺伝子を買う理由が消える**ので 38 のまま（求愛の3分の2は成立している）。**待つことは要求を下げるが、下げ切らない**（`CommitFloor` = 38）。**段階26 まで、我慢が尽きると要求は下がるのではなく消えていた**（誰でも受ける）——しかも `CommitFitness` = 78 は集団の上位1割より上なので、**実際の受諾のほとんどがその「消える」経路を通っていた＝体調が効かなくなる唯一の場所が、ほぼ全部の受諾だった**。**床は体調についての新しい規則ではなく、すでにあった規則（体調は魅力の35%）を待っている個体にも届かせるもの**。**個体数の代償はゼロ**（−1.71、誤差内）で、`killShare` −0.05 \*\*\*・子の死亡率 −0.01 \*・寿命 +1.38 \*\*・集団の持続 +7.45 \* を買い、**魅力遺伝子に初めて選択圧がかかった**（`shLooks` は床50 で +0.02 \*）。**両方が出産の代金を払えることは、近づいた側も着いた時点で確認する**（要件なので拒否。歩くのにも体力を使うため）。
- 魅力（`fitness`）＝ 魅力遺伝子 ×(1−`FitnessConditionWeight`) ＋ 体調 × `FitnessConditionWeight`（既定 0.35）。**実能力（攻撃力・理性・頭の良さ）は一切入らない**（強さと「良い相手に見えること」を1つの値に兼ねさせない）。**体調の重みを上げると個体数は増えるが、配偶者選びが「今の健康状態」だけになって遺伝する装飾という選択軸が消えるので、上げていない**（値段は個体数で約2割。数値は `HISTORY.md` 2026-09-02）。
- 遺伝子は `Agent.Genome []float64`（`engine/genome.go`）。体力上限と移動速度は個体ごと（`a.MaxVitality(cfg)` / `a.MaxSpeed(cfg)` ＝ 基準値 × 遺伝子 / 50.5）で、**体力に関する閾値（求愛条件・子の初期体力）はその個体の上限に対する割合**で持つ（絶対値だと体力遺伝子の低い個体が「一生求愛できない」崖になる）。`Gene` 定数で名前を付けた9本＝攻撃力・防御力・最大体力・移動速度・回避率・記憶容量・理性・頭の良さ・魅力。**9本すべてに役割がついた**（防御力・回避率は段階8、記憶容量は段階9）。読むときは `a.Attack()` / `a.Rationality()` / `a.Intelligence()` か `a.Gene(g)`。**9本は1つの予算を分け合う**（`a.Budget()` ＝ 合計）。初期個体だけが予算（`GeneBudgetMean` ± `GeneBudgetStd`）と配分（ディリクレ、`GeneInitAlpha`。ガンマ分布のサンプラは `engine/genome.go` に自前で実装）を抽選し、**子は予算を親の一方から継ぎ（＋`BudgetInheritSpread` の正規乱数）、遺伝子ごとに継いだ値をその予算に合わせてスケールする**。したがって**突然変異は配分を動かすだけで合計を増やさない**。`BudgetHeritability = 0` で「予算が遺伝しない世界」に切り替えられる。**予算には維持費がある**（`BudgetUpkeep`。大きな身体ほど空腹が速く進む）——これが無いと予算は世代とともに際限なく上がり、予算が制約でなくなる。
- 能力の役割: 攻撃力（体力あたりの戦闘効率）／理性（推定精度＝世界の読みのノイズ）／頭の良さ（手の評価のノイズ。戦略の幅の経路は既定で無効、`StrategyDepthUnlock = 0`）／記憶容量（覚えていられる人数・取り込む速さ・忘れる速さ）。
- 意思決定の差し込み口: `Controller` interface（`Decide(*Perception) Action`）。`engine` はトリガー時にこれを呼ぶだけ。`World.SetController(id, c)` で1体だけ差し替えられる（将来の人間操作用）。`Perception` は他個体の真の能力を含まず、観測者自身の推定値だけを渡す。
- 人間操作（`engine/human.go`、段階19）: `HumanController` は `Controller` の2つ目の実装で、**UIが置いた「命令」を、訊かれたときにそのまま返すだけ**。engine は誰が操作しているか知らない（`AIController` は1行も変えていない＝**指紋テストが無変更で通ることが証拠**）。**設計上の約束は2つ**——(1) **プレーヤーに見せるのは `Perception` の中身だけ**（真の攻撃力・防御力・回避率は出さない。AIが読めないものを人に見せると別のゲームになる）、(2) **狙えるのは視界の中だけ**（`Order` が視界外の相手を断る。「知識」は縛るが「賢さ」は縛らない＝勝てない喧嘩を売る自由はある）。**engine はいつ訊くかを変えない**ので命令は据え置きで、**即時反映は既存のトリガー機構を通す**（`World.RequestDecision` → `TriggerRequested`）。**命令が流れるのは2か所だけ**: 狙いが消えた（`TriggerTargetLost` で訊かれた）ときと、**身体が変わった**とき——AIは訊かれれば全候補を採点し直すので勝手に立ち直るが、据え置きの命令は消えた相手を永遠に指し続けるため。**血統の乗り換えは同じコントローラを子に付け替えるだけ**（`World.Heirs` ＝ 生きていて成人した子。成人の線は `Agent.IsAdult` ＝ 求愛できるかと同じ線）。**「プレイヤー」も「ゲームオーバー」も engine には無い**（後継の指名も血統の終わりも `cmd/devview` 側）。**「誰として続けるか」は死んだときだけでなく、`t` でいつでも訊ける**（2026-09-06。**engine は最初からこれを許していて**——コントローラは付け替えられるもの——**縛っていたのは devview だけだった**。置いていった身体は**死なず、世界のAIに戻って自分の一生を続ける**。**血統は前に進めるもので、死ぬまで待つ列ではない**）。**死んだときに「誰として続けるか」を訊くのも `cmd/devview` 側**（2026-09-06）——**`World.Heirs`（成人した子）より広い候補を出す**のは、あらかじめ後継を指名する線と、身体が死んだ瞬間の線が別だから: 生まれたばかりの子しかいない血統は `Heirs` では空になり、**子が画面に映ったまま「血統は終わり」と言われていた**。成人前の身体は弱い手であって不可能な手ではないので、**隠さずにラベルを付けて出す**（`its child, still growing (23% grown)`）。`Perception.Trigger`（なぜ訊かれたか）はこの段階で足した——AIは読まない。
- 訊かれて答える遊び方（`engine/guided.go`、段階23）: `GuidedController` は `Controller` の3つ目の実装で、**`AIController` を内包して全部の質問に答えたうえで、転機だけ人に回す**。**AIは留守番ではなく常にその個体を動かしている側**——だから**誰も答えない誘導ノードは純粋なAIノードと1ビットも違わない**（テストで固定）。**訊くのは5つのトリガーだけ**（殴られた・目標消失・配偶者発見・破局・体力急減）で**戦闘は1戦＝1回**。**`TriggerRequested`（人が自分から訊いたとき）も転機に入れてある**——世界はこのトリガーを出さないので、これは「転機と転機の間にできることが見ているだけになる」のを防ぐための、人だけが持つ1つの動詞（実測 112 tick に1回＝一生で20〜30回。全トリガーだと 14.5 tick に1回で、その53%が「殴られた」）。**選択肢はその個体が実際に採点した候補**（`DecisionTrace.Options`）で、**メニューを別に書かない**。**並び順はその個体自身の `ChoiceNoise` を乗せたスコア順**（ノイズ抜きの順位を見せると、その身体の頭の良さ遺伝子が買っていない賢さを渡すことになる）。同じ（行動, 対象）は effort 違いをまとめて**上限12件**（メニューではなく**場**を渡す——「一番安全な手」と「一番大胆な手」はこの順位の中で隣り合わないので、何を見せるかは見せる側が決める）。**絞るときは行動の種類ごとに1つ必ず残す**（`keepAKindOfEach`）——素直な上位N件は**求愛と食事から先に消す**が、満腹なノードがどちらも安く見積もるその瞬間こそ人が口を出したい場面だから。**答えは1手であって据え置き命令ではない**（取り上げられた瞬間に使い切られる＝段階19 の「命令が流れる場合」が要らない）。**猶予60tick**を過ぎた質問には答えられない。狙えるのは視界の中だけで、検査は `World.Answer` が行い `OrderHuman` と同じエラーを返す。**`Config` には1つも足していない**（質問の間隔も猶予も世界のルールではないのでコントローラのフィールド）。
- 節目に決める性格（`engine/disposition.go`、段階23）: 人が動かせるのは**好み3つ**（`RiskWeight` / `CompetitionWeight` / `ShockRisk`）＝**段階12a が「正解が世界に無いので学べない」と切り分けた側**で、人に渡しても何も上書きしていない唯一の量。**読み方は「世界の値の何倍か」**（3つは単位が違うので倍率でしか足せない）。**合計は継いだもので、変えられるのは配分だけ**（`World.SetDisposition` は与えられた比率を継いだ合計に合わせて縮める）——用心深くするぶんは他の2つから出すしかない＝**取引が必ず発生し、最適値に固定して逃げ切れない**。上限（`maxPreferenceFactor` = 4）はそのまま。**何を「節目」と数えるかは engine の外**（`cmd/devview` が 乗り換え・成人・ペア成立・出産 の4つを見る）。
- 贈与（`engine/endow.go`、段階27）: **engine の中で唯一、継承も獲得もしていないものを個体に渡す操作**（`World.Endow(id, gene, value)`）。**シミュレーションの側からは誰も呼ばない**（規則も、コントローラも、出生も）。理由は操作性——予算は固定で配分は遺伝するので、**渡された身体が速さにほとんど使っていないと、人が動かす世界は糖蜜になる**。**世界を速くするのではなく主人公に良い身体を渡す**（世界を速くすると、測定した世界と遊んでいる世界が別物になる）。**新しい予算で払う**（他の遺伝子から取ると、プレーヤーが同意していない取引になり、主人公が静かに脆くなる）。**足した量を返す**ので、UIが「これは世界がやったことではない」と表示できる。**贈与は血統に残る**（予算は遺伝するので主人公の子は増えた予算を継ぐ）——避ける方法は「誰の予算が本物か」という第2の規則を作ることだけで、そのほうが悪い。
- 行動の語彙: `ActRest` / `ActMove` / `ActEat` / `ActAttack` / `ActFlee` / `ActObserve` / `ActCourt` / **`ActInvite`（段階32）**。すべて effort（体力の注ぎ具合）を持つ。
- 再判断トリガー: 攻撃された／体力が一定量減った／目標を見失った・到達した／一定tick何も起きない／**視界に食料や配偶者が「入った」**。逃走用の閾値は存在しない。**「入った」であって「ある」ではない**（`SightingRetriggers` で旧挙動に戻せる）——2026-09-04 まで実装がレベルになっていて、食料の多い地域を歩く個体が毎tick 考え直していた。**直すと `Step` が2.4倍速くなり、同時に個体数がほぼ倍になった**（45 → 78 \*\*\*、餓死率4分の1）。理由は `ChoiceNoise` が候補のスコアに対して大きいこと——**毎tick 考え直す個体はそのたびにノイズを引き直し、僅差の候補の間で振動して何も遂行できない**。`CLAUDE.md` が最初から書いていた「毎tick 再計画する個体は計画を遂行できない」がまさにこれで、**2行のコードがトリガー方式の設計に穴を開けていた**。
- 高台の遮蔽（`engine/stance.go` の `cover`、段階30a）— **既定はオフ**: **自分より1段以上低いところから殴られている身体は、地面のおかげで一定確率で外させる**（`HighGroundCover`）。**世界で初めて「2つの身体の地面の関係」を読む規則**（これまでの地形はすべて「立っているセルの性質」）。**倍率ではなく床**——**回避は構えの1チャネルで戦闘・逃走中しか持たない**（15.7%、回避寄りは7.7%）ので**倍率はほとんどの場合ゼロに掛ける**（最初の版がそれで何も動かなかった。**「打撃の28%が高低差をまたぐ」は状況の頻度で、掛ける量が非ゼロの頻度ではない**）。**1段でも上なら同じ**、`EvasionCap` で頭打ち。**知覚に載るのは「相手が自分より上にいる」ことだけ**（`AgentView.Uphill` / `Self.Covered`）——**どこに高台があるかは載らない**。**測定（0.3）: `evadedShare` 0.04 → 0.06 \*\*\*、世代 +0.71 \*、餓死率 −0.39 \*、しかし `onHigh` は動かない**＝**すでに上にいる者を助けるだけで、誰も高台を目指さない**。**30b（「高い場所は安全」を世界の事実として学ぶ）は 2026-09-08 に「作らない」で閉じた**——**どちらの決定も取り消さない**。(a) は **#59 の改訂（信念は地面の種類ごとではなく帰結の種類ごと）で既に許されており**、残っていたのは許可ではなく値打ちの問題だった。その値打ちは**段階35 が同じ形の2本目（危険）で測ってゼロ**（相関0.77 の正確な信念が伝承で広がり効用式に載っても `onWater` は動かない）。**`onHigh` は食料の相関でも動かない**（`plateau` 対 `plateaulink`、24シード、−0.01 ± 0.02＝**高台は豊かでも痩せてもいない**）。(b) は知覚のホットパスを重くするうえ、**コントローラが経路計画を持たない**ので使える形にならない。**再開の条件は「どこかに立つことが食えると安全を同時に払う規則が入ったとき」**（段階36 を高さでやる形）。
- 食料を地面に結びつける（`engine/region.go` の `tieFoodToTheGround`、段階33）— **既定は 0（結びつけない）**: 地域の食料の多さを、その地域の**平均移動コストの世界平均からの乖離**に結びつける（正＝荒れた地域ほど痩せている）。**世界の側で真値どうしを相関させるだけで、信念は1つも足していない**——豊かさ（15b/15c）と行きにくさ（段階29）の信念はすでに別々にあるので、**個体は既存の効用比較で関係を自分で見つける**。**総量は変えない**（相関のあと元の合計に戻す。テストで固定）。**平坦な世界はどの値でも1ビットも変わらない**。**段階29 の頭打ちはこれで破れた**（荒れ地に立つ割合 0.42 → 0.32 \*\*\*、個体数 +8.0 \*、餓死率 −0.20 \*、`killShare` は不動）＝**あの頭打ちは交絡だった**。**それでも `speedGap` は 0.00 のまま**——**「行きにくい地面は速い個体と遅い個体を選り分けない」は交絡ではなくこの世界の事実**で、**段階20 の前提（地形が速さと体力を初めて対立させる）はここで否定された**。**食料だけで半分・信念だけで半分・両方でようやく −0.10**。**弱い相関（0.5）は中間ではなく一番悪い**。**既定を 0 にしたのは再現性のため**（相関は物理ではなく風景の性質で、既定にすると段階20・29 の測定値がどの腕からも再現できなくなる）——**遊ぶ世界にだけ渡す**（`cmd/devview -terrain` は 1）。
- 地域と休憩場所・食料の地域差（`engine/region.go`）: 世界は粗い**地域**（4×3＝12ブロック）に分かれ、地域ごとに**「休息の露出」の係数**（段階14）と**「植物の生えやすさ」の係数**（段階15a）を持つ。**食料は総量を1つも変えず、どこに生えるかだけを変える**（`FoodSpawnRate` は世界で一番選択圧に敏感なので、総量が動くと別のものを測ってしまう。テストで固定）。**食料の地域差は世界を良くした**——`rareTrough` が 0.25 → 0.40 \* と**段階10 で割ったあるべき状態A の帯に戻り**、`nearest` −19.5 \*・`clumping` +0.10 \* と**段階14 では起きなかった凝集が起きる**（集まる理由は好意でも安全でもなく食料）。ただし**強い差（±1.0）は世界を壊す**（個体数 −13.3 \*）。**「豊かな地域に集まる」の6割は食料に引かれているだけで、4割は社会的**（社会的動機を全部切ると `richGain` 0.05 → 0.03。決定事項#39 への答え）。**外敵は追ってこない**（`enemyRichGain` は誤差内）。地域ごとに「休息の露出」の係数を1つ持つ。既存の `RestExposureWeight` に**掛け算が1つ増えるだけ**で、式も状態も増えていない。**地域は1種類だけ**——段階15 の食料の豊かさも将来の地域リセットも同じ地域に乗せる（用途ごとに区画を作らない）。**地域は壁ではなく、地域IDを読むルールはゼロ**。**個体は「あそこへ行って休もう」を計画できない**（知覚に載るのは今立っている場所の係数だけ）ので、働くのは計画ではなく**選別**——安全な地域にいる個体は休息が安く済むので留まる。効果は**弱いが本物**（`shelterGain` +0.02 \*\*、差を大きくすると比例して +0.06 \*\*）で、**凝集は起きない**。`ShelterSpread = 0` で全地域が普通になり、かつ乱数を消費しないので段階14 前と1ビットも違わない。
- 世界の1日と体内時計（`engine/clock.go`、段階18）: 世界は1000tickの周期を持ち、個体ごとに「眠りやすい時刻」が違う。**時刻で変わるのは休息の回復量だけ**（視界や食料を変えると全員に同時に効く世界の変化になり、「個体どうしが違うことが何を生むか」という問いとは別）。**体内時計は予算遺伝子ではない**——9本は「量」だが時計は**円周上の位相**で、予算に入れると「夜型は殴る力を削る」という生物がしていない取引を主張することになる。好みと同じく親から丸ごと継いで変異し、何も食わない。**回復はゼロに届かない**。**「眠っている友人は見張れない」という条件1つが機構の本体**（既存の割引に条件を足しただけで新しい項ではない）——これが「たまたま寝る時刻が違う」を「誰かが見張っている」に変え、**見張り役はルールのどこにも存在しない**。**これまでで一番はっきり世界を良くした段階**（個体数 +12.6 \*、悪化した指標なし）で、`SleepingWatch = true` にすると利益が丸ごと消えるので機序は確定。**完了条件の指標（全員が同時に寝ている割合）は最初から飽和していた**（どの条件でも 0.00〜0.01）——ハッザ族の観察は体内時計を入れる前から成り立っていた。
- 毒と警告信号（`engine/plant.go`、段階17b）— **既定でオフ**: 植物が毒と警告の2遺伝子を持ち、**相関させない**（#43）。毒は隠しパラメータ、信号は `(100−理性)/100 × ノイズ` で読む。**17a とは別のスイッチ**（17a で世界を壊したのは「どこに生えるか」なので、地面で生える世界のまま防御だけ遺伝させられる）。親は立っている株から**一様に**引くので、選択は「食べられなかったこと」を通じてだけ働く。**個体数が9分の1になるので既定はオフ**（どの用量でも持たない）。**`shRationality` は 0.07 → 0.11 \*\*\* と段階9 以来はじめて上がるが、信号を読めなくしても同じだけ上がる**——上げているのは信号ではなく**世界の厳しさ**（段階12a と同じ交絡。**対照条件が無ければ「理性に食料側の役割を与えた」と誤って書いていた**）。**正直な警告は進化しない**（毒も信号も初期値0.5 付近、相関ゼロ）——**どちらにもコストが無いので信号は「言うだけ」で得をし、正直さの足場が無い**。
- 植物の遺伝と種子散布（`engine/plant.go`）— **既定でオフ**: 植物が2本の遺伝子（散布半径・再生速度）を持ち（17a）、食べられた種が動物に運ばれる（17c）。**予算制も `Decide()` も持ち込まない**（#42）。繁殖は既存の食料湧きに粒子的遺伝＋まれで大きい変異を相乗りさせただけ、17c は既存の `eat` に相乗り（#44）。**総量は不変**（運ばれた種は次の湧きを置き換える）。**進化は起きる**（`plantRegrow` 1.00 → 1.57 \*\*\*、散布半径は**予想と逆に上がる**——親の近くはすでに食べ尽くされているから）。**しかし世界を壊すので既定はオフ**（個体数 44.2 → 14.9）。**逃げ道の無い2つの失敗**——短い散布は地域を空にし（`plantEmpty` 0.53〜0.82、**何も生えていない場所には種を落とせないので吸収状態**）、長い散布は食料を地面の良し悪しと無関係にする（`richGain` 0.05 → 0.01 で段階15b・15c が学ぶ対象を失う）。**段階15 の固定版は置き換えられる下地ではなく、集団が乗っていた土台だった。** **17c は救済にならず**（空の地域は 0.55 対 0.53 で不変）、**単独でも個体数を3分の1減らす**（運ばれた種が落ちるのは「たった今食べ尽くした場所」）。復活の条件は `PARAMETERS.md` C-5i。
- 食料の栄養バランス（`engine/diet.go`）: **同じ種類ばかり食べるとその食料の栄養が一時的に目減りする**（最大35%）。**新しい状態量は作らない**（状態量は今も3つ。「最近何を食べたか」は帳簿で、読み出し時に遅延減衰）。**隠さない**——寿命は「知りようのない沈黙の集計」という意図的な例外だが、これは普通のほうで、`Perception` にも効用式にも載る（動物は自分が何かに飽きたことを当然知っている）。**割引はゼロに届かない**（飽きは他を探す理由であって、食料の隣で餓死する理由ではない）。**効果は予想どおり小さい**（個体数・`killShare` は誤差内）が、**ゼロではなかった**——`dietVariety` 0.13 \*\*\*、そして**群れ狩りが初めて有意に増えた**（共同での仕留め 18.5 → 23.2 \*\*）。**一般則が「肉に独自の栄養をつける」という専用の仕掛けの弱い版として働いた**形。**強い罰（0.8）では逆に増えない**（世界が痩せて狩りに出る余裕が消える）。
- 地域の豊かさの認識と伝承（`engine/regionlore.go`）: 個体は**自分が立った地面の豊かさを覚え**（15b）、**交換で伝える**（15c）。**自分が立った場所しか覚えられない**ので、行ったことのない場所は聞く以外に知りようがなく、それが伝承に価値を与える。**新しい感覚も新しい記憶の種類も作らない**（読むのは「見えた食料の数」、鮮度は既存の遅延減衰＋接触リセット）。**人の記憶の枠は1つも食わない**（場所は人ではない）が、**記憶遺伝子は country をどれだけよく保てるかを買う**。覚えたら**そこへ向かう選択肢が1つ増える**＝段階14 に欠けていた「選べる」。**これまでで一番強い凝集が出た**（`nearest` 140 → 100、`clumping` +0.19 \*\*\*）が、**それは15c（伝承）から来ている**——伝承を切ると凝集はまったく起きない。**知識の3分の2は人から聞いたもの**。**「知ること」と「選ぶこと」は別**（知るが行かない条件は個体数 −11.0 \*）。**`shMemory` は動かなかった**（地域12個では容量が制約にならない）。**伝えることは知識を広めるが一致はさせない**（意見のばらつきは伝承の有無で変わらない）。 **地形の行きにくさも同じ記録に入る（段階29、2026-09-06）**——**足元の移動コスト倍率を同じ訪問の平均として畳み込む1スカラー**（荒れ地・水・高さを別々の信念にしない。**誤読は足さない**＝足元は正確に感じられ、分からないのは「地域のどれだけが足元と同じか」）。**薄れない**（`RegionCostForgetPerTick = 0`。地面は留守中に動かない。ただし**同じ関数を通してある**ので、地形が動的になれば率を入れるだけ）。**候補に挙げる門は今までどおり食料の記憶の鮮度で、行きにくさは順位だけを変える**。**測定**: 荒れ地に立つ割合 0.48 → 0.42（−0.05 \*\*）で `clumping` +0.11 \*、代償は `killShare` +0.02 \*（個体数は誤差内）。**避けるようにはなるが半分にはならない**——信念が動かせるのは候補1つの順位だけで、**目の前の食料は地形と無関係に湧く**から。**重みを上げても頭打ち**（0/1/2/4 で 0.48/0.45/0.42/0.42、重み4は個体数 −11.5 \*）。**そして「自分で歩いた地面しか分からない」版が一番悪い**（個体数 −13.3 \*）——**未知の地域は「普通」と見なされるので、荒れていると知っている場所より未知のほうが良く見える＝知っていることが罰になる**。伝承がその歪みを消す。
- 視界（`engine/sight.go`）: **「自分が立っているセル＋周囲1セル」のブロック**（`SightGrid = false` で以前の半径130の円）。セルの大きさは**円と同じ面積になるよう較正**（76.8）——合わせないと「見える量」と「見える形」の変化が分離できない。**円との違いは粗さではなく、どこに立っているかに依存すること**: 近いのに見えない相手（セル境界のすぐ向こう）と遠いのに見える相手（対角セルの隅、最大177）が同時にいる。**「近い」が「見える」を意味しなくなった**のがこの段階の本体で、`neighbours`（円の物差し）が不動のまま `killRate` が −0.13 \* 下がる。**逃走成功率は動かない**（面積を合わせると逃げ切る距離の平均まで揃い、異方性は分散しか増やさない）。**視界のグリッドは空間インデックスとは別物**——インデックスは結果を変えてはいけない最適化なのでセル幅が `Config` に無く、視界は世界のルールなので `Config` にある。インデックスへの問い合わせは**半径ではなく矩形**（ブロックの中心に個体が立っていないので、半径で囲むと4倍近く舐める）。**視力は遺伝子ではない**。
- 地形（`engine/terrain.go`、段階20）: 世界は**地形の地図**（`Config.TerrainMap`、1文字1セル）の上に敷ける。**既定は空＝平坦**なので、段階20 以前の世界は1ビットも変わらない（指紋テストが無変更で通る）。**地面がするのは2つだけ**——移動コストの倍率（荒れ地・水・坂）と、**そこへ行けるかどうか**（高低差はスロープでしか、1段ずつ）。**渡れない地形は作らない**（障害物＝経路選択＝知能の話で、速さの話ではない）。**高さは平面の上の軸で別レイヤーにしない**——別レイヤーにすると世界の他の規則（食料・視界・戦闘）が全部「どの層の話か」を言わなければならなくなる。**止められた身体は横に滑り、完全に詰まったら動かず何も払わない**。**知覚に載るのは足元だけ**（`Self.Ground`。AIも人も同じ＝段階19 の約束）で、**効用式は「この先も足元と同じ地面」と仮定する**。**測定（24シード × 20000tick）で狙いは外れた**: **速さのニッチは出ない**（`speedGap` は倍率1.5・2・4 のどれでも 0.00、`onDear` は地図に占める割合そのまま＝**個体は荒れ地を避けない**）——**避けるには「あそこは荒れている」と知る必要があり、足元しか分からないので知りようがない**。**代わりに速さが買われなくなる**（倍率4で `shSpeed` −0.04 \*\*\*）＝**移動が高い世界では移動能力への投資が割に合わない**という、前提と逆の結果。**並び替えが起きたのは高さのほうだけ**（`highGap` +0.02 \*\*）——スロープは門なので上の顔ぶれは下と違う。**値段は大きい**（個体数 rough −16.8 \*／plateau −37.9 \*\*\*／country −48.2 \*\*\*、`starveRate` 上昇）が、**世界は静かになる**（`killShare` −0.06〜−0.20 \*\*\*）**し、混ざらなくなる**（`halfLife` +27〜+32 \*\*＝地形が初めて「混ざりにくさ」を作った）。**次の一手は「地面を選べるようにすること」＝段階29**（段階15b の地域の記憶と同じ形で地形を覚え、15c と同じ形で伝える）。**高台の遮蔽（回避率）は段階30** として別に立てた——`PLAN.md`。
- 水は死ぬことがある（`engine/terrain.go` の `drownings`、段階34）: **水のセルで過ごす1tickごとに、一定確率でその身体は終わる**（`DrownChancePerTick` = 0.0002。**既定は非ゼロだが、平坦な世界には水のセルが無いので乱数を1つも引かず、以前の測定値は1つも動かない**）。**地面そのものが命を奪う最初の規則**。**継続ダメージではなく即死の確率**——削り合いにすると防御で割ることになり**防御に2つ目の役目がつく**（#58）。**閾値は無い**（水に入るかどうかは効用比較のまま。足したのは `Utility.Hazard` ＝「その手が身体を水に留める見込みtick数 × 確率 × `LifeValue`」の1項だけ）。**死因は専用のバケツで、排他**（`Stats().DrownDeaths`。殴られていた身体が溺れたら殺しには数えない——`starved` は「他の死因が名乗り出なかったぶん」なので混ぜると全A/Bに紛れ込む）。**汎用のハザード表は作っていない**（#60。2つ目の危険な地面が出たときに共通化する）。**実装前に数えたら前提が1つ崩れた**——**個体は川を渡っておらず、川の中で暮らしている**（水の上が全tickの14.5%、1回の滞在が平均127tick・最長3143tick）ので、**「速い個体はサイコロを振る回数が少ないから報われる」は成立しない**（実測 `shSpeed` −0.01 \*\*＝むしろ買われなくなる。段階20 と同じ向き）。**測定（river、24シード×20000tick）**: 溺死は死因の11%、個体数 −17.3 \*\*、`killShare` −0.10 \*\*\*、餓死率 +0.27 \*、**総死亡数は不動**（溺死が殺しと餓死を置き換えた）、**しかし `onWater` は +0.00**。**一番の発見は、足元の危険を効用式から抜いた対照（`DrownKnown = false`）と有意差が1つも出ないこと**＝**この規則の効き目は全部「選別」で「選択」ではない**（罰は選択肢1つあたり 0.8 ＝ その個体自身の評価ノイズ 20 の25分の1。**重みを上げる道は無い**——5倍で個体数18・外敵が絶滅する）。**したがって「水を避ける」を作るのは段階35（危険を場所として覚え、伝える）であって、危険の強さではない**（段階14→15b→15c、段階29 に続く3度目の同じ形）。**地形アームは再ベースラインされた**（段階20・29・33 の `river`/`country` の数値は「溺れない世界」のもの＝対照アーム `riverdry`/`countrydry`）。
- 水辺は豊か（`engine/region.go` の `tieFoodToTheWater`、段階36）— **既定は 0（地図の作者が決める。`cmd/devview -terrain` は 1）**: 地域に占める水の割合に応じてそこに生える植物を増やす。**世界で初めて危険と報酬が同じ場所に乗った**（渡るのに3倍・居座れば溺れる・そして食べ物がある）。**コストの相関（段階33）とは別の項**（#62。1本に混ぜると「行きにくい地面は痩せている」と「水辺は豊か」が打ち消し合い、地図の作者が両方を要求できなくなる）。**総量は不変**（段階15a の原則。再正規化をテストで固定＝増えるのではなく内陸から移る）。**読むのは地域に占める水の割合**＝地域単位の信念が扱える一番細かい粒度（#53）。**負の値は「オフ」ではなく別の風景**（痩せた水辺）。**測定で `onWater` が初めて動いた**——痩せた水辺 0.11／一様 0.14／豊かな水辺 0.15 と**3点が一直線**で、**段階34 も段階35 も動かせなかった数字が食料の分布だけで両方向に動く**（豊かな水辺: 溺死 +3.25 \*・`nearest` −8.94 \*、**しかし死亡率 −0.27 \*・餓死率 −0.24 \*・`grewUp` +0.02 \*** ／ 痩せた水辺: `onWater` −0.03 \*\*\*・溺死 −4.79 \*\*、代価は個体数 −7.7 \*）。**段階33 →35 →36 で3度同じ結論**＝**避ける理由と食べる理由が別々の地図に乗っているかぎり、避けることは常に食料を捨てること**。**`killShare` は上がるという予想は外れ**（集まった先に食料も集まっているので1人あたりは変わらない）。
- 溺死は伝わる（`engine/regionlore.go` の `danger` と `witnessDrowning`、段階35）: **地域ごとの信念の2本目**（その地域で1tick過ごすと死ぬ確率）。**#53「地形の信念は1スカラー」への意図的な例外**——#53 が禁じたのは**地面の種類ごとの信念**で、これは**帰結の種類ごとの信念**（何を奪われるか／出てこられるか）。荒れ地と川はどちらも高く、片方だけが殺す。**知る道は3つで、どれも既存の形**: 立つ（足元の危険を訪問の平均に畳み込む＝段階29 と同じ）、**見る**（視界内で誰かが溺れると、見ていた全員が**死んだ場所の地域**について**被害者の足元の真値**を `DrownWitnessLooks` = 20 回の「見た」ぶんの重みで取り込む）、聞く（`exchangeRegions` に相乗り）。**減衰なし**（コストと同じ率を共有）、**記憶の枠は食わない**、**効用式への入り方は段階29 と同じ**（門は食料の記憶の鮮度、順位だけ変わる）。**値付けに新しい定数は無い**（危険 × `RegionDangerTicks` = 700 × `LifeValue` ÷ `RegionDrawValue`＝段階34 の式を食料の単位に戻しただけ）。**測定: 知ることは起きる。ただし伝えなければ起きない**（信念と真値の相関 0.77／細かい地域では 0.98。**目撃だけで伝承を切ると 0.33 ＝「立つだけ」の 0.37 と同じ**——溺死1件の目撃者は6.3人でも一生に見るのは1件あるかないかなので、**地図に変えているのは伝承**）。**しかし行動は変わらない**（`onWater` +0.01 \*、**溺死はむしろ +3.75 \***）。個体数は +14.5 \* 上がるが、**信念が不正確な腕でも同じだけ上がる**＝効いているのは知識ではなく「どこかの評価が下がって遠出が減ったこと」。**理由を4つ潰した**: 重み4倍でも粒度9倍（108地域・相関0.98）でも川を世界の端に移しても `onWater` は動かず、**「良い地面へ行く」手は全決定の7%も選ばれている**（`drawShare`）。**残ったのは食料**——**段階33 の設定（行きにくい地面は痩せている）を入れた世界でだけ溺死が17%減る**（−4.33 \*\*、代価は個体数 −6.9 \*）＝**段階33 の結論（避ける理由と食べる理由が別々の地図に乗っているかぎり、避けることは食料を捨てること）が危険についても成り立った**。**目撃者スキャンは共通化していない**（#60。段階31 が2つ目の実例なので、切り出すのは31 の側の仕事になった）。
- スキル（`engine/skill.go`、段階38a）— **既定はオフ**（`SkillBirthplace` = 0。`cmd/devview -terrain` は 0.5）: **ヒント枠の中身**として入る（1枠は言い伝えかスキルのどちらか、値段は同じ＝**新しい並行の仕組みを作らない**、#63）。**使って上手くならない**（練習のカウンタは無く、実効値の掛け算は `Agent.Ability` の1か所のまま）。**カテゴリごとに連続スカラー1本**（離散の等級表は `StrategyDepthUnlock` の轍）。**実効値は掛け算ではなく天井**——**min(名目, 適性遺伝子/100)**（既定の適性は速さ。`SkillAptitude` で差し替え可）。この形のおかげで**降格の規則が要らず**（子の実効値は自分の遺伝子が支える水準へ勝手に戻る）、**教えた側より教わった側がうまく使う**ことが起こる。**2種類目は採取**（段階38b。**段階16 の飽きの割引への耐性**＝**競争ではなく効率**なので独占の余地が構造的に無い〈#64〉。適性の天井は**記憶**、出生地から読むのは**「地面が何を出すか」**で**痩せた地域ほど高い**＝**地形の無い世界でも成立する**）。**適性遺伝子はスキルごとの表**で、選び方の規則は2度とも「その行いが元から属している遺伝子」。**習得は3経路・マージは1つの比較演算**（新しい値 > 今の値なら置換）＝**出生地**（その地域の何を読むかはスキルごと。**誰も知らなくても始まる唯一の経路**）／**継承**（枠ごと）／**観察**（`observe` の交換に相乗り。**言い伝えは空き枠にしか入らないが、スキルは比べて上書きする**）。**天才は既存イベントを流用し、すでに持っているスキルだけを跳ね上げる**（無いカテゴリは発明しない＝**荒れ地の無い世界にはスキルが現れず、平坦な世界は1ビットも変わらない**）。**効き方は既存の掛け算1つ**（荒れ地の**超過分**の目減り＝どれだけ上手くても平地より安くならない）で、**知覚の `Self.Ground` もその身体の実効コストを返す**。**測定（48シード）: 拡散は起きた——名目 0.80／模倣なし 0.54／跳躍もなし 0.50（ばらつきゼロ＝出生地の値そのもの）＝跳躍が作り、模倣が配る**（跳躍は1実行に1.8回）。**保有率は模倣では動かない**（0.37＝**誰が持つかは出生地、どれだけ持つかは社会**）。**選別も出る**（`skillGap` +0.05 \*\*\*）。**しかし居場所は変わらない**（`onDear` +0.01 \*、超過コストを全部消して −0.00、`speedGap` +0.00、適性を頑丈さに変えてもニッチは出ない）。
- 協力の誘い（`ActInvite` と `engine/controller.go` の `scoreFight`、段階32）: **行動語彙が段階0 以来はじめて増えた**（`invite`＝「これを一緒に倒そう」。番号は末尾＝保存済みの世界の番号を動かさない）。**「宣言」は状態ではなく行動から読む**（`AgentView.DeclaredFor` ＝ 呼んでいる相手、またはすでに殴っている相手。`AttackingMe` / `CourtingMe` と同じ見える事実で、**持つと「呼んでから立ち去った個体が誘い続ける」嘘の状態になる**）。**同じ相手に宣言している者の力を、信頼のぶんだけ自分の側に足す**（`Affinity/AffinityTrust` × `AllyTrustWeight`。**好意は参加ボーナスではなく「土壇場で離脱しない確からしさ」**＝#56）——効くのは**勝率**と**相手が倒れるまでのtick数**の2か所で、**大きい獲物が「1人では倒しきれないが2人なら倒せる」**のはここ。**閾値も協力性遺伝子もパーティも作っていない**（信頼できる相手が視界に0人なら誘いは「攻撃＋無駄な時間」なので必ず負ける＝テストで固定）。**測定（48シード）: 群れ狩りは現れなかった**（`packSize` +0.02 \*、**語彙なしの対照でも同じ**、`jointHunts`・`hunts` は誤差内）——**段階11 の診断（足りないのは狙いを合わせる手段）はこれで否定され、保留#10 も閉じた**。**代わりに世界が良くなる**（個体数 +12.83 \*\*、世代 +0.23 \*、`dIntelligence` +5.04 \*、`killShare` 不動）。**出どころは人間どうしの袋叩きではない**（狩りにだけ味方を数える対照でも +13.08 \*\*）——**一番筋の通る読みは「同じ戦いを正しく安く値付けできるようになった」**（世界にはもともと同じ相手を殴る2人がいたのに、個体は「1人で殴る前提」で払いすぎていた）。**社会的な規則で予算比が動いた初めての例**でもある（`shSpeed` +0.01 \*\*、`shVitality` −0.01 \*＝**戦いを分け合えるなら頑丈さより脚**）。**誰も当てにしない世界は語彙ごと無い世界と1ビットも違わない**（期待0では1度も選ばれない）。
- 目撃した殺害（`engine/witness.go`、段階31）: **誰かが殺された瞬間、その場に視界が入っていた全員に1本の経路が通る。** **符号を決めるのは1つの比較**——**殺した側が自分と同族で殺された側が別種なら好意**（`AffinityWitnessKill`。既存の `AffinityHunt` の**第三者版**で、**打撃を入れた者は除く**＝参加ぶんは既に払われている）、**それ以外は殺した側の強さの読み**（`KillWitnessFactor` ＝ 払って見る観察の2倍の分散＝#55）。**新しい状態も新しい記憶の種類も作らない**（既存の `Opinion` にそのまま乗る）。**「誰が殺したか」は死体の分配と同じ `recentAttackers`**。**目撃者の走査は段階35（溺死）と共通**（`forEachWitness`。**共通なのは走査だけ**で、更新先も重みの単位も違う＝#60）。**測定（48シード）: 規則は絶えず発火し、世界の数字を1つも動かさない**（個体数 −1.12 ± 4.97、`priorErr` +0.63 ± 0.84、`watchShare` 0.34 で不動）。**理由は重みではなく枠**——1件の殺害が 24.3 回の機会を作るのに**取り込まれるのは 0.21**（`killLearned`。集団の95%は記憶が上限）。**#55 の心配（無料の目撃が有料の観察を殺す）は起きない**——重みを観察と同格にしても `watchShare` は動かず、**`observe` が買っているのは観測だけではない**（同じ行動が伝承の交換を運び、相手を選べる）。**実装前に数えて前提が半分崩れた**: 殺害は素通りしていない（**戦闘中の観戦が20tickごとに視界内の全員へ読みを配っている**）ので、この段階が足したのは**戦いの最後の1回**。
- 両岸を測る（`engine/banks.go`、段階37）— **規則ではなく測定**: `World.Banks(divideX)` が世界を縦の線で割って、**両岸がどれだけ互いを見ているか**（`crossShare` ＝視界に入っている組のうち線をまたぐ割合、`crossIndex` ＝岸を無視して組にしたときの期待値で割ったもの、**`crossDry` ＝水の上に立っている者を除いた同じ値**）と**両岸がどれだけずれているか**（`bankGeneGap` ＝予算比のベクトルの全変動距離、`bankCountryGap` ＝同じ地域についての信念の差／信念の平均）を返す。`BankTracker` は同じ集団を追って **`bankMoves`（線をまたいだ回数）と `bankBoth`（一生のうち両岸に立った個体の割合）** を返す。**線は測る側が与える**（川を抜いた対照には線を導ける地形が無く、対照は腕と同じ物差しで測らなければ比べられない）。**地域グリッドは川に合わせて描き直さない**（#61。地形と地域が別の地図であることは段階22 で確認した設計）。**数えるのは人間だけ**（外敵は場所ではなく獲物についていく）。**世界には一切書き込まず、乱数も引かない**（テストで固定）。**測定結果**: 川は**視界だけ**を分ける（`crossDry` 0.17 → 0.01、幅を視界より広げると 0.00 ＝下限）が、**集団は分けない**（`bankGeneGap` +0.01、`bankCountryGap` −0.00、`gapRel`・`halfLife` も動かない）——**水の上の14%が両岸を同時に見ており**、**一生のうち両岸に立つ個体が半分いる**（`bankBoth` 0.53。池の対照 0.48 より**多い**）。**あるべき状態 B〜E は地理では達成されない。**
- 地形の下ごしらえ（`engine/terrain.go`）: 移動の体力コストが `w.moveCostOn(x, y, effort)` → `w.terrainAt(x, y)` を通る。**今は世界じゅうが `flatGround`（コスト倍率1）を返すので挙動は完全に不変**（指紋テストが無変更で通る）。狙いは**速さに「有利な場所と不利な場所」を作ること**——速さは9本で一番買われている遺伝子（予算比0.230）なのに何とも取引していないので、「速くて脆い／遅くて頑丈」が並存しない。**倍率は速度ではなくコストに掛ける**（速度を落とすのは effort がすでにやっていることで、二重にすると測定で区別できなくなる。またコストにすれば、荒れ地を速く渡ることが速さと体力の取引そのものになる）。**地域ブロック（段階14）とは別物**——地域は「世界が何をくれるか」の単位、地形は「移動が何を要求するか」の地図。**コントローラは今も平坦だと思って計画する**（`Perception` に地形は載っていない）ので、この食い違いを埋めるかどうかは地形本体の段階で決める。
- 効用比較（`AIController`）: 全候補を「生命の価値×生存確率の改善 ＋ 子孫の価値×成立確率 − 体力コスト − 時間コスト」で採点し、最良手を選ぶ。逃走・追撃・先制攻撃はすべてこの比較の結果として現れる。**食料レースの勝率は到達時間の比**（`RaceOnDistance` で旧・距離比に戻せる）——相手の速さは隠しパラメータなので世界の基準速度を仮定し、**両者が同じ effort で動くと仮定する**ので effort は約分され、残るのは脚の差だけ。**速さの予算比は動かなかった**（速さはすでに到達tick数を通じて効いていた）が、地形が入れば距離と時間は食い違うので先に直してある。
- 戦闘の構え（`engine/stance.go`）: 戦闘系の行動は effort を「殴る・守る・かわす」の3チャネルに配分する。選べるのはプリセット3種（攻め寄り／守り寄り／逃げ寄り）だけ——自由配分にすると候補が立方体に増えるため。守りはダメージを減らすが同tickの打撃も減り、かわしは確率で丸ごと無効にする。**体力消費は Σ(チャネルの単価 × 使用率)** で、使っているチャネルは役に立たなくても払う。**食事中・休息中は無防備**（構えを持たない）。**相手の防御力・回避率は隠しパラメータ**（自分のものだけが効用式に入る）。
- 外すと隙ができる（`engine/stance.go` の `composure`、段階24）: **かわされた打撃は、外した側に隙を作る**（`OpeningTicks` = 3 tick のあいだ、その個体自身の守りとかわしが `OpeningGuard` = 0.4 倍）。**世界で唯一「外すこと」に値段をつけている規則**で、**`Perception` にも効用式にも載せない**（隙は3tick、判断は数十tickに1回なので、載せると一過性の状態を「この戦いのあいだ続くもの」として見積もることになる）。**予想を6つ書いて5つ外した**——`shEvasion` は動かず（+0.00）、`killRate` は上がり（+0.16 \*）、個体数は 6.75 \* 落ちた。**理由は「打撃のうちかわされるのは 5% しかない」こと＝規則の効き目の上限は発火頻度で決まる**（段階11 の `AffinityHunt` と同じ形の見落としを2回している）。**当たったのは向きが逆**——隙ができるのは**外した側**なので、群れで囲むことが罰される（`packSize` −0.03 \*）。**狙っていなかった良い変化はあった**: 少ないほうの種族の谷が浅くなり（`rareTrough` +0.06 \*）、仲間との戦闘比が下がる（`fightRatio` −0.10 \*）——攻撃一辺倒の身体（＝外敵）が一番この税を払うため。`OpeningTicks = 0` で完全に無効。
- 戦闘: 毎tickの継続ダメージ。殴る側のコスト < 殴られる側のコスト（攻撃側有利）。打撃は全員分をまとめてから同時に適用するので、スライス上の並び順が有利不利にならない。
- 見た目からの学習（`engine/appearance.go`）: **2026-09-04 の申し送り決着**——体つきを読んだぶんの利得は 0（相関 0.08）。**`Appearance` が「攻撃力を見せないため」に攻撃力の予算上の競合（体力・速さ）から作られているのが原因**で、`LooksShowBulk`（総量を見せる）にすれば相関 0.50・利得 1.24 \*\*\* になるが、精度は上がらず個体数を払うので既定は変えない。以下は段階10 当時の記述: 初対面の相手に固定値を当てはめるのをやめた。**見えるのは体つき（`Appearance` ＝ 体力遺伝子と移動速度遺伝子の平均 × 年齢補正）だけ**で、攻撃力は入らない。各個体は強さの観測（戦闘・観戦）を「この体つきはこれくらい殴った」の1点として貯め、**最小二乗の直線を1本**当てて次の初対面に使う。**engine は写像を持たない**（同じ世界を見た2体でも違う線を引く）。**傾きは0へ引き戻す**（`AppearanceSlopePrior`。観測数が数十では素の傾きはノイズで、引かないほうがマシになる）。**記憶容量は消費しない**（覚える枠が無くて記録できなかった観察からも印象は残る）。利得は同じ実行の同じ遭遇で測る（`learnGain` +3.0、うち体つきを読んだぶんは +0.34）。**個体数は 22.8 → 14.8 に落ちるが、原因は学習ではなく水準**——`PriorStrength` を 50 から実際の平均（36）へ下げるだけで同じだけ落ちる（＝よそ者の過大評価が世界を支えていた）。
- 効用式の中の仮定（`engine/lore.go`）: 効用式が使う5つの数値は**全ノード共通の定数ではなく個体ごとの値**。**2種類あり、どこから来られるかが違う**。**世界の事実**（殴り返される度合い・求愛が通る確率）は正解が世界にあるので走る平均で経験から学び、証拠の数には上限がある（上限が無いと老いた個体が世界の変化に気づけない）。**好み**（`RiskWeight` / `CompetitionWeight` / `ShockRisk`）は正解が無いので粒子的遺伝＋突然変異＋選択に任せ、世界の値の4倍でクリップする。**殴り返しの観測は「1回の戦い＝1票」**（engagement 開始5tick後に1度だけ。1打撃ごとに数えると長い戦いが答えを支配する）で、**学ぶのは殴った側と傍観者だけ**（殴られた側の選択は世界についての証拠ではない）。**信念の学習は既定でオフ**（`LearningRate = 0`）——動かないからではなく動くからで、真の殴り返し率 0.15 を知った世界は個体数が半分になる（増えるのは殺しではなく餓死）。**好みの側は既定オン**（`LoreInitSpread = 0.15`。0.25 は個体数を2割払うので較正して下げた）で、これだけで理性への選択圧が −6.95 → −0.83 に戻る。`World.Lore()` は集団の平均・広がりと**世界が実際にやっていること**（`TrueRetaliation` / `TrueAccept`）を返す読み取り専用の測定。
- 伝承（`engine/lore.go` の `exchangeLore`）: 上の5つの仮定は**他個体と交換できる**。専用の行動は作らず**既存の `observe` に相乗り**（観察はすでに体力と時間を払い、1度に1人しか観察できないという排他性も持つ）。**一方向の「学ぶ」ではなく双方向・同時の歩み寄り**で、観察を選んだのは片方でも見られた側も変わる。**1回の歩み寄りは差の2%**（`LoreExchangeRate`）——「中間で会う」0.5 にすると、交換が1実行に19000回起きるので集団の値が数世代で1点に収束し、段階12a が作った好みの多様性が消えて個体数が半減する。**好意の加点は実際に動いた量に比例**（すでに値が近い相手との交換は自動的に無価値。判定は書いていない）。**垂直伝承と水平伝承は同じ経路**（親子は育児期間ぶん共在するので同じ交換が多く走るだけ）。交換の記憶は**割り込まない枠**で取る（殴られたことは世界が押しつける出来事だが、通りすがりの交換はそうではない）。**好意が効用の正の項になった**（`LoreValue`。他個体が生きていて近くにいることが得になる2本目の経路で、集団の**持続**を説明するのはこちら）。**被参照数は上位5分の1が0.48**——完全均等 0.20・無作為の雑音の底 0.22 に対して偏りは実在するが、「数人の教師と群衆」ではない。
- ヒント（`engine/hint.go`）: **効用式に書かれていない関係**を選択に見つけさせる枠。「**状況特徴量 × 対象行動 × 重み**」の3つ組を上限4本。**ヒントは行動を決めない**——既存の効用比較への加減点としてのみ働き、ヒント由来の分岐はどこにも無い（最大重みでも致命的な手を選ばせられないことをテストで固定）。**知能・理性でゲートしない**（`StrategyDepthUnlock` の轍を踏まない）。要求するのは**予算**だけで、**空の枠でも払う**（払っているのは入れ物）。**突然変異は二段階**——重みは通常の変異、**何についてのヒントかが変わるのは天才イベントのときだけ**。**継承は染色体式を枠単位に拡張**。**交換ではヒントは歩み寄らずコピーされる**（「腹が減ったら大物を狙え」の半分は弱い版ではなく無だから）ので、**空の枠を持つ相手にだけ**入る＝血統を通らない唯一の経路。**手口の多様性を作っているのは重みではなく配られ方**——重みを0にしても種類数は変わらないのに、模倣を切ると 1個体が持つヒント 1.50 → 0.77、種類数 7.11 → 4.38 と伝承なしの水準まで落ちる。**「頭の良さへの選択圧を下げる」は 2026-09-05 に取り下げた**——絶対値では今も出る（`dIntelligence` +10.55 \*\*\*）が、**同じ符号が理性にも出て `budget` 自体が +42.06 \*\*\* 動く**＝枠代は遺伝子に配る前の予算から引かれるので9本が一斉に小さくなっていただけで、**予算比で読むと差は1標準誤差**（一番動くのは速さ）。**同時に「重みだけ0の対照に勝つ」も消えた**（穴の開いた世界では 22.0 対 14.8、直った世界では 73.3 対 73.5 で誤差内）——ヒントが動かしているのは個体数ではなく死因の内訳（`killShare` −0.04 \*\*、`starveRate` +0.19 \*\*）。既定は当面そのまま（`TODO.md` 10b に宿題）。
- 記憶（`engine/memory.go`）: 相手ごとに1レコードで、中身は (A) リスク記憶＝その相手に失った体力の累積、(A') **好意**＝その相手にしてもらったことの累積、(B) 強さ推定＝ベイズ更新の平均と分散。(A)(A') は読み出し時に遅延減衰し、(B) は直接戦闘と観戦で更新され、理性が高いほど正確、観測数が増えるほど分散が縮む。
- **記憶は有限**: 覚えていられる人数（`MemoryCapacity × 記憶遺伝子/50.5`）、1tickに取り込める数（容量から導出。専用遺伝子は作らない）、忘却の速さ（`(100 − 記憶)/49.5` 倍）のすべてが記憶遺伝子から決まる。あふれたら**リスクも好意もゼロのレコード**を捨てる（価値の残るレコードは群衆の一人には押し出されない。押し出せるのは殴られた・ペア成立・出産・共同で仕留めたという**世界の側の出来事**だけ）。**視界に入れると、値は変えずに減衰の起点だけが更新される**（近くにいる相手のことは忘れない）。**覚えられなかった相手は何度会っても初対面のまま**（`PriorStrength` が当たる）。
- **好意**: 記憶に入る初めての正の項。発生源は**実イベントだけ**（ペア成立・出産・**直系の親子1ホップ**・**共同で仕留めたこと**・**同族が他種族を仕留めるのを見たこと**＝段階31）で、**共在時間による加点はしない**。共同狩猟の好意（`AffinityHunt`）は**血縁でも配偶者でもない相手から得られる唯一の好意**で、誰が数えられるかは新しいルールではなく**死体の分配ルールそのまま**（傍観者は入らない）。**ただし群れ狩りが現れない世界では発火機会が少なく**（20000tickで13.7回）、集団の指標は動かない——効くのは段階12 で好意が効用の正の項になってから。専用の枠を作らないので、**好意を持てる相手の人数は記憶容量で頭打ちになる**（＝集団の実質的な最大人数が、集団という概念を書かずに出てくる）。**敵意は新設していない**（リスク記憶がそのまま敵意）。今のところ効くのは休息の露出の割引だけで、効用の正の項にするのは段階12 と対にする。
- **休むことは無防備であること**（`RestExposureWeight`）: `rest` の効用に「近くの好意の無い相手の推定の強さ × 近さ」を、休んでいる間ずっと入り続けるダメージとして乗せる（`PlanHorizon`/`ShockRisk` と同じ「分岐しない1本の軌道」の形）。`Decide` の先頭で近傍を1度走査し、被攻撃ダメージと露出を同時に作る。**重みは 0.05 と小さい**——`exposed` は `recoverable()` の中で `RegenRate`（0.09）から引かれるのに、隣人1人ぶんの脅威は 1.14/tick と12倍あるので、大きくすると勾配ではなく崖になる（視界に1人いれば休息の価値ゼロ → 消耗死 → 出産前のペアが壊れる）。
- 遺伝: 子の能力は**能力ごとに親のどちらかの値をそのまま引き継ぐ**（粒子的遺伝）。引き方は能力ごとに独立で、連鎖群も性染色体も無い。混合遺伝（両親の平均）は分散を毎世代半分にするので 2026-09-01 にやめた。**突然変異はまれで大きい**（`MutationRate` 2% × `MutationStd` 40）＝毎回小さく乗せると親の値が子に残らず、粒子的遺伝にした意味が消えるため。
- 血統: 子は `ParentIDs`、親は `ChildIDs` を記録する。
- 乱数は `World` が持つ `*rand.Rand` 1本。同じ `Config`（同じ `Seed`）なら常に同じ結果になる。コントローラも `Perception.Rand` 経由でこれを使う。
- 意思決定トレース（`engine/trace.go`）: `Decide()` 1回分の記録。「どのトリガーで呼ばれたか（`Trigger`）」「比較した候補ごとの効用の内訳（`Utility`：目的ごとの `Goal{Value, Chance}`、リスク・体力コスト・時間コスト）」「頭の良さによる評価ノイズ（`Noise`）と実際に比較したスコア」「選ばれた行動」を持つ。効用の合計は `Utility.Total()` ＝ Σ(Value×Chance) − Risk − VitalityCost − TimeCost で、効用式の唯一の実装でもある（トレースが式とズレようがない）。
- トレースは**追跡対象に指定したノードだけ**記録する。`World.TrackDecisions(id, true/false)` で開始・停止、`DecisionTraces(id)`（古い順、直近16件）と `LastDecisionTrace(id)` で読み出す。記録はノードの死後も残る（死に際に何を考えていたかが一番読みたい）。指定していないノードは何も記録しない。
- 再判断トリガーは `Trigger` として名前が付く（spawned / target lost / goal reached / attacked / vitality drop / idle / food in sight / bond ended / controller set）。`AIController` 以外のコントローラ（将来の人間操作）でも、トリガーと最終行動だけは `engine` 側が記録する。
- 挙動の指紋（`engine/golden_test.go`）: 既定条件・シード1〜3を5000tick回したときの個体数・出生・死亡・戦闘数・世代数と平均値を固定値で押さえたテスト。**「速くするだけの変更」が世界を変えていないことを、同じバイナリ内の比較では証明できない**ために置いてある（2026-09-04 の記憶キャッシュがまさにこの用途で、**数値を1つも書き換えずに通ったことが挙動不変の証拠**になった）。ルールを意図して変えたときは、その変更と同じコミットで数値を更新する（赤いテストを緑にするために書き換えない）。
- テスト: `engine/world_test.go`（ルールごとの単体テストと、8000tick回す統合テスト）と `engine/decision_test.go`（シナリオテスト）。後者は状況を手で組み立てて `Decide()` を1回呼び、選ばれる行動をassertする（格上に守られた食料／格下との食料争い／逃げ場のない被攻撃／満腹で消耗している／餓死寸前で求愛を検討すらしない、など）。効用の内訳やそもそも候補に挙がったかまでトレース経由で確認しているものもある。
- ベンチマーク: `engine/bench_test.go` に `BenchmarkEngineStep` / `BenchmarkEngineStepCrowded` / `BenchmarkDecide`。
- 空間インデックス（`engine/grid.go`、非公開）: 一様グリッド。ノードと食料をセルに登録し、「この近くに誰がいるか」を全体走査せずに答える。近傍を訊く3か所（`perceive()` の他個体と食料、`nearestFoodInSight`、`exchangeReadings`）がこれを通る。**世界のルールを何も変えない**（同じシードなら1ビットも変わらないこと）のが唯一の条件なので、セル幅は `Config` ではなく `PerceptionRadius` から導出する。**インデックスは差分更新せず、移動・出生・死・食料の増減で「古い」印を付けて、次に問い合わせが来たときにまとめて作り直す**（1tickあたり実質2回）。返す候補は**必ず添字の昇順**——知覚は他個体ごとに乱数を引くので、走査順は同じシードが再現するものの一部だから。**移動ループの途中で問い合わせると毎回作り直しになる**ので、動かす前か後に訊く（実測で1tickあたり1.05回）。**個体数が数百のうちは全走査のほうが速く、効き始めるのは500〜600体から**（数値は `HISTORY.md`）。
- `engine.World.Clusters(linkDist)`: 集団の構造（単連結の連結成分。クラスタ数・サイズ分布・最大成分の割合・個体ごとの所属ラベル `Labels`）。読み取り専用の解析で、ノードは集団IDを持たない。連結距離は `engine.DefaultClusterLinkDist = 30` 固定（`Config` に入れない＝世界のルールではなく測る側の目盛りだから。距離が違う測定値どうしは比べられない）。`Spacing()` と同じく O(n²) なので間引いて測る。
- `engine.World.ClusterGaps(linkDist)`: 集団間の距離分布（各集団から最も近い別の集団までの最短距離。平均・中央値・下位1割・密度で割った相対値）。単独個体は集団としても対象としても数えない。**連結距離より短い値は原理的に出ない**（それ以内なら同じ集団になるため）。相対値は**両端を較正済み**（構造なしの配置＝0.98、格子状に避け合った配置＝1.92）なので、1未満＝偶然より寄り集まっている、1超＝避けている、と読める。**ただし `gap`（絶対）と `gapRel`（配置）は別のことを言う**——避ける規則を強くすると個体は離れるが集団の数も減るので、分母（その数の集団を無作為に置いた間隔）が分子と一緒に上がり、`gap` が38%動いても `gapRel` は動かない（2026-09-05）。**規則が密度そのものを動かすときは、正規化した指標だけを読まない。**
- `engine.MembershipTracker`: 同じ顔ぶれが一緒に居続ける時間（membership half-life）。一定間隔で `Observe(w)` を呼ぶと、同じクラスタにいた**2体の組**が何tick後まで同じクラスタに居続けるかの生存曲線を貯め、半減期を返す。クラスタは合体・分裂するので「同じクラスタ、のちほど」は定義できないが「この2体はまだ同じクラスタか」は定義できる、というのが組で測る理由。**片方が死んだ組は離れたと数えない**（在籍時間ではなく寿命を測ってしまうため）。世界には一切書き込まない。
- `engine.FightTracker`: 戦闘を「相手が誰か」で分ける。**素直な「集団内戦闘率 ÷ 集団間戦闘率」は測れない**（打撃が届く `CombatRadius` はクラスタの連結距離より短いので、殴り合う2体はつねに同じクラスタにいる＝集団間戦闘は稀ではなく不可能）。代わりに、同じ遭遇（`CombatRadius` 以内にいる組）を「`DefaultCompanionLag` tick前から同じクラスタにいた組（仲間）」と「そうでない組（よそ者）」に分け、**遭遇あたりの**戦闘率とその比を出す。比が1を下回るほど「仲間とは争わない」。世界には一切書き込まない。
- `engine.CensusTracker`: 種族ごとの個体数。一定間隔で `Observe(w)` を呼ぶと直近 `DefaultCensusWindow`（5000tick）の窓の中の**種族ごとの平均・最小・最大**を返す。1tickの数え上げでは共存しているかどうかは決まらない（あるべき状態Aが窓の平均で書かれているのはそのため）ので窓で持つ。読むのは振れ幅ではなく **Trough（窓の最小値 ÷ 平均）＝振動の谷が0にどれだけ近づいたか**。振動は許容されるが、谷が0に触れた種族だけは戻ってこない（移入が無いので0は吸収状態）。**絶滅は窓ではなく実行全体で覚える**（窓が死を通り過ぎると見えなくなるため）。種族の一覧は持たず、実際に現れたラベルを数えるだけなので、段階11 で種族が増えてもこのファイルは触らない。世界には一切書き込まない。
- `engine.Spacing()`: 個体の散らばり具合（視界内の他個体数・最近傍距離・一様分布と比べた集まり具合）。`Stats()` と別なのは O(n^2) だから。協力を入れる前後で「群れているか」を測るために用意した。
- 世界を外から変える（`engine/edit.go`、段階22）: **`SetTerrain` / `SetRegion` / `Tune` / `SetDifficulty` は `Endow` と同じ立場**——**シミュレーションの側からは誰も呼ばない**（規則も、コントローラも、出生も）。**乱数を1つも引かない**（エディタで敷いた世界が、設定ファイルで敷いた世界とまったく同じに走るため）。**個体群には触らない**（地面を変えるのと個体を動かすのは別の行為で、後者は `Repopulate`）。**世界の建て方の規則を再適用しない**（地形と食料の結びつけ〈段階33〉は世界を建てるときの規則で、**最初の編集からあとは地域は管理者のもの**）。**調整できるルールは6つだけ**（#50。食料・予算・予算のばらつき・競争心・戦闘の見積もり長・突然変異率）で、**それぞれ測定済みの範囲を持ち、外れた値は「拒否せず警告する」**——動かない世界を作る自由は管理者にあるが、それを作っていることは知らせる。**難易度は予算だけ**（`Difficulties`。`measured` が `HISTORY.md` の全数値が取られた世界そのもの）。
- 保存と読込（`engine/save.go`、段階21）: **世界まるごと**（`Config`・地形・地域・食料・全個体の記憶と信念とヒント・`tick`・乱数の位置）と、**個体群だけ**（`Node`）の2つを JSON で出し入れする。**「他所で育ったパラメータの読込」「進めた世界の配布」「天才の配置」は同じ1つの機能の応用**（#48）。**受け入れ条件は指紋テストと同じ形**——保存 → 読込 → N tick が、保存せず N tick と**1ビットも違わない**（500tick 先まで比較。**保存漏れは差分として必ず出る**ので、ファイルどうしを比べるのではなくこの形で書く）。**乱数は「種と、いくつ引いたか」を保存して読込時に引き直す**——Go の `math/rand` の状態は取り出せず（`MarshalBinary` も `GobEncode` も無い）、**生成器を差し替えると記録済みの全測定値が再現できなくなる**ため。**5000万回の引き直しで0.2秒**（10万tick分より多い）。**形式は JSON**（読める・手で直せる。Go の float64 は往復で完全一致）。**コントローラは世界の状態ではない**（読み込んだ世界は全ノードがAI＝段階19 の線）。**個体群に持ち込めるのは「その身体が何であるか」だけ**（遺伝子・性別・種族・好み・信念・ヒント・体内時計）で、**他人の名前を含むものは持ち込めない**（記憶・血統・ペア・地域の知識＝別の世界ではその名前が誰も指していない）。**読み込んだ世界は自分の `Config` を連れてくる**（世界を記述するフラグは `-load` のとき無視される）。
- `cmd/experiment`: ルール変更をA/Bで測るヘッドレスのランナー。条件（`Config` を書き換える関数）× シード × tick数を並列で回し、条件ごとの平均±標準誤差と、**同じシードどうしの差分**を出す。選択圧に触れる変更のときはこれを前後で回す。方針は「ルール変更の測定」の節、使い方は `README.md`。
- `cmd/snapshot`: 記録用の画像をPNGに描くヘッドレスの開発用ツール。`-mode world` は1瞬間の世界をクラスタごとに色分けして、`-mode curve` は membership の生存曲線を複数シードの平均で描き、`-mode genes` は能力の分布を**開始時の分布と重ねて**描く（`-terrain` を渡すと `world` は水と、両岸を数える線を敷いた世界を描く＝段階37）（継承則の変更は世界の絵には映らない＝2つの実行は「誰がどこに立っているか」が違うだけなので、分布でしか比べられない）。数値では分からない「その集団がどんな形か」「曲線がどんな形か」を記録に残すためのもの。決定的で、画像の下帯に条件と**コミット**を焼き込む（画像がリポジトリの外に出ても出所が分かるように）。出力は `docs/images/`。使い方は `README.md`。
- `cmd/devview`: ebitenで `engine` を直接描画する開発用ツール。**円は身体そのもの**（輪の大きさ＝体力上限＝体力遺伝子、塗り＝今残っている体力、後ろの尾＝移動速度、輪の太さ＝攻撃力）で、右パネルには遺伝子9本と予算比が出る。**予算制で「どんな身体を買ったか」が個体ごとに違うようになったので、体力そのものを大きさに使うのをやめた**（傷ついた大型と無傷の小型が同じに見えてしまうため）。全体を眺めるモードと、1ノードを追うモード（クリックで選択＝追跡、一時停止・1tick送り・低速再生、右パネルに意思決定トレースと他者への強さ推定）、そして**1ノードを遊ぶモード**（`h` が **AIだけ → 訊かれる（段階23）→ 自分で操縦する（段階19）→ AIだけ** の3状態を回る＝**握る手綱の量が一方向に増える順**。**2026-09-06 に順序を逆から直した**——以前は「操縦」が先だったので、**テンキーが効かない訊かれるモードで `h` を押すと手綱ではなくAIに戻り、しかも手放すと選択も外れるので次の `h` が「まずクリックしてください」になって抜けられなかった**。手放したノードは**選択されたまま**残す。訊かれるモードでは質問が立つと世界が止まり `1`〜`3` で答える／`enter` で任せる（質問が無いときの `enter` は「今すぐ訊く」）、節目には4つの性格から選ぶ。**選択肢は点数順ではなく最大5つ**——`a meal` と `a child` は**専用の枠**（血統に要るのはこの2つなのに、効用式は「その個体1人の一生」に対して値付けするので満腹なら食事は安く、寿命が残れば求愛は安い＝**まさに人が反対したい場面**）、残り3つが賭けの種類（safest / soonest / boldest）。**合計スコアは見せない**（片方に「最善」と書いてある選択は選択ではない）。**求愛できないときは何が足りないかを出す**（`CANNOT COURT: hunger 38.5 -> under 35`）。**求愛されたら `y`/`n` で答える**（2026-09-06。世界が止まり、パネルと画面上部に `#12 IS PROPOSING - [y] accept [n] refuse (60 ticks, then it decides for itself)` が出る。**答えないままでもよく、そのときはノード自身の規則が答える**）。**近づいてくる相手が何をしに来たかを線で描く**（2026-09-06。**赤＝殴りに来ている、オレンジ＝口説きに来ている**。`Perception` の `AttackingMe` / `CourtingMe` と同じ事実なので、プレーヤーだけが知る情報にはならない。**まっすぐこちらへ歩いてくることは目に見える振る舞い**で、隠しパラメータでもまだ下していない決定でもない。誰も狙っていない個体には何も出ない）。**断られたときはどちら側が断ったかと、その2つの数字を出す**（2026-09-06。`AS A MATE you are worth 92, and want 38; everybody wants 78` と `LAST PROPOSAL #60, 159 ago: YOU said no (you made them 30, wanted 38)`）——**それまでは断ったのが自分の側でも「#12 turned me down」と出ていた**。これのために `Perception.Self` に `MateValue` / `MateBar` / `LastCourt` を足した（**規則は1つも読まない**＝指紋テストは無変更で通る。**新しい知識でもない**——世界はもともと「相手が受けたか」を求愛した側に教えており、それが `AcceptChance` の学習源）。**`YOUR LINE`（血統が何代・何tick・子が何人）と `LAST TIME`（前回の答えが体力と空腹に何をしたか）を出す**——前者は**効用式に項が無い＝プレーヤーだけが持つ目的**で、ノードの採点に反対する理由になる。操作するモードではクリックで狙い、行動語彙のキーで命令、`m` は**着いたら止まる**（`ActMove` は向きしか持たないので、到着を見ているのは devview 側）。どちらも `k` で後継の指名、3枚目のパネルには**そのノードが知っていることだけ**が出る）を持つ。**身体が死んだら「誰として続けるか」を訊く**（2026-09-06。世界を止め、`1`〜`5` で選ぶ／`enter` で血統を終える）——**候補は `World.Heirs` より広く、成人前の子も出す**（指名しておく `k` は成人した子だけのまま。**あらかじめ選ぶ**のと**死んだ瞬間に選ぶ**のは別の線で、後者を `Heirs` に合わせると、生まれたばかりの子が画面に映ったまま「血統は終わり」になる）。**近い順に並べる**（死んだ身体自身の子 → 血統の他の者、成人 → 成長中）。**カメラは `z` でズーム**（1/1.6/2.5/4倍。操作中は2.5倍から追従）——**描画は全部カメラ変換を通す**（描いた画像を拡大するとどの円もぼやけるので、座標のほうを変換する）。**最初に渡す身体は「出産の代金を払って生き残れて、魅力が集団の中央値以上の成人のうち、予算上位半分で一番速いもの」**（要件2つは世界の側の数字＝`BirthVitalityCost` と集団の中央値。**どちらも欠かすと遊べない**——体力20.8対代金20の身体は生涯の4割「求愛できない」状態で、魅力7の身体は200回求愛して3回しか受け入れられなかった）。旧: 「予算上位半分のなかで一番速い成人」（順位で選び、絶対値の下限は置かない＝集団の配分が動いても古くならない。**最速だけで選ぶと速さ以外を売り払った身体になり96tickで死ぬ**）。**そのうえで速さを世界の平均まで引き上げる**（`-boost`、既定オン。`World.Endow` を通し、足すのは差分だけ＝予算はその分だけ増える。パネルに `GIVEN +23 speed ... not the world's doing` と出る）。**effort の既定は 1.0**。**血統の子には薄い金の輪**（生まれた瞬間は吹き出しで分かるが、画面上ではただの小さい円が1つ増えるだけだった。**描かれているものへのラベル付けであって新しい知識ではない**——devview は最初から世界じゅうを描いている。**視界の外の子は狙えないまま**）。**主人公は短い吹き出しで喋る**（殴られた・戦い始めた・断られた・ペア成立・出産・狙いが消えた・食べた・体力が3分の1を切った、など2〜4語。操作ノードだけ、2.5秒で消える）——**engine にイベントの仕組みは足していない**。一生は「何が変わったか」で完全に記述できるので、**前フレームの主人公との差分**から導いている（`engine` に通知を頼み始めると engine がゲームのほうへ引っ張られる、というのが全段階を通して守ってきた線）。**遊んでいるあいだ、パネルは選択より操作を優先する**（2026-09-06。選択が外れるとパネルが `no node selected` に戻り、**世界が止まっている理由＝答えるべき質問ごと消えていた**）。**何を待っているかは画面上部にも出し、そのモードで意味を持たないキーには「持たない」と言わせる**（止まった世界＋無反応のキー＋空のパネルは「壊れた」と区別がつかない）。**起動直後の入力は真に受けない**（WSLg は窓が開いた最初のフレームに ArrowUp / ArrowLeft を送ってくる）。**起動方法・キー操作・画面とパネルの読み方は `README.md`**（ここには書かない）。