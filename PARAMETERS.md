# パラメータ一覧

調整と検討のための表です。**ルールそのものは `NODE.md`（ノード）と `ENEMY.md`（外敵）**、設計と規約は `CLAUDE.md`、設計判断の経緯と測定値は `HISTORY.md`、操作方法は `README.md` にあります。ここには「どんな数字があり、それが世界に属するのかノードに属するのか」だけを書きます。

値は `engine.DefaultConfig()`（`engine/config.go`）の既定値です。**既定値はすべて暫定**で、`cmd/devview` の目視と `cmd/experiment` のA/Bで詰めている途中のものです。

## 区分

パラメータは4種類に分かれます。**この区分自体が今の設計の要約**になっています。

| 区分 | どこにあるか | 個体差 | 遺伝 | 経験で変わるか |
| --- | --- | --- | --- | --- |
| **A. 能力（予算制の遺伝子）** | `engine.Agent.Genome` | **ある** | **する**（予算と配分の両方） | しない（**継承値は一生変わらない**。実効値は年齢・性・地面で変わる） |
| **A'. 予算を食わない遺伝形質** | `engine.Agent`（`lore` / `chronotype` / `taste`） | **ある** | **する** | **一部は変わる**（信念は学習で、ヒントとスキルは模倣で入れ替わる） |
| **B. ノード固有の状態** | `engine.Agent` | ある | しない | 変わる（生きている間だけ） |
| **C. 世界のパラメータ** | `engine.Config` | **無い（全ノード共通）** | しない | 変わらない（`engine/edit.go` の編集を除く） |

**2026-09-16 現在、この表の重心は A と A' にあります。** 段階7c（予算制）・段階12a（効用式の仮定を個体ごとの値に）・段階12c（ヒント）・段階38a（スキル）・段階18（体内時計）・段階84（味）を通ったので、**個体ごとに違う数字は9本の遺伝子と予算、好み3つ、信念2つ、ヒント枠とその中身（言い伝えかスキル）、体内時計の位相、味の位相**にまで広がっています。**C に残っているのは「世界がどう動くか」と「効用式の値付けのうち、まだ誰も個体差を与えていないもの」**（`LifeValue` / `LoreValue` / `TimeCost` など目的の重み）です。

**次に C から A' へ移す候補は決まっています**——**段階94（欲求という4本目の選好）**。**今の好み3つ（`RiskWeight` / `CompetitionWeight` / `ShockRisk`）はどれも危険の読みにしか効かず、目的の側の重みには個体差が1つも無い**（`PLAN.md` の段階94・決定事項#123）。

---

## A. 能力（予算制の遺伝子・遺伝する）

`engine/agent.go` の `Agent.Genome`（`engine/genome.go`）にあります。**9本が1つの予算を分け合う**ので、**読むときは絶対値ではなく予算比で読みます**（`a.Budget()` ＝ 合計）。範囲は `MinAbility = 1` 〜 `MaxAbility = 100`。

**継承は粒子的**（段階7b）——**子は遺伝子ごとに親のどちらかの値をそのまま引き継ぎ**、**予算は親の一方から継いで `BudgetInheritSpread`（30）の正規乱数を足し**、**継いだ配分をその予算に合わせてスケールします**。**突然変異はまれで大きい**（`MutationRate` 2% × `MutationStd` 40）＝**毎回小さく乗せると親の値が子に残らない**ため。**したがって突然変異は配分を動かすだけで合計を増やしません**。抽選するのは初期個体だけで、**予算は `GeneBudgetMean`（360）± `GeneBudgetStd`（30）**、**配分はディリクレ（`GeneInitAlpha` = 0.8）**。`BudgetHeritability = 0` で「予算が遺伝しない世界」に戻せます。

**実効値 ＝ 継承値 × 年齢補正 × 性別補正 × 足元の地面**（掛け算は `Agent.Ability` の1か所。`Gene()` は継承値のままで、**繁殖と測定はこちらを読みます**）。**ただし「どれだけ入るか」を決める側（`MaxVitality` / `MaxSpeed` / 記憶の枠 / 手の数）は `capacity` を通り、足元の地面を読みません**（段階57。上限が地面で動くと、境界を1歩越えただけで自分の上限を超えた体力を持つ身体ができる）。

| 名前 | 意味 | 何に効くか |
| --- | --- | --- |
| `GeneAttack` | 体力あたりの戦闘効率 | 与ダメージ `AttackDamage × effort × 攻撃力/50.5`（`damagePerTick`） |
| `GeneDefence` | 打撃をどれだけ減らせるか | 守りのチャネル（段階8）。**`SkillWard` の天井**（段階62） |
| `GeneVitality` | 体力の器 | `MaxVitality × 体力/50.5`。**運べる数**（`carrySlots`）。**`SkillSwim` / `SkillFishWater` / `SkillPoison` の天井** |
| `GeneSpeed` | 脚 | `MaxSpeed × 速さ/50.5`。**`SkillRough` の既定の天井**（段階38a） |
| `GeneEvasion` | 打撃を丸ごと外させる確率 | かわしのチャネル（段階8）。高台の遮蔽（段階30a）もここに乗る |
| `GeneMemory` | 何人を、どれだけ長く覚えていられるか | 記憶容量 `MemoryCapacity × 記憶/50.5`、1tickの取り込み上限（容量 × `MemoryBandwidthShare`）、忘却の速さ `(100 − 記憶)/49.5` 倍。**`SkillForage` / `SkillScribe` の天井** |
| `GeneRationality` | 世界を正しく読む力 | 他者の強さ・魅力の推定に乗るノイズ `(100−理性)/100 × JudgementNoise`。比較検討の長さ（`PatienceRationality`）。**`SkillThrow` / `SkillFishLand` の天井** |
| `GeneIntelligence` | 手を選ぶ力 | 効用の評価に乗るノイズ `(100−頭の良さ)/100 × ChoiceNoise`。**`SkillHarvest` / `SkillCook` の天井**。※戦略の幅の経路は既定で無効（`StrategyDepthUnlock = 0`） |
| `GeneAttractiveness` | 配偶者から見た値打ち | `fitness` ＝ 魅力遺伝子 ×(1−`FitnessConditionWeight`) ＋ 体調 × `FitnessConditionWeight`（既定 0.35）。**実能力は一切入りません**（強さと「良い相手に見えること」を1つの値に兼ねさせない）。**`SkillTrinket` の天井** |

**ヒント枠（`hintSlots`）も同じ予算から買います**（段階12c）——`Genome` の末尾に枠ぶんが足され、**同じ継承則で継がれ、空の枠でも予算を食います**。**枠の中身（言い伝えかスキルか）は A' 側**です。

## A'. 予算を食わない遺伝形質

**「量」ではないもの、または予算と取引させたくないものがここに入ります。** どれも**親から継ぎ、突然変異で漂い、予算を1ミリも食いません**。

| 名前 | どこ | 継承 | 経験で変わるか |
| --- | --- | --- | --- |
| **好み3つ**（`RiskWeight` / `CompetitionWeight` / `ShockRisk`） | `Agent.lore` | **粒子的＋変異**（`LoreMutationStd` 0.08、初期の広がりは `LoreInitSpread` 0.15） | **変わらない**——**正解が世界に無いので学べない**（段階12a）。ただし**伝承（`meet`）で歩み寄る** |
| **子の値打ち**（`mateWeight`。段階94） | `Agent.lore` | **粒子的＋変異**（初期の広がりは `MateWeightSpread`。**既定は0＝全員同じで、乱数を1つも引かない**） | **変わらない**（好みと同じ立場）。ただし**伝承で歩み寄る**——**実測で初期の広がりの3分の1しか残らない** |
| **判断の揺れ幅**（`noiseWeight`。段階95） | `Agent.lore` | **粒子的＋変異**（中心は `NoiseWeight` = 1、初期の広がりは `NoiseWeightSpread`。**既定は0＝全員同じで、乱数を1つも引かない**） | **変わらない**（好みと同じ立場）。ただし**伝承で歩み寄る** |
| **信念2つ**（殴り返される度合い・求愛が通る確率） | `Agent.lore` | **`LamarckRate`（既定0）でだけ継ぐ**＝既定では子は世界の事前値から始まる | **変わりうるが既定では動かない**（`LearningRate` = 0。真の殴り返し率 0.15 を知ると個体数が半分になるため。段階12a） |
| **ヒントの中身**（状況×行動×重み） | `Agent.hints` | **枠単位で継ぐ** | **模倣で入る**（空き枠にコピー。段階12c） |
| **スキル**（`SkillKind` と習熟度） | 同じ `hints` の枠 | **枠ごと継ぐ** | **出生地・観察で入り、比べて上書きする**（段階38a）。**実効値は min(名目, 適性遺伝子/100)** |
| **体内時計の位相**（`chronotype`） | `Agent` | **親から丸ごと＋`ChronotypeMutation` 0.08 の漂い** | 変わらない（段階18。**円周上の方向なので予算に入れない**） |
| **味の位相**（`taste`） | `Agent` | **親から丸ごと＋`TrinketTasteMutation` 0.08 の漂い** | 変わらない（段階84。**同じ理由**） |

---

## B. ノード固有の状態（遺伝しない）

**状態量として数えられているのは体力・空腹度・成熟度と、段階40 が足した所持品だけ**です（**世界の食料を1本に数えるかどうかで、リポジトリ内に「4つ目」と「5つ目」の両方の書き方が残っています**）。**所持品だけが帳簿で書けなかったもの**——**世界のアイテムそのものが可逆に動く**ため（#65）。**残りは帳簿・固定参照・毎tick書き直される写し**で、状態量ではありません（この区別は意図的です——帳簿で済むものを状態量にしない、というのがこの project が繰り返してきた判断）。

| 名前 | 既定の初期値 | 意味 |
| --- | --- | --- |
| `Vitality` | 初期集団は60〜100、子は `ChildVitality` | 体力。0で死亡 |
| `Hunger` | 初期集団は0〜`SatiatedHunger`、子は `ChildHunger` | 空腹度。時間で上昇 |
| `Maturity` | 初期集団・外敵は1、子は0 | 成熟度。**食事で進み、飢えていれば止まる**。能力の実効値（年齢補正の若い端）と、繁殖できるかどうかを決める |
| `carried` | 空 | **手の中のもの**（段階40）。**世界の食料の枠に数えられ、腐り、死ねば落ちる**。**重さは移動コストに掛かり、枠は `CarrySlotted` / `CarrySlotsWeigh` が決める**（段階71・80a） |
| `Age` | 0 | tick数。`TicksPerYear` で年に直る。**`SenescenceYears` を過ぎると能力の実効値が落ちていく**（`NODE.md` 11-1） |
| `Lifespan` | 初期集団は `MaxLifespan` の50〜100%、子は `MaxLifespan` | 寿命の残量。過小・過度な食事で背景処理（`World.spendLifespan`）が減らす。0で死亡（`Stats().AgingDeaths`）。**`Perception` に載らず、ノード自身は知らない**（C-2b） |
| `Generation` | 0 | 世代。子は `max(親) + 1` |
| `Sex` / `Species` / `Kind` / `HomeRegion` | 生まれたとき | **固定参照**（性別・種族・外敵の種類・湧いた地域）。**一度決まったら動かない**（段階59・64） |
| `GuardianID` / `RearingTimer` | 0（子は**母**のIDと `ChildRearingTicks`） | 育児期間（段階53a で母に固定）。子はこの間、親から `RearingRadius` 以上離れると戻る。**`RearingUntilGrown = true` のときは `RearingTimer` を読まない**——終わりを決めるのは `Maturity` |
| `PartnerID` / `PairTimer` / `CooldownTimer` / `courtStartTick` | 0 | ペアの状態と、**配偶者を探し始めてからの時計**（段階28。求愛のたびには戻さない） |
| `ParentIDs` / `ChildIDs` | — | 血統リンク。**直系の親子に初期好意（`AffinityKin`）を入れる**のと、**人が操作する血統の乗り換え**（段階19）に使う |
| `opinions[相手ID]` | — | 相手ごとの記憶。`Risk`（失った体力の累積・減衰する）、`Affinity`（好意・減衰する）、`Strength`（強さの推定）、`Variance`、`Samples`。**枠の数は記憶容量で有限**（`NODE.md` 8節） |
| `regions` / `stores` | — | **場所についての信念**（地域の豊かさ・行きにくさ・危険＝段階15b・29・35、倉庫の在りか＝段階50）。**人の記憶の枠は1つも食わない**（#41。**somewhere is not somebody**） |
| `looks` | — | **見た目から強さを読む直線**（段階10）。**誰か1人の記憶ではないので枠を食わない** |
| `recentFood` / `dread`・`cheer` / `fedSum` / `seed` | 0 | **読み出し時に遅延減衰する帳簿**——飽き（段階16）、気分（段階54・既定オフ）、食べてこられた実績（段階72）、腹の中の種（段階17c・既定オフ）。**どれも状態量ではない** |
| `Nursing` / `footing` / `adornWant` / `spare` | — | **毎tick書き直される「事実の写し」**（段階66・57・84）。**蓄積しない**ので状態量ではなく、**`Agent` に置いてあるのは `Ability` などが世界を知らないから** |
| `hitBy` / `engageID` / `openUntil` / `criedAt` / `rejected` / `dearID`・`lostDX`… | — | 戦闘と求愛と発信の短期の覚書。**`openUntil`（外した隙・段階24）と `Lifespan` は `Perception` に載らない**＝ノードは自分について知らないことがある |

**「経験で変わる個体固有の値」はもう `opinions` だけではありません**——**場所の信念・倉庫・見た目の直線・飽き・気分・スキル・ヒント・信念**が同じ立場にあります。**ただし枠を食うのは人の記憶（`opinions`）だけ**で、**場所は人ではない**というのがこの設計の線です（#41）。

---

## C. 世界のパラメータ（`engine.Config`、全ノード共通）

**330個あります**（2026-09-16 に数え直し。`Width, Height` のように1行に2つ書かれているものも1つずつ数えています）。**増えたぶんのほとんどは「1つの規則を切れるようにするためのフラグ」**で、`CLAUDE.md` の「ルールの切替は `engine` の分岐ではなく `Config` の書き換えで表現する」という方針の帰結です。「効く先」列は、その値が**世界の側の事実**なのか、**ノードの身体**なのか、**ノードの判断**なのかの区別です。

### C-1. 世界そのもの

| 名前 | 既定値 | 効く先 | 意味 |
| --- | --- | --- | --- |
| `Width` / `Height` | 800 / 600 | 世界 | 世界の広さ（`cmd/devview` は 820 / 660 で上書き） |
| `Seed` | 1 | 世界 | 乱数シード。同じなら常に同じ結果になる |
| `InitialPopulation` | 60 | 世界 | 開始時の個体数 |
| `InitialFoodItems` | 70 | 世界 | 開始時の食料数 |
| `MaxPopulation` | 240 | 世界 | 個体数の上限（安全網。通常は張り付かない） |
| `MaxFoodItems` | 110 | 世界 | 食料数の上限 |
| `FoodSpawnRate` | **0.20** | 世界 | 1tickあたりの食料湧き数。**世界の性格を決める最重要パラメータ**。高いと誰も争わず能力に選択圧がかからない |
| `BoundaryMargin` | 8 | 世界 | 壁からこの距離で跳ね返る |

### C-2. ノードの身体（代謝・移動）

全ノードが同じ身体を持ちます。**ここに個体差は無い**（＝体格差や体力の上限差は今のところ存在しない）。

| 名前 | 既定値 | 効く先 | 意味 |
| --- | --- | --- | --- |
| `MaxVitality` | 100 | 身体 | **基準となる**体力の上限。実際の上限は `MaxVitality × 体力遺伝子 / 50.5`（遺伝子が真ん中なら基準どおり） |
| `MaxHunger` | 100 | 身体 | 空腹度の上限 |
| `BudgetUpkeep` | 1 | 身体 | **大きな身体を動かす維持費**。空腹の進み方が `HungerRate × (1 + BudgetUpkeep × (予算/平均予算 − 1))` になる。予算の2倍の個体は2倍の速さで腹が減る。**0にすると予算が世代とともに際限なく上がる**（実測: 50000tickで 360→588、まだ上昇中）|
| `HungerRate` | 0.05 | 身体 | 1tickあたりの空腹上昇 |
| `InitialEnemies` | 10 | 世界 | 開始時の外敵の数 |
| `EnemyBudgetMean` / `EnemyBudgetStd` | 520 / 90 | 遺伝 | 外敵の予算の抽選範囲（人間は 360 ± 30）。**大きいほど倒しにくく、倒したときの肉が多い**——集団で狩る意味はここから出る。**2026-09-03 に据え置きで確定**: 400／700／標準偏差180 のどれでも参加人数は 1.22〜1.25 で動かず、動くのは人間の個体数だけだった |
| `EnemySpawnTicks` / `MaxEnemies` | 500 / 12 | 世界 | 外敵が地図の外から入ってくる間隔と上限。**0にすると外敵は自分たちの繁殖だけになる**が、この広さの世界では捕食者が獲物を食い尽くして共倒れになる（実測: 40体で人間が60→17、外敵は4500tickで全滅） |
| `MaxMeatItems` | 140 | 世界 | 肉の上限（植物とは別枠）。**同じ枠にすると、大量死のあとに肉が枠を埋めて植物が湧かなくなり、植物食の種族が餓死する** |
| `MeatPerBudget` | 120 | 世界 | 死体が肉を1個落とすのに必要な予算。**大きい個体ほど多く落ちる**——集団で狩る理由はこれだけ。**段階39a で下げて測ったが既定は据え置き**: 60（2倍の肉）は治りが無い世界では個体数 −13.4 \*、30（4倍）は −44.0 \*\*\*。**ただし 2026-09-09 に符号が2度ひっくり返った**——**治りが入ると +7.4 \***、**さらに余剰を開くと +13.6 前後**（`meatmore` 対 `baseline`）。**同じ数値の良し悪しが、周りの規則で決まる例。** 既定を動かさないのは、食料の総量に触る変更を段階41 の測定に混ぜないため（上げるなら独立した条件として）。**一番筋の通る読みは「肉は食料が湧く場所ではなく死んだ場所に落ちるので、集団が散る」**（最近傍距離 +19.7 \*\*\*、隣人 11.2 → 6.6、出生 −107 \*\*\*）。**治りが既定になった世界では逆に個体数 +7.4 \*** で、`packSize` 2.25・`halfLife` +28.7 \*\*——**段階41（余剰）はこの組み合わせの上で測る** |
| `MeatClaimTicks` | 400 | 世界 | 倒した者が肉を独占できるtick数。**「近くにいる全員が食べられる」にすると、殴らずに待つのが最適手になって誰も狩らなくなる**（`ENEMY.md` の分配ルールの表） |
| `HuntCreditTicks` | 200 | 世界 | 「倒すのに参加した」と認められる、直前の打撃からのtick数 |
| `MeatSpoilTicks` | 900 | 世界 | **死肉が腐って消えるまで**（肉と、**釣り上げた魚**＝2026-09-10。下記 `LandedFishKeeps`）。**これが無いと誰も食べない肉が食料の上限を埋め、植物が湧けなくなる**（実装当初に個体数を2割削った） |
| `SkillThrowRelief` | 1 | 身体 | **投げる腕前が距離の目減りをどれだけ取り返すか**（段階47。0 で「学ばれ、枠を取り、何も買わない」対照）。**天井は理性**（パワーは選ばない＝#71）、**出生地の種は地域に落ちている石の数**（地面の種類ではなく＝1つの読みから2つのスキルを種付けしない）。**的の大きさは先に書いた**: 腕の長さの命中 0.90 対 実測 0.64＝**距離が奪う26ポイント**が上限。**実測: 命中率 +0.02〜+0.03 \*\*\* だが集団には届かない**（保有率 4〜8%・実効 0.08〜0.12＝**積は 0.3%**）。**ただし強い用量で `shRationality` +0.01 \*・`dRationality` +2.24 \*＝スキルが自分の天井の遺伝子を買わせた初めての兆し** |
| `LandedFishKeeps` | **false** | 世界 | **釣り上げた魚が腐らないかどうか**（2026-09-10）。false（既定）で**釣り上げた瞬間に死肉の時計が始まる**（`MeatSpoilTicks` を共有＝**死んだ魚と死体は名前が違うだけの同じもの**なので専用の数値を作らない）。**手の中でも岸に落ちていても腐り**、**世界が水に置いた魚には時計が無い**（生きているので腐らない）。**true が段階42・43 の全数値が測られた世界**。**大きさは釣り上げられた魚の3%**で、対で測って個体数 −2.75 ± 4.42（誤差内）。**副作用の向きが面白い**——**腐る世界のほうが川の魚が多い**（`fishItems` +2.18 \*\*）＝**手の中の魚は枠を占めたまま川から消えている**ので、腐ると枠が空いて世界がまた生やす |
| `Coins` | **0** | 世界 | **世界を建てるときに撒くコインの枚数**（段階51）。**0 でお金の無い世界**。**一様に撒き、地域には紐づけません**（#73＝コインの地理は「居場所を動かすのは食料だけ」という4度の測定に反する条件を増やす）。**総量は不変**（湧かず消えず、手から手へ）。**食料の枠は食いません**（段階45 の石と同じ扱い＝テストで固定）。**注意: 撒くのに乱数を引く**ので、**コインの無い腕とは乱数列が違います**——対照は「撒いたうえで `CoinValue = 0`」を使うこと |
| `CoinValue` | **0.5** | 世界 | **コインの値打ち＝「足りなくなった瞬間の食事の価値」に掛ける割引**（段階51、#73）。**この1つの数が段階のすべてを決めます**（`PLAN.md` の予想どおり）——**1.0 では誰も得をせず売買がほぼ止まり**（1実行に0.46回）、**1.0 超はお金が食料より価値を持つ＝禁じた3つ目の目的**、**0.2 では買い手が得をして売り手が断る**（売買3.58 に対し断り11.54）。**0 が偽薬の腕**（コインはあるが誰も値打ちを認めない）。**成立を支えているのは2つだけ**——**コインは持ち主の尺度で測られる**（空腹な買い手と満腹な売り手で値打ちが違う＝取引がゼロサムでない）ことと、**コインの重さがゼロ**であること |
| `CoinPricedCertain` | **false** | 世界 | **コインを「確実なもの」として採点していた世界に戻す**（段階51 の値付けの非対称。段階67 で発見、2026-09-13 に修正）。**旧実装は `addCoins` を `Chance = 1`・競走なしで採点し、重さは本当にゼロなので課されなかった**——**同じ地面の木の実は `CarryValue × 混み具合/3` に競走の勝率と運ぶ代価が掛かる**ので、**`CoinValue` も `CarryValue` も 0.5 である以上、コインは「競走も重さも無い食料」として食料に厳密に勝つ**（実測: **両方が見えている決定の 70% でコインが勝つ**）。**直した形は「コインが食事になるには2つの条件が両方成り立たなければならない」**——**後で食べ物に困ること**（`CarryValue`）と**そのとき売ってくれる誰かがいること**（`CoinValue`）——**なので両方の割引を掛け、地面のものと同じ競走を課す**（重さは課さない＝コインに残る唯一の優位で、それは設計どおり）。**`willSell` / `coinWorth` は触っていない**（手の中の2つを比べる場面では「後で要るか」が約分される）。**測定（遊ぶ地図・96シード）: コインを拾う決定は半分になり**（0.155 → 0.072）、**手の中のコインは2割減り**（`coinHolders` 0.25 → 0.21 \*\*）、**市場も個体数も動かない**（`sales` −0.18 ± 0.42、個体数 +1.43 ± 4.11）＝**段階51・68 の記録はそのまま読める**。**先読みの世界（段階67）では `gifts` +59.21 \*\*・`cooked` +14.46 \*\*\*・`storeIn` +55.85 \*\*・`halfLife` +123 \*** だが、**手はコインで塞がったまま**（`coinHolders` +0.02＝不動）＝**コインを手放す手段がこの世界に無いから**。**true が段階51・68 の全数値が測られた世界** |
| `Dropping` | **false** | 世界 | **「置く」という語があるかどうか**（段階70、`ActDrop`）。**false が段階69 までの全世界**で、そこでは語が候補に出ず、裏の算術も走らない。**段階40 は「置く」を作らないと決めていた**（手の中は距離0の食事なので詰まらない）——**食べられないものが3つ（石・コイン・本）入った今はその理由が成り立たない**。**値打ちは3つの既存の数字**（運ぶのをやめて浮く `lug` ＋ 手が空くことの値打ち − 手放すものの値打ち＝`willSell` の売り手側）。**測定: 世界によって符号が変わる**——**平坦 +7.07 \*\*（96シード。`gen` +0.24 \*\*、`dIntelligence` +3.65 \*）／経済のある遊ぶ地図 −6.29 \*（96シード。贈与 −37.99 \*、調理 −28.67 \*\*\*、倉庫 −14.68 \*\*、売買 −2.60 \*\*\*）**＝**この世界の交換はすべて手を通るので、手を空ける語は経済を削る**。**段階51a の「コインで塞がった手」には届かない**（先読みの世界では20000tickに1.88回）。**既定がオフなのはこの符号の割れ方のため**（地図の作者が決める＝`Stones` / `Coins` と同じ立場） |
| `StoreCapacity` | **6** | 世界 | **1つの倉庫が持てる個数**（段階50）。**0 で倉庫そのものが世界から消える**（置けず、選択肢にも出ない）。**倉庫は `Config` では作られません**——地形や地域と同じく**世界を敷く側が置く地物**（`World.SetStore`）なので、この値が非0 でも既定の世界には倉庫が1つもありません。**容量は縛りになっていません**（24 にした世界は6 の世界と1ビットも違わない＝**誰も溜め込まない**） |
| `MaxStores` | **16** | 世界 | **世界が持てる倉庫の数**（段階50）。#41 の免除（場所の記憶は人の記憶枠を食わない）が成り立つのは、**数が地域と同じ桁に収まるかぎり**なので上限を置く |
| `StoresKnownToAll` | **false** | 世界 | **全員が全部の倉庫を生まれたときから知っている対照**（段階50）。**「保管は残し、知ることだけを取り去る」腕**で、段階49 の `WaresSeen` と同じ形。**これがこの段階の答えを決めた**——**知識を学ぶ世界は倉庫なしより悪く**（−4.69 \*）、**この腕は良い**（+3.71 \*）。**直接の対で +8.40 \*\*** |
| `StoreFindLooks` | **50** | 世界 | **知らない倉庫を何回見たら気づくか**（段階50）。0 でこの道が閉じる。**計画が挙げた3つの道（継承・目撃・発信）はどれも「すでに知っている誰か」を要するので、これが無いと何も始まりません。** **確率ではなく回数で数えるのは測定上の判断**——乱数を引くと倉庫の無い対照と別の乱数列になり、小さな差が読めなくなる（段階45 の罠。**確率版では個体数 +11.33 \*\*\* と出て、回数版では +1.67 ± 2.85**） |
| `StoreInheritStrength` / `StoreWitnessStrength` / `StoreCryStrength` | **3 / 1 / 0.5** | 世界 | **知る道ごとの1回の重み**（段階50）。地域の知識と同じ記録に載るので**単位は「地面を1回見たぶん」**で、**忘れる速さは `RegionForgetPerTick` を共有**（新しい率を作らない）。継承が一番重いのは育児期間ずっと一緒にいるから、聞いた話が一番軽いのは**この世界の伝聞がいつも一次情報より軽い**から。**どれかを0 にするとその道が閉じる**（＝道ごとに分けて測れる）。**実測で一番広いのは発信**（466 対 目撃224 対 継承134 対 発見114。ただし1回の発信が聞いた全員に全部の場所を教えるので構造的に多い） |
| `StoreValue` | **0.5** | 世界 | **倉庫に入れたものがどれだけ返ってくると見積もるか**（段階50）。運搬の `CarryValue` と同じ割引だが**別の数**——**手の中のものは他人に取られず、倉庫のものは取られる**ので同じ賭けではない。**0 で「倉庫はあるが誰も使わない」偽薬の腕**になり、それは**倉庫の無い世界と1ビットも違わない** |
| `CarryPricedBackwards` | **false** | 世界 | **「後のために取っておく価値」の符号が逆だった世界に戻す**（段階40 のバグ。段階50 で発見）。**段階40 は `keep = (今の危険) − (足りなくなったときの危険)` と書いており、これは損の量で、意味していたのは「そのとき食べられることの得」**。実測: 既定の世界の1実行で採点された運搬候補 138,310 件のうち**正の値を持つものは1つも無い**（勝った133件も負の値で勝っている＝評価ノイズ）。**直すと拾う回数が25%増え、それ以外は何も動かない**（`takeRate` +0.92 \*\*\*、個体数 −4.33 ± 6.03。遊ぶ地図では +5.50 \*）。**true が段階40〜49 の全数値が測られた世界** |
| `OfferTicks` | **0** | 世界 | **手の中のものを掲げて知らせるのにかかるtick数であり、掲げていられるtick数**（段階49）。**0 で語彙から消える**（誰にも選択肢として出ず、誰も掲げず、誰の手も読まれない）＝この段階の対照。**値段と届く長さが同じ1つの数**（長く掲げるほど時間を払い、そのぶん「来る側が間に合う」確率が上がる）。**`TriggerIdleTicks`（40）より長くすると、退屈のトリガーが鳴き声を途中で切る**——90 で試すと `giftsCried` は 0.06 → 0.16 に上がるが、個体数は動かない。**既定を0 にしたのは測定の結論**（下記 `WaresSeen`） |
| `WaresSeen` | **true** | 世界 | **掲げられたものを他人が読めるかどうか**（段階49）。**false は「叫びはするが誰にも聞こえない」対照**で、鳴き声は同じだけ選ばれ、同じだけ時間を払う。**段階34 の `DrownKnown`・段階39 の「治ると知らせない肉」と同じ形の対照**。**これがこの段階の結論を決めた**——平坦な世界で餓死が減るのは `true` でも `false` でも同じ（−5.00 \*\* / −4.42 \*）＝**減らしているのは「立ち止まっていること」であって「誰かが聞いたこと」ではない**。**聞こえる世界のほうが個体数は少ない**（対で −9.42 \*） |
| `AffinityGift` | **6** | 世界 | **手に持っているものを渡したときに双方が得る好意**（段階48。共同で仕留めたときと同じ量）。**0 で「語彙はあるが誰も渡さない」対照**になる。**効用式での値付けは既存の `LoreValue` × 買える信頼の増分**で、**信頼は飽和するので他人への贈り物が一番高い**（実測: **95%が他人へ**）。**既定の世界では得**（個体数 +9.35 \*、`killShare` −0.01 \*、`fightRatio` −0.04 \*、`dietVariety` +0.04 \*\*）。**食料の乏しい地図では −11.27 \*\*** |
| `AffinitySale` | **0** | 世界 | **売買が成立したときに双方が得る好意**（段階68）。**0 は段階51 の世界そのもの**——**段階51 は「贈与は一方的だが売買は施しではない。両方に好意を書くと2つの規則が1つになって測定が読めなくなる」として意図的に書かなかった**。**開けたのは、その測定が「足りないのは買い手ではなく売り手」と言ったから**——`willSell` は `CoinValue×keep > max(meal, keep) − lug` で、`max(meal,keep) ≥ keep` なので**売り手は毎回 `(1−CoinValue)×keep` だけ損をする**。**値付けは `saleGoodwill` ＝ `LoreValue × 買える信頼の増分`（`addGive` と同じ式）で、`willSell` の売り手側と `addBuy` の買い手側の両方が同じものを見る**。**天井は選べない**——信頼は `AffinityTrust` = 20 で飽和するので、**いくら大きくしても値打ちは `LoreValue` = 9 を超えない**。**実装前に数えた**: 断る売り手 54.3%・平均の隔たり **15.78**・**好意2.70 で閉じるのは断りの12.6%、9.00（天井）で36.6%**。**実測（遊ぶ地図・48シード）**: 6 → `sales` +1.00（1.7 SE、予想どおり出ない）／**20 → +2.88 \*\*\*（5.12 → 8.00）・`salesRefused` −18.62 \*\***／40 → +3.48 \*\*\*（20 とほぼ同じ＝天井）。**代価は個体数 −9.44 \*・`starveRate` +0.41 \*・`gifts` −35.54 \*** |
| `SaleGoodwillKept` | **true** | 世界 | **売買が生んだ好意を実際に記憶に書くか**（段階68 の対照）。**false は「売り手も買い手も同じ好意を値付けして同じ理由で売り買いするが、誰の記憶にも何も書かれない」**＝`ParentFeedWasted` / `DrownKnown` と同じ形。**これが代価の出どころを名指しした**——**false の腕がこの群で一番売れていて（9.06 対 8.00）、個体数の落ち込みの62%を取り戻す**＝**払っているのは取引ではなく、好意が記憶に残ること**（**好意は休息を安全に見せる**＝段階9 の露出の割引。`shMemory` を6度探して出た「好意は net で損」と同じ壁）。**ただし対照の差はどれも 1.1〜1.7 SE で、向きはそろっているが1本ずつでは確定していない** |
| `Books` | **false** | 世界 | **この世界に本というものがあるか**（段階69）。オフなら乱数を1つも引かない。**本は3つ目の「拾えるが食べられないもの」**で、食べられない・腐らない・重さゼロ・手を1つ取る・食料の枠を食わない |
| `BookSubject` | `BookSkills` | 世界 | **本が何について書かれるか**。`BookSkills` が指定どおりの形で、`BookPlaces` が倉庫の場所。**2つあるのは、実装前に数えて前者に落ちる場所が無いと分かったから**——**ヒント枠は常に満杯**（1.98 / 1.98、空きは 0.000）で、**世界で最良の本を全員が無料で読めても助かるのは 3.2%・増える習熟度 0.0054**。**実測でスキルの本は 4.29冊書かれて1回も読まれない**（`booksRead` +0.00 ± 0.00）。**場所の本は1冊あたり4.1回読まれる**が、**`storeKnown` は動かない**（+0.01＝薄れる知識は運んでも積み上がらない） |
| `WriteTicks` | 40 | 世界 | **書くのと読むのにかかるtick数**。代価は時間だけ（`CallTicks` / `OfferTicks` / `CookTicks` と同じ形）。0 で「語はあるが誰も書かない」 |
| `BookFidelity` | 0.7 | 世界 | **本が書き手の知っていることのどれだけを運ぶか**。1未満なのは、又聞きが直に見たことより弱いのがこの世界のどこでも同じだから |
| `BookValue` | 0.5 | 世界 | **読むことの割引率**（`CarryValue` / `StoreValue` / `CoinValue` と同じ立場）。**16倍（8）にすると読まれた回数は 9.08 → 22.50 と倍以上になるのに `storeKnown` も個体数も動かない**＝**縛っているのは値段ではない** |
| `BookSurvivesReading` | **true** | 世界 | **読んでも本が残るか**。**これがこの規則の中心**——**残るからこそ、読み終えた本は持ち主にとって値打ちゼロで他人には正という非対称が成立する**。**実測: 残る版は1冊あたり 4.1回読まれ、消える版は 0.6回**（同じ枠数で**6.6倍**）＝**この段階で唯一はっきり効いた構造**。false は「本が食料と同じ rival な財である」対照 |
| `SexBias` | **全ゼロ** | 世界 | **遺伝子ごとの性差**（発現値 ＝ 継承値 × 年齢補正 ×（1 ± これ）。正が母寄り）。**掛け算は `Agent.Ability` の1か所だけ**で、**`Genome` と継承には触らない**（両性は同じ遺伝子プールから出る＝#4）。**測定した配分**（#85。役割から逆算）: 母＝理性・防御力・記憶／父＝パワー・速さ・頭の良さ／体力・回避率・魅力は中立。**測定（48シード、同じ役割で配分なしの世界が基準）: 0.15 で個体数 −15.96 \*\*、0.30 で −31.60 \*\*\***（**用量にほぼ比例**）。**そして役割どおりの配分（−15.96）は、性を入れ替えた配分（−7.94 \*）より悪い**——**役割が要求する3本（防御力・理性・記憶、予算比 各0.06）はこの世界が一番払わない3本**で、**取り上げる2本（速さ 0.22・頭の良さ 0.11）が一番払う2本**だから（母の寿命 −212.79 \*、入れ替えると +282.48 \*）。**`shMemory` は強い用量で +0.01 \*、入れ替えると −0.01 \* と符号が役割についてくる**（**5度不発だった量の6度目**。測っているのは継承値なので本物の選択）。**どの腕でも `clumping` +0.12〜0.16 \*\*\*・`halfLife` +10〜11 \***。**既定をゼロにしたのは、手で入れた差は発見ではないことと、個体数の代価** |
| `GuardianIsMother` | **true** | 世界 | **子がそばに留まる相手を母に固定する**（段階53a）。**false が 2026-09-11 以前の世界**（番号の若いほう＝実測で 50.5% が父）。**妊娠・出産・育児が同じ身体の仕事であることを構造にしたもの**で、**段階53 の遺伝子の振り分けはこの役割から逆算します**。**出産の代金の折半は変えていません。** **測定: 給餌オフの既定の世界で個体数 −4.31 ± 6.68**（`clumping` +0.10 \*\*、`ageMale` +356.75 \*、**`shMemory` +0.01 \***）|
| `ParentFeedByKin` | **true** | 世界 | **ガーディアンでない親も、血縁で圏内なら子に分け前を渡せる**（段階53b）。**false で「ガーディアンだけ」＝53a と組み合わせると父が給餌から完全に締め出される**（`mouthsToFeed` が `GuardianID` 一致で弾くため）。**新しい所有も状態も作らず既存の `share()` を使う**。**実装前に数えた的**: ガーディアンが圏内 0.774／**ガーディアンでない親も 0.475**（生存時 0.516）＝**給餌の機会が約1.6倍**。**測定（`ParentFeedShare = 0.5` の世界、48シード、基準は 53a 以前）: 53a 単体 −2.96 ± 4.63 に対し 53a+53b +1.19 ± 4.62**（どちらも誤差内だが向きは予想どおり）。**はっきり動くのは子の死亡割合 −0.02 \*\*\*・`grewUp` +0.01 \* と、父の寿命**（53a 単体 +259.60 \*\* → 53b で +132.81 \* ＝**役の代価のおよそ半分を給餌が返す**）|
| `LonelyValue` | **0** | 世界 | **見失った相手の方角へ歩くことの値打ち**（段階55a）。**0 で世界から消える**（何も覚えず、候補も出ず、乱数も引かない＝以前の世界と1ビット同じ）。**形は段階15b の「良い地域へ行く」と同じ**（さまよう候補が1つ増えるだけ）。**測定（48シード）: 発火は十分すぎるほどする**（全決定の7%＝15b の 0.05 より多い）**のに、集団の指標はどの用量でも下がる**（`halfLife` 5 で −7.64 \*／20 で −6.74 \*／60 で −14.33 \*\*\*）。**餓死も増える**（20 で +6.96 \*\*、60 で +21.73 \*\*\*）。**機序は「探しに行く身体は今いる集団を捨てる」**（探す相手は1人、捨てる集団は数人）|
| `LonelyWrongWay` | **false** | 世界 | **対照**（段階54 と同じ形）。**同じ候補・同じ重み・同じ代金で、方角だけ逆**。**乱数を引かないので本体と同じ乱数列を走る**（新しい方角を引く対照はそれができない＝段階45 の罠）。**実測でこれは本体の6倍悪い**（`clumping` −0.36 \*\*\* 対 −0.06 \*、`halfLife` −12.82 \*\*\* 対 −6.74 \*）＝**覚えていた方角は実在する情報**。**それでも本体は「探しに行かない世界」に届かない**＝**情報が正しいことと、それに基づいて動く価値があることは別** |
| `LonelyHalfLife` | **400** | 世界 | **方角が半分に薄れるまでのtick数。** **十分古くなれば捨てる**（下限は `lonelyFloor` = 0.05。`Config` に置かないのは、これが世界の目盛りではなく「使う側のスコアのノイズより小さくなる点」だから）。**無いと「もう無意味な方角へ一生歩き続ける個体」が生まれます**。**4倍にしても良くなりません**（個体数 −2.79 ± 5.08、`halfLife` −6.81 \*）|
| `MoodWeight` | **0** | 世界 | **今の気分が `ShockRisk`（体力が減っていること自体の危なさ）の読み方をどれだけ傾けるか**（段階54）。**0 が偽薬＝この段階の前の世界と1ビット同じ**（乱数を1つも引かない。テストで固定）。**負の値が対照**——**同じ大きさの傾きを符号だけ逆にする**（怯えた身体が大胆になる）ので、**効いているのが構造なのか大きさだけなのかが分かれる**。**測定（48シード、用量 `MoodDreadGain` 30 /`MoodCheerGain` 3）: 気分あり +7.77 \*／符号を逆 −0.98／直接の対で +8.75 \***、出生 +33.79 \*\*。**そして決め手の対照**——**同じ平均の傾きを定数として与える腕**（`ShockRisk` を 0.965 倍）**は −2.35 ± 6.05 ＝何も起きない**。**効いているのは平均の大胆さではなく、直前に何があったかに連動していること**。**既定を0 にしたのは効き目が弱いから**（1.5〜2.0 SE）——**測定は「上げてよい」と言っているが、世界のベースラインを動かすには足りない**（`ParentFeedShare` と同じ立場）。**`cmd/devview -terrain` は 0.5** |
| `MoodDreadGain` / `MoodCheerGain` | **1 / 1**（測定した用量は **30 / 3**） | 世界 | **1回の打撃と一口が、その身体の上限に対する割合で気分をどれだけ動かすか**（どちらも上限1）。**分けてあるのは、2つが同じ大きさではないと実測で分かったから**——**既定の 1/1 では満ち足り 0.12 に対し恐れ 0.02 ＝6倍の開き**で、**気分はほぼ「機嫌が良い」側の定数**になる（食事は頻繁で身体の単位でも大きく、打撃はまれで小さい）。**片方を0 にすると段階12a の問いをそのまま訊ける**（恐れだけ／満ち足りだけ）。**実測: 恐れだけ +4.98 ± 6.20、満ち足りだけ +3.10 ± 6.20、両方 +7.77 \*** ＝**どちらか片方では足りない** |
| `MoodHalfLife` | **150** | 世界 | **気分が半分に薄れるまでのtick数。** **実装前に数えた**（最後に殴られてからの経過。平坦／遊ぶ地図）: **50tick 以内 0.199/0.115、150 で 0.346/0.229、300 で 0.466/0.342、1200 で 0.673/0.637、一度でも殴られた＝天井が 0.713/0.787**。**2/3 の時間立っている気分は定数で、定数は順位を1つも変えません**。**実測でも 600 まで伸ばすと効き目が消えます**（恐れだけの腕で 150 なら +4.98、600 なら +0.50）|
| `CookTicks` | **20** | 世界 | **手の中のものを調理するのにかかるtick数**（段階52）。**対価は時間だけ**（`CallTicks`・`OfferTicks` と同じ形＝#76。新しい並行の仕組みは作らない）。**60 にすると誰も調理しません**（1実行に0回）——**ただしその腕は候補を1つ増やして採点しているので乱数列が変わり、調理の無い世界とは比べられません**（対照は `CookVitality = 0` のほう＝そちらは候補を作らないので1ビットも違わない。**段階45 の罠の4度目**）|
| `CookVitality` | **0.5** | 世界 | **調理済みの品1つが治す量**（その身体の体力上限に対する割合。**肉の `MeatVitality` と同じ単位で、既定でわざと同じ値**）。**足し算にせず良いほうを採る**ので、**同じ値なら肉を調理しても何も増えません**——**調理が効くのは誰でも持っている食べ物のほうで、交換が成り立ちうるのはそちら**（実測 `cookedMeat` = 0.00）。**0 が偽薬の腕**（語彙はあるが候補にすら挙がらない＝**調理の無い世界と1ビット同じ**。テストで固定）。**測定（48シード、対で）: 個体数 +21.10 \*\*\*（77.4 → 98.5）＝1つの規則としてこの世界で最大**、出生 +85.60 \*\*\*、`halfLife` +20.14 \*\*、`neighbours` +3.65 \*\*\*、手渡し +42.71 \*\*\*。**代価は `killShare` +0.03 \*\*\*・`dietVariety` −0.06 \*\*\*・`dIntelligence` −3.06 \***。**遊ぶ地図でも +13.06 \*\***（25.1 → 38.2）＝**食料に触る規則で両方の地図が同符号になった初めての例**（段階44・48 は符号が反転した）。**用量: 0.1 では +5.8 どまり**（0 → 0.1 → 0.5 が単調）|
| `CookQuality` | **1** | 世界 | **何も知らない身体がどれだけ上手く調理するか**（0〜1）。**1 が既定＝誰でも調理できる**で、**下げるのは地図を敷く側の仕事**（段階43 が2つの釣りの成功率を1のままにしてあるのと同じ形）。**門ではありません**（#80）——知らない身体も調理はでき、`SkillCook` が買うのはその取りこぼしのぶんだけ |
| `CookSurvivesHands` | **true** | 世界 | **調理済みのものが人手に渡っても調理済みのままか。** `false` が**交換の半分だけを抜く対照**（段階49 の `WaresSeen` と同じ形）ですが、**受け取った側が20tick かけて調理し直せてしまう**ので上限の測定にしかなりません（実測 −5.56 ± 5.65 ＝誤差内、そして**調理の回数は +66 \*\*\* に増える**）。**穴の無い形は「渡す理由そのものを消す」対**（`AffinityGift = 0`）で、そちらの実測は**調理の値打ち: 渡せる世界 +21.10 \*\*\* に対し渡せない世界 +12.46 \*\*** ＝**5分の2は他人に渡せることから来ている** |
| `SkillCookRelief` | **1** | 身体 | **調理スキルの効き目**（0 で「学ばれ、枠を取り、何も買わない」対照）。**天井の遺伝子は頭の良さ**。**出生地からは何も読みません**——**どこで生まれても同じ値**で、差は跳躍と模倣だけ（**5度の測定が「地面から種をもらったスキルは居場所を動かさない」と言っているので、6度目を数えに行かない**という判断）。**測定（`CookQuality` 0.5 の遊ぶ地図、対照と対）: 分業は出ません**（`cookSplit` ＝調理と採取の習熟度の相関 0.01 ± 0.02）。**保有率 0.06〜0.08・実効 0.14〜0.16、個体数は −5.33 \*** ＝枠の代金に届かない。**ただし `dIntelligence` +4.98 \*・`shIntelligence` +0.01 \* ＝スキルが自分の天井の遺伝子を買わせた2度目の兆し**（1度目は段階47 の理性）|
| `Throwing` | **false** | 世界 | **石を投げられるか**（段階46。`cmd/devview -terrain` は true）。**既定オフで始めた理由**は、段階12a が「実在しない報復への恐れ（信念0.70 対 実際0.15）が世界を支えている」と測っているからで、**遠隔はその実際をさらに下げるはずだった**。**実測は逆で、`trueRetal` は上がった**（+0.01 \*）＝**石は逃げ道ではなく招待状**。**当たった予想は逃走のほう**（`escapeShare` −0.02 \*\*）。**個体数 −6.42 \***（弾の多い荒れ地の地図。遊ぶ地図では −2.9 で誤差内＝**供給が薄いので発火しない**） |
| `ThrowRange` / `ThrowDamage` / `ThrowHit` / `ThrowFalloff` | 70 / 6 / 0.9 / 0.6 | 世界 | 石の届く距離（**腕15 と視界130 の間**）、1発のダメージ（近接1tickの何倍か）、腕の長さでの命中、**距離で失う命中の割合**（**ここだけが段階47 のスキルの担当**）。**ダメージを3倍にすると世界は明確に悪くなる**（個体数 −11.90 \*\*、餓死率 +0.34 \*、`dPower` +3.99 \*） |
| `Stones` | **0** | 世界 | **世界を作るときに荒れ地へばら撒く石の数**（段階45。`cmd/devview -terrain` は 60）。**荒れ地の無い地図では1つも置かれず、乱数も引かない**。**補充しません**（消費するものがまだ無い＝段階46）。**食料の枠は食わない**（最初の実装では食っており、180個の世界は植物が生えずに壊れた）。**実測（遊ぶ地図、60個）: 石が視界にある個体は13%**（荒れ地だけの地図では54%）。**3倍にしても 0.15 にしかならない**＝**段階46 の発火頻度の天井はここ** |
| `SpecialtyShare` / `SpecialtySpread` | **0** / 0.6 | 世界 | **その地域に生えるもののうち「知らないと採れない作物」の割合**と、その地域差（段階44）。**既定は0＝地図の作者が入れる**（`cmd/devview -terrain` は 0.2）。**`SpecialtyShare` はこの段階の的の大きさそのもの**（38 の結び「スキルは打ち消す規則の大きさ以上にはならない」を、設定として書き下したもの）。**総量は増えない**（普通の植物の代わりに生える） |
| `SpecialtyCatch` / `SkillHarvestRelief` | 0.25 / 1 | 身体 | **知らない身体が採れる確率**（**0にしない**＝離散の門にしないため）と、**知っていることの効き目**（0 で「学ばれ、枠を取り、何も買わない」対照）。**実測（対照と対）: 個体数 +10.06 \*・出生 +33.79 \*\*・失敗率 0.38 → 0.30 \*\*\***。**ただし `specialGain` は ±0.00＝知っている身体はその地域に居着かない**（スキルが居場所を動かさないのは5度目）。**一番良い世界は作物が均等に生えている世界**（+14.35 \*\*）＝**比較優位を生むはずの偏りが、世界にとっては損** |
| `FishCatchWater` / `FishCatchBank` | **1 / 1**（＝必ず取れる） | 世界 | **魚を仕留められる確率**（段階43。水の中／陸から手を伸ばして）。**既定は1**＝段階42 の世界のまま（**風景の性質は地図の作者が決める**。`cmd/devview -terrain` は 0.75 / 0.3）。**逃げた魚は消えず、水のどこかへ移る**（失敗の代金は歩いた時間）。**この2つの差が「安全だが取れにくい／危険だが取れる」という取引の全部** |
| `SkillFishReach` / `SkillFishRelief` | 2 / 1 | 身体 | 陸釣り1点あたり届く距離が何倍になるか／**2つの釣りスキルの効き目**（0 で「学ばれ、枠を取り、何も買わない」対照）。**上手いとは多く獲れることで、少なくて済むことではない**——最初の版は1匹の値打ちを増やしており、**上手い身体ほど水にいない**という逆の結果になった（`HISTORY.md` 2026-09-09 の訂正）。**実測: 2つの地面の差が大きく習熟度が高い世界でだけ取引が成立する**（強い用量で `anglerSplit` +0.06 \*、`wadersWet` +0.06 \*、個体数 +5.73 \*）。**弱い用量では逆向きのまま** || `FishShare` | **0** | 世界 | **世界が出す食料のうち、水の中に湧く割合**（段階42。**既定は0＝地図の作者が入れる**。`cmd/devview -terrain` は 0.25）。**総量は増えない**（魚は植物の代わり）。**地域ごとの量は水の割合に比例**（調整する数値なし＝水のセルを一様に引く）。**実測（裸の川、48シード）: `onWater` 0.13 → 0.24 \*\*\***＝**この計画で `onWater` を一番大きく動かしたもの**、`dietVariety` +0.14 \*\*\*、**代価は個体数 −16.7 \*\*\*・溺死 +10.1 \*\*\***。**0.5 は世界を壊す**（個体数 27）。**遊ぶ地図（岸が豊か）では代価は −5.3 で誤差内**。**泳ぎスキルの的も大きくなる**（溺死 −4.35 \*・`drownRate` −0.13 \*\*。魚なしでは −1.83 \*／−0.03 \*） |
| `FishForAll` | false | 世界 | 外敵にも魚を許すか（段階42）。**既定は人間だけ**——外敵に2つ目の食料を与えると段階11 以降の個体数が別の測定になる。**実測では世界はさして変わらない**（個体数 −14.0 \*\*＝人間だけの −16.7 とほぼ同じ、餓死は −3.9 \*\*） |
| `MeatFromKills` | **true** | 世界 | **倒された死体だけが肉になる**（2026-09-22。ユーザの指摘「自然死で肉になるのはおかしい」から入り、測って既定になった）。**判定は kill の集計と同じ式**（直前の tick に殴られていて、溺死でない）なので、**肉になるものと kill と数えるものがズレません**。**実測（12シード × 200000）: 個体数 161 → 215 \*、外敵 41 → 57 \*\*、餓死 −507 \*\*、崩壊 0.08 → 0.00 \***。**供給は減りません**（`meatDropped` +594、有意でない）——**減るのは無駄**で、**腐る割合 0.21 → 0.03 \*\*\***・**食べられる割合 0.78 → 0.97 \*\*\***。**自然死の肉は誰も居ない場所に落ちるから腐る**。**4度目のベースラインのリセット**で、**旧世界の腕は `meatalways`** |
| `MeatSurplusFree` | **true** | 世界 | **倒した者の取り分を「運べるぶんまで」にするか**（段階41）。既定は開く。**新しい数値は作らず、取り分＝参加者の持てる数の合計**（最低1個。運搬と同じ関数）。**実測: 個体数 +6.21 \*・世代 +0.48 \*\*・腐敗 −0.01 \*、claim 無しで食べられる肉 0.53 → 0.69**。**claim を全部やめる版（`meatnoclaim`）は −4.33 で逆に悪い**＝**残すべきは claim、開けるべきは余剰**。**肉が多い世界ほど効く**（`MeatPerBudget` 60 なら +7.42） |
| `MeatVitality` | **0.5** | 世界 | **肉1個が治す体力**（その個体自身の上限に対する割合。段階39）。**食料が体力に直接触る唯一の経路**で、**0 にすると段階39 以前の世界に戻る**。**0.5 を既定にしたのは、これが4度空振りした群れ狩りを初めて動かしたから**（参加人数 1.36 → 1.81、共同での仕留め 34 → 95、48シード）。**1.0 でも差は誤差内**（1.84）なので小さいほうを採った——「倒す→治る→また倒す」のループに余地を残さないため。**代価は世界が荒れること**（`killShare` 0.74 → 0.83、`dIntelligence` −6.72 \*\*、`dPower` +5.86 \*\*、子の死亡割合 +0.04 \*\*\*）で、**個体数は −5.73 \*（誤差の範囲）** |
| `MeatHealKnown` | true | 世界 | 治りが知覚と効用式に載るか（段階39）。**false にすると肉は同じだけ治すのに誰も知らない**＝段階34 が作った「選択と選別を分ける」対照。**実測: 参加人数の上げ幅は +0.45 → +0.21 に半減し、餓死の減少（−19.12 \*\*\*）は丸ごと消える**——**この世界で初めて「知っていること」が測れる差を作った規則** |
| `MeatNutrition` | 1 | 世界 | 肉1個が下げる空腹度の倍率（段階39）。**対照専用**——2 にすると「2食ぶん腹にたまるが治らない肉」になり、**群れ狩りはほとんど動かない**（+0.06）＝**集まる理由は栄養ではなく治ること** |
| `CarryCapacity` | **1** | 身体 | **持ち歩ける食料の数**（段階40。体力遺伝子に比例。0 で段階40 以前の世界）。**誰でも1つは持てる**——遺伝子が買うのは「そのぶん安く運べること」と「2つ目から先」で、離散の門は作らない。**1 を既定にしたのは掃引がここで符号を変えるから**: 1 で個体数 +1.25（誤差内）・餓死のうち「直前に食料を見ていた死」が −0.04 \*、**2 で −5.73 \*、3 で −7.10 \***。**代価は重さではない**（無料で運べる腕も同じだけ悪い）**し、枠の会計でもない**（枠から外すともっと悪い）——**運ぶこと自体**（食べずに拾いに歩いた身体は、旅をしたのに空腹のまま） |
| `CarrySlotted` | **true** | 世界 | **手が「重さ」だけでなく「枠」でもあるかどうか**（段階71）。**true が段階70 までの全世界**。**false で離散の門が消え、重さだけが価格になる**（積載率の上限クランプも外れる＝5個目が2個目より重くなる）。**枠の根拠はどこにも書かれていなかった**——`canCarryMore` が弁護しているのは「誰でも1つは持てる」ことだけ。**支えていた唯一の測定（容量2で −5.73 \*）は `keepValue` の符号が逆だった 2026-09-09 の世界のもの**。**実測: 平均の枠数は 1.00 で、2つ持った身体は1体も無い**（＝「遺伝子が2つ目の枠を買う」は一度も起きていない）。**外すと遊ぶ地図で −14.98 \*\***（代価は運ぶ代価のほう＝重さ0の対照で +7.19 \* 戻る）**が、先読みの世界では経済が丸ごと戻る**（`coinHolders` 0.53 → 0.32 \*\*\*、`sales` 0.12 → 4.38 \*\*\*、贈与3.7倍、個体数の代価は誤差内）。**`carrySlots` 自体は残る**——段階41（死体の余剰）が別の問いのために読むので |
| `Trinkets` | **false** | 世界 | **それ自体のために欲しがられるものが世界にあるか**（段階82。**既定はオフ＝地図の作者が入れる**。`cmd/devview -trinkets`）。**食べられず・治さず・何も教えない**。**効用式では専用の目的 `Adorn`**（他の目的は全部手段なので混ぜない）。**測定（96シード）: 1実行に1484個作られ73%が持つのに、売買は −0.55 \*\*\*・1枚超の値段も −0.17 \*\***。**増えるのは贈与 +269 \*\*\***で、**贈与を止めると個体数 −19.99 \*\*\***（＝**渡せることで元を取っている**）。**個体数は既定の用量では誤差内**（−1.34） |
| `TrinketValue` | **0.1** | 世界 | **完璧で新品の1つが命の何割ぶんの値打ちか**（段階82）。**これが欲求の全部**——裏に計算するものは無い。**目盛りはコイン（約0.2）と石（約0.001）の間**。**用量が段階のすべて**: **3倍にすると決定の23%が「作る」になり個体数 −14.29 \*\*\*・餓死率 +2.45 \*\*\***（＝食べることを押しのける第3の目的）、**3分の1では作る回数が半分以下**（620個）で世界は動かない |
| `CraftTicks` / `CraftVitality` | **30 / 2** | 世界 | **1つ作るのにかかる時間と体力**（段階82。体力は完成時に一度だけ引く＝効用式が採点したのと同じ数字）。**値段があること自体が目的**——**値段の無い利益は天井まで走る**（段階17b）し、**気前よく作れない作り手が供給側の希少性**になる |
| `TrinketSpoilTicks` | **2000** | 世界 | **1つが持つ長さ**（段階82。肉と同じ時計で手の中でも進む＝#77）。**値打ちは残り時間に比例して連続的に下がる**ので、手放すことが締切ではなく普通の比較になる。**「ずっと持っている」に天井を付けるための数値** |
| `TrinketSpread` | **0.5** | 世界 | **同じ手が作っても1つ1つどれだけ違うか**（段階82）。**この世界で初めての「種類ではなく個体ごとの値打ち」**で、**段階80 の値段が散らばりうる唯一の余地**。**0 の対照（`trinketssame`）でも値段は散らばらない**（1枚超 0.20 対 0.17）＝**散らばらない理由はここではない** |
| `TrinketsVanish` | **false** | 世界 | **作っても手に何も残らない対照**（段階82。段階49 の「聞こえない叫び」と同じ形＝同じ時間を使い、物だけが無い）。**これが機序を名指しした**——**個体数 +8.50 \*** で、**贈与は +1.9（不動）**＝**立ち止まっていることは得、物を持つことは（贈与できなければ）損** |
| `SkillTrinketRelief` | **0.5** | 学習 | **1つの値打ちのうち作り手の腕が決める割合**（段階82）。**門にしない**（腕が無くても半分の値打ちのものは作れる）。**実測では効かない**——**保有率 0.01**（`SkillBirthplace = 0.5` の世界でも）で**平均の質は 0.50 のまま**＝**ヒント枠の天井**（段階31・62・69 に続く4度目） |
| `TrinketTaste` | **1** | 世界 | **同じ品を身体ごとにどれだけ違って欲しがるか**（段階84。**2026-09-15 に既定を1にした**——**体内時計の `ChronotypeSpread` が既定1 なのと同じ立場で、この世界では「身体が互いに違う」ほうが既定**。**0 ＝段階82 の世界＝全員が同じものを同じだけ欲しがる**）。**好みは円の上の位相**（体内時計と同じ立場＝量ではなく方向なので予算を食わず、親から丸ごと継いで漂う）、**品の側も同じ円の一点**（`Food.Style`。**作り手は狙えない**）。**倍率は平均1に正規化**＝**欲しさの総量は増やさず配り直すだけ**（段階15a の食料の原則を欲求に適用）。**測定（96シード）: 個体数 +11.15 \*\*・出生 +23.5 \*\*・売買 +0.34 \*\*（飾り物の売買 +0.22 \*）・贈与 +194 \*\***。**偽薬（0.001＝同じ乱数を引いて効き目だけ無い）は −7.18 \* で飾り物ありの世界と同じ**＝**効いているのは乱数列の違いではない**。**1枚超の値段は動かない**（+0.01）＝**値段は散らばらない** |
| `TrinketTasteMutation` | **0.08** | 世界 | **子の好みが親からどれだけ漂うか**（段階84。`ChronotypeMutation` と同じ立場・同じ既定値で、**円の上なので clamp ではなく wrap**） |
| `TrinketStyleAimed` | **false** | 世界 | **作り手が自分の好きな形を作れるか**（段階84 の対照＝**自給自足のミニチュア**）。**入れると質は倍**（`trinketFitMade` 2.00）**で作る量も倍**（1490 → 2464、決定の13%）**なのに、個体数の利得は消え**（+11.15 → +0.65）、**手渡しは毎回損になる**（`trinketGain` −0.79 \*\*\*）＝**歩いても働いても手に入らないことが、この品の値打ちの全部** |
| `AdornNeedsSurvival` | **true** | 世界 | **飾り物を欲しがるのは、先を見て自分がいると思える身体だけか**（段階84。**2026-09-15 に既定をオンにした**——**段階73 の `GoalsNeedSurvival` と同じ class の直し**で、**放っておくと飾り物のある世界に既知の欠陥が残るから**。**`survives()` を値打ちに掛けるだけ**＝段階73 が子に使ったのと同じ関数）。**これが食事と飾り物を分ける唯一の規則**——**生命は死亡確率の差なので追い詰められるほど大きくなり、理由なき欲求は定数なので、まさに負けるべき場面で勝っていた**。**測定: `craftShare` 0.07 → 0.04、`adornWant` の平均 0.48、個体数 +7.79 \*\***。**そして段階82 が壊した3倍の用量が壊さなくなる**（−6.51 \*\*\* → +0.70 で誤差内、そこでの売買は +0.49 \*\* ＝この群で最大の市場） |
| `HandOverCheapest` | **false** | 世界 | **掲げるもの・渡すもの・売るものを「一番惜しくないもの」にするか**（段階84。**`firstForSale` のコメントがもともと書いていた原則を、種類の固定順ではなく比較にしたもの**）。**値打ちが種類単位だったあいだは固定順がその原則そのもの**だったが、**好みが入ると固定順は逆のことを言う**。**ついでに既存の食い違いも直る**（掲げているのは `carried[0]`、売るのは `firstForSale` で別の品のことがあった）。**測定: 品が欲しがる身体のほうへ動く**（`trinketGain` 0.00 → +0.07 \*\*\*）**が、売買はそれより3倍よく選り分ける**（`trinketGainSold` +0.25 \*\*\*）＝**買い手は自分で自分を選ぶが、贈り物の受け手は誰も選んでいない**。**飾り物の無い世界でも効く**（個体数 +4.78 \*、出生 +11.6＝**夕食を配ってしまわなくなる**） |
| `GiftPriced` | **false** | 世界 | **渡すことに、手放すものの値打ちを払わせるか**（段階84）。**`addGive` は歩く代価しか払っていなかった**——売ることも置くことも手放す物を採点するのに、**渡すことだけが無料**だった。**測定: 贈与の9割が消え**（662 → 64）、**売買も一緒に減り**（−0.75 \*\*\*）、**個体数は増える**（+7.30 \*）＝**この世界で物を配ることは net で損だった**。**既定がオフなのは、段階48 以来の贈与の測定が全部これ無しで取られているから** |
| `GiftSelfLookahead` | **0** | 判断 | **`GiftPriced` と同じ引き算を、割合で払う目盛り**（段階92a。1.0 が `GiftPriced` と同じ挙動）。**引く figure は手の中で一番安いものの値打ち**で、食料・お金・外套ではそれが**「その物を失った身体の死亡確率の差」**そのもの。**目盛りが細かいのは、手の中のものが数十の値打ちを持つのに対し贈与が買う好意は最大 2.7 だから**——**1%〜2%でようやく釣り合い、4分の1でほとんどの贈与が消える**。**測定（遊ぶ地図・96シード）: 0.25 で贈与 −117.51 \*・個体数 −3.84**、**1.0 で贈与 −252.29 \*\*\*・個体数 −5.74 \*** |
| `GiftKinWeight` | **0** | 判断 | **自分の親か子に渡すことの、好意とは別枠の2本目**（段階92b。Hamilton の `rB` で、**関係が1ホップしか無いので `r` は重みに畳んである**）。**足すのは受け手の側の得**で、**相手の空腹は隠しパラメータなので標準を仮定し、見える体力だけ相手のものに差し替えて自分と同じ物差しで値付けする**（先読みは入れ子にしない）。**血縁は好意ではなく血統リンク**（`isKin`）——**好意は稼げるので血縁度に使えない**。**測定: 血縁への贈与 0.06 → 0.17（+0.11 \*\*\*）、用量4倍で 0.33、平坦な世界で 0.60（+0.54 \*\*\*）＝誰に渡すかが初めて動いた**。**代価は遊ぶ地図で個体数 −6.32 \***（平坦では **+0.95** ＝消える） |
| `ClimateMap` | **なし** | 世界 | **その地図の天気の絵**（段階85。**1文字＝1セルで、セルの大きさは絵そのものから決まる**＝決定#135。**2026-09-20 に地域の粒度をやめた**）。`.` が普通、`1`〜`9` がその寒さ。**文字は予約**（2つ目の天気の置き場）。**空＝天気のない世界＝既定**で、その世界は1ビットも変わりません。**`TerrainMap` とは別の絵**——**地面と天気は同じ場所についての別のこと**（冷たい川も暖かい川も川）で、段階22 が地形と地域を別の地図にしたのと同じ理由 |
| `ChillDrain` | **0** | 世界 | **一番寒い場所に立っていることが1tickにいくら体力を取るか**（段階85。**何かがそれに答える前の値**）。**目盛りは飢えの目減り**——**餓死より高くつく寒さは、何も住めない場所**で、**「回復経路を必ず残す」に触れます**。**入り方は目減り**なので、**先読みは新しいコードなしに寒さを見ます**（`pDrain` がそのまま読む）。**測定（96シード、平坦な世界の右半分を寒くして）: 個体数 −19.11 \*\*（0.05 ではさらに −40.34 \*\*\*）、餓死率 +0.48 \*\*\*、最近傍 +12.80 \*\*、`halfLife` +8.94 \***。**そして寒さは飢えと寒さが奪う体力の 60%（0.05 で 76%）を取るのに、身体は寒い側にいる割合が偶然どおり**（+0.02 ± 0.02）＝**足元の寒さは全候補を等しく押し上げるので比較から約分される**（段階86） |
| `HeatDrain` | **0** | 世界 | **2つ目の天気**（段階88。地図では `a`〜`i`）。**一番暑い場所に立っていることが1tickにいくら体力を取るか**。**増やすのは1行**——列挙・地図の文字・`drainFor`・品が自分で持つ `Wards`。**知覚も効用式も増えない**（`FoodView.Ward` が「この身体の目減りをここでいくら下げるか」という絶対量なので、天気が何種類でも同じ1つの数字）。**測定（96シード、同じ量の天気を1種類にした世界と対）: 市場は大きくならず**（売買 −0.18、誤差内）、**世界は悪くなる**（個体数 −18.55 \*\*\*、餓死率 +1.06、**守られている割合 0.45 → 0.34 \*\***）＝**作り手はどちらを作るか狙えないので、交換が成立しない世界では供給が分割されるだけ** |
| `SeasonTicks` | **0** | 世界 | **天気が一巡するのにかかる長さ**（段階87b。**0 ＝動かない＝既定**）。**絵を横に滑らせる**（深くするのではない——**全員が同時に同じものを欲しがるのは1つの需要であって取引にならない**）。**既にある暦 `TicksPerYear` = 500 に乗せ、乱数は1つも引かない**。**測定（96シード）: 狙った「商品の在庫」は増えない**（暖かい所で持たれている防寒具の割合 0.46 → 0.47）——**動かない世界でも既に46%**で、**作っていたのは季節ではなく脚**（一生のうちに両側に立つ身体が53%）。**用量は1年・4年・20年でほぼ同じ**＝room が無い。**代価だけ残る**（個体数 −8〜−12、餓死率 +0.33〜+0.50、`rareTrough` −0.04〜−0.07） |
| `WardShare` / `WardStrength` | **0 / 0** | 世界 | **作った飾り物のうち何割が寒さを防ぐ性質を持って出来上がるか／完璧な1着が寒さの何割を防ぐか**（段階87a。**既定は0＝防ぐものは無い**）。**作り手は狙えない**（段階84 の `Style` と同じ手）。**1着で足りる**——`wardsOff` は**手の中の最大値**（段階81 の `max`。足し算は段階17b の轍）**なので、2着目は持ち主にとって値打ちゼロ**＝段階69 が本で見つけた非対称。**値打ちは書かない**——**寒さは目減り、目減りは死亡確率**なので、**寒い所では食事級・暖かい所ではゼロ・すでに1着あればゼロ**が自動で出る。**測定（96シード、0.3 / 0.6）: 個体数 +20.97 \*\*\***（段階52 の調理に並ぶ）、**寒さの取り分 0.55 → 0.51 \*\*\***、**`onCold` +0.03 \***（**食料以外で居場所を動かした初めての規則**）、**770着作られ85%が人手に渡り、その全部が贈与**（売買は不動＝#115(a)） |
| `GiftWorthScaled` | **false** | 世界 | **贈与が買う好意を、受け取った側にとっての値打ちに比例させるか**（段階89b・#119。**false は「誰に何を渡しても同じ好意」＝この段階の前の全世界**）。**物差しはコインが請求するもの**（`CoinValue × LifeValue` ＝1食。**寛容さのための2つ目の物差しは作らない**）、**上限は2倍**。**渡す側は狙えない**（標準を仮定する）ので、**報われるのは結果**。**実装前に数えた**: 今の贈与は受け手にとって **0.28食ぶん**、**12%は値打ちゼロ**、**96%が他人へ**。**測定（24シード・平坦な世界）: 売買 1.58 → 2.29（+0.71 \*\*）＝この鎖で一番大きく確かな売買の動き**。**贈与の数も受け手にとっての値打ちも動かず**（−60 ± 381 / +0.01）、**個体数は誤差の中**（−9.0 ± 12.8）。**平均だけを合わせた対照**（`giftflatlow` ＝どの贈与も0.28倍）**は贈与 −1198 \*\*\*・個体数 −29.9 \*\***＝**効いているのは平均ではなく形**。**2つ目の天気の世界では、やり取りは増える**（贈与 +498 \*、防寒具の受け渡し +443 \*）**が守られている割合は動かない**（0.74 のまま） |
| `TrinketFancyTicks` / `TrinketFancySpread` | **0 / 0** | 世界 | **好みが動くか**（段階90。**0 は「好みは生まれつき」＝この段階の前の全世界**）。**継いだ `taste` は素質のまま**で、そこから**今の気に入り**を引く（`TrinketFancySpread` ＝素質からどれだけ離れるか。円の上の標準偏差）。**引き直しは「手の中の数が変わったとき」と「`TrinketFancyTicks` 経ったとき」**——**前者が7か所の出来事をどこにも手を入れずに拾う**。**間隔がこの規則のすべて**（2026-09-04 に「見かけるたびに引き直す」実装が個体数をほぼ半減させている。決定の間隔は14.5tick）。**実測の引き直し頻度は 1000body-tick に 33.7 回＝30tick に1回**で、**実装前の見積もり（400tick に1回）は10倍外れた**（飾り物は贈与で絶えず手を移るため）。**測定（24シード・平坦な世界）: `fancy` は売買 +0.12（動かず）・個体数 −13.3 \***、**`fancyfast`（10tick）は売買 +0.75 \*・コイン +1.12 \* と引き換えに個体数 −25.5 \*・餓死率 +0.61 \*\***。**12シードでは売買 +1.58 \*\* に見えたものが、24シードで半分になった**（この project で3度目） |
| `AdornSatiety` / `AdornForgetPerTick` / `AdornKeepsSated` | **0 / 0 / false** | 世界 | **飾り物は満たされるか**（段階89。**0 は「4個目も1個目と同じ値打ち」＝この段階の前の全世界**）。**`AdornSatiety` ＝欲しさを半分にする保有数**、**`AdornForgetPerTick` ＝帳簿が手の中の数へ近づく速さ**（0.002 で時定数500tick＝1年）、**`AdornKeepsSated` ＝一度満たされたら二度と欲しがらない**（対照＝需要が繰り返さない世界）。**形は段階16 の食べ飽きと同じ**（減衰する帳簿1本・飽和する割引・読む場所は1つ）。**数えるのは「持っていること」**なので、**腐敗がそのまま需要の再来になる**。**実装前に数えた**: 1人あたり 2.71個（持っている身体で 4.75）・43%が0個・一生2475tick に対して品は2000tick で腐る。**測定（12シード・平坦な世界。`adornsated` / `adornsatedfast` / `adornsatedonce` 対 `adornnow`）: 保有数 2.58 → 1.89 \*\*\*、`craftShare` 0.04 → 0.02 \*\*\*、`adornSpare` 1.00 → 0.62 \*\*\***。**売買は動かない**（+0.25 ± 0.45）。**個体数は下がる**（−9.9 / −18.3 \* / −23.8 \*）——**用量の問題ではなく、贈与が減るから**（段階82 の「飾り物は人に渡せることで元を取る」の裏返し） |
| `HidePerBudget` | **0**（＝皮は落ちない） | 世界 | **外敵の死体が皮を1枚落とすのに必要な体の大きさ**（`TODO.md` の8番。**肉の `MeatPerBudget` と同じ形で、用量だけ別**）。**落とすのは外敵だけ**——人間の死体から出ると「材料はどこにでもある」ことになり、この規則の全部が消える。**種類ごとの倍率は `EnemyKind.Hide`**。**実装前に数えた**: **外敵の死は1実行（20000tick）に 59.4 回で、人間が関与したのは 22.1 回**。**今の防寒着は 3104着**なので、**材料にすると供給は桁で落ちる**——**だから用量を3つ測った**（130＝1死体に約4枚 / 33＝約16枚 / 520＝約1枚） |
| `WardNeedsHide` | **false** | 世界 | **防寒着は皮からしか作れないか**（`TODO.md` の8番。**true にすると `WardShare` の抽選は使われない**——何が防寒着になるかを決めるのは運ではなく、材料を持っているかどうか）。**皮の値打ちは「それで作れるもの」× `CarryValue`**（`craftWant` の1か所で、拾う・持つ・売る・買う・贈る・作るが同じ式を読む）。**すでに1着持っている身体にとって皮の防寒ぶんはゼロ**（`wardGap`）。**測定（12シード。`coathide` / `coathiderich` / `coathidelean` 対 `coatbite`）: 供給は狙いどおり落ち**（防寒着 3104 → 144 / 367 / 20.5、着ている割合 0.67 → 0.15 / 0.18 / 0.00）、**個体数も落ち**（98.5 → 66.3 \*\* / 87.3 / 29.7 \*\*\*）、**売買は増えない**（`sales` 2.33 → 1.00 \*\*\*、`coatsSold` 1.75 → 0.33 \*\*\*）。**初めて余剰が出た**（`hidesSpare` 0.53〜0.89）**のに、売れた皮は 0.08 枚で贈られた皮は 47〜130 枚**——**`HandOverCheapest` を足して「一番惜しくないものを掲げさせて」も `hidesSold` は12シード全部 0.00**。**5度目の同じ結論で、防寒具を商品にする案はここで閉じた** |
| `MaxHideItems` | 140 | 世界 | **地面に落ちている皮の上限**（`MaxMeatItems` と同じ安全網）。**皮は腐らない**（毛皮は保つ）ので、**誰も欲しがらない世界では際限なく溜まる**——それを止めるためだけの数。**`HidePerBudget` が0なら1枚も出ないので、この数は何もしない** |
| `ChillAheadSeen` | **true** | 世界 | **身体が一歩先のセルの天気を読むか**（決定#135）。**段階100 が地面に対してやっていることを天気に対してもやる**。**課金は `moveCostDir` の中で、段階99 の drain とまったく同じ場所・同じ単位**（足元との差だけ。**同じ天気の方角は0**）。**測定（24シード）**: **4列の絵では個体数 48.4 → 94.4（+46.00 \*\*\*）、1体あたりが払う寒さ −37%**。**10列・40列では誤差の中**——**読めるのは「段の差」で、9段しかない絵を細かく描くと隣が同じ段になる**。**天気のない世界では1つも仕事をしません** |
| `ChillGradient` | **false** | 世界 | **身体が「どちらが暖かいか」を感じ、そちらへ歩く候補を持つか**（段階86b。**目と候補は1つのスイッチ**——**目だけでは無意味**だと数え上げで分かったため）。**地図でも記憶でも伝承でもない**（顔に当たる風。地域の幅ぶん離れた地面を読む）。**移動候補への一歩ぶんの課金は 0.000013 で、候補の開き 118 に対して7桁足りない**ので、**値打ちは段階15b と同じ「その場所の差」で付ける**。**測定: 偶然より寒い側にいるぶんが −0.03〜−0.04 \*、実際に払う寒さも −0.02 \*〜−0.04 \*\***（**同じ地図で食料は 0.35 動かす＝10分の1**）。**坂より段差のほうが動く**（−0.05 \*\* 対 −0.01）。**既定がオフなのは割に合わないから**（軽い用量で個体数 +1.36 ± 7.45、強い用量では目があるほうが −7。候補の平均の値打ちは −21） |
| `ChillKnown` | **true** | 世界 | **身体が、立っている場所の寒さを自分の状態として読めるか**（段階86 の対照＝段階34 の `DrownKnown` と同じ形）。**false でも寒さは同じだけ取ります**——読めなくなるだけ。**測定: 読める世界と読めない世界で、寒い側にいる割合が区別できない**（+0.02 対 −0.01）＝**この規則の効き目は全部「選別」で「選択」ではない**。**しかも読めないほうがわずかに良い**（個体数 +4.62、餓死率 −0.36 \*\*＝**逃げ場が無いまま臆病になるぶん**） |
| `SaleAnchor` | **0** | 世界 | **買ったものを、買値より安くは手放さない度合い**（段階83・#111。**この世界で唯一の「事実の記憶ではなく非合理な偏り」**——前例は段階54 の気分）。**形は売り手の下限を買値へ引き上げるもの**（`floor + (買値−1 − floor) × SaleAnchor × 自分がこの窓の終わりにいる見込み`）で、**新しい見積もりは作らない**（買値は品に乗り、見込みは飾り物と同じ数字）。**追い詰められるほど消える**＝**普段は損を渋り、切羽詰まれば損切りする**。**測定（96シード × 2つの世界）: `anchored` = 0.00、そして規則を入れた腕は入れない腕と全指標 +0.00 ± 0.00 ＝1ビット同一**——**買われた品がもう一度売りに出されることが一度も無い**（市場の天井でも）。**既定0 はそのため** |
| `CoinBuysWarding` | **false** | 世界 | **コインが「天気を防ぐもの」への請求権にもなるか**（#116。段階81 の `CoinBuysMending` と同じ一歩の3つ目）。**値付けは `keepValue` と同じ「その身体が困る瞬間」**——**今の勾配で読むと、満ちた身体にはどの枝もゼロ**だから。**測定（96シード）: コインを持つ割合 +0.04 \*・1人あたりの枚数 +0.33 \* なのに、売買は −0.01・動いたコインは −0.25**＝**コインは持たれて使われない**（#112(c) の警告が理由のある欲求でも出た）。**そして掃いて分かったのは、枝は「1食の枝が既に正である身体」にしか払わないこと**——**平らさは財の側ではなく身体の側にある** |
| `CoinBuysOnlyMeals` | **false** | 世界 | **お金で買えるのは食べ物だけ、という世界に戻すためのスイッチ**（2026-09-15 に見つけた穴。**既定は「直った側」**）。**`addBuy` の入口が掲げられている品の栄養価を訊いていた**ので、**本と飾り物は「買う」が一度も採点されたことがなかった**（値段・売り手の側・受け渡しは全部動いていた）。**段階51・68・69・80・82 の売買の数字は全部この世界のもの**で、**直すと飾り物の世界では売買 0.36 → 0.92（+0.55 \*\*\*）＝その差は全部飾り物**（個体数の代価は −3.81 \*） |
| `CoinPrices` | **false** | 世界 | **売買の値段を枚数にするかどうか**（段階80。**80a が要ります**——枠のある世界では買い手が2枚目を持てないので値段は必ず1枚）。**下限は `willSell` を、上限は `addBuy` を割り算にしただけ**（新しい見積もりなし）。**値段はカウンターで決まり、掲げも覚えもしない**（#73 の「値段も帳簿も無い」を値札のぶんだけ緩めない）。**買い手は標準価格 `1/CoinValue` で採点して出発する**（特定の売り手の言い値は隠しパラメータなので、段階49 と同じ「世界の標準」を仮定）。**並の1食では買い手の上限はちょうど `1/CoinValue`** なので、**1枚超の値段が付くのはその品が並の1食より値打ちがあるときだけ**（飽き・治り・好意）。**測定（96シード）: 売買 2.06 → 0.85（−1.21 \*\*\*）、断り 86 → 2.8（−83.25 \*\*\*）、1枚超が売買の33%、平均1.72枚、個体数 +4.97 \*・`rareTrough` +0.14 \*\*・餓死率 −0.37 \***。**平坦では個体数は動かない**（−0.51） |
| `CoinPriceBlind` | **false** | 世界 | **値段は付くが、買い手は1枚のつもりで出発する**（段階80 の対照＝段階49 の「聞こえない対照」と同じ形。**同じ時間を使い、情報だけが無い**）。**この対照がこの段階の発見**——**売買 2.06 → 3.72（+1.66 \*\*\*）・動いたコイン2.7倍で、P13 で初めて市場が本当に大きくなった**。**代わりに無駄足が残り**（断り 68）、**世界は返ってこない**（個体数 +1.75 ± 4.17、`rareTrough` −0.00）＝**値段が市場を作り、買い手の正しい採点が市場を閉じる** |
| `SalePriceSplit` | **true** | 世界 | **値段を売り手の下限ではなく範囲の中点にするか**（段階80。**2026-09-15 に既定をオンにした**）。**コインは割れないので丸めがそのまま分配の規則**。**段階80 の世界ではほとんど違いを生まない**（個体数 +5.25 \* 対 +4.97 \*、売買は同じ）が、**値打ちの散らばる品があると、これが「値段が1つしか値を取らない」ことの唯一の原因**——**床は9割の売り手で0＝1枚**なので、**分け方を変えて初めて1枚超が 0.15 → 0.40（+0.28 \*\*\*）**になり、**代価は測定上ゼロ**（個体数 +0.69 ± 1.74、売買 −0.10 \*） |
| `CarrySlotsWeigh` | **false** | 世界 | **手の枠を取るのは重さのあるものだけかどうか**（段階80a）。**コインと本は重さゼロ**（#66）**なので手を取らない**。**`CarrySlotted = false` とは別物**——あちらは食事も無制限になり、**その代価（`rareTrough` −0.16 \*\*\*、段階76）はお金の無い地図でも同じ大きさで出る＝食べ物を運ぶ代価**で、ここはその代価に触らない。**重さゼロと数える集合は `heavyCarried` そのもの**（新しい規則を作らない）。**着手前の数え上げ**: 買い手は **1.000枚**しか持たないのに、世界には人間1人あたり **2.36枚**あり **60枚のうち 38.2枚が地面**＝**2枚目を止めているのは供給ではなく手**。**測定（96シード）: 枚数 0.90 → 2.98 \*\*\*、`load` +0.05 \*\*\*、贈与3〜4倍 \*\*\*、調理 +23.5 \*\*\*、`salesRefused` −32.8 \*\*（売買そのものは +0.30 \*）。個体数は地図で符号が変わる**（平坦 **+14.57 \*\***／遊ぶ地図 **−9.55 \*\***）。**歯止めは `CarryDiminishes`**（枚数 2.98 → 1.82。**枠が無くなって初めてあの規則が生きる**） |
| `CarryDiminishes` | **false** | 世界 | **n個目の値打ちを下げるかどうか**（段階71）。**「足りなくなった瞬間の空腹」から、すでに手にあるものが埋めるぶんを引く**だけ（手の中の勘定は `CoinValue × 枚数` を含む＝コインは食事への請求権だから）。**身体は1つの地平の中で1回しか足りなくならない**ので、**満腹で食事を持っている身体にとってコインはほとんど値打ちが無くなる**＝段階70 が語を足しても買えなかった押し出しの解消。**ただし `CarrySlotted = true` のままでは1ビットも効かない**——**手が1つの世界に「2個目」は存在しない**（実測 +0.00 ± 0.00）。**両方入れると1人あたりの所持は 2.8 → 2.3 個** |
| `BurdenIgnoresWeightless` | **true** | 世界 | **「もう1つ運ぶ代価」の勘定から、重さゼロのもの（コイン・本）を外すかどうか**（段階71。**true が直った側で既定**）。`burdenWith` は `s.Carried`（全部）を数えるのに実際の重さ `carryLoad` はそれらを除いていたので、**コインを持った身体が木の実を拾うときの `lug` が過大**だった。**ただし枠のある世界では1ビットも効かない**（実測 +0.00 ± 0.00）——**積載率のクランプ（上限1）が、手が1つの世界では両方を同じ値に潰していた**。**効くのは `CarrySlotted = false` の世界だけ** |
| `CarryCost` | 0.5 | 身体 | **満載のとき移動コストが何倍になるか**（段階40）。**分母は体力**（パワーではない＝役割を重ねない）。**速度ではなくコストに掛ける**（effort との二重計上を避ける）。**地形の倍率と掛け算になる**ので、**平坦な世界で単体を測り、地形は別条件**（#66） |
| `CarryValue` | 0.5 | 身体 | **手の中の一口を、腹の中の一口の何割と見るか**（段階40）。**さらにその場の食料の競り合いぶり（`FoodScarcity`）で割り引く**——**この割引を入れるまで運搬は個体数を 4.5 削っていた**。豊かな場所では拾う価値がほぼ0、競合が激しい場所では保険になる |
| `CarryOffTheBooks` | false | 世界 | 持っているぶんを世界の食料の枠から外すか（段階40）。**既定は数える**（＝運搬は食料を移動させるだけで増やさない）。**外した腕のほうが個体数が悪い**ので、「運搬が食料を流通から引き上げている」という読みは**否定された** |
| `CarriedMeatKeeps` | false | 世界 | 運搬中の肉の腐敗を止めるか（段階40・#77）。**既定は止めない。** 実装前に地面で測った上限どおり**ほとんど何も変わらない**（個体数 +0.81、誤差内。腐る割合 −0.02 \*）ので、**値段の無い恩恵を1つ増やす理由が無い** |
| `FoodNutrition` | 34 | 身体 | 食料1個で下がる空腹度 |
| `StarveHunger` | 60 | 身体 | これを超えると体力が減り始める |
| `StarveRate` | 0.16 | 身体 | 空腹度が最大のときの体力減少（`StarveHunger`〜`MaxHunger` で線形に立ち上がる） |
| `SatiatedHunger` | 40 | 身体 | これを下回ると回復する |
| `RegenRate` | 0.09 | 身体 | 満腹時の体力回復。`× (1 − 今tickのeffort)` |
| `MaxSpeed` | 1.7 | 身体 | **基準となる** effort 1 のときの速度。実速度は `MaxSpeed × 移動速度遺伝子 / 50.5 × √effort` |
| `MoveCost` | 0.035 | 身体 | effort 1 での1tickの移動コスト。`MoveCost × effort`（線形） |
| `PerceptionRadius` | 130 | 身体 | 視界。ここに入った食料・他個体が知覚に載る |
| `GrabRadius` | 11 | 身体 | 食べる・求愛が成立する距離 |
| `CombatRadius` | 15 | 身体 | 打撃が届く距離 |

`MaxSpeed × √effort` と `MoveCost × effort` の組み合わせが「急ぐほど距離あたりが高くつく」を作ります。

### C-2b. 寿命（背景処理・ノードは知らない）

**判断には一切効きません。** `Perception` に載らず、効用式にも項がありません。効くのは `World.metabolise` の中だけです。詳しいルールは `NODE.md` 2節。

| 名前 | 既定値 | 効く先 | 意味 |
| --- | --- | --- | --- |
| `MaxLifespan` | 3000 | 身体（背景） | 誕生時の寿命の残量。**2026-09-02 に較正**（6000では標準実行長で一度も枯渇せず、1500では世界が壊れる。3000で死因の約4%） |
| `StarveLifespanRate` | 0.2 | 身体（背景） | `Hunger > StarveHunger` の間、1tickごとに引く寿命。0で過小側を完全に切れる |
| `OverfedHunger` | 20 | 身体（背景） | これを空腹度が下回ると「腹八分目（満腹度80%）」を超えたとみなす。`SatiatedHunger`（40）より下に置いてあるので、そこそこ食べる分には寿命は減らない |
| `OverfedLifespanRate` | 0.2 | 身体（背景） | `Hunger < OverfedHunger` の間、1tickごとに引く寿命。0で過度側を完全に切れる |
| `FrailVitalityShare` | 0.20 | 身体（背景） | 体力がこの割合を下回ると「不調」。0で切れる |
| `FrailGraceTicks` | 200 | 身体（背景） | 不調がこれだけ続いて初めて寿命を削り始める。**20%を超えた時点でカウントはリセット**（長い不調のコストであって、悪かったtickの総計ではない） |
| `FrailLifespanRate` | 0.2 | 身体（背景） | その後、1tickごとに引く寿命。**効用式に載る `ShockRisk` とは別物**で、ノードは知らない |

### C-2c. 成長と老い（年齢補正）

実効値 ＝ 遺伝的素質 × 年齢補正（`Agent.AgeFactor`）。**掛け算は `Agent.Ability` の1か所だけ。** ルールは `NODE.md` 11節。

| 名前 | 既定値 | 効く先 | 意味 |
| --- | --- | --- | --- |
| `ChildAbilityShare` | 0.6 | 身体 | 生まれたときに出せる遺伝的素質の割合。**1 で成長の側を完全に切れる**（段階7d 前の世界） |
| `ChildhoodYears` | 2 | 身体 | 満腹を保った場合に成人するまでの年数。飢えていれば伸びる |
| `ReproMaturity` | 0.9 | 判断 | これ未満の成熟度では求愛が候補に挙がらない。**子ども期のコストの本体で、世代時間に直接乗る** |
| `SenescenceYears` | 12 | 身体 | この年齢を過ぎると衰えが始まる。平均年齢が約10年なので、集団の一部だけが経験する |
| `SenescenceRate` | 0.06 | 身体 | 1年あたりに落ちる割合。**0 で老いの側を完全に切れる** |
| `SenescenceFloor` | 0.40 | 身体 | 衰えの下限。ここで止まる |
| `ChildRearingTicks` | 1000 | 世界 | 育児期間（＝2年）。**0 で完全に切れる** |
| `RearingRadius` | 130 | 世界 | 子が親から離れてよい距離。**視界（`PerceptionRadius`）と同じでなければならない**——短くすると子を食料から引き離して殺す（実測: 45で成人到達率0.46、130で0.72） |
| `ParentFeedShare` | 0 | 世界 | 親の一口のうち、そばにいる育児中の子に回る割合。**0 で親は何も与えない**（2026-09-06 以前の育児）。**出産のバグを直した世界で測り直すと 0.5 で個体数 +5.08（誤差内）・`gen` +0.48 \*\*・`grewUp` +0.04 \*\*\*・子の死亡割合 −0.02 \*\*\*・`starveRate` −0.04 \*** ＝**もう費用ではない**（費用だけの対照は −7.54 \*）。既定は0のままだが、**上げる根拠は測定の側にある** |
| `ParentFeedWasted` | false | 測定用 | 対照。親は同じだけ払うが**子には届かない**（＝費用だけを取り出す）。実測 −8.38 \*\* で `grewUp` は不動 |
| `ParentFeedKnown` | true | 判断 | 親の `Perception` の栄養価に分け前を反映するか。**false は「半分しか食べていないことを知らない親」**（対照。24シードでは個体数は落ちないが `grewUp` の利得も消える＝どちらも誤差内） |
| `RearingUntilGrown` | false | 世界 | 育児の終わりを**時計ではなく子の状態**（成熟度1）で決める。**true にしても世界は良くならないので既定は false**。伸びるのは裾だけ（p90 が 1000 → 2000〜2800tick）で、長く縛られるのは飢えている子だけ。**出産のバグを直した世界で測り直すと 個体数 −10.62 \*・出生 −41.08 \*\*\*・`gen` −0.58 \*** で**さらに悪くなる**（`grewUp` は誤差内、`clumping` は +0.08 \* と少しだけ買える。2026-09-06） |
| `RegionCostWeight` | 2.0 | 判断 | 地域の「行きにくさ」を食料と同じ単位に直す重み（移動コスト倍率1つぶん＝視界内の食料2個ぶん）。**0 で段階29 前**（信念は作るが誰も読まない）。**平坦な世界では倍率が全部1なので、どの値でも1ビットも変わらない。** 掃引: 0/1/2/4 で `onDear` 0.48/0.45/0.42/0.42（**4は効きが増えず個体数だけ −11.5 \***） |
| `RegionCostForgetPerTick` | 0 | 判断 | 行きにくさの信念が薄れる速さ。**0＝薄れない**（地面は留守中に動かない）。**地形が動的になったとき（浸食・エディタ）に率を入れるだけで有効になる**よう、食料の信念と同じ関数を通してある。**段階35 の「危険」の信念も同じ率を使う**——「地面についての信念がどれくらいで古くなるか」を2つに分けない |
| `RegionCostTold` | true | 判断 | 行きにくさを伝承するか。**false は段階29a だけ**（自分で歩いた地面しか分からない）＝対照。**実測ではこれが一番悪い**（個体数 −13.3 \*） |
| `HighGroundCover` | 0 | 世界 | **下から殴られている身体が、地面のおかげで外させる確率**（段階30）。**倍率ではなく床**——回避は「構え」の1チャネルで、**戦闘・逃走中しか持たない**（実測15.7%、回避寄りの構えは7.7%）ので、倍率だと**ほぼゼロに掛けることになる**（最初の版がそうで、何も動かなかった）。**1段でも上なら同じ**（2段上は2倍難しくはない）。`EvasionCap` で頭打ち。**平坦な世界では0**（誰も誰の上にいない）。実測（0.3）: `evadedShare` 0.04 → 0.06 \*\*\*、世代 +0.71 \*、餓死率 −0.39 \*、**ただし高台にいる割合は動かない**（−0.02） |
| `TerrainFoodCorrelation` | 0 | 世界 | 地域の食料の多さを、その地域の**平均移動コストの世界平均からの乖離**に結びつける。**正＝荒れた地域ほど痩せている**（現実の読み方）、負＝荒れた地域ほど豊か。**総量は変えない**（相関のあと再正規化。テストで固定）。**平坦な世界ではどの値でも1ビットも変わらない**（乖離ゼロ）。**既定0は「地図の作者が決めること」という立場**——`cmd/devview -terrain` は 1 を渡す（実測で全指標が良い）。実測（対 = 相関なし）: 荒れ地に立つ割合 **0.42 → 0.32 \*\*\***、個体数 **+8.0 \***、餓死率 −0.20 \*、`killShare` は不動 |
| `TerrainMap` | なし（平坦） | 世界 | 世界を敷く地形。1行1列、1文字1セル（`.` 平地／`:` 荒れ地／`~` 水／`1`〜`9` その高さの平地／`A`〜`I` その高さへのスロープ）。**空なら平坦＝段階20 以前の世界そのまま。** セルの大きさは地図の行数・列数から決まる |
| `RoughMoveCost` | 2.0 | 世界 | 荒れ地の移動コスト倍率 |
| `WaterMoveCost` | 3.0 | 世界 | 水の移動コスト倍率。**渡れないのではなく高い**（渡れない＝障害物は経路選択＝知能の話で、速さの話ではない）。**段階97 以降、水は遅くもできる**（`WaterSpeedShare`）——**コストは tick あたりで課金されるので、半分の速さで渡ると同じ距離にこの倍率を倍払う**（切り分けの腕は `waterslowfree`） |
| `WaterSpeedShare` | **1**（無効） | 世界 | **水のセルに立っている身体が保つ速さの割合**（段階97。`cmd/devview -wade`）。**1 は段階96 までの世界＝水は高いだけで遅くない**。**これが地形が速度に触る唯一の場所**で、**段階20 の「倍率は速度ではなくコストに掛ける」を水についてだけ取り消したもの**（理由の(1) は対照アーム `waterslowfree` で解け、(2) は段階20・33・57 が3度 0.00 を測っている）。**掛かるのは `speedNow` であって `MaxSpeed`（上限）ではない**（段階57 の線）。**測定（96シード・基準 `riverswim`）: 狙いは達成され、世界は悪くなる**——**水の中の1tickあたり移動距離は陸の 0.978 倍（無効時＝違和感の正体）から 0.588 倍（0.5）へ**。**代価は個体数**（0.75 で **−8.04 \***、0.5 で **−37.43 \*\*\***、0.25 で **−97.24 \*\*\***）。**`onWater` が倍になる**（+0.13 \*\*\*）が**それは選択ではなく算術**（1回の滞在が 111.6 → 224.8 tick ＝ちょうど倍で、「知らない」腕でも 225.2）、**帰結として `drownRate` も倍**（+0.24 \*\*\*）。**3つの効き目の切り分け**: 遅さそのもの −23.65 \*\*\*（`waterslowfree`）／滞在が倍になる溺死 −26.34 \*\*\*（`waterslowdry`）／全部 −37.43 \*\*\*。**両岸（段階37）は無傷**（`crossIndex` +0.15 \*\*\* だが `crossDry` +0.00・`bankBoth` −0.06 \*\*）。**物理の既定はオフ＝地図の作者が入れるもの**。**遊ぶ世界（`cmd/devview -terrain`）は 0.75 を持つ**（2026-09-18。**測った3つの用量のうち一番安いもの**。`-wade 1` で段階97 以前に戻せる） |
| `WaterSlowKnown` | **true** | 世界 | **身体が、水に入って自分が遅くなったことを感じるか**（段階97 の対照＝`DrownKnown` / `ChillKnown` / `EatTicksKnown` と同じ形）。**false でも水は同じだけ遅くする**——`Perception.Self.MaxSpeed` が陸の脚を返すだけ。**測定: 知ることは害**（**知らない腕のほうが個体数 +17.04 \*\*・出生 +44.77 \*\*・`dIntelligence` +6.00 \*\***＝**規則の代価37 の半分近く**）。**居場所は1ミリも動かない**（`onWater` −0.01、`drownRate` −0.03）**が、選ぶ手が動く**——**水の中での求愛 −21%、水の中での休息 +52%**。**機序は「立ち止まることだけが遅くならない」**（移動をともなう手は全部高くなるが、休息と観察は1ミリも高くならない）。**知ることが有害だった2例目**（1例目は #76 の `EatTicksKnown`） |
| `SlopeMoveCost` | 2.0 | 世界 | スロープ（坂）の移動コスト倍率 |
| `DrownChancePerTick` | 0.0002 | 世界 | **水の上で過ごす1tickごとに死ぬ確率**（段階34）。**継続ダメージにしない**——削り合いにすると防御で割ることになり、防御に2つ目の役目がつく。**閾値も無い**（水に入るかどうかは効用比較の結果で、`Utility.Hazard` に「その手が身体を水に留める見込みtick数 × 確率 × `LifeValue`」が引かれる）。**較正は先に数えてから決めた**——river の地図では**全体のtickの14.5%が水の上**、1回の滞在が**平均127tick**（最長3143）なので、この値で**滞在1回の95%は生き延び、死因の11%が溺死**になる。**平坦な世界では乱数を1つも引かない**。実測（river、24シード×20000tick）: 個体数 −17.3 \*\*、`killShare` −0.10 \*\*\*、餓死率 +0.27 \*、**`onWater` は不動**、`shSpeed` −0.01 \*\*。**5倍（0.001）は世界を壊す**（個体数 18.0、溺死が死因の33%、希少種の谷 0.41）。4分の1（0.00005）は溺死3%・個体数は誤差内 |
| `SpawnMap` | （空＝どこでも） | 世界 | **湧く場所を1セル1文字で塗った層**（2026-09-19。`p` 植物／`f` 魚／`e` 外敵／`F` 植物と魚／`*` 全部／`.` なし。行が短ければその先は `.`）。**塗られたタイルにしか湧かない**。**量は変えない**——`FoodSpawnRate` と `EnemySpawnTicks` は今までどおりで、**段階15a の総量保存が塗られたタイルの中で閉じる**（テストで固定）。**陸に塗られた魚は無視**（段階42 の「魚を生やすのは水の中だけ」を絵に壊させない）。**外敵は「到着する場所」**で、子は親のところで生まれる。**Tiled では2枚目のタイル層**、意味はタイルの `spawn` プロパティ |
| `FishSpawnRate` | 0 | 世界 | **水が自分の取り分を持つときの、1tickに湧く魚の数**（2026-09-20）。**0 は段階42 の世界**——魚は `FishShare` の確率で**植物の代わりに**湧き、1つの上限を分け合う。**0 より大きいと魚は独立した予定で湧き、`FishShare` は読まれない**。**これは段階15a の総量保存を意図的に緩める**（世界は `FoodSpawnRate` が言うより多く育てる）ので既定は0。**保存はプールの中で閉じる。** **段階42・43・36 の数値は「水が陸から食料を取っていた世界」のもの**で、この値を入れたら読み直すこと |
| `MaxFishItems` | 0 | 世界 | **魚だけの上限**（2026-09-20）。**0 は `MaxFoodItems` を植物と分け合う**（今までどおり）。**0 より大きいと魚は自分の天井を持ち、植物の上限の計算からも外れる**——**肉に別の上限がある（`MaxMeatItems`）のと同じ理由**で、片方が豊作だともう片方が育たなくなるのを避ける |
| `FishRichMap` | （空＝一様） | 世界 | **どの水域がどれだけ魚を育てるかを1セル1文字で塗った層**（2026-09-20。凡例は `RichMap` と同じ＝桁÷5）。**空は今までどおり**——魚は水セルから一様に引かれるので、地域ごとの魚の量はその地域の水の量にちょうど比例する。**`RichMap` とは別の絵**（同じ絵にすると「豊かな岸」が「植物が多い」と「魚が多い」を同時に意味してしまい、片方だけを言えなくなる）。**陸に塗っても何も起きない**（抽選は水セルの上だけ） |
| `PlantKinds` | （空＝1種類） | 世界 | **植物の種類の表**（2026-09-20・決定事項#134。`Name` / `Share` / `Nutrition` / `Spread` / `Regrow` / `Poison` / `Signal`）。**`Nutrition` はその種類1個が下げる空腹度の倍率**（2026-09-22。0＝`FoodNutrition` そのまま。**肉の `MeatNutrition` と同じ形**で、**身体が見る値にも同じ倍率がかかる**ので効用とズレない）。**種類＝4本の遺伝子の初期値の束**で、新しい軸ではない。**`Share` は正規化**され、0 の行は湧かない。**0 のフィールドは世界の既定値**。**`PlantGenetics` も `PlantDefence` も既定オフなので、そのままでは種類は札しか変えない**——**種類が世界に現れるのは、植物が遺伝する世界だけ**。**札（`Strain`）は種にも継がれる**ので、種類は1世代で溶けない |
| `EnemyKindMap` | （空＝塗らない） | 世界 | **どのセルにどの種類の外敵が来るかを1セル1文字で塗った層**（2026-09-20。文字は `EnemyKinds[i].Key`）。**「どの種類が来るか」は `Share` のまま**で、**塗りが決めるのは「どこに」だけ**。**塗られていない種類は今までどおり**（`EnemySpread` と `Homing`）。**Tiled では `enemy` プロパティ（名前）**——**名前は行を指すだけで、数値は地図に置かない**（決定事項#133） |
| `EnemyKind.Key` | 0 | 種類 | **`EnemyKindMap` でこの種類を表す文字**（2026-09-20）。0 は「どこにも塗られていない」 |
| `EnemyKind.Roam` | 0（＝地域幅の0.5） | 種類 | **巣からこれだけ離れるまでは `EnemyHomeCost` を払わない**（2026-09-20。世界の単位）。**地域が代わりに務めていたのはこの半径**——段階64 が地域を使ったのは「10歩さまよった身体は家を離れていない」ためで、**点だけにすると1歩目から課金される**。**閾値ではない**（半径の先は距離とともに代価が上がるだけで、引き戻しは無い） |
| `LineageRule` | `LineageTree` | 世界 | **血統を長子1人にだけ継がせる**（2026-09-20。**担い手の生きている子が1人も継いでいないときだけ、生まれた子が継ぐ**＝長子が継ぎ、死ねば次）。**既定は木**（担い手の子は全員が継ぐ）。**2つある**——`LineageTree`（担い手の子は全員が継ぐ。既定）／`LineageChain`（世界にただ1人。長子、死ねば次）。**3つ目の `LineageRegionChain`（地域ごとに1人）は 2026-09-21 に削除した**（下記）。**世界の振る舞いはどれでも変わらない**（engine はタグを1度も読まない。`pop` / `gen` / `births` がどちらでも完全一致）が、**「自分の民族」の意味が変わる**: **2地域以上にいる血統の割合は 0.92 / 0.00**、**20000tick 後に残る血統は 4.67 / 0.25本**（70本中。`HISTORY.md` 2026-09-20）。**削除した3つ目について**: **タグは出生の瞬間にしか配られない**ので、**兄が立っている区画で生まれた弟は最初から担い手ではなく、兄が死んでも繰り上がらない**——**「今この国の跡継ぎは誰か」は死んだ瞬間に変わる問いで、出生時に書くタグでは原理的に表現できない**。**それを表すのは `World.LineHeirs`（問い合わせ）のほう** |
| `NestInherited` | false | 世界 | **生まれた子の巣を親の巣にする**（2026-09-20）。**既定は「生まれた場所」＝今までどおり**。**地図の偏りを残すために作って、測ったら効かなかった**（24シードで `prowlGain` −0.00 ± 0.02、`prowlKept` +0.03 ± 0.32。**そのうえ `killShare` +0.04 \*\***）——**偏りを消しているのは出生ではなく徘徊**（段階58: 外敵は出発点から平均270 離れ、地域の幅は200）。**残してあるのは、外敵を巣の近くに留める地図（`EnemyHomeCost`）では別の問いになるから** |
| `NestRateMap` | （空＝どの巣も同じ） | 世界 | **どの巣からどれだけ湧くかを、`EnemyKindMap` と同じ格子に1セル1文字で塗った層**（2026-09-22。`TODO.md` の18番）。**digit は五分の一＝`5` が普通、`9` はほぼ倍、`0` は「ここからは来ない」**。**率ではなく取り分**——**1tickに何体かは `EnemySpawnTicks`、世界の上限は `MaxEnemies` のまま**。**Tiled では `rate` プロパティ**（比。絶対は `Config`＝#133）。**塗らない世界は1ビットも変わらない**。**測定: 1:9 に塗ると巣の近くの外敵の 0.87 が重いほうにいる**（対照0.39、+0.48 \*\*\*）——**ただし `EnemyHomeCost` が入っている世界での話** |
| `NestCap` | **0（＝定員なし）** | 世界 | **普通の巣が自分の `Roam` の中に何体まで抱えるか**（2026-09-22。`TODO.md` の18番）。**満ちている巣は外から入れない**。**生まれるほうは止めない**——**`MaxEnemies` / `MaxPopulation` / `MaxFoodItems` と同じ「世界が湧かせる数」の棚**で、**身体は誰も定員を知らない**（行動閾値ではない）。**数えるのは「その巣の `Roam` の中に立っている同種」**。**測定: 巣の自然な人数は 3.12** なので、**12 では1度も発火せず、6 で巣の 0.14 が満杯、2 で 0.75 が満杯・到着の 0.33 を断る**。**締めると人間が減る**（`pop` −10.42 \*）——**外敵を減らすと肉が減るから** |
| `NestCapMap` | （空＝どの巣も `NestCap`） | 世界 | **セルごとの定員を `NestCap` の何/5 かで塗った層**（2026-09-22。格子は `EnemyKindMap` と同じ）。**`0` は「ここは誰も抱えない」＝そこからは湧かない**。**Tiled では `cap` プロパティ** |
| `EnemyAmbientShare` | **0** | 世界 | **塗った巣を無視して地図の外から入ってくる割合**（2026-09-22。`TODO.md` の18番）。**既定0＝巣が全部を引き受ける、今までどおり**。**巣が全部静かになった／満ちた国を空のままにしないための逃げ道**で、**必要な地図だけが上げる**。**0 のときは乱数を1つも引かない** |
| `HumanNests` / `HumanNestMap` | （空＝人の巣なし） | 世界 | **人がどこから世界に入ってくるか**（2026-09-22。`TODO.md` の20番・#142）。**行が持つのは `Name` / `Key` / `Rate`〈何tickに1人〉/ `Life`〈いつまで〉/ `Cap`〈自分の血統を何人に保つか〉**、**塗りが持つのは名前と場所だけ**（#133。Tiled では `human`）。**巣ごとに1本の血統タグを配る**（建国者の続き番号）。**出産ではない**——親も代金もクールダウンも無く、`EnemySpawnTicks` と同じ棚。**人間が出産以外で増える唯一の経路**なので、**この世界の個体数は巣の無い世界と直接比べられない**。**測定: 3か所から60人を配ると血統は 5.08 → 2.67 \*\*\*、人口は滴らせると 155 → 76 \*\*\***（120年で −41 \*）。**年5人・定員20 で支えると3つの国が3つとも残る**（`lines` 2.92 / 3）**が、120年後には普通の世界と完全に同じ**（人間 177.1 対 177.1、血統 2.50 対 2.50） |
| `HumanNestTicks` / `HumanNestLife` | 500 / 5000（＝年1人・10年） | 世界 | **行が `Rate` / `Life` を言わなかったときの既定**（2026-09-22）。**名前と場所だけ言いたい行が数値を持たなくて済むように**——外敵の種類の表と同じ作法 |
| `BossBudget` | **0（＝主のいない世界）** | 世界 | **巣の主が、その種類の普通の1体の何倍の予算で作られるか**（2026-09-22。`TODO.md` の19番・#141）。**主は `World.Rouse` が呼ばない限り世界にいない**（engine の中の誰も呼ばない＝`Endow` と同じ棚）。**天井がある**——**遺伝子9本 × 上限100 ＝予算900** なので、**2 でも 4 でも同じ生き物**（平均的な獣の1.73倍・平均的な身体の2.16倍）。**測定: 呼び出した主のうち倒されるのは 0.23〜0.28 で、強さを変えても動かない**——**動くのは倒されるまでの年数**（3.51 → 6.36 \*\*\*）。**巣は run の0.22 の時間空くが、個体数も定住も動かない**（32シードで誤差内）。**だから既定はオフ** |
| `BossRouseTicks` | 500（＝1年） | 世界 | **呼び出された主が「帰ってよい」ことになるまでの tick**（2026-09-22）。**引っ込む条件はこれと「いま巣のセルにいる」の2つだけ**——**体力でも間合いでもない**（閾値を作らないため）。**帰る理由は `EnemyHomeCost` × `Homely`**。**測定: 呼び出した主の4分の3は倒されずに帰る** |
| `NestQuietYears` | 5 | 世界 | **主を失った巣が、次の主が入るまで湧かない年数**（2026-09-22）。**その間は湧かず、呼び出せない**。**死因は問わない**（倒されても寿命でも同じ）。**`BossBudget` が0の世界では1度も使われない** |
| `NestQuietMap` | （空＝どの巣も `NestQuietYears`） | 世界 | **セルごとの休暇の長さを `NestQuietYears` の何/5 かで塗った層**（2026-09-22。格子は `EnemyKindMap` と同じ）。**Tiled では `quiet` プロパティ**。**既定が5年なので、塗った数字がそのまま年数になる**（`quiet: 3` ＝3年） |
| `EnemyKind.Climbs` | false | 種類 | **ランプ無しで段差を1段ずつ上り下りできる**（2026-09-20）。**それ以外は歩く者と同じ**——コスト・溺死・速度・消耗は地面の言うとおり |
| `EnemyKind.Flies` | false | 種類 | **地面の上ではなく上空にいる**（2026-09-20）。**段差を何段でも越え、溺れず、引きずられず、消耗せず**、**地面のコストの代わりに `FlyCost` を払う**。**そして取引になっている**——**空にいるあいだは殴られず、殴れず、餌も取れない**（**石だけが届く**）。**食べる・殴る・拾うを選んだ瞬間に降り、そのあいだは普通の的**。**降りるための閾値も語彙も無い** |
| `EnemyKind.FlyHeight` | 0（＝上限なし） | 種類 | **この種類が飛ぶ高さ**（2026-09-20。**地図のタイルの `height`（1〜9）と同じ目盛り**）。**その高さまでの地面は越え、それより高いものには跳ね返される**。**跳ね返されても歩く者より不利にはならない**——**ランプや1段の段差は今までどおり使える**（飛ぶことは「余分な通り道」であって「劣った通り道」ではない）。**0 は上限なし**（この規則を書く前の姿） |
| `EnemyKind.FlyCost` | 0（＝1） | 種類 | **飛ぶことの代価**（平地を1とする倍率）。**0 は1として読む**——**段階20 の「渡れない地形は作らない」の鏡で、ただの地形も作らない**（谷をただで越えられると、谷は地形をやめて背景になる） |
| `EnemyKind.Meat` | 0（＝1） | 種類 | **この種類の死体の値打ち**（体の大きさに掛かる倍率）。**未指定は1で、全種類が `Bulk / MeatPerBudget` を落とす**（今までどおり）。**的の大きさは実測で、肉は食事の 0.16（20000tick）〜0.30（200000tick）を占め、落ちた肉の 0.67〜0.80 は食べられている** |
| `EnemyKind.Hide` | 0（＝1） | 種類 | **この種類の死体が落とす皮の枚数**（体の大きさに掛かる倍率。`TODO.md` の8番）。**未指定は1**。**`Meat` と別の欄なのは、「何が食べられるか」と「何が working できるか」が別の事実だから**——**肉ばかりで皮の無い獣は1行であって特例ではない** |
| `PlantKindMap` | （空＝塗らない） | 世界 | **どのセルがどの種類の植物を育てるかを1セル1文字で塗った層**（2026-09-20。文字は `PlantKinds[i].Key`、`.` と知らない文字と行の先は「塗っていない」）。**塗ってあればその種類、塗っていなければ `Share` の抽選**（塗りのほうが、そのセルについて細かく言っているので勝つ）。**地面から出てくるものだけを決める**——**種は親の種類を保つ**ので、歩いて種類が変わることはない。**Tiled では `plant` プロパティ（名前）**で、**名前は `PlantKinds` の行を指すだけ。数値は地図に置かない**（決定事項#133） |
| `RichMap` | （空＝塗らない） | 世界 | **どのセルがどれだけ食料を育てるかを1セル1文字で塗った層**（2026-09-20。**桁 ÷ 5**＝`5` 普通・`0` 不毛・`9` 豊か。`.` と知らない文字と行の先は普通）。**塗ると、世界の側の重み付けは走らない**——`FoodSpread` は引かれず、`TerrainFoodCorrelation` と `WatersideFood` も動かない（**作者が言ったことに二つ目の答えを足さない**）。**量は変えない**（`FoodSpawnRate` のまま＝段階15a の総量保存が塗りの中で閉じる。テストで固定）。**地域には塗りの平均が入る**ので、**信念（`regionlore`）・スキル・寒さは今までどおり地域を読んで、しかも本当のことを読む**。**細かくすることが目的ではない**——**視界（`PerceptionRadius`）より小さい区画は居場所を動かさない**（実測: 48区画・108区画で `richGain` が半減。`PLAN.md` P16-2）。**Tiled では `rich` プロパティ**（0〜2の数。**発生タイルと同じ層に載せてよい**） |
| `RegionMap` | （空＝塗らない） | 世界 | **どのセルがどの地域かを1セル1文字で塗った層**（2026-09-20。文字は `RegionShapes[i].Key`、`.` と知らない文字と行の先は「塗っていない」）。**塗った地域はどんな形でもよい**——矩形にできない L字や分かれた谷が描ける。**矩形と両方あれば塗ったほうが勝つ**（セルを塗るほうが細かく言っている）。**塗られていないセルは「それ以外の場所」**。**Tiled では3枚目のタイル層**、意味はタイルの `region` プロパティ（同じ名前のタイルは1つの地域）。**`TerrainMap` / `SpawnMap` と同じ「1セル1文字＋凡例」の形** |
| `RegionShapes` | （空＝グリッド） | 世界 | **地図の作者が描いた地域**（2026-09-19。矩形を地図に対する割合 0〜1 で持つ。2026-09-20 から `Key` を持てば矩形ではなく `RegionMap` の塗りが位置を言う）。**空なら `RegionCols` × `RegionRows` の等しいブロック**（既定 4×3）。**描いた矩形の外側は「それ以外の場所」という1つの地域**になり、**重なったら後の矩形が勝つ**。**各矩形は `shelter` / `food` / `special` / `enemies` を持てて、0 は「世界が抽選した値のまま」**。**`Goal` の印だけは engine が1度も読まない**（`World.GoalRegions()` で取り出せるだけで、判定はゲーム側＝段階19 の線）。**位置から地域を引くのは O(1) のまま**（200×200 の索引表を世界を建てるときに1度だけ作る）。**実装前に数えた**: `regionIndexAt` は1tickに3〜10回（ホットパスではない）／`worthOfRegion` は地域数に比例（12地域で15回/tick、108で122、768で1,900）／**1体あたりの記憶は 48バイト × 地域数**（12地域で576B、768地域で36KB）／**個体数は地域数では動かない**（6シードで 12/108/768 を比べて 30〜53 のばらつきの中。**1シードでは「増やすと壊れる」ように見えたが雑音だった**） |
| `GroundAheadSeen` | **true**（2026-09-19 に既定にした） | 世界 | **候補を「その手が向かう方角の1セル先の地面」で値付けるか**（段階100。`cmd/devview -ahead`）。**false は段階20 以来の「この先も足元と同じ地面」**。**読むのは移動コスト倍率と目減りの2つだけ**（溺れる確率は読まない＝`Hazard` の意味を変えないため）。**読みはその手の所要tick全体に適用する**（経路計画ではなく、仮定を1セルだけ先へずらしたもの）。**測定（96シード・基準 `riverswim`）: 地形が初めて居場所を動かした**——`onWater` 0.14 → 0.12（**−0.02 \*\*\***）、**情報のない対照では −0.00**（直接の対で **+0.01 \*\*\***）、`bankMoves` −1.20 \*\* 対 対照 +0.06。**個体数は裸の川で誤差内**（+0.65）、**遊ぶ地図では +10.20 \***、**段階99 の目減りと組むと +6.05**（目減り単独は −17.56 \*\*。**直接の対で +22.73 \*\*\***）。**それでも「岸へ向かう」割合は 0.492 で動かない**（川が2セル幅なので向こう半分からは安い方角が見えない）＝**効いているのは入る側**。**性能は同じ世界で +7.6%**（9,906 → 10,660 ns/op）。**2026-09-19 に既定にした**（ユーザの判断）＝**段階7b・先読みに続く3度目のベースラインのリセット**で、**`HISTORY.md` の 2026-09-19 より前の地形の数値は「足元の地面で値付けた世界」のもの**（`cmd/experiment` の `groundunread` / `playunread` と `cmd/devview -groundunread` で再現できる）。**平坦な世界は地面が無いので何も読まず、乱数も引かない**＝指紋テストは無変更で通る |
| `GroundAheadNoise` | 1 | 世界 | **上の読みに乗る誤差の大きさ**（段階100）。**`(MaxAbility − 理性)/MaxAbility × この値` を標準偏差とする正規乱数を1方位につき1回引き、コストには加算・目減りには比例で効かせる**（**地面を読み違える身体は1つの読み違え方をする**）。**加算なので、読みが悪ければ川を平地と取り違える**。**0 は「完全に読める」腕**——**そこで `dRationality` −3.27 \*\*・`shRationality` −0.01 \*\***＝**理性に値打ちを与えているのは誤差のほう**。**2倍（雑な読み）は個体数を払わない**（+2.06）**が、渡らなくなる**（`bankBoth` −0.07 \*\*\*）＝間違った回避 |
| `GroundAheadBlind` | false | 世界 | **上の対照**: **同じ回数の乱数を引き、同じ形で値付け、読む値は世界の平均の地面**（段階100）。**段階35 の教訓**（相関0.33 の不正確な信念でも個体数は同じだけ上がった）**に対する備え**で、**この腕があったから「情報が効いた」と言える**（`onWater` の差 +0.01 \*\*\*、`bankMoves` +1.27 \*\*、目減りのある世界では個体数 22.73 \*\*\*）。**注意: これは「何も読まない腕」ではなく「地図を平らに読む腕」**——水の中では移動が安く読めるので、行動は既定の世界とも読む腕とも違う |
| `WaterDrain` | **0**（無効） | 世界 | **水のセルに立っている身体が、何をしていても毎tick失う体力**（段階99。`cmd/devview -soak`）。**0 は段階98 までの世界＝水は移動への通行料だけで、立ち止まっている身体からは何も取らない**（`WaterMoveCost` の課金は「動いた tick」にしかない）。**場所が毎tick取る目減りは段階85 が作った経路**なので、**先読み・休息の見積もり・食事の見積もりが新しいコードなしに読む**（`placeDrain(s) = s.Chill + s.Soak`）。**目盛り**: 回復速度は 0.09/tick、水の中の移動コストは 0.0389/tick、水の上の body-tick は全体の 14.8%（うち **36.9% は身体が静止**）。**測定（96シード・基準 `riverswim`）: 0.02 は測定上タダ**（個体数 **+0.30 ± 6.54**）、**0.05 で −17.56 \*\***、**0.09 で −43.98 \*\*\***、**遊ぶ地図では −12.75 \*\***（割合では倍）。**この鎖で初めて「知っていること」が行動を変えた**（感じる腕は水の中で立ち止まる割合が **−0.02 \*\*\***）**が、居場所は動かず**（`onWater` +0.01 \*、感じない腕との差は +0.00）、**場所に紐づけることにも値打ちが無い**（同じ量を世界じゅうに一律に配る対照と +4.31 ± 6.75 ＝区別がつかない）。**水が集団に課している代価の大半はこの目減りではなく通行料**（`WaterMoveCost` を 1 にすると同じ目減りを受けたまま +22.76 \*\*） |
| `WaterDrainKnown` | true | 世界 | **身体が、水が自分から取っているものを感じるか**（段階99）。**false は同じだけ取るが効用式に載らない腕**（`DrownKnown`・`ChillKnown`・`WaterSlowKnown` と同じ形）。**この対照だけが、この鎖で初めて有意な行動の差を出した**——`wetStill` 0.38（感じない）対 0.37（感じる）。**ただし個体数の差は誤差内**（+3.40 ± 6.12） |
| `DrownUnskilledFactor` | **1**（無効） | 世界 | **泳ぎを1ミリも持たない身体が、水で死ぬ確率に掛ける倍率**（段階98。`cmd/devview -sink`）。**1 は段階34〜97 の世界＝水は泳げる身体と泳げない身体を同じ確率で取る**。**`SkillSwimRelief` と1本のカーブ**——無技能で この倍率、`DrownSkillFull` でちょうど 1、そこから先は既存の軽減が下へ続く（**同じ効き目についてスキルを2か所で読まない**）。**測定（96シード・基準 `riverswim`）: 規則は狙いどおり効き、世界は悪くなる**——**溺れた身体の泳ぎ 0.05 → 0.03**（一律の対照では 0.05 のまま＝**溺れる人が変わった**）、水の中の無技能の割合 0.65 → 0.55（5倍で 0.38）。**代価は個体数**（2倍 **−23.32 \*\*\***／3倍 **−40.50 \*\*\***／5倍 **−69.49 \*\*\***、遊ぶ地図で **−25.17 \*\*\***）。**`onWater` は動かず**（+0.01〜−0.02）**「知らない」対照と区別がつかない**＝効き目は全部選別（滞在は 116.9 → 127.5 tick とむしろ延び、死で終わる割合だけが 4.1% → 8.5%）。**そして個体差そのものには値打ちが測れない**——**同じ平均倍率を一律に配る腕が +8.46 \***、**全員3倍の腕は溺死がもっと多いのに +10.20 \***（段階95 と同じ形）。**泳ぎへの選択圧も出ない**（`swimReal` −0.08 \*\*、`shVitality` −0.01 \*\*＝**世界が貧しくなると天井の遺伝子も買われない**） |
| `DrownSkillFull` | 0.5 | 世界 | **この実効習熟度（`SkillSwim`）で、上の倍率がちょうど 1 に戻る**（段階98）。**「この世界で実際に届く習熟度」に置いてある**——実装前に数えた分布が二峰性で、**水の中の身体の 71% は泳ぎゼロ、持っている身体は 0.5 前後**（p50 0.000／p75 0.125／p90 0.525／最大 0.925）。**1 にすると線形のカーブ**（腕 `sinklinear`）で、**川で育った身体ですら罰の大半を払う**——ただし**測定では sink3 と区別がつかなかった**（+1.83、誤差内）。`DrownUnskilledFactor` = 1 のときは一度も読まれない |
| `DrownBeliefPerBody` | false | 世界 | **場所についての信念（段階35 の `danger`）を、訊いている身体自身の倍率で割り増すか**（段階98）。**既定は false＝場所の危険は場所の性質**（true にすると「あそこは危ない」が話し手と聞き手で別の数字になり、伝えているものが変わる）。**測定: 一番悪い腕**（基準比 **−46.84 \*\*\***、sink3 との直接の対で −6.34・`halfLife` −28.98 \*・`dIntelligence` −3.22 \*） |
| `WatersideFood` | 0 | 世界 | **地域に占める水の割合に応じて、そこに生える植物を増やす**（段階36）。**コストの相関（`TerrainFoodCorrelation`）とは別の項**——水は「渡りにくいから避けたい」と「そばが豊か」を同時に満たす必要があり、1本に混ぜると符号が矛盾して地図の作者が両方を要求できない。**総量は不変**（再正規化をテストで固定＝増えるのではなく内陸から移る）。**負の値は「オフ」ではなく別の風景**（痩せた水辺）。**既定0の理由は段階33 と同じ**（どの風景かは地図を描く側が決める）。**`cmd/devview -terrain` は 1 を渡す。** 実測（river、24シード×20000tick）: **+1 で `onWater` +0.01 \*・溺死 +3.25 \*・`nearest` −8.94 \*、しかし死亡率 −0.27 \*・餓死率 −0.24 \*・`grewUp` +0.02 \***／**−1 で `onWater` −0.03 \*\*\*・溺死 −4.79 \*\***（代価は個体数 −7.7 \*）＝**`onWater` は食料の分布にそのまま従い、危険や信念には従わない** |
| `RegionDangerTicks` | 700 | 世界 | **地域ごとの「危険」の信念を、何tickの滞在として値付けするか**（段階35）。信念は地面と同じ単位（1tickあたりの死ぬ確率）で持ち、地域の比較は「見えた食料の数」で走るので、**変換に新しい定数を作らず**、段階34 が足元の危険を値付けした式（確率 × tick数 × `LifeValue`）をそのまま使って `RegionDrawValue` で食料の単位に戻す。既定は `PlanHorizon` と同じ700。**0 は「信念は作るが効用式では weigh しない」対照**。実測: `onWater` は動かず（+0.01 \*）**溺死はむしろ +3.75 \***、代わりに個体数 +14.5 \*・`neighbours` +1.39 \*（**遠出が減るだけで、信念が不正確な腕でも同じだけ上がる**）。**2800（4倍）でも `onWater` は +0.00** |
| `RegionDangerTold` | true | 世界 | 危険の信念を交換で伝えるか（段階35）。**false にすると、目撃していても集団の知識にならない**——相関 0.77 → 0.33 で「立つだけ」（0.37）と同じ水準に落ちる。**溺死の目撃者は1件あたり6.3人だが、個体が一生に見るのは1件あるかないか**なので、地図に変えているのは伝承のほう |
| `DrownWitnessLooks` | 20 | 世界 | **誰かが溺れるのを見ることが、その地域を何回「見た」ぶんに当たるか**（段階35）。**0 で目撃を切る**。散歩1回より重いのは死が重いからで、**新しい単位を作らず「見た回数」で量る**（この信念の物差しを2つにしない）。**取り込むのは被害者の足元の真値**で、更新されるのは**死んだ場所の地域**（見ていた側が立っている地域ではない） |
| `DrownKnown` | true | 測定用 | 足元の溺れる確率を `Perception.Self.Drown` に載せるか。**false でも水は同じだけ殺す**（対照条件＝「選択」と「選別」を分ける）。**実測: false と有意差なし**（溺死 −0.79、`onWater` −0.00、個体数 +6.1 いずれも誤差内）＝**段階34 がしていることは全部選別**。理由は大きさ——40tick の道のりに乗る罰 0.8 に対し `ChoiceNoise` は 20 |
| `CourtAnswerTicks` | 60 | 世界 | 求愛された側の**コントローラが答えるとき**、求愛者が待つtick数。過ぎるとそのノード自身の規則（`willCommit`）が答える。**AIノードには一度も発火しない**（AIは「答える」と名乗らないので即答） |
| `BirthNeedsLowerIDFirst` | false | 測定用 | **2026-09-06 に直したバグを復元する**。true だと、走り終えたペアは「先に処理されたのが番号の小さいほうだったとき」しか子を産まない（実測でおよそ半分）。**この日より前の全測定はこの世界で取られている** |

### C-3. 戦闘

| 名前 | 既定値 | 効く先 | 意味 |
| --- | --- | --- | --- |
| `AttackDamage` | 1.15 | 世界 | 平均パワーの個体が effort 1 で与える1tickのダメージ |
| `DefenceCap` / `DefenceCost` | 0.55 / 0.12 | 戦闘 | 守りが受けるダメージを最大どれだけ減らすか／1tickの単価。**攻撃側有利の非対称量を直接動かすので、変えたら必ず個体数を見る** |
| `EvasionCap` / `EvasionCost` | 0.45 / 0.20 | 戦闘 | かわしが打撃を丸ごと無効にする最大確率／1tickの単価。移動速度にも比例する |
| `CourtClockResets` | false | 繁殖 | **true なら「待ち時間」が求愛のたびに振り出しに戻る**（段階28 以前）。**断られると患者が最初からになるので、我慢が尽きることがなく、誰も誰にも妥協しない**。false では「求愛できるようになった時点」から数える。直すと個体数 **+16.79 \*\***・出生 **+48.00 \*\*\***・世代 **+1.75 \*\*\***（代償は `killShare` +0.10 \*\*\*）|
| `CommitFloor` | 38 | 繁殖 | **いくら待っても受けない魅力の下限**（段階26）。0 なら段階26 以前＝我慢が尽きると誰でも受ける。**集団の魅力分布の下位4分の1あたり**に較正（p25 = 37.5）。**段階28（時計の修正）のあとに測り直した**: 集団の100%が「待ち疲れ」ているので**床が常に効く**ようになり、意味が変わった。**床0 にすると個体数 +6.96 \*・出生 +42.29 \*\*\* だが `shLooks` が 0.16 → 0.10 に落ちる**（−0.05 \*\*\*）＝**魅力遺伝子を買う理由が消える**。床50 は個体数 −10.71 \*\*。**38 のままにしたのは、求愛の3分の2が成立していて（`trueAccept` 0.65）、かつ魅力への選択圧を保つため** |
| `CourtNeedsSurplus` | false | 繁殖 | **true なら「腹が減っている／傷ついている」で求愛を候補から消す**（段階25 以前の挙動）。既定の false では効用式が判断する——`addCourt` が出産の代金（`BirthVitalityCost/2` × 受諾確率）と、それで死ぬ確率が上がるぶんを載せるため。**`ReproHunger` と `ReproVitalityShare` は true のときだけ効く** |
| `AttackCost` | 0.30 | 世界 | 殴る側が effort 1 で払う1tickの体力。**`AttackDamage` より小さい＝攻撃側有利** |
| `OpeningTicks` / `OpeningGuard` | 3 / 0.40 | 戦闘 | **外した打撃は、外した側に隙を作る**（段階24）。以後 `OpeningTicks` tick のあいだ、その個体自身の守りとかわしが `OpeningGuard` 倍になる。**世界で唯一「外すこと」に値段をつけている規則**で、回避率は隠しパラメータなので誰も事前に読めない。`OpeningTicks = 0` で完全に無効（乱数も引かない）。**個体数を 6.75 \* 払う**ので、変えたら必ず個体数を見る |
| `KnockbackDist` | **0（オフ）** | 戦闘 | **当たった打撃が、殴られた身体を攻撃者→被攻撃者の向きに押す距離**（#136。世界単位。満額の打撃 × 基準の大きさの身体のとき）。**実際の距離は「入ったダメージ ÷ `AttackDamage`」×「`MaxVitality` ÷ その身体の最大体力」に比例**。**目盛りは `CombatRadius` 15**——5 なら取っ組み合いは続き、20 なら毎回間合いが切れる。**個体数を大きく動かす**（`country` で 52 → 115）ので、変えたら必ず個体数と `killShare` を見る |
| `KnockbackFall` | 2.3 | 戦闘 | **押されて段差を落ちた身体が、落差1段につき失う体力**（#136。`KnockbackDist` が0なら一度も使われない）。**目盛りは `AttackDamage` 1.15 ＝打撃2発ぶん**。**数発ぶんにすると崖が地形をやめて扉になる**（段階20 の線）。**実測では発火が1実行に112回しかなく、落下ダメージを0にした対照と世界が区別できない** |
| `KnockbackThrown` | false | 戦闘 | **投げた石も押すか**（#136）。既定は false——**腕の長さの打撃には体重が乗り、石には乗らない** |
| `ShovePush` | **0（オフ）** | 戦闘 | **構えの4つ目で押す距離**（#139。世界単位。満額の押し × 基準の大きさの身体のとき）。**`KnockbackDist` と分けてある**——あちらは身に起きること、これは選ぶこと。**0 だと4つ目の構えを採点すらしない**（候補1つにつき乱数を1つ引くため）。**目盛りは `CombatRadius` 15** |
| `ShoveCost` | 0.20 | 戦闘 | **押しのチャネルを満額使う1tickの体力**（#139。`AttackCost` 0.30・`DefenceCost` 0.12 と並ぶ）。**押す構えの1tickの合計は 0.316 で、殴る構えの 0.312 とほぼ同じ**——**捨てているのは値段ではなく打撃** |
| `ShoveWorks` | 0.5 | 戦闘 | **「押すと相手が間合いの外へ出る」の初期の思い込み**（#139）。**事実の側の belief**（`Retaliation` / `AcceptChance` と同じ扱い）なので、これは出発点だけ |
| `ShoveLearnRate` | 1 | 戦闘 | **1回の押しがその belief をどれだけ動かすか**（#139）。**世界の `LearningRate`（既定0）とは別**——あちらが0なのは「殴り返される確率 0.7」が事実ではなく抑止力だからで、押しの確率には二重の役目が無い。**0 にすると学習も交換も止まる**（＝`shovefixed` の腕） |
| `ShoveLearnBoth` | true | 戦闘 | **押した側だけでなく押された側も学ぶか**（#139）。**入口が2つある belief は世界でこれが初めて**なので、片側だけの腕（`shoveone`）を対照に取る |
| `ShoveGroundSeen` | true | 戦闘 | **相手の向こう側の地面（落差・水）が見えるか**（#139。`ShovePush` が0なら読まず乱数も引かない）。**読みには理性の誤差が乗る**（`GroundAheadNoise` と同じ1回の抽選で両方の値が動く）。false が `shoveblind` の腕 |
| `FleeEffort` | 0.95 | 判断 | 逃げるときの effort（固定） |
| `SkirmishTicks` | 30 | 判断 | 「小競り合いはこれくらいで終わる」という**見積もり**。実際の戦闘長ではない。倒しきる前提で値付けすると誰も何も争わなくなるので上限を置いている |

### C-4. 推定と記憶

| 名前 | 既定値 | 効く先 | 意味 |
| --- | --- | --- | --- |
| `JudgementNoise` | 40 | 判断 | 理性0の個体が他者を見誤る標準偏差。魅力の推定にはこの半分 |
| `PriorStrength` | 50 | 判断 | 見た目から当たりをつけられないときの初期推定（学習前・観測数不足のとき）。**中立な置き場所ではありません**——50 は実際の平均攻撃力（36前後）より高く、よそ者を過大評価させることで世界を支えていました。36 に下げるだけで個体数が26%減ります（`lowprior` 条件。`HISTORY.md` 2026-09-03） |
| `LearnFromLooks` | true | 判断 | 見た目からの学習。false で初対面は全員 `PriorStrength` に戻る（段階10 前の世界） |
| `LooksSlope` | true | 判断 | 学んだ直線に傾きを許すか。false は「世界の平均だけを学ぶ」（体つきを読まない）条件 |
| `AppearanceNoise` | 6 | 判断 | 体つきの観測ノイズの基準（実際は `(100 − 理性)/100` 倍）。**強さの `JudgementNoise`（40）よりずっと小さい**のは、体格のほうが見て分かるから。12 にすると体つきの情報は完全に埋もれる（傾きの利得 0.34 → 0.10） |
| `LooksShowBulk` | false | true にすると、他個体から見える体つきが「2本の遺伝子（体力・速さ）の平均」ではなく**creature の総量**になる。**攻撃力はどちらでも隠れたまま**（総量は見せても配分は見せない）。**2026-09-04 の測定**: 既定では体つきと攻撃力の相関が 0.08 しかなく、**体つきを読んだ利得は統計的にゼロ**——体力と速さは予算制で**攻撃力と同じ財布を奪い合う**ので、隠すために選んだ変数がそのために予測力を失っている。true にすると相関 0.50 \*\*\*・利得 1.24 \*\*\* になるが、**推定の精度そのものは変わらず**（傾きが「世界の平均」のぶんを肩代わりするだけ）**個体数を 8.8 \* 払う**ので既定は false |
| `AppearanceMinReads` | 8 | 判断 | 自分の直線を使い始めるまでに必要な強さの観測数。3・20 と振っても推定誤差は動かない |
| `AppearanceSlopePrior` | 60 | 判断 | 傾きを0へ引き戻す「疑い」の回数。0（素の最小二乗）にすると傾きの利得が 0.34 → 0.20 に落ちる＝**観測数が数十しかない個体の傾きはほとんどノイズ** |
| `PriorVariance` | 420 | 判断 | その不確かさ |
| `CombatObsVariance` | 90 | 判断 | 直接戦って得る観測1回の分散 |
| `SpectateObsFactor` | 3.5 | 判断 | 観戦の観測はこの倍だけ分散が大きい（＝価値が低い） |
| `RiskDecayPerTick` | 0.0015 | 判断 | リスク記憶の忘却率。`Risk × exp(−率 × 経過tick)`。**率には個体ごとの `(100 − 記憶)/49.5`（0〜2倍）が掛かる**。0にすると全員が互いを最大脅威と記憶して世界が固まる |
| `MemoryCapacity` | 12 | 記憶 | 記憶が平均の個体が覚えていられる相手の数。個体の上限は `× 記憶/50.5`。あふれたら**リスクも好意もゼロのレコード**から捨てる。**0で無制限**（段階9 前の世界）。**掃引済み（2026-09-04）: 増やすほど単調に悪い**——6人 79.9／12人 77.7／24人 73.8／無制限 54.1（−23.6 \*\*\*）。理由は**記憶がこの世界で主に生産するのが「友人」で、好意は休息を安全に見せる**から。**`shMemory` が 0.06〜0.08 なのはバグではなく正しい値付け**（`HISTORY.md` 追記44） |
| `MemoryBandwidthShare` | 0.5 | 記憶 | 1tickに取り込めるレコード数（容量に対する割合）。専用の遺伝子は作らない。**0で無制限** |
| `ContactRefresh` | true | 記憶 | 視界に入れた相手のレコードの**減衰の起点だけ**を今に更新する（値は変えない）。帯域を消費する。false で段階9 前の挙動 |
| `AffinityPairBond` / `AffinityBirth` | 18 / 18 | 記憶 | ペア成立時・出産時に双方の記憶に入る好意 |
| `AffinityKin` | 22 | 記憶 | **直系の親子1ホップ**のレコードを作るときの初期好意。祖父母・孫・きょうだいは対象外 |
| `CallTicks` | 30 | 戦闘 | **「一緒に倒そう」と呼ぶのにかかるtick数**（段階32）。0 で**語彙から消える**（誰にも選択肢として出ず、誰も呼ばない）＝この段階の対照。**呼んでいることが見えるのは呼んでいる間だけ**で、状態としては持たない（行動から読み出す） |
| `AllyTrustWeight` | 1 | 戦闘 | **同じ相手に宣言している者の力を、自分の側にどれだけ足すか**（段階32）。掛かるのは好意ではなく**信頼**（`Affinity/AffinityTrust`）＝**土壇場で離脱しない確からしさ**（#56）。0 で誰も当てにしなくなり、**そのとき誘いは1度も選ばれない**（期待0＝攻撃の劣化版）ので、`CallTicks=0` と**1ビットも違わない世界になる**。0.5（誰も完全には当てにしない）でも個体数 +9.7 \* と効果は残る |
| `AllyPreyOnly` | false | 戦闘 | 味方を数えるのを**他種族との戦い（＝狩り）だけ**に限る。**測定用の切り分け**——true でも個体数の利得は同じ（+13.1 \*\* 対 +12.8 \*\*）なので、**利得は人間どうしの袋叩きから来ていない** |
| `SkillBirthplace` | **0** | 学習 | **生まれた地域の荒れ地の割合に掛けて、その身体が最初から知っている量**（段階38a）。**0 が既定＝世界にスキルは現れない**——段階30a・33・36 と同じ扱いで、**地形があってはじめて意味を持つ規則は地図の作者が入れるもの**（`cmd/devview -terrain` は 0.5）。既定を1にすると `rough` / `river` / `country` で取った過去の測定値が全部読み直しになる |
| `SkillRoughRelief` | 0.6 | 学習 | **完全に習得し完全に適性のある身体が、荒れ地の超過コストのどれだけを免れるか**（段階38a）。**掛かるのは超過分だけ**なので、どれだけ上手くても平地より安くはならない。1.0（全部消す）にしても `onDear` は動かない |
| `SkillAptitude` | 荒れ地＝`GeneSpeed`／採取＝`GeneMemory`／泳ぎ・毒耐性＝`GeneVitality` | 学習 | **スキルごとに、実効値の天井になる遺伝子の表**（実効 = min(名目, 遺伝子/100)）。**選び方は「その行いが元から属している遺伝子」**——地面を渡るのは脚、見つけたものから多くを引き出すのは**保てる量**。**表にしてあるのは選択が本物の分岐だから**（`skilltough`＝荒れ地を頑丈さで頭打ちにする条件、`foragelegs`＝採取を脚で。**どちらもニッチも選択圧も生まなかった**） |
| `SkillPoisonRelief` | 1 | 学習 | **毒のダメージと、その身体が警告に付ける値段**の両方をどれだけ下げるか（段階38b の4つ目。段階17b の復活が前提）。**本命は後者**——実装前に数えたら**毒そのものは回復の2.1%**しか奪っておらず、**恐れの代価が個体数の17%**だった。**しかし効かない**（個体数 +4.12 ± 6.52）。**理由は、17b の復活で信号に値段を付けたこと自体が恐れを消していたから**（信号 0.60 → 0.07）。**信号が高いままの世界でも +1.79 ± 5.69** ＝ 保有率0.38 × 実効0.50 では恐れの2割しか届かない |
| `SkillSwimRelief` | 1 | 学習 | **完全に習得し完全に適性のある身体が、水で死ぬ確率（`DrownChancePerTick`）のどれだけを免れるか**（段階38b の3つ目）。**効かせる先は渡るコストではなく危険**——段階34 が「個体は川を渡らず川で暮らしている」と示したため。**3つのスキルで唯一、狙った量が有意に動いた**（溺死 −1.83 \*、`drownRate` −0.03 \*）＝**打ち消す相手が死因の11%あったから**。**2026-09-19（段階98）から、これは1本のカーブの下端**——`DrownUnskilledFactor` が無技能の側を持ち上げ、`DrownSkillFull` で 1 に戻り、そこからこの軽減が下へ続く |
| `SkillSwimSpeedRelief` | 1 | 学習 | **完全に習得し完全に適性のある身体が、水の速度低下（`WaterSpeedShare`）のどれだけを取り戻すか**（段階97）。**`SkillSwimRelief` と別の目盛りにしてある**——**同じ数字にすると `swimdead` の対照が2つの半分を同時に消し、「泳ぎは溺れないことを買うのか、速さを買うのか」が測れなくなる**（段階62 の `SkillWard` が1つの数字を共有しているのは、あちらの2つが「効き目」と「その値付け」という同じ1つの事実の両面だったから）。**1に届かないぶんだけが軽減される**＝どれだけ上手くても水が陸より速くはならない。**測定: 効くが小さい**——**切ると個体数 −8.39 \***（規則の代価37 の22%を返している）、**水の中の身体が実際に動く速度は 0.60 対 地面だけなら 0.55**＝**返したのは用量 0.5 のうち 0.05 ＝ちょうど1割**で、**実装前に数えた「水の中の身体が持っている泳ぎは 0.072〜0.119」からの予想どおり**。**`swimWet`（水の中の身体の泳ぎ）は動かない**（0.11 → 0.10）＝**スキルは居場所を動かさない、の7度目** |
| `SkillForageRelief` | 1 | 学習 | **完全に習得し完全に適性のある身体が、飽きの割引（`SamenessPenalty`）のどれだけを免れるか**（段階38b）。**競争ではなく効率**（#64）＝上手い個体は少ない量で足りるので独占の余地が構造的に無い。**実測では効果がほとんど出ない**——飽きの割引がそもそも5%しかないため（`dietDiscount` +0.00 \*）|
| `SkillGeniusJump` | 0.4 | 学習 | **天才の子が、その血統より先へ行く量**（既存の `GeniusRate` を流用）。**無いカテゴリは発明しない**。**これを0にすると集団の名目値は出生地の値そのもの（0.50、ばらつきゼロ）に落ちる**＝**跳躍が作り、模倣が配る** |
| `SkillsSpread` | true | 学習 | **見て覚えられるか**（`HintsSpread` とは別）。**言い伝えは空き枠にしか入らないが、スキルは比べて上書きする**——この違いが拡散の本体。切ると名目値 0.80 → 0.54 |
| `AffinityWitnessKill` | 2 | 記憶 | **同族が他種族を仕留めるのを見た第三者**が、殺した側に対して得る好意（段階31）。`AffinityHunt` の第三者版で、**打撃を入れた者には入らない**（参加ぶんは `AffinityHunt` で受け取っている）。**枠が空いているときだけ記録**。0 で切れる。**発火機会は1件の殺害あたり 2.0 回**（`AffinityHunt` の20倍）だが、**48シードでは個体数も `friends` も動かない**（`avengehigh` ＝ 6 にしても同じ） |
| `KillWitnessFactor` | 2 | 記憶 | **殺すところを見て取る強さの観測**が、払って見る観察の何倍の分散になるか（段階31）。**1 より大きいこと**＝ただ見ただけの情報は `observe`（体力・時間・1度に1人）より粗い、が設計上の要求（#55）。0 で切れる。**実測では 1（＝観察と同格）にしても `watchShare` は動かない**——`observe` が買っているのは観測だけではなく**伝承の交換と「相手を選べること」**だから |
| `KillWitnessLooks` | true | 記憶 | 上の観測を、**見た目から強さを読む線**（段階10）にも反映するか。**この観測だけが世界の無作為な標本ではない**（全部が「今まさに勝った者」）ので切れるようにしてある。**実測では入れても外しても `priorErr` は動かない**（±0.8 の誤差内） |
| `AffinityHunt` | 6 | 記憶 | 死体の分配に**共に与った**者どうしが互いに得る好意。**血縁でも配偶者でもない相手から得られる唯一の好意**。0 で切れる。12・24 も測ったが、集団の指標はどれでも動かず**出生だけが減る**ので一番小さい値を採った（`HISTORY.md` 2026-09-03） |
| `FightTrustCost` | **0（オフ）** | 判断 | **殴りかかることで失う、その相手の好意**（#138(i)。`AffinityGift` 6 と同じ桁で目盛る）。**効用の `Lore` 項から `goodwillWorth × trustLost` として引かれる**——**贈与が好意を買うのと同じ物差しの符号違い**。**下限は0のまま**で、**嫌われている相手を殴る値段は他人と同じ**（**下限を開けると戦い一般への税になり、それは `AttackCost` の仕事**）。**この1本だけが効用式に触る**ので、**他の4つはこれが0だと何も動かさない** |
| `AffinityNegative` | **false（オフ）** | 記憶 | **好意が0を割ることを許すか**（#138(a)）。**単独では世界を1ビットも変えない**（読み手は全部 `clamp(…, 0, 1)`）。**動くのは継ぎ目3つ**——満杯の記憶の追い出しが**絶対値**で量るようになる／**嫌っている相手への贈与も信頼を買える**（下限を −1 に開く）／**負の記録も枠を作る**。**`AffinitySnatched` はこれが false なら黙って捨てられる** |
| `AffinitySnatched` | **0（オフ）** | 記憶 | **狙っていた品を他人に食べられたときに、その相手への好意から引かれる量**（#138(vi)）。**起点は「自分が `ActEat` の対象にしていた品を、他人が食べた瞬間」だけ**——**近くにいる・競合しているでは減らさない**（それだと豊かな場所で全員の好意が減り続ける）。**発火は1実行に 1100〜1900 回で、その9割は相手が他人**なので、**本体は「0 を負へ押し下げる」働き**。**2026-09-21 に読み手が付いた**（#140）——**「他人が既に狙っている品」が知覚に載り、取りに行く価値から `goodwillWorth × trustLost` が引かれる**。**その効き方は地図次第**: **平坦な世界では用量6で何も動かず（24 にすると `snatched` −334 \*・`starved` −12.8 \*）、遊ぶ地図では6で `snatched` −499 \* だが `births` −77 \*・`pop` −29.2 \*** |
| `AffinityAlly` | **0（オフ）** | 記憶 | **加担したときに双方が得る好意**（#138(iii)）と、**加担する動機として見込む額**（#138(iv)）**を兼ねる1本**。**払うのは「既に誰かが宣言している相手に殴りかかると決めた瞬間」**で、**tick ごとではない**。**強制記録**（`memFull` 0.99 なので、空き待ちにするとほとんど発火しない）。**目盛りは `AffinityHunt` 6**（共同で仕留めた者どうしと同じ） |
| `SnatchForgiveness` | **0（オフ）** | 記憶 | **取られて実際に好意を失うかを、既に持っている好意で重みづけた硬貨で決める**（#141）。**`1 − これ × 好意/AffinityTrust`** ＝**1 なら完全に信頼する相手は毎回許され、他人は一度も許されない**。**引くのは許すべき好意があるときだけ**なので、**他人ばかりの世界は乱数の並びが変わらない**。**測ると1実行に166回しか引かれない**（取られた 2200 回に対して7.5%＝**取っていくのは9割が他人**）ので、**世界はほとんど動かない** |
| `ClaimRetriggers` | **false（オフ）** | 判断 | **誰かが品を選んだ瞬間、その品へ既に歩いていて・その相手に好意があり・姿が見えている身体を起こすか**（#142。`TriggerClaimHeard`）。**`AffinitySnatched` が0なら発火しない**。**これが #140 の値付けを実際に動かすもの**——**無いと、既に食事へ歩いている身体は `TriggerIdleTicks` 40 の手持ち無沙汰まで気づかず、「取られてから考え直す」形になる**。**入れると平坦で `snatched` −515 \*\*・友から取る割合 0.09 → 0.05 \*\*\***、**遊ぶ地図で −741 \*\***。**代償は出生 −47 \* / −99 \***。**発信は作っていない**（届く範囲は claim と同じ視界） |
| `SnatchPriced` | **true** | 判断 | **取ったら失う好意を、レースの前の値付けにも入れるか**（#140）。**false にすると台帳は書かれるが誰も読まない**——**この形を対照として持っておくため**（この project が7度測った「ただの情報は世界を動かさない」の形）。**遊ぶ地図では true のほうが友情を守る**（`friends` +1.10 \* 対 −0.51） |
| `AllyPaidOncePerFight` | **false** | 記憶 | **加担の報酬を「既に殴っている全員」ではなく「側に付いた1人」と払うか**（#141）。**true で1実行の支払いが 22838 → 6255（−73%）**になるが、**`joinShare` は同じ**＝**加担そのものは払い方に依らない**。**変わるのは鋳造量で、`fightLiked` の上がり幅も餓死の減りもそれに比例する** |
| `AllyPaidOnBlows` | **true** | 記憶 | **加担の報酬を「実際に入った打撃」で払うか、「宣言された意図」で払うか**（#140。**`AffinityAlly` が0なら一度も呼ばれない**）。**true が打撃側で既定**——**死体の分配と同じ台帳（`recentAttackers` / `HuntCreditTicks`）を読み、1打撃ごとではなく1交戦ごとに払う**。**「同じ相手を殴った者どうし」と「自分を殴っている相手を殴った者」の2つを別に数える**（後者は宣言側が丸ごと落としていた半分）。**false は最初に書いた形**で、**同じ対を再判断のたびに払い、呼んで殴らなかった者にも払う**（1実行に 13954 回。**打撃側は 22838 回で、直したつもりが増えた**） |
| `AffinityKilledMine` | **0（オフ）** | 記憶 | **好意を持っていた相手を殺した者への減点**（#138）。**「死んだ相手への好意 × この係数」**＝**比例**（**他人が殺されても killer は何も失わない**。判断ではなく損失の記帳）。**`AffinityNegative` が false なら何も起きない**。**1 で測ると1実行に1844回発火し `worstAff` −154.5**（`AffinityTrust` の7.7倍）**だが、個体数も「誰を殴るか」も動かない**——**負の好意を読む者が世界にいないため** |
| `AffinityDecayPerTick` | 0.0008 | 記憶 | 好意の忘却率。リスク記憶より**遅い**（してもらったことは、されたことより長く残る） |
| `AffinityTrust` | 20 | 判断 | 「完全に信用しきる」好意の量。休息の露出（8-1）と、**一緒に倒す相手をどれだけ当てにするか**（段階32）がこれに対する割合で決まる。**較正は 2026-09-08 に閉じた**——10 / 20 / 40 と上限0.5 のどれでも個体数の利得は誤差内（+14.6 \*\* / +12.8 \*\* / +15.7 \*\* / +9.7 \*）＝**信頼が1に届いてよいかは世界にとって問題ではない**ので 20 のまま |

### C-5. 効用式の重み（判断の値付け）

効用 = Σ(目的の価値 × 達成確率) − リスク − 体力コスト − 時間コスト。

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `LifeValue` | 100 | 生命の価値。死亡確率の改善分に掛かる |
| `OffspringValue` | 42 | 子孫の価値。`× 相手の推定魅力/100` |
| `TimeCost` | 0.012 | 1tickあたりのコスト。時間は目的ではなくコスト |
| `VitalityWeight` | 0.55 | 体力1あたりのコスト |
| `PlanHorizon` | 700 | 何tick先まで見て死亡確率を見積もるか |
| `LookaheadHorizons` | **1** | 世界 | **1段目の先をさらに何 `PlanHorizon` ぶん見通すか**（段階67。**2026-09-13 に既定を 0 から 1 にした＝段階7b 以来のベースラインのリセット**）。**0 はそれ以前の全測定値が取られた世界**＝窓が1つで、**その内側では死にようがない身体（満腹・無傷）は勾配が平ら**になり、**「後のために取っておく」ことの値打ちがゼロ**になる（段階40・50・51 が当たった壁）。**既定にするまでに3度かかった**——**個体数はいつも「入れろ」と言っていた**が、**2度は同じ日に差し戻した**（空腹の身体が食べずに求愛する／追い詰められた身体が逃げずに寝る）。**理由は3つあり3つとも直した**: 2つ目の窓が**何も食べず何も治らない**身体を運んでいた（`LookaheadUpkeep`、段階72）／**消耗の危険を2度課していた**（`LookaheadWornAgain`、段階72）／**生命の項は「窓の終わりまでに死んでいるか」という yes/no** なので**1食が死を 594 → 1038 tick へ動かしても窓1400 では 0 点**（`LookaheadNeverBlinds`、段階74）。**新旧の対（48シード）: 個体数 +53.60 \*\*\*・`killRate` −0.48 \*\*\*・`halfLife` +17.13 \*\*\*・`dIntelligence` +12.98 \*\*\*・`rareTrough` +0.03 \***、**悪化は餓死率だけ**（+0.20 \*\*、ただし総死亡率は下がる）。**以前の世界は `cmd/experiment` の `noahead` の腕と `cmd/devview -noahead` で再現できる**。**性能は1決定 14.4 → 22.7µs**（計算そのものは +11%、残りは身体が1.5倍いるぶん） |
| `LookaheadUpkeep` | **0.75** | 世界 | **2つ目の窓で、自分の食い扶持と手当てをどれだけ賄えると仮定するか**（段階72）。**0 が段階67 のまま**（何も食べず、何も治らない）。**読むのは「自分がこれまで実際にどれだけ食べてこられたか」**（減衰する帳簿。**半減期は `PlanHorizon` ＝予測しようとしている窓そのもの**なので新しい数値ではない）で、**代謝以上には賄えない**（先読みは「このままだとどうなるか」であって「うまくいったらどうなるか」ではない）。**先読みを使うなら 0.75 を推奨**——**追い詰められた身体が再び逃げるのはそこから**（`LookaheadWornAgain = false` と対で必要）**で、上の端も残る**（`keepValue` 47.3）。**1.0 にすると上の端が消える**（不足を見ない身体は取っておく理由が無い＝`keepValue` 0）。**個体数は先読み単独 +43.06 \*\*\* に対し 0.5 で +58.82 \*\*\*・0.75 で +54.30 \*\*\***（96シード） |
| `CoinBuysMending` | false | **コインが「この身体が必要とするほうの1食」への請求権になるか**（段階81。治るものも売り物なので #73 は壊れない）。**新しい重みは無い**——`mealValueAt` の治りの頭打ちが重みそのもの（体力 100/70/40/20 で 1.00/1.20/1.96/2.52倍）。**測定では比は動くのに市場は縮む**（`sales` −0.50 \*\*）——**コインの値打ちは取引の両側にあり、歩いてくるのは買い手だけだから** |
| `LookaheadHolds` | false | **2つ目の窓が「手の中に何があるか」を知るか**（段階78a）。**level であって rate ではない**（1食は空腹の1段の下げ）。**割引は `LookaheadUpkeep` を流用**。**既定がオフなのは効かないからではない**——**市場を倍にする**（`sales` 1.76 → 3.35 \*\*\*）**が、機序は段階67 の裏返し**で、**払うのは「後で要ることの値打ち」**（`storeIn` −153.88 \*\*、平坦で両方入れると個体数 −9.41 \*）|
| `LookaheadSpoils` | false | **必要になる瞬間までに腐っているものは取っておく値打ちがゼロか**（段階78b）。**測定では両方の地図で全指標が誤差内**——**数えた的（手の中の品のうち1窓で腐るのは 1.9%／0.5%）のとおり** |
| `GoalsNeedSurvival` | **true** | 世界 | **後で起きる目的（子孫・探索）の確率に「そのとき自分がいる見込み」を掛けるかどうか**（段階73）。**false が段階72 までの全世界**で、そこでは**死にかけの身体にとっても子と散歩は健康な身体と同じ値打ち**。**効用式の目的には2種類ある**——**生命は2つの死亡確率の差なので窓が長いほど縮み、子孫（`OffspringValue × 魅力`）と探索（`ExploreValue × 空腹度`）は定数なので縮まない**。**並べると窓を伸ばすほど定数が勝つ**＝先読みを入れると空腹の身体が食べずに求愛した理由。**二重計上ではない**（生命の項はこの身体が生きていること自体、子孫の項は生き延びた先の出来事）。**割り引く時間は目的が来るまでの時間**——**子は `PairBondDuration` = 150tick 先なので歩く時間＋150、探索は窓まるごと**。**窓まるごとで子を割り引くと満腹の身体が求愛をやめる**（court 3.59 対 rest 2.75 の差を 9.5% の割引がひっくり返す）。**測定（96シード）: 既定の世界では無傷**（+2.50 ± 3.78、`deathRate` −0.14 \*\*、`killRate` −0.11 \*、`dIntelligence` +2.95 \*）、**先読みの世界では個体数を7 払って `killRate` −0.49 \*\*\*・`flees` +5.45 \*\*\***。**先読みを入れるときは一緒に入れる** |
| `LookaheadNeverBlinds` | **true** | 世界 | **先を見ることが、近くを見るより少なく語ることを許すかどうか**（段階74）。**false が段階73 までの全世界**。**生命の項は「窓の終わりまでに死んでいるか」という yes/no** で、**1食は空腹の身体の死を 594 tick から 1038 tick へ動かす**——**窓700 なら内→外で答えが変わり（値打ち 14.76）、窓1400 ならどちらも内側で答えが変わらない（値打ち 0.00）**。**食事が変わったのではなく質問が変わった**。**true にすると、差は2つの窓のうち大きいほうで読む**（閾値は要らない：飽和した窓の差は0、満腹の身体では近い窓の差が0）。**失うものは無い**（鎖は近い窓について単調なので順位は同じで、変わるのは「生命」と「定数で値付けされたもの」との為替レートだけ）。**測定（96シード）: 段階72 と合わせて +46.16 \\*\\*\\***（72 単独の +54.30 \\*\\*\\* から8 落ちるが、**壊れていた4つの場面が全部戻り**、`starveRate` は小さく `killRate` −0.38 \\*\\*\\*） |
| `LookaheadReadsRate` | **0** | 判断 | **餓死までのカウントダウンを、締切ではなく速度（ハザード `1 − exp(−窓 / 残りtick)`）として読む重み**（段階93）。**0 がそれ以前の全世界**、1 が全部ハザード、間は混合。**混合なのは、消えない裾が全員の危険を一様に持ち上げるから**（一様な持ち上げ＝`LifeValue` を上げること）。**平均 `pressure` は 0.1604 → 0.2273（×1.418）** なので、**対照は `LifeValue` × 1.418**。**測定: 狙った壁は消える**（`keepValue` が0だった読みの100%が正になる）**が、売買は落ちる**（−0.86 \*\*\*、4分の1の用量でも −0.81 \*\*\* ＝用量の問題ではない）。**個体数 −17.05 \*\***（**一様な対照は −32.22 \*\*\* ＝形は水準より良い**）。**性能は `BenchmarkDecide` 20.3 → 36.9µs** |
| `LookaheadWornAgain` | **false** | 世界 | **消耗していること自体の危険（`ShockRisk`）を2つ目の窓でも課すかどうか**（段階72）。**false が正しく、既定**。**`ShockRisk` は1地平ぶんに較正された定数**なので、**鎖につなぐと2度課され、傷ついた身体は規則が言うより1.5倍絶望的になる**——**逃げることの値打ちが 1窓 46.23 に対し 2窓 0.63**。**餓死はカウントダウンなので両方の窓に属するが、消耗は立ち続けている危険なので1つの窓のもの**。**true で段階67 のままの世界**（そこでは `LookaheadUpkeep` をいくら上げても追い詰められた身体は逃げない） |
| `ShockRisk` | 0.55 | 体力が減っていること自体の危険。これが無いと空腹でない個体にとって体力消費がタダに見えて世界が殴り合いになる |
| `RestExposureWeight` | 0.05 | 休んでいる間、周囲の強さのうちどれだけが自分に降りかかると見積もるか。好意のある相手は数えない。**小さいのは意図的**——0.10 で個体数が3分の2、0.20 で半分になる。**この値は `recoverable()` の中で `RegenRate`（0.09）から引かれるのに、隣人1人ぶんの脅威は 1.14/tick と12倍あるので、大きくすると「視界に1人いれば休息の価値ゼロ」という崖になる**（→ 消耗死が増え、ペアが出産前に壊れる）。0 で段階9 前の挙動 |
| `CompetitionWeight` | 0.10 | 将来の食料競合を今のうちに減らす価値。**世界の暴力度に非常に敏感** |
| `RiskWeight` | 0.22 | 過去にその相手に払わされた体力を、どれだけ避ける理由にするか |
| `InfoValue` | 1.2 | 相手を知ることの価値 |
| `ExploreValue` | 6 | 見えていない食料を探しに行くことの価値 |

### C-5b. 式の中の仮定（個体ごとの値の中心と広がり）— 段階12a

C-5 の `ShockRisk` / `CompetitionWeight` / `RiskWeight` は、段階12a から**その値そのもの**ではなく**個体ごとの値が引かれる中心**になりました。`Retaliation` / `AcceptChance` は `controller.go` の package const から移ってきたものです（D節にあった「移すべきか＝高」の2つ）。

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `Retaliation` | 0.7 | 「攻撃したら相手が殴り返してくる度合い」の**初期の思い込み**。個体はここから始めて経験で動かす。**世界の実測値はおよそ 0.15** |
| `AcceptChance` | 0.6 | 「求愛が受け入れられる確率」の初期の思い込み。**世界の実測値はおよそ 0.55** で、こちらはほぼ当たっていた |
| `LearningRate` | **0** | 観測1回が信念をどれだけ動かすか（走る平均に対する割合）。**0 で全員が何も学ばない＝既定。効かないからではなく効くから**（下記） |
| `LoreMemory` | 60 | 1つの信念の背後に立てる証拠の数の上限。上限が無いと、老いた個体は世界が変わったことに気づけない |
| `LorePriorCount` | 8 | 初期の思い込みが何回ぶんの証拠に相当するか。最初の1回で信念が飛ばないための重し |
| `LoreInitSpread` | 0.15 | 初期個体の**好み**を中心の周りに引く広がり（比例）。**較正済み**: 0.25 では個体数を2割払う、0.15 なら誤差の中（−4.2 ± 4.4、24シード）で、末尾に残る広がりは本物 |
| `MateWeightSpread` | **0** | **「子が何の値打ちか」を身体ごとに違わせる広がり**（段階94。**0 が段階94 以前の全世界で、乱数を1つも引かず突然変異も走らない**）。**効用式の「目的の側」に個体差が入る初めての規則**（既存の選好3つはどれも危険の読み方）。**中心は `OffspringValue`、変異は `LoreMutationStd`、伝承は `LoreExchangeRate` と、既存の経路にそのまま乗る**。**測定: 集団平均が上へ動く**（42.00 → 42.32 \*\*、用量2倍で 42.75 \*\*\*、**伝承を切ると 44.37 \*\*\***）＝**手で置いた 42 は、選択が選ぶ値より低かった**。**代価はほぼゼロ**（個体数 +4.83 ± 6.35）で、**`halfLife` +24.48 \*・出生 +19.90 \***|
| `NoiseWeight` | **1** | **候補の採点の誤差の振幅にかかる倍率**（段階95。**1 が段階95 以前の全世界**）。**これが「一律に配る腕」で、段階95 の対照そのもの**。**測定: 世界はこの水準に強く反応する**——**1.15 で個体数 −14.18 \*\*・出生 −40.66 \*\*、0.93 で +4.30 ± 6.27・出生 +24.45 \***。**そして `dIntelligence` は逆符号**（1.15 で **+4.27 \*\***、0.93 で **−2.22 \***）＝**頭の良さのこの世界での唯一の仕事がこの誤差を打ち消すことなので、世界を騒がしくすれば知能への選択圧は作れる**。**片方だけ良くなっていないかを確かめる理由の実例** |
| `NoiseWeightSpread` | **0** | **その倍率を身体ごとに違わせる広がり**（段階95。**0 が段階95 以前の全世界で、乱数を1つも引かず突然変異も走らない**）。**中心は `NoiseWeight`、変異は `LoreMutationStd`、伝承は `LoreExchangeRate`**。**測定: 水準は効くが、ばらつきは効かない**——**0.15／0.30 のどちらも個体数は誤差内**（+3.80 ± 6.75／+4.60 ± 6.75）。**引く乱数は平均を保つので、水準を揃えた一律の対照は baseline そのもの**。**選択の意見は下向きだが、伝承がそれを隠す**（伝承ありでは平均 1.00 のまま、**切ると 0.93 \*\*\***）。**そして伝承を切った世界で同じ 0.93 を全員に配ると、ばらつく腕より良い**（+14.35 \*\* 対 +10.96 \*）＝**選択は水準を見つけただけで、ばらつきそのものは何も買っていない** |
| `LoreMutationStd` | 0.08 | 継承時に好みに乗る正規乱数（比例）。遺伝子の突然変異より小さいのは、50回に1回ではなく毎回起きるから |
| `LamarckRate` | 0 | 親が学んだ**信念**をどれだけ子に渡すか。0＝子は自分で世界を知る、1＝親の到達点から始める。**段階12a で初めて読まれるようになった** |

**`LearningRate` が既定で0である理由（2026-09-04 の測定）**: 学習は動きます——信念の誤差は 0.56 → 0.40（\*\*\*）。動くのに止めてあるのは、**世界の真の殴り返し率が 0.15 しかなく、0.7 という思い込みが世界を支えていた**からです。真実を知った世界は個体数が半分になります（19.0 → 10.1、\*\*）。しかも**殺し合いが増えるのではなく**（`killShare` はむしろ −0.02）、小競り合いに体力を使って散らばり、**餓死が増えます**（`starveRate` +1.71 \*\*\*、`nearest` +42.8 \*\*\*）。全員に真実（0.2）を配ると個体数は3分の1（6.1）まで落ちます。

### C-5c. 伝承とヒント（段階12b・12c）

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `LoreExchangeRate` | 0.02 | 交換1回で互いに差の何割ぶん近づくか。**0 で伝承なし。** 「中間で会う」0.5 は**間違い**だった——交換は1実行に約19000回起きるので、好みの多様性が数世代で消え、個体数が半減する（`sdShock` 0.089 → 0.055）。0.02 なら多様性も個体数も伝承なしの水準に戻り、好意のある相手の数は3倍になる |
| `AffinityLore` | 30 | 交換で動いた量あたりの好意の加点（動いた量は「世界の値に対する割合」で数える）。**1〜30 で振っても個体数も好意の人数も動かない**（効いているのは1回の大きさではなく回数）ので、出産の好意（18）に合わせた値のまま |
| `LoreValue` | 9 | 「好意のある相手を観察する」ことの追加価値＝**他個体が生きていることが自分の得になる2本目の経路**。0 で切れる。**伝承が入っている限り個体数の持ち出しはゼロ**（交換だけの条件と同じ個体数）で、交換の頻度は2.6倍・好意の人数は1.4倍になる。**この定数は2つの意味を兼ねている**（「交換される知識1単位の値打ち」と「好意1単位の値打ち」）——**段階96 が触るのは後者だけ** |
| `EatTicks` | **0** | **食べ物のところに立っている時間**（行動の拘束時間、#76。**0 が段階番号なしのこの項目より前の全世界＝手が届いた瞬間に食べ終わる**）。**`actionTicks`（呼びかけ・掲示・調理・執筆と同じカウンタ）に1行足しただけ**で、**新しい並行の仕組みは無い**。**時間は食べ物のところで使う**（歩いている間はカウンタを0に held）。**拾う（`ActTake`）は1tick のまま**（#76 の線）。**測定（96シード）: どの用量でも個体数を払う**（**8 で −6.49 \***、**40 で −45.84 \*\*\***。餓死率 +0.85 \*\*\*、世代 −2.17 \*\*\*、最近傍 +25.67 \*\*\*、`killShare` −0.12 \*\*\*）。**占める時間は用量どおり**（食べ物のところに立つ体-tick が 0.0013 → 0.0183 → **0.1439**）**だが、露出は一度も発火しない**（食事中に殴られている割合はどの用量でも 0.005）。**休息は増えず**（0.0348 → 0.0322）**時間は歩くことと戦うことから出てくる**（戦闘 0.1856 → 0.1143） |
| `EatTicksKnown` | **true** | **`EatTicks` の対照**（#76）: **false なら世界は身体を拘束するが、効用式は1tick のままで値付ける**。**段階34 の `DrownKnown`・段階86 の `ChillKnown` と同じ型**。**測定: 知っていることは、小さい用量では害になり、大きい用量では役に立たない**——**用量8 で `gather` − `gatherblind` ＝ 個体数 **−9.44 \***・人間 **−10.71 \*\***・`dIntelligence` −3.45 \***、**用量40 で +6.23 ± 9.59**。**機序は「代わりが無い費用を正確に値付けると、身体を解決から遠ざける」**（食事より安く見える手はどれも腹を満たさない）＝**段階12a の `retaliation` と同じ形**。**「効き目は全部選別で選択ではない」の3例目だが、知ることが有害だったのは初めて** |
| `AllyValue` | **0** | **好意そのものを欲しがる量**（段階96。**0 が段階96 以前の全世界**）。**好意1単位の値打ちに `AllyValue/(1 + 視界にある信頼の合計)` を足す**——**逓減なので、1人でも持てば満たされる＝退蔵しない**（#112(c) への答え）。**読む場所は3つ**（渡す・掲げる・本を書く）。**測定（96シード）: 規則は強く発火し、用量に単調**（贈与 **+131.06 \*\*\*** → **+197.49 \*\*\*** → **+415.24 \*\*\***）。**低い用量では代価が無い**（個体数 +4.03 ± 6.17、`dIntelligence` +2.80 \*）**が、平均13 を超えると払い始める**（個体数 **−7.67 \***、`dIntelligence` **−4.24 \*\***）**うえ、理由の無い対照に負ける**（下記）。**既定は0** |
| `AllyFlat` | **0** | **`AllyValue` の対照**（段階96）: **同じ量を、誰が近くにいようと一律に**。**これが無ければ「理由が効いた」のか「単に好意が高いと効いた」のかが区別できない**（段階54・95 と同じ型の対照）。**測定（用量2点で対にした）: 低い用量では区別がつかず**（贈与 +134.77 対 +131.06、他も全部誤差内）、**高い用量では理由の無いこちらが勝つ**（**贈与 752.8 対 706.1、個体数 −3.71 対 −7.67 \*、`dIntelligence` −1.55 対 −4.24 \*\***）。**機序は逓減の係数が「贈与が候補に挙がった決定」で 0.6318 と全決定の 0.7173 より 12% 低いこと**＝**渡せる相手が視界にいる身体は、すでに視界に信頼を持っている身体**。**`halfLife` はどの腕でも上がるが、誤差 ±20 前後の腕が多く順位づけできない** |
| `HintSlots` | 4 | 1個体が持てるヒントの上限本数。**0 で段階12c ごと無効**（ただし枠が予算を食うので、比較は必ず対で読む） |
| `HintSlotCost` | 5 | 枠1本が遺伝子予算から引かれる量。**空の枠でも払う。** 較正済み: 15 では個体数を払う（−3.9、48シード）、10 でも払う（−2.7）、5 で誤差の中（+1.5）。**0 にすると経済が消える**のでしない |
| `HintWeightStd` / `HintWeightMax` | 6 / 20 | 重みを引く・変異させる標準偏差と、その範囲。効用の単位（候補のスコアは5〜30、`ChoiceNoise` は20） |
| `HintTradeWorth` | 0.5 | ヒントを1本渡したことが交換の「動いた量」に足す分 |
| `LessonSlots` | **0（オフ）** | **1個体が持てる「教訓」の上限本数**（#137。**0 が段階番号なしのこの項目より前の全世界**）。**ヒントの枠とは別の枠**——**`GeneMemory`（記憶容量＝覚えていられる人数と忘却の速さ）とも、`HintSlots`（生まれつき持っている思い込みの置き場）とも役割が違い、ここに入るのは「この身体が見た死から作った規則」だけ**。**既定を0にしないと指紋が動く**（枠は予算を食うので、全身体が少し小さくなる） |
| `LessonSlotCost` | 2.5 | **教訓の枠1本が遺伝子予算から引かれる量**（#137）。**目盛りは `HintSlotCost` 5**——**教訓は〈状況×行動〉の組だけで重みを持たない**（重みは世界が決める）**ので半額** |
| `LessonWeight` | 3 | **教訓1本が、その行動の評価から引く量の大きさ**（#137。**常にマイナス**——教訓は止まった身体から学ぶものなので）。**0 がこの規則の対照**（`lessonblind`）: **枠は買われ、死は見られ、パターンは昇格して枠を埋め、どれも決定に届かない**＝「知ることだけが無い」 |
| `LessonRipeTwice` | true | **同じ鍵を2度見て初めて枠に入るか**（#137）。**1度は事故、2度はパターン**。false（`lessononce`）が、**2度目という条件が本当に効いているか**の腕 |
| `LessonsSpread` | true | **教訓が観察で他個体にコピーされるか**（#137。`HintsSpread` と同じ形で、同じ `exchangeLore` に相乗り）。false が「学ぶが教えない」の腕 |
| `Correlate` | **false（オフ）** | **「この状況でこの手をとると、少し先でこういうイベントになりやすい」を数える計器を動かすか**（`TODO.md` 24番・#148。`engine/correlate.go`）。**規則ではなく測定器**——**世界の規則を1つも変えず、乱数を1つも引かない**ので、オンにした世界はオフの世界と1ビット違わない（テストで固定）。**既定がオフなのは値段のため**（毎tick 全身体の `selfView` を組み直し、イベントごとに目撃者を走査するので**1.90倍**遅くなる）。**測定値は `docs/history/findings/4.md`** |
| `CorrelateWindow` | 700 | **1つの決定が「あとで起きたこと」の原因として数えられ続ける長さ**（tick）。**計器で唯一「短すぎると関係を隠してしまう」数字**なので、`PlanHorizon` と同じ＝この世界が意図して先を見る一番遠いところに置いてある。**ただし〈イベント→イベント〉の表にとっては長すぎる**——**700tick ではほとんどのイベントがほとんどのイベントに先行しているので割合が1に飽和し、リフトが潰れる**（最大1.93）。**30tick にすると構造が出る**（`countnear` の腕。「硬貨 → 硬貨」6.10） |
| `CorrelateTrace` | 4 | **1個体が原因の候補として抱えておく直近の決定の数。** **1 にすると `lesson.go` と同じ**（「そのとき何をしていたか」しか学べない）——**飢えた身体を肉まで運んだ手は、食べることではない** |
| `CorrelateSample` | 25 | **対になる問い**（互いに相手の持ち物を欲しがる2体がいるか／売り手の3条件がそろっているか）**を何tickごとに訊くか**。近傍について二乗の仕事なので掃かずに標本を取る（段階70 が手の塞がりを数えたのと同じ間隔） |
| `HintsSpread` | true | ヒントが個体間でコピーされるか。**false にすると血統でしか伝わらない**＝「良い手口は模倣で広まるのか」の対照条件。**実測で、手口の多様性を作っているのはこちらだった**——コピーを切ると1個体が持つヒントが 1.50 → 0.77、種類数が 7.11 → 4.38（伝承なしと同水準）まで落ちる。重みを0にしても種類数は落ちない |

### C-5f. 地域と休憩場所（段階14）

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `RegionCols` / `RegionRows` | 4 / 3 | 世界を分ける粗いブロックの数（200×200 が12枚）。**粗いのは意図的**——細かいと歩き回るうちに平均化されて「良い場所」が存在しなくなる。**この1種類の地域に、段階15 の食料の豊かさも将来の地域リセットも乗せる**（用途ごとに区画を作らない） |
| `ShelterSpread` | 0.6 | 地域ごとの「休息の露出」係数のばらつき（1 ± spread を [0,2] にクリップして `RestExposureWeight` に掛ける）。**0 で全地域が普通の地面になり、かつ乱数を1つも消費しない**＝段階14 前と1ビットも違わない実行になる（対照条件）。**較正済み**: 0.6 なら効果（`shelterGain` +0.02 \*\*）が出て他の指標は全部誤差内、1.0 なら効果は3倍（+0.06 \*\*）だが出生率 −0.20 \* と `halfLife` −108 \* を払う |

| `FoodSpread` | 0.6 | 地域ごとの「植物の生えやすさ」のばらつき（段階15a）。**総量は1つも変えず、どこに生えるかだけを変える**——`FoodSpawnRate` は世界で一番選択圧に敏感な値なので、総量が動く規則は別のものを測ってしまう（テストで固定）。`ShelterSpread` と同じく **0 で乱数も消費せず段階15a 前と1ビットも違わない**。**較正済み**: 0.6 は世界を良くする（`rareTrough` 0.25 → 0.40 \*、`nearest` −19.5 \*、餓死 −0.66 \*）が、1.0 は壊す（個体数 −13.3 \*） |
| `EnemyKinds` | **空（＝1種類）** | **外敵の種類の表**（段階59、`engine/enemykind.go`）。**1行が持つのは4つ**——`Name`（見る側のため。規則は読まない）／`Share`（湧きに占める割合。**正規化されるので合計を1にしなくてよい**）／`BudgetMean`・`BudgetStd`（**0 なら `EnemyBudgetMean` / `EnemyBudgetStd` に落ちる**）／`Homing`（段階58 の地図の重みにどれだけ従うか。1で完全、0でどこにでも）。**空なら1行＝段階58 までの外敵で、選ぶものが無いので乱数を引かない**（指紋テスト無変更）。**増やすのは1行だけ**（規則は行数を数えず、意思決定エンジンに種類の分岐は無い＝3行のテストで固定）。**種類は種ではなく交配する**（子の種類は予算と同じコインで、親が同じなら乱数を引かない）。**測定（48シード、対照は「同じ中身を2行に分けただけ」）: 表は効く**（`kindGap` 1.57）、**`Homing` は効かない**（+0.01 ± 0.02）、**混ぜ方が選択を変える**（軽×3・重×1 で `killRate` +0.20 \*\*・`dPower` +3.69 \*／軽×1・重×1 で `hunts` +8.15 \*・`halfLife` +6.45 \*・`dIntelligence` −3.98 \*\*）。**`packSize` はどちらでも動かない** |
| `FoodRenormalize` / `FoodTotalFromMap` | **true / false** | **地図が食料の総量を決められるようにするトグル2つ**（段階61）。**`FoodRenormalize`（既定 true）は段階15a・33・36 の「重みを元の合計に戻す」ループ**だが、**これを外してもほぼ何も起きない**——**地域の重みは `pickFoodRegion` で比としてしか使われない**ので、全部を同じ数で割り戻しても配分は動かない（**川の地図では1ビット同じ実行**。テストで固定）。**動くのはクランプ [0,2] に当たったときだけ**（荒れた country で個体数にして2.4ぶん）。**効くのは `FoodTotalFromMap`（既定 false）のほう**＝**1tickに生える量 ＝ `FoodSpawnRate` × 地域の重みの平均**で、**これが保存則を初めて意図的に緩める場所**。**測定（country・48シード）: 用量 ±6〜13%（`foodMean` 0.94〜1.13）では個体数は 1.0〜1.3 SE しか動かず、古い旋盤（`FoodSpawnRate` 直接）で同じ用量を回しても +0.54 ± 7.46**＝**その地図がその margin で食料に頭打ちされていないだけ**。**用量は `foodMean` / `plantRate` の列で読む**（ティが重みを切り詰めるので地図からは見積もれない） |
| `EnemyKind.Water` | **false** | **その種類が水に住む**（段階63）。**水のセルから湧き**（`spawnFish` と同じキャッシュ）、**水では溺れない**——**フラグ1つに意味が2つ乗るが、それは同じ1つの事実**（川に住むものを川は取らない）。**水の無い地図では普通の種類と同じように湧く**（湧きを黙って落とすと外敵の数が黙って変わる）。**既存の食料魚には触らない**（#95。魚は食料アイテム、これは身体）。**測定（country の地図・48シード、対照は同じ2行の表で水に住む行がないもの）: 仕組みは動き、人は川から出ない**——`enemiesWet` 0.12 → 0.21 / 0.30（+0.09 \*\*\* / +0.18 \*\*\*）に対し **`humansWet` は 0.11 → 0.15（+0.04 \*）と逆向き**。**溺死 −4.69 \*\* は死因が排他だから**（獣に殺された身体は溺死に数えられない）。**危険で人を水から出そうとしたのは34・35・63 の3度で、3度とも動かず、動かしたのは段階36 の食料だけ** |
| `SkillWardRelief` / `EnemyKind.Ward` | **0.5 / `SkillNone`** | **その獣の扱いを知っていることが、打撃から引くぶん**（段階62）。**どの獣について知っているかは種類の行が名指しする**ので、**2つの種類を同じ言い伝えで防げる**（別々にするならスキルの列挙に1行）。**形は `SkillPoison` の流用で、2つの半分が同じ数字**（打撃の割引と、その獣に付ける値段＝`AgentView.Ward`）。**天井は防御力**（段階47 の理由づけ）、**出生地の種は「その地域に言い伝えのある獣がどれだけ湧くか」**。**測定（48シード、対照は同じ表・同じ教え方で知る価値のある獣がいない腕）: 仕組みは動き、世界は動かない**——`wardHeld` 0.26〜0.32、`wardReal` 0.24、**的に届くのは人間の死の2%**（0.26 × 0.24 × 0.5 ＝3.1%、人間の死のうち外敵が関わるのが65%）。**#94 の賭け（命に関わる的なら到達率が大きい）は外れ**。**実効値が伸びないのは、天井の防御力が予算比 0.06 ＝一番買われていない遺伝子の1つだから** |
| `NursingSpeedShare` / `NursingAlways` | **1.0 / false** | **子が `RearingRadius` の内側にいるあいだの母の実効速度**（段階66。1.0＝無効）と、**その対照**（子の有無と無関係に、育児中ずっと遅くする）。**`MaxSpeed` の掛け算には入れず**（#99。あれは世界を知らない静的な鎖）、**気分（段階54）や足元の地面（段階57）と同じ「毎tick書き直される事実の写し」**として持つ。**速さを読む3か所（`moveDir` / `evasion` / `selfView`）は同じ値を読む**。**判定は tick の先頭の専用の走査**（移動ループの中では間に合わない。**`stillReared` は答えると同時に子ども時代を1tick使うので呼べない**）。**測定（48シード）: 狙ったものは全部動く**——`rearNear` 0.73 → 0.90（+0.17 \*\*\*）、`rearMoved` +190.01 \*\*、`grewUp` +0.04 \*\*\*、子の死亡割合 −0.03 \*\*\*、代価は `humans` −5.17 \*・出生 −17.62 \*。**ところが対照が同じかそれ以上動かす**（`rearMoved` +251.69 \*\*\*。用量は 1.005倍で揃えた）＝**買っているのは「遅い母」であって「子のそばで遅い母」ではない**。**構造が効いたのは集団の持続だけ**（`halfLife` +17.26 \*\* 対 +4.22 \*）。**境界のガタつきは無い**（状態が変わるのは 18 nursing-tick に1回） |
| `MateLoreChance` / `MateLoreTrades` | **0 / 1** | **出産の瞬間に、配偶者どうしが既存の交換を1回行う確率**と、**その瞬間が何回ぶんか**（段階65）。**新しい交換式は無く、`exchangeLore` をそのまま呼ぶ**（信念5つ・ヒント・スキル・地域の知識がまとめて動く）。**0 で乱数を1つも引かない**。**なぜ出産の瞬間か**: **ペアが手を組んでいるあいだの交換は実測でゼロ**（`stepPaired` は歩かせるだけで `Decide()` も `ActObserve` も走らない）で、**交配と出産は同じ瞬間**だから。**実装前に数えた的**: 1実行の交換 31,304 回に対し出生 317 回＝**1出生1回で交換全体の 1.01%**。**測定（48シード）: 狙った量は動き、世界は動かない**——`mateGap`（出産の瞬間の親2人の隔たり、％。**交換の前に読む**）2.37 → 2.28 / 2.29 / 2.18（−0.10 \* / −0.08 \* / −0.19 \*\*）、`loreSpread` −0.12〜−0.18 \*、`teachTop` −0.01 \*、個体数は1 SE（**用量5倍の腕が一番小さい＝雑音**）。**用量5倍で縮みは2.4倍**＝段階12b の「1回の歩み寄りは差の2%」がそのまま効く |
| `EnemyKind.CanEatPlants` / `PlantAppetite` | **false / 1** | **その種類の外敵が人間の食料を消化できるか**と、**植物が肉の何割の値打ちか**（段階60）。**消化可否は候補への門**（`canEat` の1か所。通る5経路は自動）、**食欲は `dietValue` の中の掛け算1つ**（**見積もりと一口が同じ倍率を通る**ので身体が自分について嘘をつかれない）。**確率のコインフリップは無い**＝**「滅多に食べない」は効用比較の結果**（段階25 の2階建て。**同じ空腹で食欲1.0 なら食べ 0.25 なら食べないことをテストで固定**）。**測定（48シード、対照は同じ1行の表で植物に何も書かない腕）: 外敵が4倍**（`enemies` 12.4 → 49.0、+36.67 \*\*\*）、**人間は −6.17 \***、`plantsToEnemy` 952（世界が生やす 4000 の24%）、`hunts` +46.98 \*\*\*、`killShare` +0.03 \*\*\*。**発火機会は 0.68**（外敵の3分の2が視界に植物を持っている）。**食欲4倍でも食べた量は1.9倍**＝**段階16 の飽きの割引が新しい食べ手にも効く** |
| `HumanHomeCost` | **0（オフ）** | 世界 | **人間が生まれた場所から離れることの代価**（`TODO.md` の6番・#130。地域の幅あたり・1tickあたり。段階64 の `EnemyHomeCost` を人間に広げたもので、**課し方も同じ＝leash ではなく効用の一項**）。**入れた理由は王朝の遊び**——**行動圏が無いと普通の一生が12地域のうち7.0を横切るので、「まだ誰も行っていない国へ行く」がプレイヤーの行為として世界から区別できない**。**測ると徘徊は確かに減る**（訪れた地域 7.03 → 6.33〈用量1〉→ 5.4〈用量3〉）**が、勝率は動かず**（36シードで旅 0.31 対 漂流 0.25）、**用量を上げると血統が死ぬ**（絶滅 0.56 → 0.92）。**既定0** |
| `EnemyHomeCost` | **0** | **外敵が「世界に入ってきた地域」から離れることの代価**（段階64。**1tickあたり、地域の幅1つぶん離れるごとに**＝段階34 の `Hazard` と同じ形。**自分の地域の中は無料**＝#53）。**種類ごとの強弱は `EnemyKind.Homely`**。**引き戻しではない**（育児の紐の形は採らない＝閾値になり、境界の際で振る舞いが切れる）。**帰り道は候補1つで値打ちを持たない**（全候補が「その手がどこへ連れて行くか」で課金されるので安いから勝つ）。**測定（48シード、基準は `EnemySpread` 0.6 単独）: 段階58 の湧きの偏りが初めて残った**——`prowlArrive` 0.11 のうち **6% → 45%**（`prowlGain` +0.06 \*\*\*）、**`prowlBite` +0.21 \*\*\* ＝危険地帯が噛む**、`enemyAway` 1.24 → 0.64、`enemyAtHome` 0.26 → 0.53。**代価は外敵が飢えること**（`hunts` −56 \*\*\*、`rareShare` 0.15 → 0.10、餓死 +20 \*\*\*、`packSize` −0.38 \*\*\*）で、**人間には良い**（個体数 +8.94 \*、`killShare` −0.04 \*\*\*）。**用量0.5 でもほぼ同じ利得**なので、**強くする理由は「危険地帯の濃さ」以外に無い** |
| `EnemySpread` | **0** | **地域ごとの「世界の外敵のうち何割がここに湧くか」のばらつき**（段階58。1 ± spread を [0,2] にクリップし、`pickFoodRegion` と同型の重みつき抽選で湧く地域を選ぶ）。**湧く総数は変えず位置だけを変える**（段階15a の論法）。**0 で乱数も消費しない**。**人間の湧きには効かない**（人間は出産でしか増えない）。**「危険」と呼ばないのは、その語が段階35 の「その地域で死ぬ確率という信念」で埋まっているから**。**測定（48シード）: 規則は完璧に発火し、世界が消す**——`prowlArrive` +0.11 \*\*\*（±0.6）／**+0.25 \*\*\***（±0.9）に対し `prowlGain` は ±0.01（**残るのは6%**）。**理由は「外敵の38〜41% が世界の中で生まれる」ことと「出発点から平均270 歩く（地域の幅は200）」**。**偏らせると外敵はむしろ散る**（`enemyCrowd` −0.20 \*\*）。**世界の数字は動かない**（個体数 +2.08 ± 6.72、`killShare` −0.00）。**既定は 0＝地図の作者が入れるもので、「危険地帯」にするには段階64 が要る** |
| `RegionAbilitySpread` | **0** | **地域ごとの「そこに立つと何がどれだけできるか」のばらつき**（段階57。1 ± spread を [0,2] にクリップして、その地域に立っている身体の**発現値すべて**に掛ける）。**0 で乱数も消費せず段階56 前と1ビットも違わない**。**上限には効かない**（`MaxVitality` / `MaxSpeed` / 記憶の枠 / 手の数は `Agent.capacity` を通る＝**地面で上限が動くと、境界を1歩越えただけで上限超過の身体ができる**。速さを外したのは段階20 の「倍率は速度ではなくコストに掛ける」と二重計上しないため）。**測定（48シード）: どの用量でも世界は動かない**——`standGain` は 0.3 で +0.01 \*、0.6 で +0.00（**用量に応じない**）、`regionsSeen` +0.14 \*（**むしろ歩く**）、`oneRegion` 不動、個体数 −0.71／+0.19。**理由は約分**（戦えるほど近い組の100%が同じ地域にいるので、地面の補正は勝ち負けの比較から消える）。**既定は 0＝地図の作者が入れるもの** |
| `RegionFavourSpread` | **0** | **地域ごとの「どの遺伝子を贔屓するか」のばらつき**（段階57c。9本それぞれ 1 ± spread を引き、**9本の平均が1になるよう正規化**＝地域そのものの良し悪しは作らない）。**0 で乱数も消費しない**。**57a の約分（地域の全員に同じ倍率）を構造的に止めるための形**で、**同じ場所に立つ違う建て方は違う倍率を受ける**（テストで固定）。**測定（48シード）: 約分は止まったが並び替えは起きない**——**一番合う地域は平均より 7.63%（±0.3）／15.51%（±0.6）良い**のに**取れたのは2%**（`suitGain` +0.32 ± 0.20 \*）、**しかも出生地の好みを持ち歩く対照が同じ大きさを出す**（+0.30 ± 0.21 \*）＝**全部が出生地の運**。**歩き方は不動**（`regionsSeen` −0.10 \*）。**強い用量の副作用**: 餓死 +7.81 \*、予算 −12.93 \*\*、`sdPower` −1.65 \*（**多様性は薄まる**）。**既定は 0＝地図の作者が「この土地は大柄な身体に向く」と言いたいときの道具** |
| `RegionAbilityCarried` | **false** | 上の対照（段階57）。**true にすると、同じ倍率を「出生地の地面から引いて一生持ち歩く」**＝スキルと同じ可搬なものになる。**乱数を引く場所も順番も同じ**なので2つの腕は同じ乱数列を走り、違いは「離れることに代価があるか」だけ。**測定: 対照のほうが世界を動かした**（0.6 で個体数 −8.40 \*、`halfLife` −9.38 \*\*、`dIntelligence` −6.82 \*\*、`shIntelligence` −0.02 \*\*）——**遺伝しない当たり外れを身体ごとに配ると、選択そのものが薄まる** |

### C-5j. 世界の1日と体内時計（段階18）

**時刻で変わるのは「休息の回復量」だけ**です（視界でも食料の湧きでもない——それらは全員に同時に効く世界の変化になり、「個体どうしが違うことが何かを生むか」というこの段階の問いとは別）。**体内時計は予算遺伝子ではありません**（9本は「量」、時計は円周上の「位相」。予算に入れると「夜型は殴る力を削る」という生物がしていない取引を主張することになる）。

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `TicksPerDay` | 1000 | 世界の1日の長さ。**0 で時計なし**（段階18 前の世界。乱数も消費しない）。**4000 にすると悪化する**（自分の悪い時刻が長く続き、回復できない期間が延びる。個体数 −6.4、餓死 +1.32 \*） |
| `RestPhaseDepth` | 0.5 | 自分の時刻と反対の時刻で回復量が何倍違うか（`RegenRate × (1 ± depth)`）。**ゼロには届かない**——「回復経路を必ず残す」は世界が捨てられない唯一のルール |
| `ChronotypeSpread` | 1 | 集団の時計が1日のどれだけに散らばるか。**0 で全員同時刻**＝この段階の対照条件（1日はあるが寝る時刻について誰も違わない世界） |
| `ChronotypeMutation` | 0.08 | 子の時計が親からずれる幅。**円周上で折り返す**（真夜中に継ぎ目を作らない） |
| `SleepingWatch` | false | true にすると、**眠っている友人も見張れる**ことになる（段階18 前の挙動）。**これがこの段階の機序を決める対照条件**——true にすると、ばらつきの利益（個体数 +12.6 \*）が丸ごと消える |

**「眠っている友人は見張れない」は新しい項ではなく、既存の「信頼できる相手は休息の危険から差し引く」に付けた条件1つ**です。これが「たまたま寝る時刻が違う」を「誰かが見張っている」に変えます——**見張り役はルールのどこにも存在しないまま**。

**完了条件の指標（全員が同時に寝ている割合）は最初から飽和していました**（どの条件でも 0.00〜0.01）。集団の中の個体はそもそも別々のことをしているので、体内時計を入れる前からハッザ族の観察は成り立っていました。実際に動いたのは個体数と集団化です。

### C-5i. 植物の遺伝と種子散布（段階17a・17c）— **既定でオフ**

**仕組み・テスト・対照条件・測定はすべて入っていますが、既定ではオフです。** 動かないからではなく、**世界を壊すから**（下記）。どちらも 0 / false で乱数を1つも消費しないので、**指紋テストが無変更で通る＝段階16 の世界に完全に戻る**ことが確認できます。

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `PlantGenetics` | **false** | 植物が2本の遺伝子（散布半径・再生速度）を持ち、親から継ぐ。**予算制も `Decide()` も無い**（#42）。**総量は変えず**、「誰の子か」と「どこに落ちるか」だけを決める。**オンにすると個体数が3分の1に**（44.2 → 14.9） |
| `PlantSpread` / `PlantSpreadMax` | 90 / 500 | 種の落ちる距離の初期値と進化の上限。**短いと地域が空になり**（植物のいない地域が0.82、吸収状態）、**長いと食料が地面の良し悪しと無関係になる**（`richGain` 0.05 → 0.01 で段階15b・15c が学ぶ対象を失う）。**両方を逃れる設定は無い** |
| `PlantMutationRate` / `PlantMutationStd` | 0.05 / 0.4 | まれで大きい変異（個体の遺伝と同じ形） |
| `SeedSurvival` / `SeedGutTicks` | **0** / 120 | 食べられた種が生き延びる確率と、運ばれる時間（段階17c）。**既存の `eat` に相乗り**で新しい行動も状態も無い。運ばれた種は次の湧きを**置き換える**（総量不変）。**風散布が地図を空にする問題の答えとして設計したが、答えにならなかった**（空の地域は 0.55 対 0.53 で不変）。**しかも単独で個体数を3分の1減らす**——運ばれた種が落ちるのは「動物がいま立っている場所」＝「たった今食べ尽くした場所」だから |

**復活の条件**: (a) 空になった地域が戻れる経路（種子バンク、地図外からの流入）、または (b) 集団が食料の地理に依存しなくなる何か。**あるいは (c) 単に測り直すこと**——段階17 の測定は 2026-09-04 の見かけトリガーの修正より前に行われており、**当時の世界は個体数がほぼ半分でした**。倍の余裕がある世界なら植物の進化や毒を支えられるかもしれません。

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `PlantDefence` | **false** | 植物が毒と警告信号の2遺伝子を持つ（段階17b）。**相関させない**（#43）。**17a とは別のスイッチ**——17a で世界を壊したのは「どこに生えるか」なので、地面の良し悪しで生える世界のまま防御だけを遺伝させられる。親は立っている株から**一様に**引くので、選択は「食べられなかったこと」を通じてだけ働く。**オンにすると個体数が9分の1**（44.2 → 3.7） |
| `PoisonDamage` / `SignalNoise` | 18 / 0.25 | 毒の最大ダメージと、警告の読み間違い（`(100−理性)/100` が掛かる）。**用量18 では世界が持たない**（個体数 4.9。壊すのは毒そのものではなく**恐れ**——警告は平均0.5 なのでどの株も「半分危ない」と見え、1食9の体力に値付けされて食べる手が負ける。`starveRate` が16倍、`dietDiscount` 0.99＝**ほとんど何も食べていない**）。**「どの用量でも持たない」は 2026-09-08 に取り消した**——旧記録（毒2で個体数6.2）は**見かけトリガーの修正前の世界の値**で、**今の世界では毒2 で 71.1**（baseline 85.7 の −17%）|
| `PlantSignalCost` | **0** | **目立つことが種で払う値段**（2026-09-08。復活条件として名指ししていた「植物側のコスト」）。**信号は選択されるのに値段が無かった**（避けられた株が残るので毒2 で 0.5 → 0.60 へ走る）。0.8 にすると **0.60 → 0.07 に落ち**、個体数の代価はほぼ無い（71.1 → 67.3、誤差内）。**復活させるならこれを入れる** |
| `PlantPoisonSaves` | **0** | 毒に比例した確率で**噛まれても株が残る**（食べた側は毒を受けて栄養を得ない）。**毒が植物にとって得になる唯一の経路**で、入れると `plantPoison` が 0.57 → 0.75 と**初めて上がる**。**しかし使えない**——**この世界の個体数は食料で頭打ち**なので、毒の得は「世界から食料が消えること」と同義。**個体数 4.2**（噛みつきの75%が空振り）。**毒に値段を付けても（下記）0.53 までしか下がらず、個体数は 6.4** |
| `PlantPoisonCost` | **0** | 毒であることが種で払う値段。**遺伝子には効く**（0.75 → 0.53）が、**世界は戻らない**（個体数 6.4）。**`PlantPoisonSaves` と対でのみ意味がある** |

**`shRationality` は上がる（0.07 → 0.11 \*\*\*、段階9 以来はじめて）が、理由は信号ではない。** 信号を読めなくした条件でも同じだけ上がるので、上げているのは**世界が厳しくなったこと**（段階12a と同じ交絡）。

**正直な警告は進化しない**（2026-09-08 に追試して、理由が1段深くなった）。**毒は植物にとって1ミリも得ではなかった**——食べられた株は毒があろうと無かろうと消えるので、**毒は選択されようが無く漂うだけ**（0.55）。一方**信号は選択される**（避けられた株が残るので 0.60 まで走る）が、**値段が無いので上限へ向かう**。**両方に値段を付けた世界では、相関がはじめて弱く正になった**（`plantHonesty` +0.10 ± 0.04）——**理論どおり、コストのある信号とコストのある防御が揃ってはじめて正直さの足場ができる**。**ただしその世界は餓死で壊れている**（個体数 6.4）ので、**現時点で使えるのは「毒2＋信号のコスト」まで**。

**復活の結論（2026-09-08）**: **`PlantDefence = true, PoisonDamage = 2, PlantSignalCost = 0.8` なら世界は持つ**（個体数 67.3＝baseline の −22%、絶滅0）。**`shRationality` +0.03 \*\*\*・`dRationality` +21.0 \*\*\*** と理性への選択圧は強く上がるが、**信号を読めなくしても同じだけ上がるという 2026-09-04 の交絡は解消していない**（世界が厳しいから上がる）。**既定はオフのまま**（−22% は地図の作者が払うと決めるべき値段）。

### C-5h. 食料の栄養バランス（段階16）

**新しい状態量は作っていません**（状態量は今も3つ）。「最近何を食べたか」は殴られた相手を覚えているのと同じ帳簿で、読み出し時に遅延減衰します。**隠していません**——`Perception` にも効用式にも空腹度と同じように載ります。

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `SamenessPenalty` | 0.35 | 同じものばかり食べているときの栄養の目減りの上限。**0 でルールごと無効**（対照条件）。**ゼロには届かない**——飽きは「他を探す理由」であって「食料の隣で餓死する理由」ではない。**0.8 にすると逆効果**（世界が痩せて狩りに出る余裕が消え、群れ狩りの増加が消える。個体数 −10.3 \*） |
| `DietSatiety` | 4 | 何個食べると罰が半分に達するか（飽和型。最初の数個は同じで、20個目は10個目と変わらない） |
| `DietForgetPerTick` | 0.002 | 食べるのをやめてから飽きが薄れる速さ |

**実測**: `dietVariety` 0.13 \*\*\*、`dietDiscount` 0.96 \*\*\*、個体数・`killShare` は誤差内（`PLAN.md` の事前予想どおり）。**ただし群れ狩りが初めて有意に増えた**（共同での仕留め 18.5 → 23.2 \*\*）——植物に飽きた個体にとって肉が「植物では取れない価値」を持つため。

### C-5g. 地域の豊かさの認識と伝承（段階15b・15c）

**個体は自分が立った地面しか覚えられません。** 行ったことのない場所は「聞く」以外に知りようがなく、それが伝承（15c）に価値を与えます。**人の記憶の枠は1つも食いません**（場所は人ではない。#41）。

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `RegionLearnRate` | 1.0 | 1回の観測（そこにいる間に見えた食料の数）が推定をどれだけ動かすか。**0 で15b・15c ごと無効**＝段階15a のままの世界（乱数も消費しない） |
| `RegionMemory` | 40 | 推定が何回ぶんの観測に相当するかの上限。**記憶遺伝子で倍率が掛かる**（平均で1.0、上限で約2、下限で0.2） |
| `RegionForgetPerTick` | 0.0004 | 離れてからの薄れ方。**記憶遺伝子で割られる**（良い記憶ほど長く保つ）。実測で、記憶10の個体は約1000tick（2年）、記憶95の個体は約20000tick（40年）で country を失う |
| `RegionNoise` | 2.0 | 地面の読み間違いの大きさ。他のすべての誤読と同じく `(100−理性)/100` が掛かる |
| `RegionToldCount` | 2 | 人から聞いた見解が何回ぶんの観測に相当するか。**低いのは意図的**——聞いた話は自分で1度見れば覆るべき。**0 で15c だけを切れる**（対照条件） |
| `RegionPrior` | 3 | 普通の地面で視界に入る食料のおおよその数。**ルールではなく単位**（地域の交換を他の交換と同じ尺度に載せるためだけ） |
| `RegionDrawValue` | 4 | 「良いと覚えている地面へ向かう」ことの価値（信じている差1あたり）。**0 で「知るが行かない」**＝知ることと選ぶことを分ける対照条件。**性能上の注意**: これを有効にすると `Step` が2.4倍になる（原因は既存の再判断トリガー。`TODO.md` と `HISTORY.md` 2026-09-04） |

**地域は壁ではありません。** 移動を妨げず、地域IDを読むルールもゼロで、するのは「その場所で `RestExposureWeight` に掛かる数が変わる」ことと「そこに植物が生えやすい／にくい」ことだけです。

### C-5e. 視界（段階13）

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `SightGrid` | true | 視界を「自セル＋周囲 `SightCells` リング」のブロックにする。**false で以前の半径 `PerceptionRadius` の円**（対照条件） |
| `SightCellSize` | 76.8 | 視界セルの1辺。**円と同じ面積になるよう較正**（9·size² = π·130²）。合わせないと「見える量」の変化と「見える形」の変化が分離できない |
| `SightCells` | 1 | 自セルの周囲いくつのリングまで見えるか。**遺伝子にはしない**——0 は世界が壊れ（個体数 4.4）、2 はやや悪い、という離散で極端に非線形な量なので、遺伝子にすると予算に対する固い閾値になる（`StrategyDepthUnlock` と同じ轍） |

`PerceptionRadius`（130）は `SightGrid = false` のときの視界であり、かつ**測る側の物差し**として残っています（`Spacing()` の `neighbours` / `nearest`、`MemoryUse()` の `restNear`、効用式やヒントの「近さ」の正規化）。**物差しをルールに追随させると、変更をまたいだ比較ができなくなる**ので円のままにしてあります。

### C-5d. 食料レースと地形（地形の下ごしらえ）

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `RaceOnDistance` | false | true で食料レースの勝率を**距離の比**で判定する（下ごしらえ前のルール）。既定の false は**到達時間の比**で、相手の速さは見えないので世界の基準速度（`MaxSpeed`）を仮定し、自分の速さだけが実値で入る。**平均的な身体なら両者は数値まで一致する。** 入れても `shSpeed` は動かない（速さはすでに到達tick数を通じて効いていた）が、**地形が入ると距離と時間は本当に食い違う**ので先に直してある |

地形そのもののパラメータは C-2c の表の末尾（`TerrainMap` / `RoughMoveCost` / `WaterMoveCost` / `SlopeMoveCost` / `WaterSpeedShare` …）にあります。**倍率は移動コストに掛けるのが原則**——速度を落とすのは effort がすでにやっていることで、二重にすると測定で区別できないからです。**2026-09-18（段階97）に水だけがこの原則から外れました**（`WaterSpeedShare`）。**外せた根拠は対照アーム**——`waterslowfree`（遅いが高くない腕）が単独ではっきり効くので、**速度の倍率と effort は測定で区別できます**。**それでも既定は無効**で、**他の地面には速度の倍率を置いていません**。

### C-6. 再判断のトリガー

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `TriggerVitalityDrop` | 4 | 前回の決定からこれだけ体力が減ったら考え直す。**「逃げろ」の閾値ではない** |
| `TriggerIdleTicks` | 40 | 何も起きずにこれだけ経ったら考え直す |
| `SightingRetriggers` | false | true にすると、食料・配偶者の「見かけ」トリガーが**視界にある間ずっと毎tick**発火する（2026-09-04 に直すまでの挙動）。**既定の false は「視界に入った瞬間だけ」。** 直した効果は性能と挙動の両方で大きい——`Step` が2.4倍速くなり（1010 → 419µs）、かつ**個体数がほぼ倍**（45.0 → 77.7 \*\*\*）、餓死率が4分の1（2.19 → 0.58 \*\*\*）。理由は `ChoiceNoise`（20）が候補のスコア（5〜30）に対して大きいこと——**毎tick 考え直す個体はそのたびにノイズを引き直し、僅差の候補の間で振動して何も遂行できない** |

### C-7. 頭の良さの2経路

| 名前 | 既定値 | 意味 |
| --- | --- | --- |
| `StrategyDepthUnlock` | **0** | 戦略の深さゲートの刻み幅。**0 は「ゲート無し」＝全戦略解禁**。測定の結果この経路は知能への選択圧を弱めていたため既定でオフ |
| `ChoiceNoise` | 20 | 知能0の個体が効用の評価を誤る標準偏差。**現在これが頭の良さの唯一の経路** |

### C-8. 繁殖と遺伝

| 名前 | 既定値 | 効く先 | 意味 |
| --- | --- | --- | --- |
| `FitnessConditionWeight` | 0.35 | 繁殖 | 「良い相手に見えるか」のうち**体調**が占める割合。残りは魅力遺伝子そのもの。実能力は一切入らない。**0 だと瀕死の相手にも求愛して個体数 −18、0.7 だと個体数 +16 だが配偶者選びが状態だけになって遺伝する装飾が読まれなくなる**（2026-09-02 に `world.go` の package const から移した） |
| `ReproHunger` | 35 | 判断 | 空腹度がこれ未満でないと求愛しない |
| `ReproVitalityShare` | 0.70 | 判断 | **自分の体力上限に対する割合**がこれ以上でないと求愛しない。絶対値にすると、体力遺伝子が低い個体は「どれだけ満たしても一生求愛できない」崖になる |
| `PairBondDuration` | 150 | 世界 | ペア成立から出産までのtick |
| `MatingCooldown` | 140 | 世界 | ペア解消後、次に求愛できるまでのtick |
| `BirthVitalityCost` | 40 | 世界 | 出産で両親が折半して払う体力 |
| `ChildVitalityShare` | 0.58 | 世界 | 子の初期体力（**その子の体力上限に対する割合**） |
| `ChildHunger` | 18 | 世界 | 子の初期空腹度 |
| `GeneBudgetMean` | 360 | 遺伝 | **初期個体だけ**が引く予算（遺伝子9本の合計）の平均。1本あたり平均40 |
| `GeneBudgetStd` | 30 | 遺伝 | 同じく予算の標準偏差。初期個体はおよそ 270〜450 に収まる |
| `GeneInitAlpha` | 0.8 | 遺伝 | 初期個体の**配分**を引くディリクレ分布の集中度。**1未満ほど偏る**（予算の大半が数本に乗る）。1を大きく超えると9本が横並びになり、初期集団に「種類の違い」が無くなる。一様乱数を正規化する形は使わない（それはディリクレではなく、偏りを作れない） |
| `BudgetInheritSpread` | 30 | 遺伝 | 子の予算に乗る正規乱数の標準偏差。**血統ごとの予算は世代数 n に対しておよそ σ√n で広がる**ので、広すぎると配分への選択圧より先に水準が動いて読めなくなる |
| `BudgetHeritability` | 1 | 遺伝 | 親の予算をどれだけ引き継ぐか。1＝そのまま継ぐ（既定）、0＝毎回 `GeneBudgetMean` から抽選（予算が遺伝しない世界）、中間＝平均への回帰 |
| `GeniusRate` / `GeniusBudget` | 0.017 / 495 | 遺伝 | まれな大当たりの出生。**出生あたりの確率**なので「10年に一度」は出生数によってずれる（実測の出生率でおよそ10年に一度）。予算は親から継いだ値を**置き換える**（倍率ではない） |
| `GreatGeniusRate` / `GreatGeniusBudget` | 0.0017 / 630 | 遺伝 | さらにまれな段階。900（全部100）の手前で止めてある |
| `TicksPerYear` | 500 | — | 上の確率を「何年に一度」と言うための単位。**ルールはどこも読んでいない**。段階7d で寿命と突き合わせて本決めにする |
| `MutationRate` | 0.02 | 遺伝 | 能力1つが突然変異する確率。**98%の遺伝子は親の値のまま子に渡る。0にしても選択は止まらない**（初期集団が持っていた多様性の中での選択は続く）。止まるのは新しい変異の供給だけ |
| `MutationStd` | 40 | 遺伝 | 突然変異が起きたときの標準偏差。**まれで大きい**形にしてあるのは、毎回小さく乗せるとどの親の値も子に残らず、粒子的遺伝にした意味が消えるから。`MutationRate` との組は「以前と同じだけの多様性が集団に残る」ように較正した（名目の分散を合わせるだけでは端のクリップで15%足りず、確率を2倍にして合わせた。`HISTORY.md`） |
| `LamarckRate` | 0 | 遺伝 | 親が生涯に学んだ値をどれだけ子に渡すか。**段階12a で有効になった**（C-5b を参照）。0 のままなのは、集団に広まった信念が「そう信じた者が生き残ったから」なのか「教わったから」なのかを分けられるようにするため |
| `PatienceBase` | 25 | 判断 | 比較検討の基本tick数 |
| `PatienceRationality` | 0.5 | 判断 | 理性1点あたりの追加tick数。理性の利得を打ち消している疑いがある |
| `CommitFitness` | 78 | 判断 | この魅力の相手なら比較検討を待たず即決する |
| `MateRejectDuration` | 40 | 判断 | 見送った候補を再検討しない期間 |

---

## D. `Config` に無い定数（コード内のマジックナンバー）

**テストで個別に潰せず、A/Bもできない値**です。調整したくなったら `Config` に移す必要があります。CLAUDE.md の「定数は `Config` のフィールドとして持つ」方針から漏れているもの、という見方もできます。

| 値 | 場所 | 意味 | 移すべきか |
| --- | --- | --- | --- |
| `MinAbility = 1` / `MaxAbility = 100` | `agent.go:5` | 能力の範囲 | しなくてよい（スケールの定義） |
| `midAbility = 50.5` | `agent.go:10` | ダメージ計算の基準パワー | しなくてよい |
| `pairFollowEffort = 0.35` | `world.go:18` | ペアが相手を追う effort | 低 |
| `spectateInterval = 20` | `world.go:23` | 観戦による推定更新の間隔 | 中 |
| `observeTicks = 10` | `world.go:26` | 観察が完了するまでのtick | 中 |
| `maxFoodOptions = 4` / `maxAgentOptions = 6` | `controller.go:8` | 候補に挙げる食料・他個体の上限 | 中（速度と質のトレードオフ） |
| `effortLevels = {0.4, 1.0}` | `controller.go:15` | effort の選択肢。**2段階しかない** | 中 |
| `fleeExposureTicks = 10` | `controller.go:46` | 逃げ切るまで相手の間合いにいる想定tick | 中 |
| ~~`retaliation = 0.7`~~ / ~~`pAccept = 0.6`~~ | — | 2026-09-04（段階12a）に `Config` の `Retaliation` / `AcceptChance` へ移し、**そこからさらに個体ごとの値になった**。C-5b を参照 | 済 |
| `observeTicks = 10`（再掲） | `world.go` | 観察が完了するまでのtick。**段階12b でここが伝承の単位にもなった**（観察が完了した瞬間に交換が1回走る） | 中 |
| `retaliationSampleTicks = 5` | `world.go` | engagement が始まって何tick後に「殴り返したか」を1度だけ数えるか。初撃で数えると相手が考え直す暇が無く必ず「殴り返さない」になる | 中 |
| `engagementGapTicks = 20` | `world.go` | 打撃がこれだけ途切れたら別の戦いとして数え直す | 低 |
| 求愛の effort `0.6` / 観察の effort `0.3` / 探索の effort `0.4` | `controller.go` | 各行動の固定 effort | 低 |
| `traceHistory = 16` | `trace.go:13` | 記録する決定の件数 | しなくてよい（開発用） |
| 空間グリッドのセル幅 ＝ `PerceptionRadius` | `grid.go`（`gridCellSize`） | 近傍探索の目の粗さ。**世界のルールではない**（変えても結果が1ビットも変わってはいけない）ので `Config` に入れない。いちばん広い問い合わせ半径から導出しているので、両者がずれることがない | **移してはいけない**。速度だけの値で、`Config` は世界のルールの一覧だから |
| `maxGridCells = 16384` | `grid.go` | グリッドの上限セル数。`PerceptionRadius` を極端に小さくした `Config` でも破綻しないようにセル幅を倍にして収める | しなくてよい（安全網） |

---

## E. 測定済みの感度

`cmd/experiment` で測ったもの。数字は24シード × 20000tick、同一シードの差分です。

| 変えたもの | 効果 |
| --- | --- |
| `StrategyDepthUnlock` 16 → 0 | 平均知能の上がり幅 **+4.52 ± 0.95**、戦闘 +3589、個体数は変わらず |
| `StrategyDepthUnlock` 16 → 20 | 平均知能の上がり幅 **−3.87 ± 0.79**（選択圧が負に反転） |
| `ChoiceNoise` 20 → 0 | 知能の選択圧がほぼ消える（+0.63 ± 0.51）。個体数 +30 |
| `FoodSpawnRate` 0.20 → 0.30 | 個体数 +68、戦闘 +4587、**殺害数 +27（増える）**、殺害が死因に占める割合 −0.02 |
| `FoodSpawnRate` 0.20 → 0.12 | 個体数 −57、戦闘 −6251、殺害数 −53、割合 +0.03 |

段階9（2026-09-02、24シード × 20000tick、同一シードの差分）:

| 変えたもの | 効果 |
| --- | --- |
| `RestExposureWeight` 0 → 0.05 | `restNear` 6.4 → 2.0、**個体数は動かない**（−0.2 ± 6.9） |
| `RestExposureWeight` 0 → 0.10 | 個体数 −20（3分の2に）。**避け合いではない**（`clumping` は不動）。のべ人-tickあたりで消耗死 +45%・成立したペア −16%・ペアあたりの出産 −16% |
| `RestExposureWeight` 0 → 0.20 | 個体数 −27（半分に） |
| `MemoryCapacity` 12 → 6 / 24 / 0（無制限） | 好意を持っている相手の数 2.16 → 1.01 / 2.55 / 3.88。**記憶容量が「同時に結ばれていられる人数」の上限**。**2026-09-04 に個体数まで測った: 79.9 / 77.7 / 73.8 / 54.1 と単調に悪化**——記憶を増やすと友人が増え、好意が休息を安全に見せるため。**記憶容量への正の選択圧が出ない理由はこれ**で、地域の数でも忘却の速さでもない（どちらも試した） |
| 段階9 の4つ全部（容量・帯域・接触・露出） | `dPower` +5.6 → **−5.3**（選択圧がパワーから頭の良さへ）。`killShare` は動かない |

段階10（2026-09-03、96シード × 20000tick）。**推定誤差は同じ実行の中で3つの推定器を同じ遭遇に当てて比べます**（条件どうしは出会う相手が違うので比べられない）:

| 変えたもの | 効果 |
| --- | --- |
| 学んだ推定 vs 固定の50（同じ遭遇） | 初対面の誤差 30.3 → 33.4＝**利得 +3.04 \*\*\***（50000tick では +8.07 \*\*\*） |
| そのうち体つきを読んだぶん | **+0.34 \*\*\***（利得全体の11%。残り89%は「世界の平均を知った」だけ） |
| `AppearanceNoise` 6 → 0 / 12 / 40 | 体つきの利得 0.34 → 0.32 / 0.10 / 0.12。**6 で既に頭打ち**、12 で消える |
| `AppearanceSlopePrior` 60 → 0 | 体つきの利得 0.34 → 0.20（素の最小二乗はノイズを拾う） |
| 経験のある観察者 vs 無い観察者（同じ実行） | 26.6 vs 33.5（50000tick では 21.5 vs 35.8） |
| `LearnFromLooks` on/off | 個体数 −8.0 \*\*\*・餓死率 +0.71 \*\*・出生 −11.1 \*\*\*。**ただし原因は学習ではなく水準**（下） |
| `PriorStrength` 50 → 36（学習なし） | 個体数 −6.0 \*\*・餓死率 +0.86 \*\*\*・出生 −11.3 \*\*\*＝**学習を入れたときとほぼ同じ**。よそ者を過大評価することが世界を支えていた |

段階11 の残り（2026-09-03、96シード × 20000tick、同一シードの差分。`nohuntaffinity` との対）:

| 変えたもの | 効果 |
| --- | --- |
| `AffinityHunt` 0 → 6（既定） | 個体数 −2.7（有意でない）、世代 −0.35 \*、餓死率 +0.29 \*、最近傍距離 +6.9 \*。**集団の指標（`halfLife` / `fightRatio` / `gapRel`）は50000tickでは符号が定まらない** |
| `AffinityHunt` 0 → 12 | 個体数 −4.2 \*、世代 −0.61 \*\*、餓死率 +0.42 \*\*、最近傍距離 +14.4 \*\* |
| `AffinityHunt` 0 → 24 | 個体数 −4.8 \*、世代 −0.62 \*\*、餓死率 +0.60 \*\*\*、最近傍距離 +21.2 \*\*\* |
| `RestExposureWeight` = 0 のもとで `AffinityHunt` 0 → 6 | **餓死率は動かない**（−0.00 ± 0.20）。餓死が増えるのは好意が休息の露出を割り引くからだと確かめた対 |
| `EnemyBudgetMean` 520 → 400 / 700、`EnemyBudgetStd` 90 → 180 | **参加人数はどれも 1.22〜1.25 で不動**。動くのは人間の個体数（+12.7 / −7.6 / −3.0） |

読み取れること:

- **殺害が死因に占める割合は 0.59〜0.64 でほとんど動かない。** 食料を2.5倍の幅で振っても変わらないので、協力の効果を測る指標として使える（食料供給の増減と混同しにくい）
- 一方で**殺害の絶対数は食料が増えるほど増える**（個体数が増えて遭遇が増えるため）。「食料を増やせば争いが減る」は成り立たない
- 能力の選択圧（`dPower` +8.3、`dIntelligence` +6.9）は食料供給の増減に対して頑健。`dRationality` だけが +0.5〜1.0 と一桁小さく、**理性は今もほとんど選択されていない**
- 群れ具合（`clumping`）は既定で 1.19。食料を絞ると 1.27 に上がる（少ない食料の周りに集まる）
- **記憶容量（`shMemory`）への選択圧は、役割をつけても出ません**（0.07〜0.08 でどの条件でも動かない）。容量が効いていること自体は測れている（上の表）ので、「効いているが得ではない」状態です
