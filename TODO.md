# TODO（優先順位つきタスク一覧）

**`PLAN.md` を作業単位に落として優先順位をつけたものです。** ルールの中身・理由・完了条件の詳細は `PLAN.md` の該当節にあります。ここには**何を・どの順で・なぜその順か**だけを書きます（同じことを2箇所に書かないため）。

- 進め方: 上から順に。1タスク＝1コミット程度の粒度を目安にする
- 各段階の詳細な受け入れ条件・触る文書は `PLAN.md` の同名の節を見る
- 「なぜそう決めたか」は `HISTORY.md` の設計判断ログ

## 優先順位の考え方

| 優先度 | 意味 |
| --- | --- |
| P0 | 土台。これを先にやらないと以降の測定・性能が全部やり直しになる |
| P1 | 遺伝の中核。ここで測定のベースラインが1度リセットされる |
| P2 | 表現力の拡張。P3（協力・伝承）の前提になる |
| P3 | 本命（外敵と伝承＝集団の創発）。P0〜P2 が揃ってから |
| — | 保留。条件が満たされたときだけ復活 |

**順序を入れ替えてよいもの / いけないもの**

- 6.5 → 7a → 7b → 7c → 7d は**この順を守る**（後段が前段の測定基準・性能・遺伝子表現に乗るため）
- 段階8（チャネル化）は 7c の後ならいつでもよい。**段階9 より後ろに回してもよい**
- 段階9（好意）は**段階12 の前提**。段階10・13 は独立していて後回し可
- `protocol` / `server` / `client` の着手時期は未定（`CLAUDE.md` の未決事項）

---

## P0：土台（最優先）

### 1. 段階6.5：計測の先行整備
> 目的: 「あるべき状態」の目標値を憶測でなく実測から決める。読み取り専用なので安い。以降の全段階の効果測定に使い回す

- [x] クラスタ検出（素朴な実装でよい）→ クラスタ数・サイズ分布 — `World.Clusters(linkDist)`、連結距離 30 固定、`cmd/experiment` に `clusters` / `clusterSize` / `grouped` / `largestShare`
- [x] membership half-life（同じ顔ぶれが一緒に居続ける時間） — `engine.MembershipTracker`、組の共在生存曲線として測る、`cmd/experiment` に `halfLife` / `together` / `censored`
- [x] 集団内戦闘率 ÷ 集団間戦闘率 — `engine.FightTracker`。書かれたとおりには測れないので「仲間（200tick前から同じクラスタ）÷ よそ者」の遭遇あたり戦闘率に置き換えた
- [x] 集団間の距離分布 — `World.ClusterGaps(linkDist)`、`cmd/experiment` に `gap` / `gapP10` / `gapRel`
- [x] 種族ごとの個体数 — `engine.CensusTracker`。窓（5000tick）の平均で持ち、読むのは振れ幅ではなく **谷（最小 ÷ 平均）**。絶滅だけは窓ではなく実行全体で覚える。`Agent.Species` のラベルも先に置いた（今は human だけ・ルールは参照しない）
- [x] `cmd/experiment` の出力に上記を追加（O(n²) なので頻度は間引く。クラスタの4指標・half-life の3指標・戦闘率の3指標・距離の3指標・種族の4指標をすべて追加済み）
- [x] **完了条件**: 現状を 50000tick 回して測り、「集団が無い世界のベースライン」として `HISTORY.md` に記録する（**目標値はまだ書かない**＝段階11 の完了条件へ）— 2026-09-01、24シード × 50000tick。**塊はあるが集団は無い**（半減期60tick・`fightRatio` 0.93・`gapRel` 0.80）

**段階6.5 は 2026-09-01 に完了。** 次は段階7a。

### 2. 段階7a：空間グリッド（挙動を変えない高速化）
> 目的: `perceive()` の実質 O(n²) を潰す。以降の全段階の土台であり、地域割りの出所にもなる

- [x] `World` に一様グリッドを持たせ、ノードと食料をセルに登録する — `engine/grid.go`。セル幅＝`PerceptionRadius`、差分更新せず「古い」印＋遅延再構築、候補は添字の昇順で返す（走査順が乱数の消費順なので）
- [ ] `perceive()` の近傍探索をグリッド経由にする（**円の視界判定はそのまま残す**）
- [ ] 食料ごとの「最も近い他個体」もグリッド経由にする
- [ ] **完了条件**: 同一シードで導入前と統計値が完全一致すること
- [ ] ベンチを前後で取り `HISTORY.md` に記録

---

## P1：遺伝の中核

### 3. 段階7b：継承則の入れ替え（染色体式＋幅）
> 目的: 混合遺伝（毎世代で分散が半減）をやめ、粒子的遺伝で多様性を維持する

- [ ] `tryBirth()` を「遺伝子ごとに親のどちらかを引く＋正規乱数」に変更（引き方は遺伝子ごとに独立）
- [ ] `Config` に `MutationRate`（既定 0.01）を追加、`MutationStd` を再較正
- [ ] `LamarckRate` を `Config` に用意（この段階では未使用の枠だけ）
- [ ] **完了条件**: 前後で `cmd/experiment` を回し、能力の分散と選択圧の変化を記録

### 4. 段階7c：遺伝子ベクトル化と予算制 ← **この段階が最大**
> 目的: 能力3種を `Genome []float64`（遺伝子9種）に置き換え、親から継ぐ予算制にする。**魅力もここで入れる**（後から遺伝子を足すと予算も測定基準も取り直しになる）

- [ ] `Power/Rationality/Intelligence` → `Genome []float64`（遺伝子9種）に置換
- [ ] 初期個体: 予算を抽選（`GeneBudgetMean`/`GeneBudgetStd`）＋ 配分をディリクレ分布から引く
  - [ ] ガンマ分布のサンプラを自前で書く（外部依存を増やさない）／一様乱数の正規化は使わない／1本あたり 1〜100 にクリップ
- [ ] 子の予算＝親の予算 ＋ 正規乱数（`BudgetInheritSpread`）。`BudgetHeritability` は 0/1 で切替可に残す
- [ ] 継承時の正規化（親由来の値を子の予算に合わせてスケール）
- [ ] 天才2段階（`GeniusRate` / `GreatGeniusRate`）。`TicksPerYear` は出発点 500 を仮置き
- [ ] 既存の `Power`→攻撃力、`MaxVitality`/`MaxSpeed` を遺伝子側へ移す
- [ ] `fitness` を魅力遺伝子で置き換える（**実能力とは相関させない**）
- [ ] 再判断トリガーに「魅力の高い個体が視界に入った」を追加
- [ ] `cmd/experiment`: 能力指標を**絶対値→予算比**に変更、「世代ごとの平均予算」と「予算の分布」を出力
- [ ] **完了条件**: devview で「大きくて遅い個体」と「小さくて速い個体」が並存すること／`BudgetHeritability` 0 と 1 を同一シード対で比較しドリフトを見る

### 5. 段階7d：成長・老い・寿命と育児期間
> 目的: 伝承（段階12）の土台＝親子が一定時間近くにいる状況を作る。**寿命消費だけ 2026-08-31 に先行実装済み**（意思決定に一切関与しない背景処理という制約つき）

- [x] 寿命を状態として持つ（`Agent.Lifespan`）／過小（空腹）・過度（腹八分目超）で消費／`Config` で各々 0 にして切れる／`cmd/experiment` に `aged`・`agedShare`
- [ ] **年齢補正**: 能力の実効値を「遺伝的素質 × 年齢補正」にする。成長と老いは**同じ関数の両端**。掛け算は1箇所に閉じる
- [ ] **成長**: 食事で進む（新アクションは作らず既存 `eat` に相乗り）。満腹で頭打ち。自立期間の目安は3年
- [ ] 成長の頭打ち（100%）と過度コストの開始（80%）を**別の閾値**として分離する
- [ ] **#33 慢性的な低体力による寿命コスト**（体力20%割れの継続で消費。既存 `ShockRisk` とは別物として扱う。20%超えでリセットする単純版から）
- [ ] **育児期間**: 出産後 `ChildRearingTicks` の間は子が親の近くに留まる。`= 0` で完全に切れること。**給餌は作らず保護だけ**
- [ ] 親側の負担（移動制約・体力コスト）を入れるか決めて入れる／子の生存が親の効用にどう入るか決める
- [ ] **`TicksPerYear` を本決めにする**（3条件を満たす値）→ 確定後、**段階7c の天才発生間隔の測定をやり直す**
- [ ] **完了条件**: 死因3種（餓死・戦闘・老衰）の内訳／年齢分布と世代数／成長中と成長後で食べ方が変わることを devview で目視／子の餓死率と自立到達率／`ChildRearingTicks` 0 と有効の比較／#33 の有効・無効を同一シード対で比較
- [ ] **注意**: 死因が増えるので、段階7c までの測定値と直接比べない（ベースラインを取り直す）

---

## P2：表現力の拡張

### 6. 段階9：記憶の容量・帯域・忘却曲線と、好意 ← **段階12 の前提**
> 目的: `opinions` を有限容量にし、忘却を「容量の取り合い」にする。あわせて記憶に**初めての正の項（好意）**を入れる。**敵意は新設しない**（既存のリスク記憶をそのまま敵意として扱う）

- [ ] `opinions` を容量つきに（あふれたら重要度の低いものから捨てる）／単位時間の更新上限（帯域）／忘却曲線を遺伝子化
- [ ] **好意**を `Opinion` の新フィールドとして追加。容量は既存の記憶容量遺伝子で管理（専用遺伝子は作らない）
- [ ] 発生源はこの段階では2つだけ（**実イベントのみ・共在時間による加点はしない**）: ペア形成・出産／血縁（**直系の親子1ホップのみ**）
- [ ] **#32 休息を無防備にする**: `ActRest` の `Utility` にリスク項を足す（近くの好意を持たない個体）。既存の走査に相乗りさせて探索コストを増やさない。`Config` で 0 にして今の挙動に戻せること
- [ ] **#22 接触による減衰起点のリセット**（減衰式は変えず「経過tick」の起点だけ更新。好意・リスク記憶の両方に適用）
- [ ] **完了条件**: #32 の 0／有効の同一シード対で餓死率・個体数・平均体力を確認（**原則「回復経路を必ず残す」を壊していないこと**）／devview で目視／記憶容量で好意の相手数が頭打ちになること／`BenchmarkDecide` を前後で取る

### 7. 段階8：攻撃・防御・回避のチャネル化
> 目的: 「攻撃力・防御力・移動・回避の使い方で体力の減りが決まる」。**この段階が一番危険**（攻撃側有利の非対称量が動き、個体数を崩しうる）

- [ ] effort をプリセット3種（攻め寄り／守り寄り／逃げ寄り）に拡張
- [ ] 防御＝ダメージ乗数を下げる／同tickの攻撃出力も下がる
- [ ] 回避＝確率で完全無効／行動機会で払う
- [ ] 体力消費 = Σ(チャネルごとの 最大値 × 使用率 × 単価)
- [ ] **完了条件**: `BenchmarkDecide`（現在 8.0µs・候補約40件）が候補約70件でどれだけ悪化するかを測る／**個体数を必ず確認する**

---

## P3：本命（外敵と伝承）

### 8. 段階11：種族の汎用化と外敵（協力の土俵）
> 目的: 「隣人が生きていると自分が得」を**世界側の事実**として置く。「協力」という言葉は engine に登場させない

- [ ] 人間・外敵で同じノード構造・同じ意思決定エンジンを共有する
- [ ] 外敵に「大きさ」を持たせ、ドロップ数を大きさで決める／分配は攻撃に参加した者だけ
- [ ] **共同戦闘を好意の主要な加点源にする**
- [ ] 食料3種（共通食料A/B＋死体からの肉）。人間は共食い不可、外敵は種族を問わず食べる
- [ ] **完了条件**: 群れ狩りが現れるか現れないかを測る／段階6.5 の指標を全部測り直す／**`PLAN.md` の「あるべき状態」の表に目標数値を書き込む**

### 9. 段階10：見た目からの学習
> 目的: 初対面の相手に `PriorStrength = 50` を当てはめるのをやめる。**独立しているので前後させてよい**

- [ ] 遺伝子から観測可能な相関値 `Appearance` を作り `AgentView` に載せる（**真値は載せない**）
- [ ] 各ノードが「見た目→能力」の写像を自分の戦闘・観戦から学習する
- [ ] **完了条件**: 世代が進むと初対面の推定誤差が下がること

### 10. 段階12：伝承（等価交換）とヒントの進化 ← **集団創発の本命**
> 目的: 効用式のグローバル定数を個体ごとの値にして学習させ、**他個体と交換できるようにする**。伝えるのは effort そのものではなく**effort を決める前提**

- [ ] **12a**: `retaliation` / `pAccept`（世界の事実）と `RiskWeight` / `CompetitionWeight`（好み）を個体ごとの値にする。材料は成功・失敗・観察。`LearningRate = 0` で全部止まること
- [ ] **12b**: 深い伝承を既存の `ActObserve` に相乗りさせる。**双方向・同時の歩み寄り**（一方向の「学ぶ」にしない）。好意の加点量は実際に動いた値に比例。垂直伝承（親→子）と水平伝承を同じ経路で扱う。`LamarckRate` を有効化
- [ ] **12b**: 被参照数の分布を測る（ハブ化していないか）
- [ ] **12b**: **好意を効用の正の項にする**
- [ ] **12c ヒント**: 上限本数のヒント枠（状況特徴量 × 対象行動 × 重み）。**効用への加減点としてのみ働き行動は直接決めない**。遺伝子予算を消費する
- [ ] **12c**: 突然変異は二段階（重みの微調整＝通常の変異／構造の変化＝**既存の天才イベントを流用**）。継承は染色体式を枠単位に拡張
- [ ] **12c**: トレース（`trace.go`）に「ヒント由来の加点」を出す
- [ ] **完了条件**: 分散と空間相関の指標を足し `LamarckRate` 0/有効で比較／地域ごとの言い伝えの分布／多様性の定量化／好意の重み 0/有効の A/B／**あるべき状態 B〜E を段階6.5 の指標で判定**／被参照数の分布／**個体数と死因の内訳を必ず確認**

### 11. 段階13：視界をグリッド単位にする（挙動変更）
> 7a と分けるのは、こちらが挙動の変更だから。**独立していて後回し可**

- [ ] 視界を円から「自セル＋周囲Nグリッド」に変更
- [ ] 視力（N）を遺伝子にするかどうかを決める
- [ ] **完了条件**: 異方性が入るので、逃走成功率と戦闘数の変化を測る

---

## 実装前に決めること（担当段階ごと）

`PLAN.md` の「未決」を、決着をつける段階に割り振ったものです。

**段階7c までに決める**
- [ ] `BudgetInheritSpread` の値（分布は正規乱数で確定済み。残るのは値だけ）
- [ ] 初期配分のディリクレ分布の α（出発点 α ≈ 0.5〜1）
- [ ] 天才の発生率を「出生あたりの確率」で持つか「世界側の間隔」で持つか
- [ ] 天才2段階の発生率と、予算まわりの数値（`PLAN.md` の表は出発点の案であって決定ではない）
- [ ] `fitness` を魅力遺伝子に置き換えたあと、体調（体力）をどれだけ残すか
- [ ] 予算のドリフトに価格を戻すか（**先に決めず、7c で平均予算の推移を測ってから**）

**段階7d までに決める**
- [ ] `TicksPerYear` の値（**本決め**）
- [ ] 育児期間の長さと、親側の負担
- [ ] 寿命消費の既定値（`MaxLifespan` / `StarveLifespanRate` / `OverfedLifespanRate`）— 現在の暫定値では標準実行長でほぼ発火しない

**段階9 までに決める**
- [ ] 記憶の「単位時間に記憶できる量」と「忘却曲線」の扱い（容量だけ予算内・帯域は容量から導出、という案）
- [ ] 血縁の初期好意ブーストの大きさ（範囲は「直系の親子のみ・世代減衰なし」で確定済み）
- [ ] 「接触」（#22 の起点リセット）の定義＝視界内の遭遇でよいか、交換のチェックが走ったときだけか

**段階11 までに決める**
- [ ] 外敵のサイズ分布（協力が原理的に成立するサイズが十分現れる分布であること）

**段階12 までに決める**
- [ ] 等価交換の歩み寄り率と、動いた量を好意に換算する係数
- [ ] ヒント枠の上限本数（案 3〜5本）と、枠1つあたりの予算消費量
- [ ] ヒントの状況特徴量として何を許すか（`Perception` から取れるものに限る、までは確定）
- [ ] 深い伝承に相互占有を追加するか（**被参照数の分布を実測してから判断**）

**段階11 の完了時に書く**
- [ ] 「あるべき状態」の目標数値（集団数・戦闘率比・持続時間・種族ごとの個体数の帯）

---

## 保留（条件つきで復活・着手しない）

- 明示的な「協力」（`ActCooperate` ＋ 協力性遺伝子）— 段階11 まで進めて群れ狩りがまったく現れない場合にだけ再検討
- 明示的な「集団」（集団ID・所属行動・敵対フラグ）— 段階12 まで進めて**あるべき状態 B〜E** が満たせない場合にだけ再検討
- `protocol` / `server` / `client` — engine 側をどこまで進めてから着手するかは未定
- 配偶者を巡る同性間の戦い、地形
- 激しい活動（求愛・戦闘）による寿命消費 — 今回は実装しない（食事量だけを見る簡易近似で代替）
