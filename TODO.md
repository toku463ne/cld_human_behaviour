# TODO（優先順位つきタスク一覧）

**`PLAN.md` を作業単位に落として優先順位をつけたものです。** ルールの中身・理由・完了条件の詳細は `PLAN.md` の該当節にあります。ここには**何を・どの順で・なぜその順か**だけを書きます（同じことを2箇所に書かないため）。

- 進め方: 上から順に。1タスク＝1コミット程度の粒度を目安にする
- 各段階の詳細な受け入れ条件・触る文書は `PLAN.md` の同名の節を見る
- 「なぜそう決めたか」は `HISTORY.md` の設計判断ログ

## 優先順位の考え方

| 優先度 | 意味 |
| --- | --- |
| P0 | 土台。これを先にやらないと以降の測定・性能が全部やり直しになる |
| P1 | 遺伝の中核。ここで測定のベースラインが1度リセットされる |
| P2 | 表現力の拡張。P3（協力・伝承）の前提になる |
| P3 | 本命（外敵と伝承＝集団の創発）。P0〜P2 が揃ってから |
| P4 | 環境と食料生態系（世界の側に差をつける）。2026-09-03 追加。段階12 と並行してよいが、内部の順序は守る |
| P5 | ゲーム化とマップエディタ（人が世界に触れるようにする）。2026-09-05 追加。**内部の順序は守る**（理由は下の P5 の節） |
| — | 保留。条件が満たされたときだけ復活 |

**順序を入れ替えてよいもの / いけないもの**

- 6.5 → 7a → 7b → 7c → 7d は**この順を守る**（後段が前段の測定基準・性能・遺伝子表現に乗るため）
- 段階8（チャネル化）は 7c の後ならいつでもよい。**段階9 より後ろに回してもよい**
- 段階9（好意）は**段階12 の前提**。段階10・13 は独立していて後回し可
- **段階14 → 15 → 16 → 17 はこの順を守る**（14 が地域という語彙を作り、15 の固定版を 17 が置き換える。16 は 17 の前にベースラインを取るためにここにある）。**段階18（体内時計）は段階12 より後**
- **P5 は 19 → 20 → 21 → 22 の順を守る**（20 の未決が 21・22 の形を決め、21 が 22 の保存経路になる。理由は P5 の節）。**19 だけは P4 の宿題と並行してよい**（世界の状態に触れないため）
- `protocol` / `server` / `client` の着手時期は未定（`CLAUDE.md` の未決事項）

---

## P0：土台（最優先）

### 1. 段階6.5：計測の先行整備
> 目的: 「あるべき状態」の目標値を憶測でなく実測から決める。読み取り専用なので安い。以降の全段階の効果測定に使い回す

- [x] クラスタ検出（素朴な実装でよい）→ クラスタ数・サイズ分布 — `World.Clusters(linkDist)`、連結距離 30 固定、`cmd/experiment` に `clusters` / `clusterSize` / `grouped` / `largestShare`
- [x] membership half-life（同じ顔ぶれが一緒に居続ける時間） — `engine.MembershipTracker`、組の共在生存曲線として測る、`cmd/experiment` に `halfLife` / `together` / `censored`
- [x] 集団内戦闘率 ÷ 集団間戦闘率 — `engine.FightTracker`。書かれたとおりには測れないので「仲間（200tick前から同じクラスタ）÷ よそ者」の遭遇あたり戦闘率に置き換えた
- [x] 集団間の距離分布 — `World.ClusterGaps(linkDist)`、`cmd/experiment` に `gap` / `gapP10` / `gapRel`
- [x] 種族ごとの個体数 — `engine.CensusTracker`。窓（5000tick）の平均で持ち、読むのは振れ幅ではなく **谷（最小 ÷ 平均）**。絶滅だけは窓ではなく実行全体で覚える。`Agent.Species` のラベルも先に置いた（今は human だけ・ルールは参照しない）
- [x] `cmd/experiment` の出力に上記を追加（O(n²) なので頻度は間引く。クラスタの4指標・half-life の3指標・戦闘率の3指標・距離の3指標・種族の4指標をすべて追加済み）
- [x] **完了条件**: 現状を 50000tick 回して測り、「集団が無い世界のベースライン」として `HISTORY.md` に記録する（**目標値はまだ書かない**＝段階11 の完了条件へ）— 2026-09-01、24シード × 50000tick。**塊はあるが集団は無い**（半減期60tick・`fightRatio` 0.93・`gapRel` 0.80）

**段階6.5 は 2026-09-01 に完了。** 次は段階7a。

### 2. 段階7a：空間グリッド（挙動を変えない高速化）
> 目的: `perceive()` の実質 O(n²) を潰す。以降の全段階の土台であり、地域割りの出所にもなる

- [x] `World` に一様グリッドを持たせ、ノードと食料をセルに登録する — `engine/grid.go`。セル幅＝`PerceptionRadius`、差分更新せず「古い」印＋遅延再構築、候補は添字の昇順で返す（走査順が乱数の消費順なので）
- [x] `perceive()` の近傍探索をグリッド経由にする（**円の視界判定はそのまま残す**）— あわせて `nearestFoodInSight` と `exchangeReadings` も
- [x] 食料ごとの「最も近い他個体」もグリッド経由にする — **個別に問い合わせない**（観測者に見えない個体まで拾って答えが変わるため）。他個体の走査に相乗りするので、上の項目で自動的にグリッド経由になっている
- [x] **完了条件**: 同一シードで導入前と統計値が完全一致すること — `cmd/experiment` の出力がバイト単位で一致、加えて `engine/golden_test.go` で指紋を固定
- [x] ベンチを前後で取り `HISTORY.md` に記録 — **数百体では全走査のほうが速く、効き始めるのは500〜600体から**（2966体で −30%）

**段階7a は 2026-09-01 に完了。** 次は段階7b（継承則の入れ替え）。

---

## P1：遺伝の中核

### 3. 段階7b：継承則の入れ替え（染色体式＋幅）
> 目的: 混合遺伝（毎世代で分散が半減）をやめ、粒子的遺伝で多様性を維持する

- [x] `tryBirth()` を「遺伝子ごとに親のどちらかを引く＋正規乱数」に変更（引き方は遺伝子ごとに独立）— 能力の標準偏差が約2倍、選択圧は理性で約4倍。**ここで過去の測定値とは比較できなくなる**
- [x] `Config` に `MutationRate` を追加、`MutationStd` を再較正 — **既定は 0.01 ではなく 0.02**（名目の分散を合わせるだけでは端のクリップで15%足りなかった）、`MutationStd` は 40
- [x] `LamarckRate` を `Config` に用意（この段階では未使用の枠だけ。段階12 まで誰も読まない）
- [x] **完了条件**: 前後で `cmd/experiment` を回し、能力の分散と選択圧の変化を記録 — 分散は約2倍（5.6〜6.1 → 11.0〜14.5）、選択圧は2〜3倍。**新しいベースラインは `HISTORY.md` の 2026-09-01「段階7b 完了」の項**

**段階7b は 2026-09-01 に完了。** 次は段階7c（遺伝子ベクトル化と予算制）。

### 4. 段階7c：遺伝子ベクトル化と予算制 ← **この段階が最大**（2026-09-01 にほぼ完了。残りは下記の完了条件2つ）
> 目的: 能力3種を `Genome []float64`（遺伝子9種）に置き換え、親から継ぐ予算制にする。**魅力もここで入れる**（後から遺伝子を足すと予算も測定基準も取り直しになる）

- [x] `Power/Rationality/Intelligence` → `Genome []float64`（遺伝子9種）に置換 — 挙動不変のコミットとして分離
- [x] 初期個体: 予算を抽選（`GeneBudgetMean`/`GeneBudgetStd`）＋ 配分をディリクレ分布から引く
  - [x] ガンマ分布のサンプラを自前で書く（外部依存を増やさない）／一様乱数の正規化は使わない／1本あたり 1〜100 にクリップ
- [x] 子の予算＝親の予算 ＋ 正規乱数（`BudgetInheritSpread`）。`BudgetHeritability` は 0〜1 の連続値で切替可
- [x] 継承時の正規化（親由来の値を子の予算に合わせてスケール）→ **突然変異は配分を動かすだけで合計を増やさない**
- [x] 天才2段階（`GeniusRate` / `GreatGeniusRate`）。`TicksPerYear` は出発点 500 を仮置き（**実測の間隔は22.8年。狙いの10年とずれているので段階7d で測り直す**）
- [x] 既存の `Power`→攻撃力、`MaxVitality`/`MaxSpeed` を遺伝子側へ移す（**体力の閾値は「自分の上限に対する割合」に変えた**）
- [x] `fitness` を魅力遺伝子で置き換える（**実能力とは相関させない**）
- [x] 再判断トリガーに「魅力の高い個体が視界に入った」を追加
- [x] `cmd/experiment`: 能力指標を**絶対値→予算比**に変更、「世代ごとの平均予算」と「予算の分布」を出力
- [x] **予算のドリフトに価格を入れた**（`BudgetUpkeep` ＝ 大きい身体ほど空腹が速い）。**効くが止まらない**（588→530）。理由は「この世界では誰も飢えて死なない」から
- [x] **完了条件の残り**（段階8 のあとに判定・2026-09-01）: **条件の文言は満たさないが、条件が確かめたかったことは満たす。** 「大きくて遅い／小さくて速い」は今も並存しない（体力と移動速度はどちらも「殴る」に対抗して買われる同じ側なので競合しない）。一方で**異なる種類の個体は並存している**——最強の対立軸は「殴る個体 対 考える個体」（相関 −0.309、基準線 −0.125）。詳細は `HISTORY.md` 追記25
- [x] **目視の半分**（2026-09-01）: `cmd/devview` の描き方を変えた。**輪の大きさ＝体力上限、塗り＝残り体力、後ろの尾＝速さ、輪の太さ＝攻撃力。** 右パネルに遺伝子9本と予算比も出るようにした。**段階7c の完了条件はこれで全部済み**
- [x] **段階8 への申し送りは解決した**（2026-09-01）: `killShare` 0.92 → 0.79、個体数 63 → 111。防御・回避という買い物が増えたことで、攻撃力への一極集中が解けた
- [x] **「段階7c までに決める」の6項目に数値をつけた**（2026-09-02）。`BudgetInheritSpread` 30・`GeneInitAlpha` 0.8・`FitnessConditionWeight` 0.35（`Config` へ移した）・`BudgetUpkeep` 1 は**そのまま据え置きと決定**、天才の発生率は**出生あたりの確率のまま・値の確定だけ段階7d へ移送**。根拠と外した予想は `HISTORY.md` の同日の項と追記27

**段階7c は 2026-09-02 に完了。** 予算はもう暴走せず（20000tickで+51、そこから60000tickまでで+31と減速）、遺伝子の上限張り付きは19〜27%だが**予算は最大900に対して432**なので、縛っているのは予算のほう（特化であって飽和ではない）。

### 5. 段階7d：成長・老い・寿命と育児期間
> 目的: 伝承（段階12）の土台＝親子が一定時間近くにいる状況を作る。**寿命消費だけ 2026-08-31 に先行実装済み**（意思決定に一切関与しない背景処理という制約つき）

- [x] 寿命を状態として持つ（`Agent.Lifespan`）／過小（空腹）・過度（腹八分目超）で消費／`Config` で各々 0 にして切れる／`cmd/experiment` に `aged`・`agedShare`
- [x] **年齢補正**: 実効値＝遺伝的素質 × `Agent.AgeFactor`。成長（若い端）と衰え（老いた端）は同じ1本の曲線で、**掛け算は `Agent.Ability` の1か所だけ**。`Gene()` は遺伝的素質のまま（繁殖と測定はこちらを読む）
- [x] **成長**: 食事で進む（新アクションなし）。満腹で最大、飢えれば止まる。**子ども期は3年ではなく2年**（3年だと世界が壊れる）
- [x] 成長の頭打ち（100%）と過度コストの開始（80%）を別の閾値にした。**ただし行動差は出ない**——成長も寿命も `Perception` に載せない設計なので、変わるのは結果だけ
- [x] **#33 慢性的な低体力による寿命コスト**（`FrailVitalityShare` / `FrailGraceTicks` / `FrailLifespanRate`。20%超えでリセット。`ShockRisk` とは別物で効用式に載らない）
- [x] **育児期間**: `ChildRearingTicks`（既定1000）。`= 0` で切れる。給餌なし。**`RearingRadius` は視界と同じ130でなければならない**（45では子を食料から引き離して殺す）
- [x] 親側の負担は**入れない**と決めた／子の生存は親の効用に**入れない**と決めた（子が留まる側だけで成立する）
- [x] **`TicksPerYear` = 500 を本決めにした**（3条件を満たす。天才の発生間隔は段階7c で集計し直し済み）
- [x] **完了条件**: 死因3種は 戦闘62%・餓死34%・消耗4%（どれも支配的でない）／平均年齢10.1年・20000tickで約5世代／成人到達率0.72・子の死は死因の5%／`ChildRearingTicks` 0 との対で `deathRate` +0.71 \*・`nearest` +19.5 \*（**ただし子の生存率は動かない＝効いているのは保護ではなく凝集**）／#33 は個体数に影響なし／**「食べ方が変わる」だけは原理的に測れない**（上記）
- [x] **注意**: 死因が増えたので、段階7c までの測定値と直接比べないこと。**新しいベースラインは `HISTORY.md` の 2026-09-02「段階7d」の項**

**段階7d は 2026-09-02 に完了。** 個体数は成長なしの世界の6割（44.7 → 26.3）で、**これは調整不足ではなく子ども期そのものの値段**（死亡率は上がっておらず、世代の回転が遅くなった形）。

---

## P2：表現力の拡張

### 6. 段階9：記憶の容量・帯域・忘却曲線と、好意 ← **段階12 の前提**
> 目的: `opinions` を有限容量にし、忘却を「容量の取り合い」にする。あわせて記憶に**初めての正の項（好意）**を入れる。**敵意は新設しない**（既存のリスク記憶をそのまま敵意として扱う）

- [x] `opinions` を容量つきに（あふれたら重要度の低いものから捨てる）／単位時間の更新上限（帯域）／忘却曲線を遺伝子化 — 帯域は**容量から導出**（専用遺伝子なし）、忘却は `(100 − 記憶)/49.5` 倍
- [x] **好意**を `Opinion` の新フィールドとして追加。容量は既存の記憶容量遺伝子で管理（専用遺伝子は作らない）
- [x] 発生源はこの段階では2つだけ（**実イベントのみ・共在時間による加点はしない**）: ペア形成・出産／血縁（**直系の親子1ホップのみ**）
- [x] **#32 休息を無防備にする** — **既定の重みは 0.05**。0.10 以上にすると個体数が落ちる（**休息の価値が丸ごとゼロになる崖**に入り、消耗死が増えて出産前のペアが壊れるため。`HISTORY.md` の測り直しを参照）
- [x] **#22 接触による減衰起点のリセット**（減衰式は変えず「経過tick」の起点だけ更新。好意・リスク記憶の両方に適用）
- [x] **完了条件**: 全部済み。個体数は動かず（−0.2 ± 6.9）／`restNear` 6.5 → 2.0／`friends` が容量に追随（6/12/24/∞ で 1.01/2.16/2.55/3.88）／devview で目視／`BenchmarkDecide` 10.0 → 11.2µs
- [x] **申し送りを閉じた（2026-09-04）。出ないのが正しい値付けだった。** 3度測って動かなかった `shMemory` を、個体数がほぼ倍になった世界で**掃引して形を見た**ところ、**平らではなく右下がり**だった: 記憶6人 → 個体数79.9／12人 → 77.7／24人 → 73.8／無制限 → 54.1（−23.6 \*\*\*）。**記憶がこの世界で主に生産するのは「友人」で、好意は休息を安全に見せるので net で損。** `memFull` は 0.93 なので容量自体はしっかり縛られている（「制約になっていない」わけではない）
- [x] **記録してあった診断（地域が12個では容量が縛られない）は間違いだった。** 地域を108個にしても個体は91.2個を知り `shMemory` は動かない——**決定事項#41 が「地域の記憶は容量の枠に入れない」と決めているので、地域をいくら増やしても容量は縛られない**。**2段階前に自分で決めたことと矛盾する仮説を書き留めていた**。接触リフレッシュを切る筋も試したが、やはり容量を増やすと悪くなる
- [ ] **新しい観察（別項目）**: 「好意 → 休息が安全に見える」経路が**段階12b・15b・ここの3度 net の負として現れている**。段階18 で「眠っている友人は見張れない」を足して個体数が +12.6 \* 上がったのもこの経路を弱めたから。**`AffinityTrust` が完全な安心（`trust` が1に届く）を許していることの妥当性を測る価値がある**——記憶の話とは別なので独立項目

**段階9 は 2026-09-02 に完了。** 次は段階10（見た目からの学習）か段階7d（成長・老い）。

### 7. 段階8：攻撃・防御・回避のチャネル化
> 目的: 「攻撃力・防御力・移動・回避の使い方で体力の減りが決まる」。**この段階が一番危険**（攻撃側有利の非対称量が動き、個体数を崩しうる）

- [x] effort をプリセット3種（攻め寄り／守り寄り／逃げ寄り）に拡張
- [x] 防御＝ダメージ乗数を下げる／同tickの攻撃出力も下がる
- [x] 回避＝確率で完全無効／移動速度に依存
- [x] 体力消費 = Σ(チャネルごとの 単価 × 使用率）
- [x] **完了条件**: `BenchmarkDecide` 8.0µs → 10.3〜11.1µs（+3割）／**個体数は 86 → 111 に増えた**（`killShare` 0.92 → 0.79）。防御・回避の両方に予算が回るようになり、**選択圧がパワーから頭の良さへ移った**

**段階8 は 2026-09-01 に完了**（段階10 より先に順序を入れ替えた）。`PLAN.md` の「一番危険」という警告は**逆だった**——危険だったのは入れないことのほう。数値は `HISTORY.md` 追記24。

---

## P3：本命（外敵と伝承）

### 順序の追記（2026-09-06・出来事の伝播と協力の誘いが入ったので）

**決めた順序: 31（目撃の伝播）→ 32（協力の誘い）→ 29（地形の認識と伝承）→ 30（高台の遮蔽）→ 21（保存/読込）→ 22（エディタ）。理由は4つ。**

**（2026-09-06 の更新）29 は先に通し、その続きの 33 が入りました。今の順序は 33 → 31 → 32 → 30 → 21 → 22 です。理由は3つ。**

- **33 を先頭に置くのは、開いたままの診断は始まっていない問いより高くつくから。** 段階29 の結論は今のところ「**避けるようにはなるが半分にはならない。ただし、その頭打ちが交絡のせいか本当の限界かは分からない**」で止まっている。**この宙ぶらりんの上に、30（遮蔽）も 22（エディタ）も、保留の「水辺の豊かさ」も乗っている**。しかも 33 は**パラメータ1つと再正規化**で、腕はもう用意してある
- **P3（31・32）との関係は前回と同じで、交絡しない。** 地形は既定オフなので、33 を先に通しても 31・32 のベースラインは動かず、測り直しは要らない。**逆も同じ**なので、群れ狩りの答えを先に見たければ 31 を前に出してよい
- **30 は 33 のあと。** 「高いところに値打ちがある」を測るのは、**地形が個体数を払い続けたままなのかどうかが決まってから**のほうが読みやすい（前回の理由 2(c) がそのまま生きている）。**負の相関（荒れた地域ほど豊か＝リスクとリターン）は 33 の正の相関が解けたあとの独立条件**で、狙いとしては 30 と同じ群（「代償を払ってでも行く値打ち」）

1. **31・32 を 29・30 より前に置くのは、開いている問いの重さが違うから。** 「群れ狩りは現れるか」は**段階11 以来ずっと開いたまま**で、**あるべき状態 B〜E** というこのプロジェクトの採点表そのものに載っている。一方 **地形は既定オフ**（`TerrainMap` が空）なので、29・30 を後に回しても**世界のベースラインは1ビットも動かず、待っているのはエディタ（22）だけ**
2. **31 を 32 より先に置くのは、安いほうを先に確定させるため。** 31 は語彙も状態も増やさず既存の推定と好意に載せるだけ。**そして 31 を後にすると、32 の看板の数字（`packSize`）を測ったベースラインが動いて測り直しになる**（「測定を伴う段階の前に既定を動かさない」の同じ形）。**31b の既定がオフになる可能性がある**（好意の発生源を広げる値段）ので、**誰を信じてよいかが広がるのかどうかを、誘いが載る前に決めておく**
3. **31 と 32 は同時に測らない。** 31 は好意とリスク推定の**発生源**を広げ、32 は好意の**使い道**を広げる。同じ回に入れると、群れ狩りが現れたときに「誘えるようになったからか、誰を信じてよいか分かるようになったからか」を分けられない（`PLAN.md` の同名の節）
4. **2つの群は互いに交絡しない**ので、順序は価値の判断だけで決めてよい（地形は既定オフ＝31・32 のベースラインを動かさない）。**遊ぶための世界を先に良くしたいなら 29 を前に出してよく、測り直しは要らない。** 動かしてはいけないのは**それぞれの内部順序**（31a→31b、32 の中の順、29a→29b、30a→30b）と、**伝承・好意に触る変更を同じ回に2つ入れないこと**

### 8. 段階11：種族の汎用化と外敵（協力の土俵）
> 目的: 「隣人が生きていると自分が得」を**世界側の事実**として置く。「協力」という言葉は engine に登場させない

- [x] 人間・外敵で同じノード構造・同じ意思決定エンジンを共有する — 違いは**予算の抽選範囲と食性の2点だけ**
- [x] 外敵に「大きさ」を持たせ、ドロップ数を大きさで決める／分配は攻撃に参加した者だけ（**食べられない種族には権利を渡さない**）
- [x] **共同戦闘を好意の加点源にする**（2026-09-03、`AffinityHunt` = 6）— 誰が数えられるかは**分配ルールそのまま**。**「主要な」加点源にはならない**（20000tick に13.7回しか発火しない）ので、集団の指標は動かない。意味は段階12 の前提＝**非血縁の好意**を作ったこと
- [x] 食料2種（植物＋死体からの肉）。人間は共食い不可、外敵は種族を問わず食べる — **A/Bの2種類の共通食料は作っていない**（AとBを区別するルールが無いので、ラベルが2つ増えるだけになる）
- [x] **完了条件**: 群れ狩りが現れるか現れないかを測る → **現れなかった**（`packSize` は動機を切っても 1.43 → 1.46 で変わらない）／段階6.5 の指標を測り直した／`PLAN.md` の表に**あるべき状態A の帯**を書き込んだ（B〜E は段階12 のあとに判定）

**段階11 は 2026-09-03 に完了**（実装は 2026-09-01、残っていた好意の加点源と外敵のサイズ分布が 2026-09-03）。群れ狩りが現れなかったので、`TODO.md` 保留欄の「明示的な協力」の再検討条件は満たされた。**ただし再検討の前に段階12（伝承）を通すこと**——「一緒に狩ったことが記憶に残らない」は解消したが、残る「狙いを合わせる手段が無い」は段階12 でしか埋まらない。**獲物を大きくしても群れ狩りは現れない**ことは実測済み。**段階12 は 2026-09-04 に通ったので、この前提も消えた**——再検討は下の 8c（段階32）。

### 8b. 段階31：目撃した殺害が伝わる（脅威の周知と、討伐への好意）
> 目的: 誰かが誰かを殺すところを見ても、見ていた者に**何も残らない**のを直す。**新しい状態も新しい記憶の種類も作らない**——既存の強さ推定と好意に、**符号違いの1本の経路**で載せる。詳細は `PLAN.md` の段階31 と決定事項#55

- [ ] **31a**: 人間が人間を殺した瞬間、**視界内の全ノード**の (B) 強さ推定を更新する（**1回の特に情報量の大きい観測**として）
- [ ] **重みは `ActObserve` 1回分より粗く。** 観察は体力と時間を払い、1度に1人という排他性まで背負っている。**目撃がただで同じだけ教えると観察という選択肢が死ぬ**（#55）
- [ ] **完了条件に `ActObserve` の選択率を入れる**（下がりすぎていたら重みが大きすぎる）
- [ ] **記憶容量は既存の `Opinion` の出し入れルールのまま**（新しい枠を作らない）
- [ ] **31b**: 外敵を倒した側への**好意**を、見ていただけの第三者にも加点する（`AffinityHunt` は分配に与った者限定なので、その拡張）
- [ ] **既知のリスク込みで測る**: 好意の発生源を広げると「**好意 → 休息が安全に見える**」経路が強まる（段階12b・15b・記憶の掃引の3度 net の負）。**`restNear`・個体数・消耗死を必ず一緒に見る**
- [ ] **予想を先に書く**: `priorErr` 系は下がる／`killShare` は下がる方向だが**「避ける」は世界を縮めた前例がある**ので個体数を見る／31b は `friends`・`restNear` 上昇と引き換えに個体数を払いうる
- [ ] 触る文書: `NODE.md`（8節）、`PARAMETERS.md`、`HISTORY.md`、`CLAUDE.md`

### 8c. 段階32：外敵討伐への協力の誘い（保留#10 の再検討）
> **保留から出した**（2026-09-06）。再検討条件は満たされている——群れ狩りは動機・サイズ・栄養価をどれだけ振っても現れず（`packSize` 1.2〜1.5）、段階11 が名指しした「狙いを合わせる手段が無い」の前提だった段階12 は 2026-09-04 に通った。**ゲーム向けの社会機能ではなく P3 の一部。** 詳細は `PLAN.md` の段階32 と決定事項#56

- [ ] **行動語彙を1つ足す**（「誘う」）。**段階0 以来はじめて語彙が増える**ので `NODE.md`「行動の語彙」と `cmd/devview` のキー操作も同時に
- [ ] **受けるかどうかは既存の効用比較の1候補として採点する。ハードコードされた好意の閾値は作らない**（段階25 と同じ原則）
- [ ] **好意は参加ボーナスではなく信頼**——誘い手が土壇場で離脱しない確からしさとして**リスク見積もりを割り引く**。既存の `AffinityTrust` を戦闘の文脈へ広げるだけ
- [ ] **協力性遺伝子は作らない**（#10 の骨格は保つ）。増えるのは語彙1つと好意の使い道1つだけ
- [ ] **プレーヤー専用にしない**（AI同士でも誘いが働いて初めて群れ狩りを測れる）
- [ ] **事後の好意加点は新設しない見込み**（`AffinityHunt` がそのまま条件を満たす）
- [ ] **予想を先に書く**: `packSize` / `jointHunts` が**段階11 以来はじめて動くか**（動かないなら段階11 の診断そのものが誤り）／`killShare`・個体数・世代数も一緒に（**外敵が倒しやすくなることは食料が増えることでもある**）
- [ ] **発火頻度を先に数える**（段階24 の教訓）。誘いが1実行に何回起きるか
- [ ] **`AffinityTrust` の較正（`trust` が1に届いてよいか）をこの段階の中で閉じる**——上の 10b の未測定項目で、戦闘へ広げると影響が大きくなる
- [ ] 触る文書: `NODE.md`（行動の語彙・8節）、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`

### 9. 段階10：見た目からの学習 【完了・2026-09-03】
> 目的: 初対面の相手に `PriorStrength = 50` を当てはめるのをやめる。**独立しているので前後させてよい**

- [x] 遺伝子から観測可能な相関値 `Appearance` を作り `AgentView` に載せる（**真値は載せない**）— 体力遺伝子と移動速度遺伝子の平均 × 年齢補正。攻撃力は入れない
- [x] 各ノードが「見た目→能力」の写像を自分の戦闘・観戦から学習する — 最小二乗の直線1本、**傾きは0へ引き戻す**、**記憶容量は消費しない**
- [x] **完了条件**（2026-09-03）: 条件間ではなく**同じ実行の同じ遭遇**で3つの推定器を比べる形に測り方を変えた。学習の利得 +3.04 \*\*\*（50000tick では +8.07）、経験ありの観察者 26.6 対 経験なし 33.5、devview で目視
- [x] **申し送りを閉じた（2026-09-04）。2つは別々の話だった。**
- [x] **`rareTrough` の帯割れは解決済みで、しかも見た目とは無関係だった。** どの条件でも 0.62（`nolooks` も同じ）。段階12〜18 と見かけトリガーの修正で世界が健康になったぶん。**同じ段階で観測されたというだけで同じ原因とは限らない**
- [x] **「体つきを読んだぶんは11%」は 0% に悪化していた**（`slopeGain` +0.06 ± 0.06、相関 0.22 → 0.08）。学習そのものは6倍の価値になった（`learnGain` +3.04 → +18.00 \*\*\*）が、**体つきの寄与は消えた**。「世界が豊かになれば信号が強くなる」という期待は逆に外れた
- [x] **理由が分かった: `Appearance` の定義が予算制と噛み合っていない。** 体力と速さは「攻撃力を見せないため」に選ばれたが、**予算制ではその2本は攻撃力と同じ財布を奪い合う**。**隠すために選んだ変数が、まさにそのために予測力を失っていた**
- [x] **修正方法も実装・測定済み（`LooksShowBulk`）: 見えるものを「総量」にすると相関 0.08 → 0.50 \*\*\*、体つきの利得 0.06 → 1.24 \*\*\***（総量は見せても配分は隠れるので、攻撃力は依然として隠しパラメータ）
- [x] **それでも既定は変えない。** 推定の精度そのものは1ミリも上がらず（`priorErr` 18.53 対 18.56。傾きが「世界の平均を知る」ぶんを肩代わりするだけ）、**個体数を 8.8 \* 払う**。**買えるものが無いのに値段だけ払う変更**なので対照条件として残す

### 10. 段階12：伝承（等価交換）とヒントの進化 ← **集団創発の本命**
> 目的: 効用式のグローバル定数を個体ごとの値にして学習させ、**他個体と交換できるようにする**。伝えるのは effort そのものではなく**effort を決める前提**

- [x] **12a**（2026-09-04）: `retaliation` / `pAccept`（世界の事実）と `RiskWeight` / `CompetitionWeight` / `ShockRisk`（好み）を個体ごとの値にした（`engine/lore.go`）。**信念の学習は既定でオフ**——動かないからではなく動くから。真の殴り返し率は 0.15 で、`0.7` は実在しない報復への恐れとして世界を支えていた（知ると個体数が半分、しかも増えるのは殺しではなく餓死）。**好みの側は既定オン**（広がり 0.15、較正済み）で、これだけで理性への選択圧が負から0へ戻る
- [x] **12b**（2026-09-04）: 伝承を `ActObserve` に相乗りさせた。**双方向・同時の歩み寄り**だが、**1回は差の2%**——「中間で会う」0.5 は段階12a が作った多様性を消して個体数を半減させる（交換は1実行に19000回起きる）。好意の加点は動いた量に比例。垂直・水平は同じ経路
- [x] **12b**: 被参照数の分布を測った。上位5分の1が0.48（完全均等0.20、無作為の底0.22）＝**偏りは実在するがハブ化はしていない**ので相互占有は足さない
- [x] **12b**: **好意を効用の正の項にした**（`LoreValue`）
- [x] **12c ヒント**（2026-09-04）: 上限4本の枠（状況×行動×重み）。**加減点としてのみ働き、ヒント由来の分岐は無い**。**空の枠でも予算を食う**
- [x] **12c**: 突然変異は二段階（重み＝通常の変異／構造＝天才イベントの流用）。継承は枠単位の染色体式
- [x] **12c**: トレースに「ヒント由来の加点」を出した（`Utility.Hint`、devview では `hunch`）
- [x] **`LearningRate` を再測定した**（48シード）。**結論は変わらず既定 0**——誤差は 0.56 → 0.40 \*\*\* に下がるが個体数は 15.8 → 9.2 \*\*。段階12b の好意はまだ「よそ者への抑止力」にはなっていない
- [x] **完了条件**: 分散の指標を足した／多様性を定量化した／好意の A/B／**あるべき状態 B〜E を判定**（`fightRatio` 達成・`halfLife` 達成だが下限値・`gapRel` 未達）／被参照数／個体数と死因。**地域ごとの言い伝えの分布だけは段階14（地域という語彙）待ち**

### 10b. 段階12 から出た宿題（小さい・独立）
> 性能の2件は 2026-09-04 に完了。条件つきで残していた2件は 2026-09-05 に再判定して閉じた（**どちらも「仕組み」ではなく「物差し」の話だった**——片方は予算制のもとで絶対値を読んでいたこと、もう片方は密度で割る指標が分子と分母を同時に動かすこと）。代わりに**新しい宿題が1件**出ている

- [x] **`BenchmarkEngineStep` は絶対値が読めない**（2026-09-04 に修正）。`benchWindow`（400tick）ごとに世界を作り直す形にした（作り直しの間はタイマーを止める）。`-benchtime` を10倍振っても 747〜787µs に収まる。**性能を直す前に、性能を測る形を先に直した**
- [x] **`decay()` の性能**（2026-09-04 に解決）。**書いてあった原因は症状のほうだった**——`math.Exp` の96%は `weakestOpinion`（捨てる記録を探す全走査）から来ていて、しかも**71%の個体でその走査は必ず失敗していた**（捨てられる＝重み0の記録が、交換で好意が付いたことで消えたため）。記録の重みは0に漸近しても到達しないので**答えは二度と変わらない**。`Agent.noSpareMemory` にキャッシュし、記録が出入りしたときだけ破棄する。`Step` 940µs → 345µs、段階12b の持ち出しは 2.4倍 → 1.12倍。**伝承なしの側も速くなった**（386 → 308µs）＝この無駄は段階12b が作ったのではなく頻度を上げただけ。**指紋テストが無変更で通ることが挙動不変の証拠**
- [x] **「ヒントは頭の良さへの選択圧を下げる」は再判定で閉じた（2026-09-05、64シード × 40000tick）。** 絶対値では今も出る（`dIntelligence` は nohints − baseline で +10.55 \*\*\*）が、**頭の良さに固有の話ではなかった**——同じ符号が理性（+6.01 \*\*\*）にも出て、**`budget` そのものが +42.06 \*\*\*** 動く。ヒント枠は遺伝子に配る前の予算から引かれる（`world.go:1046` `fitBudget(genome, budget-hintCost)`）ので9本が一斉に小さくなる。**予算比で読むと差は1標準誤差**（`shIntelligence` +0.01 ± 0.01）で、一番動くのは頭の良さではなく速さ（`shSpeed` −0.02 \*\*\*）。**段階7c が「指標は絶対値ではなく予算比で読む」と書いていたとおり**、12c の観察は予算制のもとで絶対値を読んだ副作用だった
- [ ] **新しい宿題: 12c の個体数の利得が消えた。** 同じ 64シードで baseline 対 `deadhints`（同じ値段・重み0）は **+0.20 ± 5.10 ＝ 誤差内**（12c では 22.00 対 14.75）。段階10c の申し送り「段階14〜18 の測定はすべて穴の開いた世界のもの」が**12c 自身にも当てはまった**。既定を変えるかは、ヒントが何を買っているか（`killShare` −0.04 \*\*、`starveRate` +0.19 \*\*）を読み直してから決める
- [x] **`gapRel` の再判定を終えた（2026-09-05）。未達のまま（0.83 ± 0.03、目標1.2）で、しかも地域差では動かない。** 段階14・15a・15b・15c を切っても入れても全条件が 0.78〜0.87 に収まる。**予想は逆に外れた**——食料の地域差を消した `noplantgap` のほうがわずかに高い（+0.05 \*）
- [x] **動かないのは測り方のせいではない**（`TestClusterGapsRelativeOfAnAvoidingLayout` を追加）。無構造な配置が 0.98、格子状に避け合った配置が **1.92** と読めるので、**1.2 は目盛りの中にある**。世界の側が届いていない
- [x] **世界にある唯一の「避ける」規則を4倍にしても動かない。** `hardrestexposure`（48シード）は `restNear` 5.17 → 2.54 \*\*\*、`nearest` +8.34 \*、`gap` +12.79 \* と**個体は確かに避けるようになる**（個体数を 15.2 \*\* 払って）のに、`gapRel` は +0.01 ± 0.04。**避けると集団の数も減る**ので、密度で割る `gapRel` は分子と分母が同時に上がって動かない。**あるべき状態E は今の規則の強弱では届かず、`gap` と `gapRel` は別のことを言っている**（`PLAN.md` の表に反映済み）

### 10c. 段階15 から出た宿題 【完了・2026-09-04】
> **性能の宿題のつもりだったが、世界で一番大きなバグだった。** 直すと `Step` が2.4倍速くなる（1010 → 419µs）だけでなく、**個体数がほぼ倍**（45.0 → 77.7 \*\*\*）、餓死率が4分の1（2.19 → 0.58 \*\*\*）、世代数1.5倍。理由は `ChoiceNoise`（20）が候補のスコア（5〜30）に対して大きいこと——**毎tick 考え直す個体はそのたびにノイズを引き直し、僅差の候補の間で振動して何も遂行できない**。`CLAUDE.md` が最初から書いていた「毎tick 再計画する個体は計画を遂行できない」に、2行のコードが穴を開けていた。**予想（性能と採食効率のトレードオフになる）は逆向きに外れた**
> **申し送り**: 段階14〜18 の測定はすべてこの穴が開いた世界で行われている。方向は対で取っているので保たれるが、**絶対値は今と違う**。既定オフにした段階17 の3つは、倍の余裕がある世界で測り直す価値がある

- [x] **`TriggerFoodInSight` がエッジではなくレベルになっている。** コメントは「Food came into view」（視界に**入った**）だが、実装は「視界に**ある**」:
>     if a.Action.Kind == ActRest || a.Action.Kind == ActMove {
>         if w.nearestFoodInSight(a) >= 0 { return TriggerFoodInSight }
>     }
>   食料の多い地域を歩く個体は**毎tick 再判断する**（前のtickにその食料を見て歩き続けると決めたばかりなのに）。段階15 で「良い地面へ向かう」個体が普通になった結果、**`Step` が同じ個体数・同じ密度で 2.4倍**（450 → 1100µs）になった。プロファイルの形は同一で回数だけが1.9倍。**段階15 が作った問題ではなく露出させた問題**なので、独立したコミットで測って直す（決定回数が減るので挙動が変わる）

### 11. 段階13：視界をグリッド単位にする（挙動変更）
> 7a と分けるのは、こちらが挙動の変更だから。**独立していて後回し可**

- [x] 視界を円から「自セル＋周囲1グリッド」に変更した（2026-09-04、`engine/sight.go`。面積を合わせて較正、`SightGrid=false` で円に戻る）
- [x] **視力は遺伝子にしない**（0リング致死・2リングやや悪い＝離散で非線形なので `StrategyDepthUnlock` と同じ轍。個体差を入れるならセルの大きさを連続に振る）
- [x] **完了条件**: `flees` / `escapeShare` を新設して測った。**逃走成功率は動かず**（面積を合わせると逃げ切る距離の平均まで揃う＝異方性は分散しか増やさない）、動いたのは殺し（`killRate` −0.13 \*）。機序は「**近いのに見えない相手**は攻撃の候補に挙がらない」
- [ ] **申し送り**: `Step` が +28%（345 → 442µs）。矩形での問い合わせとセル幅の導出直しで 520 → 442µs までは戻した

---

### 12. 地形の下ごしらえ（小さい・独立・いつでもよい）
> 目的: 速さが「体力と取引する」形になる唯一の見込みが地形。その前の下ごしらえ2つ。**速さは効いていないのではなく一番買われている遺伝子**（予算比0.230）で、足りないのは「速さが有利な場所と不利な場所」。地形の種類ごとの効き方と、ニッチが成立する条件は `PLAN.md` の同名の節

- [x] 食料レースの勝率 `pGet` に**速さを入れた**（2026-09-04）。到達時間の比に置き換え、相手の速さは見えないので世界の基準速度を仮定する（`RaceOnDistance` で旧ルールに戻せる）。**狙いは外れた**——`shSpeed` は動かない（+0.00）。**速さはすでに到達tick数を通じて移動コスト・時間コストに効いていた**ので、`pGet` は二次的な経路だった。この空振りで「足りないのは速さが有利な場所と不利な場所」という仮説のほうが確からしくなった
- [ ] **申し送り**: 1回目の実装（自分の effort 対 相手の全力）は**個体数を3分の1にした**。片側にだけ effort を入れると「ぶらぶら歩いて食べに行く」手が全滅する。相手が全力で走っているという仮定に根拠は無い（近くの他個体は寝ているかもしれない）
- [x] 移動コストを**地形照会関数**経由にした（2026-09-04、`engine/terrain.go`）。世界じゅうが平坦を返すので**挙動は完全に不変**（指紋テストが無変更で通ることが証拠）。**倍率は速度ではなくコストに掛ける**（速度を落とすのは effort がすでにやっている／コストなら荒れ地を速く渡ることが速さと体力の取引になる）。**コントローラは今も平坦だと思って計画する**——地形を `Perception` に載せるかは地形本体の段階で決める

---

## P4：環境と食料生態系（2026-09-03 追加）

> ここまでが個体（遺伝子・記憶・意思決定）を作る段階だったのに対し、ここからは**個体が置かれている場所に差をつける**段階。詳細と理由は `PLAN.md` の「環境と食料生態系（段階14〜18）」と決定事項#36〜#46

### 13. 段階14：休憩場所（既存の休息露出への相乗り）
> 目的: 「休むのに向いた場所」を世界に作る。**一番安い**（`RestExposureWeight` に係数を1つ掛けるだけ）ので、この群の最初に試してよい

- [x] **地域ブロック**を `World` に持たせた（2026-09-04、`engine/region.go`。4×3、壁ではない、地域IDを読むルールはゼロ）
- [x] 地域ごとの係数を `RestExposureWeight` に掛けた。`ShelterSpread = 0` で**乱数も消費せず1ビットも違わない**実行に戻る
- [x] `cmd/devview` に地域の境界と係数を描いた（＋右パネルに今立っている地面の係数）
- [x] **予想を先に書いた**
- [x] **完了条件**: **5つ中2つしか当たらず。** 選別は起きる（`shelterGain` +0.02 \*\*、強度に比例）が、**凝集は起きない**。個体数は不変
- [ ] **申し送り**: 効果が小さいのは**個体が場所を選べない**から（今いる場所しか感じられない）。**段階15b（地域の豊かさの推定）のほうが効く見込み**で、今回の小ささはその必要性の証拠

### 14. 段階15：植物食料の地域差（固定版）と、地域の豊かさの認識・伝承 【完了・2026-09-04】
> **15b/15c も同日に完了。** 一番大きな成果は**集団を1か所に集めたのが伝承だった**こと（自分の経験だけでは `nearest` は動かず、伝承を入れると 140 → 100 \*\*）。**`shMemory` への正の選択圧は出ず**、`PLAN.md` が予告していた疑い先（地域12個では容量が制約にならない）が当たった
> **15a は 2026-09-04 に完了**（`FoodSpread`。総量を変えず分布だけ変える。`rareTrough` が 0.25 → 0.40 \* とあるべき状態A の帯に復帰し、段階14 で起きなかった凝集が起きた。「豊かな地域に集まる」の6割は食料・4割は社会的）
> 目的: 場所によって食べ物の量が違う世界にし、それを**覚えて・伝えられる**ようにする。**15c は段階12b が入っていないと載る先が無い**

- [ ] 15a: 地域ごとの `FoodSpawnRate` 倍率。全地域 1.0 で元に戻ること。**総量は保存する**（変えると個体数が動いて交絡する）
- [ ] 15a: **`FoodSpawnRate` は `CompetitionWeight` / `SkirmishTicks` と同格に敏感**。個体数だけでなく**地域ごとの選択圧と `killShare`** を見る
- [ ] 15a: **集団形成の動機だけを切った対照条件**と比べる（段階11 の `PreyValue=0` と同じ要領）。**「豊かな地域は集団で守られる」を仮定しない**——内側が平和で境界が荒れる形もありうる
- [ ] 15a: 外敵が獲物の濃度に引かれるので、**捕食圧も豊かな地域に連動して集まる**はず。種族ごとの分布を測る
- [ ] 15b: 地域の豊かさの推定（ノイズは既存と同じ `(100−理性)/100`）。鮮度は #22 の遅延減衰＋接触リセットを流用。**新しい知覚も新しい鮮度管理も作らない**
- [ ] 15b: **相手ごとの記憶容量の枠には入れない。ただし記憶容量遺伝子が精度・更新速度に効く**（段階9 で出なかった `shMemory` への初めての正の選択圧になりうる）
- [ ] 15c: 等価交換の学習パラメータに**地域IDごとのベクトル**を足すだけ。垂直伝承は**育児期間の共在**に相乗り（「教える」行動は作らない）
- [ ] **完了条件**: `LearningRate = 0` との対で経験と伝承を分離／地域ごとの推定値の分布を出せること

### 15. 段階16：食料の栄養バランス 【完了・2026-09-04】
> **予想（効果はほとんど出ない）は個体数・`killShare`・死因については当たり。ただしゼロではなく、群れ狩りを初めて有意に増やした**（18.5 → 23.2 \*\*）——**一般則（同じものばかりは損）が、完了条件に書かれていた専用の仕掛け (a)「肉に独自の栄養」の弱い版として働いた**

- [x] 直近の消費履歴で `FoodNutrition` を一時的に割り引く（`engine/diet.go`）。**新しい状態量は作らず、隠さない**
- [x] **他の変更と混ぜていない**
- [x] **予想を先に書いた**（`PLAN.md` の事前予想が、実装中に上乗せした私の予想より正確だった）
- [x] **完了条件**: 小さいことを確認した。**(a) は一般則が弱い版として果たしていた**ので、本番は (b)（段階17）待ち
- [ ] **申し送り**: **強い罰（0.8）は逆効果**（世界が痩せて狩りに出る余裕が消え、群れ狩りの増加が消える）。`CompetitionWeight` と同じ「上げると崩れる」型

### 16. 段階17：植物の遺伝と種子散布
> **17a・17c は 2026-09-04 に実装・測定完了、既定はオフ。** 動かないからではなく**世界を壊すから**（個体数 44.2 → 14.9）。**短い散布は地域を空にし（吸収状態）、長い散布は地理を学べなくする**という逃げ道の無い2つの失敗で、パラメータでは解けない。**段階15 の固定版は置き換えられる下地ではなく、集団が乗っていた土台だった。** 復活の条件は `PARAMETERS.md` C-5i。**17b も 2026-09-04 に実装・測定完了、既定はオフ**（個体数が9分の1）。**`shRationality` が段階9 以来はじめて上がった（0.07 → 0.11 \*\*\*）が、信号を読めなくしても同じだけ上がる**＝上げているのは世界の厳しさで、`PLAN.md` の予想は数字が当たって理由が外れた。**正直な警告は進化しない**（毒にも信号にもコストが無いので足場が無い）

- [ ] 17a 風散布: 植物に軽量な遺伝子（散布半径・再生速度）だけを持たせる。**9本の予算制アーキテクチャは持ち込まない／`Decide()` も作らない**（繁殖は既存の食料湧きと同じ確率イベントに粒子的遺伝を相乗り）
- [ ] 17b 毒と信号: 遺伝子2本（毒の強さ／**警告信号の強さ**＝匂いと目立ちやすさをまとめたもの）。**2本を相関させない**。毒は隠しパラメータ、推定は `(100−理性)/100 × ノイズ`
- [ ] 17b **予想を先に書く**: `shRationality` が上がるはず（理性に食料側の役割が初めて付く）。毒と信号の相関を世代ごとに観察（**ベイツ型擬態が出なくても失敗ではない**）
- [ ] 17c 摂食散布: 既存の `eat` に相乗り（**新しい行動も「付着」状態も作らない**）。**最後に切り出して単独で測る**（採食効率に直接効く）
- [ ] **監視**: 植物の**地域ごとの局所絶滅**を `CensusTracker` / Trough と同じ発想で見る（0 は吸収状態）

### 17. 段階18：夜型・朝型（体内時計） 【完了・2026-09-04】
> 目的: 活動時間帯が個体ごとに違えば、**見張り役を割り当てずに**夜通しの警戒が生まれる。**段階12 より後。段階14・15 とは束ねない**

- [x] **世界に周期的な時間帯を新設した**（2026-09-04、1日＝1000tick）
- [x] 体内時計を持たせた。**予算遺伝子にはしない**（量ではなく円周上の位相なので）
- [x] **時刻で変わるのは休息の回復量だけ**と決めた（視界や食料だと全員に同時に効く世界の変化になり、問いが変わる）
- [x] **完了条件**: 測ったところ**指標は最初から飽和していた**（どの条件でも 0.00〜0.01）。ハッザ族の観察は体内時計の前から成り立っていた
- [x] **実際に動いたのは個体数**（+12.6 \*、これまでで一番良くした段階）。**機序は「眠っている友人は見張れない」という条件1つ**で、`SleepingWatch` の対照で確定

### 18. 地形全般（川・高低差・スロープ・穴・崖）→ **P5 の 20 へ移した**（2026-09-05）
> ゲーム化の一部（管理者が地形を配置する）として扱うほうが筋が通るため、**P5 の 20. 段階20** に節を移し、そこで実装・測定する。**この群の最後**であることと、上の 12（地形の下ごしらえ）が前提であることは変わらない

---

## P5：ゲーム化とマップエディタ（2026-09-05 追加）

> ここまでが「世界のルールを作る」段階だったのに対し、ここからは**その世界を人が触れるようにする**段階。詳細と理由は `PLAN.md` の「ゲーム化とマップエディタ（段階19〜22）」と決定事項#47〜#50

**決めた順序: 19（人間操作）→ 20（地形本体）→ 21（保存/読込）→ 22（エディタ）。理由は4つ。**

1. **19 を先頭にするのは、19 だけが世界の状態の形に依存しないから。** 触るのは `Controller` という境界だけで、20〜22 で世界に何が増えても作り直しにならない。しかも**ゲーム化の一番の不確実性**（`Perception` の情報量と行動語彙で人間が遊べるか）を、地形もエディタも無い**今の平坦な世界のまま**確かめられる。ここで「行動語彙が足りない」と分かれば、それは 20〜22 より前に効く発見になる
2. **20 を 21・22 より前に置くのは、地形の未決が器の形を決めてしまうから。** 高台の多層構造をどう表現するか（平面に高さの軸か、別レイヤーか）は、そのまま「21 が何を保存するか」「22 が何を編集するか」になる。ルールが固まる前に器を作れば、ルールを直すたびに器も作り直しになる（#49。段階17 で「固定版は置き換えられる下地ではなく、集団が乗っていた土台だった」と分かったのと同じ轍）
3. **21 を 22 の前に置くのは、エディタが編集した地図を残すことが、そのまま保存/読込だから。** 逆順にすると**エディタ専用の保存形式**を作ることになり、#48（3つの要件のために別々の機構を作らない）に反する
4. **測定を伴うのは 20 だけ。** 19・21・22 は世界のルールを1つも変えないので `cmd/experiment` の対を取る必要がなく、受け入れ条件は**指紋テストが無変更で通ること**になる。この違いが、20 を単独のコミット・単独の測定サイクルにする理由

**P5 に入る前に P4 の宿題（10b の新しい宿題・段階13 の性能の申し送り・段階17 の測り直し）を閉じる必要は無い。** どれも小さく独立していて、19・21・22 とは触る場所が重ならない。ただし **20（地形本体）の測定に入る前には、既定の `Config` を動かす宿題（ヒントの既定をどうするか）に決着をつけておく**——ベースラインが動くと地形の測定をやり直すことになる。**この注意は 29・30 にもそのまま当てはまる**（測定を伴う段階の前に既定を動かさない）。

### 順序の追記（2026-09-06・段階20 の続き2つが入ったので）

**決めた順序: 29（地形コストの認識と伝承）→ 30（高台の遮蔽と、それを事実として学ぶこと）→ 21（保存/読込）→ 22（エディタ）。理由は4つ。**（**2026-09-06 の追記**: この4つの前に **31・32（P3 の「順序の追記」）** が入りました。**地形は既定オフなので2つの群は交絡せず**、遊ぶための世界を先に良くしたいなら 29 を前に出しても測り直しは要りません）

1. **29・30 を 22 より前に置くのは、#49 の理由がそのまま生きているから。** 段階20 の結論は「**地形は個体数を払って、静かさと混ざりにくさを買う**」で、既定は平坦のまま。この状態でエディタを作ると、**「置くと世界が悪くなるもの」を配置する道具**になる。地形が持てる意味（**選べること**＝29、**高いところに値打ちがあること**＝30）を先に確定させてから器を作る
2. **29 を 30 より先に置くのは3つの理由から。** (a) 29 は**段階20 の測定が名指しした欠けている半分**（選ぶ理由）を、**既存の仕組み（`engine/regionlore.go`）に1本足すだけ**で埋める、**一番安い実験**。安い診断を先に回すのは掃引と同じ習慣。(b) 30 は**設計の未決**（攻撃者との高さの差分＝これまでの「足元だけ見ればいい」から初めて外れる）と、**段階12a の前例**（戦闘に関わる事実を正確に学ばせると個体数が半減しうる）の2つのリスクを抱えている。(c) **読みやすさ**——地形が今の値段（rough −16.8 \*／country −48.2 \*\*\*）を払い続けたままだと、**30 の効果がその代償に埋もれる**。29 で値段が下がってから測るほうが `killShare` と `highGap` の動きを切り分けられる
3. **29b と 30b を同時に測らない**（伝える中身が別の仕組みだから。`PLAN.md` の「地形の続き」の節）。順番に並べれば自然に守られる
4. **割り込ませてよいのは 21 だけ。** 21 は世界のルールを1つも変えず、受け入れ条件も指紋テストの形なので、29・30 のどちらの後に置いても成立する。**むしろ後に回すほど保存対象が固まっていて安い**（29・30 はどちらも engine の状態を増やす）。**22 は割り込ませない**——理由1 のとおり。**遊べるビルドを先に配りたくなったときに前に出せるのは 21、出してはいけないのは 22**

### 19. 段階19：人間操作コントローラと血統乗り換え（devview）【完了・2026-09-05】
> **`Perception` の情報量も行動語彙も足りていた。足りなかったのは「取り消し」**——AIは訊かれれば全候補を採点し直すので古い計画から自力で抜けられるが、**据え置きの命令は自力で抜けられない**。そこで命令が流れる場合を2つ作った（狙いが消えた／身体が変わった）。**engine のルールは1つも変わっていない**（指紋テストが無変更で通る）

- [x] `HumanController`（`engine/human.go`）。engine から見て AI と区別がつかない
- [x] devview: `h` 乗り移り、クリックで狙い、r/m/e/a/f/o/c、`1`〜`5` effort、`s` 構え、Enter 今すぐ考え直させる、`k` 後継の指名。3枚目のパネル（Tab）が操作画面
- [x] **トリガー方式との噛み合わせ**: 命令は「次に訊かれたときの答え」、即時反映は `World.RequestDecision` →既存の `TriggerRequested`。**人間のためのハードコード分岐はゼロ**
- [x] **プレーヤーにも `Perception` の外は見せない**（#47）。狙えるのも視界の中だけ（`Order` が断る）
- [x] 血統乗り換え（`World.Heirs` ＝ 生きていて成人した子。`Agent.IsAdult` は `CanReproduce` と同じ線）
- [x] **完了条件**: (1)(2) を実地で確かめ、(3) 指紋テストは無変更で通る
- [ ] **申し送り**: **`RequestDecision` は撃ち放題**。押した回数だけ考え直せるので、**毎tick 押す操作は 10c で塞いだ穴を人間の手で開け直せる**（`ChoiceNoise` を引き直し続ける個体は何も遂行できない）。遊ぶ段になったら間隔か価格を決める——**AIの側に無い自由をプレーヤーに与えている唯一の場所**
- [ ] **申し送り**: 操作ノードがペアになると `stepPaired` の間は操作できない（既存のルール）。今は画面に何も出ないので、遊ぶ段で「今は結ばれているので手が出せない」と出す

### 19b. 段階19 を遊んで出た宿題（小さい・独立）
> devview で実際に操作して出たもの。engine のルールには触れていない

- [ ] **長い実行は二山になる**（2026-09-05 実測、16シード × 100000tick）。**13回は 131〜193 に増えるが、3回は序盤の谷から戻らない**（5・34・66。出生が 940〜1649 対 60〜205）。**個体数が一桁になると視界130 の世界では配偶者に出会えない＝0 でなくても吸収状態。** 数値は `HISTORY.md` の同日の項。**段階20 以降で 100000tick の比較をするならシードを増やす**（20000〜40000tick は分岐前なので影響を受けにくい）
- [ ] **`RequestDecision` は撃ち放題**（段階19 の申し送りの再掲）。命令キーを押すたびに1回訊くようにしたので、**キーを連打すると 10c で塞いだ穴を人間の手で開け直せる**。今は測定に影響しないが、遊ぶ段では間隔か価格が要る。**段階23 の「訊かれる」モードではこの穴は開かない**（人が押しても訊かれる回数は増えず、答えは1手で使い切られる）ので、残っているのは `-drive`（直接操縦）の側だけ

### 19c. 段階23 の申し送り（小さい・独立）

- [ ] **質問のレートを測ったのは既定条件だけ。** 112 tick に1回は世界が今の姿のときの数字で、**段階20（地形）で移動が難しくなれば `target lost` と `vitality drop` の頻度が動く**。地形を入れたら測り直す
- [ ] **節目の4つ（乗り換え・成人・ペア成立・出産）は決め打ち。** 遊んでみて多すぎ／少なすぎなら足し引きする。engine 側は合計と上限しか守っていないので、`cmd/devview` の `milestone()` を触るだけで済む
- [ ] **性格の4択も決め打ち**（用心深い／気が短い／命を惜しむ／親のまま）。engine はどんな比率でも受けるので、選択肢の設計は遊びながら詰める
- [ ] **人が選んだ性格が世代を越えてどう効くかは測っていない。** 好みは粒子的遺伝で子に渡るので、**プレーヤーの血統だけ好みが偏っていく**はず。血統を追う測定は今は無い

### 20. 段階20：地形本体（川・高台・スロープ・積み上げ可能な高低差）【完了・2026-09-06】
> **P4 の「18. 地形全般」をここへ移した**（同じもの）。**この群で唯一、選択圧と個体数が動く段階**なので、単独のコミット・単独の測定サイクルにする。上の「12. 地形の下ごしらえ」は済んでいる

- [x] **エディタより先にルールを入れて測る**（#49）。決め打ちマップ4種（`rough` / `river` / `plateau` / `country`）で `cmd/experiment` と `cmd/devview` の両方を回した
- [x] **1種類ずつ測った**（荒れ地・川・高台をそれぞれ単独の腕にし、倍率も1.5／2／4 で掃引）。**障害物・隘路・遮蔽は入れていない**（下の残りへ）
- [x] **決めた2つ**: 高さは**平面の上の軸**（別レイヤーにしない）／知覚は**足元だけ**で **AIと人で分けない**
- [x] **予想を先に書いて、外した**。`speedGap` は倍率1.5・2・4 のどれでも 0.00（ニッチは出ない）。**個体は荒れ地を避けない**（`onDear` は地図に占める割合そのまま）——**足元しか分からないので避けようがない**。**速さは逆に買われなくなる**（倍率4で `shSpeed` −0.04 \*\*\*）。**並び替えが出たのは高さのほうだけ**（`highGap` +0.02 \*\*）
- [x] **監視**: 絶滅は0。ただし**個体数の値段は大きい**（rough −16.8 \*／plateau −37.9 \*\*\*／country −48.2 \*\*\*）ので**既定は平坦のまま**。一方 `killShare` −0.06〜−0.20 \*\*\* と `halfLife` +27〜+32 \*\*（**地形が初めて「混ざりにくさ」を作った**）
- [x] 触った文書: `NODE.md`（11-5）、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`
- [ ] **残り（この段階から出た宿題）**: **地面を選べるようにする**（通った地面を覚える／食料を地形に結びつける。前者を先に測る）、**障害物・隘路・遮蔽**（障害物は経路の仕組みとセット、遮蔽は視界とセット）

### 20b. 段階29：地形コストの認識と伝承（段階15b/15c の再利用）【完了・2026-09-06】
> **結果**: 避けるようにはなった（`onDear` 0.48 → 0.42、−0.05 \*\*、`clumping` +0.11 \*）が、**個体数は戻らず `speedGap` も 0.00 のまま**。**重みは頭打ち**（2 で飽和、4 は個体数だけ −11.5 \*）。**29a 単独が一番悪い**（−13.3 \*）＝**未知を「普通」と見なす既定値が、学ぶことを損にしていた**。**次の一手は食料を地形に結びつけること**（下の新しい宿題）
> 目的: 段階20 の「**個体が地面を選べないので住み分けは起きない**」を埋める。**前例は 14 →15b →15c**——**良い場所があるだけでは凝集せず、自分の経験だけでも凝集せず、伝承が入って初めて「行ったことのない場所へ向かう」が選択肢になった**。詳細は `PLAN.md` の段階29 と決定事項#53

- [ ] **29a**: 地域ごとのベクトルに**「その地域の平均移動コスト倍率」を1本足すだけ**。荒れ地・水・高さを別々の信念にしない
- [ ] **減衰は無し（減衰率0で同じ関数を使い回す）**。地形は静的な事実だが、将来動的になったとき（浸食・エディタ）にすぐ有効化できる形にする
- [ ] **粒度は既存の12ブロック**（`TerrainMap` の解像度に合わせない）。**今のコントローラは経路計画を持たない**ので、粗い信念が使いこなせる天井
- [ ] **記憶容量の枠には入れない**（#41 を踏襲）
- [ ] **29b**: 伝承は `exchangeLore` に相乗り（15c と同じ）。**伝承だけ切った対照と対で測る**
- [ ] **予想を先に書く**: 個体数は段階20 の代償の一部を取り戻す／`clumping` 上昇・`nearest` 低下／**`speedGap` が初めて動くか**。**そして `killShare` を必ず一緒に見る**——良い場所に集まりすぎると競合が増えて逆に上がりうる（「片方だけ良くなっていないか」）
- [ ] **監視**: 荒れた地域を完全に空にしていないか（吸収状態）。個体数と地域ごとの分布
- [x] 触った文書: `NODE.md`（8-1 の続き）、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`
- [ ] **新しい宿題（この段階から出た）**: **食料の分布を地形に結びつける**。**避ける理由と食べる理由が別々の地図に乗っているかぎり、重みでは頭打ちを越えられない**（避けることは常に食料を捨てること）。**交絡するので測り方を先に決める**——地形あり・食料一様／地形あり・食料が地形に従う、の対で測る
- [ ] **新しい宿題（小さい）**: **完了条件に「測る前から答えが分かる指標」を置かない**。`regionCostRank` は 1.00 で飽和した（地面は静的、足元は正確）。**信念が正しいことと、役に立つことは別**

### 20c. 段階33：食料の分布を地形に結びつける（段階29 の頭打ちの原因を切り分ける）【完了・2026-09-06】
> **結果**: **頭打ちは交絡だった**（`onDear` 0.42 → 0.32 \*\*\*、個体数 +8.0 \*、餓死率 −0.20 \*、`killShare` 不動）。**それでも `speedGap` は 0.00**＝**「地形は速い個体と遅い個体を選り分けない」はこの世界の事実**。**食料だけで半分・信念だけで半分・両方で −0.10**。**弱い相関は中間ではなく一番悪い**。**既定は0**（風景の性質なので地図の作者が決める。devview の `-terrain` は1を渡す）
> 目的: **段階29 の「避けるようにはなるが半分にはならない」が、交絡のせいか本当の限界かを決める。** 新しい能力を足す段階ではなく、**開いたままの診断を閉じる段階**。詳細は `PLAN.md` の段階33 と決定事項#57

- [ ] **世界の真値どうしを相関させるだけ**（`Config` に符号つき1つ）。**豊かさの信念とコストの信念はすでに別々にあるので、個体は既存の効用比較を通して自分で見つける**。**相関を覚える・伝える第三の仕組みは作らない**
- [ ] **総量は変えず分布だけ**（段階15a の原則）。相関のあとで再正規化し、**テストで固定**する
- [ ] **粒度は段階29 の地域単位のコスト**（`regionMeanCost`）にそのまま乗せる
- [ ] **平坦な世界は1ビットも変わらないこと**（地図が無ければ乖離ゼロ）。指紋テストが証拠
- [ ] **正の相関（荒れた地域ほど痩せている）だけを先に測る。** 負の相関は**頭打ちを意図的に強めるもの**なので混ぜない（正で解けることを確認したあとの独立条件）
- [ ] **予想を先に書く**: `onDear` が 0.42 の頭打ちを超えて下がる／個体数の落ち込みが緩和される／`clumping` 上昇／**`killShare` は上がりうる**（良い地域が豊かさと安全さを兼ねるので競合が集中）／**`speedGap` が動けば「住み分けはコストではなく、コストと食料が同じ地図に乗っていることから生まれる」と言える**
- [ ] **監視**: 「荒れていて痩せた地域」が完全に空にならないか（吸収状態）
- [x] 触った文書: `NODE.md`（8-1 の続き）、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`
- [ ] **残した宿題**: **負の相関（荒れた地域ほど豊か）**を独立条件として測る（狙いが違う。段階30 と同じ群）／**`speedGap` が動かないと確定したので、「速さと体力の並存」（段階7c の完了条件）を地形に期待するのはやめ、別の経路を立てるか条件を取り下げるかを決める**

### 20d. 段階30：高台の遮蔽（回避率）と、それを世界の事実として学ぶこと【30a 完了・30b は行き止まり／2026-09-06】
> **30a**: 規則は効く（`evadedShare` 0.04 → 0.06 \*\*\*、世代 +0.71 \*、餓死率 −0.39 \*）が、**`onHigh` は動かない**＝**誰も高台を目指さない**。**倍率版は何も動かさなかった**——回避は構えの1チャネルで戦闘中しか持たないので、**倍率はゼロに掛けていた**（発火頻度は「状況」ではなく「掛ける量が非ゼロの頻度」を数える）。**30b は先に決めた2つ（#53 の1スカラー／段階20 の足元だけ）のどちらかを取り消さないと動かない**ので、判断ごと宿題に残した
> 目的: 対応表の「高台＝到達経路の制限＋**遮蔽**」の後半。段階20 は移動コストと到達可否の2つに絞った回なので、これはその続き。詳細は `PLAN.md` の段階30 と決定事項#54

- [x] **30a（完了）**: 遮蔽そのもの。**床として実装**（倍率では効かない）。**世界は少し良くなるが居場所は変わらない**
- [ ] **30b（行き止まり・判断が要る）**: **(a) 地域の信念に高さを2本目として足す（#53 を取り消す）か、(b) 近くの高さを知覚に載せる（段階20 の決定を取り消す）か。** どちらを取り消すかを決めるのが先で、**30a が「居場所は変わらない」を出したことがその判断材料**
- [ ] 旧: **30a（先）**: 遮蔽そのもの。**攻撃者と自分の高さの相対関係で決まる**——**これまでの地形効果がどれも「足元だけ」だったのに対し、初めて「相手との差分」を見る**ので着手時に設計する
- [ ] **戦闘中の行動は、これだけで出るはず**（既存の閉じた式にそのまま乗るので、新しい知覚も学習も要らない）
- [ ] **まず高低差のある戦闘が何回起きるかを数える**（段階24 の教訓＝規則の効き目の上限は発火頻度で決まる）
- [ ] **30b（後・別条件）**: 「高い場所は安全だ」を `engine/lore.go` 側の**世界の事実**として学習・伝承させる。**`engine/regionlore.go`（場所ごとの値）とは別の枠**（#54）
- [ ] **既定オンだと決めつけない。** 段階12a の `retaliation`（**間違った恐れが世界を支えていた**）に近い性質の事実。**30a を単体で測ってから 30b を足し、個体数・`killShare`・`highGap` の動きを見てから既定を決める**
- [ ] 触る文書: `NODE.md`、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`

### 21. 段階21：世界・ノードの保存/読込（汎用シリアライズ基盤）
> 目的: 「他所で育ったパラメータの読込」「進めた世界の配布」「天才の配置」を**同じ1つの機能**として実装する（#48）。`protocol` そのものではなく、**その前段の実地検証**

- [ ] `World` / `Agent` / `Genome` / 記憶 / 好み・言い伝え / ヒント / 地域 / 植物 / 地形 / `tick` の出し入れ
- [ ] **乱数の状態を保存するかを決める**（決定性の根拠なので、保存しないならその旨を明示する）
- [ ] **受け入れ条件は指紋テストと同じ形**: 保存 → 読込 → N tick が、保存せず N tick と**1ビットも違わない**こと（＝保存漏れが無いことの証拠）
- [ ] **ノードだけを読み込む経路**と、**どこまで持ち込むか**の線引き（記憶と血統は相手IDを含むので世界をまたげない）
- [ ] 天才の管理者配置は、**段階22 のUIが揃ってから**足す軽い機能。単独では着手しない
- [ ] 触る文書: `README.md`、`HISTORY.md`、`CLAUDE.md`（アーキテクチャ／未決事項）

### 22. 段階22：マップエディタv0 とパラメータ調整UI
> 目的: 地図を設計する体験の最小版。編集対象（地域の係数）は段階14・15 ですでにあり、地形は段階20 で入っている

- [ ] 地域（4×3）の係数と、段階20 の地形を地図上で編集する
- [ ] 難易度＝予算（`GeneBudgetMean` / `GeneBudgetStd`）。**測定用の既定値とゲーム用のプリセットを分ける**（#50）
- [ ] **全パラメータを機械的にUI化しない**（#50）。取り扱い注意の一群に絞り、**安全域を外れる操作には警告**
- [ ] **tick と描画の分離**を既存の速度切替の延長として実装する（新しいアーキテクチャは持ち込まない。ここでの形が `server` の配信間引きの原型）
- [ ] 保存・読込は**段階21 の仕組みをそのまま使う**（エディタ専用の形式を作らない）
- [ ] 触る文書: `README.md`、`HISTORY.md`

---

## 実装前に決めること（担当段階ごと）

`PLAN.md` の「未決」を、決着をつける段階に割り振ったものです。

**段階7c までに決める（2026-09-02 に全部決めた）**
- [x] `BudgetInheritSpread` の値 — **30 のまま**。8倍に振ってもドリフトは動かない（上昇は拡散ではなく選択）。60 は個体数 −15 \*
- [x] 初期配分のディリクレ分布の α — **0.8 のまま**。**α は初期条件ではなく、その実行の多様性そのもの**（2.0 で末尾の能力の分散が3分の2、`killShare` +0.07 \*\*\*）
- [x] 天才の発生率を「出生あたりの確率」で持つか「世界側の間隔」で持つか — **出生あたりのまま**。ただし間隔は個体数で動く（20000tickで33年、60000tickで11年）
- [x] 天才2段階の発生率と、予算まわりの数値 — **0.017 / 0.0017 のまま。値の確定は段階7d（`TicksPerYear` と対）へ移送**。正しく集計すると 11.1年／144年 で狙いどおり
- [x] `fitness` を魅力遺伝子に置き換えたあと、体調（体力）をどれだけ残すか — **0.35 のまま**、`Config.FitnessConditionWeight` へ移した。**0.7 のほうが個体数は2割多いが、性選択の軸が消えるので採らない**（値段を承知で払う）
- [x] 予算のドリフトに価格を戻すか — **`BudgetUpkeep = 1` のまま**。`noupkeep` は `dBudget` +30 \*\*\*・`deathRate` +0.84 \*\*\*

**段階7d までに決める（2026-09-02 に全部決めた）**
- [x] `TicksPerYear` = **500**（本決め）。3条件を満たす: 大天才100年＝50000tick が実行長と同程度／子ども期2年＋世代2100tick で複数世代／衰えは発火するが支配的でない
- [x] 天才2段階の発生率（段階7c から移送）— **0.017 / 0.0017 のまま確定**。`TicksPerYear` が動かなかったので再較正は不要
- [x] 育児期間の長さ＝**1000tick（2年）**、親側の負担＝**入れない**
- [x] 寿命消費の既定値 — `MaxLifespan` を **6000 → 3000**（6000では一度も発火せず、1500では世界が壊れる。3000で死因の4%）。rate は 0.2 のまま

**段階9 までに決める（2026-09-02 に全部決めた）**
- [x] 記憶の「単位時間に記憶できる量」と「忘却曲線」の扱い — **案どおり**。容量だけ予算内、帯域は容量 × `MemoryBandwidthShare`（0.5）、忘却は `(100 − 記憶)/49.5` 倍
- [x] 血縁の初期好意ブーストの大きさ — `AffinityKin = 22`（ペア成立・出産の 18 より少し大きい）
- [x] 「接触」（#22 の起点リセット）の定義 — **視界内の遭遇でよい**。帯域を消費させた（させないと「起点の更新だけは無限にできる」ため）

**段階11 までに決める（2026-09-03 に決めた）**
- [x] 外敵のサイズ分布 — **既定（520 ± 90）で確定**。400／700／標準偏差180 のどれでも参加人数は 1.22〜1.25 で不動、動くのは人間の個体数だけ

**段階12 までに決める**
- [ ] 等価交換の歩み寄り率と、動いた量を好意に換算する係数
- [ ] ヒント枠の上限本数（案 3〜5本）と、枠1つあたりの予算消費量
- [ ] ヒントの状況特徴量として何を許すか（`Perception` から取れるものに限る、までは確定）
- [ ] 深い伝承に相互占有を追加するか（**被参照数の分布を実測してから判断**）

**段階14 までに決める**
- [ ] 休憩場所の割引率の幅（`RestExposureWeight` に掛ける係数の下限・上限）。**露出は `RegenRate` の12倍の単位で入る**ので、強くしすぎると「安全な地域では休息が常に最善」、弱すぎると何も変わらない
- [ ] 地域ブロックの粗さ（グリッドを何セル束ねるか）。**大きく取りすぎると局所適応の前に地域が絶滅する**。出発点は 2〜3 × 2〜3

**段階15 までに決める**
- [ ] 地域ごとの `FoodSpawnRate` の差の付け方（倍率の幅／**総量は保存する**）
- [ ] 地域の豊かさのベクトルの持ち方（スカラー1つか、強さ推定と同じベイズ更新か）と、**記憶容量遺伝子が精度・更新速度にどう効くか**の式

**段階16 までに決める**
- [ ] 割引の強さと履歴の長さ（直近何回ぶんを見るか、割引の下限）。**値を詰めるのは食料源が複数になってから**

**段階17 までに決める**
- [ ] 毒のコストの置き場所（体力への直接損害か、その場の栄養価の低下か）。**前者は死因が1つ増える**
- [ ] 摂食散布の確率と遅延（種が生き残る確率／何tick後にどこへ生えるか）

**段階18 までに決める**
- [ ] 時間帯の周期と、時間帯によって何が変わるか（**着手時に詳細化する。先に決めない**）

**段階19 までに決める（2026-09-05 追加。同日すべて決めた）**
- [x] 人間の入力粒度と、トリガー方式との噛み合わせ — **「次に訊かれたときの答え」を置く形**に決めた。即時反映は別操作（Enter）で、**既存のトリガー（`requested`）をそのまま通す**。入力のたびに自動で再判断を要求する形は採らない（10c で塞いだ「毎tick 考え直す」を人間の手で開け直すことになるため）
- [x] 子への転移の規則 — **指名はいつでもでき、押すたびに成人した子を巡回する**（`k`）。**後継未指定で死んだら血統は終わり**。選び方は engine に持たせない（「一番強い子」のような基準は世界のルールではなくプレーヤーの判断）
- [x] **命令が流れる場合**（実装中に出た。事前には挙がっていなかった項目）— **狙いが消えたときと、身体が変わったとき**の2つだけ。それ以外に engine が人間の命令を上書きすることはない

**段階20 までに決める（2026-09-05 追加）**
- [ ] 高台の多層構造の表現（`(x, y)` に高さの軸を足すか、地域のような別レイヤーか）
- [ ] `Perception` に地形を載せるか。**AI と人間で知覚の中身を分けるかも含めて**（分けるなら #47「人間にも `Perception` の外は見せない」との整合を先に取る）

**段階21 までに決める（2026-09-05 追加）**
- [ ] 保存形式（gob / json）と、**乱数の状態を保存するか**（決定的再現の根拠）
- [ ] 「育ったパラメータ」としてどこまで持ち込むか（記憶と血統は相手IDを含むので世界をまたげない）

**段階22 までに決める（2026-09-05 追加）**
- [ ] UI に出すパラメータの範囲と、安全域の警告をどこに持つか（コードか `PARAMETERS.md` か）

**段階11 の完了時に書く（2026-09-01 に記入済み）**
- [x] 「あるべき状態」の目標数値（集団数・戦闘率比・持続時間・種族ごとの個体数の帯）— `PLAN.md` 冒頭の表。**A は達成**（帯＝希少種が5〜20%・谷0.3以上）、**B〜E は目標だけ書いて判定は段階12 のあと**（`fightRatio` 0.6 以下・`halfLife` 150tick 以上・`gapRel` 1.2 以上）

---

## 保留（条件つきで復活・着手しない）

- ~~明示的な「協力」（`ActCooperate` ＋ 協力性遺伝子）~~ — **2026-09-06 に保留から出した（上の 8c ＝ 段階32）。** 条件は 2026-09-01 に満たされ（群れ狩りは現れなかった）、残っていた前提の段階12（伝承）も 2026-09-04 に通った。**ただし #10 が却下したものをそのまま作るのではない**——協力性遺伝子は作らず、**行動語彙1つと好意の使い道1つ**だけ足す
- 明示的な「集団」（集団ID・所属行動・敵対フラグ）— 段階12 まで進めて**あるべき状態 B〜E** が満たせない場合にだけ再検討
- `protocol` / `server` / `client` — engine 側をどこまで進めてから着手するかは未定
- 配偶者を巡る同性間の戦い（地形は保留から外し、P4 の最後＝上の 18 に置いた）
- 激しい活動（求愛・戦闘）による寿命消費 — 今回は実装しない（食事量だけを見る簡易近似で代替）
- 独立した睡眠機構（疲労度という状態量・強制的な `ActSleep`）— **実装しない**。狙っていた緊張は `RestExposureWeight` ですでに出ている（休まない世界では消耗死 +45%）。**測定でこの緊張が弱すぎると分かった場合にだけ**復活
- 水辺に独自の豊かさを与えること（河畔植生のような個性）— **コストに応じて食料量が決まる一般式（段階33）と両立しない特別扱い**になるので、**まず一般式だけで様子を見る**（2026-09-06）
- 動物付着散布（毛に引っ付く種）と水散布 — 前者は「付着」という新しい状態が要るので採らない。後者は**川・水場が入ってから**
- 肉に「植物では取れない栄養」という独自の価値をつけること — 段階16 の完了条件に条件つきで置いた（段階11 で群れ狩りが現れなかった一因を補う可能性がある）
