# TODO（優先順位つきタスク一覧）

**`PLAN.md` を作業単位に落として優先順位をつけたものです。** ルールの中身・理由・完了条件の詳細は `PLAN.md` の該当節にあります。ここには**何を・どの順で・なぜその順か**だけを書きます（同じことを2箇所に書かないため）。

- **今どこまで終わったかは、すぐ下の「現在地」を見る**（見出しの【完了・日付】がその段階の答え）
- 進め方: 上から順に。1タスク＝1コミット程度の粒度を目安にする
- 各段階の詳細な受け入れ条件・触る文書は `PLAN.md` の同名の節を見る
- 「なぜそう決めたか」は `HISTORY.md` の設計判断ログ

## 現在地（2026-09-12 更新・P10 と P11（56〜66）を全部通した）

**この節が「どこまで終わったか」の唯一の答えです。** 各段階の中のチェックボックスは作業中のメモなので、**完了かどうかは見出しの【完了・日付】とこの表で判断してください**（完了した段階の中に、あとから足した宿題として未チェックの行が残っていることがあります）。

**終わったもの（段階番号順）**

| 段階 | 何を | いつ |
| --- | --- | --- |
| 0〜6 | 差し込み口・状態モデル・戦闘・記憶・リスク判断・戦略選択 | 2026-08-12 |
| 6.5 | 計測の先行整備（集団化の5指標） | 2026-09-01 |
| 7a・7b・8 | 空間グリッド／粒子的遺伝／攻撃・防御・回避のチャネル化 | 2026-09-01 |
| 7c・7d | 遺伝子ベクトルと予算制／成長・老い・寿命・育児 | 2026-09-01〜02 |
| 9 | 記憶の容量・帯域・忘却曲線と好意 | 2026-09-02 |
| 10・11 | 見た目からの学習／種族の汎用化と外敵 | 2026-09-03 |
| 12a・12b・12c | 個体ごとの仮定／伝承（等価交換）／ヒント | 2026-09-04 |
| 13〜18 | 視界のグリッド化・休憩場所・地域差と伝承・栄養バランス・植物の遺伝（既定オフ）・体内時計 | 2026-09-04 |
| 19・23〜28 | 人間操作と血統乗り換え／訊かれて答える遊び方と、遊んで出た6つの手直し | 2026-09-05 |
| 20・29・33・30a | 地形本体／地形コストの認識と伝承／食料を地形に結びつける／高台の遮蔽 | 2026-09-06 |
| 21・22 | 世界とノードの保存/読込／マップエディタv0 とパラメータ調整UI | 2026-09-06 |
| **34・35・36** | **溺死／溺死の周知と、場所の危険という2本目の信念／水辺の豊かさ** | **2026-09-07** |
| 37〜51 | 両岸の測定／スキル／物を持つ世界（肉・運搬・余剰・魚・特産・石・遠隔）／交換（バーター・発信・倉庫・通貨） | 2026-09-08〜10 |
| **52** | **調理——段階50・51 の壁を、勾配の読み方を変えずに迂回した（この世界の1規則として最大の +21.10 \*\*\*）** | **2026-09-11** |
| **54** | **気分——非合理な偏りは、強さと構造の両方がそろって初めて効く（既定はオフ）** | **2026-09-11** |
| **53・53a・53b** | **母がガーディアン／どちらの親も養える／性差の振り分け（役割どおりが役割を逆にしたものより悪い。既定はオフ）** | **2026-09-11** |
| **55a** | **見失った方角——探しに行くことが集団を壊す（既定はオフ。55b・55c は作らない）** | **2026-09-11** |
| **56** | **あるべき状態を土台とマップの性格に分けた（判定する世界の列・G・H・「マップ共通の土台」。コードは無変更）** | **2026-09-12** |
| **57** | **`RegionBias`——非可搬にしても居着かない。理由は約分（近所の全員に同じだけ効く）。既定はオフ、57b は作らない** | **2026-09-12** |
| **57c** | **地面が建て方を選ぶ版——約分は止まったが、今度は一生のうちに平均される（取れたのは並び替えの余地の2%、しかも全部が出生地の運）。既定はオフ** | **2026-09-12** |
| **58** | **外敵の湧きを地域に紐付けた——湧きは狙いどおり偏り、一生のうちに散る（残るのは6%）。既定はオフ。段階64 が前提だと分かった** | **2026-09-12** |
| **59** | **外敵タイプのプリセット表（`Species` は増やさない）——表は効き、混ぜ方が世界の選択を変える。種類は種ではなく交配する。既定は1種類** | **2026-09-12** |
| **64** | **外敵の行動範囲——離れることに値段をつけたら、段階58 の湧きが初めて残った（6%→45%）。危険地帯が噛む。既定はオフ** | **2026-09-12** |
| **61** | **地図が総量を決められるようにした——ただし再正規化は錠前ではなく、効くのは総量のほう。測定では動かず、古い旋盤と同じ** | **2026-09-12** |
| **60** | **外敵が人間の食料を食べられるようにした——外敵が4倍（餌が数を決めていた）、人間は −6.17 \*。`pop` だけ読むと逆に見える** | **2026-09-12** |
| **62** | **外敵タイプごとの対抗スキル——仕組みは動くが届くのは人間の死の2%。#94 の「命に関わる的なら到達率が大きい」は外れ** | **2026-09-12** |
| **63** | **水中の外敵——川に住んで狩るものを置いても人は川から出ない（4度目）。P10 はこれで完了** | **2026-09-12** |

**残っているもの（この順。理由は各群の「順序の追記」）**

| 順 | 段階 | 状態 | 節 |
| --- | --- | --- | --- |
| — | **P8（交換）は 2026-09-10 に全部通した**（48・49・50・51）。**段階51 までは全部完了** | 完了 | P8 |
| — | **段階52（調理）は完了**。**52c は「作らない」で決着**（手の中のものは誰にも取れないので独占が起こりえない）。**残りは 52d（条件つき）と 52e（場所に紐づける拡張・後回し）** | 完了 | P9 |
| — | **段階54（気分）は完了・既定オフ**。**上げる根拠は測定の側にある**（+7.77 \*、ただし 1.5 SE） | 完了 | P9 |
| — | **段階53（性差）は完了・既定オフ**。**役割どおりの配分が役割を逆にした配分より悪い**（世界が払う遺伝子と役割が要求する遺伝子が別）。**`shMemory` は6度目に動いたが、代価は個体数の3分の1** | 完了 | P9 |
| — | **段階55a（見失った方角）は完了・既定オフ**。**探しに行くことが集団を壊す**（発火は十分、`halfLife` はどの用量でも低下）。**55b・55c は「作らない」で決着** | 完了 | P9 |
| — | **段階56（あるべき状態の再構成）は完了**。**コードは無変更**で、`PLAN.md` の表に「判定する世界」列と G・H、`CLAUDE.md` に「マップ共通の土台」 | 完了 | P10 |
| — | **P10（マップの性格・段階56〜64）は 2026-09-12 に全部通した**。**狙いどおり動いたのは段階64 だけ**で、残りが繰り返し言ったのは**「場所に結びつけたものは身体が動くことで消える」**と**「居場所を動かすのは食料の分布だけ」**の2つ | 完了 | P10 |
| — | **段階65（交配時の配偶者間伝承）は完了・既定オフ**。**狙った量は動き、世界は動かない**（どちらも実装前に数えた1% のとおり）。**ペアは今も自分からは伝え合わない** | 完了 | P11 |
| — | **P11（伝わる機会・段階65〜66）は 2026-09-12 に全部通した**。**2つとも「狙った量は動き、世界は動かないか代価を払う」**で、**測って初めて分かったことのほうが大きい**（65＝ペアは1回も伝え合っていなかった、66＝効いているのは遅さであって子のそばで遅いことではない） | 完了 | P11 |
| — | **段階57（`RegionBias`）・57c は完了・どちらも既定オフ**。**2つの形で2つの別の壁**（約分／一生のうちに平均される）。**57b は作らない**。**居場所を動かすのは食料の分布だけ、の4度目の確認** | 完了 | P10 |
| — | **段階52d：生では割に合わない食料**（**52 の効果が弱すぎたときだけ**。52 と同じ回に混ぜない＝#80） | 条件つき | P9 |
| — | **魚を生き物にする（攻撃力の小さい第3の種族）。段階42・43 の本来の意図で、今の実装は食料アイテム**（#78） | 保留 | P8 のあと |
| — | 38b：残り（追跡・介抱）。**採取・泳ぎ・毒耐性は完了、肉の保存は 40 の運搬が兼ねるので作らない**（#68） | 保留 | 38b |
| — | 行動の拘束時間（独立・いつでもよい。ただし単体で測る） | 未着手 | P7 |

**2026-09-12 に P11（伝わる機会・段階65〜66）を足しました。** **出産のあと、親子と配偶者のあいだで言い伝えはどう伝わるか**という相談で、**相談された4点のうち1つは「何もしない」で決着しています**（#97＝父からの伝承）——**`exchangeLore` は段階12b からずっと関係を読まず**、**父子が伝承の射程に居合わせる機会が少ないのは段階53a で `GuardianID` を母に固定した副作用として構造的にそうなっている**だけなので、**増やす仕組みも塞ぐ変更も、その線を壊します**。**残る2つはどちらも新しい交換の仕組みを作りません**——**65 は既存の `exchangeLore` を交配の瞬間に1回呼ぶだけ**（**ペアが手を組んでいるあいだ言い伝えが1回も交換されていない**ことを先に確認済み）、**66 は母の実効速度を落として共在を増やすだけ**（**強制近接は作らない**＝好意の高い相手の観察はすでに正の効用項なので、**子が自分の判断で留まりを選ぶはず**）。詳細は下の P11 の節。

**2026-09-12 に P10（マップの性格・段階56〜64）を足しました。** **Cowork 側の設計相談（「あるべき状態を全マップに求めるのは厳しすぎないか」「マップの各地点の集団と外敵の性質をマップが決められるようにしたい」「外敵をもっと多様にしたいが行動範囲は緩やかに縛りたい」）を段階に落としたものです。** **出発点は「地図によって符号が変わる」が実測で5例たまったこと**——運搬 +1.25（誤差内）対 −8.90 \*、贈与 +9.35 \* 対 −11.27 \*\*、魚 −16.7 \*\*\* 対 −5.3、特産と石は地図の作者が入れるもの、調理は**両方の地図が同符号になった初めての例**としてわざわざ書いてある。**それなのに受け入れ基準（あるべき状態 A〜F）は、どの世界で判定するものなのかを一度も書いていません**（F だけは 2026-09-11 に「`coins` の腕で測る」と書いた）。**段階56 はそこだけを直す、コードを1行も変えない段階**で、他の8つとは独立なので**最初に通しました（2026-09-12）**——**基準は1つも変えず、列1本と行2本と `CLAUDE.md` の節1つだけ**です。**書いてみて分かったことが2つあり**（**保存則は守られていたのにどこにも明文化されていなかった**／**「閾値を作らない」がマップごとに難易度を振ってよいことの根拠そのものだった**）、**後者は段階61 が保存則を初めて緩める前に書いておくべきものでした**。**残りの8つが増やすのは世界を建てる側の自由度であって規則の数ではなく**、**どれも既定オフで乱数を1つも余分に引かないこと（＝指紋テスト無変更）を最初の条件**にしています。詳細は下の P10 の節。

**2026-09-11 に、あるべき状態へ F（通貨が流通し、売買が適度に行われている）を足しました。** **A〜E と違い、既定の世界では測れません**（コインは地図の作者が撒くもの＝#73 なので、測るのは `coins` の腕）。**今の実測は4つの目標すべてに未達**——**断り 45.60 対 売買 5.12（8.9倍）**／**コインは60枚中 13.8 しか手にない（0.23）**／`saleRate` 0.58／餓死率。**一番遠いのは断りの多さ**で、**足りないのは買い手ではなく売り手**という段階51 の構造が、**調理（段階52）でむしろ強まっています**（断りは段階51 時点の 6.17 から 45.60 へ＝**治る食事を持っている身体はそれを手放さない**）。詳細は `PLAN.md` の「あるべき状態」。

**2026-09-11 に段階54（気分）を完了しました。** **非合理な偏りは、強さと構造の両方がそろって初めて効きます。** **最初の測定は全部 null で、符号を逆にしても同じでした**——そこで傾きの実際の大きさを見たら **0.5%**（恐れの平均 0.01 × 重み 0.5）で、**評価ノイズが 20 ある世界で好みを0.5% 動かすのは測定ではありません**（段階24 の失敗を繰り返しかけました）。**十分な用量では3つの腕が正しい順に分かれます**: **気分あり +7.77 \***／**同じ大きさで符号を逆 −0.98**（直接の対で **+8.75 \***）／**同じ平均の傾きを定数として与えると −2.35 ± 6.05 ＝何も起きない**。**決め手は3つ目の対照**——**効いているのは平均の大胆さではなく、直前に何があったかに連動していること**。**実装前に数えた半減期の効き方は当たりました**（2/3 の時間立っている気分は定数＝600 に伸ばすと消える）が、**外したのは2つの入力の大きさ**——**満ち足りは恐れの6倍**（食事は頻繁で大きく、打撃はまれで小さい）なので利得を分けました。**既定はオフ**（1.5〜2.0 SE、用量も手で置いたもの）。詳細は 54 の節。

**2026-09-11 に段階52（調理）を完了しました。** **この世界の1つの規則としては最大の効き目です**（個体数 **+21.10 \*\*\***＝77.4 → 98.5。段階18 の +12.6、段階32 の +12.8、段階48 の +9.35 を超える）。**答えは実装前に数えた的に出ていました**——**何か持っている身体のうち「取っておく値打ちが正」は 0.037（段階50 の問い）なのに、「調理した品が生より良い」は 0.235**（＝**6倍の的で、何か持っている身体の92%**）。**治りは空腹度ではなく「今どれだけ体力が欠けているか」から読まれる**ので、**満腹で傷ついた身体＝余剰を持っている身体に届きます**。**独占は起きず、前提のほうが崩れました**——#81 が用意していた claim は、**この世界では調理が手の中で起き、持ち物を奪う行動が無いので要りませんでした**。**代わりに数えた「調理した品がどこへ行くか」は独占の正反対**（1回の調理につき人手に渡るのが既定 0.55・遊ぶ地図 0.97 ＝**この世界で一番よく人手に渡るもの**）。**交換の半分は本物**でした（渡す理由を消した対で +21.10 \*\*\* 対 +12.46 \*\* ＝5分の2）。**分業は出ません**（`cookSplit` 0.01 ± 0.02＝6度目）。**そして食料に触る規則で両方の地図が同符号になった初めての例**です（遊ぶ地図 +13.06 \*\*）——**調理は食料を増やしも減らしもせず、同じ1個の価値を変えるだけ**だから。詳細は 52 の節。

**2026-09-11 に段階52〜55 を立てました（P9）。** **段階50 と段階51 が独立に同じ壁に当たったことが出発点です**——**取っておく価値もコインの値打ちも効用式が「今の死亡リスクの勾配」から読み、その勾配は満腹の身体では平ら**で、**余剰を持っているのは満腹の身体**（倉庫で `ActStore` が312回採点されて勝つのは3回、通貨で断りが売買の2倍）。**この世界のノードは「今」しか見ていない**、というのが診断です（#79）。**ただし根っこ（効用式の計画の形＝先読み）そのものには触りません**——段階4・段階15 と同格の下部構造の入れ替えで、**記録済みの測定値が全部引き直しになる**からです。代わりに**壁を迂回する当たり方を4つ**用意しました。**先頭の52（調理）だけが、この壁を構造的に避けられます**——**回復の値打ちは空腹度ではなく「今どれだけ体力が欠けているか」から読まれる**（段階39 の `recoverable` の頭打ち）ので、**満腹でも傷ついている身体には正の値打ちがあり**、**余剰を持つ身体と値打ちが正の身体が初めて重なりうる**。**しかもこれは55（孤立）の必要性そのものを検証する前提**でもあります（**調理が本物の依存関係を作って集団化が起きるなら、孤立ペナルティという人工的な機構は要らない**）。**53（性差）だけは配分の方向が未決なので、決まるまで着手しません。** 詳細は `PLAN.md` の「今しか見ていないこと（段階52〜55）」と、下の P9 の節。

**2026-09-10 に段階51（通貨）を完了し、`PLAN.md` の全段階を通しました。** **実装前に数えたら答えが半分出ていました**——#73 の式（値打ち＝割引された期待食料価値）のままだと、**コインは食事への請求権なので、同じ身体にとって現物より高くなることはありません**（55,000 看視で **0.0000**）。**それでも成立するのは2つの理由**: **コインは持ち主の尺度で測られる**（空腹な買い手と満腹な売り手で値打ちが違う＝比較優位）ことと、**コインの重さがゼロ**であること。**測定: 売買は起きるが小さく**（20000tickに 3.21回）、**断りはその2倍**（6.17）＝**足りないのは買い手ではなく売り手**。**割引率が段階のすべてを決めました**（`PLAN.md` の予想がそのまま当たった唯一の項目）——**1.0 で市場が止まり**（0.46回）、**1.0 超は禁じた3つ目の目的**、**0.2 では買い手が得をして売り手が断る**（3.58 対 **11.54**）。**個体数は +11.65 \*\* ですが、その半分は売買が1度も起きない腕でも出ます**（+5.10 \*）ので、**市場そのものの差（+6.54 \*、1.5 SE）を3.2回の売買に帰属させるのは弱い**。**偽薬が1ビット同じにならないことも見つけました**——**コインを撒くのに乱数を引く**ので、対照は「コインの無い世界」ではなく「撒いたうえで値打ち0」（段階45 の罠の3度目）。**そして手を経由して2つの規則が破られていたのを見つけて直しました**（外敵が贈られた植物を食べ、人間が人間の肉を食べていた＝**口では常に守られていた禁止が、贈り物と手の中の食事という2つの経路を迂回していた**）。詳細は 51 の節。

**2026-09-10 に段階50（倉庫）を完了しました。** **偽薬の腕が1ビットも動きませんでした**（「倉庫はあり、全員が知っていて、誰も使わない」＝倉庫の無い世界と全指標 +0.00 ± 0.00）＝**測れたものは全部「身体が実際に使ったこと」から来ています**。**そして知識の壁が倉庫を損なものにしていました**——学ぶ腕は倉庫なしより **−4.69 \***、全員が知る腕は **+3.71 \***、**直接の対で +8.40 \*\***（餓死率 −0.65 \*）＝**倉庫は隠し場所であるかぎり損で、共有の食料置き場になった瞬間に得**（**段階49 と正反対の形**）。**誰も溜め込みません**（入っている合計 0.19 個、容量4倍の世界は1ビットも違わない）。**理由を数えたら、蔵に客がいませんでした**——**余剰を持っているのは満腹の身体で、満腹の身体にとって「後のために取っておく」は値打ちがゼロ**（効用式は今の死亡リスクの勾配から読み、その勾配は満腹では平ら。両方そろう瞬間は全体の0.7%、`ActStore` は1実行に312回採点されて勝つのは3回）。**知る道は4つ必要でした**（計画の3つはどれも「すでに知っている誰か」を要するので何も始まらない）。**「見つける」を確率ではなく回数にしたのが測定を救いました**（確率版は +11.33 \*\*\* と出て、回数版は +1.67 ± 2.85＝段階45 の罠）。**そして途中で段階40 のバグが出ました**——**「後のために取っておく価値」の符号が逆で、138,310 件の候補に正の値が1つも無かった**（直すと拾う回数 +25%、他は不動）。**段階51 への反証可能な予想: コインも「後のために取っておく価値」なので同じ壁に当たる。** 詳細は 50 の節。

**2026-09-10 に段階49（発信）を完了しました。** **語は使われ、聞かれ、歩かれ、そして世界の数字は1つも動きません**（発信は決定の 0.5〜1%、売り物が視界にある決定が3〜4%、手渡しの5〜6%が発信のあと。個体数は平坦 +5.33 ± 6.54、遊ぶ地図 +1.10 ± 4.11）。**決め手は「聞こえない対照」でした**（`WaresSeen = false`＝叫びはするが誰にも聞こえない）——**平坦な世界で餓死が減るのは聞こえる腕でも聞こえない腕でも同じ**（−3.06 \* 対 −4.42 \*）＝**減らしているのは「誰かが聞いたこと」ではなく「立ち止まっていること」**。**この規則が本当に足したのは、腹の減った身体が使える「安く待つ」という手**でした（`rest` は満腹でないと何も返さない）。**「知ることは起きるが行動は変わらない」の4度目**ですが、今回は**知ることも歩くことも手渡しも実際に起きたうえで数字が動きません**——**理由は実装前に数えた的**（余剰は実在する〈持っている身体27%・うち85%が空腹でない〉のに、**売り手の52%はすでに買い手を見ており**、**買う側の的は空腹の瞬間の2.9%**、そのうち97%は地面の食料も見えている）。**買う側の値付けが最初5倍甘かったのも見つけて直しました**（歩いた5回に1回しか渡されない。足したのは新しい定数ではなく**地面の食料と同じ競走**）。**既定はオフ**。**ついでに魚の規則を1つ足しました**——**水から出た魚は死肉**（釣り上げた瞬間に死体と同じ時計が始まる。`LandedFishKeeps = true` で戻せる。大きさは釣り上げられた魚の3%、個体数は動かないが**腐る世界のほうが川の魚は多い**）。**段階50・51 への申し送り: 配管を測るときは「同じ時間を使い、情報だけが無い」対照を必ず対で回すこと**——これが無ければ「発信は餓死を10%減らす」と書いていました。詳細は 49 の節。

**2026-09-09 に段階48（門番）を完了しました。門は開いています**——既定の世界で20000tickに391回の手渡しが起き、**95%が他人へ**（血縁5%、配偶者0%）。**予想は逆でした**（「血縁と配偶者の間で起き、他人にはほとんど起きない」）——**理由は値付けの帰結**で、**信頼は飽和するので、すでに親しい相手への贈り物は何も買わず、他人への贈り物が一番高い**。**既定の世界では得**（個体数 +9.35 \*、`fightRatio` −0.04 \*、**`dietVariety` +0.04 \*\* ＝贈り物が食事を多様にする＝値段も市場も無いまま交換経済の効果が1つ出た**）。**ただし食料の乏しい遊ぶ地図では −11.27 \*\***（段階44 と同じ形＝**食料を使う規則の符号は世界の余裕で決まる**）。**測っている途中で遊ぶ地図の個体数が9まで落ちているのも見つけました**——段階44 の特産作物が原因で、用量を下げて25 に戻しました。**49〜51 は作ってよい**という結論です。詳細は 48 の節。

**2026-09-09 に段階47 を完了し、P7 を通しました**（遠隔のスキル）。**結果は段階38 が畳んだ上限の式どおり**——**保有率 0.08 × 実効 0.12 × 的 0.26 ＝ 0.25%** で、**命中率は +0.03 \*\*\* 動くのに集団の数字は1つも動きません**（的の大きさを先に書き下したので、これは予言として当たった形です）。**ただしスキルが自分の天井の遺伝子を買わせた初めての兆しが出ました**（`shRationality` +0.01 \*、`dRationality` +2.24 \*）——38b は記憶について5度測って動かせませんでした。**理性はこの世界で一番買われていない遺伝子**なので、そこに掛けたスキルが弱いのは道理でもあります（頭の良さに掛け替えると実効は 0.08 → 0.15。**それでも役割の線＝#71 のほうを守りました**）。詳細は 47 の節。

**2026-09-09 に段階46 を完了しました**（遠隔攻撃）。**一番大きい予想が外れました**——遠隔は「殴り返されずに殴る」規則なので**世界の本当の殴り返され率を下げるはず**でしたが、**上がりました**（`trueRetal` +0.01 \*）。**石は逃げ道ではなく招待状**で、**投げた身体は唯一の石を失い、詰めてきた相手と近接で戦うことになります**（もう1つの読みは「投げる相手は元気な身体で、元気な身体は殴り返す」）。**当たったのは逃走のほう**（`escapeShare` −0.02 \*\*＝逃げ切れなくなる）。**値段は地図で決まり**（弾の多い荒れ地で個体数 −6.42 \*、遊ぶ地図では誤差内）、**段階45 で数えた供給がそのまま効き目の上限になっていました**。**3倍痛くすると明確に壊れます**（−11.90 \*\*）。**既定はオフ**。詳細は 46 の節。

**2026-09-09 に段階45 を完了しました**（小石）。**使い道が無いので誰も拾わず、この段階の中身は供給を数えることでした**——**遊ぶ地図で石が視界にある個体は13%**、荒れ地だけの地図では54%、**3倍撒いても 0.15**（荒れ地が地図の一部でしかなく、身体はそこにあまりいない）。**段階46 の発火頻度の天井はここで決まっています**。**そして測定がバグを1つ見つけました**——石が食料の枠を食っており、**180個置いた世界は植物が1本も生えずに個体数が4**になっていました（**テストは通っていた**）。詳細は 45 の節。

**2026-09-09 に段階44 を完了しました**（特産食料）。**スキルが初めて世界を大きく良くしました**（個体数 +10.06 \*、出生 +33.79 \*\*、採り損ね 0.38 → 0.30 \*\*\*）——**的の大きさ（世界の作物の2割は知らないと採れない）を設定に書いてから測った初めての例**で、**38 の結びを裏返すと「規則を書けばスキルの値打ちは書いた分だけある」**ということです。**しかし居着きません**（`specialGain` ±0.00＝**スキルが居場所を動かさないのは5度目**）。**そして一番良い世界は、作物が世界じゅうに均等に生えている世界でした**（+14.35 \*\*）——**比較優位を生むはずの偏りそのものが世界にとっては損**で、**経済圏の土台に使えるのは「誰ができるか」であって「どこでできるか」ではない**。**役に立つスキルは保有率も上がる**ことも初めて観測しました（+0.06 \*）。詳細は 44 の節。

**2026-09-09 に段階43 を完了し、同日その形を作り直しました**（陸釣りと水中取り）。**最初の版は「上手い＝1匹から多くを得る」で書いており、採取スキル（#64）の形をそのまま魚に流用した誤りでした**——**上手い身体ほど少ない匹数で足りるので水にいる時間が短くなり**、`anglerSplit` が −0.09 と逆向きに出ていました。**上手い漁師が得るのは獲れる数**なので、**魚が逃げる確率**として書き直すと**符号が戻りました**（強い用量で `anglerSplit` +0.06 \*、`wadersWet` +0.06 \*、個体数 +5.73 \*）。**ただし取引が成立するのは「2つの地面の成功率の差が大きく、習熟度が高い世界」でだけ**で、弱い用量では逆向きのままです（水の成功率が陸を圧倒し、陸釣りが誰も乾いた場所に留めないため）。**そして測定上の落とし穴も見つかりました**——`anglerSplit` は**出生地も測っています**（何も買わないスキルの対照でも −0.07〜−0.13 出る）ので、**対照は「同じ種を配って何も買わない」でなければ、種の配られ方を規則の効果と読み違えます**。詳細は 43 の節。

**2026-09-09 に段階42 を完了しました**（魚）。**`onWater` がこの計画で初めて大きく動きました**——0.13 → 0.24（+0.11 \*\*\*）。**段階34（危険）も35（正確な信念と伝承）も動かせず、36（岸の恵み）が 0.11 → 0.15 だった数字**で、**危険と報酬がついに同じセルに乗りました**（水の中にしか無いものは、入るしかない）。**代価は地図で3倍変わります**——裸の川では個体数 −16.7 \*\*\*、**岸がすでに豊かな遊ぶ地図では −5.3 で誤差内**。**泳ぎスキルの的も大きくなりました**（溺死 −4.35 \*。魚なしの段階38b は −1.83 \*）＝**段階38 の再開条件への答えの半分**。**既定は 0**（風景の性質は地図の作者が決める＝段階33・36 と同じ扱い。`cmd/devview -terrain` は 0.25）。詳細は 42 の節。

**2026-09-09 に段階41 を完了しました**（肉の余剰）。**前提はまた半分崩れました**——余剰は実在する（肉の26%）が、**claim はもともと独占ではなく順番待ち**で、**すでに食べられている肉の54%は claim を持たない者が食べています**。**規則が変えられるのは「誰が食べられるか」ではなく「いつ」**でした。**開くと個体数 +6.21 \*・世代 +0.48 \*\*・腐敗 −0.01 \***。**claim を全部やめる版は −4.33 で逆に悪い**＝**残すべきは claim、開けるべきは余剰**。**そして肉の量（`MeatPerBudget` 60）の符号が2度ひっくり返りました**——段階39a で −13.4 \*、治りが入って +7.4 \*、余剰も開いて +13.6 前後。**「この値は悪い」という記録は、その時点の世界についての記述であってパラメータについての記述ではない**（既定は 120 のまま）。詳細は 41 の節。

**2026-09-09 に段階40 を完了しました**（運搬）。**世界が始まって以来はじめて状態量が増えました。** **的は実装前に数えたとおり実在し**（餓死の61%は直前100tick以内に食料を見ている）、**当たりました**（その割合 −0.04 \*）。**ただし最初の版は個体数を 4.5 削りました**——犯人は重さでも枠の会計でもなく**「拾う理由の粗さ」**で、**保険の価値をその場の食料の競り合いぶりで割り引く1項**を入れると代価が消えます（+1.25、誤差内）。**容量は1で符号が変わります**（2で −5.73 \*、3で −7.10 \*）。**遊ぶ地図では今も負けます**（−8.90 \*）＝**運搬が一番要る世界が、運搬が一番高くつく世界**。**#77（運搬中は腐らない）も閉じました**（+0.81＝誤差内、地面で測った上限どおり）。**そして無関係のバグが1つ落ちてきました**——答えを突き返すコントローラにも `TriggerCourted` が飛んでおり、段階23 の「誰も答えない誘導ノードはAIと1ビットも違わない」が**その場面が起きるまで真だっただけ**でした（直して回帰テストを追加）。詳細は 40 の節。

**2026-09-09 に段階39 を完了しました**（P7 の先頭）。**群れ狩りが5度目にして現れました**——`packSize` 1.36 → 1.81、共同での仕留め 34 → 95（48シード）。**動かしたのは栄養ではなく治癒**で、**同じだけ腹にたまる肉では +0.06 しか動きません**。**効き目の半分は「知っていること」から来ます**（治りを知覚から抜く対照で +0.45 → +0.21、餓死の減少は丸ごと消える）＝**段階34 が有意差ゼロだった同じ形の対照で、初めて差が出ました**。**既定は `MeatVitality` = 0.5 で、これで世界のベースラインが動きます**（`killShare` 0.74 → 0.83、`dIntelligence` −6.72 \*\*）——**2026-09-09 より前の測定値は `meatoff` の世界のもの**。詳細は 39 の節。

**2026-09-09 に3つの設計相談（運搬能力・魚・遠隔攻撃／経済圏／肉の経済設計）を段階に落としました。** **段階39〜51 と、独立項目1つ（行動の拘束時間）。** **順序の基準は「的の大きさ」**——段階38 の再開条件（**打ち消す規則の大きさ以上にはならない**）はスキルに限った話ではなかったからです。実測の的は **戦闘死 / 餓死 / 溺死11% / 肉が食べられていないこと（`dietVariety` 0.12）/ 飽きの割引5%（空振り済み）**。**（2026-09-09 の訂正: 死因の割合は出産のバグを直す前の値を引いていました。今の平坦な世界は戦闘74%・餓死23%で、段階39 のあとは 83%・16% です。順序の理由は変わりませんが、段階40 は的を数え直してから始めること。）****先頭の2つ（39 肉・40 運搬）はこの中で一番大きい的を持ち、しかも何にも依存していません。** **配管（発信・倉庫・通貨）を先に作らないのは、知識の側が3度続けて世界の数字を動かさなかったから**（段階30b・31・35）で、**P8 の先頭にはその門番（48 バーター）を置きました**——交換が起きなければ通貨も倉庫も測るものがありません。詳細は P7・P8 の節と `PLAN.md` の決定事項#65〜#76。

**2026-09-08 に 38b の3つ目（毒耐性）も完了し、段階38 を区切りました。** **4種類やって出た上限は「保有率 × 実効値 × 的の大きさ」**——集団への到達が7〜26%、的が2〜17% なので、**集団の指標としては数%が天井**です。**唯一有意に動いた泳ぎは、この積が一番大きかった**。詳細は 38b の節。

**2026-09-08 に段階17b（毒と警告信号）の復活を測りました。** **「どの用量でも世界は持たない」は取り消し**——旧記録は見かけトリガー修正前の世界のもので、**今は毒2 で −17%**。**壊していたのは毒ではなく恐れ**（餓死率16倍）。**毒は植物にとって1ミリも得ではなかった**（食べられた株は毒の有無に関係なく消える）ので、**信号だけが選択され、値段が無いので上限へ走っていた**。**`PlantSignalCost` を足すと信号は 0.60 → 0.07 に落ち、代価はほぼゼロ**。**毒に得をさせる版は食料が消えて壊れる**（個体数 4.2）。**復活の形は「毒2＋信号のコスト」**で、既定はオフのまま。詳細は C-5i と `HISTORY.md`。

**2026-09-08 に 38b の2つ目（泳ぎ）も完了しました。** **3つのスキルで唯一、狙った量が有意に動きました**（溺死 −1.83 \*）——**先に「打ち消す相手が集団の何%を動かしているか」を数えたから**（溺死は死因の11%、飽きは5%）。**それでも `onWater` は動きません**（6度目）。**2026-09-08 に 38b の1つ目（採取）も完了しました。** **設計どおりに動き、ほとんど何も買いません**——学ばれ広がり天井も効く（名目 0.55／実効 0.18／模倣なし 0.19）のに、**一口の価値は動かない**（打ち消す相手＝飽きの割引が5%しかない）。**#64 が心配した独占は起きず**、代わりに**「スキルはそれが打ち消す規則の大きさ以上にはならない」**が分かりました。詳細は 38b の節。

**2026-09-08 に段階38a を完了し、10b（ヒントの既定）も閉じました。** スキルは**ヒント枠の中身**として入りました。**知識は社会的に育ちます**——名目習熟度は全部入りで 0.80、模倣を切ると 0.54、天才の跳躍も切ると 0.50（＝出生地の値そのもの、ばらつきゼロ）＝**跳躍が作り、模倣が配る**。**しかし居場所は変わりません**（`onDear` +0.01 \*、荒れ地の超過コストを全部消しても不動）。**既定は 0**（地形があってはじめて意味を持つ規則は地図の作者が入れる＝段階30a・33・36 と同じ）。詳細は 38a の節。

**2026-09-08 に段階30b を判断しました**（`PLAN.md` の段階30）。**どちらの決定も取り消さず、30b は「作らない」で閉じます**——(a) は #59 の改訂ですでに許されており（信念は帰結ごと＝遮蔽は3つ目の帰結）、**残っていたのは許可ではなく値打ちの問題**。その値打ちは段階35 が同じ形の機構で測って**ゼロ**でした。**`onHigh` は食料の相関でも動きません**（今日測って −0.01 ± 0.02）。**38b の「高台上り」も同じ理由で外しました。**

**2026-09-08 に段階32 を完了しました**（協力の誘い）。**群れ狩りは現れず、段階11 の診断（足りないのは狙いを合わせる手段）は否定されました**——`packSize` 1.34 → 1.36 は語彙なしの対照でも同じだけ出ます。**代わりに世界が良くなりました**（個体数 +12.83 \*\*、`dIntelligence` +5.04 \*）。**保留#10（明示的な協力）はこれで閉じます。** 詳細は 8c の節。

**2026-09-08 に段階31 を完了しました**（目撃した殺害）。**規則は絶えず発火し、世界の数字を1つも動かしません**——1件の殺害あたり 24.3 回の観測機会が生まれるのに**取り込まれるのは5回に1回**（集団の95%は記憶が上限）。**#55 が心配した「観察が死ぬ」も起きません**（重みを払った観察と同格にしても `watchShare` は不動）。詳細は 8b の節。

**2026-09-08 に段階37 を完了しました**（水辺の群はこれで全部）。**川は視界を分けるが集団は分けない**——乾いた地面の者どうしの遭遇は 0.17 → 0.01 まで落ちるのに、両岸の遺伝子も信念もずれず、**一生のうち両岸に立つ個体が半分いる**。詳細は 37 の節。

**独立した宿題（順序に縛られない・小さい）**: **2026-09-09 に1つ足した**——**満杯の判定が死体も植物の枠に数えている**（`spawnFood` の入口。`MaxMeatItems` を別に持つ設計と食い違う。段階45 で石を除くときに見つけたが、`HISTORY.md` の全数値が測られた世界がそれなので据え置いた。**直すなら独立した変更として測る**）。 **2026-09-08 に 10b・19b・段階31（目撃者スキャンの共通化）・段階34 の `species` 監視・段階36 の `plantEmpty` 監視を閉じた。** 残っているのは **19c（段階23 の申し送り）／段階13 の性能の申し送り／段階22 の残り（天才の管理者配置）／段階35 の残り（`drawShare` は地域信念の天井）／段階36 の残り（帯を測る指標——**段階37 の `crossIndex` / `crossDry` がその答えになった**）**。

**2026-09-10 に保留を1つ足しました（#78）。** **魚は本来「攻撃力の小さい外敵（第3の種族）」の意図でした**が、段階42・43 の実装は**食料アイテム**です——自分では動かず、攻撃力も意思決定も持たず、**2つの釣りスキルはどちらも「止まっている的への成功率の掛け算」**でしかありません。**意図どおりなら陸釣り＝寄ってきた魚を捕る／水中取り＝こちらから寄って捕る**で、**「誰が距離を詰めたか」という区別が今のコードには存在しません**。**保留の理由は規模**（決めることが多く、段階42・43・34・36 の測定が全部引き直しになる）で、**復活の条件は段階50・51 のあとに1段階まるごとの枠を取れるとき**。詳細は `PLAN.md` の #78 と保留の節。

**着手していない大物**: `protocol` / `server` / `client`（時期未定）。

---

## 優先順位の考え方

| 優先度 | 意味 |
| --- | --- |
| P0 | 土台。これを先にやらないと以降の測定・性能が全部やり直しになる |
| P1 | 遺伝の中核。ここで測定のベースラインが1度リセットされる |
| P2 | 表現力の拡張。P3（協力・伝承）の前提になる |
| P3 | 本命（外敵と伝承＝集団の創発）。P0〜P2 が揃ってから |
| P4 | 環境と食料生態系（世界の側に差をつける）。2026-09-03 追加。段階12 と並行してよいが、内部の順序は守る |
| P5 | ゲーム化とマップエディタ（人が世界に触れるようにする）。2026-09-05 追加。**内部の順序は守る**（理由は下の P5 の節） |
| P6 | 水辺（危険と豊かさ）と技能。2026-09-07 追加。**P3 のあと**。**内部の順序は守る**（理由は下の P6 の節） |
| P7 | 物を持つ世界（肉・運搬・魚・特産・遠隔）。2026-09-09 追加。**P6 のあと**。**並べ替えの基準は「的の大きさ」**（理由は下の P7 の節） |
| P8 | 交換（バーター・発信・倉庫・通貨）。2026-09-09 追加。**P7 のあと**。**先頭のバーターが門番で、そこが空振りなら残りは作らない** |
| P9 | 今しか見ていないこと（調理・感情・性差・孤立）。2026-09-11 追加。**P8 のあと**。**段階50・51 が独立に当たった壁への、4つの別々の当たり方**（#79） |
| — | 保留。条件が満たされたときだけ復活 |

**順序を入れ替えてよいもの / いけないもの**

- 6.5 → 7a → 7b → 7c → 7d は**この順を守る**（後段が前段の測定基準・性能・遺伝子表現に乗るため）
- 段階8（チャネル化）は 7c の後ならいつでもよい。**段階9 より後ろに回してもよい**
- 段階9（好意）は**段階12 の前提**。段階10・13 は独立していて後回し可
- **段階14 → 15 → 16 → 17 はこの順を守る**（14 が地域という語彙を作り、15 の固定版を 17 が置き換える。16 は 17 の前にベースラインを取るためにここにある）。**段階18（体内時計）は段階12 より後**
- **P5 は 19 → 20 → 21 → 22 の順を守る**（20 の未決が 21・22 の形を決め、21 が 22 の保存経路になる。理由は P5 の節）。**19 だけは P4 の宿題と並行してよい**（世界の状態に触れないため）
- **P6（34 → 35 → 36 → 37）は 2026-09-08 に全部通した。** 順序を守った理由は残す（34 が水の意味を変えて地形アームを再ベースラインし、35 と 36 は混ぜると原因が分けられず、37 は実装ではなく観測）。**38（スキル）は 10b（ヒントの既定）を閉じてから**、**泳ぎは 34 のあと**（理由は P6 の節）
- ~~**31（目撃の伝播）は 35（溺死の周知）より前**~~ → **順序は入れ替わり、35 が1つ目の実例になった**（2026-09-07）。**共通化は 2026-09-08 に 31 の側で行った**（`forEachWitness`。共通なのは走査だけで、更新先も重みの単位も違う）。**30b の判断は水辺の群のあと**（35 が2本目の信念の値段を先に払う）
- **P7 は 39 → 40 → 41 と 42 → 43 と 40 → 45 → 46 → 47 の順を守る**（余剰の定義が運搬上限／魚が無ければ釣りの的が無い／弾は運搬に、遠隔スキルは行動の単体測定に依存）。**45〜47（遠隔の群）と P8 は入れ替えてよい**（どちらも 40 のあとで、互いに依存しない）。**行動の拘束時間はいつでもよい**（ただし単体で測り、運搬と同じ回に入れない＝#76）
- **P8 は P7 の 44 より後ろでなければならない**（比較優位の両側＝39・41 の動物側と 44 の植物側が揃ってから叩く門だから）。**48 が空振りなら 49〜51 は作らない**
- **P9 は 52 → 54 → 53 → 55 の順**（**52 が先頭なのは壁を構造的に避けられる唯一の形で、しかも 55 の必要性そのものを検証する前提になるから**。**54 は 52 と並行してよい**が `ChoiceNoise` を触るので他の測定と同じ回に混ぜない。**53 は配分の方向が未決なので後ろ**。**55 は 52 の結果待ち**）。**52d は 52 の効果が弱すぎたときだけ**
- `protocol` / `server` / `client` の着手時期は未定（`CLAUDE.md` の未決事項）

---

## P0：土台（最優先）

### 1. 段階6.5：計測の先行整備 【完了・2026-09-01】
> 目的: 「あるべき状態」の目標値を憶測でなく実測から決める。読み取り専用なので安い。以降の全段階の効果測定に使い回す

- [x] クラスタ検出（素朴な実装でよい）→ クラスタ数・サイズ分布 — `World.Clusters(linkDist)`、連結距離 30 固定、`cmd/experiment` に `clusters` / `clusterSize` / `grouped` / `largestShare`
- [x] membership half-life（同じ顔ぶれが一緒に居続ける時間） — `engine.MembershipTracker`、組の共在生存曲線として測る、`cmd/experiment` に `halfLife` / `together` / `censored`
- [x] 集団内戦闘率 ÷ 集団間戦闘率 — `engine.FightTracker`。書かれたとおりには測れないので「仲間（200tick前から同じクラスタ）÷ よそ者」の遭遇あたり戦闘率に置き換えた
- [x] 集団間の距離分布 — `World.ClusterGaps(linkDist)`、`cmd/experiment` に `gap` / `gapP10` / `gapRel`
- [x] 種族ごとの個体数 — `engine.CensusTracker`。窓（5000tick）の平均で持ち、読むのは振れ幅ではなく **谷（最小 ÷ 平均）**。絶滅だけは窓ではなく実行全体で覚える。`Agent.Species` のラベルも先に置いた（今は human だけ・ルールは参照しない）
- [x] `cmd/experiment` の出力に上記を追加（O(n²) なので頻度は間引く。クラスタの4指標・half-life の3指標・戦闘率の3指標・距離の3指標・種族の4指標をすべて追加済み）
- [x] **完了条件**: 現状を 50000tick 回して測り、「集団が無い世界のベースライン」として `HISTORY.md` に記録する（**目標値はまだ書かない**＝段階11 の完了条件へ）— 2026-09-01、24シード × 50000tick。**塊はあるが集団は無い**（半減期60tick・`fightRatio` 0.93・`gapRel` 0.80）

**段階6.5 は 2026-09-01 に完了。** 次は段階7a。

### 2. 段階7a：空間グリッド（挙動を変えない高速化） 【完了・2026-09-01】
> 目的: `perceive()` の実質 O(n²) を潰す。以降の全段階の土台であり、地域割りの出所にもなる

- [x] `World` に一様グリッドを持たせ、ノードと食料をセルに登録する — `engine/grid.go`。セル幅＝`PerceptionRadius`、差分更新せず「古い」印＋遅延再構築、候補は添字の昇順で返す（走査順が乱数の消費順なので）
- [x] `perceive()` の近傍探索をグリッド経由にする（**円の視界判定はそのまま残す**）— あわせて `nearestFoodInSight` と `exchangeReadings` も
- [x] 食料ごとの「最も近い他個体」もグリッド経由にする — **個別に問い合わせない**（観測者に見えない個体まで拾って答えが変わるため）。他個体の走査に相乗りするので、上の項目で自動的にグリッド経由になっている
- [x] **完了条件**: 同一シードで導入前と統計値が完全一致すること — `cmd/experiment` の出力がバイト単位で一致、加えて `engine/golden_test.go` で指紋を固定
- [x] ベンチを前後で取り `HISTORY.md` に記録 — **数百体では全走査のほうが速く、効き始めるのは500〜600体から**（2966体で −30%）

**段階7a は 2026-09-01 に完了。** 次は段階7b（継承則の入れ替え）。

---

## P1：遺伝の中核

### 3. 段階7b：継承則の入れ替え（染色体式＋幅） 【完了・2026-09-01】
> 目的: 混合遺伝（毎世代で分散が半減）をやめ、粒子的遺伝で多様性を維持する

- [x] `tryBirth()` を「遺伝子ごとに親のどちらかを引く＋正規乱数」に変更（引き方は遺伝子ごとに独立）— 能力の標準偏差が約2倍、選択圧は理性で約4倍。**ここで過去の測定値とは比較できなくなる**
- [x] `Config` に `MutationRate` を追加、`MutationStd` を再較正 — **既定は 0.01 ではなく 0.02**（名目の分散を合わせるだけでは端のクリップで15%足りなかった）、`MutationStd` は 40
- [x] `LamarckRate` を `Config` に用意（この段階では未使用の枠だけ。段階12 まで誰も読まない）
- [x] **完了条件**: 前後で `cmd/experiment` を回し、能力の分散と選択圧の変化を記録 — 分散は約2倍（5.6〜6.1 → 11.0〜14.5）、選択圧は2〜3倍。**新しいベースラインは `HISTORY.md` の 2026-09-01「段階7b 完了」の項**

**段階7b は 2026-09-01 に完了。** 次は段階7c（遺伝子ベクトル化と予算制）。

### 4. 段階7c：遺伝子ベクトル化と予算制 ← **この段階が最大** 【完了・2026-09-01（残りの6つの決定は 2026-09-02）】
> 目的: 能力3種を `Genome []float64`（遺伝子9種）に置き換え、親から継ぐ予算制にする。**魅力もここで入れる**（後から遺伝子を足すと予算も測定基準も取り直しになる）

- [x] `Power/Rationality/Intelligence` → `Genome []float64`（遺伝子9種）に置換 — 挙動不変のコミットとして分離
- [x] 初期個体: 予算を抽選（`GeneBudgetMean`/`GeneBudgetStd`）＋ 配分をディリクレ分布から引く
  - [x] ガンマ分布のサンプラを自前で書く（外部依存を増やさない）／一様乱数の正規化は使わない／1本あたり 1〜100 にクリップ
- [x] 子の予算＝親の予算 ＋ 正規乱数（`BudgetInheritSpread`）。`BudgetHeritability` は 0〜1 の連続値で切替可
- [x] 継承時の正規化（親由来の値を子の予算に合わせてスケール）→ **突然変異は配分を動かすだけで合計を増やさない**
- [x] 天才2段階（`GeniusRate` / `GreatGeniusRate`）。`TicksPerYear` は出発点 500 を仮置き（**実測の間隔は22.8年。狙いの10年とずれているので段階7d で測り直す**）
- [x] 既存の `Power`→攻撃力、`MaxVitality`/`MaxSpeed` を遺伝子側へ移す（**体力の閾値は「自分の上限に対する割合」に変えた**）
- [x] `fitness` を魅力遺伝子で置き換える（**実能力とは相関させない**）
- [x] 再判断トリガーに「魅力の高い個体が視界に入った」を追加
- [x] `cmd/experiment`: 能力指標を**絶対値→予算比**に変更、「世代ごとの平均予算」と「予算の分布」を出力
- [x] **予算のドリフトに価格を入れた**（`BudgetUpkeep` ＝ 大きい身体ほど空腹が速い）。**効くが止まらない**（588→530）。理由は「この世界では誰も飢えて死なない」から
- [x] **完了条件の残り**（段階8 のあとに判定・2026-09-01）: **条件の文言は満たさないが、条件が確かめたかったことは満たす。** 「大きくて遅い／小さくて速い」は今も並存しない（体力と移動速度はどちらも「殴る」に対抗して買われる同じ側なので競合しない）。一方で**異なる種類の個体は並存している**——最強の対立軸は「殴る個体 対 考える個体」（相関 −0.309、基準線 −0.125）。詳細は `HISTORY.md` 追記25
- [x] **目視の半分**（2026-09-01）: `cmd/devview` の描き方を変えた。**輪の大きさ＝体力上限、塗り＝残り体力、後ろの尾＝速さ、輪の太さ＝攻撃力。** 右パネルに遺伝子9本と予算比も出るようにした。**段階7c の完了条件はこれで全部済み**
- [x] **段階8 への申し送りは解決した**（2026-09-01）: `killShare` 0.92 → 0.79、個体数 63 → 111。防御・回避という買い物が増えたことで、攻撃力への一極集中が解けた
- [x] **「段階7c までに決める」の6項目に数値をつけた**（2026-09-02）。`BudgetInheritSpread` 30・`GeneInitAlpha` 0.8・`FitnessConditionWeight` 0.35（`Config` へ移した）・`BudgetUpkeep` 1 は**そのまま据え置きと決定**、天才の発生率は**出生あたりの確率のまま・値の確定だけ段階7d へ移送**。根拠と外した予想は `HISTORY.md` の同日の項と追記27

**段階7c は 2026-09-02 に完了。** 予算はもう暴走せず（20000tickで+51、そこから60000tickまでで+31と減速）、遺伝子の上限張り付きは19〜27%だが**予算は最大900に対して432**なので、縛っているのは予算のほう（特化であって飽和ではない）。

### 5. 段階7d：成長・老い・寿命と育児期間 【完了・2026-09-02】
> 目的: 伝承（段階12）の土台＝親子が一定時間近くにいる状況を作る。**寿命消費だけ 2026-08-31 に先行実装済み**（意思決定に一切関与しない背景処理という制約つき）

- [x] 寿命を状態として持つ（`Agent.Lifespan`）／過小（空腹）・過度（腹八分目超）で消費／`Config` で各々 0 にして切れる／`cmd/experiment` に `aged`・`agedShare`
- [x] **年齢補正**: 実効値＝遺伝的素質 × `Agent.AgeFactor`。成長（若い端）と衰え（老いた端）は同じ1本の曲線で、**掛け算は `Agent.Ability` の1か所だけ**。`Gene()` は遺伝的素質のまま（繁殖と測定はこちらを読む）
- [x] **成長**: 食事で進む（新アクションなし）。満腹で最大、飢えれば止まる。**子ども期は3年ではなく2年**（3年だと世界が壊れる）
- [x] 成長の頭打ち（100%）と過度コストの開始（80%）を別の閾値にした。**ただし行動差は出ない**——成長も寿命も `Perception` に載せない設計なので、変わるのは結果だけ
- [x] **#33 慢性的な低体力による寿命コスト**（`FrailVitalityShare` / `FrailGraceTicks` / `FrailLifespanRate`。20%超えでリセット。`ShockRisk` とは別物で効用式に載らない）
- [x] **育児期間**: `ChildRearingTicks`（既定1000）。`= 0` で切れる。給餌なし。**`RearingRadius` は視界と同じ130でなければならない**（45では子を食料から引き離して殺す）
- [x] 親側の負担は**入れない**と決めた／子の生存は親の効用に**入れない**と決めた（子が留まる側だけで成立する）
- [x] **`TicksPerYear` = 500 を本決めにした**（3条件を満たす。天才の発生間隔は段階7c で集計し直し済み）
- [x] **完了条件**: 死因3種は 戦闘62%・餓死34%・消耗4%（どれも支配的でない）／平均年齢10.1年・20000tickで約5世代／成人到達率0.72・子の死は死因の5%／`ChildRearingTicks` 0 との対で `deathRate` +0.71 \*・`nearest` +19.5 \*（**ただし子の生存率は動かない＝効いているのは保護ではなく凝集**）／#33 は個体数に影響なし／**「食べ方が変わる」だけは原理的に測れない**（上記）
- [x] **注意**: 死因が増えたので、段階7c までの測定値と直接比べないこと。**新しいベースラインは `HISTORY.md` の 2026-09-02「段階7d」の項**

**段階7d は 2026-09-02 に完了。** 個体数は成長なしの世界の6割（44.7 → 26.3）で、**これは調整不足ではなく子ども期そのものの値段**（死亡率は上がっておらず、世代の回転が遅くなった形）。

---

## P2：表現力の拡張

### 6. 段階9：記憶の容量・帯域・忘却曲線と、好意 ← **段階12 の前提** 【完了・2026-09-02】
> 目的: `opinions` を有限容量にし、忘却を「容量の取り合い」にする。あわせて記憶に**初めての正の項（好意）**を入れる。**敵意は新設しない**（既存のリスク記憶をそのまま敵意として扱う）

- [x] `opinions` を容量つきに（あふれたら重要度の低いものから捨てる）／単位時間の更新上限（帯域）／忘却曲線を遺伝子化 — 帯域は**容量から導出**（専用遺伝子なし）、忘却は `(100 − 記憶)/49.5` 倍
- [x] **好意**を `Opinion` の新フィールドとして追加。容量は既存の記憶容量遺伝子で管理（専用遺伝子は作らない）
- [x] 発生源はこの段階では2つだけ（**実イベントのみ・共在時間による加点はしない**）: ペア形成・出産／血縁（**直系の親子1ホップのみ**）
- [x] **#32 休息を無防備にする** — **既定の重みは 0.05**。0.10 以上にすると個体数が落ちる（**休息の価値が丸ごとゼロになる崖**に入り、消耗死が増えて出産前のペアが壊れるため。`HISTORY.md` の測り直しを参照）
- [x] **#22 接触による減衰起点のリセット**（減衰式は変えず「経過tick」の起点だけ更新。好意・リスク記憶の両方に適用）
- [x] **完了条件**: 全部済み。個体数は動かず（−0.2 ± 6.9）／`restNear` 6.5 → 2.0／`friends` が容量に追随（6/12/24/∞ で 1.01/2.16/2.55/3.88）／devview で目視／`BenchmarkDecide` 10.0 → 11.2µs
- [x] **申し送りを閉じた（2026-09-04）。出ないのが正しい値付けだった。** 3度測って動かなかった `shMemory` を、個体数がほぼ倍になった世界で**掃引して形を見た**ところ、**平らではなく右下がり**だった: 記憶6人 → 個体数79.9／12人 → 77.7／24人 → 73.8／無制限 → 54.1（−23.6 \*\*\*）。**記憶がこの世界で主に生産するのは「友人」で、好意は休息を安全に見せるので net で損。** `memFull` は 0.93 なので容量自体はしっかり縛られている（「制約になっていない」わけではない）
- [x] **記録してあった診断（地域が12個では容量が縛られない）は間違いだった。** 地域を108個にしても個体は91.2個を知り `shMemory` は動かない——**決定事項#41 が「地域の記憶は容量の枠に入れない」と決めているので、地域をいくら増やしても容量は縛られない**。**2段階前に自分で決めたことと矛盾する仮説を書き留めていた**。接触リフレッシュを切る筋も試したが、やはり容量を増やすと悪くなる
- [ ] **新しい観察（別項目）**: 「好意 → 休息が安全に見える」経路が**段階12b・15b・ここの3度 net の負として現れている**。段階18 で「眠っている友人は見張れない」を足して個体数が +12.6 \* 上がったのもこの経路を弱めたから。**`AffinityTrust` が完全な安心（`trust` が1に届く）を許していることの妥当性を測る価値がある**——記憶の話とは別なので独立項目

**段階9 は 2026-09-02 に完了。** 次は段階10（見た目からの学習）か段階7d（成長・老い）。

### 7. 段階8：攻撃・防御・回避のチャネル化 【完了・2026-09-01】
> 目的: 「攻撃力・防御力・移動・回避の使い方で体力の減りが決まる」。**この段階が一番危険**（攻撃側有利の非対称量が動き、個体数を崩しうる）

- [x] effort をプリセット3種（攻め寄り／守り寄り／逃げ寄り）に拡張
- [x] 防御＝ダメージ乗数を下げる／同tickの攻撃出力も下がる
- [x] 回避＝確率で完全無効／移動速度に依存
- [x] 体力消費 = Σ(チャネルごとの 単価 × 使用率）
- [x] **完了条件**: `BenchmarkDecide` 8.0µs → 10.3〜11.1µs（+3割）／**個体数は 86 → 111 に増えた**（`killShare` 0.92 → 0.79）。防御・回避の両方に予算が回るようになり、**選択圧がパワーから頭の良さへ移った**

**段階8 は 2026-09-01 に完了**（段階10 より先に順序を入れ替えた）。`PLAN.md` の「一番危険」という警告は**逆だった**——危険だったのは入れないことのほう。数値は `HISTORY.md` 追記24。

---

## P3：本命（外敵と伝承）

### 順序の追記（2026-09-06・出来事の伝播と協力の誘いが入ったので）

**決めた順序: 31（目撃の伝播）→ 32（協力の誘い）→ 29（地形の認識と伝承）→ 30（高台の遮蔽）→ 21（保存/読込）→ 22（エディタ）。理由は4つ。**

**（2026-09-06 の更新）29 は先に通し、その続きの 33 が入りました。今の順序は 33 → 31 → 32 → 30 → 21 → 22 です。理由は3つ。**

**（2026-09-07 の更新）33・21・22 は通り、水辺の群（34〜37）とスキル（38）が入りました。今の順序は 31 → 32 → 34 → 35 → 36 → 37 → 30b → 38 です。理由は下の P6 の節にまとめてあります**——要点は3つ。**(1) 31・32 は先頭のまま**（開いている問いの重さは変わらない）。**(2) 31 は 35 より前**（目撃者スキャンを2つ目の実例が出てから共通化するため）。**(3) 30b は水辺の群のあと**（35 が「地域の信念に2本目を足す」を先に払うので、その値段を見てから #53 を取り消すかを決める）。

- **33 を先頭に置くのは、開いたままの診断は始まっていない問いより高くつくから。** 段階29 の結論は今のところ「**避けるようにはなるが半分にはならない。ただし、その頭打ちが交絡のせいか本当の限界かは分からない**」で止まっている。**この宙ぶらりんの上に、30（遮蔽）も 22（エディタ）も、保留の「水辺の豊かさ」も乗っている**。しかも 33 は**パラメータ1つと再正規化**で、腕はもう用意してある
- **P3（31・32）との関係は前回と同じで、交絡しない。** 地形は既定オフなので、33 を先に通しても 31・32 のベースラインは動かず、測り直しは要らない。**逆も同じ**なので、群れ狩りの答えを先に見たければ 31 を前に出してよい
- **30 は 33 のあと。** 「高いところに値打ちがある」を測るのは、**地形が個体数を払い続けたままなのかどうかが決まってから**のほうが読みやすい（前回の理由 2(c) がそのまま生きている）。**負の相関（荒れた地域ほど豊か＝リスクとリターン）は 33 の正の相関が解けたあとの独立条件**で、狙いとしては 30 と同じ群（「代償を払ってでも行く値打ち」）

1. **31・32 を 29・30 より前に置くのは、開いている問いの重さが違うから。** 「群れ狩りは現れるか」は**段階11 以来ずっと開いたまま**で、**あるべき状態 B〜E** というこのプロジェクトの採点表そのものに載っている。一方 **地形は既定オフ**（`TerrainMap` が空）なので、29・30 を後に回しても**世界のベースラインは1ビットも動かず、待っているのはエディタ（22）だけ**
2. **31 を 32 より先に置くのは、安いほうを先に確定させるため。** 31 は語彙も状態も増やさず既存の推定と好意に載せるだけ。**そして 31 を後にすると、32 の看板の数字（`packSize`）を測ったベースラインが動いて測り直しになる**（「測定を伴う段階の前に既定を動かさない」の同じ形）。**31b の既定がオフになる可能性がある**（好意の発生源を広げる値段）ので、**誰を信じてよいかが広がるのかどうかを、誘いが載る前に決めておく**
3. **31 と 32 は同時に測らない。** 31 は好意とリスク推定の**発生源**を広げ、32 は好意の**使い道**を広げる。同じ回に入れると、群れ狩りが現れたときに「誘えるようになったからか、誰を信じてよいか分かるようになったからか」を分けられない（`PLAN.md` の同名の節）
4. **2つの群は互いに交絡しない**ので、順序は価値の判断だけで決めてよい（地形は既定オフ＝31・32 のベースラインを動かさない）。**遊ぶための世界を先に良くしたいなら 29 を前に出してよく、測り直しは要らない。** 動かしてはいけないのは**それぞれの内部順序**（31a→31b、32 の中の順、29a→29b、30a→30b）と、**伝承・好意に触る変更を同じ回に2つ入れないこと**

### 8. 段階11：種族の汎用化と外敵（協力の土俵） 【完了・2026-09-03】
> 目的: 「隣人が生きていると自分が得」を**世界側の事実**として置く。「協力」という言葉は engine に登場させない

- [x] 人間・外敵で同じノード構造・同じ意思決定エンジンを共有する — 違いは**予算の抽選範囲と食性の2点だけ**
- [x] 外敵に「大きさ」を持たせ、ドロップ数を大きさで決める／分配は攻撃に参加した者だけ（**食べられない種族には権利を渡さない**）
- [x] **共同戦闘を好意の加点源にする**（2026-09-03、`AffinityHunt` = 6）— 誰が数えられるかは**分配ルールそのまま**。**「主要な」加点源にはならない**（20000tick に13.7回しか発火しない）ので、集団の指標は動かない。意味は段階12 の前提＝**非血縁の好意**を作ったこと
- [x] 食料2種（植物＋死体からの肉）。人間は共食い不可、外敵は種族を問わず食べる — **A/Bの2種類の共通食料は作っていない**（AとBを区別するルールが無いので、ラベルが2つ増えるだけになる）
- [x] **完了条件**: 群れ狩りが現れるか現れないかを測る → **現れなかった**（`packSize` は動機を切っても 1.43 → 1.46 で変わらない）／段階6.5 の指標を測り直した／`PLAN.md` の表に**あるべき状態A の帯**を書き込んだ（B〜E は段階12 のあとに判定）

**段階11 は 2026-09-03 に完了**（実装は 2026-09-01、残っていた好意の加点源と外敵のサイズ分布が 2026-09-03）。群れ狩りが現れなかったので、`TODO.md` 保留欄の「明示的な協力」の再検討条件は満たされた。**ただし再検討の前に段階12（伝承）を通すこと**——「一緒に狩ったことが記憶に残らない」は解消したが、残る「狙いを合わせる手段が無い」は段階12 でしか埋まらない。**獲物を大きくしても群れ狩りは現れない**ことは実測済み。**段階12 は 2026-09-04 に通ったので、この前提も消えた**——再検討は下の 8c（段階32）。

### 8b. 段階31：目撃した殺害が伝わる（脅威の周知と、討伐への好意） 【完了・2026-09-08】
> 目的: 誰かが誰かを殺すところを見ても、見ていた者に**何も残らない**のを直す。**新しい状態も新しい記憶の種類も作らない**——既存の強さ推定と好意に、**符号違いの1本の経路**で載せる。詳細は `PLAN.md` の段階31 と決定事項#55

- [x] **31a**: 人間が人間を殺した瞬間、**視界内の全ノード**の (B) 強さ推定を更新する（`engine/witness.go`）
- [x] **重みは `ActObserve` 1回分より粗く**（`KillWitnessFactor` = 2）。**完了条件の `watchShare` は 0.34 で不動**——**重みを観察と同格（1）にしても不動**なので、#55 の心配は実測では起きない（`observe` は観測だけでなく**伝承の交換と相手を選べること**を買っている）
- [x] **記憶容量は既存の `Opinion` の出し入れルールのまま**。**これが規則の効き目の上限になった**——1件の殺害あたり 24.3 回の機会に対し、**取り込まれるのは 0.21**（`memFull` 0.95）
- [x] **31b**: 外敵を倒した側への**好意**を第三者にも（`AffinityWitnessKill` = 2）。**打撃を入れた者は除く**（参加ぶんは `AffinityHunt` が払っている）
- [x] **既知のリスク込みで測った**: `restNear` は −0.36 \*（**予想と逆に下がる**）、`friends` +0.08、個体数 −1.12 ± 4.97。**懸念していた経路は動かなかった**
- [x] **予想を先に書いた**——**3つとも外れた**（`priorErr` 下がらず・`killShare` 下がらず・`friends`/`restNear` 上がらず）
- [x] **実装前に発火頻度を数えた**（段階24・34 の教訓）。そこで**前提が半分崩れた**——殺害は素通りしていない。**戦闘中の観戦が20tickごとに視界内の全員へ読みを配っている**ので、段階31 が足したのは「戦いの最後の1回」
- [x] **目撃者スキャンを共通化した**（#60。`forEachWitness`。**共通なのは走査だけ**で、更新先も重みの単位も違う）
- [x] 触った文書: `NODE.md`（8節）、`PARAMETERS.md`、`HISTORY.md`、`CLAUDE.md`、`README.md`、指紋テストの数値
- [ ] **残した宿題**: **`killLearned` 0.21 は「ただで配られる情報は受け取り手が無い」という一般則**で、段階32（誘い）も同じ天井に当たりうる／**`AffinityTrust` の較正**（`trust` が1に届いてよいか）は段階32 の中で閉じる

### 8c. 段階32：外敵討伐への協力の誘い（保留#10 の再検討） 【完了・2026-09-08】
> **保留から出して、閉じた。** 再検討条件は満たされていたが、**答えは「作っても群れ狩りは現れない」**。詳細は `PLAN.md` の段階32 と決定事項#56

- [x] **行動語彙を1つ足した**（`ActInvite`＝「これを一緒に倒そう」。`cmd/devview` は `i`、線は緑）。**段階0 以来はじめての追加**で、番号は末尾に足した（保存済みの世界の番号を動かさないため）
- [x] **受けるかどうかは既存の効用比較の1候補。閾値はどこにも無い**——信頼できる相手が視界に0人なら誘いは「攻撃＋無駄な時間」なので必ず負ける、という形で自然に消える（テストで固定）
- [x] **好意は参加ボーナスではなく信頼**（`Affinity/AffinityTrust`）。効くのは2か所＝**勝率**と**相手が倒れるまでのtick数**（`SkirmishTicks` で打ち切られない見積もりになる）
- [x] **協力性遺伝子は作っていない。** 増えたのは語彙1つと好意の使い道1つだけ
- [x] **AIにも同じ語彙**（`addInvite`）。**プレーヤー専用ではない**
- [x] **事後の好意加点は新設していない**（`AffinityHunt` のまま）
- [x] **発火頻度を先に数えた**: `callShare` 0.01・`joinShare` 0.01（全決定の1%ずつ）
- [x] **予想を先に書いた**——**`packSize` は動かないほうに賭けて当たった**
- [x] **`AffinityTrust` の較正をこの段階で閉じた**（10 / 20 / 40 と上限0.5 のどれでも利得は誤差内＝**1に届いてよい**）
- [x] 触った文書: `NODE.md`（6節の語彙・7-3b）、`PARAMETERS.md`、`PLAN.md`、`HISTORY.md`、`CLAUDE.md`、`README.md`、指紋テストの数値
- [ ] **残した宿題**: **個体数 +12.83 \*\* の出どころ**は「同じ戦いを正しく安く値付けできるようになった」という読みで説明したが、**直接は確かめていない**（確かめるなら「味方を数えるが `exchange` には効かせない」腕）／**`shSpeed` +0.01 \*\* / `shVitality` −0.01 \***＝社会的な規則で予算比が動いた初めての例なので、次に予算比を読むときは覚えておく


### 9. 段階10：見た目からの学習 【完了・2026-09-03】
> 目的: 初対面の相手に `PriorStrength = 50` を当てはめるのをやめる。**独立しているので前後させてよい**

- [x] 遺伝子から観測可能な相関値 `Appearance` を作り `AgentView` に載せる（**真値は載せない**）— 体力遺伝子と移動速度遺伝子の平均 × 年齢補正。攻撃力は入れない
- [x] 各ノードが「見た目→能力」の写像を自分の戦闘・観戦から学習する — 最小二乗の直線1本、**傾きは0へ引き戻す**、**記憶容量は消費しない**
- [x] **完了条件**（2026-09-03）: 条件間ではなく**同じ実行の同じ遭遇**で3つの推定器を比べる形に測り方を変えた。学習の利得 +3.04 \*\*\*（50000tick では +8.07）、経験ありの観察者 26.6 対 経験なし 33.5、devview で目視
- [x] **申し送りを閉じた（2026-09-04）。2つは別々の話だった。**
- [x] **`rareTrough` の帯割れは解決済みで、しかも見た目とは無関係だった。** どの条件でも 0.62（`nolooks` も同じ）。段階12〜18 と見かけトリガーの修正で世界が健康になったぶん。**同じ段階で観測されたというだけで同じ原因とは限らない**
- [x] **「体つきを読んだぶんは11%」は 0% に悪化していた**（`slopeGain` +0.06 ± 0.06、相関 0.22 → 0.08）。学習そのものは6倍の価値になった（`learnGain` +3.04 → +18.00 \*\*\*）が、**体つきの寄与は消えた**。「世界が豊かになれば信号が強くなる」という期待は逆に外れた
- [x] **理由が分かった: `Appearance` の定義が予算制と噛み合っていない。** 体力と速さは「攻撃力を見せないため」に選ばれたが、**予算制ではその2本は攻撃力と同じ財布を奪い合う**。**隠すために選んだ変数が、まさにそのために予測力を失っていた**
- [x] **修正方法も実装・測定済み（`LooksShowBulk`）: 見えるものを「総量」にすると相関 0.08 → 0.50 \*\*\*、体つきの利得 0.06 → 1.24 \*\*\***（総量は見せても配分は隠れるので、攻撃力は依然として隠しパラメータ）
- [x] **それでも既定は変えない。** 推定の精度そのものは1ミリも上がらず（`priorErr` 18.53 対 18.56。傾きが「世界の平均を知る」ぶんを肩代わりするだけ）、**個体数を 8.8 \* 払う**。**買えるものが無いのに値段だけ払う変更**なので対照条件として残す

### 10. 段階12：伝承（等価交換）とヒントの進化 ← **集団創発の本命** 【完了・2026-09-04】
> 目的: 効用式のグローバル定数を個体ごとの値にして学習させ、**他個体と交換できるようにする**。伝えるのは effort そのものではなく**effort を決める前提**

- [x] **12a**（2026-09-04）: `retaliation` / `pAccept`（世界の事実）と `RiskWeight` / `CompetitionWeight` / `ShockRisk`（好み）を個体ごとの値にした（`engine/lore.go`）。**信念の学習は既定でオフ**——動かないからではなく動くから。真の殴り返し率は 0.15 で、`0.7` は実在しない報復への恐れとして世界を支えていた（知ると個体数が半分、しかも増えるのは殺しではなく餓死）。**好みの側は既定オン**（広がり 0.15、較正済み）で、これだけで理性への選択圧が負から0へ戻る
- [x] **12b**（2026-09-04）: 伝承を `ActObserve` に相乗りさせた。**双方向・同時の歩み寄り**だが、**1回は差の2%**——「中間で会う」0.5 は段階12a が作った多様性を消して個体数を半減させる（交換は1実行に19000回起きる）。好意の加点は動いた量に比例。垂直・水平は同じ経路
- [x] **12b**: 被参照数の分布を測った。上位5分の1が0.48（完全均等0.20、無作為の底0.22）＝**偏りは実在するがハブ化はしていない**ので相互占有は足さない
- [x] **12b**: **好意を効用の正の項にした**（`LoreValue`）
- [x] **12c ヒント**（2026-09-04）: 上限4本の枠（状況×行動×重み）。**加減点としてのみ働き、ヒント由来の分岐は無い**。**空の枠でも予算を食う**
- [x] **12c**: 突然変異は二段階（重み＝通常の変異／構造＝天才イベントの流用）。継承は枠単位の染色体式
- [x] **12c**: トレースに「ヒント由来の加点」を出した（`Utility.Hint`、devview では `hunch`）
- [x] **`LearningRate` を再測定した**（48シード）。**結論は変わらず既定 0**——誤差は 0.56 → 0.40 \*\*\* に下がるが個体数は 15.8 → 9.2 \*\*。段階12b の好意はまだ「よそ者への抑止力」にはなっていない
- [x] **完了条件**: 分散の指標を足した／多様性を定量化した／好意の A/B／**あるべき状態 B〜E を判定**（`fightRatio` 達成・`halfLife` 達成だが下限値・`gapRel` 未達）／被参照数／個体数と死因。**地域ごとの言い伝えの分布だけは段階14（地域という語彙）待ち**

### 10b. 段階12 から出た宿題（小さい・独立）
> 性能の2件は 2026-09-04 に完了。条件つきで残していた2件は 2026-09-05 に再判定して閉じた（**どちらも「仕組み」ではなく「物差し」の話だった**——片方は予算制のもとで絶対値を読んでいたこと、もう片方は密度で割る指標が分子と分母を同時に動かすこと）。代わりに**新しい宿題が1件**出ている

- [x] **`BenchmarkEngineStep` は絶対値が読めない**（2026-09-04 に修正）。`benchWindow`（400tick）ごとに世界を作り直す形にした（作り直しの間はタイマーを止める）。`-benchtime` を10倍振っても 747〜787µs に収まる。**性能を直す前に、性能を測る形を先に直した**
- [x] **`decay()` の性能**（2026-09-04 に解決）。**書いてあった原因は症状のほうだった**——`math.Exp` の96%は `weakestOpinion`（捨てる記録を探す全走査）から来ていて、しかも**71%の個体でその走査は必ず失敗していた**（捨てられる＝重み0の記録が、交換で好意が付いたことで消えたため）。記録の重みは0に漸近しても到達しないので**答えは二度と変わらない**。`Agent.noSpareMemory` にキャッシュし、記録が出入りしたときだけ破棄する。`Step` 940µs → 345µs、段階12b の持ち出しは 2.4倍 → 1.12倍。**伝承なしの側も速くなった**（386 → 308µs）＝この無駄は段階12b が作ったのではなく頻度を上げただけ。**指紋テストが無変更で通ることが挙動不変の証拠**
- [x] **「ヒントは頭の良さへの選択圧を下げる」は再判定で閉じた（2026-09-05、64シード × 40000tick）。** 絶対値では今も出る（`dIntelligence` は nohints − baseline で +10.55 \*\*\*）が、**頭の良さに固有の話ではなかった**——同じ符号が理性（+6.01 \*\*\*）にも出て、**`budget` そのものが +42.06 \*\*\*** 動く。ヒント枠は遺伝子に配る前の予算から引かれる（`world.go:1046` `fitBudget(genome, budget-hintCost)`）ので9本が一斉に小さくなる。**予算比で読むと差は1標準誤差**（`shIntelligence` +0.01 ± 0.01）で、一番動くのは頭の良さではなく速さ（`shSpeed` −0.02 \*\*\*）。**段階7c が「指標は絶対値ではなく予算比で読む」と書いていたとおり**、12c の観察は予算制のもとで絶対値を読んだ副作用だった
- [x] **閉じた（2026-09-08）: 既定は今のまま**（`HintSlots` = 4、`HintsSpread` = true）。48シードで測り直した——**ヒントを丸ごと消しても個体数は増えない**（+4.00 ± 6.78）が、**`killShare` は +0.03 \*\*\* と世界が荒れる**。**枠の値段は実在する**（`budget` +28.70 \*\*\*）。**中身が買っているのは世代**（同じ値段で重み0の対照に対し +0.42 \*\*）で、**模倣が買っているのは個体数**（模倣を切ると −7.71 \*、出生 −20.60 \*）。**消す理由が無いので消さない。** 旧: **12c の個体数の利得が消えた。** 同じ 64シードで baseline 対 `deadhints`（同じ値段・重み0）は **+0.20 ± 5.10 ＝ 誤差内**（12c では 22.00 対 14.75）。段階10c の申し送り「段階14〜18 の測定はすべて穴の開いた世界のもの」が**12c 自身にも当てはまった**。既定を変えるかは、ヒントが何を買っているか（`killShare` −0.04 \*\*、`starveRate` +0.19 \*\*）を読み直してから決める
- [x] **`gapRel` の再判定を終えた（2026-09-05）。未達のまま（0.83 ± 0.03、目標1.2）で、しかも地域差では動かない。** 段階14・15a・15b・15c を切っても入れても全条件が 0.78〜0.87 に収まる。**予想は逆に外れた**——食料の地域差を消した `noplantgap` のほうがわずかに高い（+0.05 \*）
- [x] **動かないのは測り方のせいではない**（`TestClusterGapsRelativeOfAnAvoidingLayout` を追加）。無構造な配置が 0.98、格子状に避け合った配置が **1.92** と読めるので、**1.2 は目盛りの中にある**。世界の側が届いていない
- [x] **世界にある唯一の「避ける」規則を4倍にしても動かない。** `hardrestexposure`（48シード）は `restNear` 5.17 → 2.54 \*\*\*、`nearest` +8.34 \*、`gap` +12.79 \* と**個体は確かに避けるようになる**（個体数を 15.2 \*\* 払って）のに、`gapRel` は +0.01 ± 0.04。**避けると集団の数も減る**ので、密度で割る `gapRel` は分子と分母が同時に上がって動かない。**あるべき状態E は今の規則の強弱では届かず、`gap` と `gapRel` は別のことを言っている**（`PLAN.md` の表に反映済み）

### 10c. 段階15 から出た宿題 【完了・2026-09-04】
> **性能の宿題のつもりだったが、世界で一番大きなバグだった。** 直すと `Step` が2.4倍速くなる（1010 → 419µs）だけでなく、**個体数がほぼ倍**（45.0 → 77.7 \*\*\*）、餓死率が4分の1（2.19 → 0.58 \*\*\*）、世代数1.5倍。理由は `ChoiceNoise`（20）が候補のスコア（5〜30）に対して大きいこと——**毎tick 考え直す個体はそのたびにノイズを引き直し、僅差の候補の間で振動して何も遂行できない**。`CLAUDE.md` が最初から書いていた「毎tick 再計画する個体は計画を遂行できない」に、2行のコードが穴を開けていた。**予想（性能と採食効率のトレードオフになる）は逆向きに外れた**
> **申し送り**: 段階14〜18 の測定はすべてこの穴が開いた世界で行われている。方向は対で取っているので保たれるが、**絶対値は今と違う**。既定オフにした段階17 の3つは、倍の余裕がある世界で測り直す価値がある

- [x] **`TriggerFoodInSight` がエッジではなくレベルになっている。** コメントは「Food came into view」（視界に**入った**）だが、実装は「視界に**ある**」:
>     if a.Action.Kind == ActRest || a.Action.Kind == ActMove {
>         if w.nearestFoodInSight(a) >= 0 { return TriggerFoodInSight }
>     }
>   食料の多い地域を歩く個体は**毎tick 再判断する**（前のtickにその食料を見て歩き続けると決めたばかりなのに）。段階15 で「良い地面へ向かう」個体が普通になった結果、**`Step` が同じ個体数・同じ密度で 2.4倍**（450 → 1100µs）になった。プロファイルの形は同一で回数だけが1.9倍。**段階15 が作った問題ではなく露出させた問題**なので、独立したコミットで測って直す（決定回数が減るので挙動が変わる）

### 11. 段階13：視界をグリッド単位にする（挙動変更） 【完了・2026-09-04】
> 7a と分けるのは、こちらが挙動の変更だから。**独立していて後回し可**

- [x] 視界を円から「自セル＋周囲1グリッド」に変更した（2026-09-04、`engine/sight.go`。面積を合わせて較正、`SightGrid=false` で円に戻る）
- [x] **視力は遺伝子にしない**（0リング致死・2リングやや悪い＝離散で非線形なので `StrategyDepthUnlock` と同じ轍。個体差を入れるならセルの大きさを連続に振る）
- [x] **完了条件**: `flees` / `escapeShare` を新設して測った。**逃走成功率は動かず**（面積を合わせると逃げ切る距離の平均まで揃う＝異方性は分散しか増やさない）、動いたのは殺し（`killRate` −0.13 \*）。機序は「**近いのに見えない相手**は攻撃の候補に挙がらない」
- [ ] **申し送り**: `Step` が +28%（345 → 442µs）。矩形での問い合わせとセル幅の導出直しで 520 → 442µs までは戻した

---

### 12. 地形の下ごしらえ（小さい・独立・いつでもよい） 【完了・2026-09-04】
> 目的: 速さが「体力と取引する」形になる唯一の見込みが地形。その前の下ごしらえ2つ。**速さは効いていないのではなく一番買われている遺伝子**（予算比0.230）で、足りないのは「速さが有利な場所と不利な場所」。地形の種類ごとの効き方と、ニッチが成立する条件は `PLAN.md` の同名の節

- [x] 食料レースの勝率 `pGet` に**速さを入れた**（2026-09-04）。到達時間の比に置き換え、相手の速さは見えないので世界の基準速度を仮定する（`RaceOnDistance` で旧ルールに戻せる）。**狙いは外れた**——`shSpeed` は動かない（+0.00）。**速さはすでに到達tick数を通じて移動コスト・時間コストに効いていた**ので、`pGet` は二次的な経路だった。この空振りで「足りないのは速さが有利な場所と不利な場所」という仮説のほうが確からしくなった
- [ ] **申し送り**: 1回目の実装（自分の effort 対 相手の全力）は**個体数を3分の1にした**。片側にだけ effort を入れると「ぶらぶら歩いて食べに行く」手が全滅する。相手が全力で走っているという仮定に根拠は無い（近くの他個体は寝ているかもしれない）
- [x] 移動コストを**地形照会関数**経由にした（2026-09-04、`engine/terrain.go`）。世界じゅうが平坦を返すので**挙動は完全に不変**（指紋テストが無変更で通ることが証拠）。**倍率は速度ではなくコストに掛ける**（速度を落とすのは effort がすでにやっている／コストなら荒れ地を速く渡ることが速さと体力の取引になる）。**コントローラは今も平坦だと思って計画する**——地形を `Perception` に載せるかは地形本体の段階で決める

---

## P4：環境と食料生態系（2026-09-03 追加）

> ここまでが個体（遺伝子・記憶・意思決定）を作る段階だったのに対し、ここからは**個体が置かれている場所に差をつける**段階。詳細と理由は `PLAN.md` の「環境と食料生態系（段階14〜18）」と決定事項#36〜#46

### 13. 段階14：休憩場所（既存の休息露出への相乗り） 【完了・2026-09-04】
> 目的: 「休むのに向いた場所」を世界に作る。**一番安い**（`RestExposureWeight` に係数を1つ掛けるだけ）ので、この群の最初に試してよい

- [x] **地域ブロック**を `World` に持たせた（2026-09-04、`engine/region.go`。4×3、壁ではない、地域IDを読むルールはゼロ）
- [x] 地域ごとの係数を `RestExposureWeight` に掛けた。`ShelterSpread = 0` で**乱数も消費せず1ビットも違わない**実行に戻る
- [x] `cmd/devview` に地域の境界と係数を描いた（＋右パネルに今立っている地面の係数）
- [x] **予想を先に書いた**
- [x] **完了条件**: **5つ中2つしか当たらず。** 選別は起きる（`shelterGain` +0.02 \*\*、強度に比例）が、**凝集は起きない**。個体数は不変
- [ ] **申し送り**: 効果が小さいのは**個体が場所を選べない**から（今いる場所しか感じられない）。**段階15b（地域の豊かさの推定）のほうが効く見込み**で、今回の小ささはその必要性の証拠

### 14. 段階15：植物食料の地域差（固定版）と、地域の豊かさの認識・伝承 【完了・2026-09-04】
> **15b/15c も同日に完了。** 一番大きな成果は**集団を1か所に集めたのが伝承だった**こと（自分の経験だけでは `nearest` は動かず、伝承を入れると 140 → 100 \*\*）。**`shMemory` への正の選択圧は出ず**、`PLAN.md` が予告していた疑い先（地域12個では容量が制約にならない）が当たった
> **15a は 2026-09-04 に完了**（`FoodSpread`。総量を変えず分布だけ変える。`rareTrough` が 0.25 → 0.40 \* とあるべき状態A の帯に復帰し、段階14 で起きなかった凝集が起きた。「豊かな地域に集まる」の6割は食料・4割は社会的）
> 目的: 場所によって食べ物の量が違う世界にし、それを**覚えて・伝えられる**ようにする。**15c は段階12b が入っていないと載る先が無い**

- [ ] 15a: 地域ごとの `FoodSpawnRate` 倍率。全地域 1.0 で元に戻ること。**総量は保存する**（変えると個体数が動いて交絡する）
- [ ] 15a: **`FoodSpawnRate` は `CompetitionWeight` / `SkirmishTicks` と同格に敏感**。個体数だけでなく**地域ごとの選択圧と `killShare`** を見る
- [ ] 15a: **集団形成の動機だけを切った対照条件**と比べる（段階11 の `PreyValue=0` と同じ要領）。**「豊かな地域は集団で守られる」を仮定しない**——内側が平和で境界が荒れる形もありうる
- [ ] 15a: 外敵が獲物の濃度に引かれるので、**捕食圧も豊かな地域に連動して集まる**はず。種族ごとの分布を測る
- [ ] 15b: 地域の豊かさの推定（ノイズは既存と同じ `(100−理性)/100`）。鮮度は #22 の遅延減衰＋接触リセットを流用。**新しい知覚も新しい鮮度管理も作らない**
- [ ] 15b: **相手ごとの記憶容量の枠には入れない。ただし記憶容量遺伝子が精度・更新速度に効く**（段階9 で出なかった `shMemory` への初めての正の選択圧になりうる）
- [ ] 15c: 等価交換の学習パラメータに**地域IDごとのベクトル**を足すだけ。垂直伝承は**育児期間の共在**に相乗り（「教える」行動は作らない）
- [ ] **完了条件**: `LearningRate = 0` との対で経験と伝承を分離／地域ごとの推定値の分布を出せること

### 15. 段階16：食料の栄養バランス 【完了・2026-09-04】
> **予想（効果はほとんど出ない）は個体数・`killShare`・死因については当たり。ただしゼロではなく、群れ狩りを初めて有意に増やした**（18.5 → 23.2 \*\*）——**一般則（同じものばかりは損）が、完了条件に書かれていた専用の仕掛け (a)「肉に独自の栄養」の弱い版として働いた**

- [x] 直近の消費履歴で `FoodNutrition` を一時的に割り引く（`engine/diet.go`）。**新しい状態量は作らず、隠さない**
- [x] **他の変更と混ぜていない**
- [x] **予想を先に書いた**（`PLAN.md` の事前予想が、実装中に上乗せした私の予想より正確だった）
- [x] **完了条件**: 小さいことを確認した。**(a) は一般則が弱い版として果たしていた**ので、本番は (b)（段階17）待ち
- [ ] **申し送り**: **強い罰（0.8）は逆効果**（世界が痩せて狩りに出る余裕が消え、群れ狩りの増加が消える）。`CompetitionWeight` と同じ「上げると崩れる」型

### 16. 段階17：植物の遺伝と種子散布 【実装・測定完了、既定オフ・2026-09-04】
> **17a・17c は 2026-09-04 に実装・測定完了、既定はオフ。** 動かないからではなく**世界を壊すから**（個体数 44.2 → 14.9）。**短い散布は地域を空にし（吸収状態）、長い散布は地理を学べなくする**という逃げ道の無い2つの失敗で、パラメータでは解けない。**段階15 の固定版は置き換えられる下地ではなく、集団が乗っていた土台だった。** 復活の条件は `PARAMETERS.md` C-5i。**17b も 2026-09-04 に実装・測定完了、既定はオフ**（個体数が9分の1）。**`shRationality` が段階9 以来はじめて上がった（0.07 → 0.11 \*\*\*）が、信号を読めなくしても同じだけ上がる**＝上げているのは世界の厳しさで、`PLAN.md` の予想は数字が当たって理由が外れた。**正直な警告は進化しない**（毒にも信号にもコストが無いので足場が無い）

- [ ] 17a 風散布: 植物に軽量な遺伝子（散布半径・再生速度）だけを持たせる。**9本の予算制アーキテクチャは持ち込まない／`Decide()` も作らない**（繁殖は既存の食料湧きと同じ確率イベントに粒子的遺伝を相乗り）
- [ ] 17b 毒と信号: 遺伝子2本（毒の強さ／**警告信号の強さ**＝匂いと目立ちやすさをまとめたもの）。**2本を相関させない**。毒は隠しパラメータ、推定は `(100−理性)/100 × ノイズ`
- [ ] 17b **予想を先に書く**: `shRationality` が上がるはず（理性に食料側の役割が初めて付く）。毒と信号の相関を世代ごとに観察（**ベイツ型擬態が出なくても失敗ではない**）
- [ ] 17c 摂食散布: 既存の `eat` に相乗り（**新しい行動も「付着」状態も作らない**）。**最後に切り出して単独で測る**（採食効率に直接効く）
- [ ] **監視**: 植物の**地域ごとの局所絶滅**を `CensusTracker` / Trough と同じ発想で見る（0 は吸収状態）

### 17. 段階18：夜型・朝型（体内時計） 【完了・2026-09-04】
> 目的: 活動時間帯が個体ごとに違えば、**見張り役を割り当てずに**夜通しの警戒が生まれる。**段階12 より後。段階14・15 とは束ねない**

- [x] **世界に周期的な時間帯を新設した**（2026-09-04、1日＝1000tick）
- [x] 体内時計を持たせた。**予算遺伝子にはしない**（量ではなく円周上の位相なので）
- [x] **時刻で変わるのは休息の回復量だけ**と決めた（視界や食料だと全員に同時に効く世界の変化になり、問いが変わる）
- [x] **完了条件**: 測ったところ**指標は最初から飽和していた**（どの条件でも 0.00〜0.01）。ハッザ族の観察は体内時計の前から成り立っていた
- [x] **実際に動いたのは個体数**（+12.6 \*、これまでで一番良くした段階）。**機序は「眠っている友人は見張れない」という条件1つ**で、`SleepingWatch` の対照で確定

### 18. 地形全般（川・高低差・スロープ・穴・崖）→ **P5 の 20 へ移した**（2026-09-05）
> ゲーム化の一部（管理者が地形を配置する）として扱うほうが筋が通るため、**P5 の 20. 段階20** に節を移し、そこで実装・測定する。**この群の最後**であることと、上の 12（地形の下ごしらえ）が前提であることは変わらない

---

## P5：ゲーム化とマップエディタ（2026-09-05 追加）

> ここまでが「世界のルールを作る」段階だったのに対し、ここからは**その世界を人が触れるようにする**段階。詳細と理由は `PLAN.md` の「ゲーム化とマップエディタ（段階19〜22）」と決定事項#47〜#50

**決めた順序: 19（人間操作）→ 20（地形本体）→ 21（保存/読込）→ 22（エディタ）。理由は4つ。**

1. **19 を先頭にするのは、19 だけが世界の状態の形に依存しないから。** 触るのは `Controller` という境界だけで、20〜22 で世界に何が増えても作り直しにならない。しかも**ゲーム化の一番の不確実性**（`Perception` の情報量と行動語彙で人間が遊べるか）を、地形もエディタも無い**今の平坦な世界のまま**確かめられる。ここで「行動語彙が足りない」と分かれば、それは 20〜22 より前に効く発見になる
2. **20 を 21・22 より前に置くのは、地形の未決が器の形を決めてしまうから。** 高台の多層構造をどう表現するか（平面に高さの軸か、別レイヤーか）は、そのまま「21 が何を保存するか」「22 が何を編集するか」になる。ルールが固まる前に器を作れば、ルールを直すたびに器も作り直しになる（#49。段階17 で「固定版は置き換えられる下地ではなく、集団が乗っていた土台だった」と分かったのと同じ轍）
3. **21 を 22 の前に置くのは、エディタが編集した地図を残すことが、そのまま保存/読込だから。** 逆順にすると**エディタ専用の保存形式**を作ることになり、#48（3つの要件のために別々の機構を作らない）に反する
4. **測定を伴うのは 20 だけ。** 19・21・22 は世界のルールを1つも変えないので `cmd/experiment` の対を取る必要がなく、受け入れ条件は**指紋テストが無変更で通ること**になる。この違いが、20 を単独のコミット・単独の測定サイクルにする理由

**P5 に入る前に P4 の宿題（10b の新しい宿題・段階13 の性能の申し送り・段階17 の測り直し）を閉じる必要は無い。** どれも小さく独立していて、19・21・22 とは触る場所が重ならない。ただし **20（地形本体）の測定に入る前には、既定の `Config` を動かす宿題（ヒントの既定をどうするか）に決着をつけておく**——ベースラインが動くと地形の測定をやり直すことになる。**この注意は 29・30 にもそのまま当てはまる**（測定を伴う段階の前に既定を動かさない）。

### 順序の追記（2026-09-06・段階20 の続き2つが入ったので）

**決めた順序: 29（地形コストの認識と伝承）→ 30（高台の遮蔽と、それを事実として学ぶこと）→ 21（保存/読込）→ 22（エディタ）。理由は4つ。**（**2026-09-06 の追記**: この4つの前に **31・32（P3 の「順序の追記」）** が入りました。**地形は既定オフなので2つの群は交絡せず**、遊ぶための世界を先に良くしたいなら 29 を前に出しても測り直しは要りません）

1. **29・30 を 22 より前に置くのは、#49 の理由がそのまま生きているから。** 段階20 の結論は「**地形は個体数を払って、静かさと混ざりにくさを買う**」で、既定は平坦のまま。この状態でエディタを作ると、**「置くと世界が悪くなるもの」を配置する道具**になる。地形が持てる意味（**選べること**＝29、**高いところに値打ちがあること**＝30）を先に確定させてから器を作る
2. **29 を 30 より先に置くのは3つの理由から。** (a) 29 は**段階20 の測定が名指しした欠けている半分**（選ぶ理由）を、**既存の仕組み（`engine/regionlore.go`）に1本足すだけ**で埋める、**一番安い実験**。安い診断を先に回すのは掃引と同じ習慣。(b) 30 は**設計の未決**（攻撃者との高さの差分＝これまでの「足元だけ見ればいい」から初めて外れる）と、**段階12a の前例**（戦闘に関わる事実を正確に学ばせると個体数が半減しうる）の2つのリスクを抱えている。(c) **読みやすさ**——地形が今の値段（rough −16.8 \*／country −48.2 \*\*\*）を払い続けたままだと、**30 の効果がその代償に埋もれる**。29 で値段が下がってから測るほうが `killShare` と `highGap` の動きを切り分けられる
3. **29b と 30b を同時に測らない**（伝える中身が別の仕組みだから。`PLAN.md` の「地形の続き」の節）。順番に並べれば自然に守られる
4. **割り込ませてよいのは 21 だけ。** 21 は世界のルールを1つも変えず、受け入れ条件も指紋テストの形なので、29・30 のどちらの後に置いても成立する。**むしろ後に回すほど保存対象が固まっていて安い**（29・30 はどちらも engine の状態を増やす）。**22 は割り込ませない**——理由1 のとおり。**遊べるビルドを先に配りたくなったときに前に出せるのは 21、出してはいけないのは 22**

### 19. 段階19：人間操作コントローラと血統乗り換え（devview）【完了・2026-09-05】
> **`Perception` の情報量も行動語彙も足りていた。足りなかったのは「取り消し」**——AIは訊かれれば全候補を採点し直すので古い計画から自力で抜けられるが、**据え置きの命令は自力で抜けられない**。そこで命令が流れる場合を2つ作った（狙いが消えた／身体が変わった）。**engine のルールは1つも変わっていない**（指紋テストが無変更で通る）

- [x] `HumanController`（`engine/human.go`）。engine から見て AI と区別がつかない
- [x] devview: `h` 乗り移り、クリックで狙い、r/m/e/a/f/o/c、`1`〜`5` effort、`s` 構え、Enter 今すぐ考え直させる、`k` 後継の指名。3枚目のパネル（Tab）が操作画面
- [x] **トリガー方式との噛み合わせ**: 命令は「次に訊かれたときの答え」、即時反映は `World.RequestDecision` →既存の `TriggerRequested`。**人間のためのハードコード分岐はゼロ**
- [x] **プレーヤーにも `Perception` の外は見せない**（#47）。狙えるのも視界の中だけ（`Order` が断る）
- [x] 血統乗り換え（`World.Heirs` ＝ 生きていて成人した子。`Agent.IsAdult` は `CanReproduce` と同じ線）
- [x] **完了条件**: (1)(2) を実地で確かめ、(3) 指紋テストは無変更で通る
- [ ] **申し送り**: **`RequestDecision` は撃ち放題**。押した回数だけ考え直せるので、**毎tick 押す操作は 10c で塞いだ穴を人間の手で開け直せる**（`ChoiceNoise` を引き直し続ける個体は何も遂行できない）。遊ぶ段になったら間隔か価格を決める——**AIの側に無い自由をプレーヤーに与えている唯一の場所**
- [ ] **申し送り**: 操作ノードがペアになると `stepPaired` の間は操作できない（既存のルール）。今は画面に何も出ないので、遊ぶ段で「今は結ばれているので手が出せない」と出す

### 19b. 段階19 を遊んで出た宿題（小さい・独立）
> devview で実際に操作して出たもの。engine のルールには触れていない

- [x] **長い実行は二山になる**（2026-09-05 実測、16シード × 100000tick）。**これは直すものではなく、測るときに守る注意**として残す。**13回は 131〜193 に増えるが、3回は序盤の谷から戻らない**（5・34・66。出生が 940〜1649 対 60〜205）。**個体数が一桁になると視界130 の世界では配偶者に出会えない＝0 でなくても吸収状態。** 数値は `HISTORY.md` の同日の項。**段階20 以降で 100000tick の比較をするならシードを増やす**（20000〜40000tick は分岐前なので影響を受けにくい）
- [x] **`RequestDecision` の撃ち放題を塞いだ（2026-09-08）**。**間隔にした**（`cmd/devview` の `askEvery` = 15tick。**世界が普通のノードに訊く間隔が14.5tick なので、人は世界と同じだけせっかちでいられて、それ以上にはなれない**）。**命令キーは制限しない**——**命令は新しい情報**なのですぐ反映されるべきで、**何も足さないのは「考え直せ」のほうだけ**。断るときは画面に残りtickを出す。**engine には触っていない**（トリガーの値段は世界の話、人が引ける頻度は遊びの話）

### 19c. 段階23 の申し送り（小さい・独立）

- [x] **地形を入れて測り直した（2026-09-08、6シード × 20000tick）**: 平坦で **15.0 tick に1回**（段階23 当時は 14.5）、遊ぶ地図（`country`）で **17.6 tick に1回**。**動く向きは予想と逆で、地形があると訊かれる回数は17%減る**——**世界が空くから**（個体数 86 → 32 で、視界に入る食料も相手も減る）。**転機だけに絞ったモードは 112 → 130 tick 前後**の見当になり、**「一生で20〜30回」の設計はそのまま成立する**ので調整は不要
- [ ] **節目の4つ（乗り換え・成人・ペア成立・出産）は決め打ち。** 遊んでみて多すぎ／少なすぎなら足し引きする。engine 側は合計と上限しか守っていないので、`cmd/devview` の `milestone()` を触るだけで済む
- [ ] **性格の4択も決め打ち**（用心深い／気が短い／命を惜しむ／親のまま）。engine はどんな比率でも受けるので、選択肢の設計は遊びながら詰める
- [ ] **人が選んだ性格が世代を越えてどう効くかは測っていない。** 好みは粒子的遺伝で子に渡るので、**プレーヤーの血統だけ好みが偏っていく**はず。血統を追う測定は今は無い

### 20. 段階20：地形本体（川・高台・スロープ・積み上げ可能な高低差）【完了・2026-09-06】
> **P4 の「18. 地形全般」をここへ移した**（同じもの）。**この群で唯一、選択圧と個体数が動く段階**なので、単独のコミット・単独の測定サイクルにする。上の「12. 地形の下ごしらえ」は済んでいる

- [x] **エディタより先にルールを入れて測る**（#49）。決め打ちマップ4種（`rough` / `river` / `plateau` / `country`）で `cmd/experiment` と `cmd/devview` の両方を回した
- [x] **1種類ずつ測った**（荒れ地・川・高台をそれぞれ単独の腕にし、倍率も1.5／2／4 で掃引）。**障害物・隘路・遮蔽は入れていない**（下の残りへ）
- [x] **決めた2つ**: 高さは**平面の上の軸**（別レイヤーにしない）／知覚は**足元だけ**で **AIと人で分けない**
- [x] **予想を先に書いて、外した**。`speedGap` は倍率1.5・2・4 のどれでも 0.00（ニッチは出ない）。**個体は荒れ地を避けない**（`onDear` は地図に占める割合そのまま）——**足元しか分からないので避けようがない**。**速さは逆に買われなくなる**（倍率4で `shSpeed` −0.04 \*\*\*）。**並び替えが出たのは高さのほうだけ**（`highGap` +0.02 \*\*）
- [x] **監視**: 絶滅は0。ただし**個体数の値段は大きい**（rough −16.8 \*／plateau −37.9 \*\*\*／country −48.2 \*\*\*）ので**既定は平坦のまま**。一方 `killShare` −0.06〜−0.20 \*\*\* と `halfLife` +27〜+32 \*\*（**地形が初めて「混ざりにくさ」を作った**）
- [x] 触った文書: `NODE.md`（11-5）、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`
- [ ] **残り（この段階から出た宿題）**: **地面を選べるようにする**（通った地面を覚える／食料を地形に結びつける。前者を先に測る）、**障害物・隘路・遮蔽**（障害物は経路の仕組みとセット、遮蔽は視界とセット）

### 20b. 段階29：地形コストの認識と伝承（段階15b/15c の再利用）【完了・2026-09-06】
> **結果**: 避けるようにはなった（`onDear` 0.48 → 0.42、−0.05 \*\*、`clumping` +0.11 \*）が、**個体数は戻らず `speedGap` も 0.00 のまま**。**重みは頭打ち**（2 で飽和、4 は個体数だけ −11.5 \*）。**29a 単独が一番悪い**（−13.3 \*）＝**未知を「普通」と見なす既定値が、学ぶことを損にしていた**。**次の一手は食料を地形に結びつけること**（下の新しい宿題）
> 目的: 段階20 の「**個体が地面を選べないので住み分けは起きない**」を埋める。**前例は 14 →15b →15c**——**良い場所があるだけでは凝集せず、自分の経験だけでも凝集せず、伝承が入って初めて「行ったことのない場所へ向かう」が選択肢になった**。詳細は `PLAN.md` の段階29 と決定事項#53

- [ ] **29a**: 地域ごとのベクトルに**「その地域の平均移動コスト倍率」を1本足すだけ**。荒れ地・水・高さを別々の信念にしない
- [ ] **減衰は無し（減衰率0で同じ関数を使い回す）**。地形は静的な事実だが、将来動的になったとき（浸食・エディタ）にすぐ有効化できる形にする
- [ ] **粒度は既存の12ブロック**（`TerrainMap` の解像度に合わせない）。**今のコントローラは経路計画を持たない**ので、粗い信念が使いこなせる天井
- [ ] **記憶容量の枠には入れない**（#41 を踏襲）
- [ ] **29b**: 伝承は `exchangeLore` に相乗り（15c と同じ）。**伝承だけ切った対照と対で測る**
- [ ] **予想を先に書く**: 個体数は段階20 の代償の一部を取り戻す／`clumping` 上昇・`nearest` 低下／**`speedGap` が初めて動くか**。**そして `killShare` を必ず一緒に見る**——良い場所に集まりすぎると競合が増えて逆に上がりうる（「片方だけ良くなっていないか」）
- [ ] **監視**: 荒れた地域を完全に空にしていないか（吸収状態）。個体数と地域ごとの分布
- [x] 触った文書: `NODE.md`（8-1 の続き）、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`
- [ ] **新しい宿題（この段階から出た）**: **食料の分布を地形に結びつける**。**避ける理由と食べる理由が別々の地図に乗っているかぎり、重みでは頭打ちを越えられない**（避けることは常に食料を捨てること）。**交絡するので測り方を先に決める**——地形あり・食料一様／地形あり・食料が地形に従う、の対で測る
- [ ] **新しい宿題（小さい）**: **完了条件に「測る前から答えが分かる指標」を置かない**。`regionCostRank` は 1.00 で飽和した（地面は静的、足元は正確）。**信念が正しいことと、役に立つことは別**

### 20c. 段階33：食料の分布を地形に結びつける（段階29 の頭打ちの原因を切り分ける）【完了・2026-09-06】
> **結果**: **頭打ちは交絡だった**（`onDear` 0.42 → 0.32 \*\*\*、個体数 +8.0 \*、餓死率 −0.20 \*、`killShare` 不動）。**それでも `speedGap` は 0.00**＝**「地形は速い個体と遅い個体を選り分けない」はこの世界の事実**。**食料だけで半分・信念だけで半分・両方で −0.10**。**弱い相関は中間ではなく一番悪い**。**既定は0**（風景の性質なので地図の作者が決める。devview の `-terrain` は1を渡す）
> 目的: **段階29 の「避けるようにはなるが半分にはならない」が、交絡のせいか本当の限界かを決める。** 新しい能力を足す段階ではなく、**開いたままの診断を閉じる段階**。詳細は `PLAN.md` の段階33 と決定事項#57

- [ ] **世界の真値どうしを相関させるだけ**（`Config` に符号つき1つ）。**豊かさの信念とコストの信念はすでに別々にあるので、個体は既存の効用比較を通して自分で見つける**。**相関を覚える・伝える第三の仕組みは作らない**
- [ ] **総量は変えず分布だけ**（段階15a の原則）。相関のあとで再正規化し、**テストで固定**する
- [ ] **粒度は段階29 の地域単位のコスト**（`regionMeanCost`）にそのまま乗せる
- [ ] **平坦な世界は1ビットも変わらないこと**（地図が無ければ乖離ゼロ）。指紋テストが証拠
- [ ] **正の相関（荒れた地域ほど痩せている）だけを先に測る。** 負の相関は**頭打ちを意図的に強めるもの**なので混ぜない（正で解けることを確認したあとの独立条件）
- [ ] **予想を先に書く**: `onDear` が 0.42 の頭打ちを超えて下がる／個体数の落ち込みが緩和される／`clumping` 上昇／**`killShare` は上がりうる**（良い地域が豊かさと安全さを兼ねるので競合が集中）／**`speedGap` が動けば「住み分けはコストではなく、コストと食料が同じ地図に乗っていることから生まれる」と言える**
- [ ] **監視**: 「荒れていて痩せた地域」が完全に空にならないか（吸収状態）
- [x] 触った文書: `NODE.md`（8-1 の続き）、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`
- [ ] **残した宿題**: **負の相関（荒れた地域ほど豊か）**を独立条件として測る（狙いが違う。段階30 と同じ群）／**`speedGap` が動かないと確定したので、「速さと体力の並存」（段階7c の完了条件）を地形に期待するのはやめ、別の経路を立てるか条件を取り下げるかを決める**

### 20d. 段階30：高台の遮蔽（回避率）と、それを世界の事実として学ぶこと【30a 完了・2026-09-06／30b は「作らない」で決着・2026-09-08】
> **30a**: 規則は効く（`evadedShare` 0.04 → 0.06 \*\*\*、世代 +0.71 \*、餓死率 −0.39 \*）が、**`onHigh` は動かない**＝**誰も高台を目指さない**。**倍率版は何も動かさなかった**——回避は構えの1チャネルで戦闘中しか持たないので、**倍率はゼロに掛けていた**（発火頻度は「状況」ではなく「掛ける量が非ゼロの頻度」を数える）。**30b は先に決めた2つ（#53 の1スカラー／段階20 の足元だけ）のどちらかを取り消さないと動かない**ので、判断ごと宿題に残した
> 目的: 対応表の「高台＝到達経路の制限＋**遮蔽**」の後半。段階20 は移動コストと到達可否の2つに絞った回なので、これはその続き。詳細は `PLAN.md` の段階30 と決定事項#54

- [x] **30a（完了）**: 遮蔽そのもの。**床として実装**（倍率では効かない）。**世界は少し良くなるが居場所は変わらない**
- [x] **30b の判断（2026-09-08）**: **どちらも取り消さない。「作らない」で閉じる。** (a) は **#59 の改訂ですでに許されている**（信念は地面の種類ごとではなく**帰結の種類ごと**＝遮蔽は3つ目の帰結）ので、残っていたのは**許可ではなく値打ちの問題**。その値打ちは**段階35 が同じ形の機構で測ってゼロ**（信念は相関0.77 まで正確に育ち伝わり効用式に載ったのに `onWater` は動かず、4つの説明を潰した）。**`onHigh` は食料の相関でも動かない**（`plateau` 対 `plateaulink`、24シード、−0.01 ± 0.02）＝`onWater` を動かせた唯一の梃子もここでは効かない。(b) は**もっと高い**（知覚のホットパス＋経路計画を持たないコントローラ）のに、(a) の強い版が動かなかった以上、根拠が無い。**再開の条件**: どこかに立つことが**食えると安全を同時に払う**規則が入ったとき（段階36 を高さでやる形）
- [x] 30a の設計メモ（相手との差分を初めて見る／戦闘中の行動は既存の閉じた式にそのまま乗る／発火頻度を先に数える）は 2026-09-06 に消化済み
- [x] ~~**30b（後・別条件）**: 「高い場所は安全だ」を `engine/lore.go` 側の**世界の事実**として学習・伝承させる（#54）~~ → **作らない**（上の判断）。**#54 の「lore.go 側に置く」という置き場所の指定も、これで使われないまま閉じる**——場所に紐づかない事実は、どこへ向かえばよいかを言えない、というのが 30a が残した結論そのもの
- [x] ~~**既定オンだと決めつけない**~~ → 決める対象そのものが無くなった

### 21. 段階21：世界・ノードの保存/読込（汎用シリアライズ基盤）【完了・2026-09-06】
> **乱数は「種と引いた回数」で保存し、読込時に引き直す**（生成器を差し替えると記録済みの測定値が再現できなくなるため。5000万回で0.2秒）。**受け入れ条件は指紋テストと同じ形で通した**。**個体群が世界をまたげるのは「その身体が何であるか」だけ**
> 目的: 「他所で育ったパラメータの読込」「進めた世界の配布」「天才の配置」を**同じ1つの機能**として実装する（#48）。`protocol` そのものではなく、**その前段の実地検証**

- [x] `World` / `Agent` / `Genome` / 記憶 / 好み・言い伝え / ヒント / 地域 / 植物 / 地形 / `tick` の出し入れ（JSON）
- [x] **乱数**: 種と引いた回数を保存し、読込時に引き直す。**`countingSource` は流れを変えない**（指紋テストが無変更で通る）
- [x] **受け入れ条件**: 保存 → 読込 → **500tick 走らせて**保存しなかった世界と一致（`engine/save_test.go`）。**ファイルどうしは比べない**——保存漏れは保存の中に無いので見つからない
- [x] **ノードだけの経路**（`Nodes` / `SaveNodes` / `LoadNodes` / `Repopulate`）と線引き: **持ち込めるのは身体が何であるか**（遺伝子・性別・種族・好み・信念・ヒント・体内時計）、**他人の名前を含むものは置いていく**
- [x] `cmd/devview` に `F2`（保存）・`-load`・`-nodes`。保存した世界の続きを実際に動かして目視確認
- [ ] 天才の管理者配置は、**段階22 のUIが揃ってから**足す軽い機能。単独では着手しない
- [x] 触った文書: `README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`

### 22. 段階22：マップエディタv0 とパラメータ調整UI【完了・2026-09-06】
> `cmd/devview` の1モード（`F3`）。**engine は `engine/edit.go` の4操作だけ**（`Endow` と同じ立場・乱数を引かない）。**6つのルールに測定済み範囲を添えて警告する（止めない）／難易度は予算だけ／エディタ専用形式は作らない**
> 目的: 地図を設計する体験の最小版。編集対象（地域の係数）は段階14・15 ですでにあり、地形は段階20 で入っている

- [x] 地域（4×3）の係数と、段階20 の地形を地図上で編集する（10種類のブラシ。**地形と地域は別の地図で、片方を塗っても他方は動かない**）
- [x] 難易度＝予算。**`kind` / `measured` / `hard` の3プリセット**で、**`measured` は `HISTORY.md` の全数値が取られた世界そのもの**
- [x] **6つに絞り、測定済み範囲を併記し、外れたら警告する（拒否はしない）**——動かない世界を作る自由は管理者のもの
- [x] **速度に `headlong`（1フレーム64tick）**を足した＝時計と描画の分離。既存の速度切替の延長
- [x] 保存・読込は段階21 のまま（**エディタ専用の形式は作っていない**）
- [x] 触った文書: `README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`
- [x] **天才の管理者配置（2026-09-08 完了）**: `World.Inspire(id)` ＝ **天才イベントを手で起こす**。**やることは天才の出生と同じ2つだけ**——**枠を1つ増やし**（`Endow` と同じく**新しい予算で払う**ので、遺伝子から黙って取らない）、**すでに持っているスキルを跳ね上げる**。**無いカテゴリは発明しない**（天才の出生と同じ）。**乱数を1つも引かない**——`drawHint` で新しい思いつきを引くと**編集が世界の次の一手を変えてしまう**ので、**決定的な半分だけを実行し、空いた枠は誰かの思いつきが埋める**（`exchangeHints`）。`cmd/devview` のエディタで `g`

---

## P6：水辺と技能（2026-09-07 追加）

> Cowork 側の2つの設計相談（水辺の危険と伝承／スキルシステム。2026-09-07）を段階に落としたものです。詳細と理由は `PLAN.md` の「水辺の危険と伝承（段階34〜37）」「スキル（段階38）」と決定事項#58〜#64

**決めた順序: 31（目撃の伝播）→ 32（協力の誘い）→ 34（溺死）→ 35（溺死の周知と場所の危険）→ 36（水辺の豊かさ）→ 37（集落分化の観測）→ 30b の判断 → 38a（最初の1種類のスキル）→ 38b（残りを1つずつ）。理由は6つ。**

1. **31・32 を先頭に残すのは、前回の順序の理由がそのまま生きているから。** 「群れ狩りは現れるか」は**段階11 以来ずっと開いたまま**で、**あるべき状態 B〜E** というこのプロジェクトの採点表そのものに載っている。**水辺は地形の上に建つので既定オフの世界では1ビットも動かず**（`TerrainMap` が空なら水のセルが無い）、31・32 のベースラインを動かさない。**逆も同じ**なので、**遊ぶ世界を先に良くしたいなら 34 を前に出してよく、測り直しは要らない**（29 のときと同じ扱い）
2. **31 を 35 より先に置くのは、共通化を2つ目の実例が出てから行うため**（#60）。両者は「視界内のノードを集める」処理が同じ形になるが、**先に汎用の目撃者スキャンを設計すると、実例1つでは正しい抽象化か検証できない**。**31 を具体で書き、35 のときに共通部分だけを切り出す**。これは水辺の文書自身が「最初から汎用ハザード表を作るな」と書いているのと同じ理由
3. **34〜37 の内部順序は動かさない**（相談で指定された順）。**34 は既存の地形アームの再ベースラインを兼ねる**——水の意味が変わるので、**段階20・29・33 が `river` / `country` で取った数値は 34 のあとに読み直す**。**35 と 36 を同じ回に入れない**のは、一方が信念で他方が世界の側の分布だから（混ぜると「避けるようになった」のか「行く価値が増えた」のかを分けられない）。**37 は実装ではなく観測**（#61）なので必ず最後
4. **30b を水辺の群のあとに置くのは、35 がその判断の前例を作るから。** 30b は「**#53（地形の信念は1スカラー）を取り消すか、段階20 の（知覚は足元だけ）を取り消すか**」で止まっている。**35 は危険という2本目を足す＝(a) 側を実際に払ってみることなので、その値段を見てから決めるほうが安い**（安い診断を先に回す、という掃引と同じ習慣）。逆順にすると **30b の測定基準が 34〜36 で動く**
5. **38（スキル）を最後にするのは3つの理由から。** (a) スキルは**ヒント枠に乗る**ので、**10b の宿題（ヒントの既定をどうするか）を先に閉じる必要がある**——「測定を伴う段階の前に既定を動かさない」の同じ形。(b) **枠は空でも予算を食う**（段階12c）ので、9本の遺伝子の水準が一斉に動き、**予算比で読み直しになる**。他の段階の測定と同じ回には入れられない。(c) **泳ぎは段階34 に依存する**
6. **最初の1種類は「荒れ地耐性」にする**（相談の候補は荒れ地耐性か高台上り）。**理由は3つ**——(a) **段階33 が残した宿題に直接答える**（「`speedGap` が動かないので、速さと体力の並存を地形に期待するのはやめ、別の経路を立てるか条件を取り下げるかを決める」＝**スキルがその別の経路そのもの**）。(b) **`onDear`（荒れ地に立つ割合）という較正済みの指標がすでにある**（0.48 → 0.42 → 0.32）ので、効いたかどうかを既存の物差しで読める。(c) **高台上りは 30b の未決に絡み**（どこが高いかを知る手段が無いと目指せない）、**泳ぎは 34 に依存する**ので、どちらも前提を抱えている

**この群に入る前に閉じておくこと**: **10b（ヒントの既定）**は 38 の前提（上の理由5）。**34 の前に閉じる必要はない**——水辺は地形の上に建ち、ヒントの既定は平坦な世界の値なので交絡しない。

### 38a. 段階38a：最初の1種類のスキル（荒れ地耐性）【完了・2026-09-08】
> **結果**: **知識は社会的に育つが、居場所は変わらない。** 名目 0.80（全部入り）／0.54（模倣なし）／**0.50（跳躍もなし＝出生地の値そのもの、ばらつきゼロ）**＝**跳躍が作り、模倣が配る**。`skillGap` +0.05 \*\*\*（荒れ地にいる者のほうが上手い）。**しかし `onDear` +0.01 \*、`speedGap` +0.00**——**超過コストを全部消しても動かない**。詳細は `PLAN.md` の段階38a と決定事項#63

- [x] **ヒント枠の中身にする**（新しい並行の仕組みを作らない）。1枠は言い伝えかスキルのどちらか、値段は同じ
- [x] **一度きり習得。使って上手くならない**（実効値の掛け算は `Agent.Ability` の1か所）
- [x] **カテゴリごとに連続スカラー1本**（離散の等級表を作らない）
- [x] **習得は3経路、マージは1つの比較演算**（出生地・継承・観察）。**降格の規則は書いていない**——実効 = min(名目, 適性遺伝子/100) なので勝手に戻る
- [x] **天才は既存イベントを流用**し、**すでに持っているスキルだけを跳ね上げる**（無いカテゴリは発明しない＝**荒れ地の無い世界にはスキルが現れない**）
- [x] **効果は既存の掛け算1つ**（荒れ地の超過コストの目減り）。**知覚の `Self.Ground` もその身体の実効コストを返す**
- [x] **既定は 0**（`SkillBirthplace`）＝**地図の作者が入れるもの**。**平坦な世界は1ビットも変わらない**（指紋テストが無変更で通る）＝過去の `rough` / `river` / `country` の測定値は読み直し不要
- [x] **予想を先に書いた**——**拡散（4）と天才の拠点化（5）は当たり**、**`onDear` の向き（1）も当たり**（上がった）、**`speedGap`（2）は外れ**（動かない）
- [x] **段階33 の宿題に答えた**: **スキルは「速さと体力の並存」への別経路ではなかった**。適性を頑丈さに変えた対照（`skilltough`）でもニッチは出ない
- [x] 触った文書: `NODE.md`（5-3e）、`PARAMETERS.md`、`PLAN.md`、`HISTORY.md`、`CLAUDE.md`、`README.md`
- [ ] **残した宿題**: **「使うほど上手くなる」を作らずに済んだのは、出生地が最初の1粒を配ってくれるから**——**採取・毒耐性のように「場所と結びつかないスキル」には最初の1粒をどこから出すかという問題が残る**（38b の設計で最初に決めること）／**保有率（0.37）は模倣では動かない**＝誰が持つかは出生地が決めるので、**場所と結びつかないスキルでは保有率の決まり方が変わる**

### 38b. 段階38b：2種類目から先（1つずつ足す）【採取は完了・2026-09-08】
> **採取の結果**: **設計どおりに動き、ほとんど何も買わなかった。** `forageHeld` 0.38、`forageNominal` 0.55（模倣を切ると 0.19）、**実効 0.18**（記憶の天井。適性を脚にすると 0.44）。**`dietDiscount` は +0.00 \***——**飽きの割引がそもそも5%**。詳細は `PLAN.md` の段階38b と決定事項#64

- [x] **採取＝競争ではなく効率**（#64）。**独占は起きなかった**（`richGain` +0.01 \*）＝#64 の心配は当たらず
- [x] **38a の宿題に答えた**: **採取も場所と結びついていた**——読むのは「地面が何でできているか」ではなく「**何を出すか**」（痩せた地域ほど高い＝necessity が教える）。**地形の無い世界でも成立する**のが2種類目の意味
- [x] **適性遺伝子はスキルごとの表にした**（荒れ地＝速さ／採取＝記憶）。**記憶に初めて仕事を頼んだが、応えなかった**（`shMemory` −0.00＝5度目の不発。`foragelegs` で天井を緩めても結果は同じ）
- [x] **天才の跳躍は持っているスキルから1つ選ぶ**（2種類あるので「最初の枠」固定は枠の順序についての規則になる）
- [x] **予想を先に書いた**——**2（記憶は動かない）と5（枠の取り合い）は当たり**、**1（`dietDiscount` が上がる）は形だけ当たって大きさが外れ**、**6（住み分け）は外れ**
- [x] **泳ぎ（2026-09-08 完了）**: **3つで唯一、狙った量が有意に動いた**（溺死 −1.83 \*、`drownRate` −0.03 \*、効かない対照では不動）。**先に数えたのが効いた**——打ち消す相手が死因の11%あったから。**`onWater` は動かない**（6度目）。**枠の値段は積み上がる**（川の地図では2つのスキルが枠を奪い、`hintKinds` −3.28 \*\*\*）
- [x] **17b（毒と警告信号）の復活を測った（2026-09-08）**——**「どの用量でも持たない」は取り消し**（旧記録は見かけトリガー修正前の世界）。**毒2＋`PlantSignalCost` なら世界は持つ**（個体数 −22%、絶滅0）＝**毒耐性スキルの前提はこれで揃った**。**壊していたのは毒ではなく恐れ**で、**毒は植物にとって1ミリも得ではなかった**（食べられた株は毒の有無に関係なく消える＝選択の外）。**毒に得をさせると食料が消えて世界が壊れる**（個体数 4.2）ので、**毒耐性を作るときも「食料の実効量に触らない」ことが条件**
- [x] **毒耐性（2026-09-08 完了）**: **先に数えた**——**毒そのものは回復の2.1%**（採取の5%より小さい）、**恐れの代価が個体数の17%**。**だから狙いは毒ではなく恐れ**にした（`Self.PoisonResist`）。**しかし効かない**（個体数 +4.12 ± 6.52）——**数えた的が、17b の復活で信号に値段を付けたこと自体によって消えていた**（信号 0.60 → 0.07）。**信号が高いままの世界でも +1.79**（届くのは的の2割）
- [x] **「場所と結びつかないスキル」の宿題はここで解けた**: **出生地はスキルごとに対応する地域の性質を読む**（毒耐性＝その地域に立っている株の平均の毒）
- [ ] **段階38 はここで区切った。** 残り（追跡・介抱）は**同じ上限に当たる**ことが分かっている——**スキルが集団を動かせる量は「保有率 × 実効値」（7〜26%）× 的の大きさ（2〜17%）**。**足すなら先に「保有率を上げる経路」（出生地が配る量・枠の数）か「もっと大きい的」を作る**。**2026-09-09 の追記**: **その「もっと大きい的」を P7 が作りに行く**——段階42（魚）は泳ぎの的を、段階39（肉の量と回復）は採取の的（`dietVariety` 0.12）を、段階44（特産食料）は的の大きさそのものをパラメータとして持つ。**追跡・介抱に戻るのはそのあと**
- [ ] **毒耐性は 17b の復活と対**（17b は既定オフ＝個体数が9分の1）。**追跡・介抱**はその後（**肉の保存は 2026-09-09 に「作らない」で閉じた**——運搬能力がその役目を果たすので、狭い的に1枠を割り当てない。#68）
- [ ] **残した宿題**: **「場所と結びつかないスキル」はまだ1つも出ていない**（泳ぎ＝水、毒耐性＝植物、追跡＝獲物）。**出生地の経路が要らなくなる日が来たら、最初の1粒の出どころを設計し直す**

### 34. 段階34：溺死（水は渡れるが、渡ると死ぬことがある）【完了・2026-09-07】
> **結果**: 溺死は死因の11%、個体数 −17.3 \*\*、`killShare` −0.10 \*\*\*、**しかし `onWater` は不動**。**危険を効用式から抜いた対照（`DrownKnown = false`）と有意差ゼロ**＝**効き目は全部「選別」**。**「速い個体が報われる」は外れ**（`shSpeed` −0.01 \*\*）——**実装前に数えたら個体は川を渡っておらず、川の中で暮らしていた**（平均滞在127tick）。**したがって「水を避ける」を作るのは段階35 であって危険の強さではない**
> 目的: 今の「水」は移動コストが高いだけのセルで、**渡っても何も起きない**。ここで値段をつける。**同時に、地形がイベントを周知する装置になるための最初の実例を具体的に1つ作る**（抽象を先に作らない）。詳細は `PLAN.md` の段階34 と決定事項#58・#60

- [x] **毎tickの確率で即死。継続ダメージにしない**（#58）——既存のパワー・防御に新しい掛け算を足さずに済むから。~~速い個体は水に浸かるtickが少ないので既存の速さが報われる~~ は**外れた**（実測 `shSpeed` −0.01 \*\*）
- [x] **閾値は作らない。** 入るかどうかは効用比較の結果として現れさせる（候補の期待値に溺死確率を織り込むだけ）
- [x] **死因に専用のバケツを新設する**（`Stats()`）。**餓死・戦闘に混ぜない**——混ぜると他のすべての変更のA/B測定に紛れ込む
- [x] **確率は地形の側に**（`terrain` の3つ目のフィールド）。**汎用のハザード表は作らない**（#60。2つ目が出たときに共通化する）
- [x] **実装前に数える**（段階24 の教訓）: **水のセルに乗っているtickが1実行に何回あるか**。これが効き目の上限で、確率の較正はこれを見てから
- [x] **平坦な世界は1ビットも変わらないこと**（指紋テストが証拠）
- [x] **予想を先に書く**: `drowned` は 0 ではないが数%／**34 単体では `onWater` は大きくは下がらない**（足元しか分からないので入る前に見積もれない＝下がるのは選別の側）／個体数は下がる（値段を測るのが主目的）／`killShare`・餓死率は**率と絶対数の両方**で見る
- [x] **監視**: 川で分断された岸が吸収状態にならないか（段階17 の轍）
- [x] **再ベースライン**: 段階20・29・33 の `river` / `country` の数値を読み直す
- [x] 触る文書: `NODE.md`、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`
- [x] **計画に無かったが足した**: 対照条件 `DrownKnown`（水は同じだけ殺し、知覚からだけ抜く）。**これが無ければ「個体が危険を避けるようになった」と誤って書いていた**
- [ ] **残した宿題**: **段階35 の完了条件は `onWater` が動くこと**（動かなければ、原因は伝承ではなく「水の無いほうへ行く」という選択肢が無いことのほう）／~~**`drownhigh` では外敵が絶滅する実行が出る**ので、川のある地図では `species`・`rareTrough` を必ず見る~~ → **測って閉じた（2026-09-08、48シード）**: **既定の川は安全**（`species` 2.00・`rareTrough` 0.59・全48シードで絶滅0）。**5倍では 1.94 \***（`rareTrough` −0.15 \*\*\*、`rareSwing` +0.25 \*\*\*）。**そして遊ぶ地図（`countrydanger`）が 1.92 \*\***——**極端な用量だけの話ではない**ので、**`country` で長く回すときは `species` を見る**／**2つ目の危険な地面が出たら共通化する**（#60）

### 35. 段階35：溺死の周知と、場所の危険という2本目の信念 【完了・2026-09-07】
> **結果**: **知ることは起きた（信念と真値の相関 0.77／細かい地域では 0.98）が、行動は変わらない**（`onWater` は下がらず溺死は +3.75 \*）。**目撃だけでは何も増えず（0.33）、地図に変えているのは伝承**。**効かない理由は候補でも重みでも粒度でもなく食料**——`drawShare` は7%（門は開いている）、重み4倍・108地域・川を端に移す、を潰して残ったのは「水の上にも食料がある」こと。**段階33 の設定を入れた世界でだけ溺死が17%減る**（−4.33 \*\*、代価は個体数 −6.9 \*）
> 目的: 34 は「入った個体が消える」だけで**見ていた者に何も残らない**（段階31 が人の殺害について直そうとしている穴と同じ）。**段階29 が地形コストで示した「知ることで避けられる」の危険版。** 詳細は `PLAN.md` の段階35 と決定事項#59・#60

- [x] **目撃者スキャンは段階31 の実装を2つ目の実例として共通化する**（#60）。共通なのは視界の定義と粗い重みだけで、**更新先は違う**（31 は個体の評価、35 は場所の危険）
- [x] **更新先は地域ごとの信念の2本目**（#59。`engine/regionlore.go`）。**段階29 の移動コスト倍率には混ぜない**——**コストが高いだけの荒れ地と、命に関わる川が区別できなくなる**
- [x] **減衰は無し**（#53 のまま。同じ関数を使い回して率だけ0）
- [x] **更新経路は直接経験と伝承の2つ**（15b/15c と同じ形。伝承は `exchangeLore` に相乗り＝専用の行動は作らない）
- [x] **記憶容量の枠は食わない**（#41。場所は人ではない）
- [x] **段階30b の轍を踏まない**——**最初から地域IDに紐づく信念**なので、「事実は学べるが、どこへ向かえばよいか言えない」にはならない
- [x] **完了条件**: 伝承だけを切った対照と対にして、**知識のうち伝聞の割合**と `onWater` の差を分離する（段階15c・29b で2度使った測り方）
- [x] **予想を先に書く**: `onWater` は 34 単体より下がるが**半分にはならない**（段階29 の前例）／`drowned` 低下・個体数は 34 の代償の一部を回収／**認識だけの条件を必ず対照に置く**（29a 単独が一番悪かった＝**未知が「普通」に見えると、知っていることが罰になる**）／`clumping` 上昇
- [x] **監視**: `ActObserve` の選択率（目撃がただで教えると観察が死ぬ）／地域の局所絶滅
- [x] 触る文書: `NODE.md`、`PARAMETERS.md`、`HISTORY.md`、`CLAUDE.md`
- [ ] **残した宿題**: **目撃者スキャンの共通化は段階31 の仕事になった**（35 が1つ目の実例になったため）／**信念を weigh すると個体数が上がるのは危険とは無関係な副作用**なので段階29 のコストも読み直す価値がある／**`drawShare` = 0.07 は地域信念の段階すべての天井**

### 36. 段階36：水辺の豊かさ（保留を解く） 【完了・2026-09-07】
> **結果**: **`onWater` が初めて動いた**——痩せた水辺 0.11／一様 0.14／豊かな水辺 0.15 と**3点が一直線**。**動かしたのは食料の分布だけで、危険（34）も信念（35）も動かせなかった**。豊かな水辺は溺死 +3.25 \*・`nearest` −8.94 \* と引き換えに**死亡率 −0.27 \*・餓死率 −0.24 \***（世界は楽になる）。**`killShare` 上昇の予想は外れ**（集まった先に食料も集まっている）。**既定は0**（地図の作者が決める。devview の `-terrain` は 1）
> 目的: 水を**罰だけの地形にしない**。**この世界で初めて、危険と報酬が同じ場所に乗る。** 保留の理由（一般式と両立しない特別扱い）は段階33 が通ったことで消えた。詳細は `PLAN.md` の段階36 と決定事項#62

- [x] **新しい知覚も新しい伝承も作らない**（#57 と同じ理由）。豊かさの信念と危険の信念は**すでに別々に頭の中にある**ので、**世界の側で真値を相関させれば個体が自分で見つける**
- [x] **コストの項とは別の項にする**（#62）。**1本の移動コスト倍率に混ぜると符号が矛盾する**
- [x] **総量は変えず分布だけ**（段階15a の原則）。再正規化を**テストで固定**
- [x] **段階33 の `TerrainFoodCorrelation` とは別のパラメータ**にし、**33 の既定（0）を動かさずに測る**
- [x] **予想を先に書く**: `onWater` は 35 より**上がる**（**下がりきらないことが正しい**＝駆け引きが成立している証拠）／`drowned` も個体数も上がる＝**両方を見る**／`clumping` 上昇・`nearest` 低下／`killShare` は上がりうる
- [x] **監視**: 川沿いに全員が集まって内陸が空にならないか
- [x] 触る文書: `PARAMETERS.md`、`HISTORY.md`、`CLAUDE.md`
- [ ] **残した宿題**: **`clumping` は動かないのに `nearest` は動く**＝川沿いの集まりは「塊」ではなく「帯」で、段階6.5 の指標では見えない（段階37 の新指標がその一歩）／~~**`WatersideFood` を大きくするなら `plantEmpty`（+0.04 \*）を見る**~~ → **測って閉じた（2026-09-08、48シード）**: **心配は逆向きだった**——豊かな水辺は**何も生えていない地域を減らす**（0.22 → 0.18、−0.03 \*）。痩せた水辺は +0.02（誤差内）。**旧値 +0.04 \* は出産のバグを直す前の世界のもの**

### 37. 段階37：川が世界を2つに分けるか（観測であって実装ではない） 【完了・2026-09-08】
> **実装項目ではない**（#61）。34〜36 を入れた最終形に対する観測。詳細は `PLAN.md` の段階37

- [x] **地域グリッドを川に合わせて描き直さない**（#61）。**地形と地域が別々の地図であることは段階22 で確認した意図的な設計**
- [x] **既存の集団化指標で読む**（`Clusters` / `ClusterGaps` / `MembershipTracker`）。**`gapRel` だけを読まない**（密度そのものが動くときは正規化した指標が動かない）——**川と池の対照ではどれも動かない**
- [x] **新しい指標を2つ**（`engine/banks.go`）: **川をまたいだ遭遇率**（`crossShare` / `crossIndex`）／**両岸の乖離**（`bankGeneGap` ＝予算比の全変動距離、`bankCountryGap` ＝同じ地域についての信念の差）
- [x] **測ってから2つ足した**: **`crossDry`**（水の上の者を除いた遭遇率。**この世界の水は住むもの**なので、川の真ん中の身体が「両岸は交流している」と読ませる）／**`BankTracker`**（`bankMoves` / `bankBoth` ＝**一生のうち両岸に立った個体の割合**）
- [x] **条件は「川のある地図 対 同じ地図から川を抜いたもの」の対**。**面積とコストの総量を合わせる**——`river` 対 `ponds`（24セル）、`waterrichlink` 対 `pondslink`（遊ぶ世界）、**視界より広い** `gorge` 対 `pools`（48セル）
- [x] **あるべき状態 B〜E の表と突き合わせる。** **どちらも未達**（`gapRel` 0.76〜0.86・`halfLife` 33〜38）なので**表は書き換えない**。地理が達成した場合に社会の欄と分ける、という取り決めは `PLAN.md` の表の注に残した
- [ ] **残した宿題**: **面積を合わせた対照は、地形の分布そのものを均してしまうことがある**（`pools` は水が12地域に等しく散るので `dangerRank` が 0.00 になる＝定数との相関）。**地域単位の指標を読むときは、対照の側の真値の分散も見る**

---

## P7：物を持つ世界（2026-09-09 追加）

> Cowork 側の3つの設計相談（運搬能力・魚・遠隔攻撃／経済圏／肉の経済設計。2026-09-09）を段階に落としたものです。**相談の文書はどれも優先順位を決めていない**ので、順序はここで決めました。ルールの中身・理由・完了条件は `PLAN.md` の「物を持つ世界（段階39〜47）」「交換（段階48〜51）」と決定事項#65〜#76

**決めた順序: 39（肉の量と回復）→ 40（運搬）→ 41（肉の余剰）→ 42（魚）→ 43（釣り2種）→ 44（特産食料）→ 45（小石）→ 46（遠隔攻撃）→ 47（遠隔のスキル）→ P8（48 バーター → 49 発信 → 50 倉庫 → 51 通貨）。理由は7つ。**

1. **並べ替えの基準を「的の大きさ」にした。** 段階38 が区切りに残した再開条件（**スキルは、それが打ち消す規則の大きさ以上にはならない**）は、**スキルに限った話ではありませんでした**。これまでに数えた的は——**戦闘死62% / 餓死34% / 溺死11% / 肉が食べられていないこと（`dietVariety` 0.12）/ 飽きの割引5%**。**最後のひとつを的にした採取は空振りし、11% を的にした泳ぎだけが動いた**（38b）。**この順序はその読みをそのまま使っています。**
2. **肉（39）を先頭にしたのは、一番安くて一番多くの宿題を閉じるから。** 新しい状態も新しい語彙も要らず（`MeatPerBudget` と回復量の2つ）、**保留「肉に植物では取れない栄養」（段階16 の完了条件）を解き**、**群れ狩りへの5度目の挑戦を初めて獲物の側から行い**（動機・サイズ・栄養・語彙で4度空振り。残っていたのは「倒した先に何があるか」だけ）、そして **`dietVariety` 0.12 を動かせば採取スキルの的も後から大きくなる**（38b の再開条件の片方＝「もっと大きい的を作る」）。**先頭に置く条件を全部満たしているのはこれだけです。**
3. **運搬（40）を2番目にしたのは、経済圏の全部がこれに乗るからだけではありません。** **それ自体がこの世界で一番大きい的（餓死＝死因の34%）に当たる独立した規則**です（持てるということは、食料のある場所と食べる時刻を切り離せるということ）。**だから経済が空振りしても運搬は残る。** そして **「速さと体力の対立」への4度目の挑戦**でもあります——段階20（地形のコスト）・33（食料を地面に結びつける）・38a（荒れ地耐性）はどれも `speedGap` を 0.00 のまま残しましたが、**重い荷物は初めて、速さと体力を同じ天秤に載せます**。
4. **41（余剰）が 40 の直後なのは、余剰の定義が運搬上限そのものだから**（#68。新しい調整用の数値を作らずに済む）。**肉まわりの内部順序（量+回復 → 余剰開放 → 価格）は相談で指定されたとおり守ります**——価格は P8（通貨）まで出てこないので、この順序は自動的に満たされます。
5. **42→43（魚と釣り）と 44（特産）を後ろに置いたのは、どちらも「食料の分布を変える」＝一番効く手だからです。** 39 が食事の経済を動かした直後には測れません（ベースラインが動く）。**魚が特産より先なのは、水には較正済みの物差しがすでにあるから**——`onWater` は 0.11 / 0.14 / 0.15 の直線（段階36）で、**同じ一般形（地域の性質が量を決める食料）の最初の実例を、答えの読み方が分かっている場所で作る**。2つ目の実例が 44 で、**共通化はそのときに行います**（#60 と同じ「具体を1つ書いてから一般化する」）。**43 は 38 の再開条件「もっと大きい的」への直接の答え**でもあります（泳ぎは今のところ溺死＝11% という1つの的しか持っていない）。
6. **遠隔（45〜47）を経済の配管より前に置いたのは、この記録がずっと支持してきた優先順位から。** **知識の側は3度続けて世界の数字を動かさず**（段階30b は作る前に値打ちがゼロと分かり、31 は絶えず発火して何も動かさず、35 は相関0.77 の正確な信念を伝承で広げても `onWater` を動かさなかった）、**規則と食料の側は動かしてきました**（34 の危険・36 の食料・38b の泳ぎ）。 **ただし遠隔はこの群で唯一、世界を壊しうる項目です**——**殴り返されずに殴れる規則**なので、段階12a が「実在しない報復への恐れが世界を支えている」と測った、その支えを直接削ります。**だから既定オフで始め、`TrueRetaliation` と個体数を必ず対で読みます**（#71）。
7. **P8 の先頭に 48（バーター）を置いたのは、そこが門番だから。** **通貨も倉庫も発信も配管であって、「なぜ自分で採らずに買うのか」（比較優位）が無ければ全部空振りします**（#73）。**手渡しは安い診断**——運搬が入っていれば行動語彙1つで書けます。**ここで交換が1度も起きなければ 49〜51 は作りません。** **比較優位の両側（39・41 の動物側と 44 の植物側）が揃ってから叩く門**なので、48 は 44 より後ろでなければなりません。**これは「安い診断を先に回す」の5度目**です（段階30b の判断・35 の粒度掃引・38b の的の数え直し・37 の観測）。

**入れ替えてよいもの**: **行動の拘束時間（下の独立項目）はいつでもよい**（ただし単体で測る。運搬の拾う・置くと同じ回に入れない＝#76）。**45〜47（遠隔の群）は P8 と入れ替えてよい**（依存が無く、どちらも 40 のあと）。

**入れ替えてはいけないもの**: **39 → 40 → 41**（余剰の定義が運搬上限）、**42 → 43**（魚が無ければ釣りの的が無い）、**40 → 45 → 46 → 47**（弾は運搬に、遠隔スキルは行動の単体測定に依存）、**44 → 48**（比較優位の両側が揃ってから）。

### 39. 段階39：肉を食べる価値にする（量と全回復）【完了・2026-09-09】
> **結果: 群れ狩りが5度目にして現れた**（`packSize` 1.36 → 1.81、共同での仕留め 34 → 95）。**動かしたのは栄養ではなく治癒**（同じだけ腹にたまる肉では +0.06）。**効き目の半分は「知っていること」から**（知らない対照で +0.21）＝**段階34 が有意差ゼロだった対照で初めて差が出た**。既定は `MeatVitality` = 0.5、`MeatPerBudget` は据え置き。詳細は `PLAN.md` の段階39 と `HISTORY.md`

- [x] **量と回復量は別のレバーとして測る**（#68）。同時に動かすと「群れ狩りが増えたのは肉が美味しいからか、余剰が出たからか」を分けられない
- [x] **39a（量）**: `MeatPerBudget` を下げるだけ。**新しい規則は1つも要らない**。`MaxMeatItems` の枠が先に埋まらないことを確認する
- [x] **39b（回復量）**: **食料が体力に直接触る初めての経路**。**効用式には項を足さない**——`recoverable` の頭打ちがそのまま「満タンの個体は肉を安く見る＝温存する」を作るはず
- [x] **対照は「空腹度だけを大きく満たす肉」**（美味しくなったのか薬になったのかを分ける）
- [x] **実装前に数える**: 人間が肉を食べている割合／腐って消える肉の割合（**捨てられている量が少なければ、量を増やしても余剰は生まれない＝段階41 の前提が崩れる**）／1回の狩りが作る item 数
- [x] **予想を先に書く**: `dietVariety` 上昇（**上がらなければ他は読まなくてよい**）／`packSize` は動くとしたら 39b から／`killShare` 上昇／餓死率低下／`restShare` 低下
- [x] **監視**: 外敵が「歩く食料」になって吸収状態に入らないか（`species` / `rareTrough`）／`killShare` の上昇が対外敵の狩りから来ているか（`fightRatio`）
- [x] 触る文書: `NODE.md`、`ENEMY.md`、`PARAMETERS.md`、`HISTORY.md`、`CLAUDE.md`

### 40. 段階40：運搬（所持品という5つ目の状態量）【完了・2026-09-09】
> **結果: 的は実在し、当たった**（餓死のうち「直前100tickに食料を見ていた」割合 −0.04 \*）。**最初の版は個体数を 4.5 削り、犯人は重さでも枠の会計でもなく「拾う理由の粗さ」だった**——保険の価値をその場の食料の競り合いぶりで割り引くと代価が消える（+1.25、誤差内）。**容量は1で符号が変わる**（2で −5.73 \*）。**遊ぶ地図では今も負ける**（−8.90 \*）。**#77 も閉じた**。詳細は `PLAN.md` の段階40 と `HISTORY.md`

- [x] **状態量を増やす初めての例外**（#65）。**帳簿にできないから**（拾う・落とす・渡すという可逆な操作を受ける）
- [x] **遺伝子は10本目を作らない**（#65）。**容量は体力遺伝子から**——ヒント枠の前例（段階12c で `budget` が +42.06 \*\*\* 動いた）があり、10本目は段階7c 以降の予算比を全部読み直しにする
- [x] **対価は 重さ ÷ 体力 を移動コストに掛ける**（#66）。**パワーは使わない**（役割を重ねない）。**速度そのものは落とさない**（effort との二重計上）
- [x] **地形との掛け算は別条件**（#66）。平坦な世界で単体を先に測る
- [x] **拾う・置くは 1tick で始める**（#76。拘束時間と混ぜない）／**死んだら落とす**（総量保存をテストで固定）／**腐敗は持っていても進む**
- [x] **実装前に数える**（#67）: **「食料を持っていれば助かった死」の頻度**＝餓死した個体のうち直前に視界へ食料が入っていた割合。**これが的の大きさそのもの**
- [x] **予想を先に書く**: 餓死率低下・個体数上昇／**`shSpeed` は下がるほうに賭ける**（段階20・34 で2度、移動が高い世界では移動能力が買われなくなった）／`shVitality` 上昇（上がりすぎなら容量固定の対照を置く）／`clumping` は動かない
- [x] **監視**: 世界の食料の総量（持たれている item も枠に数える）／`BenchmarkEngineStep`
- [x] 触る文書: `NODE.md`、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`

### 41. 段階41：肉の余剰（運搬上限が「余剰」を定義する）【完了・2026-09-09】
> **結果: 余剰は実在した**（肉の26%）**が、claim はもともと独占ではなかった**（食べられている肉の54%はすでに他人が食べている＝変えられるのは「誰が」ではなく「いつ」）。**開くと個体数 +6.21 \*・世代 +0.48 \*\***、`meatFreeShare` 0.53 → 0.69 \*\*\*。**claim を全部やめる版は −4.33 で逆に悪い**。**余剰は運ばれずその場で食べられる**（`held` −0.02 \*＝「商品になりうる肉が増える」は外れ）。詳細は `PLAN.md` の段階41 と `HISTORY.md`

- [x] **余剰＝参加者全員の運搬上限を超えた分**（#68）。**新しい数値を作らない／非力な集団ほど余剰が増える**
- [x] **所有権のルールは変えない**（`claimedBy` はそのまま）／**余った肉は既存の食料競合に委ねる**
- [x] **予想**: 「商品になりうる肉」は増える（主指標）／`killShare` は動かない／**`packSize` は下がりうる**（余剰は参加していない者の取り分なので、参加の動機を薄める。**下がるなら 39 と 41 は取引関係にある**）
- [x] **監視**: 腐って消える割合（開放しても誰も来ないなら増えた肉は腐るだけ）

### 42. 段階42：魚（水の中にしかない食料）【完了・2026-09-09】
> **結果: `onWater` がこの計画で初めて大きく動いた**（0.13 → 0.24 \*\*\*）。段階34・35 は動かせず、36 が 0.11 → 0.15 だった数字で、**危険と報酬がついに同じセルに乗った**。**代価は裸の川で個体数 −16.7 \*\*\*、遊ぶ地図では −5.3（誤差内）**。**泳ぎの的も大きくなった**（溺死 −4.35 \*）。**既定は 0＝地図の作者が入れる**。詳細は `PLAN.md` の段階42 と `HISTORY.md`

- [x] **3つ目の `FoodKind`。水のセルにしか湧かない**／**段階36 の項には混ぜない**（陸の豊かさとは別）
- [x] **総量は増やさない**（段階15a。植物の湧きの一部を置き換え、再正規化をテストで固定）
- [x] **既定では人間だけが取る**（外敵の食性を変えると段階11 以降が読み直しになる）
- [x] **平坦な世界は1ビットも変わらないこと**（指紋テスト）
- [x] **予想**: **`onWater` 上昇**（段階36 の直線の外挿として読む）／溺死は増えるが個体数は上がる／`dietVariety` 上昇（**上がらなければ魚は「湧く場所が違うだけの植物」**）／**泳ぎスキルの的が大きくなる**
- [x] **監視**: 内陸が空にならないか（`plantEmpty`）／人間だけが水へ移ることで外敵の獲物が減らないか（`species`）

### 43. 段階43：陸釣りと水中取り（安全と収量の取引）【完了・2026-09-09】
> **結果（作り直し後）: 取引は成立する。ただし2つの地面の差が大きく、習熟度が高い世界でだけ**（`anglerSplit` +0.06 \*、`wadersWet` +0.06 \*、個体数 +5.73 \*）。**最初の版は「収量」を1匹あたりの栄養として実装しており**、そのせいで**上手い身体ほど少ない匹数で足りて水にいる時間が短くなり**、符号が逆に出ていた（#64 の形を魚に流用した誤り）。**魚が逃げる確率として書き直したら戻った。** 詳細は `PLAN.md` の段階43 と `HISTORY.md`

- [x] **スキル2つ**（陸釣り＝溺死のサイコロを振らない／水中取り＝振る）。**この非対称がこの段階の全部**
- [x] **出生地の種は別々の地域の性質から読む**（38a の「同じセルから2つ種付けしない」）
- [x] **効かせ方は収量。溺死確率には触らない**（そこは泳ぎの担当＝2つのスキルで同じ量を打ち消さない）
- [x] **予想**: **住み分けが初めて構造的に成立しうる**（同じ資源に安全と収量の取引があるのはここが初めて）／泳ぎの保有率が上がる（出生地の重なりを通じて）／**枠の取り合いは4つに増える**ので「値段だけの対照」を必ず置く（38b で `hintKinds` −3.28 \*\*\*）

### 44. 段階44：特産食料（比較優位の植物側）【完了・2026-09-09】
> **結果: スキルが初めて世界を大きく良くした**（個体数 +10.06 \*、失敗率 0.38 → 0.30 \*\*\*）——**的の大きさを設定に書いてから測った初めての例**。**しかし居着かない**（`specialGain` ±0.00＝5度目）。**一番良い世界は作物が均等に生えている世界**（+14.35 \*\*）＝**比較優位を生むはずの偏りが世界にとっては損**。**経済の土台に使えるのは「誰ができるか」であって「どこでできるか」ではない。** 詳細は `PLAN.md` の段階44 と `HISTORY.md`

- [x] **座標の知識（POI）は作らない**（#70）。**粒度は地域単位のまま**
- [x] **必ず連続の傾斜**（離散ゲートにしない＝`StrategyDepthUnlock` と視力の2度の轍）／**総量は保存する**
- [x] **的の大きさは明示的なパラメータ**（世界の食料の何%を特産にするか）＝**そのままスキルの効き目の上限**
- [x] **一般形の共通化はここで行う**（段階42 が1つ目の実例。#60 の形）
- [x] **予想**: **保有者が居着く**（38a・38b で3度出なかった定住が、ここでは食料が実際に多いので出るはず）／個体数は下がる（総量保存なので誰かの取り分が減る）／**`killShare` は上がらないほうに賭ける**（段階36 で同じ予想を外した）
- [x] **監視**: 保有者が集まりすぎて取り合いになっていないか／痩せた地域が空にならないか（`plantEmpty`）

### 45. 段階45：小石（運搬の2つ目の中身）【完了・2026-09-09】
> **結果: この段階の中身は供給を数えることだった**（使い道が無いので誰も拾わない）。**遊ぶ地図で石が視界にある個体は13%**、荒れ地の地図では54%、**3倍撒いても 0.15**＝**段階46 の発火頻度の天井**。**測定がバグを1つ見つけた**（石が食料の枠を食い、180個の世界は植物が生えずに個体数4）。詳細は `PLAN.md` の段階45 と `HISTORY.md`
- [x] **拾える場所を地形に紐づける**（#72）／**新しい所有権のルールは作らない**／**重さを持つ**（弾を多く持つほど遅い）／**段階40 に依存**

### 46. 段階46：遠隔攻撃という行動【完了・2026-09-09】
> **結果: 一番大きい予想が外れた**——**殴り返され率は下がるどころか上がった**（`trueRetal` +0.01 \*）。**石は逃げ道ではなく招待状**。**当たったのは逃走**（`escapeShare` −0.02 \*\*）。**個体数 −6.42 \***（遊ぶ地図では誤差内＝段階45 の供給が上限）。**3倍痛くすると壊れる**（−11.90 \*\*）。**既定はオフ**。詳細は `PLAN.md` の段階46 と `HISTORY.md`
> **この群で唯一、世界を壊しうる項目。** 詳細は `PLAN.md` の段階46 と決定事項#71

- [x] **既存の近接戦闘のスキル化ではない**（#64 が禁じたのはそちら）。**ダメージ・被弾は既存の3遺伝子で近接と同じ式**
- [x] **間合いは `CombatRadius` より広く視界より狭い**（見えない相手は狙えない＝段階19 の約束）／**弾が無ければ候補に挙がらない**（身体にできないこと＝段階25 の線引き）／**段階24 の隙は流用しない**
- [x] **既定オフで始め、`TrueRetaliation` と個体数を対で読む**（段階12a：実在しない報復への恐れが世界を支えている）
- [x] **実装前に数える**: 今の戦闘のうち間合いの外から始まっているものの割合（＝発火頻度の上限）／逃走の成功率
- [x] **予想**: `TrueRetaliation` 低下・個体数低下（**下がらなければ段階12a の読みのほうを疑う**）／**`escapeShare` 低下＝これが本体**／理性への選択圧は上がりうるが、**対照（命中が距離で落ちない腕）が無ければ 17b と同じ誤読になる**
- [x] **監視**: 外敵の絶滅（人間だけが遠隔を持つなら一方的に削られる）

### 47. 段階47：遠隔攻撃のスキル【完了・2026-09-09】
> **結果: 上限どおり**（保有率 0.08 × 実効 0.12 × 的 0.26 ＝ 0.25%＝命中率は +0.03 \*\*\* 動くのに集団の数字は動かない）。**ただしスキルが自分の天井の遺伝子を買わせた初めての兆し**（`shRationality` +0.01 \*）。詳細は `PLAN.md` の段階47 と `HISTORY.md`
- [x] **遠隔だけが持つ部分（距離による命中の目減り）にだけ効かせる**（#71）／**適性にパワーを選ばない**（理性か頭の良さ）／**出生地は「その地域で小石が拾えるか」**／**段階46 に依存**
- [x] **予想**: 理性の予算比が動くとしたらここ。**ただし 38 の上限（保有率 × 実効値 × 的の大きさ）は同じように効くので期待値は数%**

### 独立した項目：行動の拘束時間（いつでもよい）
- [x] **既存の前例に乗せる**（#76。`CallTicks` / `SkirmishTicks` / `observe` の排他性）。**新しい並行の仕組みを作らない**
- [x] **単体で先に測る**。**運搬の拾う・置くと同じ回に入れない**
- [x] **これは採取スキルの的を大きくしうる**（38b は「飽きの割引が5%」で空振りした）——**拘束中の機会費用と露出リスク**という新しい規則を採取が打ち消す形になるため
- [x] **予想**: `restShare` と `killShare` が動く（拘束中は無防備＝段階9 の形が食事にも及ぶ）／個体数は下がる

---

## P8：交換（2026-09-09 追加。P7 のあと）

> **この群は全部が配管です。** 先頭の 48 が門番で、**そこで交換が起きなければ 49〜51 は作りません**（#73）。詳細は `PLAN.md` の「交換（段階48〜51）」

### 48. 段階48：バーター（交換が起きるかの観測）【完了・2026-09-09】
> **結果: 門は開いた**（20000tickに391回、**95%が他人へ**）。**予想は逆**——理由は値付けの帰結（信頼は飽和するので他人への贈り物が一番高い）。**既定の世界では得**（+9.35 \*、`dietVariety` +0.04 \*\*）、**食料の乏しい地図では −11.27 \*\***。**49〜51 は作ってよい。** 詳細は `PLAN.md` の段階48 と `HISTORY.md`
> **段階37 と同じく、主な成果物は数値であって仕組みではありません。**

- [x] **行動語彙を1つ（渡す）。受けるかどうかは既存の効用比較が採点する**（閾値を置かない＝段階25・32 の原則）
- [x] **好意は既存のものを使う**（新しい信頼も評判も作らない）
- [x] **段階39〜44 のあとに測る**（比較優位の両側が揃ってから）
- [x] **完了条件は「起きるか起きないか」。起きなければ 49〜51 は作らない**
- [x] **予想**: 血縁と配偶者の間では起きる／他人の間ではほとんど起きない／渡るものはほぼ肉。**これが当たったら、通貨より先に要るのは「他人を信用する理由」**であって、コインでも倉庫でもない

### 49. 段階49：発信（売り物の周知）【完了・2026-09-10】
> **結果: 語は使われ、聞かれ、歩かれ、世界の数字は動かない。** **決め手は「聞こえない対照」**（`WaresSeen = false`）——**餓死の減少は聞こえない腕でも同じだけ出る**＝**効いているのは立ち止まっていることのほう**。**既定はオフ**（`OfferTicks = 0`。`cmd/devview -terrain` は 30）。詳細は `PLAN.md` の段階49 と `HISTORY.md`

- [x] **記憶ではなく「見える事実」にした**（`AgentView.Offering`。行動から読み、どこにも保存しない）
- [x] **目撃者スキャンは使っていない**（売り物を出している身体はずっと視界にいるので `perceive` がすでに配る）
- [x] **対価は時間だけ**（`OfferTicks`）／**発信は効用比較の1候補**／**範囲は視界内だけ**
- [x] **買う側に新しい語彙を作らなかった**（段階15b の「良い地域へ行く」と同じ `move` の1候補）
- [x] **実装前に的を数えた**（余剰は実在するが、売り手の52%はすでに買い手を見ており、買う側の的は空腹の瞬間の2.9%）
- [x] **買う側の値付けが5倍甘かったのを実測で見つけて直した**（歩いた5回に1回しか渡されない → 地面の食料と同じ競走で割り引く）
- [x] **記録した緊張**: 発信は「荷物を持った身体がここにいる」も知らせる＝**奪う行動を作れば諸刃になる**（`PLAN.md` の保留のまま）
- [x] **付随**: **水から出た魚は死肉**にした（釣り上げた瞬間に `MeatSpoilTicks` の時計が始まる。`LandedFishKeeps` で戻せる）。**「魚が陸に落ちている」のはバグではなく正しい**——世界が魚を生やすのは水の中だけだが、釣り上げた魚は持ち主のもの。抜け道だったのは場所ではなく時計
- [x] 触った文書: `NODE.md`、`PARAMETERS.md`、`README.md`、`CLAUDE.md`、`PLAN.md`、`cmd/devview`、`cmd/experiment`、指紋テスト

### 50. 段階50：倉庫（場所ごとの記憶と保管）【完了・2026-09-10】
> **結果: 偽薬は1ビットも動かず、知識の壁だけが世界を動かした。** 学ぶ腕 −4.69 \*／全員が知る腕 +3.71 \*／**直接の対 +8.40 \*\***。**誰も溜め込まない**（0.19 個）。**理由は「余剰を持っているのは満腹の身体で、満腹の身体には取っておく値打ちがゼロ」**。詳細は `PLAN.md` の段階50 と `HISTORY.md`

- [x] **知識は地域信念の形**（減衰あり・記憶枠を食わない・`MaxStores` で上限）
- [x] **習得経路は4つ必要だった**——計画の3つ（継承・目撃・発信）はどれも「すでに知っている誰か」を要するので何も始まらない。足したのが**見つける**（`StoreFindLooks`）
- [x] **確率ではなく回数で数えた**（乱数を引くと対照と別の乱数列になる＝段階45 の罠。**確率版 +11.33 \*\*\* → 回数版 +1.67 ± 2.85**）
- [x] **倉庫は敷く側が置く地物**（`World.SetStore`／devview のエディタは `k`）／**建設という行動は作っていない**
- [x] **実装前に数えた**（食料の枠が満杯の時間: 平坦 9% 対 遊ぶ地図 **86%**＝痩せた地図では倉庫に入れた1個は生えなかった植物1本）
- [x] **対照を2つ回した**——**情報だけを消す**（`StoresKnownToAll`）と**行動だけを消す**（`StoreValue = 0` の偽薬）。**後者が1ビットも動かなかったことが、前者の差を信用できる根拠**
- [x] **予想の答え合わせ**: 「知識は血統に集中」＝**外れ**（一番広い道は発信）／「通わなければ忘れる」＝**そのとおり**／「段階35 の再演」＝**外れ**（知識の壁を外すと世界が動いた）
- [x] **付随**: **段階40 の「後のために取っておく価値」の符号が逆だったのを発見・修正**（138,310 件の候補に正の値が1つも無かった。`CarryPricedBackwards` で戻せる）
- [x] 触った文書: `NODE.md`、`PARAMETERS.md`、`README.md`、`CLAUDE.md`、`PLAN.md`、`engine/save.go`、`cmd/devview`、`cmd/experiment`、指紋テスト

### 51. 段階51：通貨【完了・2026-09-10】
> **結果: 割引率が段階のすべてを決め、足りないのは買い手ではなく売り手だった。** 売買 3.21 / 断り 6.17（`CoinValue` 1.0 では 0.46 / 1.69、0.2 では 3.58 / 11.54）。**個体数 +11.65 \*\* だが半分は売買ゼロの腕でも出る。** 詳細は `PLAN.md` の段階51 と `HISTORY.md`
- [x] **独立した3つ目の目的にしていない**（値打ちは `CoinValue × keepValue`＝運搬・倉庫と同じ1つの関数）
- [x] **コインは1種類・重さゼロ・食料の枠を食わない・地域に紐づかない・総量不変・半減期も供給上限も無し**
- [x] **売るかどうかは世界の規則**（`willSell`＝`willCommit` と同じ形）／**買えるのは掲げられているものだけ**（＝段階49 が店先。49 → 51 の鎖が閉じた）
- [x] **実装前に数えた**: #73 の式のままだと**コインが食料より高くなることはない**（55,000 看視で 0.0000）——**成立を支えるのは「値打ちが持ち主ごとに違うこと」と「重さがゼロであること」だけ**
- [x] **予想の答え合わせ**: 「割引率がすべてを決める」＝**当たり**／「中間が細い」＝**細くはないが一方通行**（下げるほど買い手が得をし売り手が断る）／「段階50 と同じ壁」＝**半分当たり**（満腹の売り手には値打ちが無いので断りが売買の2倍）／「個体数は動かない」＝**外れ**（+11.65 \*\*、ただし半分は売買ゼロの腕でも出る）／「偽薬は1ビット同じ」＝**外れ**（撒くのに乱数を引く）
- [x] **対照を2つ回した**（情報＝店先を消す腕、行動＝値打ちを0 にする偽薬）
- [x] **付随**: **手を経由して「外敵は肉だけ」「同族の肉は食べない」が破られていたのを発見・修正**（外敵が贈られた植物を持つ状態が17,066体-tick）

---

## P9：今しか見ていないこと（2026-09-11 追加。P8 のあと）

> **段階50 と段階51 が独立に同じ壁に当たりました**——**取っておく価値もコインの値打ちも「今の死亡リスクの勾配」から読まれ、その勾配は満腹の身体では平ら**で、**余剰を持っているのは満腹の身体**（#79）。**この群はその根っこ（先読みそのもの）には触らず、壁を迂回する当たり方を4つ用意して、どれが効くかを測ります。** 詳細は `PLAN.md` の「今しか見ていないこと（段階52〜55）」

**順序の理由（52 → 54 → 53 → 55）**

- **52 が先頭なのは2つの理由**: (1) **4つの中で唯一この壁を構造的に避けられる**——**回復の値打ちは空腹度ではなく「欠けている体力」から読まれる**ので、**満腹でも傷ついた身体には正の値打ちがある**（#80）。(2) **55 の必要性そのものを検証する前提になる**——**調理による本物の依存関係が集団化を生むなら、孤立ペナルティという人工的な機構は要らない**
- **54 は 52 と独立**なので並行着手してよい。**ただし `ChoiceNoise` を触るので、他の測定を伴う変更と同じ回に混ぜない**（12b・15b・31 で確立した作法）
- **53 は「どの遺伝子をどちらの性に振るか」が未決**（#82）。**決まるまで着手しない**ので、他の3つより後ろに置く
- **55 は 52 の結果待ちの条件つき**（#84）。**着手する前に的を数える**——「視界内に好意のある相手がゼロ」の割合が小さければ、**段階30b・31・35・49 の5度目**になる

### 52. 段階52：調理とスキル分業【完了・2026-09-11】
> **結果: この世界の1つの規則としては最大の効き目**（個体数 **+21.10 \*\*\***、48シード）。**実装前に数えた的が6倍だったことがそのまま答え**（取っておく値打ちが正 0.037 に対し、調理が生より良い 0.235）。**独占は起きず、前提のほうが崩れた**（手の中のものは誰にも取れない＝`CookClaimTicks` は書かなかった）。**分業は6度目の不発**（`cookSplit` 0.01 ± 0.02）。詳細は `PLAN.md` の段階52 と `HISTORY.md`

- [x] **行動語彙を1つ（`ActCook`）、対価は時間だけ**（`CookTicks` = 20）／**4種類目の `FoodKind` にしていない**／**門にしていない**／**腐敗も止めていない**／**場所にも紐づけていない**
- [x] **効用式に新しい項を足していない**（治りは段階39 とまったく同じ場所）
- [x] **治る理由は足さず良いほうを採った**——**結果 `cookedMeat` = 0.00 ＝調理は誰でも持っている食べ物に乗った**（交換が成り立ちうるのはそちら）
- [x] **偽薬（`CookVitality = 0`）が1ビット同じであることをテストで固定**。**「誰も調理しない用量」（`CookTicks = 60`）は候補を採点するので乱数列が変わる**＝段階45 の罠の4度目
- [x] **交換の半分は本物**（`AffinityGift = 0` との2×2：+21.10 \*\*\* 対 +12.46 \*\* ＝5分の2が他人に渡せることから）。**「渡すと調理が落ちる」対照には穴がある**（受け取った側が20tick で調理し直せる）
- [x] **遊ぶ地図でも +13.06 \*\*** ＝**食料に触る規則で両方の地図が同符号になった初めての例**（調理は食料を増やしも減らしもしない）
- [x] **52b（スキル）は分業を作らない**が、**`dIntelligence` +4.98 \* ＝スキルが自分の天井の遺伝子を買わせた2度目の兆し**
- [x] 触った文書: `NODE.md`、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`、`cmd/devview`、`cmd/experiment`、指紋テスト

### 52d. 段階52d：生では割に合わない食料（52 の結果待ち・条件つき）
> 着手条件: **52 で効果が弱すぎる**（38 系の「保有率 × 実効値 × 的の大きさ」の壁）と測定で分かったときだけ。**52 と同じ回に混ぜない**

- [ ] **ゼロではなくとても低い生食価値**にする（#80。**完全な門は段階17b の領域**）
- [ ] **段階44 の `SpecialtyCatch = 0.25` と同じ「連続だが非対称」な値付け**
- [ ] **的の大きさは明示的なパラメータ**（世界の食料の何%か＝#70 と同じ形。**この割合がそのまま効き目の上限**）

### 54. 段階54：報酬と恐れを、評価ノイズの構造として持たせる【完了・2026-09-11】
> **結果: 強さと構造の両方がそろって初めて効く。** 弱い用量（好みを0.5% しか動かさない）では**符号を逆にしても結果が同じ**。十分な用量で **気分あり +7.77 \*／符号逆 −0.98／同じ平均の傾きを定数として −2.35 ± 6.05**。**既定はオフ**。詳細は `PLAN.md` の段階54 と `HISTORY.md`

- [x] **選択を上書きしない**（2026-08-14 の却下を踏まない）／**相手ごとに持たない**（＝恨みを持たない）／**新しい検知を作らない**
- [x] **傾けるのは事実ではなく好み**（`ShockRisk`＝段階12a が切り分けた3つの1つ）。**身体が持っている値は動かず、今それをどう読むかだけが傾く**
- [x] **実装前に数えた**（最後に殴られてからの窓ごとの割合 0.199/0.346/0.466/0.585/0.673、天井 0.713＝**2/3 の時間立っている気分は定数**）。**半減期を600 にすると効き目が消える＝予想どおり**
- [x] **偽薬（`MoodWeight = 0`）は1ビット同じ**（テストで固定）／**保存にも入れた**
- [x] **決め手の対照は「同じ平均の傾きを定数として与える腕」**——`ShockRisk` を 0.965 倍するだけで、**コードを1行も足さずに交絡を1つ潰せた**
- [x] **外した予想**: 「効くとしたら恐怖の側」＝**外れ**（片方だけでは足りず、両方そろって初めて効く）。**満ち足りは恐れの6倍**だったので利得を分けた
- [x] 触った文書: `NODE.md`、`PARAMETERS.md`、`README.md`、`HISTORY.md`、`CLAUDE.md`、`PLAN.md`、`cmd/devview`、`cmd/experiment`

### 53. 段階53：性別による発現の差【完了・2026-09-11】
> 目的: 「性別は能力に影響しない」（`NODE.md` 1節）を変え、繁殖投資の非対称性を緩やかな性差として反映する。**配分は抽象的な男女像からではなく、構造として作る役割から逆算した**（#85）

- [x] **53a【完了・2026-09-11】: ガーディアンを母に固定した**（`tryBirth` の `child.GuardianID`。今は「どちらでもルール上は関係ない」で番号の若いほう＝**実測でも 50.5% が父**）。**元の世界へ戻す切替を残す**
- [x] **53b【完了・2026-09-11】: ガーディアンでない親（多くは父）の給餌経路を開けた**（#86。**53a と分離しない**——`mouthsToFeed` は `GuardianID` 一致で弾くので、53a だけ入れると父が締め出される）。**判定は「血縁かつ `RearingRadius` 圏内」に緩めるだけ**で、新しい所有も状態も作らず既存の `share()` を使う。**`ActGive` には頼らない**（配偶者への贈与は実測 0%＝信頼が飽和していて値が付かない）
- [ ] **実装前に数えた（済）**: ガーディアンが圏内 0.774 ／ **ガーディアンでない親が圏内 0.475**（生存 0.921）＝**53b は給餌の機会を 1.6 倍にする変更**なので、**53a 単体・53a+53b・どちらも無し の3つを対で測る**。**給餌は既定でオフ**なので測る腕は `ParentFeedShare = 0.5`
- [x] **配分（#85）**: 母＝理性・防御力・**記憶容量**／父＝パワー・速さ・頭の良さ／最大体力・回避率・魅力は中立。**掛け算は `Agent.Ability` の1か所だけ**で、`Genome` と継承には触らない
- [x] **`shMemory` は6度目に動いた**（強い用量で +0.01 \*、**性を入れ替えると −0.01 \* と符号が役割についてくる**）。**ただし代価は個体数の3分の1**＝**記憶に値段をつけることはできるが、そのためには世界を貧しくするしかない**
- [x] **読んだのは集団全体の予算比**（どの遺伝子も 0.01 以内＝**#4 の混ぜ合わせの上限が予想どおり効いた**）
- [x] **一番の発見**: **役割どおりの配分（−15.96 \*\*）が、役割を逆にした配分（−7.94 \*）より悪い**——**世界が金を払っている遺伝子と役割が要求する遺伝子が別**だから。**既定は全ゼロ**

### 55. 段階55：孤立のリスク【55a 完了・2026-09-11／55b・55c は「作らない」】
> **結果: 探しに行くことが集団を壊した。** **着手条件は2つとも通り**（段階52 の `halfLife` は目標150 に対し59、的は「好意のある相手が視界にいない時間 0.460」）、**発火も十分すぎるほどした**（全決定の7%＝段階15b の 0.05 より多い）。**それでも `halfLife` はどの用量でも下がる**（−6.7〜−14.3）。**逆向きの対照は6倍悪い**＝**情報は本物、でも行く価値が無い**。詳細は `PLAN.md` の段階55 と `HISTORY.md`

- [x] **55a: 見失った方角を1本だけ覚え、さまよう候補を1つ増やした**（座標は持たない／**方角は相手が見えているあいだに取る**＝座標が書かれる瞬間がどこにも無い）
- [x] **劣化の規則を先に決めた**（半減期400、下限0.05 で捨てる）。**4倍にしても良くならない**
- [x] **対照は段階54 の形**（同じ候補・同じ重み・同じ代金で方角だけ逆＝**乱数を引かないので同じ乱数列を走る**）
- [x] **段階7d の決定は覆していない**
- [x] **機序**: **探しに行く身体は今いる集団を捨てる**（探す相手は1人、捨てる集団は数人）＝**集団を保つために作った規則が集団を壊した**
- [x] **55b（地域信念版）は作らない**——**失敗しているのは粒度でも正確さでもなく「行くこと」そのもの**なので、粗くしても同じところで失敗する。**復活の条件は「行かずに使える」形を思いついたとき**
- [x] **55c（段階7d を覆す版）は保留のまま**——**足りないのは動機ではなく、行くこと自体が損**なので、目的項を足しても同じところで失敗する見込み

---

## P10：マップの性格（2026-09-12 追加。P9 のあと）

> **「地図によって符号が変わる」が実測で5例たまりました**（運搬 +1.25 対 −8.90 \*、贈与 +9.35 \* 対 −11.27 \*\*、魚 −16.7 \*\*\* 対 −5.3、特産と石は地図の作者が入れるもの、調理は**両方の地図が同符号になった初めての例**としてわざわざ書いてある）。**それなのに受け入れ基準（あるべき状態）は、どの世界で判定するものなのかを一度も書いていません**——**56 はそこだけを直す、コードを1行も変えない段階**です。**残りの8つはマップが決められることを増やします**が、**増やすのは世界を建てる側の自由度であって規則の数ではありません**。詳細は `PLAN.md` の「マップの性格（段階56〜64）」、決定事項は #88〜#96

**順序の理由（56 → 57 → 58 → 59 → 61 → 60 → 62 → 63 → 64）**

- **56 は他の全部と独立で、コードを1行も変えない**ので最初に通してよい。**以後の段階を「どの世界で測るのか」を書く土台にもなる**
- **57（`RegionBias`）も完全に独立**。**「地域から離れない理由」への6度目の挑戦**で、**他のどの段階より先に測ってよい**
- ~~**58 と 59 は対**~~ → **58 は完了し、順序が変わった（2026-09-12）**。**58 の実測で「地域ごとに外敵の性格が違う」という 59〜63 の土台が現状では成り立たない**と分かった（湧いた瞬間の偏りは +0.25 \*\*\* なのに、居場所に残るのは6%）。**新しい順序は 59 → 64 → 61 → 60 → 62 → 63** で、**64（湧いた地域から離れるコスト）を前倒しする**——**あれは「危険地帯」の付け足しではなく前提だった**
- **61 は 58〜60 と独立に実装できる**が、**58〜60 と組み合わせて初めて「本当に危険な地域」が作れる**。**60 より前に置いたのは、60 が食料のプールに新しい消費者を足す段階だから**——**総量を動かせる状態を先に作っておかないと、非線形に効いたときに「総量のせいか消費者のせいか」を分けられない**
- **60 は 59 に依存**（食欲はタイプごとの属性としてテーブルに乗る）
- **62 は 59 に依存**（対抗する相手＝タイプが先に無いと定義できない）
- **63 は 58・59・62 に依存**（地域紐付け湧きの水域版＋タイプ1行＋対抗スキル1つ）。**既存の食料魚には一切触らない**
- **64 は 58 に依存**（湧いた地域＝`HomeRegion` の記録）。**57 と組み合わせるとより一貫した性格になるが、必須ではない**

**全体を通じた条件**

- **57〜64 はどれも既定値ゼロ／既定テーブル1行／既定オフで、乱数を1つも余分に引かない**（＝**指紋テストが無変更で通る**）。**段階45 が踏んだ罠**（確率の抽選を足したせいで2つの腕が別の乱数列を走る。段階50 でも同じ形で「見つける」を確率から回数に変えた）**を、9段階ぶん繰り返さない**
- **60 と 61 は食料の保存則に触る**ので、**他の段階より慎重に、小さい値から**測る

### 56. 段階56：「あるべき状態」を土台とマップの性格に分ける【完了・2026-09-12】
> **結果: 基準は1つも変えず、列1本（判定する世界）と行2本（G・H）と `CLAUDE.md` の節1つを足した。** **コードは1行も変えていない。** **直した穴は「A〜E がどの腕の数値なのかどこにも書かれていなかった」こと**（地図で符号が逆になる例は5つ）。**書いてみて分かったのは、保存則が守られていたのに明文化されていなかったことと、「閾値を作らない」がマップごとに難易度を振ってよいことの根拠そのものだったこと**。詳細は `PLAN.md` の段階56 と `HISTORY.md`

- [x] **表に「判定する世界」列を1本**（平坦な校正世界／遊ぶ地図／複数マップ全般）。**F は既にその書き方をしているので、列に一般化するだけ**
- [x] **G（探索はプレイヤーの仕事）**——`richGain` / `shelterGain` / `drawShare` が測ってきたのは**選別**であって探索ではない（段階14・15 の実測）。**探索の余地はマップの構造が生むもの**
- [x] **H（マップごとの性格は測定対象ではなく設計対象）**——平坦な世界で確認するのは「既定オフで指紋テストに影響しないこと」だけ
- [x] **`CLAUDE.md` に「マップ共通の土台」の節**（新しいルールは増やさない。回復経路・閾値を作らないこと・役割を重ねないこと・**保存則**）
- [x] **閾値を作らないことの帰結を1文**——**極端なマップ設定は degrade にしかならず、「構造的に生存不可能」という壊れ方をしない**。これがマップごとに難易度を振ってよい唯一の根拠
- [x] **保存則を1文だけ新しく明文化する**（予算の継承総量・死体の claim・食料の総量が閉じていること）。**段階61 が初めてこれを緩めるので、緩める前に書いておく**
- [x] **コードは1行も変えない**（`go test ./...` が前後で同じであることだけ確認）

### 57. 段階57：`RegionBias`（現在地に効く、非可搬の能力補正）【完了・2026-09-12】
> **結果: 6度目の不発。ただし失敗の仕方が違う。** **的は大きかった**（`regionsSeen` 5.91／12＝一生に世界の49%を歩く）のに、**`standGain` は +0.01 \* で用量に応じず**、`regionsSeen` はむしろ **+0.14 \***。**理由は「知りようがない」ではなく約分**——**殴れる距離にいる組の100%が同じ地域**なので、**全員に同じだけ掛かる倍率は勝ち負けの比較から消える**。**そして対照（同じ倍率を身体につける）のほうが世界を動かした**（個体数 −8.40 \*、`dIntelligence` −6.82 \*\*）。**既定はオフ。** 詳細は `PLAN.md` の段階57 と `HISTORY.md`

- [x] **`region` にスカラー1本**（`Shelter` / `Food` / `Special` と同じ抽選パターン。**スプレッド0なら乱数を引かない**）
- [x] **掛け算は `Agent.Ability` の1か所**（年齢補正・性別補正の隣）。**`Genome` と継承には触らない**＝**測定が読む予算比は汚れない**
- [x] **上限（`MaxVitality` / `MaxSpeed`）に効かせるかを先に決める**——素直に掛けると**歩いて隣の地域に入っただけで現在体力が上限を超える**（`SexBias` は生涯不変なのでこの罠を踏まなかった）
- [x] **57a（選別だけ）を先に測る**——**効用式は「この先も足元と同じ地面」と仮定する**（段階20）ので、**移動先で補正を失うことは見積もりに入らない**。**段階14 がまさにこの形で +2% だった**
- [x] **57b（地域信念版）は「作らない」で決着**——**知って行っても、着いた先でも全員が同じ倍率を受ける**ので、**知識以前に差が存在しない**。**代わりに 57c（地域ごとに効く遺伝子が違う版）を立てた**＝約分をやめる一番安い形
- [x] **実装前に数える**: **一生に訪れる地域数の分布**（1地域で終える個体の割合）と、**段階38a と同じ的（`onDear`）**
- [x] **予想は6つ書いて5つ外した**（`standGain` は動かず、`regionsSeen` はむしろ増え、`shSpeed` も `clumping` も予想と逆かゼロ）。**当たった1つ（可搬な対照では `standGain` ≈ 0）も、本体が 0 だったので差が取り出せていない**
- [x] **上限には効かせていない**（`Agent.capacity` を分けた）——**地面で上限が動くと、境界を1歩越えただけで自分の上限を超えた体力を持つ身体ができる**

### 57c. 段階57c：地面が身体を選ぶ版【完了・2026-09-12】
> **結果: 約分は止まった。それでも並び替えは起きない——一生のうちに平均されるから。** **的は実在した**（`suitCeiling` 7.63%／15.51% \*\*\*）が、**取れたのはその2%**。**そしてその2%も立っている場所とは無関係**——**出生地の好みを持ち歩く対照が同じ大きさを出す**＝**観測された並び替えは全部「自分に合う地域で生まれた身体が少し多く生き残る」**。**既定はオフ。** 詳細は `PLAN.md` の段階57c と `HISTORY.md`

- [x] **地域の持ち物をベクトル1本に**（9本の平均を1に正規化＝**地域そのものの良し悪しは作らない**）。**約分が止まったことはテストで固定**
- [x] **測ったのは `suitGain` と、その天井 `suitCeiling`**（段階38 の「的の大きさに対して何%取れたか」の形）
- [x] **予想は5つ書いて3つ外した**。**一番の収穫は外れ方**——**「対照では 0 になるはず」が外れ、その理由が「対照は出生地の運を測っていた」**
- [x] **「場所ごとに違う選択は多様性を保つ」も外れ**（強い用量で `sdPower` −1.65 \*、`sdIntelligence` −1.21 \*＝**薄まる**）
- [x] **段階57 の結論**: **2つの形で2つの別の壁**（約分／一生のうちに平均される）**＋共通の3つ目**（効用式が移動先の地面を知らないので離れることに値段が付かない）。**居場所を動かすのは食料の分布だけ、の4度目の確認**

### 58. 段階58：外敵の湧きを地域に紐付ける【完了・2026-09-12】
> **結果: 湧きは狙いどおり偏り、一生のうちに散る。** **発火の瞬間は完璧**（`prowlArrive` +0.25 \*\*\*）なのに、**居場所には6%しか残らない**（`prowlGain` ±0.01）。**理由は2つとも数えた**——**新しい外敵の4割は世界の中で生まれ**（重みが触れていない）、**残りも出発点から地域1つぶん以上歩く**（平均270 対 地域幅200）。**偏らせると外敵はむしろ散る**（`enemyCrowd` −0.20 \*\*）。**段階57 と同じ形の3つ目で、初めて「消えない形」＝段階64 を名指しした。** 詳細は `PLAN.md` の段階58 と `HISTORY.md`

- [x] **`region` にスカラー1本 ＋ `pickFoodRegion` と同型の抽選**（0 で乱数を引かない）。**人間の湧きには触っていない**
- [x] **地域単位の測定を作った**（`World.Prowl` と地域ごとの死亡タリー。**保存にも入れた**）
- [x] **「規則が発火した瞬間の効き目」と「世界に残る効き目」を別の列にした**（`prowlArrive` 対 `prowlGain`）——**これが無ければ「重みが効かない」と書いて終わっていた**
- [x] **予想は5つ書いて2つ外した**（外したのは「湧いた場所に留まる」という暗黙の前提と、その帰結）
- [x] **順序への影響**: **59〜63 の土台が現状では成り立たない**ので、**段階64 を先に通す**

- [ ] **`region` にスカラー1本（`Danger` 仮）** ＋ **`pickFoodRegion` と同型の重みつき抽選**。**新しい抽選の仕組みは作らない**
- [ ] **人間の湧きには触らない**（人間は出産でしか増えないので、効くのは初期配置と `EnemySpawnTicks`）
- [ ] **地域単位で読める測定をこの段階で作る**（`CensusTracker` の地域版か簡易集計）。**58〜61 はこれが無いと何も言えない**
- [ ] **予想**: 密度に意図した偏りが出る／**世界全体の個体数は動かない**（総量は変えず位置だけ＝段階15a の論法。**動いたら動いた理由のほうが結果**）／`fightRatio` と `starveRate` は**その地域だけで**動く

### 59. 段階59：外敵タイプのプリセット表（`Species` は増やさない）【完了・2026-09-12】
> **結果: 表は設計どおり動き、混ぜ方が世界の選択を変えた。そして種類は種ではなかった**（求愛は種類を見ないので、2つの種類は最初から交配していた＝テストが落ちて分かった。**子の種類は予算と同じコイン**）。**表は効き**（`kindGap` +0.47 \*\*\*）、**`Homing` は効かず**（段階58 どおり）、**重いのが4分の1なら殺しとパワー・半分なら狩りと凝集**（ただし `dIntelligence` −3.98 \*\*）。**既定は空の表＝1種類。** 詳細は `PLAN.md` の段階59 と `HISTORY.md`

- [x] **1行が持つのは4つ**（名前・割合・予算の範囲・`Homing`）。**`Agent.Kind` はタグ1つ**（湧いたときに決まる固定参照）
- [x] **増やしやすさを要件にした**（ユーザの指示）: 行数を数えない／空欄は既定に落ちる／割合は正規化／**意思決定エンジンに種類の分岐を作らない**／**指標は表の長さに依らない**。**3行のテストで固定**
- [x] **対照は `kindssame`（同じ中身を2行に分けただけ）**——**baseline と比べると段階58 のぶんが混ざる**（対照の `halfLife` −5.16 \* は 58 単独とほぼ同じ）
- [x] **`kindMix` の底は標本**（0.05〜0.07＝到着48体に対する 1/√48）
- [x] **予想は5つ書いて2つ外した**（`kindMix` の底と、対照の読み方）

### 61. 段階61：地域の食料総量を可変にする（再正規化をやめられるようにする）【完了・2026-09-12】
> **結果: 実装前に前提が崩れた。** **再正規化は錠前ではない**——地域の重みは比としてしか使われないので、割り戻しても配分は動かない（川の地図で1ビット同じ実行）。**錠前は総量**で、`FoodTotalFromMap`（`plantRate = FoodSpawnRate × 重みの平均`）を足した。**測定では動かない**（±6〜13% で 1.0〜1.3 SE）——**古い旋盤（`FoodSpawnRate` 直接）で同じ用量を回しても動かない**ので、**その地図がその margin で食料に頭打ちされていないだけ**。**既定はオフ。** 詳細は `PLAN.md` の段階61 と `HISTORY.md`

- [x] **`FoodRenormalize`（既定 true）／`FoodTotalFromMap`（既定 false）の2つ**。**効くのは後者だけ**
- [x] **用量を列にした**（`foodMean` / `plantRate`）——**ティが重みを [0,2] に切り詰めるので、平均は地図から見積もれない**
- [x] **新しい旋盤を古い旋盤と同じ用量で並べて回した**＝**今回の一番の収穫**（やらなければ 2 SE の見間違いを書いていた）
- [x] **保存則を初めて意図的に緩めた**ので `CLAUDE.md` の「マップ共通の土台」に但し書きを入れた

### 60. 段階60：外敵が人間の食料を狙う（消化可否と食欲の2階建て）【完了・2026-09-12】
> **結果: 外敵が4倍になった**（12.4 → 49.0、+36.67 \*\*\*）。**植物の総量は1つも増えていない**ので、**外敵の数を決めているのは湧きの設定ではなく餌**。**人間は −6.17 \***。**そして `pop` は +34.54 \*\*\* で、そのすべてが外敵**——**種族ごとの列を足して初めて人間が減っているのが見えた**（同じ日に2度目の「列がなければ逆を書いていた」）。**的はこの群で一番大きい**（外敵の3分の2が視界に植物を持っている）。詳細は `PLAN.md` の段階60 と `HISTORY.md`

- [x] **消化できるかは種類の行**（`CanEatPlants`）。**触ったのは `canEat` の1か所**
- [x] **食欲は `dietValue` の中の掛け算1つ**（見積もりと一口が同じ倍率を通る）。**コインフリップは書いていない**
- [x] **「門ではない」ことをテストで固定**（同じ空腹で食欲1.0 なら食べ 0.25 なら食べない。**どちらでも候補には挙がる**）
- [x] **種族ごとの死に方の列を足した**（`humanKillShare` / `enemyKillShare` / `humansByEnemy` と `humans` / `enemies`）
- [x] **予想は5つ書いて2つ外した**（`humanKillShare` の向きと、的の大きさ）

### 62. 段階62：外敵タイプごとの対抗スキル【完了・2026-09-12】
> **結果: #94 の賭け（命に関わる的なら到達率が大きい）は外れた。** **仕組みは動く**（保有率 0.26〜0.32、実効値 0.24）が、**届くのは人間の死の2%**（保有率 × 実効値 × 効き ＝3.1%、人間の死のうち外敵が関わるのが65%）。**「命に関わる」ことが大きくするのは掛け算の3つ目の項だけ**。**2つ目が伸びないのは、天井にした防御力が予算比 0.06 ＝一番買われていない遺伝子の1つだから**（段階47 が理性で同じことを踏んでいた）。**既定は「どの種類も `Ward` を持たない」。** 詳細は `PLAN.md` の段階62 と `HISTORY.md`

- [x] **スキルは1種類**（`SkillWard`）。**どの獣かは種類の行が名指しする**（`EnemyKind.Ward`）＝2つの種類を同じ言い伝えで防げる
- [x] **形は `SkillPoison` の流用**（打撃から引かれるぶんと、その獣に付ける値段が同じ数字）。**相手の攻撃力は隠しパラメータのまま**
- [x] **天井は防御力**、**出生地の種は「その地域に言い伝えのある獣がどれだけ湧くか」**（テストで固定）
- [x] **予想は5つ書いて2つ当たり、2つ外れ、1つは的がなかった**（`wardGap` は地形の指標で、平坦な腕では定義されていない）

### 63. 段階63：水中の外敵（既存の食料魚には触らない）【完了・2026-09-12】
> **結果: 川に何かを住まわせても、人は川から出ない**（出ないどころか `humansWet` +0.04 \*）。**仕組みは動く**（`enemiesWet` 0.12 → 0.30）。**これで4度目**——**34（溺れる）・35（覚えて伝える）・63（住んで狩る）はどれも動かず、動かしたのは段階36 の食料だけ**。**溺死 −4.69 \*\* は死因が排他だから**（獣に殺された身体は溺死に数えられない）。**既存の食料魚には1行も触っていない。** 詳細は `PLAN.md` の段階63 と `HISTORY.md`

- [x] **表に1行**（`EnemyKind.Water`）。**湧く場所と溺れないことは同じ1つの事実**なのでフラグ1つ
- [x] **湧き場所は `w.water`**（`spawnFish` と同じキャッシュ）。**水の無い地図では普通の種類と同じように湧く**（黙って落とすと外敵の数が黙って変わる）
- [x] **既存の魚との横取りは作っていない**（#78 の保留は保留のまま）
- [x] **予想は6つ書いて3つ当たり、2つ外れ、1つは理由が違った**

### 64. 段階64：外敵の行動範囲を緩やかに制限する【完了・2026-09-12】
> **結果: この群で初めて、狙った量が狙ったとおりに動いた。** **段階58 の湧きの偏りが居場所に残るようになり**（6% → 45%、`prowlGain` +0.06 \*\*\*）、**危険地帯が初めて噛んだ**（`prowlBite` +0.21 \*\*\*）。**効いているのはコストであって帰り道ではない**（帰り道は決定の1%＝55a の正反対）。**代価は外敵が飢えること**（`hunts` −56 \*\*\*、`rareShare` 0.15 → 0.10）＝**「危険地帯を作る」と「世界を穏やかにする」は同じ操作の両面**。**既定はオフ。** 詳細は `PLAN.md` の段階64 と `HISTORY.md`

- [x] **`Agent.HomeRegion` は固定参照**（湧いた／生まれた地域。移住も更新もしない）
- [x] **コストは `Hazard` と同じ形**（1tickあたり、遠いほど高い＝`Utility.Roam`）。**自分の地域の中は無料**
- [x] **引き戻しは作らず、「値段だけで、行ける」ことをテストで固定した**
- [x] **帰り道は候補1つで値打ちを持たない**（安いから勝つ）。**無ければ遠くの身体は全候補を等しく嫌いになるだけ**＝55a が裏側から見つけたこと
- [x] **種類の行にも乗せた**（`EnemyKind.Homely`）
- [x] **予想は6つ書いて4つ当たり、1つ半分、1つ外れ**（外れたのは「人間側の指標は動かない」）

---

## P11：伝わる機会（2026-09-12 追加。P10 のあと）

> **出産のあと、親子と配偶者のあいだで言い伝えはどう伝わるか**という相談を段階に落としたものです。**新しい交換の仕組みは1つも作りません**——**`exchangeLore` は段階12b からずっと関係を読まない**（親子がよく伝え合うのは世界が二人を長く同じ場所に置くからで、家族についての規則があるからではない）ので、**この群が触るのは「どれだけ一緒にいるか」と「いつ1回呼ばれるか」だけ**です。詳細は `PLAN.md` の「伝わる機会（段階65〜66）」、決定事項は #97〜#99

**順序の理由（65 → 66、ただし独立）**

- **65（交配時の配偶者間伝承）は完全に独立**で、**触るのは `tryBirth` の中の1行だけ**。**先に測ってよい**
- **66（育児中の母の速度低下）は既存の `RearingRadius` / `keepToGuardian` / `GuardianID`（段階53a）に依存する**が、**コードは変えず値を読むだけ**。65 とは独立なので並行着手してよい
- **相談の4点のうち「父からの伝承」は「何もしない」で決着した**（#97）——**父子が伝承の射程に居合わせる機会が少ないのは段階53a の副作用として構造的にそうなっている**だけで、**増やす仕組みも塞ぐ変更も、`exchangeLore` が守ってきた「関係で特別扱いしない」線を壊す**

**全体を通じた条件**

- **どちらも既定で指紋テストが無変更**（`MateLoreChance = 0` / `NursingSpeedShare = 1.0`）
- **どちらも「的の大きさ」を先に数える**——65 は**交換全体に対する出生の割合**（見積もり1%）、66 は**母が圏内にいる割合**（段階53b の実測 0.774 が近い＝**増やせる余地は最大でも 0.226**）

### 65. 段階65：交配のときに配偶者どうしが伝え合う【完了・2026-09-12】
> **結果: 狙った量は動き、世界は動かない**——どちらも実装前に数えた2つの数字のとおり（**1出生に1回＝交換全体の1.01%**、**ペアが手を組んでいるあいだの交換はゼロ**）。**`mateGap` −0.10 \* 〜 −0.19 \*\***（交換の**前**に読んでいるので「近い者どうしが番うようになった」ぶん）、`loreSpread` −0.12 \*〜−0.18 \*、`teachTop` −0.01 \*。**個体数は1 SE で用量5倍の腕が一番小さい＝雑音**。**既定はオフ。** 詳細は `PLAN.md` の段階65 と `HISTORY.md`

- [x] **`tryBirth` の中の確率1回 → 既存の `exchangeLore` を1回**（`MateLoreChance`、既定0）。**新しい交換式は作っていない**
- [x] **実装前に数えた**（交換 31,304／出生 317／ペアどうしの交換 0）
- [x] **`mateWatch` は規則を入れても 0 のまま**（出産の交換は別に数える）＝**ペアは今も自分からは伝え合わない**
- [x] **`loreRate` は足したぶんより大きく上がる**＝**直接の効き目と二次的な効き目を同じ列で見ない**
- [x] **予想は6つ書いて5つ当たり**（外したのは「集団のばらつきのほうが弱いはず」＝**番いを近づけることと集団を混ぜることは同じ操作だった**）

### 66. 段階66：育児中の母は遅くなる【完了・2026-09-12】
> **結果: 狙ったものは全部動き、対照も同じだけ動かした。** 子は近くにいるようになり（`rearNear` 0.73 → 0.90）、伝承は増え（`rearMoved` +190 \*\*）、**子はよく育つ**（`grewUp` +0.04 \*\*\*）。**ところが「同じ遅さを子の有無と無関係に与える」対照が同じかそれ以上動かす**——**買っているのは「遅い母」であって「子のそばで遅い母」ではない**。**構造が効いたのは集団の持続だけ**（`halfLife` +17.26 \*\* 対 +4.22 \*）。**既定は無効。** 詳細は `PLAN.md` の段階66 と `HISTORY.md`

- [x] **`MaxSpeed` には入れず、段階54・57 と同じ形のフラグ**（`Nursing`）。**速さを読む3か所を揃えた**
- [x] **判定は tick の先頭の専用の走査**（移動ループの中では間に合わない）。**`stillReared` は答えると同時に子ども時代を使うので呼べない**——訊くだけの述語を別に書いた
- [x] **実装前に数えた**（母子の交換は全交換の 6.6%、`rearNear` の余地は 0.2 ほど）
- [x] **境界のガタつきは無い**（**18 nursing-tick に1回しか状態が変わらない**。目視でも確認）
- [x] **予想は5つ書いて3つ当たり、1つ半分、1つ外れ**（外したのは「構造のある腕が対照に勝つ」）

---
