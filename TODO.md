# TODO（やることリスト）

## この文書の読み方

- **ここにあるのは、まだ終わっていない作業だけです。** 終わったものは**この文書に残しません**——段階ごとの結論は `docs/stages.md`、測定値は `HISTORY.md`、以前のこの文書の中身は `docs/todo-archive.md`（凍結）。
- **上から順にやります。** 番号は**実装順**で、**前のものが後のものの前提**になっているときは各項目の「なぜこの順か」に書いてあります。**前提が無いものは、そう書いてあるので前倒しできます。**
- **項目の形は全部同じです**——**見出し（番号・題・状態）／一文で何をするか／チェックリスト／なぜこの順か／壊してはいけない線**。
- **チェックの意味**: `[ ]` まだ。`[x]` 済み。**1項目ぜんぶ済んだら、`docs/stages.md` に1行足してこの文書から消します。**
- **設計・数え上げ・予想は `PLAN.md` の同名の節**にあります。ここには書きません（同じことを2箇所に書かないため）。**終わった段階の設計は `docs/history/plan/`**（2026-09-21 に `PLAN.md` から切り出した。索引は `PLAN.md`）。

## 現在地（2026-09-23）

**段階100（一歩先の地面を読む）まで完了し、既定にしました。** **2026-09-19 に「Tiled で描いた地図を読む」も通し、2026-09-20 に 1〜5・7〜10・12 を通しました**（記録は `docs/stages.md`）。

**24（経験から学ぶ相関の計器）は 2026-09-23 に通しました**——**既定はオフ**。**門は通り**（効用式が書いていない関係が32〜40本。**判定は2度直した**）、**設計が3つ切れました**（鍵を argmax で取らない／食料の物々交換は作らない／イベントは身体自身の率を目盛りに切る）。**残る一番大きい問いは「機会は68%あるのに、交換の賞金が評価誤差の0.41倍しかない」こと**——**本体に入る前に賞金を誤差より大きくする道を1つ決めること**（`PLAN.md` #148）。

**同日の追加（ユーザの指定）: 本体は「合成」ではなく「物への逆伝播」で作ります**——**リンクに登場した物に結果の値打ちを符号つきで配る**（合成は中間項を落とすので、硬貨を拾う理由が式から消える）。**計器で測って機構は働きました**: **1つ手に入れることが予告する値打ちは、硬貨 +43.25 / 肉 +25.41 / コート +24.15 / 皮 +21.20 / ただの飾り物 −18.61**（`LifeValue` 100・評価誤差 10.4）。**一番きれいなのはコート対ただの飾り物の43点差**——**同じ物・同じ作り方・同じ手の枠で、違うのは寒さを止めるかどうかだけ**。**交絡の対照も作りました**（同じ状態の身体が普通に得るものを基準にした層別推定）——**予想は2つとも外れ、外れ方が方法の話でした**: **硬貨は15%しか落ちず**（交絡はこの数字の6分の1しか説明しない）、**コートと皮は6割落ちるがそれは過剰な制御**（層別に使った「消耗」こそコートが防いでいるもの＝媒介変数での条件付け）、**肉は106%残る**。**この対照は物によって意味が違います**（`docs/history/findings/4.md` の 9b・9b-2・9b-3）。

**25（物の値打ちを学ぶ＝#148 の本体）は 2026-09-23 に通しました**——**既定はオフ**。**この鎖で一番大きく売買を動かした規則です**（`sales` +1.58 \*\*、用量反応あり）。**200000tick × 32シードで取り直したら代価は無く、強い用量はむしろ利得でした**（`collapsed` −0.09 \*・`peak` +15.72 \*・`sales` +292 \*\*\*）**ので、既定の重みを 0.25 → 1 に変えました。** **残る宿題は2つ**: 二重計上の根治（式が予測した分を引いた残差を学ぶ）／寒さの用量を下げた皮の世界で測り直す。

**残っている作業は4項目で、順序は 21 → 22 → 23 → 11 です**（**番号は立てた順、上の行が実装順**）: **21（飛行の天井を既定で2段に）→ 22（訓練された民を巣に配る）→ 23（遊ぶ世界を先に育てる）**、そして **11（セーブのアップロードの薄い API。いつでもよい）**。**21〜23 は 2026-09-23 にユーザの指定で立てたもので、設計は `PLAN.md` の #144〜#146**。**王朝ものの勝ち条件は変えません**（#147＝**その国で産んだ子の数は条件ではなく戦略**）。

**24 は同じ日にユーザが提起した #148 の入口です**——**「この条件でこの行動をとると、少し先でこういうイベントになりやすい」を経験から学び、合成で鎖を縮め、目撃でも学び、伝承で配る**という一式で、**期待は貨幣が集団の合意として立ち上がること**。**本体は先読み以来の規模**なので、**まず読み取り専用の計器で「その相関が実在するか」を数え、その数字で設計を決めます**（設計・数え上げ・崩してはいけない線は `PLAN.md` の #148）。**検討の過程で、この世界は物々交換を一度も作っていないことが分かりました**——**段階48 が実装したのは一方通行の贈与**で、**2品が同時に動く語彙は今も無い**。

**20（人間が湧く巣）は 2026-09-22 に通しました**——**既定は空**。**地図の作者が「ここから民が始まる」と言えるようになり、巣ごとに1本の血統タグ（＝画面上の色1つ）を配ります**。**得たものは測定のほう**: **(a) 置き場所は血統の数を動かす**（3か所から出すだけで 5.08 → 2.67 \*\*\*）、**(b) 本当に支えると3つの国が3つとも残る**（`lines` 2.92 / 3）、**(c) それでも支えは自分より長く続かない**——**村が黙って100年たつと、世界はその村が無かった世界と完全に同じ**（人間 177.1 対 177.1）。**巣が決めるのは誰がどこで始めるかで、誰が残るかは世界が決める。**

**19（巣の主を誘い出す）は 2026-09-22 に通しました**——**既定はオフ**。**規則は設計どおり動きます**（誘い出し→戦い→休暇→主が戻る、全部テストで固定）**が、巣を空けることに測れる値打ちがまだありません**（32シードで個体数も定住も誤差内）。**得たものは3つ**: **(a) 呼び出した主の4分の3は倒されずに帰る**（0.23〜0.28 しか倒されない）／**(b) 強さが動かすのは倒される割合ではなく年数**（3.57 → 6.36 \*\*\*）／**(c) 身体には世界の天井があり**（予算900＝平均的な身体の2.16倍）、**「天才でなければ倒せない」は数値でも作れない**。

**18（発生率と定員を巣ごとに）は 2026-09-22 に通しました**——**4つとも既定はオフ**（`NestRateMap` / `NestCap` / `NestCapMap` / `EnemyAmbientShare`）。**前提は生きていた**: **段階58 の「湧きの偏りは居場所に残らない」は地域の粒度の話**で、**巣の粒度なら 1:9 の塗りが 0.87 まで残る**（`nestRated`、対照0.39）——**ただし `EnemyHomeCost` が入っている世界だけ**（`nestGain` 0.07 → 0.20 \*\*）。**数え上げが設計を2度切りました**（`EnemyTotalFromNests` は作らず、`EnemyAmbientShare` の既定は0だった）。**そして予想していなかったことが1つ**——**定員を締めると人間が減る**（`pop` −10.42 \*）。**外敵を減らすと肉が減るから**で、**19番が踏む段差です。**

**3件が共有する最初の1つは 18 で数え終わりました**——**巣のまわりに外敵は残る**（`nestGain` 0.20 \*\*、ただし `EnemyHomeCost` 2 の世界で）。**したがって 19 の「巣が静かになる」には報酬がありうる**が、**測るときは肉の側を一緒に読むこと**（上）。**そして世界は `MaxEnemies` に張り付いているのに、天井を倍にしても外敵は +2.34 \*\* しか増えません**——**数を決めているのは天井ではなく世界**（段階60 の再演）。

**19 は王朝もの（6番）の梃子の候補でもあります**——**2026-09-21 の測定で「旅も転移先の選択も勝率を1ビットも動かさない」（0.75 対 0.75）と出た理由は「勝利条件が集団のふるまいなのに、プレイヤーは1体しか握っていない」**でした。**誘い出しは、1体の身体が集団の環境を変えられる数少ない手**なので、**6番の目標 (b)「選択が勝率を動かすこと」に直接あたるかを 19 の数え上げで読みます。**

**6（継承と転移＝王朝の遊び方）は 2026-09-21 に通しました**——**勝ち負けのあるゲームがこれで初めて成立**（`cmd/devview -dynasty`）。**engine に足したのは読み取り専用の2本だけ**（`LineHeirs` / `SettledIn`）で、**クリア条件も10年の待ちも転移も全部ゲーム側**。**数え上げと、そのあとのユーザの指摘で、`PLAN.md` が選んでいた継承則 `LineageRegionChain` を削除しました**——**あの則はタグを出生の瞬間にしか配らない**ので、**兄が立っている区画で生まれた弟は永久に担い手にならず、担い手が死んだ区画は次の出産まで空**（遊ぶ地図で絶滅率 0.55）。**意図は「長男が死んだら次男、兄弟が尽きたら三男の子」**で、**それは死んだ瞬間に変わる問いだからタグでは書けません**。**`World.LineHeirs`（区画ごとに最年長の生きている担い手）がそのまま満たしています**（3兄弟の例をテストで固定）。 **ただし、通したあとに「ゲームとして成立するか」を測ったら、まだ成立していません**（12シード × 60000tick・誰も操作しない床）: **今の実装のままだと 0.83 で負け**（`quickestBody` が血統の生死を見ないので小さい血統を渡される＝**何もしないうちに決まるコイン投げ**）、**大きい血統から始めるよう直すと今度は 0.67〜0.92 で「何もしなくても勝つ」**、**そして「どの国で続けるか」の選択が勝率を1ビットも動かさない**（0.75 対 0.75）。**「自分の民族」を自分の子孫に狭めても同じ。** **構造の問題**——**勝利条件は集団のふるまいで、プレイヤーは1体しか握っていない**。**次に触るときの目標は (a) 操作しない床の勝率 < 0.2、(b) 選択が勝率を動かすこと**（数値は `PLAN.md` と `docs/history/work/12.md`）。

**2026-09-21 に手当てを2つ試しました**（ユーザの指定）: **人間に行動圏を与え**（`HumanHomeCost`、既定0＝`-settle`）、**死後の待ちを10年から1年に**（**「10年に1度の天才」の想定を取り下げたので長さが自由になった**）。**待ちの短縮は効きました**（絶滅 0.92 → 0.56〜0.75、遊べる世代が約2 → 約3.5）。**行動圏も狙いどおり徘徊を減らします**（訪れた地域 7.0 → 6.3〈用量1〉→ 5.4〈用量3〉）。**それでも旅は勝率を動かしません**（36シードで 0.31 対 0.25 ＝雑音）——**ただし「建国」だけは +26% 増える**（1.47 対 1.17）。**診断はこの差**: **プレイヤーは新しい国に子孫を置けるが、置いた国を同時に保てるだけ血統を生かしておけない**（どの腕でも絶滅が 0.56〜0.92）。**次に測るべきは「プレイヤーに血統の生存を動かす手があるか」。** **なお測定のプレイヤーは粗い方策なので、これは床であって天井ではありません。**

**申し送り**: **`tiled/samples/001_3division.tmj` は地域レイヤが非表示なので、読むと地域が0個になる**（前からある、今回と無関係の問題）。

**16（押すことを選ぶ）は 2026-09-21 に通しました**——**既定はオフ**（遊ぶ地図で個体数を 19〜27 体払う）。**得たものは規則より2つの一般則**: **(a)「情報だけでは世界は動かない」が6度目にして初めて覆った**（**信念を凍結すると個体数 +18.7 \*・崩壊 0.17 → 0.00 \***）**が、同じコミットの中の別の情報（背後の崖が見える）は何も動かさなかった**——**効用式の掛け算側に入る情報は手の選び方を変え、足し算側に入る情報はまず「非ゼロになる割合」を数えるべき**。**(b)「選べる」ことは規則を強くせず弱くした**——**受動ノックバックは打撃のたびに起きるが、選べる押しは打撃を捨てて初めて起きる**（平坦な世界で `killShare` 0.81 → 0.68 \*\*\* 対 0.79 の誤差内）。**15 が学ぶべき鍵については、崖も水も的が 5.4〜5.9% / 0.14〜0.32% しかないことが確定**——**「間合い」のほうを見ること。**

**15（死に方を覚える）は 2026-09-21 に通しました**——**既定はオフ**。**得たものは、この項目でも規則より測り方**: **代価を共有する対照（`lessonblind` ＝枠は買われ、死は見られ、パターンは昇格して枠を埋め、決定に届かない）を baseline の下に並べると本体と区別がつかない**（`pop` −25.6 \* 対 −14.5）**が、対照自体を基準に取り直すとはっきり違う**（`pop` −11.08 \*・`fights` +6089 \*・`flees` +2.63 \*）——**予算が動かす量のほうが規則が動かす量より大きいときは必ずこうなります**。**宿題が1つ残りました**: **過去に「情報だけでは動かない」と結論した測定**（段階31・35・49・100 と 14番・17番）**が同じ形になっていなかったか確かめること**——**少なくとも段階100 の `aheadblind` は代価を共有する対照**です。**そして数え上げが2度、設計を切りました**（`PLAN.md`）。

**依存関係**: **11 は他に依存しません。** **19 と 20 は 18 が作った巣の帳簿の上に乗ります**（2026-09-22 に通しました）。**20 は外敵の湧きに1行も触らないので前倒しできます**（王朝もの＝6番が建国者の置き場所を要るなら、そちらを先に）。**他の項目は【完了】の印つきで下に残してあります**（消すのは指示があってから）。

**2026-09-20 に分かった、次に効く3つ**:

- **遊ぶ地図は今の既定では 20000tick で 42%崩壊する**（飾り物を入れると58%）。**段階82・84 の飾り物の数値はもっと保っていた頃の地図のもの**で、**飾り物の群（12番）は平坦な世界で測り直した**
- **市場を代価なしに動かした規則は1つだけ**（**89b ＝贈与を「当たったかどうか」で払う**。売買 +0.71 \*\*）。**次に市場を触るならこの上に乗せる**
- **防寒具を商品にする案は閉じた**（8番。5度目）

**ブラウザのクライアント（9〜11）は、下ごしらえが済んでいます**——**2026-09-19 に WASM でビルドも実行も確認**（コードを1行も変えずに、Windows の Chrome で canvas 生成・世界が進み・地形とノードと HUD が描かれた）**し、5つを測りました**: **速度 614µs/tick＝native の1.54倍・16.7ms に27tick**／**`devview.wasm` 14.2MB〈gzip 3.4MB〉・起動0.32秒**／**育った世界のセーブ 0.90MB〈gzip 0.15MB〉**／**画面に約300個**／**タップで訊かれるのは 112tick に1回**。**数値と手順は `PLAN.md` の「ブラウザで遊ぶクライアント」**。

**決まったこと（2026-09-19・ユーザの判断。決定事項#132）**: **開発機と端末で「続き」が違うことは許容します**——**この世界はプレイヤーが動かすので、続きはどのみちプレイヤー次第で変わる**から。**engine は直しません**（`decay` と `survives()` の自前実装も、arm64 の FMA を止める作業も、やらない）。**プレイヤーどうしは同じ `.wasm` なら1ビット同じ**（Chrome・Edge・node で digest 一致）。**必要になったときの逃げ道**＝配るスナップショットを WASM 側で育てる／試す（`node wasm_exec_node.js`）は `PLAN.md` に書いてあります。

**決まったこと（2026-09-20・ユーザの判断＝決定事項#136〜#138）**:

- **13**: **崖落ちを作る。** **押しは下りに限って `canStep` を無視し**（上りは無視しない）、**落ちたら落差に比例した追加ダメージ**。**死因のバケツは増えません**（落下死は既存の条件で自動的に戦闘死になる）。
- **14**: **好意を「避ける理由」ではなく「戦いを動かすもの」にする**——**嫌いな相手とは食料を争い、好きな相手の戦いには加担し、加担すると好意が上がる**（動機は先読みの好意狙い）。**項目の形が変わったので `PLAN.md` と 14 番を書き直しました。**
- **15**: **学習したパターンは hunch の枠と共用せず、予算から買う新しい遺伝スロットに置く**（`hintSlots` と同じ形）。

**同日に追加で決まったこと**: **好意はマイナスを許します**（14 番の残り1点＝ユーザの判断）。**負を許すこと自体は世界を1ビットも変えません**（今ある読み手は全部 `clamp(…, 0, 1)` で下を止めている）——**変わるのは負の側を読む項を足したときだけ**なので、**2つを別のコミットに分けられます**。

**そのとき直す継ぎ目が3つあります**（14 番に書きました）: **記憶の追い出しが恨んでいる相手を真っ先に捨てる**／**負の相手への贈与が「信頼を0しか買わない」と評価される＝仲直りの経路が消える**／**記憶が 99% 満杯なので、恨みの記録を `forced` にしないとほとんど残らない**。

**それ以外は上から順に進められます。**

**今の既定で覚えておくこと**（測定値を読むときに効きます）:

- **測定のベースラインは3度リセットされている**——段階7b（粒子的遺伝）／2026-09-13（先読み）／2026-09-19（一歩先の地面。**地形の腕だけが対象**）。`HISTORY.md` のそれ以前の数値は別の世界のものです。
- **旧世界を再現する腕**: `noahead`（先読み前）、`groundunread` / `playunread`（地面の読み前）、`beforeTheFlip`（段階84 の3つ）。

---

## 1. 豊かさを地域から切り離して塗れるようにし、食料の種類ごとに分ける【完了・2026-09-20】

**一文**: **地域（信念用の粗い区画）と豊かさグリッド（成長確率に直接効く実データ）を分け、植物と魚に独立したグリッドと取り分プールを持たせる。**

> **この項目はすべて済んでいる。** **`TODO.md` の決まりでは、済んだ項目はここから消して `docs/stages.md` に1行足す**（この文書を履歴にしないため）。**消すのは指示があってから。**

- [x] **崩壊率を数える腕**（2026-09-20。`cmd/experiment` に `collapsed` / `fellAt` / `peak` と `-deadbelow`。**20000tick では世界の運命が見えないと分かったため**＝`simulation/001_3division.md`）
- [x] **実装前に数えた**（2026-09-20。**この項目の形が変わった**——**細かくするのは測定上は逆向き**で、48区画・108区画にすると `richGain` が半減する。**作ったのは「細かいグリッド」ではなく「作者が塗れるグリッド」**。数値と予想は `PLAN.md` P16-2）
- [x] **豊かさグリッドを地域から分離し、Tiled で塗れるようにした**（2026-09-20。`engine/rich.go` / `Config.RichMap` / Tiled の `rich`。**既定はオフ＝塗らない世界は1ビットも変わらない**。**地域には塗りの平均が入る**ので信念・スキル・寒さは今までどおり地域を読む）
- [x] **魚のグリッドと取り分プール**（2026-09-20。`FishSpawnRate` / `MaxFishItems` / `FishRichMap`。**3つとも既定オフ**。**総量保存はプールの中で閉じる**＝意図的な緩め。**測ったら食料は増えたのに個体数は増えず、増えたぶんは溺死で払われた**＝`HISTORY.md`）
- [x] **植物の種類**（2026-09-20。`Config.PlantKinds` ＝決定事項#134 の「遺伝子の初期値の束」。**札は `plantGenes.Strain` で種に継がれる**。読み出しは `World.PlantStrains()`）
- [x] **タイルから植物の種類を塗る**（2026-09-20。`Config.PlantKindMap` ＋ `PlantKind.Key`、Tiled の `plant`。**地図は名前と場所だけを運び、数値は運ばない**＝決定事項#133）
- [x] **総量保存をグリッドの中で保つ**（2026-09-20・テストで固定。**塗っても `FoodSpawnRate` の本数は変わらない**）／**魚の側は未着手**
- [x] 文書（2026-09-20。`PARAMETERS.md`・`docs/features/`・`README.md`・`tiled/properties.md`・`HISTORY.md`）／**魚を足すときにもう一度**

**なぜこの順か**: **Tiled の読み込み（2026-09-19 に完了）が要る**（グリッドを塗る手段）。**そして「発生タイル」の層も同じ日に入った**（`SpawnMap`＝**塗られたタイルにしか湧かない**）ので、**この項目に残っているのは「重み」のほう**——**どこに湧くか（マスク）は済んでいて、どれだけ湧くか（濃淡）がまだ**。**そして拠点（6・7）はこのグリッドの高食料区画として定義する。**

**壊してはいけない線**: **総量保存**（段階15a）。**魚は今「植物の代わりに湧く」ので、独立プールにすると段階42・43・36 の数値が読み直しになります**——**意図的な変更として明記すること**。

---

## 2. 外敵の種別（type）を広げる【完了・2026-09-20】

**一文**: **`EnemyKind` の表に「移動能力フラグ」と「ドロップ」を足す。**

- [x] **実装前に数えた**（2026-09-20。**肉は食事の 0.16〜0.30 を占め、落ちた肉の 0.67〜0.80 は食べられている＝ドロップには的がある**。**一方で地形は居場所を面積比までしか動かしていない**（plateau で `onHigh` 0.33、river で `onDear` 0.13）**＝移動能力フラグの的は小さい**。数値は `PLAN.md` P16-3）
- [x] **移動能力フラグ**（2026-09-20。`Climbs` ＝段差だけ／`Flies` ＝段差・溺死・速度・消耗を無視し `FlyCost` を払う。**泳げるは `Water`（段階63）が既にある**。**4つ＋段差を分けて書いた**）
- [x] **ドロップ**（2026-09-20。`EnemyKind.Meat` ＝体の大きさに掛かる倍率、未指定は1）
- [x] **タイルから種類を引く**（2026-09-20。`Config.EnemyKindMap` ＋ `EnemyKind.Key`、Tiled の `enemy`。**塗った種類はそこにしか来ない**（`Share` は「どの種類か」、塗りは「どこに」）。**数値は地図に置かない**＝決定事項#133）
- [x] **飛行型に残す制約**（2026-09-20。**`FlyCost` の未指定は0ではなく1**＝**「渡れない地形は作らない」の鏡で、ただの地形も作らない**。谷をただで越えられると、谷は地形をやめて背景になる）
- [x] 文書（2026-09-20。`ENEMY.md`・`PARAMETERS.md`・`docs/features/enemy.md`・`PLAN.md`・`HISTORY.md`）

**なぜこの順か**: **12（材料と加工）の材料がここから出る**。**1・2 には依存しないので、前倒しできます。**

**壊してはいけない線**: **種類は種ではない**（段階59。求愛は種類を見ないので交配する）／**`Water` フラグは「溺れない・遅くならない・消耗しない」を1つの事実として束ねている**——新しいフラグも同じ粒度で決めること。

---

## 3. 外敵の巣（湧き地点）と徘徊【完了・2026-09-20。**継承は測って既定にしなかった**】

**一文**: **Tiled の点で巣を置き、外敵は巣から離れるほど高い代価を払う。**

- [x] **実装前に数えた**（2026-09-20。**段階64 の `prowlGain` 6%→45% は今の世界では再現せず 0.00**。**湧きの偏りは居場所に1つも残っていない**。数値は `PLAN.md` P16-4）
- [x] **巣は地図が塗ったセルそのもの**（2026-09-20。オブジェクト層の点は作らなかった——**`EnemyKindMap` が既にセル単位で種類を置いており、そこが巣**。**半径は `EnemyKind.Roam`** で、**数値は地図に置かない**＝決定事項#133）
- [x] **`Agent.HomeX/HomeY` ＋ `EnemyKind.Roam`**（2026-09-20。**地域の中心から、実際に来た点へ**。**半径は地域幅の0.5固定から行の数字へ**）
- [x] **生まれた場所のまま**（2026-09-20。**親の巣を継ぐ版を作って測り、効かなかった**——`prowlGain` −0.00 ± 0.02、しかも `killShare` +0.04 \*\*。**`Config.NestInherited` として残すが既定はオフ**）
- [x] 文書（2026-09-20。`ENEMY.md`・`PARAMETERS.md`・`docs/features/enemy.md`・`PLAN.md`・`HISTORY.md`）

**なぜこの順か**: **Tiled の読み込み（2026-09-19 に完了）が要る**（点を地図に置く手段）。**3 とは独立。**

**壊してはいけない線**: **閾値を作らない**——**`roam_radius` を「超えたら強制的に戻す」にすると、全段階で守ってきた線を初めて破ります**。**段階64 と同じく「離れるほど高い代価」**にすれば、十分に良い手があれば遠くでも選びます。

---

## 4. 民族を血統タグで追う【完了・2026-09-20】

**一文**: **ゲーム開始時の血統をタグとして子に継承させ、誰が「自分の民族」かを追えるようにする。**

- [x] **実装前に数えた**（2026-09-20。**偶然ほぼ常に起きる**——20000tick で残る約5血統のうち **92%が2地域以上、85%が3地域以上**に同時にいる。平均7地域／12。**クリア条件は「立っている」より強い意味の「定住」でなければならない**。数値は `PLAN.md` の王朝ものの節）
- [x] **タグの継承**（2026-09-20。`Agent.Lineage`。**建国者ごとに1本、子は母の血統**。**乱数を1つも引かない**＝テストで固定。**読み出しは `World.Lineages()`**）
- [x] **ゴールエリアの印**（2026-09-19 に先に入れた。Tiled で `goal` を付けると `RegionShape.Goal`、読み出しは `World.GoalRegions()`。**engine は1度も読まない**＝判定はゲーム側。**2026-09-20 から、矩形だけでなく地域を塗るタイルにも付けられる**＝**ゴールが L字でもよい**）
- [x] **保存/読込に載せた**（2026-09-20。`agentSnap` が `Agent` を埋め込んでいるので公開フィールドは自動で乗る。テストで固定）
- [x] 文書（2026-09-20。`NODE.md`・`docs/features/core.md`・`PLAN.md`・`HISTORY.md`）

**なぜこの順か**: **6〜9 の全部がこれを使います。** **1・2 には依存しないので前倒しできます。**

**壊してはいけない線**: **`engine` には「プレイヤー」を置かない**（段階19）。**タグは血統の印であって、操作者の印ではありません。**

---

## 5. 拠点と「定住」を測れるようにする【完了・2026-09-20】

**一文**: **豊かさグリッドの高食料区画を拠点と呼び、そこに血統が定住しているかを測る器を作る。**

- [x] **「住んでいる」の定義を決めた**（2026-09-20。**直近40読み（1000tick＝2年）の6割をその区画で過ごしていること**。3候補のうち「滞在の割合」——**出生地は変わらないので動いた血統と動かない血統を区別できず**、**顔ぶれの持続は既に `MembershipTracker` が別の問いに答えている**）
- [x] **測った**（2026-09-20。**生きている身体のうち、どこかに定住しているのは 0.44**——**半分以上は流浪している**。**「立っている」と「住んでいる」の差**: 3地域以上が木で 0.85 → 0.67、地域ごとで 0.40 → **0.13**）
- [x] **測定器**（2026-09-20。`engine/settle.go` の `SettlementTracker`。**読み取り専用・乱数を引かない**。`DefaultSettleStep` 25 / `DefaultSettleWindow` 40 / `DefaultSettleShare` 0.6）
- [x] `cmd/experiment` の列（2026-09-20。`settled` / `settleLines` / `settleRegions` / `settleInTwo` / `settleInThree`）
- [x] 文書（2026-09-20。`docs/features/core.md`・`PLAN.md`・`HISTORY.md`）

**なぜこの順か**: **1（豊かさグリッド）と 4（血統タグ）が要る。** **7 の勝ち負けはこの器で判定します。**

**壊してはいけない線**: **拠点を作ることは食料を増やすことではない**（段階15a の取り分の原則）。

---

## 7. 寒冷地の罰【完了・2026-09-20。**規則は足さず、既にあったものを殺す寒さと合わせたら効いた**】

**一文**: **防寒着が生存率を上げる。**（2026-09-20 にユーザが置き換えた。**元は「防寒着を持たない身体が寒冷地に定住しにくくなるようにする」**だったが、**数え上げで狙いが世界の動きと逆向きだと分かった**——**寒さは居場所を変えさせるのではなく動きを止める**ので、「定住しにくくする」は寒さに頼れない。**置き換えた一文は測って真**: **人口 41.5 → 84.0（+42.54 \*\*\*）**）

- [x] **実装前に数えた**（2026-09-20。**この項目の狙いが世界の動きと逆向きだと分かった**——**寒さを2.5倍にすると、寒い側から出ていくのは 0.53 → 0.45 だけ**（−0.08 \*）**なのに、定住は 0.44 → 0.56 に上がる**（+0.13 \*\*）。**寒さは居場所を変えさせるのではなく、動きを止める。** 数値は `PLAN.md` の段階7の節）
- [x] **段階86b が既にその形をやっていた**（2026-09-15。**候補が身体を置く先の寒さを値付ける**）。**結論は「寒さは避けられる。避ける価値が無いだけ」**——**地域1つ分の暖かさを買うのに地域1つ分を歩く**。**そして `onCold` を動かした唯一のものが防寒具だった**（+0.03 \*）
- [x] **罰の強さ**（2026-09-20。**`ChillDrain` 0.05 が「殺す寒さ」**——0.02 では誰も死なず、防寒着が効く余地が無い。**防寒着ありで人口84・崩壊率0.21、なしで41.5・0.50** なので、**「無いと詰む」ではなく「あるほうが有利」の側**）
- [x] **対照**（2026-09-20。`coatbiteless` ＝同じ世界・同じ飾り物・防がない。**`ChillKnown` の「感じない」対照は段階86 が既に持っている**）
- [x] 文書（2026-09-20。`PLAN.md`・`docs/features/land.md`・`HISTORY.md`。**規則は1行も足していないので `NODE.md` / `PARAMETERS.md` は変わらない**）

**なぜこの順か**: **5（定住の器）が無いと、罰が効いたかどうかを測れません。**

**壊してはいけない線**: **閾値を作らない**（「防寒着が無ければ入れない」は書かない）。

---

## 8. 材料と加工（防寒着を作って売る）【完了・2026-09-20。**市場は開かず、防寒具を商品にする案を閉じた**】

**一文**: **外敵のドロップを材料にして防寒着を作り、身内には贈与・他人には売買が自然に立ち上がるかを見る。**

- [x] **越えるべき数値が決まった**（2026-09-20。**ユーザの「防寒着の目的は商売を発生させることだった」から測った**）——**殺す寒さ・全部の飾り物が防ぐ・値段とコインありの世界で、1実行に 2820着 作られ 2491回 渡り、売買は 1.79回**（1400回に1回）。**世界の売買の総数は 2.33**（そのうち77%が防寒具＝**市場そのものが無い**）。**`coatsSold` 1.79 と `sales` 2.33 が、この段階が桁で超えるべき数値**
- [x] **梃子を2つ潰した**（2026-09-20）——**希少にする**（`coatrare`）は**売買 1.79 → 0.04** で市場ではなく凍死が増え（個体数 92.0 → 52.9）、**贈与に代価を付ける**（`coatpriced`）は**贈与も売買も減る**（段階84d・88 に続く3度目）。**残る梃子は #115 の「自給自足」＝この段階そのもの**
- [x] **実装前に数えた**（2026-09-20。**外敵の死は1実行に 59.4 回、人間が関与したのは 22.1 回、今の防寒着は 3104着**＝**材料にすると供給は桁で落ちる**ので、**用量を3つに割った**。**売買の成立率は 0.17**＝2.33 対 断り 4.58）
- [x] **材料（3 のドロップ）と加工の2段階**（2026-09-20。`FoodHide` ＝5つ目の食べられない品・`HidePerBudget` / `EnemyKind.Hide` / `WardNeedsHide`。**既定はオフ**。**値打ちは1か所**＝`craftWant` × `CarryValue`）
- [x] **新しい分岐で「身内／他人」を切っていない**（既存の効用比較のまま）
- [x] **立ち上がらなかった**（2026-09-20。**`sales` 2.33 → 1.00 \*\*\*、`coatsSold` 1.75 → 0.33 \*\*\***＝**桁で動くどころか減った**）。**何が足りないかは分かった**——**`hidesSpare` 0.53〜0.89 ＝売り手の余剰は初めて出たのに、売れた皮は 0.08 枚で贈られた皮は 47〜130 枚**。**`HandOverCheapest` を足した腕でも `hidesSold` は12シード全部 0.00**＝**足りないのは売り手ではなく、手渡しが無料で即時であること**（#115(a)。**値段を付けると交換そのものが消えることは4度測ってある**ので、**最小限の調整として入れられるものが無い**）
- [x] **したがって防寒具を商品にする案を閉じた**（87a・88・`coatrare`・`coatpriced` に続く5度目。**規則は残り、既定はオフ**——地図の作者が「獣を狩らないと冬を越せない土地」を作るのに使える）
- [x] 文書（2026-09-20。`NODE.md`・`ENEMY.md`・`PARAMETERS.md`・`docs/features/things.md`・`docs/features/land.md`・`README.md`・`PLAN.md`・`HISTORY.md`・`docs/stages.md`）

**なぜこの順か**: **2（ドロップ）と 7（寒冷地の罰）が要る。**

**壊してはいけない線**: **先に読むこと**——**段階87a は「770着が作られ85%が人手に渡り、その全部が贈与で売買は不動」**、**段階88 は「贈与に値段を付けると交換そのものが消える」**、**段階79・81 は「市場を閉じているのは `CoinValue` ではなく手が1つであること」**。**そして 2026-09-20 に「希少にする」も潰れた**——**足りないのは品薄ではなく「自分で作れないこと」**。

**この段階が失敗したら、防寒具を商品にする案そのものを閉じる。** **同じことを4度測ったことになるため**（87a・88・`coatrare`・`coatpriced`）。

---

## 9. 入力をタップと長押しに【完了・2026-09-20】

**一文**: **タップで対象選択、2回目のタップで既定行動、長押しで行動メニュー。**

- [x] **タップで対象選択 → 2回目のタップで既定行動**（2026-09-20。**食料＝食べる／外敵＝攻撃／人＝観察／地面＝そこまで歩く**。**人を2タップで殴らない**のは、取り消せないことを2タップにしないため——攻撃は長押しのメニュー）
- [x] **長押しで行動メニュー**（2026-09-20。**その狙いに意味のある命令だけ**を並べ、**数字キーでも選べ、外をタップすれば閉じる**）
- [x] **メニューを4つの質問にも使った**（2026-09-20。後継選び・求愛の返事・訊かれるモードの質問・節目の選択＝**数字キーだけだったものが指で答えられる**。**新しい訊き方は足していない**）
- [x] **キーとマウスは残した**（**指とマウスは1つの層**＝`pointer`。**クリックは押した瞬間から離した瞬間に変わった**＝それがタップの定義）
- [x] **判定は時間で、フレーム数ではない**（2026-09-20。**フレーム数で2度間違えた**——遅い端末では長押しが1.5秒になり、**ブラウザで1秒に4フレームしか来ない状況を実測した**。**押している間に1フレームも来なかった長押しは、離した時点で長押しとして扱う**）
- [x] **目視**（2026-09-20。**WASM ＋ WSL から配信 ＋ Windows の Chrome ＋ 合成タッチイベント**。**タップで狙い、2度目で `walking to the ground at 164,560`、長押しで4項目のメニュー**。**手順の罠2つは `PLAN.md` と `HISTORY.md`**）
- [x] 文書（2026-09-20。`README.md`・`docs/features/game.md`・`PLAN.md`・`HISTORY.md`・`docs/stages.md`）

> **残っているキーボード専用**: エディタ（F3）・速度・ズーム・パネル切り替え・履歴・後継の指名（`k`）・構え（`s`）・保存（F2）。**遊ぶのに要るのは構えとズームくらいで、10番（描画をタイルに）で画面の作りを変えるときに一緒に決める。**

**なぜこの順か**: **他に依存しません。いつでも前倒しできます**（`engine` を1行も変えないので、他の作業と衝突しません）。**下ごしらえ（WASM でのビルドと実行、5つの測定）は 2026-09-19 に済んでいます**。**ただし 0 を「要件にする」と決めたなら、engine の直しが先です。**

**壊してはいけない線**: **段階19 の2つの約束**——**見せるのは `Perception` の中身だけ**／**狙えるのは視界の中だけ**。**画面に描いてある＝ノードが知っている、ではありません。**

---

## 10. 描画をタイルに【完了・2026-09-20】

**一文**: **円をタイル画像に置き換える。**

- [x] **実装前に数えた**（2026-09-20。**1画面 260枚**＝見積もりどおり。**そして画面の外も全部描いていた**——遊ぶズームでは 270 のうち 52 しか画面に無い。**前の速度: ブラウザ 13.9 fps・ネイティブ 25 fps**）
- [x] **1枚のシート＋透過**（2026-09-20。**16px 四方・灰色で描き、貼るときに `ColorScale` で色**。**絵は `cmd/tiles/art.go` に1文字1ピクセル**＝地形や天気と同じ書き方。**大型の獣は大きく貼られる**——絵の大きさは体力上限で、円の半径と同じ量）
- [x] **状態アニメーション**（2026-09-20。立つ・歩く・食べる・戦うの4状態×2コマ。**コマ送りは世界のtick**なので、止めた世界は止まって見える）
- [x] **個体差は `ColorScale`、向きは左右反転**（2026-09-20。色は性別＋ID から引いた小さなばらつき＝**遺伝子とは無関係**）
- [x] **ハッシュ付きファイル名＋ manifest、プリロードしてから開始**（2026-09-20。**ネイティブは埋め込み・ブラウザは `fetch`**＝15MB の wasm と 1.4kB の絵は変わる頻度が違う）
- [x] **画面の外は描かない**（2026-09-20。**この項目に無かったが、数えたら出てきた**）
- [x] **目視**（2026-09-20。ネイティブと、WASM をブラウザで。**タップと長押しも絵の上でそのまま動く**）
- [x] **測定**（2026-09-20。**同じ wasm を `-circles` と対で**: **ブラウザ 11.0 → 14.4 fps ＝絵のほうが速い**。**予想と逆**で、理由は **ebiten の `vector` が Go 側で三角形に分解すること**。ネイティブの差は雑音の中）
- [x] 文書（2026-09-20。`README.md`・`docs/features/game.md`・`HISTORY.md`・`docs/stages.md`）

**なぜこの順か**: **9 のあと**（何をタップできるかが決まってから、その見た目を作る）。

**壊してはいけない線**: **`engine` は描画を知らない**——タイルも manifest もアニメーションもクライアント側。

---

## 11. セーブのアップロードの薄い API【未着手】

**一文**: **育てた世界を配り、遊んだ世界を回収できるだけの最小の API。**

- [ ] 形を決める（**スナップショット1個の受け渡しであって、tick ごとの配信ではない**）
- [ ] 文書: `CLAUDE.md`（アーキテクチャ）・`HISTORY.md`（設計判断ログ）

**なぜこの順か**: **最後でよい**（無くても遊べる）。**ここで初めて `server` の話が出ますが、2026-08-11 に書いた `protocol` の設計とは別物です。**

---

## 12. 飾り物の欲しがり方を仕上げる（段階89・90・89b）【完了・2026-09-20。**3つとも既定はオフ。売買を代価なしに動かしたのは89b だけ**】

**一文**: **`PLAN.md` に節だけ書いて着手していない段階が3つ残っている。** **どれも小さく、互いに独立で、上の 1〜11 とも独立。**

- [x] **段階89: 飾り物は満たされる**（2026-09-20。`AdornSatiety` / `AdornForgetPerTick`。**段階16 の食べ飽きと同じ帳簿1本**。**自己制限は起きた**——保有数 2.58 → 1.89 \*\*\*、作る決定 0.04 → 0.02 \*\*\*。**しかし売買は動かず、個体数が下がる**——**用量ではなく、贈与が減るから**）
- [x] **段階90: 今の気に入り**（2026-09-20。`TrinketFancyTicks` / `TrinketFancySpread`。**継いだ好みは素質のまま、そこから引く**。**出来事の数え上げを10倍外した**——400tick に1回のはずが30tick に1回〈飾り物は贈与で絶えず手を移る〉。**売買を動かしたのは速い腕だけ**〈+0.75 \*〉**で、代価は個体数 −25.5 \*・餓死率 +0.61 \*\***）
- [x] **段階89b（#119・推奨）: 贈与が買う好意を、受け取った側にとっての値打ちに比例させる**（2026-09-20。`GiftWorthScaled`。**売買 1.58 → 2.29〈+0.71 \*\*〉＝この鎖で一番確かな動きで、個体数の代価は誤差の中**。**ただし贈与の当たり具合は改善しない**——動いたのは「外れた贈与が報われないぶん売るほうが相対的に良くなる」側。**対照が決め手**: 同じ平均を平らに与えると贈与 −1198 \*\*\*・個体数 −29.9 \*\* ＝**効いているのは平均ではなく形**）
- [x] 各段階の中身・実装前に数えたこと・予想と結果は **`docs/history/plan/8.md` の同名の節**（2026-09-21 に `PLAN.md` から移した）と `HISTORY.md`
- [x] **測る世界を変えた**（2026-09-20。**段階82・84 の飾り物の腕は全部「遊ぶ地図」だが、今の既定ではその地図は 20000tick で 42%〈飾り物入りで58%〉崩壊する**ので、この群は平坦な世界〈`adornnow`〉で測った）

> **次に市場を触るなら 89b の上に乗せること。** **代価なしに売買を動かした唯一の規則**で、**89 と 90 はどちらも「売買はわずかに動き、人が減る」**だった。

**なぜこの順か**: **他の何にも依存しません。** **P14（気候と、それに答える品）の積み残しで、8（材料と加工）を触る前にやると、そこで読むべき「贈与と売買」の地形が一段はっきりします。**

**壊してはいけない線**: **段階92 が見つけた飽和**（`AffinityKin` 22 > `AffinityTrust` 20 なので `trustBought` は親子にゼロを返す）——**89b はそこと同じ場所を触ります**。

---

## 13. 被弾ノックバック（#136）【完了・2026-09-20。**既定はオフ。地形の規則ではなく、攻撃側有利を削る戦闘の規則だった**】

**一文**: **殴られた身体が攻撃者→被攻撃者の向きへ押され、押し込まれた先の地形がそのまま効く。**

> **測ってわかったこと**: **これは地形の規則ではなかった。** **`country` で個体数 52.0 → 114.6 \*\*\*・崩壊 0.33 → 0.00** だが、**崖落ちは1実行に112回・水は44回**しか起きず、**落下ダメージを0にした対照と区別がつかず**、**水も崖も1つも無い平坦な世界でも `killShare` 0.81 → 0.68 \*\*\*・`fightRate` −27%**。**効いているのは「殴られた側が間合いの外へ押し出され、攻撃側が詰め直しに体力を払う」こと**＝**攻撃側有利を削る側**。**`dPower` −16.2 \*\***。**既定はオフのまま**（既定にするとベースラインのリセット4度目になる）。**数値は `HISTORY.md`、設計は `docs/history/plan/10.md`。**

- [x] **実装前に数えた**（2026-09-20。**ここで本命が入れ替わった**——**崖は水の25〜50倍の的**〈殴打の 6.3〜14.6% 対 0.13〜2.05%〉、**しかも水は溺死率 0.0002/tick なので押し込んでも誰も死なない**〈250回の押し込みで0.5体〉）
- [x] **予想を書いた**（測定前に。**3つとも外れた**——個体数は下がらず倍増し、落下は効かず、溺死の増加は押し込みではなく個体数のせいだった）
- [x] **押し先は溜めて、殴打のループの外で一度に適用**（`resolveAttacks` はループの中で空間インデックスに訊くので、中で動かすと打撃ごとに作り直しになる＝`CLAUDE.md` の規約。**副産物として解決順に依存しなくなった**）
- [x] **重さの補正**（入ったダメージ ÷ `AttackDamage` × 基準の大きさ ÷ その身体の最大体力）
- [x] **崖落ち**（ユーザの判断＝#136。**下りだけ `canStep` を無視し、上りは無視しない**。**落差1段につき `KnockbackFall` 2.3 ＝打撃2発ぶん**）
- [x] **死因のバケツは増やさなかった**（打撃が同tickなので落下死は既存の判定で戦闘死。claim も `witnessKill` も押した者を含んだまま動く）。**ただし押して溺れさせた死は `drowned` のままで `witnessKill` を通らない**——**14 の好意の連鎖に乗せるかは、そこで決める**
- [x] **残りの決めごと2つ**: **投擲は押さない**（`KnockbackThrown` false）／**押された身体は移動コストを払わない**（自分で動いていないため）
- [x] **測定**（12シード × 20000tick。`country` に対して `knock` / `knocknofall` / `knockfar`、平坦な世界に対して `knockflat`）と**目視**（`cmd/devview -terrain country -knock 20`）
- [x] **ベンチ**（既定＝オフで前後。`BenchmarkEngineStep` も `BenchmarkDecide` も雑音の中。指紋テストは1つも動いていない）
- [x] 文書（`NODE.md` 7-4c・`PARAMETERS.md`・`docs/features/enemy.md`・`README.md`〈`-knock` と4つの指標〉・`PLAN.md`・`HISTORY.md`・`docs/stages.md`）

**壊してはいけない線**（守った）: **効用式に「押されるかもしれない」という項を足さない**（押された先の危険は既存の `Hazard` が次の決定で読む）／**移動ループの中でインデックスに訊かない**／**渡れない地形は作らない**（上りは押せない）。**そして測定で分かった新しい線**——**この規則は攻撃側有利を削るので、用量を変えたら `killShare` と `pop` を必ず対で読む。**

---

## 14. 好感度が戦いを動かす（#138）【完了・2026-09-21。**6つとも既定はオフ。測って分かったのは「負の好意を読む者が世界にいない」こと**】

**一文**: **好意が「誰と誰が戦うか」を決めるようにする**——**食べ物を取られれば好意が下がり（マイナスまで）、嫌いな相手とは争い、好きな相手の戦いには加担し、加担すると好意が上がる。**

> **測ってわかったこと**: **(i)〈殴ると相手の好意を失う〉だけが世界を動かし、しかも地図で符号が逆だった**——**平坦な世界で `pop` +26.4 \*\*\*（+18%）・`fightLiked` 0.14 → 0.12 \*\***、**遊ぶ地図で `pop` −33.4 \*・`fightLiked` +0.06 \***（**同じ規則が地図で逆符号になった6例目**。この規則がしているのは「殴る決定を減らすこと」で、**平坦では喧嘩は浪費、遊ぶ地図では食事の手段**）。**(i) と (vi) は噛み合うと予想して打ち消し合い**（`pop` −15.2 \*）、**(iii)+(iv) は13954回払われて `joinShare` を 0.03 → 0.04 \*\* しか動かさず**、**6本目（友を殺した者への減点）は1844回発火して `worstAff` −154.5 \*\*\* にしても何も動かない**。**理由は1つ**——**負の好意を読む者が世界に1人もいない**（`trustLost` の下限は0なので、0を割った相手には効かない）。**ユーザの狙い (v)〈嫌われた身体が袋叩きに遭う〉には「負の好意を殴る理由として読む項」が要り、それは「恨みの状態を持たない」を覆す判断**なので、**そこまで行かずに閉じた。** **数値は `docs/history/work/11.md`、設計は `docs/history/plan/10.md`。**

- [x] **3-1（討伐を見た者の好意）は実装済み**（段階31b の `AffinityWitnessKill`）
- [x] **(ii) の機械も実装済み**（段階32。**足りないのは動機と報酬だった**）
- [x] **実装前に数えた**（2026-09-21。**`friendFightShare` 0.22 / 0.16 に対し加担は 0.02〜0.03 ＝的は大きい**／**`snatched` 1892 / 1134**／**`snatchFriend` 0.10 / 0.16 ＝9割は他人**）
- [x] **予想を書いた**（5つの腕それぞれ。**5つ中3つ外れた**）
- [x] **(i) 好意を `scoreFight` に入れた**（`FightTrustCost`。`trustLost` ＝ `trustBought` の鏡で、**同じ `Lore` 項の中で引き算**＝物差しは1本）
- [x] **2段目の窓には好意を持ち込んでいない**（先読みが持ち越すのは体力と空腹だけ＝段階67・72。**天秤は同じ1回の採点の中**）
- [x] **(iii) `AffinityAlly` を1本足した**（**双方向**・**強制記録**＝`memFull` 0.99 なので空き待ちだと発火しない）
- [x] **(iv) `scoreFight` の `Lore` 項に「いまその相手を殴っている友1人ごとの `trustBought`」を足した**
- [x] **好意のマイナスを許した**（`AffinityNegative`）**と、継ぎ目3つ**（記憶の追い出しを**絶対値**で／`trustBought` の下限を −1 に開いて**仲直りの経路を残す**／**負の記録も枠を作る**）
- [x] **(vi) 取られたら好意が下がる**（`AffinitySnatched`。**`eat` の中で1回、狙っていた身体にだけ、乱数を引かず**）
- [x] **(vi) の量**（`AffinityGift` 6 と同じ）と**数え上げ**（`snatched` / `snatchFriend`）と**予想**と**(i) と対で測ること**
- [x] **「好意を持っていた相手を殺した者」への減点**（`AffinityKilledMine`。**比例**＝他人が殺されても何も失わない）
- [x] **対照**（`spitei` / `spitevi` / `spite` / `sideswith` / `sides` / `sidesplay` / `spiteiplay` / `mourn`。**「情報だけが無い」対照＝`spitevi` を独立した腕で回した**）
- [x] **測り方を1つ足した**（**`fightLiked`**＝殴る決定のうち相手に好意がある割合。**`fightCompanion` は群れで割っているので、好意の規則をそれで測ると効いていても不活性に見える**——**これが無ければ (i) は「何も起きなかった規則」として閉じていた**）
- [x] **ベンチ**（`BenchmarkEngineStep` 778,768 → 759,458 ns/op ＝雑音の中）と**指紋**（1つも動いていない）
- [x] 文書（`NODE.md` 7-4d と8節と9節・`PARAMETERS.md`・`docs/features/society.md`・`README.md`・`PLAN.md`・`docs/history/work/11.md`・`docs/stages.md`）

**残した宿題2つ**（`docs/history/plan/10.md` の同名の節。**どちらも着手条件つき**）: **(iv) の非対称**——**加担の値打ちは「いま殴っている友」全員の合計なのに、失う好意は相手1人ぶん**なので、**友が集まっている身体が殴られる**（`sidesplay` の `fightLiked` +0.09 \*\*）。**直す当ては3つある**（合計ではなく最大／相手への好意を人数倍／`AllyValue` と同じ減衰）。**そして「負の好意を読む項」を作るかどうか**——**作れば狙いは出るが、`CLAUDE.md` の原則を覆す判断になる。** **前者は 17番の (B) が同じ場所を触り、後者への答えの1つ（原則を破らずに「締め出される」を出す道）が 17番の (A) です。**

---

## 17. 狙いが見え、加担は打撃で数える（#140）【完了・2026-09-21。**両方とも既定はオフ。的のある世界でだけ発火し、発火したところでは食料を失う**】

**一文**: **食料の「狙っている」が見えるようにして、取りに行く価値から「失う好意」を引く**（A）。**そして加担の報酬を、宣言ではなく実際に入った打撃で数える**（B）。

> **測ってわかったこと**: **(A) は平坦な世界では何も動かさず（`snatched` −179 の誤差内）、遊ぶ地図で初めて動く**（**−499 \***、1509 → 1010）——**理由は `claimLiked` 0.10 対 0.15 ＝遊ぶ地図では身体が良い地域に集まるので、食料を争う相手が「知っている相手」になる**。**そして発火したところでは食料を失う**（`births` −77 \*・`pop` −29.2 \*）。**用量を4倍にすると平坦でも発火する**（`snatched` −334 \*・`starved` −12.8 \*・`friends` −1.00 \*\*）＝**無反応は「的が小さい」ではなく「用量が小さい」だった**。**(B) は払いすぎを直すつもりが `sideTaken` 13954 → 22838 に増え**（打撃で数えると機会が多い）、**本体は会計の修正ではなく「友情の製造機」だった**（`joinShare` は旧形と同じ +0.01 \*\*\* しか動かないのに `fightLiked` +0.05 \*・`claimLiked` +0.03 \*・`starved` −17.5 \*）。**数値は `docs/history/work/11.md`、設計は `docs/history/plan/10.md`。**

- [x] **実装前に数えた**（2026-09-21。**`claimSeen` 0.21 / 0.14**・**`claimLiked` 0.10 / 0.17**＝**`snatchFriend` とほぼ一致**＝**譲る相手と奪う相手は同じ顔ぶれ**）
- [x] **予想を書いた**（**2つとも外れた**。(A) の符号の検定は「遊ぶ地図で減る」側だけ当たり、(B) は「減るはず」が増えた）
- [x] **(A) 狙いを知覚に載せた**（`FoodView.ClaimedBy`。**`RivalID` を埋めている既存の走査に比較を1つ**。**発信も新しい状態も2度目の走査も無い**）
- [x] **(A) 値付けは `goodwillWorth × trustLost` を `Lore` 項から引くだけ**（**レースに勝つ確率を掛ける**＝負けたレースは誰の好意も損なわない。**他人の品は0**＝食べることへの税にしない）
- [x] **(A) は `RivalID` の隣に置いた**（あちらは「一番近い者」という別の事実で、レースの勝率はそれで計算されている）
- [x] **(B) 加担の報酬を打撃で数えた**（`AllyPaidOnBlows`、**既定 true**。`recentAttackers` / `HuntCreditTicks` ＝死体の分配と同じ台帳。**「同じ相手を殴った者どうし」と「自分を殴っている相手を殴った者」の2つ**）
- [x] **(B) は1交戦につき1回**（**「その相手の台帳に自分がまだ載っていない」ことが交戦の始まりの印**）。**旧形は `AllyPaidOnBlows = false` で残してある**（対で回すため＝`declared` の腕）
- [x] **測定**（平坦・遊ぶ地図・用量4倍の3通り。腕は `give` / `give24` / `giveplay` / `blows` / `declared`）
- [x] **ベンチ**（`BenchmarkEngineStep` 759,458 → 763,231 ns/op、`BenchmarkDecide` 21,401 → 20,987 ns/op ＝雑音の中）と**指紋**（1つも動いていない）
- [x] 文書（`NODE.md` 7-4d・`PARAMETERS.md`・`docs/features/society.md`・`README.md`・`PLAN.md`・`docs/history/work/11.md`・`docs/stages.md`）

**この2回（14番・17番）で出た一般則**: **好意を食事より重くする規則は、食料が制約になっている世界では必ず個体数を下げる。** **平坦な校正世界は食料が緩いのでそれが見えず、遊ぶ地図で初めて出る。** **「好意で世界を動かす」には、食べ物を取り合わない場面（贈与・加担・伝承）を通すしかない**——**次に好意まわりを触るときの出発点はここ。**

---

## 18. 発生率と定員を巣ごとに持たせる（#143）【完了・2026-09-22。**外敵の側だけ。人間の巣の定員は20番と同じ回に入ります**】

**一文**: **外敵がどこからどれだけ湧くか、そして巣が何体まで抱えるかを、地域のスカラーではなく地図が塗った巣の数字で決める。**

- [x] **実装前に数えた**（2026-09-22。**前提は生きていた**——`nestGain`〈巣の近くにいる割合 − 巣の近くの面積割合〉は **0.07〈`EnemyHomeCost` 0〉/ 0.03〈0.5〉/ 0.20 \*\*〈2〉**で、**段階58 の `prowlGain` 0.00 は地域の粒度の話だった**。**ただし残るのは代価のある世界だけ**。**用量は 0.02 ではなく 0.5〜2 で振った**〈掃引され書き上げられた範囲がそれ〉。**世界は `MaxEnemies` に半分以上の時間張り付いているが、天井を倍にしても外敵は +2.34 \*\* しか増えない**。数値は `PLAN.md` P16-9）
- [x] **巣ごとの率**（2026-09-22。`Config.NestRateMap` ＝ `EnemyKindMap` と同じ格子に digit を塗る＝五分の一。**Tiled の語は `nest` ではなく `rate`**——**`nest: true` は既に色板に塗られており、読み替えると意味が二重になる**ので、**新しい語を1つ足すほうを選んだ**。`tiled/properties.md` の `nest` / `roam` の行は「実装しなかった語」として書き直した）
- [x] **既定は取り分**（2026-09-22。総数は `EnemySpawnTicks` / `MaxEnemies` のまま）／~~`EnemyTotalFromNests`~~ **作らなかった**（**数え上げが切った1つ目**。**上げても世界が受け取らない**ので、保存則を緩める価値が無い）
- [x] **アンビエントは別枠のまま残した**（2026-09-22。`Config.EnemyAmbientShare`。**既定は0＝塗った巣が全部を引き受ける、今までどおり**——**「既定1が今のまま」は書いたときの読み違いで、実装のときに直した**＝**数え上げが切った2つ目**）
- [x] **巣ごとの定員**（2026-09-22。`Config.NestCap` ＋ `NestCapMap`、Tiled の `cap`。**満ちている巣は外から入れないだけで、生まれるほうは止めない**＝テストで固定。**行動閾値ではなく `MaxEnemies` と同じ棚**）
- [x] **数え方は外敵と人間で分ける**（2026-09-22。**外敵は空間**＝`Roam` の中の同種の頭数。**人間は血統で数える**が、**人間の巣そのものが 20番**なので、**その定員は20番と同じ回に入ります**）
- [x] **定員が発火しているかを確かめた**（2026-09-22。**巣の自然な人数は 3.12** なので、**定員12 は1ビットも変えず**、**6 で巣の 0.14 が満杯**、**2 で 0.75 が満杯・到着の 0.33 を断る**。**`HomeX/HomeY` で数える側には倒さなかった**——空間で数えても発火する）
- [x] **取り分が居場所に残ることを測った**（2026-09-22。**1:9 に塗ると `nestRated` 0.39 → 0.87 \*\*\***＝湧きの 0.9 がほぼそのまま。**世界そのものは動かない**＝設計どおり）
- [x] **`ENEMY.md` の整理**（2026-09-22。3節に2026-09-22 の項・8-1 の表の「地域重み」の行・8-2 の注記。**`EnemySpread` / `Homing` は消さずに「古い道」**）
- [x] 文書（2026-09-22。`ENEMY.md`・`PARAMETERS.md`・`docs/features/enemy.md`・`tiled/properties.md`・`README.md`・`PLAN.md`・`HISTORY.md`）

> **19番が踏む段差を1つ見つけました**: **定員を締めると人間が減る**（`pop` −10.42 \*・`enemies` −2.62 \*\*）。**外敵を減らすと肉が減る**——**2026-09-22 から肉になるのは倒された死体だけ**なので、**獣を間引くことは人間の食料を間引くこと**。**巣の主を倒す報酬を測るときは、肉の側も一緒に読むこと。**

**なぜこの順か**: **19（ボス）と 20（人間の巣）が乗る帳簿をここで作りました**（行の表・塗り・率・定員）。**11 とは独立。**

**壊してはいけない線**: **湧きの総数は変えない**（段階58。**緩める案は数え上げが落とした**）／**数は地図に載せない**（#133。塗るのは比）／**古い道を消さない**（`EnemySpread` / `Homing`）。

---

## 19. 外敵の巣にボスを置く（#141）【完了・2026-09-22。**既定はオフ。規則は動くが、巣を空けることの値打ちがまだ測れない**】

**一文**: **プレイヤーが巣の主を誘い出せるようにし、倒されたら巣が休暇に入り、放置されたら巣へ帰って引っ込む。**

- [x] **実装前に数えた**（2026-09-22。**人間は1実行に 29.75 体の獣を倒し、倒される獣の 0.95 に人間の手が入っている。ただし党は 1.70 人**——**主は「1〜2人で倒せる身体」でなければ倒されない**。**そして身体には天井がある**——**遺伝子9本 × 上限100 ＝予算900** なので、**`BossBudget` 2 と 4 は同じ生き物**〈平均的な獣の1.73倍〉。**掃引を ×1 / ×2 / ×4 から ×1 / ×1.5 / ×1.73 に変えた**。数値は `PLAN.md` P16-7）
- [x] **予想を先に書いた**（2026-09-22。**そして外れた**——下）
- [x] **ボスは行の上の数字1つ**（2026-09-22。`Config.BossBudget`。**既定オフ＝乱数を1つも引かない**。**意思決定エンジンに「主か」を訊く分岐は無い**）
- [x] **巣が走行中に状態を持つ**（2026-09-22。`engine/boss.go` の `nestLife` ＝主・出た tick・休暇の終わり。**保存/読込に載せた**）
- [x] **誘い出しは engine の外から呼ぶ1本**（2026-09-22。`World.Rouse(nest)`。**`Endow` / `Inspire` と同じ棚**。**「巣をクリックして YES/NO」は `cmd/devview`**＝`-bosses`。テストで固定）
- [x] **放置したら帰って引っ込む**（2026-09-22。**条件は「`BossRouseTicks` 経った」と「いま巣のセルにいる」の2つだけ**。**引っ込みは死ではない**＝死因のバケツも肉も増えない、持ち物だけ落とす。テストで固定）
- [x] **倒れたら休暇**（2026-09-22。**死因は問わない**。**長さはタイルの `quiet` ＝ `NestQuietYears`〈既定5年〉の何/5 か**。**休暇中は湧かず呼べない**。テストで固定）
- [x] **巣の密度は地図の調整のまま**（2026-09-22。**エンジンに「密度」を作っていない**。**測定器を1本足した**＝`denSettled` / `denRoom`）
- [x] **休暇が定住を助けるかを測った**（2026-09-22。**助けない**——32シードで `denSettled` +0.02 ± 0.03。**12シードで出た +0.05 \* は消えた**）
- [x] **測るための「誘い出す者」を作った**（2026-09-22。`cmd/experiment` の `rouser` ＝2年に1体。**床であって天井ではない**）
- [ ] **巣ゼロの世界が簡単すぎないかは測っていない**（**報酬が測れない以上、巣の有無で難易度を語る前提が立たない**。19番の梃子が見つかってから）
- [x] 文書（2026-09-22。`ENEMY.md`・`PARAMETERS.md`・`docs/features/enemy.md`・`tiled/properties.md`・`README.md`・`PLAN.md`・`HISTORY.md`）

> **測って分かった3つ**: **(1) 呼び出した主の4分の3は倒されずに帰る**（`bossKillShare` 0.23〜0.28）——**予想「×1 なら0.9以上」は外れた**。**(2) 強さが動かすのは倒される割合ではなく年数**（3.57 → 6.36 \*\*\*）——**「倒される run が半分前後」は強さでは作れない**。**(3) 報酬が測れない**——**巣は run の0.22 の時間空いているのに、個体数も定住も動かない**。
>
> **測定器のバグを1つ踏んで直した**（この項目で一番の収穫）: **「消えた身体」を全部「倒された」と数えていた**——**引っ込みと死は外からは同じ出来事**。**巣が休暇に入ったかで区別**したら `bossKillShare` が 0.98 → 0.23 になった。**「死ではない消え方」を作った規則は、測定器の側でも死と区別すること。**

**なぜこの順か**: **18 の帳簿（巣が率と状態を持つ形）の上に乗りました。**

**壊してはいけない線**: **閾値を作らない**（**引っ込む条件も体力や間合いにしない**＝守った）／**engine はプレイヤーを知らない**（段階19・決定#130。**YES/NO もクリックも文言もゲーム側**＝守った）／**引っ込みを死にしないし、引っ込められるのは `Rouse` が出した主1体だけ**（テストで固定）／**種類は種ではない**／**湧きの総数は変えない**。

---

## 20. 人間が湧く巣（#142）【完了・2026-09-22。**既定は空。予想が2つ外れ、「支えは自分より長く続かない」が確定**】

**一文**: **地図が塗った巣から人が湧き、巣ごとに1本の血統を配り、10年ほどで湧かなくなる。**

- [x] **実装前に数えた**（2026-09-22。**一様に置いた60人の建国者は 20000tick で 5.08 本の血統になり、一番大きい血統だけで世界の半分**〈`lineBiggest` 0.54〉。**巣が配るのは、ほとんど死ぬ血統**）
- [x] **予想を先に書いた**（2026-09-22。**2つ外れた**——下）
- [x] **形は外敵の巣とそろえた**（2026-09-22。`Config.HumanNests`〈`Name` / `Key` / `Rate` / `Life` / `Cap`〉＋ `HumanNestMap`、Tiled は `human`。**数値は地図に置かない**。**行が言わなければ `HumanNestTicks` / `HumanNestLife`**＝年1人・10年）
- [x] **巣ごとに血統タグを1本**（2026-09-22。**建国者の続き番号**なので衝突しない。**画面ではそのまま家系の色になる**）
- [x] **定員 `Cap` で「配る」から「支える」へ**（2026-09-22。**その巣の血統が何人生きているかで数える**＝人はタグを持ち子に渡すので線を数えられる。**外敵の巣が空間で数えるのとの非対称は明記**）
- [x] **寿命 `Life` が尽きたら補充をやめるだけ**（2026-09-22。湧いた身体はそのまま生きる。**送った数は保存/読込を往復**）
- [x] **位置づけを1点精密にして書いた**（2026-09-22。**`Endow`/`Inspire` は engine の外からしか呼ばれない**が、**これは tick の中で世界が自分で呼ぶ**ので `EnemySpawnTicks` の棚。**「通常の出産経路とは別枠」という指定の中身はそのまま**——テストで固定: **出生数は1つも増えず、`ParentIDs` は空、成人として来る**）
- [x] **人が出産以外で増えるのは初めてなので明記した**（2026-09-22。`NODE.md` の4節。**腕は必ず互いに対して読む**）
- [x] **将来の拡張の席は作らなかった**（2026-09-22。確認だけ: **`HumanNest` に `Nodes []Node` を1本足せば済む**——`World.Nodes()` / `LoadNodes` が既にその形を運び、`Repopulate` は一様にばらまいているので**違うのは位置の決め方だけ**）
- [x] **`cmd/devview` で目視**（2026-09-22。`-villages 3`。**村はその民の色の枠で描かれ、黙ったら薄くなる**。**枠のまわりに同じ色の人が集まって見える**）
- [x] 文書（2026-09-22。`NODE.md`・`PARAMETERS.md`・`docs/features/core.md`・`tiled/properties.md`・`README.md`・`PLAN.md`・`HISTORY.md`）

> **測って分かった4つ**: **(1) 置き場所は血統の数を動かす**——**3か所から同時に出すだけで 5.08 → 2.67 \*\*\***（予想は「動かない」。**始まる場所が60から3になれば生き残る線もそれだけ減る**）。**(2) 人口を削るのは置き場所ではなく滴らせること**（同時なら誤差内、20年かければ 155 → 76 \*\*\*。**ただし120年で −41 \* まで縮む立ち上がりの遅さ**）。**(3) 本当に支えると3つの国が3つとも残る**（年5人・定員20 で `lines` 2.92 / 3、`lineBiggest` 0.58 \*\*\*）——**一様な世界は60本のうち5.08本**。**(4) 支えは自分より長く続かない**——**村が黙って100年たつと、世界はその村が無かった世界と完全に同じ**（人間 177.1 対 177.1、血統 2.50 対 2.50）。**巣が決めるのは誰がどこで始めるかで、誰が残るかは世界が決める。**
>
> **副産物**: **人がまばらな世界では定住が増える**（0.46 → 0.56 \*\*、120年でも 0.59 \*\*）。**支えて人を増やすと戻る**（0.43 \*\*\*）＝**定住は村ではなく密度の関数**。**定員5 は少なすぎる**（崩壊 0.25 \*）。

**なぜこの順か**: **18・19 が作った巣の帳簿と同じ形を人に写しました。** **外敵の湧きには1行も触っていません。**

**壊してはいけない線**: **予算の継承総量**（巣から湧く身体は `randomAgent` と同じ引き方で、`Endow` のように予算を足さない＝守った）／**出産の経路に触らない**（巣は `addAgent` を呼ぶだけ＝テストで固定）／**閾値を作らない**（「この巣の身体はこの地域から出ない」は書いていない）／**世界の上限は残る**（`MaxPopulation` はそのまま効く＝テストで固定）。

---

## 21. 飛行の天井を既定で2段にする（#144）【未着手】

**一文**: **`FlyHeight` を書かなかった種類は高さ3を越えられないようにする**（今は「書かなければ天井なし」）。

- [ ] `Config` に飛行の既定の天井（2）を足し、`canStep` の `FlyHeight <= 0` をそれで埋める
- [ ] テスト2本（**天井を明示すれば越える／黙っていれば越えない**）
- [ ] `-beasts` の世界で「flyer が高さ3を越えた回数が0」を1度確認する
- [ ] 文書: `enemykind.go` のコメント（「未設定は天井なし」を書き換える）・`PARAMETERS.md`・`docs/features/enemy.md`・`docs/history/work/`

**なぜこの順か**: **前提が無く、30分で終わる。** **22・23 の訓練ステージを描く前に入れておくと、壁の意味が最初から決まっている。**

**壊してはいけない線**: **飛行は「もう1つの通り道」であって「劣った通り道」ではない**（天井が低くて飛べないところは歩く者と同じ道を通れる＝段階20 の線）。**既定の世界は1ビットも変わらない**（何も飛ばないので、指紋テストがそのまま証拠）。

---

## 22. 訓練された民を巣に配る（#145）【未着手】

**一文**: **小さなステージで育てた民を書き出し、別のステージの巣がその民を出せるようにする**（場面ごとに巣を使い分ける）。

- [ ] `HumanNest.Nodes []Node`（空なら今までどおり乱択。**空のときは乱数を1つも引かない**）
- [ ] 送るときに `w.rng` から1つ引いてその一覧から選ぶ
- [ ] 外敵も同じ形（`EnemyKind.Nodes`。巣の主にも効く）
- [ ] 民を書き出すヘッドレスの道具（`SaveNodes(w.Nodes())`。23番と共有）
- [ ] `cmd/devview -nest hearth=river.json` のように巣ごとに指定
- [ ] 保存と読込に乗ることを確認（**セーブの大きさを1度測る**）
- [ ] 数える(1): **川のあるステージと乾いたステージで育てた民の `SkillSwim` / `SkillFishWater` 保有率と熟達**（差が出なければこの機能には中身が無い）
- [ ] 数える(2): **訓練された民の巣は、乱択の巣より村・家系が1年もつか**（`findings/2.md`・`findings/3.md` と同じ測り方）
- [ ] 文書: `PARAMETERS.md`・`docs/features/core.md`・`README.md`・`docs/history/findings/`

**なぜこの順か**: **21 の後、23 の前。** **23（成熟した世界から遊ぶ）と道具を共有**し、**22 の測定「何年訓練すれば分布が寝るか」がそのまま 23 の答えになる。**

**壊してはいけない線**: **engine はファイルを知らない**（読むのは devview）。**乱数は `World` の1本**。**訓練済みの民を入れた世界は `HISTORY.md` の数値と直接比べられない**——腕の名前で区別する。**スキルは既に `Node` に乗っている**（ヒント枠の中身）ので、**新しい状態を足さない。**

---

## 23. 遊ぶ世界を先に育てる（#146）【未着手】

**一文**: **プレーヤーは成熟した世界で生まれる**——**成熟は年数ではなく指標で決め**、**育てるのはヘッドレス、遊ぶのは保存データから。**

- [ ] 4つの指標を2.5年ごとに60年ぶん記録する（**予算の平均／配分の偏り／ヒント枠の埋まり／スキル保有率と熟達**）
- [ ] 曲線が寝る年を `-warm` の既定にする（**巣の補充が切れたあとで測ること**）
- [ ] `cmd/devview -warm <年>`（身体を渡す前に世界を回すだけ。規則は1つも変えない）
- [ ] 育った世界と民を書き出す（22番と共有。`F2` は既にある）
- [ ] 文書: `README.md`・`docs/features/core.md`・`docs/history/findings/`

**なぜこの順か**: **22 の後。** 書き出しの道具と測定を共有する。

**壊してはいけない線**: **`-warm` は `Step()` を呼ぶだけ**（世界の規則を変えない）。**渡す身体の選び方は今のまま**（`quickestBody`。2026-09-23 に「人がいなければ誰も渡さない」に直した）。

---

## 24. 経験から学ぶ相関の計器（#148）【完了・2026-09-23。**門は通り、設計が3つ切れた**】

> **結果**: **作る値打ちはある**——**身体が頼れる関係（86〜100本）のうち37〜41%（32〜40本）は効用式が半分も期待していない**ので二重計上にならない。**固定の窓は4〜15%しか説明しない**ので**イベント式で正しい**。**〈イベント→イベント〉は100マス中72マスが発火し、硬貨→食事は31244回、硬貨の78%は539tickで手から出ていく**ので**合成の足跡は届く**。**二重の欲求の一致は68%で常態だが、賞金は評価誤差の0.41倍**で、**食料側はゼロ**。**売り手の3条件は2万回そろって売買は1.58回。** **設計は3つ切れた**（鍵を argmax で取らない／食料の物々交換は作らない／イベントは身体自身の率を目盛りに切る）。数値は `docs/history/findings/4.md`、書き戻しは `PLAN.md` の #148

**一文**: **「この条件でこの行動をとると、少し先でこういうイベントになりやすい」という相関が、この世界に実在するかを数えるだけ**——**読み取り専用で、世界の規則を1つも変えず、乱数を1つも引かない。**

- [x] イベントの語彙を仮に置く（**生命と子孫の2通貨に落ちるものだけ**。Δ体力／Δ空腹／Δ`extra`／溺れ／死／子の成立／得た・失った）
- [x] 数える(1): イベント種ごとの発火回数／1体の生涯に同じ鍵が2度来る割合
- [x] 数える(2): **(状況, 行動) → イベント の相関が基準率と違う鍵の本数。うち候補間で差がつくもの／効用式に既に書かれていないもの**
- [x] 数える(3): 同じ集計を**固定 k（30 / 200 / 700）**でも取る（**イベント式 対 時間式を数字で決める**）
- [x] 数える(4): 値打ちが**評価ノイズ**を超える鍵の本数（**目盛りは走行中に測った1候補あたりの誤差の幅＝10.4〜11.4**。**段階86b のコメントにある 139 は別の量なので混ぜないこと**——`docs/history/findings/4.md`）
- [x] 数える(5): **イベント × イベントの2次元共起表**（合成が成り立つか。特に「硬貨を得た → 食べた／助かった」）
- [x] 数える(6): 硬貨の保有時間の分布（取得→使用、使われない割合）
- [x] 数える(7): **二重の欲求の一致の発生率**（視界内で互いに相手の持ち物を自分のより高く評価する2体が1実行に何回）
- [x] 数える(8): その利得の大きさ／飾り物が手にある割合／飽きの割引（5%側）での同じ発生率
- [x] 数える(9): 贈与の往復（Aに渡した → 後でAから渡された）の頻度
- [x] 数える(10): 余剰を持ち・買い手が視界にいて・その買い手が硬貨を持っている状態の回数
- [x] 数える(11): 1体あたりのバイト数と `BenchmarkEngineStep` / `BenchmarkDecide`
- [x] **平坦な世界 ＋ `countryskill`、89b（`GiftWorthScaled`）の上、12シード**で回す（遊ぶ地図は飾り物入りで20000tickに58%崩壊する）
- [x] 文書: `docs/history/findings/`（**問いの単位で1ファイル**）・`README.md`（計器の読み方）
- [x] **数字が出たら本体の設計3点を決めて `PLAN.md` #148 に書き戻す**（イベント式か時間式か／合成を入れるか／A〈飾り物の物々交換〉か D〈贈与の上の互恵〉か）

**なぜこの順か**: **21 より先。** **読み取り専用で指紋を1ビットも動かさない**ので、21〜23 の前に割り込んでも後続に何の影響もありません（2026-09-23・ユーザの指定）。そして**本体は先読み以来の規模**——**イベント式の相関学習＋目撃・合成・物々交換の3部構成**——なので、**作る値打ちがあるかを先に数字で決めます**。**数える(2)の3列目がゼロなら本体は作りません。**

**壊してはいけない線**: **計器は世界に書き込まず乱数も引かない**（既存の `Clusters` / `Trade` / `CarryUse` と同じ扱い）。**連結距離のような測る側の目盛りは `Config` に入れない**（`CLAUDE.md` のコーディング規約）。**`HintFeature` にも `DeathFeature` にも足さない**（足すと規則をオフにしても全世界の指紋が動く）。**代価を共有する対照（`lessonblind` 形）は本体の測定に入る時点で最初から作り、`-base` に置く。**

---

## 25. 物の値打ちを学ぶ（#148 の本体）【完了・2026-09-23。**既定はオフ。この鎖で一番大きく売買を動かした**】

> **結果**: **`sales` +1.58 \*\***（1.75 → 3.33。**前の最高は段階89b の +0.71 \*\***）**で、用量反応もある**（0.1 で +0.50 誤差内、0.25 で +1.58 \*\*、1.0 で +2.75 \*\*\*）。**代価は `collapsed` +0.25 \***（個体数は誤差内）。**皮の世界では代価が無く**（`pop` +9.25 \*）、**`hidesSold` がこの企画で初めて 0 でなくなった**。**予想していなかったのは「教え合わないほうが売買が多い」こと**（+3.67 \*\*\*）——**全員が同じ値打ちを信じると交換する理由が消える**。数値は `docs/history/findings/4.md` の 11

- [x] 物の種類ごとの意見（`ItemSlots` の枠を予算から買う。**コートは `Ward > 0` の飾り物として別扱い**）
- [x] 値打ちは既存の効用式のまま（`pressures → gap → LifeValue`）。**良し悪しのラベルを付けない**
- [x] **自分の暮らしぶりを引く**（引かないとこの世界では全部の物が正の値打ちを持つ）
- [x] `Utility.Thing` を1本。**拾う・買うで加点、置く・渡すで減点。分岐はどこにも無い**
- [x] 観察でコピーされる（`ItemsSpread`）
- [x] イベントの検出は計器と同じ1パスを共有する
- [x] 重みを決める（**0.25**。この規則で唯一、測らずに選んだ数字）
- [x] 腕7本と指標11本、テスト7本
- [x] 文書: `NODE.md`・`PARAMETERS.md`・`docs/features/things.md`・`docs/history/findings/4.md`・`docs/history/work/13.md`

**`collapsed` は 200000tick × 32シードで取り直しました**（`docs/history/findings/4.md` の 11b）——**代価は無く**（+0.06 ± 0.08）、**強い用量はむしろ利得**（`collapsed` −0.09 \*・`fellAt` +16063 \*・`peak` +15.72 \*・`sales` +292 \*\*\*）。**既定の重みを 0.25 → 1 に変えました。**

**二重計上の根治もやりました**（`ItemValueResidual`。既定 false）——**きれいな勝敗ではなく、残差は売買を3分の2しか動かさない代わりに唯一の代価が消えます**。**二重計上は中立ではなく増幅していた**、というのが読み。**既定は生のまま**ですが、**貨幣の実験には残差版を使うこと**（アンカーを含んだ数字では「アンカーの外側に値打ちが立つか」に答えられない）。

**残っている宿題**（次に触るとき）: **寒さの用量を下げた皮の世界**で測り直す。**そして効用式そのものについて3つ**（2026-09-23 のユーザの指摘）: **`Risk` が攻撃を抑える向きにしか効かない**（自分を一番傷つけた相手が一番攻撃しにくい相手になる）／**`Hazard` と `Weather` は同じ「地面の危険」が `pressures` に食われているかどうかだけで2項に割れている**／**`Risk` と好意が個人の内側に閉じている**（`EstStrength` は伝わるのに）。

---

## 条件つきで復活するもの

**着手条件が満たされたときだけやります。** 条件と理由は `PLAN.md` の「保留」の節。

- [ ] **段階52d: 生では割に合わない食料**（着手条件: 調理の効果が弱すぎるとき。段階52 は +21.10 \*\*\* だったので**今は条件を満たしていません**）
- [ ] **`PLAN.md` の保留の節にあるその他**（魚を生き物にする・地域ごとの世界リセット・配偶者を巡る同性間の戦い ほか）

---

## 小さい宿題（独立・いつでも）

- [ ] **節目の4つ（乗り換え・成人・ペア成立・出産）は決め打ち。** 遊んでみて増減させる。
- [ ] **性格の4択も決め打ち**（用心深い／気が短い／命を惜しむ／親のまま）。
- [ ] **人が選んだ性格が世代を越えてどう効くかは測っていない。**

---

## 終わったもの

**この文書には残しません。**

- **段階ごとの結論**: `docs/stages.md`（段階0〜100）
- **測定値と設計判断の理由**: `HISTORY.md`
- **2026-09-19 より前のこの文書の中身**（完了した群の一覧・申し送り・作業メモ）: `docs/todo-archive.md`（凍結）
