# 変更の履歴と設計判断ログ

**今の操作方法は `README.md`、設計とルールは `CLAUDE.md` にあります。** この文書は「いつ何が変わったか」と「なぜそう決めたか」を残します。

2つの部から成ります。どちらも**末尾に追記**し、過去の項目は書き換えません。

- **変更の記録** — 結果として何がどう変わったか。`README.md` は常に最新の状態だけを書く方針なので、そこから落ちた記述（廃止した操作、変わったコマンド、以前の既定値）はここに移します
- **設計判断ログ** — なぜそう決めたか。大きな決定（言語選定・通信方式など）を、日付と理由つきで残します。同じ議論を繰り返さないため（2026-08-16 に `CLAUDE.md` から移設）

同じ変更が両方に載ることはあります。片方は「何が変わったか」、もう片方は「なぜそう決めたか」なので、重複ではありません。

---

# 変更の記録

## 2026-08-12 engine と cmd/devview の最初の版

`human_evolution_sim.html`（v0のブラウザ版プロトタイプ）をGoに移植。`engine` パッケージと、それを直接importして描画する `cmd/devview` を追加した。

- 起動は `go run ./cmd/devview` のみ。オプションは無し
- 操作はスペース（一時停止）とクリック（ノード選択）だけ。ウィンドウは900x650で、世界がウィンドウ全体を占めていた
- クリックで見えるのは血統と他者への強さ推定（推定値±標準偏差・リスク・観測数・真値）

## 2026-08-12 実装ロードマップ 段階0〜6

状態モデルを 体力・空腹度・食料 の3軸に置換し、能力を パワー／理性／頭の良さ に分離。トリガー方式の意思決定、効用比較、継続戦闘と逃走、記憶と強さ推定、先制排除、頭の良さによる戦略選択までを実装。

- `DefaultConfig` を「食料が足りない世界」に振り直した（`FoodSpawnRate` 1.5→0.20相当）。食料が余っていると争いも餓死も起きず、能力に選択圧がかからないため
- ベンチマークの初期値: `BenchmarkEngineStep` 300体で約283µs/op、`BenchmarkEngineStepCrowded` 約633µs/op、`BenchmarkDecide` 約5.3µs/op

## 2026-08-13 意思決定トレースと、1ノードを追うための操作

`Decide()` 1回分（トリガー・比較した候補と効用の内訳・選ばれた行動）を記録できるようにし、devview で1ノードを追えるようにした。

- `engine`: `World.TrackDecisions(id, on)` / `DecisionTraces(id)` / `LastDecisionTrace(id)` を追加。記録は指定したノードだけ
- `engine`: 効用式の実装を `Utility`（目的ごとの `Goal{Value, Chance}` ＋リスク・体力コスト・時間コスト）に一本化した。表示用の別計算は作っていない
- `engine`: 再判断のきっかけに名前が付いた（`Trigger`）。それまでは bool 1個で理由を捨てていた
- `cmd/devview`: ウィンドウを 1280x660 に拡張し、右に幅460の固定パネルを追加。世界の描画領域は 820x660 になった（それ以前は世界がウィンドウ全体）
- `cmd/devview`: 操作を追加 — `→`/`n`（1tick進める）、`-`/`=`（速度5段階）、`[`/`]`（決定履歴）、Tab（トレース／強さ推定の切替）、Esc（選択解除）。クリックでの選択は「選択＝追跡」に変わった
- `cmd/devview`: 起動オプション `-follow` / `-slow` / `-seed` を追加
- `engine/decision_test.go` を新設（状況を組み立てて `Decide()` を1回呼び、選ばれる行動をassertするシナリオテスト）
- 速度への影響: `BenchmarkDecide` 5.36µs→5.77µs（+7.5%）、`BenchmarkEngineStep` 276µs→283µs。`Utility` 構造体の組み立て分で、追跡していないノードでも払う。同一シード5000tickで統計値が完全一致することは確認済み

## 2026-08-13 README.md と HISTORY.md を分けた

操作方法を `README.md` に集約し、この文書（`HISTORY.md`）を履歴の置き場にした。`CLAUDE.md` の「現在の機能」にあった devview の起動方法・キー操作・画面の読み方は README.md に移した（同じことを2箇所に書かないため）。

## 2026-08-14 A/B実験ランナーと、戦略深さゲートの既定オフ化

ルール変更が能力の選択圧をどちらに動かすかを測るため、ヘッドレスの `cmd/experiment` を追加した。その最初の測定結果として、頭の良さの「戦略の幅」の経路（深さゲート）を既定で無効にした。

- `cmd/experiment/` を新設。N条件 × Nシード × Ntick を並列で回し、条件ごとの平均±標準誤差と、**同じシードどうしの差分**（paired difference）を表示する。`-csv` で推移の時系列も出る。使い方は README.md
- `engine`: `Config.StrategyDepthUnlock = 0` が「ゲートなし（全戦略解禁）」を意味するようになった。ゼロ除算を避ける `strategyDepth()` を経由する
- `engine`: **`DefaultConfig` の `StrategyDepthUnlock` を 16 → 0 に変更**（既定で深さゲートが無効になった）。頭の良さは評価ノイズ（`ChoiceNoise`）だけを通して効く
- 測定値（24シード × 20000tick、同一シードの差分）:
  - `nogate` − `baseline`: 平均知能の上がり幅 **+4.52 ± 0.95**、戦闘 +3589、殺害 +23、餓死 −22、個体数 −2.0 ± 2.1（変化なし）
  - `gate20`（閾値を20/40/60に）− `baseline`: 平均知能の上がり幅 **−3.87 ± 0.79**（知能への選択圧が負に反転する）
  - `noquality`（`ChoiceNoise = 0`）− `baseline`: 知能の上がり幅 −1.71 ± 0.81。ゲートだけでは知能はほぼ選択されない（+0.63 ± 0.51）
  - 60000tick × 16シードでも向きは同じ（`nogate` +4.86 ± 2.29）
- 速度への影響: `BenchmarkDecide` 5.8µs → 8.0µs（**+36%**）。ゲートが無くなり全個体が観察・攻撃まで採点するようになったぶん。`BenchmarkEngineStep` は同時測定で 382µs（ゲート有）対 357µs（無）で、差は測定ノイズの範囲
- `engine/world_test.go` の `TestIntelligenceGatesTheStrategiesAvailable` は明示的に `StrategyDepthUnlock = 16` を設定するようになった（既定がオフになったため）。`engine/decision_test.go` に中段の閾値とオフスイッチのテストを追加

## 2026-08-14 集団化の指標と、ルール・パラメータの文書を分離

協力・外敵を入れる前段として、**群れているかどうかを測る指標**を追加し、外敵を入れる前のベースラインを取った。あわせてルールとパラメータの記述を CLAUDE.md から独立した文書に切り出した。

- `engine`: `World.Spacing()` を追加。`AvgNeighbours`（視界内の他個体数）／`AvgNearestDist`（最近傍までの距離）／`Clumping`（同じ個体数を一様にばらまいた場合との比）を返す。O(n^2) なので `Stats()` とは別にしてある（`Stats()` は毎フレーム読まれる）
- `cmd/experiment`: 指標 `clumping` / `neighbours` / `nearest` / `killShare`（死因に占める殺害の割合）を追加。条件 `morefood`（`FoodSpawnRate` 0.30）と `scarce`（0.12）を追加
- **`NODE.md` / `ENEMY.md` / `PARAMETERS.md` を新設。** CLAUDE.md の「シミュレーションのルール」「設計中のルール」の詳細はここへ移し、CLAUDE.md 側には「崩してはいけない原則」と将来実装だけを残した
- ベースライン測定（24シード × 20000tick）:
  - `clumping` 1.19、`neighbours` 18.1、`nearest` 21.2、`killShare` 0.61
  - 食料供給を 0.12〜0.30 で振っても `killShare` は 0.59〜0.64 でほとんど動かない。**一方で殺害の絶対数は食料が増えるほど増える**（個体数が増えて遭遇が増えるため）。「食料を増やせば争いが減る」は成り立たない
  - 能力の選択圧（`dPower` +8.3、`dIntelligence` +6.9）は食料供給に対して頑健。`dRationality` だけが +0.5〜1.0 と一桁小さい

## 2026-08-16 設計判断ログを CLAUDE.md から HISTORY.md へ移した

`CLAUDE.md` の「設計判断ログ」節をこの文書の後半へそのまま移設した。コードの変更はない。

- この文書を「変更の記録」と「設計判断ログ」の2部構成にした。冒頭の見出しは `# 変更の履歴と設計判断ログ` に変わった
- 既存の日付つき項目は本文をそのまま残した（見出しの階層だけ、2部構成に合わせて付け直した）
- `CLAUDE.md` から「設計判断ログ」節（2026-08-11〜2026-08-14 の全項目）を削除し、この文書を指すようにした。「文書の分担」「新機能を追加するときの流れ」「パフォーマンス計測」の各節の記述も、判断の置き場が変わったことに合わせて書き換えた
- `README.md` / `NODE.md` / `PARAMETERS.md` の「なぜそうなっているか」の参照先を `CLAUDE.md` から `HISTORY.md`（設計判断ログ）に変えた

## 2026-08-31 寿命消費（背景処理）を先行実装

段階7d（成長・老い・寿命と育児期間）のうち、**寿命消費（過小・過度のルール）だけ**を、意思決定に一切関与しないバックグラウンドの機械的処理という制約つきで先に実装した。年齢補正・成長・育児期間・`TicksPerYear` の確定はまだ未着手。

- `engine`: `Agent.Lifespan`（新しい状態。`Perception` には載らない）を追加。`World.spendLifespan`（`World.metabolise` から呼ばれる）が唯一これを書き換える場所で、`Hunger > StarveHunger`（過小）または `Hunger < OverfedHunger`（過度＝腹八分目超）の間だけ毎tick減らし、0で死なせる
- `engine`: `Config` に `MaxLifespan` / `StarveLifespanRate` / `OverfedHunger` / `OverfedLifespanRate` を追加（各既定値は `engine/config.go` 参照）。過度・過小はそれぞれ独立に0で切れる
- `engine`: `Stats.AgingDeaths` を追加し、`Deaths` の内訳として餓死・戦闘死と区別できるようにした
- `engine`: `World.addAgent` に、既存の `Vitality` と同じ形の安全網を追加（`Lifespan <= 0` のとき `MaxLifespan` を補う）。生の `Agent{}` リテラルを組み立てるテストが多いため
- `cmd/experiment`: 指標 `aged` / `agedShare`（死因のうち寿命消費によるものの数・割合）を追加。既存の `starved` は `deaths - killed - aged` に変わった。動作確認用の条件 `brittlelifespan`（`MaxLifespan` を1500に縮める）を追加
- `engine/world_test.go`: `TestModerateHungerDoesNotSpendLifespan` / `TestChronicHungerSpendsLifespan` / `TestChronicOverfeedingSpendsLifespan` / `TestLifespanExhaustionKillsAndIsCountedSeparatelyFromCombat` / `TestBareAgentGetsAFullLifespanByDefault` を追加。`TestAgeDoesNotKill` のコメントを更新（`Age` 自体は今も何もしないが、寿命消費という別の経路で死ぬことがある、という区別を明記）
- 測定（`cmd/experiment`、8シード×20000tick・6シード×60000tick）: **既定値では `aged` = 0**（標準実行長では発火しない。生存者の大半が寿命を使い切るより先に餓死・戦闘で死ぬため）。`brittlelifespan` 条件（8シード×20000tick）では `aged` = 12.00 ± 0.57（死因の約4%）、個体数・世代数は baseline と有意差なし、絶滅なし。既定値のチューニングは今回行わない（`PLAN.md` の未決事項に追加）
- 触った文書: `CLAUDE.md`（現在の機能・実装ロードマップ）、`NODE.md`（1・2・4・11節）、`PARAMETERS.md`（B節・C-2b節新設・件数58→62）、`PLAN.md`（段階7d の寿命節にチェックを入れ、未決事項に既定値のチューニングを追加）、`README.md`（`cmd/experiment` の指標表に `aged` を追加）

---

## 2026-08-31 クラスタ検出（段階6.5の1つめ）

段階6.5（計測の先行整備）のうち、**クラスタ数とサイズ分布**を実装しました。読み取り専用の解析で、ノードは集団IDを持ちません（集団の識別は view 層の仕事、という `PLAN.md` の方針どおり）。

- `engine`: `engine/cluster.go` を追加。`World.Clusters(linkDist)` が単連結（`linkDist` 以内にいる者どうしを推移的につなぐ）で連結成分を求め、`Clustering` を返す。中身はサイズ分布そのもの（`Sizes`、大きい順、合計＝個体数）と、その要約（`Groups`＝2体以上の成分数、`Singletons`、`Largest`、`LargestShare`、`AvgGroupSize`、`GroupedShare`）
- `engine`: 連結距離の既定値を `DefaultClusterLinkDist = 30`（package const）として置いた。**`Config` には入れていない** — 世界のルールを何も変えず、測る側の目盛りでしかないため。`Spacing()` と同じく O(n²) なので、毎tickではなく間引いて測る前提
- `cmd/experiment`: 指標 `clusters` / `clusterSize` / `grouped` / `largestShare` を追加（`Spacing()` と同じサンプリング間隔、末尾2割の平均）
- `README.md`: 上記4指標を「主な指標」の表に追加

**現状のベースライン**（既定条件・12シード × 50000tick・末尾2割の平均、連結距離30）:

| 指標 | 値 |
| --- | --- |
| `pop` | 147.33 ± 2.95 |
| `clusters` | 26.27 ± 0.31 |
| `clusterSize` | 4.36 ± 0.04 |
| `grouped` | 0.77 ± 0.00 |
| `largestShare` | 0.12 ± 0.00 |
| `clumping` | 1.17 ± 0.01 |

決定事項#17 のとおり、これは**「集団が無い世界のベースライン」として記録するだけ**で、目標値は書きません（段階11 の後に書く）。段階6.5 の残り（membership half-life、集団内/集団間の戦闘率比、集団間の距離分布、種族ごとの個体数）は未着手です。


## 2026-08-31 集団のようすを画像で残す（cmd/snapshot）

クラスタ数の数値だけでは「その集団がどんな形をしているか」が分からないので、1回分の実行の1瞬間をPNGに描くヘッドレスの開発用ツール `cmd/snapshot` を追加しました。同じオプションなら常に同じ画像になります（`engine` が決定的なので）。

- `engine`: `Clustering.Labels` を追加（個体ごとの所属クラスタ＝`Sizes` への添字、`Agents()` の順）。同サイズの成分は最小メンバー順で並べ、map の反復順に依存しないようにした。engine 自身は読まない（ビューアが色分けするためのもの）
- `cmd/snapshot`（新規）: クラスタごとの色分け＋連結した対の間に線、singleton は灰色、下帯に条件と**コミット**を焼き込む。文字は外部依存を増やさないよう 5x7 のビットマップフォントを自前で持つ（`cmd/snapshot/font.go`）
- `README.md`: 使い方と画像の読み方を追加

**記録した画像**: `docs/images/clusters-link30-e93025b.png`（コミット `e93025b`、seed 1・50000tick・連結距離30）。

その時点の値は 個体数144／クラスタ26／平均サイズ4.2／集団に属する割合76%／最大成分12%で、12シードのベースライン（クラスタ 26.27 ± 0.31、最大成分 0.12 ± 0.00）のほぼ中央にあたります。**連結距離30 がどう見えるかの基準画像**として残すもので、集団化の目標ではありません（段階11 より前に集団が形成されないのは想定どおり）。線が引かれている対が「連結距離の中にいる」ことを示すので、鎖状につながっただけのクラスタと本当の塊が見分けられます。


## 2026-08-31 membership half-life（段階6.5の2つめ）

段階6.5 の2つめ、**同じ顔ぶれが一緒に居続ける時間**を実装しました。クラスタ数は1枚の絵の中の塊を数えるだけなので、**たまたますれ違っている塊と本物の集団が見分けられません**。それを分けるのがこの指標です。

- `engine`: `engine/membership.go` を追加。`MembershipTracker` に一定間隔で `Observe(w)` を呼ぶと、**同じクラスタにいた2体の組**が何tick後まで同じクラスタに居続けるかの生存曲線を貯め、`Membership`（`Survival` / `HalfLife` / `Censored` / `Pairs`、任意のラグを補間する `At(lag)`）を返す
- **組（ペア）で測る。** クラスタは合体・分裂するので「同じクラスタ、のちほど」は合体をまたいで意味を保てないが、「この2体はまだ同じクラスタか」は保てる
- **片方が死んだ組は「離れた」と数えない。** 数えると在籍時間ではなく寿命を測ってしまう
- 観測の刻みと窓も `DefaultClusterLinkDist` と同じ理由で定数にした（`DefaultMembershipStep = 25` / `DefaultMembershipLags = 80`＝窓2000tick）。刻みが違う生存曲線どうしは比べられない
- `cmd/experiment`: 指標 `halfLife` / `together`（500tick後の生存率）/ `censored` を追加。**測るのは末尾2割だけ**（能力を末尾2割で読むのと同じ理由で、序盤の個体数が落ち着くまでの共在時間は問われていない）。実行時間は 12シード × 50000tick で 5.4秒 → 6.6秒
- `README.md` / `CLAUDE.md` を更新

**現状のベースライン**（既定条件・12シード × 50000tick・末尾2割）:

| 指標 | 値 |
| --- | --- |
| `halfLife` | 60.15 ± 1.19 tick |
| `together`（500tick後） | 0.14 ± 0.00 |
| `censored` | 0.00 ± 0.00 |

生存曲線の形（8シード平均）:

| ラグ | 0 | 25 | 50 | 100 | 200 | 500 | 1000 | 2000 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 生存率 | 1.000 | 0.651 | 0.526 | 0.396 | 0.269 | 0.145 | 0.088 | 0.058 |

**単一の指数関数ではありません。** 半減期は60tickなのに、500tick後でも14%、2000tick後でも6%の組が一緒にいます。速い成分（すれ違い）と遅い成分（何かで一緒に居続ける組）が混ざっています。段階11・12 で集団が現れるなら、動くべきなのは**遅い成分の重み**です。

決定事項#17 のとおり、これも「集団が無い世界のベースライン」として残すだけで、目標値は書きません。段階6.5 の残りは 集団内/集団間の戦闘率比・集団間の距離分布・種族ごとの個体数 の3つです。


## 2026-08-31 membership の生存曲線を画像で残す

半減期という1つの数字は、それを読み取った曲線の形を隠します。`cmd/snapshot` に `-mode curve` を足して、生存曲線そのものを記録できるようにしました。

- `cmd/snapshot`: `-mode world`（従来）／`-mode curve`（新規）。曲線は複数シードの平均で、**各シードを薄い線で重ねて描く**（平均だけを見て「どの実行でもこうなる」と読まないため）。横軸は**対数**（半減期が60tick付近、末尾は2000tickなので、線形軸だと片方が潰れる）。赤い破線が平均の半減期
- `README.md` / `CLAUDE.md`: `-mode` と画像の読み方を追加

**記録した画像**: `docs/images/membership-curve-571e9aa.png`（コミット `571e9aa`、シード1〜8・50000tick・連結距離30・刻み25・窓2000、半減期 58.8tick）。

**この絵の要点は、曲線が対数軸で直線にならないこと**＝単一の指数関数ではないことです。速い成分（すれ違い）と遅い成分が混ざっていて、段階11・12 で集団が現れるなら動くべきは遅い成分です。シード間のばらつきは小さく、曲線の形はシードによらないことも見て取れます。


## 2026-08-31 戦闘を相手で分ける（段階6.5の3つめ）

段階6.5 の3つめ「集団内戦闘率 ÷ 集団間戦闘率」を実装しました。ただし**書かれたとおりの形では測れない**ことが分かったので、意図を保ったまま定義を置き換えています（理由は下の設計判断ログ・追記16）。

- `engine`: `engine/fights.go` を追加。`FightTracker` に一定間隔で `Observe(w)` を呼ぶと、**遭遇（`CombatRadius` 以内にいる組）**を「`DefaultCompanionLag`（200）tick前から同じクラスタにいた組＝仲間」と「そうでない組＝よそ者」に分け、それぞれの**遭遇あたりの**戦闘率と、その比を返す
- 両方とも遭遇あたりの率にしてある。仲間のほうが近くにいる回数が多いことだけで答えが決まらないようにするため
- 200tick前に居なかった個体（新生児など）の遭遇は**推測せず捨てる**
- `cmd/experiment`: 指標 `fightCompanion` / `fightStranger`（遭遇100回あたりの戦闘数・%）/ `fightRatio` を追加。実行時間は 12シード × 50000tick で 6.6秒のまま（サンプル時のみ O(n²)）
- `README.md` / `CLAUDE.md` を更新

**現状のベースライン**（既定条件・12シード × 50000tick・末尾2割）:

| 指標 | 値 |
| --- | --- |
| `fightCompanion` | 1.41 ± 0.09 %（遭遇100回あたり） |
| `fightStranger` | 1.52 ± 0.10 % |
| `fightRatio` | 0.95 ± 0.05 |

**比は1と区別がつきません＝一緒に居続けた相手かどうかは、殴るかどうかに影響していません。** これは想定どおりで、今の世界には他個体が生きていることが自分の得になる経路が1本もない（`NODE.md` 11節）以上、相手を選ぶ理由がありません。段階11（外敵＝共同戦闘）と段階12（好意）を入れたあとにこの比が1を割るかどうかが、あるべき状態 C の判定になります。

母数は1シードあたり 仲間の遭遇 約6000回・よそ者の遭遇 約13000回（戦闘は 60〜100回・170〜220回）で、比は十分な回数の上に載っています。**よそ者の遭遇のほうが2倍多い**のは、共在の半減期が60tickで判定ラグが200tickだから、つまり「手の届く距離にいる相手」の大半は初対面だからです。

段階6.5 の残りは 集団間の距離分布・種族ごとの個体数 の2つです。


## 2026-08-31 集団間の距離分布（段階6.5の4つめ）

段階6.5 の4つめ、**集団間の距離分布**を実装しました。基準E（異なる集団のノード同士は、戦闘になるか近づかない）のうち「近づかない」側の指標です（「戦闘になる」側は前項の `fightRatio`）。

- `engine`: `World.ClusterGaps(linkDist)` を追加。各集団について**最も近い別の集団のメンバーまでの距離**を求め、その分布（`Gaps`、昇順）と要約（`Mean` / `Median` / `P10` / `Relative`）を返す
- **単独個体は集団としても、距離を測る対象としても数えない。** 数えると「集団どうしがどれだけ離れているか」ではなく「その間がどれだけ空いているか」を測ることになる
- **連結距離より短い値は原理的に出ない**（30以内なら同じ集団になるため）。分布は30を下限に持つ
- `Relative` は同じ数の集団を無作為にばらまいた場合の期待最近傍距離で割った値。**「構造なしの配置＝1」であることを較正テストで確認済み**（`TestClusterGapsRelativeOfARandomLayout` で 0.98）。したがって1と比べて読める
- `cmd/experiment`: 指標 `gap` / `gapP10` / `gapRel` を追加
- `README.md` / `CLAUDE.md` を更新

**現状のベースライン**（既定条件・12シード × 50000tick・末尾2割）:

| 指標 | 値 |
| --- | --- |
| `gap`（最近接集団までの平均距離） | 54.29 ± 0.50 |
| `gapP10`（下位1割） | 32.39 ± 0.11 |
| `gapRel`（無作為配置比） | 0.80 ± 0.01 |

**集団は互いを避けていません。** それどころか `gapRel` が0.80＝**偶然より寄り集まっています**（同じ食料に引かれるためと思われる）。`gapP10` が32.4と連結距離30のすぐ上にあるのも同じことを言っていて、近い集団どうしは常に合体寸前です。基準E が満たされるなら、段階11・12 のあとに `gap` と `gapRel` が上がり、`gapP10` が30の床から離れるはずです。

**当初 `Relative` は「構造なしでも1を割る（集団は点ではなく面なので参照値が甘い）から、1との距離は読むな」と書いていましたが、較正したら0.98でした。** 面であることの影響は、集団が集団間の距離に比べて小さい今の形では無視できます（集団が大きくなったら成り立たなくなるので、その旨をコメントに残した）。この較正をしていなければ 0.80 を「測り方のクセ」として捨てるところでした。

段階6.5 の残りは **種族ごとの個体数**（段階11 以降に効く）だけです。


## 2026-09-01 種族ごとの個体数（段階6.5の5つめ）

段階6.5 の最後、**種族ごとの個体数**を実装しました。基準A（異なる種族がそれぞれ一定数存在する）の指標です。今は種族が human ひとつしかないので、測っているのは**人間の個体数の振動そのもの**です。それが段階11 で外敵が入ったあとの比較対象になります。

- `engine`: `Agent.Species`（`Species` 型）を追加。**今は全員 human で、ルールは一切これを参照しません**（知覚にも載せない）。ゼロ値が human なので、種族を書かずに作ったノードは全部 human のまま
- `engine`: `CensusTracker` を追加。一定間隔で `Observe(w)` を呼ぶと、直近 `DefaultCensusWindow`（5000tick）の窓の中の種族ごとの**平均・最小・最大・全体に占める割合・谷・振れ幅**を返す
- **1tickの数え上げでは答えにならないので窓で持ちます。** 2種族が同じ1フレームに写っていても、片方が消えかけている最中かもしれない。共存は瞬間ではなく期間の性質です（基準Aが「窓の平均」で書かれているのはそのため）
- **読むのは振れ幅（`Swing`）ではなく谷（`Trough` ＝ 窓の最小 ÷ 平均）です。** 基準Aは振動を許容すると明記しているので、振れ幅の大きさ自体は何も言いません。移入が無い以上**0は吸収状態**なので、決めるのは「谷が0にどれだけ近づいたか」だけです
- **絶滅は窓ではなく実行全体で覚えます。** 窓が死を通り過ぎると、絶滅した種族は「行が減った」ようにしか見えなくなる。消えた種族も `Extinct` 付きの行として残します
- **種族の一覧（ロースター）は持ちません。** 実際にノードに付いていたラベルを数えて必要なだけ伸びるので、段階11 で種族が増えてもこのファイルは触りません。一度も現れなかったラベルは「絶滅した種族」ではなく、単に存在しないものとして扱います
- `cmd/experiment`: 指標 `species` / `rareShare` / `rareTrough` / `rareSwing` を追加（一番少ない種族を見る。共存はいちばん苦しい種族で決まり、隣の元気な種族では決まらないため）。census だけは末尾2割ではなく**実行全体**で観測します（絶滅は起きた時に捕まえないと窓から消える）
- `NODE.md` / `README.md` / `CLAUDE.md` を更新

**現状のベースライン**（既定条件・24シード × 50000tick・直近5000tickの窓）:

| 指標 | 値 |
| --- | --- |
| `species`（生き残っている種族数） | 1.00 ± 0.00 |
| `rareShare`（一番少ない種族の割合） | 1.00 ± 0.00 |
| `rareTrough`（窓の最小 ÷ 平均） | 0.90 ± 0.01 |
| `rareSwing`（(最大−最小) ÷ 平均） | 0.18 ± 0.01 |
| `extinct` | 0.00 ± 0.00 |

**人間の個体数は平均の±1割ほどで揺れていて、0には遠い**（24シードで絶滅0）。段階11 で外敵が入ったとき、`rareShare` が0から離れて `rareTrough` が0に触れなければ「2種族が共存している」と言えます。

**予想を外した記録**: 指標が定数でないことを確かめるため食料量を振ったところ、**食料を減らせば谷が深くなるだろうという予想は外れました。** 個体数は146→86と大きく減るのに、`rareTrough` は 0.90→0.89（−0.01 ± 0.01）、`rareSwing` は 0.18→0.21（+0.03 ± 0.02）でどちらも誤差の範囲すれすれです。**振動の大きさは食料の絶対量ではなく比で決まっていて、絞っても相対的な揺れ方は変わりません。** 一方、食料を増やした側（0.30）では `rareTrough` 0.93（+0.03 ***）・`rareSwing` 0.14（−0.05 ***）とはっきり平らになりました。**応答は非対称で、豊かにすると平らになるが、絞っても荒れない。** 指標が動くことは確認できましたが、動く向きの予想は半分外していました。

読み取り専用の測定で世界の挙動は1tickも変わらないので（同一シードの `pop` が変更前と一致）、`cmd/devview` での目視確認は行っていません。

これで段階6.5 の指標は5つとも揃いました。残るは**完了条件（50000tick のベースラインを `HISTORY.md` にまとめる）**だけです。


# 設計判断ログ

日付は決定した会話のタイミング。あとから追記するときは末尾に追加する（過去の行は書き換えない）。

- 2026-08-11: 実装言語をGoに決定。理由: 速度、開発者の既存Go実績（goLogAnalyzer等）、ビルド・テストループの速さ。
- 2026-08-11: サーバ・クライアント型を採用。理由: 将来的に複数人に使ってもらうことを想定。
- 2026-08-11: ネイティブアプリを採用し、ブラウザ版は当面見送り。
- 2026-08-11: コード（識別子・コメント・ログ／エラーメッセージ）はすべて英語で統一することに決定。ドキュメント（CLAUDE.mdなど）は日本語のままでよい。
- 2026-08-11: パフォーマンスに影響しうる変更（ホットパス）のときは、都度Goの軽量ベンチマーク（`go test -bench`）を実行して確認する運用を採用。CIで強制する重いゲートにはせず、開発者自身の習慣として運用する。
- 2026-08-11: `protocol`/`server`/`client` が揃う前でも機能ごとに目視確認できるよう、`cmd/devview`（engineを直接importする簡易ebitenビューア、ネットワークなし）を導入する方針を追加。本番の`client`とは別物として扱う。
- 2026-08-12: モジュール名を `github.com/toku463ne/cld_human_behaviour` に決定。
- 2026-08-12: シミュレーションの定数を package const ではなく `engine.Config` のフィールドとして持つことに決定。理由: テストでルールを1つずつ無効化して（`MutationStd = 0` など）他のルールから切り離して検証できるようにするため。`Config` が実質的な仕様の一覧にもなる。
- 2026-08-12: 乱数は `World` が持つ `*rand.Rand` 1本に統一し、固定シードでの再現性を担保する。乱数源のinterface化は見送った（ホットパスに間接呼び出しを増やしたくないため）。確率が絡むルールのテストは「決定的にできるものは条件を作って決定的に検証」「ゆらぎ自体が本質のものは固定シードでN回試行して割合を比較」の2本立てで書く。
- 2026-08-12: engineの近傍探索は当面 v0 と同じ線形走査のままにする。理由: 数百ノードでは十分速く、まずシンプルさを優先するため。`BenchmarkEngineStep` の初回ベースライン（300体で約343µs/op）はその判断材料として残す。
- 2026-08-12: 死亡個体は v0（30tickごとにまとめて削除）と違い、毎tickの最後にその場で取り除くことにした。理由: `Agents()` が常に生存個体だけを返すほうが、devviewや将来の `protocol` のスナップショットにとって扱いやすいため。出産で生まれた子も同様にtick末にまとめて追加する（ループ中にsliceへappendするとバッキング配列が再確保されうるため）。
- 2026-08-12: 最終形をゲーム（1ノードを操作し子に乗り換えて生き延びる）と定め、そのために「意思決定の差し込み口（コントローラ／知覚／行動の共通境界）」と血統記録を早期に敷く方針を採用。成長・老い・自然死は将来実装。
- 2026-08-12: 状態量を 体力・空腹度・食料 の3軸に確定（一方向結合で回復経路を保証）。能力を パワー（体力あたりの戦闘効率）／理性（推定精度）／頭の良さ（戦略の幅と選択品質）の3種に役割分離。
- 2026-08-12: 意思決定はトリガー方式（毎tickではない）。逃走用のハードコード閾値は設けず、逃走・追撃は効用比較の結果として創発させる。優先順位は 生命>子孫、時間はコスト。
- 2026-08-12: 戦闘は継続ダメージ方式で攻撃側有利（殴る側 < 殴られる側のコスト）。相手のリスク記憶＝自分が失った体力の累積（ゆるく減衰）。復讐は感情ではなく食料競合の先制排除として効用式に載せ、専用の恨み系状態は持たない。
- 2026-08-12: 相手の強さは (A)直接戦闘のリスク記憶 と (B)観戦ベースの強さ推定 の2系統で扱う（(B)も今段階から実装）。見た目からの分類は将来「学習で獲得」する形にし今はやらない。
- 2026-08-12: 実行の重さは方針(b)（素朴に作ってベンチで重くなってから最適化）で進める。

- 2026-08-12: 段階0〜6を実装。以下は実装時に決めたこと。
  - 知覚（`Perception`）には他個体の真の能力を一切載せない。コントローラが見るのは自分の推定値（理性に応じてぼかした値）だけ。これで「真の戦闘力は隠しパラメータ」を型で保証する。
  - 効用式の生存確率は「餓死までのカウントダウン」と「体力が減っていること自体の危険（`ShockRisk`）」の2つを合成する。後者がないと、空腹でない個体にとって体力の消費がタダに見えて、世界が殴り合いになる。
  - 「休む」「食べる」の見積もりでは、受けている被ダメージを差し引いたうえで回復量を計算する（`recoverable`）。これがないと「殴られながら休んで回復する」が最善手に見えて、逃走が絶対に選ばれない。
  - 「さまよう」の価値は空腹度だけで決める。全体の死亡リスクに比例させると、追い詰められた個体が逃げずに散歩に出る。
  - 頭の良さの「選択品質」は、最善手を選ぶ確率ではなく**効用の評価に乗るノイズ**として実装した。確率方式だと外したときに一様ランダムな行動（＝しばしば自殺的な攻撃）を選んでしまい、世界が暴力的になりすぎる。
  - 戦闘コストの見積もりは「相手を倒しきるまで」ではなく `SkirmishTicks` で頭打ちにする。倒しきる前提で価格をつけると1食を巡る小競り合いが常に割に合わず、誰も何も争わなくなる。
  - ペア期間の終了はその tick の移動より先に処理する。そうしないと、ループで先に処理された側だけが移動コストを払い、両親が非対称になる。
  - 老い・自然死は実装しない（設計どおり将来実装）。個体は餓死か戦闘でのみ死ぬ。そのぶんリスク記憶の減衰が効いてくる。
- 2026-08-12: 段階0〜6実装後のベンチ再測定（同じRyzen 5 5500U）。`BenchmarkEngineStep` 300体で約283µs/op・18 allocs/op（v0移植時は343µs/op）。混雑シナリオ `BenchmarkEngineStepCrowded`（300体を300x260に詰めて食料を絞る）で約633µs/op、`BenchmarkDecide`（効用比較1回）で約5.3µs/op・0 allocs。
- 2026-08-12: `DefaultConfig` は「食料が足りない世界」に振り直した（`FoodSpawnRate` 1.5→0.20 相当）。理由: 食料が余っていると争いも餓死も起きず、能力に選択圧がまったくかからないため（実際、最初の実装では戦闘が20000tickで0回だった）。
- 2026-08-13: 意思決定トレース（`engine/trace.go`）を追加。以下は実装時に決めたこと。
  - 効用の内訳（`Utility`）を「表示用の別計算」にはせず、**効用式そのものの唯一の実装**にした（`Utility.Total()` が各行動の点数を返す）。表示用に式を書き直すと、トレースが実際の判断とズレたときに気づけない。
  - 記録は追跡対象のノードだけ。全ノード常時記録は決定1回あたりのコストを跳ね上げるうえ、読む側も追えない。エージェント側は `trace *traceLog` ポインタを持ち、非追跡なら `nil` チェック1回で終わる。
  - トレースはノードの死後も残す（`World.traces` はIDキー）。死因を読み返すのが一番の用途のため。
  - 履歴はノードごとに直近16件のリングバッファで、`Options` のスライスを使い回す（追跡中に毎決定でallocしない）。読み出し時だけコピーを返す。
  - 再判断トリガーに名前を付け（`Trigger`）、`needsDecision = true` を立てていた各所を `requestDecision(理由)` に置き換えた。「なぜ考え直したか」はトレースの一行目として一番重要な情報で、旧実装はbool 1個で理由を捨てていた。
  - ベンチ再測定（同じRyzen 5 5500U、`-benchtime=3s -count=3`）: `BenchmarkDecide` 5.36µs→5.77µs（+7.5%）、`BenchmarkEngineStep` 276µs→283µs。原因は `Utility` 構造体（float64×17）の組み立てで、追跡していないノードでも払う。ポインタ渡し・トレース時のみ構造体を保持など数パターン測ったが、いずれも1〜2%しか戻らずAPIが汚くなるだけだったので、可読性を採った。同一シード5000tickで統計値が完全一致することは確認済み（リファクタで挙動は変わっていない）。
  - シナリオテストを書いていて、engineの側が正しく自分の想定のほうが間違っていたケースが2件あった（「格上に守られた食料は諦めて別を探すはず」→ 実際は勝率0.19でも命がかかった食事の期待値のほうが徘徊より高いので賭けに行く、が正しい）。トレースを見て初めて分かったので、この道具立て自体は当たりだった。なお、食料の争奪は純粋な**距離の競走**として値付けされていて、「着いた先で殴られる」ぶんは入っていない。今のところ意図どおり（相手の強さは戦闘の効用側で見る）だが、覚えておくべき単純化。

- 2026-08-14: ルール変更をA/Bで測る `cmd/experiment` を導入し、その最初の結果として**戦略の深さゲートを既定で無効にした**（`StrategyDepthUnlock` 16→0）。以下は判断の根拠と、実装時に決めたこと。
  - きっかけは「知能48以上は深さ軸で何も買えない」という指摘。閾値は `StrategyDepthUnlock` の整数倍（16/32/48）だが、初期能力は一様分布 `[25,75]`、20000tick後も概ね24〜73に収まる。つまり16の閾値は一度も発火せず、32もほとんど跨がず、**実質的に効いているのは48（先制排除）の1本だけ**だった。
  - 測定（24シード × 20000tick、同一シードの差分）: ゲートを切ると平均知能の上がり幅が **+4.52 ± 0.95** 増える。逆に閾値を20/40/60に詰めると **−3.87 ± 0.79**（知能への選択圧が負に反転する）。`ChoiceNoise = 0` にして評価ノイズの経路だけを潰すと、知能の上がり幅は +0.63 ± 0.51 でほぼ0。つまり**知能を選択しているのは評価ノイズの経路だけで、深さゲートはむしろ足を引っ張っていた**。60000tickでも向きは同じ。
  - なぜそうなるかの解釈: ゲートは愚かな個体を「損な手を思いつけない」形で保護する。先制攻撃と観察は、今のパラメータでは平均すると割に合わない手なので、それを賢い個体だけに解禁すると賢さが罰になる。ゲートを外すと全員が同じ土俵に立ち、あとは評価の正確さだけが効く。
  - ゲートの仕組み自体は消さずに `Config` に残した（`StrategyDepthUnlock = 0` が「ゲートなし」）。理由: 再設計の土台であり、A/Bの対照条件としても要る。既定を変えるだけで挙動が切り替わる形にしておくほうが、コードを消すより後戻りが安い。
  - 副作用として世界は暴力的になった（戦闘 +25%、殺害 +15%、餓死 −16%）が、個体数・世代数は変わらない（−2.0 ± 2.1）。設計メモの「攻撃側有利は個体数を崩しうる」という警告の範囲内には収まっている。
  - 速度: `BenchmarkDecide` 5.8µs→8.0µs（+36%）。ゲートが無くなり全個体が観察・攻撃まで採点するため。`BenchmarkEngineStep` は同時測定で差が測定ノイズの範囲だったので、まずは可読性と正しさを採って最適化しない。重くなったら候補の枝刈り（方針(b)）で対応する。
  - 測定の作法を CLAUDE.md の「ルール変更の測定（cmd/experiment）」の節に切り出した。とくに **paired（同一シードの差分）でないと何も見えない** ことと、**能力は末尾2割の平均で読む** ことは、これを作る過程で分かった実務上の要点。

- 2026-08-14: 協力・外敵に着手する前段として、**集団化の指標を先に作ってベースラインを取った**（段階A）。あわせて文書を分割した。以下は判断の根拠。
  - 指標を先に作ったのは、「群れているように見える」は証拠にならないため。`Spacing()` の `Clumping` は個体数で割ってあるので、集団が増えただけで上がることがない。`Stats()` に入れなかったのは O(n^2) で、`Stats()` は devview が毎フレーム呼ぶため。
  - **当初の想定が測定で覆った件**: 「食料を増やせば殺しは減るので、協力の効果と混同する」と考えていたが、実測では `FoodSpawnRate` を 0.12〜0.30 で振っても `killShare`（死因に占める殺害の割合）は 0.59〜0.64 でほぼ動かず、殺害の**絶対数はむしろ食料が増えるほど増える**（個体数が増えて遭遇が増えるため）。したがって `killShare` は食料供給と交絡しにくく、協力の効果を測る指標として使える。`morefood` / `scarce` の条件は対照として残す。
  - 文書を `NODE.md` / `ENEMY.md` / `PARAMETERS.md` に分けたのは、CLAUDE.md の冒頭に書いた「肥大化してきたら分割する」方針どおり。ルールの記述が CLAUDE.md と実装の両方にあると必ず片方が古くなる（実際、深さゲートを既定オフにした後も「戦略の幅と選択品質は頭の良さでゲートされる」という記述が残っていた）。CLAUDE.md 側には**崩してはいけない原則**だけを残し、「何が起きるか」は `NODE.md` を唯一の置き場にした。
  - `ENEMY.md` は中身が全部未実装だが先に作った。外敵は「何を作るか」より「何を決めていないか」のほうが多く、決めていないことを書き留める場所が要るため。

- 2026-08-14: 種族の汎用化（人間・外敵で同じノード構造・同じ意思決定エンジンを共有し、パラメータ範囲と種族固有遺伝子だけで種族差を表現）、食料3種＋死体ドロップ（人間は共食い不可、外敵は種族を問わず肉を食べる）、遺伝子ベクトル（`Genome []float64`）化と学習パラメータの分離、模倣則による文化の創発、地域ごとの世界リセット（random immigrants）、将来の地形対応、の方向性を合意（別セッションでの設計相談）。性能面は近傍探索の空間分割が本質的なボトルネックであり、パラメータ・種族・食料種の増加自体はほぼ無視できるコストと判断（空間分割グリッドの導入を優先する）。詳細ルールは実装着手時に `NODE.md` / `ENEMY.md` / `PARAMETERS.md` へ書く。着手時期・`protocol`/`server`/`client` との順序は未確定（CLAUDE.md の「未決事項」参照）。

- 2026-08-16: 設計判断ログを `CLAUDE.md` からこの文書へ移し、今後もここに追記することにした。理由: CLAUDE.md は「今どうなっているか（設計・規約・原則）」を読む文書で、毎回の作業で最初から読み直される。過去の判断は追記のたびに伸び続ける一方で、読み返すのは「なぜこうなっているか」を確かめたいときだけなので、伸びても困らない `HISTORY.md` 側に置くほうが釣り合う。日付つきで末尾に追記していく点も、`HISTORY.md` の既存の運用と同じ。CLAUDE.md 側には**今も有効な結論だけ**を残し、その経緯はこの文書を指す形にした。

- 2026-08-18: 段階7以降の設計を詰め、`PLAN.md`（チェックリスト形式の実装計画）を新設した。ユーザからの10項目の要望を検討した結果と、その判断の理由は次のとおり。

  - **遺伝子を予算制にした**（攻撃力・防御力・最大体力・移動速度・回避率・記憶容量の6種、合計 `GeneBudget` は一定）。当初の検討では「最大値を遺伝させると価格が無いので全部100に張り付き、選択圧が測れない遺伝子が5本増えるだけになる」と指摘し、体の大きさに比例する基礎代謝で価格をつける案を出したが、ユーザから予算制の提案があり、そちらを採った。予算制のほうが優れている点は2つ。(1) 価格が制約そのものなので、代謝率という追加のパラメータを較正しなくてよい。(2) 測る対象が**水準ではなく配分**になり、「全部上がった」という読めない結果が構造的に起きない。
  - **天才は予算そのものが増える個体**とした。`CLAUDE.md` の将来実装にある「天才＝学習パラメータの1つを通常の突然変異よりずっと大きく飛躍させる」と同じ役割を、遺伝子側で果たす。ただし超過分には維持コストを課す。でないと天才が単純上位互換になり、天才かどうかだけが効く世界になる。
  - **継承を混合遺伝（両親の平均）から染色体式（遺伝子ごとに親のどちらかを引く）＋正規乱数に変えることにした。** 理由は分散。混合遺伝は毎世代ごとに分散を約半分にするので、現在は `MutationStd` だけが多様性の唯一の源になっている。粒子的遺伝なら分散が維持され、選択圧が読みやすくなる。突然変異率を1%にすると分散注入が1/100になるので、幅は現在の std=4 から std≈40 相当へ較正が要る（`0.01 × 40² = 16 = 4²`）。結果として「稀に大きく飛ぶ」になり、これも天才の機構と同型。**この変更を境に過去の測定値とは直接比較できなくなる**ので、7b で新しいベースラインを取る。
  - **ラマルク継承を採用するが、`LamarckRate` で切替可能にした。** 2026-08-16 に保留していた「子は親の生まれたときの傾向を継ぐのか、学習後の傾向を継ぐのか」への回答。ラマルクを固定にしなかったのは測定のため。学習の収束は遺伝より桁違いに速いので、ラマルク固定だと `cmd/experiment` の「20000tickでの平均能力の変化量」が選択圧ではなく学習速度を測っている状態になり、文化のクラスタ化と遺伝のクラスタ化も分離できなくなる。「ルールの切替は engine の分岐ではなく `Config` の書き換えで表現する」という既存方針の適用でもある。
  - **防御力と回避率は払う資源を変えることで分離した**（防御＝体力で払う、回避＝行動機会で払う）。どちらも「食らうダメージを減らす」なので、同じ資源で払わせると深さゲートと `ChoiceNoise` で起きた失敗（同じ軸の裏表で、選択圧がどちらから来たか測れない）と同型になる。「能力の役割を重ねない」原則の適用。
  - **effort の配分はプリセット3種に離散化した。** 現在 `BenchmarkDecide` は 8.0µs で候補は約40件。配分を連続値にすると数百件になる。`effortLevels` を2段階に留めているのと同じ判断。
  - **記憶容量を有限にした。** 容量が無制限のままで忘却曲線だけを遺伝子にすると、忘れることが純粋な損なので0に進化して終わる。容量を有限にして初めて「古いものを捨てて新しい相手を覚える」というトレードオフが生まれ、忘却曲線が測れる形になる。`opinions` のメモリが O(個体数) から O(容量) に落ちる副産物もある。
  - **見た目からの推定は、engine が写像を持たず各ノードが学習する形にした。** `AgentView` には遺伝子から作った観測可能な相関値（`Appearance`）＋観測ノイズだけを載せ、真値は載せない。「相手の真の能力は隠しパラメータ」という型による保証を維持したまま、`CLAUDE.md` の「決め打ちではなく学習の結果として得られるもの」を満たせる。一番の効果は、初対面の相手に `PriorStrength = 50` を当てはめるのをやめられること。指標も明確（世代とともに初対面推定の誤差が下がるか）。
  - **明示的な「協力」（相互選択の行動＋協力性遺伝子）は却下した。** `ENEMY.md` の方針どおり、外敵と「倒した者だけがドロップを得る」という世界側の事実だけを置き、打算は既存の効用式にやらせる。協力性遺伝子を先に入れると「協力が起きたのは遺伝子を入れたからでは？」となり、創発したのか作り込んだのかが測れなくなる。**段階11 まで進めて一向に協力が現れなかった場合にだけ再検討する**（現れなかったという事実自体が、明示的な機構を入れる根拠になる）。外敵の大きさでドロップ数を決めるというユーザの案は、協力の頻度を外敵のサイズ分布というツマミ1本で調整できる点で良い。成立条件は「ドロップ数 ÷ 参加人数 > 単独で同じ時間に得られる期待値」。
  - **空間グリッドを2段階に分けた。** 「広域探索の絞り込みだけに使う（円の視界判定はそのまま＝挙動不変）」と「視界をグリッド単位にする（挙動変更。角の方向だけ遠くまで見える異方性が入り、逃走成功率と食料への競走が変わる）」は別物。混ぜると、性能改善のつもりが選択圧を動かしていた、という事故になる。なお `CLAUDE.md` に「既存の空間分割グリッドを粗くまとめたブロックを流用」とあったが、**空間分割はまだ存在しない**（`perceive()` は全ノードを線形走査していて実質 O(n²)）。7a で初めて作る。
  - 未決のまま残したもの: 「染色体」が性染色体か遺伝子ごとの独立分配か（現在は後者を前提に記述）、理性・頭の良さを予算に入れるか（今の決定では予算の外＝ただで伸びる）、記憶の帯域と忘却曲線を予算の内外どちらに置くか、`GeneBudget` と天才の各値、外敵のサイズ分布。いずれも `PLAN.md` の「未決」節にある。
- 2026-08-18（追記）: 予算制の詳細を詰めた。**予算（遺伝子の合計値）は固定ではなく出生ごとに幅をもって抽選する**、**天才は2段階（10年に一度／100年に一度）で異常に大きな予算を引く**、**理性・頭の良さも予算に入れる（遺伝子は8種）**、の3点。以下は決めるときに整理した論点。

  - **遺伝するのは配分（比率）、予算（水準）は遺伝しない**、という切り分けにした。予算に幅を持たせると「予算そのものが上がるだけの遺伝子」になりかねず、それでは価格が無いのと同じで予算制にした意味が消える。子は遺伝子ごとに親のどちらかの値を引き、そのあと自分が引いた予算に合うようスケールする。結果として**天才の子は配分の型は継ぐが予算の大きさは継がない**。切替は `BudgetHeritability` で残す（未決）。
  - 予算が遺伝しないので、**天才の超過分に維持コストを課す必要も無くなった**（同日の前半では課す方針だった）。水準に選択圧がかからない以上、放っておいても張り付かない。
  - **選択圧の測り方が変わる。** 予算に個体差がある以上、能力の絶対値を平均すると予算抽選のばらつきを拾う。`cmd/experiment` の指標を**予算に対する比率**に変える必要がある。これは 2026-08-14 に決めた「能力は末尾2割の平均で読む」と同じ種類の実務上の要点で、変えないと条件差がノイズに埋もれる。
  - **理性・頭の良さを予算に入れたことで、既存の未決事項（理性への選択圧が弱い）が測りやすくなる。** 予算制では「理性に配分するくらいなら攻撃力に回す」という形で現れるので、理性の配分比が下がり続けるなら、理性の利得が価格に見合っていないことが直接読める。予算制が理性問題の診断装置を兼ねる。
  - **稀な天才は進化の駆動力ではなく物語として扱う。** 100年に一度の個体が集団平均を動かすことはないし、`cmd/experiment` の 20000tick にはまず現れない。効果を測るときは発生率を上げた条件を別に用意し、本番の頻度は演出として戻す。役割を取り違えると「天才を入れたのに何も変わらない」という誤った結論になる。
  - **予算の上限にはクリップ由来の天井がある。** 遺伝子1本の範囲が1〜100なので、予算が 8×100 = 800 に近づくと全遺伝子が100に張り付き、配分という概念そのものが消える。大天才の予算も1本平均70程度（560前後）で止める。
  - 「10年に一度」を engine に翻訳するには暦が要る（`TicksPerYear`）。発生率は世界側で間隔を管理するのではなく**出生あたりの確率**で持つ方針（世界レベルの状態を増やさない）。ただしその場合、実際の発生間隔は個体数に依存してずれる。
- 2026-08-19: **魅力の遺伝子化・あるべき状態（受け入れ基準）・伝承**を決めた（`PLAN.md` の決定事項 #12〜#16）。以下は決めるときに整理した論点。

  - **魅力を9本目の遺伝子にした。** 予算を食うが生存には一切効かない純粋な装飾で、性選択でだけ報われる。予算制の下では「攻撃力に配るか魅力に配るか」という実在のトレードオフがそのまま出るので、予算制の題材として筋がいい。遺伝子が9本になったので `GeneBudgetMean` を 320→360、天才の予算も比例して上げ、クリップ由来の天井は 9×100 = 900 になった。**魅力は 7c で最初から入れる**（後から遺伝子を足すと予算も測定のベースラインも取り直しになる）。
  - **既存の `fitness`（パワー35%＋理性15%＋知能15%＋体力35%）を魅力遺伝子で置き換える。** ここに魅力を足すだけだと、パワーが「戦闘に勝つ」と「配偶者を得る」の2役を持ったままになり、パワーへの選択圧が戦闘由来か性選択由来か測れなくなる。「能力の役割を重ねない」原則の適用で、防御と回避を払う資源で分けたのと同型の判断。
  - **魅力と実能力は相関させない。** engine が相関を決め打ちすると、「見た目から能力が読めるか」という段階10 の学習の答えを先に書いてしまう。無相関にしておけば「装飾は嘘をつけるのか」をシミュレーションに答えさせられる。ハンディキャップ原理を入れるなら別設計（未決）。
  - **「魅力の高い相手が見えたら近づきやすくする」は行動として書かない。** 求愛の効用は既に `OffspringValue × 相手の推定魅力/100` なので、魅力が上がれば接近しやすくなるのは今の効用式のまま起きる。足すのは再判断トリガー1本だけ。「ハードコードされた行動閾値を作らない」原則（トリガーは「考え直せ」であって「こうしろ」ではない）の適用。
  - **性選択は戦闘由来の圧より強く出やすい**（出生のたびに働く）。全員が魅力に予算を寄せて戦闘力が崩れる暴走がありうるので、`CompetitionWeight` / `SkirmishTicks` と同じ「触ったら個体数を確認する」対象として扱う。
  - **「あるべき状態」を受け入れ基準として文書化した**（多種の共存／human の集団が複数／集団内の戦闘率が低い／集団が行動を共にする／集団間は戦うか近づかない）。目的は将来のゲーム性で、プレイヤーが制圧する対象＝集団が世界に自然に存在していないとゲームが成立しない。**ただしこれは仕様ではない。** 集団IDと所属行動を書けば明日にでも満たせるが、それは「集団があるのは我々がそう書いたから」であって、明示的な協力を却下した #10 と同じ理由で世界を測る意味が消える。創発で狙い、達成できなかった場合にだけ明示的な機構を再検討する、という条件つきの構えを揃えた。
  - **目標の数値は先に書かず、先に測ることにした（段階6.5 を新設）。** 今の世界のクラスタ数も集団内／集団間の戦闘率比も一度も測っていないので、今数字を決めても根拠がない。クラスタ検出・membership half-life・戦闘率比・集団間距離は読み取り専用で安く作れて、以降の全段階の効果測定に使い回せる。**membership half-life を入れたのは、「集団」と「たまたま近くにいる塊」を区別できる指標がこれだけだから。**
  - **今の世界では集団化は原理的に起きない、という認識を明文化した。** `NODE.md` 11節のとおり、効用式で他個体について語る項は `rival`（消えると得）だけで、他個体が生きていることが自分の得になる経路が1本もない。あるべき状態の鍵は魅力でも外敵でもなく**正の項を1本足すこと**で、それが親密度（無害に共在した時間）＋伝承。外敵ドロップ（段階11）も正の項だが狩りの最中しか効かないので、集団の**持続**は説明できない。
  - **集団間の敵対は「よそ者には正の項が無い」ことだけから出す。** 親密な相手は学習源として価値があるので離れたくない、見知らぬ相手は価値ゼロで `rival` の項だけが残るので攻撃するか避ける。憎悪パラメータを作らずに済むので、「恨みの状態を持たない」原則を壊さない。
  - **伝承で伝えるのは effort の配分そのものではなく、effort を決める前提。** effort はプリセット3種＝効用比較の結果（#7）なので、配分を直接コピーさせると効用式と競合する第二の意思決定系ができ、`Utility.Total()` が効用式の唯一の実装だという今の強み（トレースが式とズレようがない）が失われる。段階12 の学習対象（`retaliation` / `pAccept` / `RiskWeight` / `CompetitionWeight`）を伝承の対象にすれば、「あの集団は守り寄りの effort を選ぶ」は結果として現れる。
  - **成功・失敗体験（観察含む）は既存の観戦経路に相乗りする。** 第三者の戦いを観戦して強さ推定を更新する仕組みが既にあるので、新しい観察の仕組みは作らない。
  - **育児期間（親子の共在）を独立した段階7d にした。** `PairBondDuration` の延長で安く作れる一方、空間分布を直接動かすので、`ChildRearingTicks = 0` で完全に切れないと「集団ができた」のどれだけが育児由来か分離できない。垂直伝承（親→子）は、育児期間ぶん共在するという事実を通じて水平伝承の特別な場合として同じ経路で起きる（伝承の仕組みを2つ持たない）。
  - **親密度は記憶容量を食わせる（段階9）。** ただでいくらでも覚えられるなら全員と親密になり、集団は世界に1個しかできない。容量が集団サイズの実質的な上限になる。
  - **空間局在が無いと文化は世界全体で1つに収束する。** 模倣が距離無制限だと全員が同じ値になるので、段階7a のグリッドと有限の記憶帯域がクラスタが分かれるための前提条件。段階12 の完了条件を「平均ではなく分散と空間相関」にしてあるのはこのため。
  - **多種の共存は帯（band）で判定する。** 捕食-被食は本質的に振動する（Lotka-Volterra）ので、固定値で書くと永久に達成できない目標になる。ニッチ分離の軸には計画済みの食料3種を使う。
  - **プレイヤーに集団を見せるための識別は view 層の仕事**とした。クラスタ検出は読み取り専用の解析なので、engine に集団IDを持たせる理由にはならない。
- 2026-08-30: **好意の発生源・伝承の等価交換・「ヒント」の進化**を決め、`PLAN.md` の段階9・段階12 を書き換えた（決定事項 #17〜#23、#14 と #16 は差し替え）。以下は決めるときに整理した論点。

  - **段階11 より前は集団が形成されなくてよい、と前提を見直した。** 2026-08-19 に「今の世界では集団化は原理的に起きない」と書いておきながら、段階6.5 で B〜E の目標値を決めることになっていた。これは矛盾で、集団が生まれる経路（共同戦闘・伝承）が入る前の世界に目標値を書いても達成不能な数字になる。**測定インフラは予定どおり段階6.5 で先に作り、目標値の記入だけを段階11 の完了条件へ移した。** 段階6.5 で測る値は「集団が無い世界のベースライン」として意味がある。実際の人類の社会性も外部の脅威に強く駆動されたと考えられており、外敵より前に集団を期待しないという単純化には裏付けがある。
  - **敵意（脅威記憶）を新設しないことにした。** 距離・接近ベースで敵意を積む案を検討したが、既存のリスク記憶（その相手に払わされた体力の累積・遅延減衰つき）が既に同じ役割を果たしている。新しい状態を足すと「恨みの状態を持たない」原則との境界も曖昧になる。記憶に足すのは好意の1項だけにした。
  - **好意も「共在時間で積む」案を捨て、実イベントからのみ生まれる形にした。** 視界にいる時間に比例して加点する案は 2026-08-19 の親密度そのものだったが、上の前提見直しで「外敵の前に好意を生成する」急ぎの理由が消えた。共在時間は**近くにいるだけで無条件に価値が生まれる**ので、食料への競走の副産物である近接まで好意に化けてしまう弱点もある。発生源をペア形成・出産／血縁／共同戦闘／継承の4つに絞ると、どれも「実際に何かが起きた」という事実に紐づく。血縁ブーストを入れたのは、ゼロから積み上げなくても種ができるようにするため（`ParentIDs` / `ChildIDs` は今まで記録するだけで使っていなかった）。
  - **深い伝承は既存の `observe` に相乗りさせる。** 新しい観察の仕組みを作らないという 2026-08-19 の方針をそのまま延長した。`observe` には既に体力コストと `observeTicks` があるので、深い関わりの価格を新しく較正しなくてよい。排他性も `observe` が持つ「1度に1人しか観察できない」がそのまま発信側の帯域制限になる。**ただしこれは受信側を守らない** — 人気者が何十人からも参照されるハブ化は防げないので、`cmd/experiment` に**被参照数の分布**を足し、偏りを実測してから相互占有を検討する。先に機構を入れない、という #10 と同じ構え。
  - **伝承を一方向の「学ぶ」ではなく双方向・同時の等価交換にした。** 学習対象ごとに値が違えば互いに歩み寄り、**好意の加点を実際に動いた量に比例させる**。この形にすると「すでに値が近い相手との交換はほぼ無価値」が判定を書かずに自動で出る。副次効果として、一度学び尽くした相手は交換価値を失うので、特定個体への集中が自然に緩む。ハブ化対策としても働くが、これは**見込みであって測定ではない**ので、被参照数の分布で確かめる。
  - **接触だけで減衰の起点をリセットすることにした。** 減衰式（`exp(−減衰率×経過tick)`）は変えず、「経過tick」の起点の更新条件だけを広げる。今は値が動いたときだけ更新されるが、**等価交換のチェックが走った時点で、差がゼロで不成立でも更新する**。新しいトリガーが要らないのが良いところ。**好意とリスクの両方に適用**したのは、一度痛い目に遭った相手を見かけるたびに警戒が新鮮に保たれるほうが自然だから。結果として記憶の寿命が距離で決まるようになり、家族・配偶者・同じ集団のメンバーの記憶はほとんど減衰せず、遠方の相手は素直に消える。
  - **距離制限と拡散は別問題だと明記した。** 伝承も好意も `PerceptionRadius` / `GrabRadius` で元々近距離限定だが、近距離のイベントが連鎖すれば時間をかけて世界全体が1つの文化に均質化しうる。**距離を絞るのは拡散を遅らせる効果しかなく、止める効果はない。** 止めるのは記憶容量の有限化（段階9）と、突然変異・出生による地域ごとの多様性の継続的な注入。2026-08-19 に「空間局在が無いと文化は1つに収束する」と書いたが、局在があっても収束しうる、が正しい。
  - **伝承の中身を固定パラメータから「ヒント」に広げた（今回の一番大きな追加）。** `retaliation` のような固定名のスカラーだけを伝えると、伝わる内容の上限を我々が先に決めてしまう。代わりに〈状況特徴量 × 対象行動 × 重み〉の3つ組を上限本数だけ持たせ、内容そのものを進化させる。**必須条件は「ヒントが行動を直接決めないこと」** — 既存の効用比較への加点／減点としてのみ働かせる。ここを外すと「ハードコードされた行動閾値を作らない」原則が崩れ、`Utility.Total()` が効用式の唯一の実装だという今の強み（トレースが式とズレようがない）も失われる。トレースにヒント由来の加点を1行足して透明性を保つ。
  - **ヒント枠は遺伝子予算を食わせる。** 枠がただなら全員が上限まで持ち、予算制にした意味が消える（記憶容量を有限にしたのと同じ理由）。副作用として能力の水準が下がるので、段階12 の測定は**枠 0 本の条件と対にして**読む必要がある。段階7c の較正時に、後からヒント枠が予算を食うことを見込んでおく。
  - **ヒントの構造の変異には既存の天才イベントを流用する。** 重みの微調整は通常の突然変異、状況特徴量と対象行動そのものの変化は天才。`CLAUDE.md` の「天才＝学習パラメータの1つを大きく飛躍させる」を数値から構造へ広げるだけで、新しい概念を増やさずに済む。実装は学習分類子システムに近い構造になるが、**新しい呼び方は導入しない**（既存の「学習パラメータ」の延長として文書化する）。
  - 未決のまま残したもの: ヒント枠の上限本数と1枠あたりの予算消費量、等価交換の歩み寄り率と好意への換算係数、相互占有を実際に入れるか（被参照数の実測待ち）、「接触」の定義（視界内の遭遇か `observe` の交換チェックか）、血縁ブーストが世代をまたいで薄まるか。いずれも `PLAN.md` の「未決」節にある。
- 2026-08-30（追記）: **継承の分配方式を「遺伝子ごとの独立分配（メンデル的独立分配）」で確定した**（`PLAN.md` の決定事項#4。同日まで未決だった項目）。性染色体にすると能力が性で偏る別の世界になり、「配分に対する選択圧を測る」という予算制の目的に対して交絡が1つ増える。連鎖群（遺伝子をまとめて引く）も作らない — 連鎖があると遺伝子どうしが相関し、どの遺伝子への圧なのかが読めなくなる。実装は「遺伝子ごとに 50% でどちらの親から引くか決める」だけで済む。
- 2026-08-30（追記2）: **予算（遺伝子の合計値）を親から継ぐ形に変更した**（`PLAN.md` の決定事項#2・#3。2026-08-18 に決めた「出生ごとに抽選＝予算は遺伝しない」からの変更）。子の予算＝**親の予算 ± 一定幅の乱数**（`BudgetInheritSpread`）、初期個体だけ `GeneBudgetMean` ± `GeneBudgetStd` で抽選する。

  - 変更の効果は「恵まれた血統」が世界に現れること。予算が遺伝しないと、天才は常に1代限りの物語で終わり、親の恵まれ方が子孫に何も残らない。予算を継がせると「代々恵まれた家系」と「代々乏しい家系」が生まれ、プレイヤーが血統を乗り継ぐゲームの題材としても筋がいい。
  - **一方で、2026-08-18 に予算を遺伝させなかった理由はそのまま残る。** 予算には価格が無く、大きいほど無条件に有利なので、選択は常に上向きに働く。親の値に上下等しい乱数を足す形は復元力の無いランダムウォークなので、選択と合わさると**平均予算は上へドリフトし、最後は全遺伝子が上限100に張り付いて「配分」という概念が消えうる**。これは予算制の目的（配分への選択圧を測ること）が失われる状態。
  - **そこで、価格を先に入れるのではなく、測ってから入れることにした。** 段階7c の完了条件に「世代ごとの平均予算の推移」と「`BudgetHeritability` 0 と 1 の同一シード対比較」を追加し、単調に上がり続けるなら価格を戻す。戻し方の候補は (a) 予算に比例する維持コスト（基礎代謝）、(b) 親の予算と `GeneBudgetMean` の中間から引く＝平均への回帰（`BudgetHeritability` を 0〜1 の連続値にすればこの1本で表現できる）、(c) クリップのみ（張り付くので非推奨）。**先に価格を入れると、ドリフトが実際に起きるのかどうかが分からないまま2つのルールを同時に入れることになる。**
  - **`BudgetHeritability` は捨てずに残す。** 旧仕様（0＝毎回抽選）と新仕様（1＝親から継ぐ）を同じバイナリで回せないと、「予算が遺伝することで何が変わったか」を同一シードの対で測れない。「ルールの切替は engine の分岐ではなく `Config` の書き換えで表現する」の適用。
  - 副次的な影響: 能力の絶対値と予算に対する比率の乖離が前より大きくなる（水準そのものが世代とともに動くため）ので、`cmd/experiment` を比率で測ることの重要性が上がった。また「稀な天才は物語であって進化の駆動力ではない」という 2026-08-18 の整理は、天才の予算が子孫に残るぶん半分だけ効かなくなる（1個体の物語が恵まれた血統になりうる）。
  - 未決のまま残したもの: `BudgetInheritSpread` の値と分布（一様か正規か）、両親のどちらの予算から引くか（現在は遺伝子と同じく独立に片方から引く前提。両親の平均にすると分散が毎世代半分になり、混合遺伝をやめた 7b の判断と逆向きになる）、価格を戻すかどうか（段階7c の測定待ち）。
- 2026-08-30（追記3）: **子がどちらの親の予算を引くかは 50% のランダム**（両親の平均は取らない）で確定した（`PLAN.md` の決定事項#2）。遺伝子の独立分配（#4）と同じ引き方に揃うので、予算だけ別の継承則を持たない。両親の平均にしなかったのは分散のため — 平均は毎世代ごとに予算の分散を半分にするので、混合遺伝をやめた 7b の判断（粒子的遺伝で分散を維持する）と逆向きになる。ランダムに片方から引けば、`BudgetInheritSpread` の乱数と合わせて予算の多様性が世代をまたいで保たれる。
- 2026-08-30（追記4）: **初期個体の配分はディリクレ分布（α ≈ 0.5〜1）で引くことにした**（`PLAN.md` の決定事項#24。一様乱数の正規化は不採用）。理由はユーザの指摘どおりで、一様乱数を正規化する方式は「あらゆる配分パターンが等確率」にはならず、**1本の遺伝子に予算が集中するような極端な配分が出にくい方向にバイアスがかかる**（i.i.d. の一様乱数を正規化すると分布が中央に寄る。ちなみにディリクレ α=1（単体上の一様）が欲しい場合は、一様ではなく指数乱数を正規化する）。狙っているのは「多少の偏りも生まれる初期集団」なので、偏りの強さを α ひとつで直接いじれるディリクレのほうが適している。α の値は `cmd/experiment` で振って決める。

  - 実装上の注意: 標準ライブラリにガンマ分布のサンプラが無いので自前で書く（外部依存を増やさない方針）。Marsaglia–Tsang 法は `NormFloat64` と `Float64` だけで書けて、α<1 は `U^(1/α)` のブーストで対応できる。乱数は `World` が持つ `*rand.Rand` 1本を使う（同じシードで同じ結果、という規約）。
  - もう1点: 遺伝子1本の範囲が 1〜100 なので、α が小さいほど出る極端な配分はクリップに当たる。**クリップ後に再正規化して合計を予算に合わせないと、「予算に対する比率」で選択圧を測る前提が狂う**。
- 2026-08-30（追記5）: **親の予算に足す乱数は正規乱数で確定**（`BudgetInheritSpread` はその標準偏差。`PLAN.md` の決定事項#2）。一様乱数だと幅の外にはみ出す変化が原理的に起きないので、「稀に大きく飛ぶ」が表現できない — 継承則を混合遺伝から染色体式＋正規乱数へ変えた 7b の判断（2026-08-18）と同じ理由で、遺伝子側と予算側で乱数の性格を揃えた。値は出発点として `GeneBudgetStd` と同じ 30 を置き、`cmd/experiment` で詰める。**血統ごとの予算のばらつきは世代数 n に対しておよそ `σ√n` で広がる**（σ=30 なら10世代で ±95 程度）ので、ここが広すぎると配分への選択圧より先に水準が動いてしまい、予算制で測りたかったものが読めなくなる。段階7c の「平均予算の推移」の測定は、この値の較正にもそのまま使う。
- 2026-08-30（追記6）: **`TicksPerYear` を現実の秒数（365×24×3600）から決めない方針を明記し、暫定値を数百程度（200〜1000、出発点500）とした**（`PLAN.md` の未決節と帰結）。このエンジンの tick は現実の秒と対応していない。既存の tick 基準の値（`SkirmishTicks = 30` / `PairBondDuration = 150` / `PlanHorizon = 700` / `cmd/experiment` の標準実行長 20000〜60000tick）はすべて数十〜数万のオーダーに収まっており、秒換算だと1年が3000万tickになって、**「10年に一度」の天才が実験中に一度も発火しない**（実行長の何千倍の間隔になる）。目安は「100年に一度が実験の実行長と同程度かやや大きい」で、500 なら 10年＝5000tick、100年＝50000tick と、既存のスケールにも実験の実行長にも収まる。

  - **この値は暫定のプレースホルダーとして扱う。** 将来「成長・老い・自然死」（`CLAUDE.md` の将来実装）を入れた時点で、死亡時の `Age` の分布が測れるようになるので、それが現実的な人間の寿命（数十年）に相当するよう `TicksPerYear` を逆算し直す。それまでは、この値に依存する結論（「天才は世代あたり何人現れるのが自然か」など）を出さない。`CLAUDE.md` の老いの項にも、実装時に決め直すことを書き添えた。
- 2026-08-30（追記7）: **魅力と実能力は無相関のままにすることで確定した**（`PLAN.md` の決定事項#25。同日まで未決だった項目）。ハンディキャップ原理（強い個体しか高い魅力を維持できない＝魅力と実能力を engine 側で相関させる）は**今は導入しない**。理由は「無相関でも偏らないと思うから」ではなく、2026-08-19 に魅力を遺伝子にしたときの当初の狙い（#12）がそのまま有効だから — engine が相関を決め打ちすると、「見た目から能力が読めるのか＝装飾は嘘をつけるのか」という**段階10 の学習に答えさせたい問いの答えを engine が先に書いてしまう**。これは決定事項#9（見た目からの推定は各ノードが学習する）および#10（協力を明示的に実装しない）と同じ形の判断。

  - **代わりに、性選択圧は `CompetitionWeight` と同格の危険パラメータとして扱う。** engine 側にブレーキを置かないと決めた以上、暴走の検出は測定に頼るしかない。段階7c の実装後に `cmd/experiment` で**魅力への選択圧（配分比の変化量＝`dAttractiveness` 的な指標）と個体数を必ず確認する**。
  - **切替の条件も先に決めておく。** 魅力への配分が暴走して他の遺伝子を圧迫する、または個体数が崩れる場合は、そこで初めて (a) ハンディキャップ原理側の設計に切り替える か (b) 魅力の効用への効き方（`pAccept` の重み）を弱める、のどちらかを検討する。**先に入れず、測ってから入れる**という予算のドリフト（追記2）と同じ進め方。
- 2026-08-30（追記8）: **好意の記憶容量の食い方を「相手ごとに1レコード＝1枠、好意はそのレコードのフィールド」で確定した**（`PLAN.md` の決定事項#26。同日まで未決だった項目）。容量の単位は既存の「意見」レコード（`Opinions(id)` が返す、リスク記憶＋強さ推定を持つレコード）で、好意はそこに新しいフィールドとして乗る。**その相手を覚えていること自体が1枠を消費し、脅威として覚えても味方として覚えても消費は同じ1つ。** 容量は既存の9遺伝子の記憶容量でそのまま管理し、好意専用の遺伝子もパラメータも足さない。

  - この設計の一番の効果は、**1ノードが同時に好意を持てる人数＝集団の実質的な最大メンバー数が、記憶容量の遺伝子値そのもので決まる**こと。集団サイズという創発量に、既存の遺伝子1本という明示的なつまみが対応するので、A/B で容量を振ればそのまま集団サイズを振れる。
  - **副産物として「脅威を多く覚えている個体ほど味方を覚える枠が削られる」というトレードオフが自動的に出る。** そのために新しいルールを書かないのが良いところで、「能力の役割を重ねない」原則にも沿う — 記憶容量という1つの能力の中で敵味方の情報が競合するだけなので、選択圧の軸は増えない。
  - **好意と脅威を別々の容量で管理する案（別枠案）は不採用。** 遺伝子とパラメータが増え、測定も「どちらの容量への選択圧か」を分けて読む必要が出て複雑になる。決定事項#18（敵意を新設せず既存のリスク記憶を使う）と同じ、状態を増やさない方向の判断。
  - **切替の条件は先に決めておく。** 実測で集団サイズが明らかに不自然（小さすぎる／大きすぎる）と分かった場合にだけ、そこで初めて分離を検討する。予算のドリフト（追記2）や性選択の暴走（追記7）と同じ「先に入れず、測ってから入れる」の進め方。
- 2026-08-30（追記9）: **血縁ブーストの範囲を「直系の親子（1ホップ）のみ・世代をまたぐ減衰ルールは作らない」で確定した**（`PLAN.md` の決定事項#27。同日まで未決だった項目）。`ParentIDs` / `ChildIDs` を1ホップだけ辿って親子の双方に初期好意を置き、祖父母・孫・きょうだいは血縁として扱わない。それらの関係は他の発生源（共同戦闘・継承／模倣・接触による起点リセット）に委ねる。

  - **決め手は「新しい状態も新しいパラメータも増やさない」こと。** 世代をまたぐ減衰を入れると、血統を何ホップも辿る処理とホップ数ごとの減衰係数が必要になり、どちらも「何ホップまで辿るか」という恣意的な打ち切りを我々が決めることになる。1ホップなら `ParentIDs` / `ChildIDs` の使い道は「親子に初期好意を置く」1箇所で終わる。決定事項#18（敵意を新設しない）・#26（好意に専用枠を作らない）と同じ方向の判断。
  - **近縁者が結びつくかどうかは、実イベントに答えさせる。** 祖父母と孫が結びつくなら、それは育児期間ぶん近くにいて接触・模倣が多く走るからで、engine が血統距離を計算したからではない。血縁係数を engine が先に書くと、「血のつながりは世界にどれだけ効くのか」という問い自体が消える — #10（協力を明示的に実装しない）・#25（魅力と実能力を相関させない）と同じ構え。
  - **ブーストの大きさは初期値を仮置きし、`cmd/experiment` で調整する。** `Config` のフィールドにして 0 で切れる形にする（好意の A/B に使う）。大きすぎると家族だけで集団が閉じて集団サイズが記憶容量より先に頭打ちになり、小さすぎると種が育つ前に減衰する。段階9 の完了条件の測定でこの両側を見る。
- 2026-08-30（追記10）: **方針転換 — 成長・老い・自然死を「将来実装」から前倒しし、段階7d（育児期間）と同じ段階で実装することにした。あわせて `TicksPerYear` を暫定プレースホルダーから本決めの値に切り替える**（`PLAN.md` の決定事項#28〜#31、段階7d）。`CLAUDE.md` では「入れるときは遺伝的素質 × 年齢補正の形にし、年齢補正を当面つねに1にしておく」として先送りしていたが、**育児期間の設計に成長の概念が不可欠**だと分かったため前倒しする（「子はまだ弱い」が書けないと、親が近くにいて守ることに意味が無い）。実装の形は当初方針どおり「遺伝的素質 × 年齢補正」のままで、**成長（0→1）と老い（1→減衰）は同じ1本の関数の両端**として扱う（実効値を決める場所を2つにしない）。

  - **子育て中の食糧確保は「保護」だけにし、給餌は作らない（#29）。** 親が子に食料を渡す新しい仕組みを入れず、子は自分で食料を得ようとするが弱い、親は近くにいて**既存の戦闘・先制排除で競合相手を排除する**、という間接支援だけにする。新しい行動もルールも増えない。**足りるかどうかは実装後に子の餓死率と自立到達率で測り、不足するようならそのとき初めて給餌を検討する** — #10・#25・#27 と同じ「先に機構を入れず、測ってから入れる」構え。
  - **成長は既存の `eat` に相乗りさせる（#30）。** 「成長する」という新しいアクションは作らない。食料摂取で年齢補正の成長側が進み、**満腹（空腹度0＝100%）で頭打ち**になる。
  - **寿命は過小・過度の両側で削れ、閾値を意図的にずらす（#31。今回の設計の要）。** 過小＝空腹の継続で、既存の体力減に加えて寿命も削る。過度＝**満腹ではなく「腹八分目」（80%程度）を超えた時点**から寿命を削り始める。**成長の頭打ち（100%）と過度コストの開始点（80%）が別の値であることから、「成長中の個体は多少寿命を削ってでも満腹近くまで食べるほうが得、成長を終えた個体は腹八分目で止めるほうが得」という年齢による行動差が、特別な場合分けを一切書かずに効用比較から出る。** 「ハードコードされた行動閾値を作らない」原則を守ったまま、年齢による行動差を世界に出せるのがこの形の利点。A/B では2つの閾値を同じ値にした条件と対にして、行動差がこの差分から来ていることを示す。
  - **「満腹に近いのになぜ食べるのか」は新しい判断閾値ではなく既存の `rival` で説明する（#31）。** 食べると寿命が減るだけなら満腹手前で止まるが、`rival`（その食料が競合相手に渡ると将来自分が損をする）が正の側にあるので、**寿命コストを払ってでも先に食べる**が候補として残る。ここに項を足さないこと。
  - **理論的な位置づけは disposable soma 説の簡易近似。** 有限の資源を成長・繁殖に回すか維持・修復に回すかの配分の結果として老化が生じる、という老化の進化理論の1つ。**今回は食事量だけを見るいちばん単純な形にとどめる**が、求愛・戦闘などの激しい活動も寿命消費に効かせる拡張余地を骨格として残す（**今回は実装しない**。寿命という状態さえ入っていれば、後から項を足すだけで足せる）。
  - **`TicksPerYear` は「老い実装待ちの暫定値」ではなくなった（#28）。** 待つ相手を前倒ししたので、段階7d で本決めにする。決め方は3条件で挟む: (1) 大天才（100年に一度）が実験の実行長と同程度かやや大きい、(2) **自立期間3年＋想定生涯が、標準実行長（20000〜60000tick）の中で複数世代分観測できる**、(3) **老いが実行時間内に一度も発火しない／老化死が支配的で他の死因が見えなくなる、のどちらにも転ばない**。段階7c（天才の発生間隔）と段階7d（寿命）は同じ1つの値を使うので、7c では出発点の 500 を置き、**7d で確定したら 7c の測定をやり直す**。2026-08-30（追記6）で書いた「秒換算（365×24×3600）を根拠にしない」はそのまま有効。
  - **副作用: 死因が3つになるのでベースラインが動く。** 今は餓死と戦闘の2つ（`NODE.md`）。老化死が加わると比率が変わり、段階7c までに取った測定値と直接は比べられなくなるので、段階7d の前後で同一シードの対を取り直す。
  - 未決のまま残したもの: 年齢補正カーブの形と係数、過度・過小それぞれの寿命消費の係数、想定生涯（3年の何倍か）、親側の負担（移動の制約・体力コスト）を入れるか、子の生存が親の効用にどう入るか（`OffspringValue` を通すか育児期間の共在だけで済ませるか）。いずれも段階7d の実装時に粗い値を置き、`cmd/experiment` で詰める。
- 2026-08-30（追記11）: **休息を無防備にすることにした**（`PLAN.md` の決定事項#32、段階9）。今の `ActRest` は `Utility` に `Life` の項しか持たず**リスクがゼロ**なので、どこで誰の隣で休んでも同じ点数になる。ここに「**対象の近くにいる、好意を持っていない他個体の存在**」によるリスクを足し、**好意のある個体が近くにいる場合は乗せない（または割り引く）**。

  - **新しい状態も新しい強さ評価器も追加しない。** 好意は #26 で決めた既存の相手ごとの記憶レコード（強さ推定・リスク記憶と同じ枠）の**フィールドを参照するだけ**で、リスクの大きさも既存の `EstStrength`・リスク記憶・距離から作る。#18（敵意を新設しない）・#26（好意に専用枠を作らない）と同じ、状態を増やさない方向。
  - **既存の走査に相乗りさせ、追加の探索コストを出さない。** 現在は `addRest` の中の `incoming(p)` が `p.Others` を1度なめ、そのあと `addAgents` が同じスライスをなめている。**`Decide` の先頭で1度だけ走査して「被攻撃ダメージ」と「よそ者の露出量」を同時に作る**形にまとめる。`BenchmarkDecide`（現在 約8.0µs）を前後で取り、**走査が1本増えていたら実装が間違っている**という見方をする。
  - **これは正の項ではなく、よそ者に対する負の項。** 好意のある相手が近くにいても**単独でいるのがいちばん安いまま**（リスクを乗せない／割り引くだけで、マイナスにはしない）。したがって集団化の主エンジンは**段階11（共同戦闘）と段階12（伝承）のまま**で、#32 が足すのは「よそ者から離れて休む」という**分離の側**の力。集団ができたことの説明に #32 を使わない、という読み方の注意も `PLAN.md` に書いた。ただし**好意が空間分布に効く最初の経路**ではあるので、段階9 で好意が効用に触れるのはここだけになる（正の項は段階12 と対にする、という 2026-08-19 の整理は維持）。
  - **原則「回復経路を必ず残す」に触れる変更なので、重み 0 で完全に切れる形にする。** 休息は満腹時の唯一の回復手段なので、リスクが強すぎると誰も休めなくなり世界が単調に消耗する。段階9 の完了条件に、重み 0 との同一シード対で**餓死率・個体数・平均体力**を見る項目を足した。
  - 今回の対象は `ActRest` だけ。`eat` や `observe` にも同じ無防備さを広げるかは、休息での効き方を見てから決める。
- 2026-08-31（追記12）: **数手先の見積もりは、ミニマックス的な探索木ではなく「閉じた式」と「ヒント」の役割分担で行うことにした**（`PLAN.md` の決定事項#34・#35、段階12）。将棋のような分岐探索（候補ごとに数手先まで枝を広げる方式）は不採用。理由は2つ：候補数（現状約40）が手数ごとに掛け算で増えて `BenchmarkDecide`（現状約8.0µs）が桁違いに重くなること、そしてこの世界には相手も適応的に動く・相手の真の能力は隠しパラメータという二重の不確実性があり、深い事前計画より**トリガー方式による頻繁な考え直し**のほうが動的な多主体環境では有効なことが多いこと。

  - **役割分担の基準は「設計者が式を書き下せるかどうか」。** (a) 書き下せる場面は、既存の `PlanHorizon`（餓死カウントダウン）／`ShockRisk`／`SkirmishTicks`／`fleeExposureTicks`／`recoverable` と同じパターン（分岐せず「今の状態がこのまま続いたら数tick先どうなるか」を1本の軌道として見積もる）を、新しい場面（休息の無防備化の被害見積もり、育児中に子から離れるリスク、好意のない他個体への接近リスクなど）に広げる。(b) 書き下せない・気づいていない関係は、ヒント（決定事項#23）に発見させる。両方とも同じ `Utility.Total()` に加算される項として実装し、アーキテクチャは分けない。
  - **寿命はこの見積もりの対象に含めない。** ノードは寿命を知覚せず、効用式にも寿命の項を作らないという既存方針（#33 でも寿命コストを意思決定に一切関与させなかったのと同じ構え）を優先する。
  - **ヒントの評価は個体ではなく世代規模で行われる、という点が (b) の要。** ヒントは実行時には状況特徴量から行動への単純な参照（O(1)）でしかなく、そのヒントが良いかどうかの評価は個体1回の決定ではなく世代を超えた集団規模の生存競争そのものが行う。探索のコストを「今この瞬間の1個体」から「時間をかけた進化」に移す設計になっている。
  - **導入順序は非対称。** (a) は各機能を実装するたびに一緒に追加できるが、(b) は段階12 のヒント制度自体が入るまで効果が出ない。段階12 までは (a) だけが唯一の手段になる。
  - **深さ・ヒント枠を能力でゲートしない、と明記した（#35）。** 2026-08-14 の測定で `StrategyDepthUnlock`（知能が高いほど戦略の選択肢が広がるゲート）が逆効果（愚かな個体を損な手から保護してしまい、知能への選択圧をむしろ下げた）と分かり既定で無効化した経緯があり、#34 の実装でも同じ罠を繰り返さないことを決定事項として残した。
  - **ヒントの多様性は定量化してはじめて意味を持つ。** 段階12 の完了条件に、地域ごとの言い伝えの分布だけでなく、**集団内で実際に使われているヒントの種類数、またはエントロピーのような指標**と、**それぞれのヒントが実際に生存率に効いているか**の確認を足した。この測定は、好意・集団・外敵による世界の地域差が十分ついてから行うほうが良い結果が期待できる。
- 2026-08-31（追記13）: **段階7d のうち寿命消費（過小・過度のルール）だけを、年齢補正・成長・育児期間より先に実装した。** 明示の制約は、寿命の残量・変化を `Perception` に一切含めないこと、効用式にも新しいゴール項を足さないこと、寿命消費は完全にバックグラウンドの機械的処理であること、ノードは自分の寿命を知らず死因としての老化も理解しないこと、選択圧は世代を超えた差次的生存だけを通じてかかること。この5点はいずれも既存の「相手の真の能力は隠しパラメータ」「ハードコードされた行動閾値を作らない」原則の**寿命版**にあたるので、他の原則と同じ強さで扱った。**#33（慢性的な低体力コスト）とは別物**——#33 は体力側の慢性コストで追記12より前に決定だけ済んでいたが、今回実装したのは空腹側（過小・過度）の消費で、#33 はまだ未実装のまま `PLAN.md` に残っている。

  - **なぜ寿命消費だけを切り出したか。** 段階7d は年齢補正（成長と老いを1本の関数の両端にする）・成長（`eat` に相乗り）・寿命（過小・過度）・育児期間の4つがセットになっているが、寿命消費だけは他の3つに依存せずに実装できる。年齢補正は成長カーブという別の未決事項を抱え、育児期間は空間分布を直接動かすので単独で測る必要がある（`PLAN.md` の完了条件）。寿命消費を単独で切り出せば、他の3つを後回しにしても death cause が1つ増えるだけで済み、A/B の対も取りやすい。
  - **`OverfedHunger`（腹八分目の閾値）は成長の頭打ち（満腹＝空腹度0）とは独立に決めた。** `PLAN.md` の #31 は「2つの閾値をずらすことで、成長中は満腹近くまで食べ、成長後は腹八分目で止める、という行動差を場合分けなしに出す」ことを狙っているが、これは成長（＝年齢補正）が実装されて初めて意味を持つ対比で、今回はまだ成立しない。**`OverfedHunger` を `SatiatedHunger`（40）より下の20に置いたのは、成長が無い今の世界でも「そこそこ食べる分には寿命が減らない自由な帯」を残すため**で、将来成長を足したときにこの帯がそのまま使える設計にしてある。
  - **既定値（`MaxLifespan = 6000`、両rateとも0.2）を計測したところ、標準実行長（20000〜60000tick）では `aged` が0だった。** 生存者の大半は寿命を使い切るより先に餓死・戦闘で死ぬため。これは「バグ」ではなく「まだ調整していない」状態だと判断し、`MaxLifespan` を無理に下げて既定挙動を変えることはしなかった——寿命消費はまだ年齢補正も `TicksPerYear` も無い状態での先行実装であり、`PLAN.md` の未決事項にあるとおり `TicksPerYear` の本決めは段階7d の残り（年齢補正・成長・育児期間）と合わせて行う。**代わりに `cmd/experiment` に `brittlelifespan`（`MaxLifespan` を1500に縮める）という検証用の条件を足し、実際に Step() のループを通して寿命消費が発火し、個体数を大きく崩さずに死因の一部になることだけを確認した**（8シード×20000tickで死因の約4%、個体数・世代数は baseline と有意差なし）。値そのもののチューニングは、年齢補正・`TicksPerYear` が決まってからまとめて行う（先に価格を入れず、測ってから入れる、という予算制のドリフト対応（追記2・7・8・9）と同じ進め方）。
  - **`Agent.Lifespan` を `Vitality` と対称に扱った。** `World.addAgent` には元々 `Vitality <= 0` を `MaxVitality` に補う安全網があり、これは生の `Agent{}` リテラルから直接ノードを作るテストが大量にあるため（ゼロ値のままだと即死してしまう）。`Lifespan` にも同じ理由で同じ形の安全網を足した。これを入れないと、既存の全テスト（`Lifespan` を明示的に設定していない）が寿命消費導入の初回 `metabolise()` で即座に殺されてしまうところだった。

- 2026-08-31（追記14）: **クラスタの連結距離を 30 に決め、`Config` ではなく package const で持つことにした**（`engine.DefaultClusterLinkDist`）。`Config` に入れなかったのは、これが世界の振る舞いを何も変えない「測る側の目盛り」だからで、`Config` を「世界のルールの一覧」として保つ既存の方針を崩したくなかった。定数にしたのは比較可能性のため — **連結距離が違う測定値どうしは比べられない**ので、実行ごとに動かせる形にすると A/B の前提が崩れる。
  - **値は測ってから決めた。** 当初は `PerceptionRadius`（130）を使うつもりだったが（`Spacing()` が視界半径を使っている前例に引きずられた）、既定の個体数では1個体あたり視界内に約19体いて、単連結の**パーコレーション閾値（2次元で平均近傍数 ≈ 4.5）を大きく超える**。実測すると連結距離130では最大成分が個体数の98%を占め、「集団が1つある」としか言わない使い物にならない目盛りだった。
  - 8シード × 50000tick の末尾10000tickで連結距離を振った実測（個体数 ≈ 144）:

    | 連結距離 | 集団数 | 平均サイズ | 集団に属する割合 | 最大成分の割合 |
    | --- | --- | --- | --- | --- |
    | 15 | 24.05 | 2.71 | 0.450 | 0.046 |
    | 30 | 25.65 | 4.42 | 0.774 | 0.115 |
    | 40 | 20.43 | 6.43 | 0.880 | 0.196 |
    | 60 | 9.72 | 15.19 | 0.967 | 0.407 |
    | 130 | 1.24 | 129.51 | 0.999 | 0.984 |

  - 30 を選んだ理由は3つ。(1) 集団数のピーク付近で、集団化が進んだときも緩んだときも数字が動く余地がある。(2) 最大成分がまだ個体数の1割程度で、パーコレーションまで十分な余裕がある（40 だと 0.20、60 だと 0.41 で、上向きの変化が飽和しやすい）。(3) `CombatRadius`（15）のちょうど2倍で、「互いの手が届く距離にいる」という世界側の意味を持たせられる（恣意的な数字にしない）。
  - **`largestShare` を指標に含めたのは、この目盛り自体の信頼性チェックとして。** 単連結は数珠つなぎになるので、`largestShare` が1に近づいたら `clusters` や `clusterSize` は「集団ができた」ではなく「世界が1つの塊につながった」を意味する。他の3指標を読む前にこれを見る、という順序を `README.md` にも書いた。
  - 個体数依存は残る（同じ配置でも密な世界ほどつながる）。これは `Spacing.Clumping` が既に抱えている性質と同じで、同じ世界サイズの実行どうしを比べる、という同じ注意書きで扱う。

- 2026-08-31（追記15）: **membership half-life を「クラスタの追跡」ではなく「組の共在の生存曲線」として測ることにした。** 素直に思いつくのは各クラスタに識別子を振って追いかける形だが、**クラスタは合体・分裂する**ので「同じクラスタ、のちほど」は合体をまたいだ時点で定義できない（2つが合わさったものは、どちらの継続なのか）。一方「この2体はまだ同じクラスタにいるか」は合体しようが分裂しようが常に答えられる。組で測ればクラスタの同一性を一度も問わずに済む。
  - **片方が死んだ組は「離れた」と数えない**（生存分析でいう打ち切り扱い）。数えてしまうと、集団の安定性ではなく**寿命**を測ることになる。段階7d で寿命消費が入り、段階11 で外敵が入ると死亡率そのものが動くので、ここを混ぜると以降の比較が全部使えなくなる。
  - **半減期は「窓の中で半分に落ちなかった」ことがありうる**ので、`Censored` と、常に定義される `together`（固定ラグでの生存率）を対にした。集団化が進んだ世界では半減期が窓を超える可能性が高く、そのとき片方の条件だけ数値が窓の端に張り付いて比較が壊れるのを防ぐため。`cmd/experiment` は打ち切り時に窓の端の値を出すが、それが下限値でしかないことは `censored` の列で分かる。
  - **予想が外れた記録**: 生存曲線の遅い成分（500tick後も14%が一緒にいる）は**ペア結合（`PairBondDuration = 150`）だろうと予想したが、逆だった。** `PairBondDuration` を1にすると半減期は 58.8 → 93.7 に**伸び**、遅い成分も厚くなった（500tickで 0.145 → 0.201）。個体数はほぼ同じ（146 → 154）だがクラスタ数が 25.9 → 21.5 に減っており、**結合したペアが連れ立って動くことで、そのペアが元いたクラスタの他の組を引き剥がしている**——つまりペア結合は共在を安定させるどころか壊す側にある、という読みが立つ。ただしこれは1つのノブを潰しただけの粗い切り分けで（`PairBondDuration` は出産の間隔や移動そのものも変える）、機構として確かめたわけではない。**遅い成分の正体は未解明のまま残す。**

- 2026-08-31（追記16）: **「集団内戦闘率 ÷ 集団間戦闘率」を、書かれたとおりの形では実装しなかった。** 打撃が届くのは `CombatRadius`（15）以内だけで、これはクラスタの連結距離（30）より短い。つまり**殴り合っている2体は、その瞬間つねに同じクラスタにいます**（単連結の定義から、30以内にいれば必ず同じクラスタになる）。実測でも20000tickの間にクラスタ内の打撃5994発に対しクラスタ間は0発でした。集団間の戦闘は「稀」なのではなく**構造上あり得ない**ので、この比は 5994 ÷ 0 になります。
  - **連結距離を `CombatRadius` より短くすれば測れる**が、それは目盛りを指標に合わせて曲げる行為で、しかも「集団」の定義が打撃の届く距離より狭くなり、集団と呼べるものではなくなる。採らなかった。
  - **代わりに、同じ遭遇を「いつからの相手か」で分けた。** `CombatRadius` 以内にいる組を、`DefaultCompanionLag`（200tick）前から同じクラスタにいたか否かで仲間・よそ者に分ける。基準 C が本当に訊いているのは「集団の中では争いが少ないか」＝**連れ立ってきた相手だと殴りにくいのか**であって、遠くて手の届かない相手と殴り合わないことではない。ラグを200にしたのは実測した共在の半減期（約60tick）の約3倍で、「ふつうのすれ違いを越えて一緒にいる組」を仲間と呼ぶため。
  - **両方を遭遇あたりの率にした。** 全ペアを母数にすると、よそ者のペアは数が桁違いに多いが大半は遠くて出会わないため、率が不当に小さくなり比が意味を失う。「出会ったときにどうするか」だけを比べる。
  - この置き換えで、**段階11 以降の判定は当初の意図どおりできる**（仲間との争いが減れば比は1を割る）。あるべき状態 C の文言は `PLAN.md` の表に残したまま、測り方の欄が実際の定義を指す形にした。

- 2026-09-01（追記17）: **種族ごとの個体数を「窓の平均」と「谷」で測ることにし、種族ラベル（`Agent.Species`）だけを段階11 より先に置いた。**
  - **ラベルを先に置いた理由。** 種族ごとに数える測定は、種族という区別が無いと形すら決められない。段階11 まで待つと、外敵を入れるのと同時に測り方を決めることになり、**比較対象になるはずの「外敵が入る前の値」が存在しないまま**になる。ラベル自体はルールを1つも変えない（ゼロ値が human、知覚に載せない、どの分岐も参照しない）ので、先に置くコストはほぼゼロで、得られるのは「同じ物差しで測った変更前の値」。`Lifespan` を意思決定に一切関与させずに先行実装したとき（追記13）と同じ構えです。
  - **窓で持つのは、共存が期間の性質だから。** 1tickの数え上げは「今この瞬間、両方いる」しか言わない。基準A が最初から「窓（例: 直近5000tick）の平均」と書かれているのは同じ理由で、指標の側でそれに合わせました。窓は基準A が挙げている5000tickをそのまま使っています。
  - **振れ幅ではなく谷を読む、と決めた。** 基準A は**振動を許容する**と明記しているので、振れ幅（`Swing`）が大きいこと自体は失格の理由になりません。一方、移入が無い世界では**0は吸収状態**——一度0に触れた種族は戻ってこない——なので、振動が問題になるのは谷が0に届いたときだけです。だから読むべき数字は「谷が平均の何割か」（`Trough`）で、振れ幅は「許容されている振動を見えるようにしておく」ための副次的な値として残しました。
  - **絶滅は窓ではなく実行全体で覚える。** 窓は5000tickしか見ないので、序盤に消えた種族は窓が通り過ぎた時点で痕跡ごと消え、**「絶滅した」ではなく「最初からいなかった」と区別がつかなくなります**。一度でも生きていた種族は消えた後も行として残し、`Extinct` と `ExtinctTick` を付ける形にしました。`Extinct`（今0か）と `ExtinctTick`（最初に0になったのはいつか）を2つの事実に分けてあるのは、将来の地域リセット（新規個体の投入）が入ると「絶滅したが戻ってきた」がありうるためです。
  - **種族の一覧をコードに持たせなかった。** `CensusTracker` は「今どんな種族がいるか」を知らず、ノードに実際に付いていたラベルを数えて必要なだけ伸びます。段階11 で種族が増えてもこのファイルは触らずに済み、**一度も現れなかった種族が「絶滅した種族」として現れる**という誤りも構造的に起きません。
  - **予想が外れた記録**: 指標が定数でないことの確認として食料量を振ったとき、**「食料を絞れば個体数の振動が荒くなり谷が深くなる」と予想して外しました。** 実際には個体数が4割減っても `rareTrough` はほぼ動かず（0.90 → 0.89）、逆に**食料を増やした側だけがはっきり平らになりました**（0.90 → 0.93 ***、`rareSwing` 0.18 → 0.14 ***）。振動の大きさは食料の絶対量ではなく比で決まっていて、応答は**豊かな側にだけ効く非対称**でした。段階11 で外敵を入れると人間側の食料事情も変わるので、「個体数が減った ＝ 振動が荒れた」と読まないための予備知識として残します。
