# 段階ごとの完了記録（`CLAUDE.md` の「実装ロードマップ」から切り出したもの）

**2026-09-19 に `CLAUDE.md` が 150,000 文字の上限に達したので、ここへ切り出した。文章は1文字も変えていない。**

**この文書が正**（`CLAUDE.md` の「実装ロードマップ」には現在地と方針だけが残っている）。**これから作るものは `PLAN.md`、次に何をやるかは `TODO.md`、測定値と理由は `HISTORY.md`。**

**測定のベースラインは2度リセットされている**——**段階7b（粒子的遺伝）** と **2026-09-13 の「先読みを既定にした」**。**`HISTORY.md` の 2026-09-13 より前の数値は「窓が1つ」の世界のもの**（`cmd/experiment` の `noahead` の腕と `cmd/devview -noahead` で再現できる）。

---

## 完了した段階（追記順）

**2026-09-18 に段階97（水の中では遅い）を完了した**（**ユーザの「水中の移動に違和感がある」から**。**既定はオフ**＝`WaterSpeedShare` = 1。**水は移動コストにしか効いておらず、速度は地形を読まなかった**ので、**水の中でも陸の 0.978 倍の速さで進んでいた**。**これは段階20 の「地形の倍率は速度ではなくコストに掛ける」を水についてだけ部分的に取り消したもので、理由の点検を先に書いた**——**(1)「effort と区別がつかない」は対照アーム `waterslowfree`（遅いが高くない水）が単独で効くことで解け**〈個体数 −23.65 \*\*\*〉、**(2)「速くて脆い／遅くて頑丈が並ぶ唯一の見込み」は段階20・33・57 が3度 0.00 を測って反証済み**。**取り消したのは禁止だけで「コストにも掛ける」は残した**（水は遅く、かつ高い）。**掛ける先は `speedNow` であって `MaxSpeed`（上限）ではない**＝段階57 の線は無傷。**実装前に数えた的はこの world で最大級**〈**実際に動いた距離の 14〜20% が水の中**、1回の滞在 115〜127 tick、**ただし水の中の身体が持っている泳ぎは 0.072〜0.119** ＝スキルの上限を先に言っていた〉。**測定（96シード）: 狙いは達成され、世界は悪くなる**〈0.75 で **−8.04 \***、0.5 で **−37.43 \*\*\***、0.25 で −97.24 \*\*\*、遊ぶ地図で −17.09 \*\*\*〉。**予想は7つ書いて3つ当たり・3つ外れ・1つ半分**。**一番大きい結果は「知ることは害」**〈知らない腕のほうが個体数 **+17.04 \*\***・出生 +44.77 \*\*。**居場所は1ミリも動かず**〈`onWater` −0.01〉**動くのは選ぶ手**〈水の中での求愛 −21%、休息 +52%〉＝**立ち止まることだけが遅くならないので相対的に勝ち、川の中で立ち止まることはこの世界で一番してはいけないこと**。段階12a の `retaliation`・#76 の拘束と同じ形の3例目で、**知ることが無差別ではなく有害だったのは2例目**〉。**そして `onWater` が倍になったのは選択ではなく算術**〈1回の滞在も 111.6 → 224.8 tick とちょうど倍で、**「知らない」腕でも 225.2 と同じ**＝**占有率は好みではなく脚が決めている**〉。**両岸（段階37）は無傷**〈`crossIndex` +0.15 \*\*\* だが **`crossDry` は +0.00 で4群すべて不動**・`bankBoth` −0.06 \*\* ＝**`crossIndex` は水の上の身体を数に入れるので、川の中の人口を倍にする規則はそれだけで動く**＝段階37 の「単独で読むな」が初めて実害の形で発火した〉。**スキルは効くが小さい**〈切ると −8.39 \* ＝代価の22%を返す。実効で返るのは用量の1割で、**数え上げからの予想どおり**。`swimWet` は不動＝**スキルは居場所を動かさない、の7度目**〉）。**2026-09-17 に「行動の拘束時間」（#76・段階番号なし）を完了した**（**既定は0**。**「採取には数tickかかる」を既存の `actionTicks` に1行足しただけ**＝呼びかけ・掲示・調理・執筆と同じ仕組みで、**新しい並行の機構はゼロ**。**時間は食べ物のところで使う**〈歩いている間はカウンタを0に held〉。**実装前の数え上げが的の小ささを先に言っていた**——**「採っている瞬間」は一生の 0.13%**〈食事の時間の 98.9% は歩いている時間〉、**そして食事中に殴られている割合は全体の3分の1以下**〈0.0051 対 0.0176＝身体は喧嘩から離れて食べている〉＝**計画が名指しした「拘束中は無防備」の的はもともと小さい**。**測定（96シード）: どの用量でも個体数を払う**〈用量8 で **−6.49 \***、40 で **−45.84 \*\*\***、餓死率 +0.85 \*\*\*、世代 −2.17 \*\*\*〉**が、この項目の答えは対照との直接の対のほう**——**知っていることは、小さい用量では害になり**〈`gather` − `gatherblind` ＝ 個体数 **−9.44 \***・人間 **−10.71 \*\***・`dIntelligence` −3.45 \*〉、**大きい用量では何の役にも立たない**〈+6.23 ± 9.59〉＝**得になることは一度も無い**。**機序は「代わりが無い費用を正確に値付けても、身体を解決から遠ざけることしかできない」**〈食事が高くなったと正しく知った身体は食事を休息・観察・攻撃と比べて低く見積もるが、そのどれも腹を満たさない〉＝**段階12a の `retaliation` とまったく同じ形**。**「効き目は全部選別で選択ではない」の3例目**〈34 溺死・86 寒さ〉**だが、知ることが無差別ではなく有害だったのは初めて**。**予想は5つ書いて3つ当たり**〈外したのは「休息が増える」——休息は平らで、時間が出てくるのは歩くことと戦うことから〉。**測定の途中で自分の変更を1つ直した**——**最初の実装は拾う（`ActTake`）の見積もりにも採取の時間を乗せていた**（**#76 が名指しで警告していたその混入**）。**スキルとの組み合わせはやらない**）。**2026-09-17 に段階96（好意への欲求）を完了し、P15 を通した**（**既定は0**。**好意1単位の値打ちは段階48 以来ずっと `LoreValue` = 9 の定額**で、**世界に誰もいない身体にとっても友人に囲まれた身体にとっても同じだった**。**足したのは逓減する上乗せ**〈`LoreValue + AllyValue/(1 + 視界にある信頼の合計)`＝**1人でも持てば満たされるので貯め込めない**＝#112(c) への答え〉。**実装前の数え上げで、当てにしていた理由のほうが先に落ちた**——**`rival` 項の「乏しさのゲート」は天井に張り付いている**〈`clamp(FoodScarcity,0,3)/3` は平坦で 0.9310・遊ぶ地図で 0.8661・**食料3倍でも 0.9088**、天井に張り付く決定が 79〜88%。`FoodScarcity` は「他個体数 ÷ 見えている食料」なので密度で調節されている〉＝**その裏返しを作っても定数にしかならない**ので **`ENEMY.md` 6章 E はここで「作らない」で閉じた**。**測定（96シード・理由つきと一律を用量2点で対にした）: 規則は強く発火し、理由は用量が上がるほど損になる**——**贈与は +131 → +197 → +415 \*\*\* と用量に単調**。**低い用量では理由のあるなしで区別がつかず**〈+131.06 対 +134.77〉**代価も無い**〈個体数 +4.03、`dIntelligence` +2.80 \*〉。**高い用量では、同じ平均を一律に配る腕のほうが贈与を多く作り**〈752.8 対 706.1〉**個体数の代価も** \*\*−3.71 対 −7.67 \*\*\* **`dIntelligence` の代価も小さい**〈−1.55 対 **−4.24 \*\***〉。**機序は測った**——**逓減の係数は「贈与が候補に挙がった決定」で 0.6318 と全決定の 0.7173 より 12% 低い**＝**渡せる相手が視界にいる身体は、すでに視界に信頼を持っている身体**（同じ人だから）。**一般則: 欲求に付ける理由が、その欲求を行使できる機会と負の相関を持つ状態を読むなら、同じ平均の強さで理由なしより少ない行動しか生まない**。**steer はできている**〈贈与が勝った決定の係数は 0.7901＝実際に贈っているのは孤独な身体〉**が総量が減る**。**買えたものは1つ**——**`halfLife` +22〜+32**〈63.9 → 85.9〜95.7。段階55a が狙って失敗した量で、段階52 の調理以来の大きさ〉。**この段階は数値の予想を実装前に書いていない**〈`PLAN.md` に 予想 節が無く、足さずに進めた＝作法の抜け〉）。**2026-09-17 に段階95（揺れの振幅そのものを形質にする）を完了した**（**既定は据え置き**＝振幅1・広がり0で1ビット同じ。**候補の採点の誤差の大きさは `(100 − 頭の良さ)/100 × ChoiceNoise` の1本で決まっていた**＝**1つの遺伝子が「2つの手を見分ける力」と「自分の順位から外れる幅」の両方を決めていた**ので、**「衝動的 vs 計算的」という軸が無かった**。**足したのは倍率1本**〈`lore` に1フィールド＝段階94 と同じ枠。**掛け算なので、全部の手を見分けられる身体には動かすものがない**〉。**実装前に数えた的はこの世界で最大級**〈**決定の 85.2% は最良と次点の差が自分の振幅より小さく、48.2% は10分の1より小さい**。**乱数を引き直すと勝者が 69.8% で変わる**〉。**答えは「水準は効き、ばらつきは効かない」**——**一律に 1.15 へ上げると個体数 −14.18 \*\*・出生 −40.66 \*\***、**0.93 へ下げると +4.30**、**ばらつかせる腕はどちらも誤差内**〈+3.80 / +4.60〉。**決定的なのは水準を揃えた対照**——**伝承を切った世界で、選択が見つけた 0.93 を全員に手で配る腕のほうが、ばらつく腕より良い**〈**+14.35 \*\* 対 +10.96 \***。**2つの腕は同じ 0.93 に着いている**ので**差はばらつきそのもの**〉＝**選択は水準を見つけただけ**。**選択の意見は「下げろ」で bet-hedging は選ばれなかった**が、**それが見えるのは伝承を切ったときだけ**〈既定では平均 1.00 のまま。**伝承が広がりを 0.15 → 0.05 に潰す**＝段階94 とまったく同じ比〉。**そして一番大きい発見は測り方についてのほう**——**`dIntelligence` と個体数が水準について逆符号で動く**〈1.15 で +4.27 \*\*／−14.18 \*\*、0.93 で −2.22 \*／+4.30〉＝**この世界で頭の良さがしている唯一の仕事がこの誤差を打ち消すことなので、`dIntelligence` は世界を騒がしくするだけで製造できる**。**`JudgementNoise` 側は作らない**）。**2026-09-16 に段階94（欲求という4本目の選好）を完了した**（**既定は広がり0＝乱数を1つも引かず、指紋テストが無変更で通る**。**効用式の「目的の側」に個体差が入る初めての規則**——**既存の選好3つ（`RiskWeight` / `CompetitionWeight` / `ShockRisk`）はどれも危険の読み方で、「この身体は子を強く欲しがる」を表現できなかった**。**置き場所は予算の外**〈体内時計の位相・味の位相と同じ立場〉で、**実装は `lore` に1フィールド**＝**継承・突然変異・伝承・保存・知覚が全部ただで付いてきた**。**測定（96シード）: 選択圧は出て、向きは上**——**集団平均 42.00 → 42.32 \*\***、**用量2倍で 42.75 \*\*\***、**伝承を切ると 44.37 \*\*\***＝**手で置いた定数は、選択が選ぶ値より低かった**〈段階12a の好み3つが動かなかったのとは別の結果＝**危険の読み方には正解が無いが、目的の重みには世界の側の答えがある**〉。**そして「鈍らせているのは選択ではなく伝承」が数字で言えた**〈初期の広がり 6.3 は実行末に 1.93＝3分の1まで潰れ、伝承を切ると 8.40 と初期より広がる〉。**代価はほぼゼロ**〈個体数 +4.83 ± 6.35〉**で、集団の持続が買えた**〈`halfLife` +24.48 \*、出生 +19.90 \*〉。**`SetDisposition`（段階23 の節目の性格）には入れない**——**あの3つは単位が同じだから合計を固定できるが、4本目は目的の重みなので、同じ財布に入れると「用心深さを売って子への欲を買う」という別のゲームの話になる**。**実装前に数えた的**: 求愛は決定の 48.3% で採点され 7.8% で勝つ〈負ける相手は観察33%・食事19%・攻撃8.8%〉、成人の 11.5% は身体的に繁殖できない）。**2026-09-16 に段階93（飢えを「締切」ではなく「速度」で読む）を完了した**（**既定は0**。**P14 の診断にこの群で唯一まっすぐ当たる段階で、狙った壁は消え、主指標は逆に動いた**——**`keepValue` が0だった読みの100%が正になる**〈満ちて無傷の身体の 27.7% がそれ〉**のに、売買は −0.86 \*\*\*、4分の1の用量でも −0.81 \*\*\* ＝用量の問題ではない**。**機序は段階79・81 が書いていたとおり**——**売り手の床も買い手の天井も同じ `keepValue` から出るので、勾配を立てると両側が同時に上がり、上がったぶんは「持ち続けること」に乗る**＝**「平らな勾配だから売らない」は成り立たなかった**。**形は水準より良い**〈個体数 −17.05 \*\* に対し、同じだけ一様に上げる `LifeValue` × 1.418 の対照は **−32.22 \*\*\***。`killShare`・`dIntelligence`・逃走の3つで符号が割れる＝段階67 と同じ形の2度目〉。**段階74 は速度の読みの下では損**〈切った腕のほうが代価が3分の1〉、**段階67 の「打撃を2窓目に持ち込まない」は変更していない**。**既定にすると20本のテストが落ち、うち2本は「壁が在ること」を固定していたもの＝狙いどおりだが、残りは求愛・ペア・売買**＝**生命の勾配を上げると定数で値付けされた子孫が負ける、の3度目**。**性能は 20.3 → 36.9µs**）。**2026-09-16 に段階92（痛い贈与と、血縁というもう1本の道）を完了した**（**既定はどちらも0**。**誰に渡すかがこの鎖で初めて動いた**——**血縁への贈与 0.06 → 0.17 \*\*\*、用量4倍で 0.33、平坦な世界では 0.60 \*\*\***。**規則は「血縁に渡せ」とは言っておらず、足したのは受け手の得だけ**。**実装前の数え上げで構造が1つ見つかった**——**`AffinityKin` = 22 は信頼の飽和点 `AffinityTrust` = 20 を超えているので `trustBought` は親子にゼロを返し、`addGive` はそこで抜けていた**＝**自分の子に渡すことは候補に挙がってすらいなかった**〈「95%が他人へ」は好みではなく式の形〉。**目盛りは実装中に決まった**——**手の中の食べ物はその身体にとって 18〜94、贈与が買う好意は最大 2.7 なので、価格の1〜2%でようやく釣り合う**〈段階84d・88 の「値段を付けると交換が消える」はこの引き算そのもの〉。**代価の大半は規則ではなく「値付けと現物のずれ」**——**採点しているのは手の中で一番安いもの、出ていくのは手の先頭**で、**`HandOverCheapest` で揃えると個体数の代価が消える**〈−2.70 → −0.39、渡された飾り物 +180.60 \*\*\*〉＝**既定にするなら3つ一組**。**予想は6つ書いて3つ当たり**〈外したのは「貧食料マップで個体数が改善する」——遊ぶ地図ではどの腕も払い、平坦でだけ代価が消える〉）。**2026-09-16 に段階91（飾り物の贈与アブレーションの読み直し・測定だけ）を完了した**（**P15 の先頭**。**実装前に数えた1項目が仮説の半分を先に否定した**——**`weightless()` は飾り物も返す**ので、**`CarrySlotsWeigh = true` の腕（段階82・84 の飾り物の腕すべて）では飾り物は手を1つも取らない**〈`handsTrinket` = 0.00〉。**そして測って分かった一番大きいことは、対照そのものが市場を閉じていたこと**——**`AffinityGift = 0` にすると `addOffer` が候補に挙がらず**〈発信の値打ちは `addGive` が置く `bestGiftGain`〉、**買えるのは掲げられているものだけ**〈段階51〉なので、**売買・断り・動いたコインが厳密に 0.00 になる**。**段階52 の `cookmean`・82 の `trinketsmean`・84 の `tastemean`・88 の腕がこれに当たり、個体数の結論は無傷だがあの腕の売買は「贈与の無い世界の商売」ではない**〈テストで固定〉。**仮説の骨格は当たっていた**——**無料の経路を閉じると自給は増える**〈作られた飾り物 +267〜+493 \*\*\*、`craftShare` +0.01 \*\*\*〉**が、増えるのは「作る」であって「買う」ではない**。**飾り物が本当に手を取る世界では飾り物そのものが作られず**〈1133 → 135〉**贈与を切っても何も動かない**）。**2026-09-15 に #116（コインの請求先を広げる）を完了した**（**段階81 の一歩の3つ目＝「天気を防ぐもの」への請求権**。**最初の実装は誰に対してもゼロ**——**`warmthValue` は今の勾配を読むが、満ちた身体の死亡確率は平ら**なので、**`keepValue` と同じ「その身体が困る瞬間」で値付ける形に直した**。**そして状態を掃いて答えが出た**: **枝は「1食の枝が既に正である身体」にしか払わない**〈満ちて無傷の身体では天気の枝も1食の枝も 0.0000〉＝**平らさは財の側ではなく身体の側にある**。**したがって #118 の「財を増やせば請求権が正になる」はこの世界では成り立たない**。**測定（96シード）: コインを持つ割合 +0.04 \*・1人あたりの枚数 +0.33 \* なのに、売買 −0.01・動いたコイン −0.25**＝**コインは持たれて使われない**〈#112(c) の警告が理由のある欲求でも出た〉。**既定はオフ**）。**2026-09-15 に段階88（天気を増やす＝#118 の実装）を完了した**（**設計は狙いどおり「増やすのは1行」**〈列挙・地図の文字・値段・品が自分で持つ `Wards`。**知覚も効用式も1行も増えない**〉**が、市場は大きくならず世界は悪くなった**——**売買 −0.18〈誤差内〉、個体数 −18.55 \*\*\*、守られている割合 0.45 → 0.34 \*\***。**理由は供給の分割**: **作り手はどちらの天気に答える品を作るか狙えない**ので、**半分は自分のいる場所で役に立たない物**になり、**それを正しい場所へ回す交換が無いかぎり無駄になる**。**#118 の「線形に増える」は外れ**——**「variety は弱い環を攻める」は交換が既に成立している世界でだけ正しい**。**そして #115(a) を直接試して否定された**: **贈与に値段を付けると贈与は半減し売買も半減する**〈−878 \*\*\* / −0.80 \*\*\*〉＝**交換が売買に化けるのではなく交換そのものが消える**〈段階84 の `giftpaid` と同じ符号の2度目〉。**実装中に1つ直した**——**`wardValue` が手の中の最良を引いていたので着ている1着の値打ちが常に0**だったが、**直しても世界は1ビットも動かない**〈`addGive` はそもそも手放すものを採点していない〉）。**2026-09-15 に段階87b（季節）を完了した**（**#117 の候補(b)**。**天気の絵を横に滑らせる**〈深くするのではない——全員が同時に同じものを欲しがるのは1つの需要であって取引にならない〉。**既にある暦に乗せ、乱数は1つも引かない**。**狙った「商品の在庫」は増えなかった**〈暖かい所で持たれている防寒具の割合 0.46 → **0.47**〉——**動かない世界でも既に46%**で、**作っていたのは季節ではなく脚**〈一生のうちに両側に立つ身体が53%＝段階87a の数え上げ〉。**用量は1年・4年・20年でほぼ同じ**＝**room が無いことによる失敗**。**代価だけ残る**〈個体数 −8〜−12 \*、餓死率 +0.33〜+0.50、`rareTrough` −0.04〜−0.07〉。**既定はオフ**）。**2026-09-15 に段階87a（寒さに答える品）を完了した**（**飾り物の一種として入れた**——**新しい種類は作らず**、**一定の割合で「寒さを防ぐ」性質を持って出来上がる**〈作り手は狙えない＝段階84 の `Style` と同じ手〉。**決めたのは3つ**: **1着で足りる**〈`wardsOff` は手の中の最大値＝段階81 の `max`。**だから2着目は持ち主にとって値打ちゼロ**＝段階69 の非対称〉／**値打ちは書かない**〈寒さは目減り、目減りは死亡確率なので、**寒い所では食事級・暖かい所ではゼロ・すでに1着あればゼロ**が自動で出る〉／**読む場所は既存の5つ**〈拾う・作る・買う・手放す・贈る。**`handWorth` に入ることが要点で、この世界で初めて「同じ物の値打ちが、物を変えずに場所で変わる」**〉。**実装前に数えた**: **一生のうちに両側に立つ身体は 53.1%**〈商品は運べる〉、**死の31.5%は寒い側にしかいなかった身体**〈腐敗の的〉。**測定（96シード）: 個体数 +20.97 \*\*\***〈段階52 の調理に並ぶ〉**・寒さの取り分 0.55 → 0.51 \*\*\*・`onCold` +0.03 \***＝**食料以外で居場所を動かした初めての規則**。**770着作られ85%が人手に渡り、その全部が贈与**〈売買は不動〉＝**#115(a) の測定による裏づけ**。**予想は5つ書いて5つ当たり**）。**2026-09-15 に段階86b（どちらが暖かいかを感じる）を完了した**（**ユーザの「グラデーションがあれば寒くない方へ行きやすくならないか」から**。**問いは正しい場所を指していた**——段階86 の地図は2値の段差で、内部には読むものが無かった。**実装の途中で数えて1つ目が無意味だと分かった**: **移動候補を「その手が連れて行く先の寒さ」で課金する形は、平均 0.000013 に対し候補の開きが 118 で7桁足りない**〈**天気は地域の幅400 で変わり、1決定は10〜20 しか運ばない**〉。**だから段階15b と同じ目的地つきの候補にした**〈値打ちは一歩の差ではなくその場所の差〉。**測定: 動く**〈偶然よりのぶん −0.03〜−0.04 \*、払う寒さも −0.02 \*〜−0.04 \*\*〉**が、食料は同じ地図で 0.35 動かす＝10分の1**。**坂より段差のほうが動く**〈−0.05 \*\* 対 −0.01。**候補の値打ちはその場の急さに比例するので、なめらかに配ると弱い信号になる**〉。**そして割に合わない**〈候補の平均の値打ちは −21＝地域1つ分の暖かさに地域1つ分の歩き。強い用量では目があるほうが個体数 −7〉。**既定はオフ**。**段階86 の結論は撤回ではなく限定つき**——**「寒さは居場所を動かさない」ではなく「どちらが暖かいか分かっても、動く価値がない」**）。**2026-09-15 に段階86（寒さを入れて測る）を完了した**（**寒さは巨大な税で、誰も避けない**。**的はこの world で過去最大**〈**飢えと寒さが奪う体力の 60%、強い用量で 76%**。段階34 の溺死は死因の11%、段階62 は人間の死の2%〉**なのに、寒い側にいる割合は 0.52 ＝偶然どおり**〈+0.02 ± 0.02、用量2.5倍で −0.00〉。**「感じない」対照〈`ChillKnown = false`＝同じだけ取るが自分の状態として読まない〉と区別がつかない**〈−0.01〉＝**効き目は全部「選別」で「選択」ではない**〈段階34 の答えを6倍の的で〉。**理由は約分**——**足元の寒さは全候補の危険を等しく押し上げるので、勝ち負けを決める比較から消える**〈段階57 と同じ形。**共通項はいくら大きくても共通項**〉。**動かしたのは食料だけ**〈寒い側が豊かなら +0.11 \*\*\*、痩せていれば −0.24 \*\*\*〉＝**「居場所を動かすのは食料の分布だけ」の7度目**。**代価は個体数 −19.11 \*\***〈強い用量で −40.34 \*\*\*〉**・餓死率 +0.48 \*\*\*・最近傍 +12.80 \*\*・`halfLife` +8.94 \***。**そして知ることはむしろ損**〈`coldblind` のほうが個体数 +4.62・餓死率 −0.36 \*\*。有意ではないが符号が揃う＝逃げ場が無いまま臆病になる〉。**帰結として4本目の地域信念は作らない**〈段階35 が同じ形を測り済み〉**し、段階87〈持ち歩けるもので答える〉が「もう1つの選択肢」から「唯一の道」になった**）。**2026-09-15 に段階85（気候の下ごしらえ）を完了した**（**ユーザの「将来マップに寒暖・明暗・乾湿を置き、飾り物がそれに役立つものになっていけないか」から**。**2026-09-04 の「地形の下ごしらえ」と同じ形**——**読みを1か所に通し、その1か所が中立を返し、指紋は1ビットも動かない**。**決めたのは3つ**: **天気は地域の性質**〈セルではない＝**この世界が場所について学んで伝える仕組みは全部地域に乗っている**〉／**取るのは体力の目減り**〈**これが他を全部タダにする**——先読みは新しいコードなしに寒さを読み、**それを打ち消すものは食料と同じ勾配で値段が付く**〉／**危険のうち「自分の代謝」の側**〈打撃は2つ目の窓へ持ち越さないが、**場所は考えているあいだに暖かくならない**＝段階67 の線〉。**継ぎ目は2つ**（`chillOf`＝寒さが数になる唯一の場所、`wardsOff`＝持ち物が天気に答える場所、**今は常に0**）。**数を入れると世界は壊れず身体が寒い側から出ていく**〈個体数 −23.50 \*、devview で目視〉。**次は 86 → 87 → 88**）。**2026-09-15 に段階83（買った値段のアンカー）を「作らない」で決着した**（**その段階自身が指定した実装前の数え上げが判定条件だった**——「同じ品が2度売買される割合。低ければアンカーは一度も発火しない」。**実測で `resaleAsked` = 0.00**、**市場の天井**〈飾り物3倍・お金5倍で売買 1.29/実行〉**でも 0.00**。**理由は算術**——市場は1実行に1件、手の中の売れる物60個のうち買われた物は 0.02 個なので、**期待される再販は 0.0003 件/実行**。**規則が禁じているのではなく市場が小さすぎる**。**規則そのものも `SaleAnchor` として入れた**〈**既定0**。**売り手の下限を買値へ引き上げ、追い詰められているほどその引き上げが消える**。**新しい見積もりは作らない**——買値は品に乗り、見込みは飾り物と同じ数字〉**が、発火は実測0**〈`anchored` = 0.00、**規則を入れた腕は入れない腕と全指標 +0.00 ± 0.00 ＝1ビット同一**。**手で状況を組み立てれば効くことはテストで固定**〉。**復活の条件は `resaleAsked` が0でなくなったとき**。**段階79 と同じ形の2度目**）。**2026-09-15 に段階84 の3つのルールを既定にした**（**味・先読みの割引・余剰の分け方**。**分離して測ったうえでユーザが選んだ**——**分け方は値段のばらつきを丸ごと買って代価は測定上ゼロ**〈1枚超 +0.28 \*\*\*、個体数 +0.69 ± 1.74〉、**味は個体数 +11.15 \*\***、**先読みの割引は味の入った世界では誤差内だが原理の側の直し**〈定数の目的が縮む目的に勝つ＝段階73 と同じ欠陥〉**で3倍の用量を壊さなくする**。**既定の世界は1ビットも動かない**——3つとも飾り物か値段のある世界でしか読まれない。**それ以前の腕は `beforeTheFlip` でピン留めし、前後で1ビット同じことを確認した**）。**2026-09-15 に段階84（欲しがり方が身体ごとに違う）を完了した**（**ユーザの指摘から**——段階82 は「いらないものは買わない」で終わったが、**意図は「欲しいように設定する」ことで、食事と飾り物を分けるのは先読み**だった。**実装して最初に見つけたのは、飾り物も本も買えなかったこと**〈`addBuy` の入口が掲げられた品の**栄養価**を訊いていたので、**栄養の無いものは「買う」が一度も採点されたことがない**。値段も売り手の側も受け渡しも全部動いていたのに。**段階82 の `canEat` と同じ形で同じ日に2度目**。**直すと売買 0.36 → 0.92 \*\*\* で、増分は全部飾り物**〉。**形は4つ**: **味**〈円の上の位相＝体内時計と同じ立場。**作り手は狙えない**＝歩いても働いても手に入らない欠乏。**平均1に正規化**＝欲しさの総量は増やさず配り直すだけ〉／**先読みが欲しさを決める**〈`survives()` を値打ちに掛けるだけ＝段階73 と同じ関数〉／**手放すのは一番惜しくないもの**／**贈与にも値段**。**測定（96シード）: 味は個体数 +11.15 \*\*・出生を元に戻し**〈飾り物は出生を 73.3 → 50.0 \*\*\* 奪っていた〉、**売買 +0.34 \*\***。**先読みの割引は3倍の用量を壊さなくし**〈−6.51 \*\*\* → +0.70〉**そこがこの群で一番大きい市場**〈+0.49 \*\*〉。**それでも値段は散らばらない**〈1枚超 +0.01。**散らばらせるのは余剰の分け方＝`SalePriceSplit` で +0.25 \*\*\*、味の有無に依らない**〉。**選り分けをするのは売買で贈与ではない**〈持ち主が替わって増えた好み: 売買 +0.25 \*\*\* 対 全体 +0.07 \*\*\*＝**買い手は自分で自分を選ぶが、贈り物の受け手は誰も選んでいない**〉。**自給自足はやはり市場を殺す**〈作り手が自分の好きな物を作れると質も量も倍になるのに利得が消える〉。**既定は全部オフ**）。**2026-09-15 に段階82（飾り物）を完了し、P13 の実装段階を通した**（**それ自体のために欲しがられるものを1つだけ作り、値段の仕組みを食料の向かい風の外で試した**。**答えは「市場は現れない」**〈`sales` −0.55 \*\*\*、1枚超の値段も −0.17 \*\*〉。**開いたのは値段の要らない交換路のほう**〈贈与 +269 \*\*\*、切ると個体数 −19.99 \*\*\*〉＝**誰も必要としないものは渡しても誰も飢えない**。**対照が機序を割り**〈手に何も残らない腕は +8.50 \*〉、**用量を3倍にすると決定の23%が「作る」になって世界が壊れる**。**測定して初めて「飾り物が食べられていた」バグが見つかった**〈`canEat` の列挙に落ちた4つ目の種類。最初の +35.56 \*\*\* はそれ〉。**既定はオフ**）。**2026-09-15 に段階80（値段を変数にする）を完了した**（**値段は市場を作り、買い手が正しく採点することが市場を閉じる**。**対照〈値段は付くが買い手は1枚のつもりで出発する〉で売買 2.06 → 3.72 \*\*\*・動いたコイン2.7倍＝P13 で初めて市場が本当に大きくなった**が、**標準価格で採点させると売買は 0.85 に落ち、代わりに1実行83回の無駄足が消えて世界が返る**〈個体数 +4.97 \*、`rareTrough` 0.35 → 0.49 \*\*〉。**並の1食では買い手の天井はちょうど `1/CoinValue`** ＝段階79 の壁の買い手側。**値段は散らばらない**〈1枚か2枚〉ので **#112(b) の相場は作らない**。**既定はオフ**）。**2026-09-15 に段階80a（手を取るのは重さのあるものだけ）を完了した**（**段階80 の前提**。**着手前の数え上げで「段階80 にはそのままでは動かせる変数が無い」と分かったので立てた段階**——買い手が1枚しか持てない世界では、価格を変数にしても成立する集合は今日と同じ。**仕組みは全部動き**〈枚数 0.90 → 2.98 \*\*\*、贈与2.6倍、`load` が倍、調理 +46%〉、**個体数は地図で符号が変わる**〈平坦 +14.57 \*\*／遊ぶ地図 −9.55 \*\*〉＝**空いた手は夕食で埋まり、食べ物を運ぶ規則は豊かな世界では得で痩せた世界では損**〈段階40・44・48 と同じ形の4度目〉。**既定はオフ＝地図の作者が入れる**。**段階80 の的は10倍になったが、値段は散らばらない**）。**2026-09-13 に段階69（本）を完了した**（**仕組みは狙いどおりに動き、狙った非対称も作れ、世界は1ミリも動かない**。**指定どおりのスキルの本は 4.29冊書かれて1回も読まれず**〈**ヒント枠は常に満杯**＝段階31 の壁の3例目。**実装前に数えた的 3.2% × 0.0054 がそのまま出た**〉、**場所の本は1冊あたり4.1回読まれる**〈消える版は0.6回＝**6.6倍**〉。**それでも `storeKnown` は動かない**〈薄れる知識は運んでも積み上がらない〉。**値打ちを16倍にしても注意が倍になるだけ**。**段階67 の手の取り合いが本で再現された**）。**2026-09-13 に段階68（売ることは手渡しでもある）を完了した**（**市場は動き、値段は好意が記憶に残ることのほうだった**——`sales` 5.12 → 8.00 \*\*\*、断り −18.62 \*\*。**天井は選べない**〈信頼が飽和するので値打ちは `LoreValue` = 9 が上限で、**断るときの平均の隔たり 15.78 に届かないことが実装前に分かっていた**〉。**代価は個体数 −9.44 \***で、**対照〈同じ値付けで何も記憶に書かない〉が一番売れていて落ち込みの62%を取り戻す**＝**払ったのは取引ではなく好意が記憶に残ること**〈好意は休息を安全に見せるので net で損、という `shMemory` 6度の結論と同じ壁〉。**ユーザが挙げた3候補を採らなかったのは、3つとも「コインを持つ」値打ちを上げて段階67 が測った手の取り合いを強めるから**）。**2026-09-13 に段階67（効用式に1段だけ先読みを入れる）を完了した**（**この世界に入れた1つの規則として個体数を動かした量が過去最大**〈**+52.44 \*\*\***。それまでの最大は段階52 の調理 +21.10 \*\*\*〉。**本体は対照のほう**——**先読みは平均 `pressure` を1.53倍にし、生命項は `pressure` の差なので、一様に1.53倍すること＝`LifeValue` = 153**。**その平らな対照は個体数 −17.40 \*\*\*、倍にすると −42.83 \*\*\***で、**`flees`・`hunts`・`dIntelligence`・`nearest` が全部逆符号**＝**生命を高く買うと身体は臆病になり、先が見えるようにすると目的を持つ**。**主指標のコインの売買は下向きに動き**〈5.12 → 0.21〉、**理由は2つで大きいほうは数えていなかった**——**手がコインで埋まって食べ物を持てなくなる**〈`holders` 0.46 → 0.75 と上がるのに `load` は 0.15 → 0.03、贈与 −190 \*\*\*、調理 −70 \*\*\*〉。**同じ地図からコインだけを抜くと経済は1ミリも壊れず、そこで段階50 の壁が破れた**〈`storeIn` 27.7 → 387.5〉。**既定はオフ**）。**2026-09-12 に段階66（育児中の母は遅くなる）を完了し、P11（伝わる機会・段階65〜66）を通した**（**狙ったものは全部動いた**——子は近くにいるようになり〈0.73 → 0.90〉、母子の伝承は増え、**子はよく育つ**〈`grewUp` +0.04 \*\*\*〉、代価は集団のほう〈人間 −5.17 \*〉。**ところが「同じ遅さを子の有無と無関係に与える」対照が同じかそれ以上動かす**〈用量は 1.005倍で揃えた〉＝**買っているのは「遅い母」であって「子のそばで遅い母」ではない**。**構造が効いたのは集団の持続だけ**〈`halfLife` +17.26 \*\* 対 +4.22 \*＝平らな腕の母は子から離れているときも遅いので群れに戻れない〉。**既定は無効**）。**2026-09-12 に段階65（交配のときに配偶者が伝え合う）を完了した**（**実装前に数えた2つの数字がこの段階の全部を決めた**——**1出生に1回足すと交換全体の1.01%**、そして**ペアが手を組んでいるあいだの交換はゼロ**〈「少ない」ではなく、`stepPaired` が `Decide()` も `ActObserve` も走らせないので経路が存在しない〉。**狙った量は動き**〈`mateGap` 2.37% → 2.18%、`loreSpread` も同じだけ〉、**世界の指標は動かない**〈1 SE、用量5倍の腕が一番小さい＝雑音〉。**既定はオフ**）。**2026-09-12 に段階63（水中の外敵）を完了し、P10（マップの性格・段階56〜64）を全部通した**（**川に住んで狩るものを置いても、人は川から出ない**——**出ないどころかわずかに入る**〈`humansWet` 0.11 → 0.15〉。**危険で人を水から出そうとしたのは34・35・63 の3度で、3度とも動かず、動かしたのは段階36 の食料だけ**。**溺死 −4.69 \*\* は死因が排他だから**〈獣に殺された身体は溺死に数えられない〉。**既存の食料魚には1行も触っていない**。**P10 全体では、狙った量が狙いどおり動いたのは段階64 だけ**で、**残りが繰り返し言ったのは「場所に結びつけたものは身体が動くことで消える」と「居場所を動かすのは食料の分布だけ」の2つ**）。**2026-09-12 に段階62（外敵タイプごとの対抗スキル）を完了した**（**#94 の賭け＝「的が命に関わるので集団への到達率が過去より大きいはず」は外れた**——**仕組みは動く**〈保有率 0.26〜0.32、実効値 0.24〉**のに届くのは人間の死の2%**〈0.26 × 0.24 × 効き0.5 ＝3.1%、人間の死のうち外敵が関わるのが65%〉。**「命に関わる」ことが大きくするのは掛け算の3つ目の項だけで、1つ目と2つ目はどのスキルでも同じ仕組みが決めている**。**実効値が伸びないのは天井にした防御力が予算比 0.06 ＝一番買われていない遺伝子の1つだから**〈段階47 が理性で同じことを踏んでいた〉。**既定はどの種類も `Ward` を持たない**）。**2026-09-12 に段階60（外敵が人間の食料を狙う）を完了した**（**外敵が4倍になった**〈12.4 → 49.0、+36.67 \*\*\*〉——**植物の総量は1つも増えていない**ので、**外敵の数を決めているのは湧きの設定ではなく餌**だった。**人間は −6.17 \***。**そして `pop` は +34.54 \*\*\* で、そのすべてが外敵**——**種族ごとの列を足して初めて人間が減っているのが見えた**〈同じ日に2度目の「列や対照がなければ逆を書いていた」〉。**的はこの群で一番大きい**〈外敵の3分の2が視界に植物を持っている〉）。**2026-09-12 に段階61（地図が総量を決める）を完了した**（**実装前に前提が崩れた段階**——**再正規化は錠前ではなかった**〈地域の重みは比としてしか使われないので、割り戻しても配分は動かない＝川の地図で1ビット同じ実行〉。**錠前は総量のほうで、`FoodTotalFromMap` がそれを開ける**〈`plantRate = FoodSpawnRate × 重みの平均`〉。**測定では ±6〜13% の用量で個体数は動かず、古い旋盤〈`FoodSpawnRate` 直接〉で同じ用量を回しても動かない**＝**その地図がその margin で食料に頭打ちされていないという世界の性質**。**新しい旋盤は古い旋盤と並べて回す、というのが今回の作法上の収穫**）。**2026-09-12 に段階64（外敵の行動範囲）を完了した**（**この群で初めて、狙った量が狙ったとおりに動いた**——**段階58 の湧きの偏りが居場所に残るようになり**〈6% → 45%、`prowlGain` +0.06 \*\*\*〉、**危険地帯が初めて噛んだ**〈`prowlBite` +0.21 \*\*\*〉。**効いているのはコストであって帰り道ではない**〈帰り道は決定の1%＝55a の正反対〉。**代価は外敵が獲物を見つけられず飢えること**〈`hunts` −56 \*\*\*、`rareShare` 0.15 → 0.10〉＝**「危険地帯を作る」と「世界を穏やかにする」は同じ操作の両面**〈人間の個体数 +8.94 \*〉。**既定はオフ**）。**2026-09-12 に段階59（外敵タイプのプリセット表）を完了した**（**`Species` は2値のまま、種類は `Config.EnemyKinds` の1行**。**増やしやすさを設計の中心に置いた**——行を足す以外に何も要らず、指標も表の長さに依らない〈3行の世界が1行のコードも足さずに動くことをテストで固定〉。**実装して初めて分かったのは「種類は種ではない」こと**——求愛は種類を見ないので**最初から交配していた**〈子の種類は予算と同じコイン〉。**表は効き**〈`kindGap` 1.57〉、**`Homing` は効かず**〈段階58 どおり〉、**混ぜ方が世界の選択を変える**〈重いのが4分の1なら `killRate` +0.20 \*\*・`dPower` +3.69 \*、半分なら `hunts` +8.15 \*・`halfLife` +6.45 \* だが `dIntelligence` −3.98 \*\*〉。**既定は空の表＝1種類**）。**2026-09-12 に段階58（外敵の湧きを地域に紐付ける）を完了した**（**規則は発火した瞬間には完璧に効き、世界が一生のうちに消す**——湧いた場所の偏り +0.25 \*\*\* に対し、実際に立っている場所に残るのは6%。**理由は2つとも数えた**〈外敵の4割は世界の中で生まれ、残りも出発点から地域1つぶん以上歩く〉。**偏らせると外敵はむしろ散る**〈既定で固まって見えるのは人間を追っているから〉。**段階57a・57c と同じ形の3つ目で、初めて「消えない形」＝段階64 を名指しした**——**59〜63 より先に 64 を通す**）。**2026-09-12 に段階57c（地面が建て方を選ぶ版）も完了した**（**約分は止まったが、今度は一生のうちに平均される**——**一番合う地域は平均より 7.6〜15.5% 良い**のに**取れたのは2%**で、**その2%も出生地の運**〈持ち歩く対照が同じ大きさを出す〉。**1つの身体は一生に12地域のうち5.9 に立つ**ので、**場所に縛られた倍率は一生のあいだに均される**。**能力の側から居場所を動かす道は2つの形で塞がり、居場所を動かすのは食料の分布だけという4度目の確認になった**）。**2026-09-12 に段階57（現在地に効く、非可搬の能力補正）を完了した**（**6度目の不発だが、失敗の理由が初めて分かった**——**地面の補正は近所の全員に同じだけ効く共通項なので、勝ち負けを決める比較から約分されて消える**〈殴れる距離にいる組の100%が同じ地域〉。**そして対照のほうが世界を動かした**〈同じ倍率を身体につけると個体数 −8.40 \*・`dIntelligence` −6.82 \*\* ＝遺伝しない当たり外れは選択を薄める〉。**既定はオフ。57b は作らず、約分をやめる 57c を立てた**）。**2026-09-12 に段階56（あるべき状態を土台とマップの性格に分ける）を完了した**（**コードは1行も変えない段階**。`PLAN.md` の受け入れ基準の表に「判定する世界」列と G・H の2行、この文書に「マップ共通の土台」の節。**基準そのものは1つも変えていない**——**直したのは「A〜E がどの腕の数値なのか書かれていなかった」ことで**、**同じ規則が地図で逆符号になる例は5つたまっていた**。**書いてみて分かったのは、保存則が守られていたのに明文化されていなかったことと、「閾値を作らない」がマップごとに難易度を振ってよいことの根拠そのものだったこと**）。**2026-09-11 に段階55a（見失った方角）を完了した**（**探しに行くことが集団を壊す**——**全決定の7%が選ぶほど発火したうえで `halfLife` がどの用量でも下がる**。**逆向きの対照は6倍悪い**＝情報は本物だが**行く価値が無い**。**55b・55c は作らない**）。**同日に段階53（性差）を完了した**（**役割から逆算した配分が、役割を逆にした配分より悪い**——**世界が金を払っている遺伝子と役割が要求する遺伝子が別**だから。**それでも `shMemory` が5度の不発のあと6度目に動き、符号が役割についてくる**が、**代価は個体数の3分の1**。**既定はオフ**）。**同日に段階53a・53b（母がガーディアン／どちらの親も養える）を完了した**（**先に役割を作り、遺伝子の振り分けはそこから逆算する**＝#85。**ガーディアンの 50.5% は父だった**ものを母に固定し、**その副作用で父が給餌から締め出されるのを同じ回で埋めた**＝#86。**集団の数字は誤差内だが、役の代価は父の寿命に出た**〈+259.60 \*\* → 給餌を開けて +132.81 \*〉。**`shMemory` が振り分け前から +0.01 \***）。**同日に段階54（気分）を完了した**（**非合理な偏りは、強さと構造の両方がそろって初めて効く**——弱い用量では符号を逆にしても同じで、十分な用量で**気分あり +7.77 \*／符号逆 −0.98／同じ平均の傾きを定数として −2.35 ± 6.05** と分かれる。**効いているのは平均の大胆さではなく直前に何があったかに連動していること**。**既定はオフ**）。**同日に段階52（調理）を完了した**（P9 の先頭。**段階50・51 が別々に当たった壁を、効用式の計画の形に触らずに迂回した**——**治りは空腹度ではなく「欠けた体力」から読まれる**ので、**満腹で傷ついた身体＝余剰を持っている身体に届く**。**実装前に数えた的は6倍**〈0.037 → 0.235〉で、**結果も同じ規模**＝**個体数 +21.10 \*\*\* でこの世界の1規則として最大**。**独占は起きず、前提のほうが崩れた**〈手の中のものは誰にも取れない〉。**分業は6度目の不発**〈`cookSplit` 0.01 ± 0.02〉）。**段階0〜6は 2026-08-12 に完了**（`engine` に実装、`cmd/devview` で目視確認済み）。**段階6.5（計測の先行整備）は 2026-09-01 に完了**（5指標＝クラスタ・membership half-life・戦闘の相手・集団間距離・種族ごとの個体数。「集団が無い世界」のベースラインは `HISTORY.md` の同日の項）。**段階7a（空間グリッド）と段階7b（継承則の入れ替え）は 2026-09-01 に完了**（7a＝近傍探索をインデックス経由に、挙動不変。7b＝粒子的遺伝とまれで大きい変異。**測定のベースラインはここで一度リセットされている**——数値は `HISTORY.md`）。**段階11（種族の汎用化と外敵）は 2026-09-01 に実装、2026-09-03 に完了**（`PLAN.md` の順を飛ばした。**群れ狩りは現れなかった**——対照条件つきで確認。`HISTORY.md` 追記23。残っていた「共同戦闘を好意の加点源にする」と「外敵のサイズ分布」は 2026-09-03 に決着。`HISTORY.md` 同日の項）。**段階7c（遺伝子ベクトル化と予算制）は 2026-09-01 に実装完了**（9本の遺伝子が1つの予算を分け合う。**完了条件のうち「大きくて遅い／小さくて速い の並存」だけは段階8 のあとでないと判定できない**）。**段階7d（成長・老い・育児期間）は 2026-09-02 に完了**（`TicksPerYear = 500` を本決め、`MaxLifespan` を 3000 に較正。死因は戦闘62%・餓死34%・消耗4%）。**段階7c の残っていた決定（予算の継承幅・初期配分の α・天才の発生率・魅力の体調重み・予算の価格）は 2026-09-02 に数値をつけて完了**（`HISTORY.md` 同日の項と追記27。**天才2段階の発生率の確定だけは `TicksPerYear` と対なので段階7d へ移送**）。**段階8（チャネル化）は 2026-09-01 に完了**（`killShare` 0.92 → 0.79、個体数 63 → 111、選択圧がパワーから頭の良さへ移った）。**段階9（記憶の容量・帯域・忘却曲線と好意）は 2026-09-02 に完了**（記憶が有限になり、記憶に初めての正の項が入った。選択圧がさらに頭の良さへ寄った。**記憶容量への選択圧は出ない**——段階12 で再測定する）。**段階10（見た目からの学習）は 2026-09-03 に完了**（初対面の推定誤差が固定値より 3.0 小さい。ただし利得の9割は「世界の平均を学んだ」ぶんで、体つきを読んだぶんは1割）。**段階12a（効用式の仮定を個体ごとの値にする）は 2026-09-04 に完了**（好みは可変で既定オン、信念の学習は動くが既定オフ。理由は下記）。**段階12b（伝承）・12c（ヒント）も同日に完了**（等価交換とヒントの模倣。**あるべき状態 B〜E のうち B は達成、`halfLife` はほぼ、`gapRel` は未達**）。**段階13（視界のグリッド化）・地形の下ごしらえ・段階14（休憩場所）も 2026-09-04 に完了**。**段階15・16・17・18 も 2026-09-04 に完了（17 は実装・測定のうえ既定オフ）。`PLAN.md` の段階は全部通した**——残っているのは `TODO.md` の宿題（再判断トリガーの性能）と、保留中の項目（`protocol`/`server`/`client`）だけ。**2026-09-08 に段階38b の3つ目（毒耐性）も完了し、段階38 を区切った**（**仕組みは動き、効果は雑音の底**——**数えた的が、17b の復活で信号に値段を付けたこと自体によって消えていた**〈信号 0.60 → 0.07〉。**信号が高いままの世界でも届くのは的の2割**。**4種類やって出た上限＝「保有率 × 実効値」**〈0.32〜0.38 × 0.18〜0.70 ＝ 集団への到達7〜26%〉**に的の大きさ〈2〜17%〉が掛かるので、集団の指標としては数%が天井**。**唯一有意に動いた泳ぎは、この積が一番大きかった**）。**2026-09-08 に段階38b の2つ目（泳ぎ）も完了した**（**3つのスキルで唯一、狙った量が有意に動いた**——溺死 −1.83 \*〈−7%〉、`drownRate` −0.03 \*、効かない対照では不動。**先に「打ち消す相手が集団の何%を動かしているか」を数えたから**＝溺死は死因の11%、飽きは5%。**予想を5つ書いて5つとも当たった**。**それでも `onWater` は動かない**〈6度目〉、**枠の値段は積み上がる**〈`hintKinds` −3.28 \*\*\*〉）。**2026-09-08 に段階38b の1つ目（採取）も完了した**（**設計どおりに動き、ほとんど何も買わない**——学ばれ広がり天井も効く〈名目 0.55／実効 0.18／模倣なし 0.19〉のに `dietDiscount` は +0.00 \*。**打ち消す相手＝飽きの割引がそもそも5%**だから。**#64 が心配した独占は起きず**、**記憶は5度目も選択圧に応えなかった**）。**2026-09-08 に段階38a（最初のスキル＝荒れ地耐性）を完了した**（**既定はオフ**＝地図の作者が入れる。**知識は社会的に育つが居場所は変わらない**——名目習熟度は 0.80／模倣なし 0.54／跳躍もなし 0.50〈＝出生地の値そのもの、ばらつきゼロ〉で**跳躍が作り模倣が配る**が、`onDear` は +0.01 \* しか動かず**超過コストを全部消しても不動**。**段階33 の宿題「速さと体力の並存への別経路」への答えは「スキルでもなかった」**）。**同日に 10b（ヒントの既定）も閉じた**（消しても個体数は増えず `killShare` が +0.03 \*\*\* 上がるので**既定は今のまま**）。**2026-09-08 に段階32（協力の誘い）を完了した**（**群れ狩りは現れず、段階11 の診断が否定された**——狙いを合わせる手段を与えても `packSize` は 1.34 → 1.36 で、語彙なしの対照でも同じだけ出る。**代わりに個体数 +12.83 \*\***、そして**社会的な規則で予算比が動いた初めての例**〈`shSpeed` +0.01 \*\*〉。**保留#10 はこれで閉じた**）。**2026-09-08 に段階31（目撃した殺害が伝わる）を完了した**（**規則は絶えず発火し、世界の数字を1つも動かさない**——1件の殺害が 24.3 回の観測機会を作るのに取り込まれるのは 0.21 で、**上限は重みではなく記憶の枠**。**#55 が心配した「観察が死ぬ」も起きない**）。**2026-09-08 に段階37（川が世界を2つに分けるか）を完了した**（P6 の最後。**実装ではなく観測**——規則は1つも足さず、読み取り専用の測定を `engine/banks.go` に足しただけ。**川は視界を分けるが集団は分けない**: 乾いた地面の者どうしの遭遇は 0.17 → 0.01 に落ちるのに、両岸の予算比も地域信念もずれない。理由は**水の上に個体数の14%が立っていて両岸を同時に見ていること**と、**一生のうち両岸に立つ個体が半分いること**）。**2026-09-07 に段階34（溺死）・35（溺死の周知）・36（水辺の豊かさ）を完了した**（P6。**34＝規則は効くが効き目は全部「選別」**、**35＝知識は正確に育ち伝わるが、水から出る理由にはならない**、**36＝`onWater` を動かすのは食料の分布だけ**——3点（痩せた水辺 0.11／一様 0.14／豊かな水辺 0.15）が一直線に並ぶ。詳細は下記「現在の機能」）。**2026-09-06 に段階20（地形本体）・段階29（地形コストの認識と伝承）・段階33（食料を地形に結びつける）・段階30a（高台の遮蔽）を完了した**（30b は先に決めた2つのどちらかを取り消さないと動かないので宿題）（実装・測定。既定は平坦のまま＝地形は世界を始める側が渡すもの。**狙っていた速さのニッチは出ず、逆に速さが買われなくなった**——下記「現在の機能」）。**2026-09-05 に段階19〜22（ゲーム化とマップエディタ）を足し、同日 段階19 と段階23 を完了した**: 19＝人間操作コントローラと血統乗り換え【完了】、20＝地形本体（旧「地形全般」）、21＝世界・ノードの保存/読込、22＝マップエディタv0 とパラメータ調整UI、**23＝選択を要求されるゲーム【完了】**（遊び方そのもの。19 と同じく世界のルールを変えないので 20 の前に通した）、**24＝外すと隙ができる【完了】**（戦闘の規則。ゲーム化とは独立）、**25＝求愛の閾値を効用式に移した【完了】、26＝待っても下げない魅力の床【完了】、27＝直接操作と操作性【完了】、28＝求愛の待ち時間が数えるもの【完了】**。**順序は 19 → 20 → 21 → 22 で確定**（`TODO.md` の P5 に理由）。`protocol`/`server`/`client` の着手時期は未定のままだが、**スナップショット形式は段階21 の実地の上に設計する**（`PLAN.md` 決定事項#48）。

- **2026-09-13：先読みを既定にした（ベースラインのリセット）。**【`LookaheadHorizons` 1・`LookaheadUpkeep` 0.75・`LookaheadNeverBlinds`・`GoalsNeedSurvival` を既定に。**段階7b 以来の意図的なリセットで、`HISTORY.md` の 2026-09-13 より前の全数値は「窓が1つ」の世界のもの**（`cmd/experiment` の `noahead` の腕と `cmd/devview -noahead` で再現できる）。**新旧の対（48シード）で個体数 +53.60 \*\*\*・`killRate` −0.48 \*\*\*・`halfLife` +17.13 \*\*\*・`dIntelligence` +12.98 \*\*\*・`rareTrough` +0.03 \***、**悪化は餓死率だけ**（総死亡率は下がる＝殺されることの置き換え）。**3度目でようやく通った**——**個体数はいつも「入れろ」と言っていたが、2度は同じ日に差し戻した**（崩してはいけない場面が壊れたから）。**性能は1決定 14.4 → 22.7µs**】
- 段階74：生命の項は、区別できるほうの窓で読む。【完了。2026-09-13。**ユーザの問い（「どのみち死ぬ」状況で求愛できるのはおかしい）から出た**——**おかしいのは食事が0点になっていること**で、**生命の項は「窓の終わりまでに死んでいるか」という yes/no**、**1食は死を 594 → 1038 tick へ動かす**ので、**窓700 では答えが変わり（14.76）窓1400 では変わらない（0.00）**。**規則は「差は2つの窓のうち大きいほうで読む」1つだけ**（閾値不要・失うもの無し＝鎖は近い窓について単調）。**壊れていた4つの場面が全部戻り、段階67 の効き目も残る**（`keepValue` 47.28）。**+46.16 \*\*\***（段階72 単独の +54.30 \*\*\* から8 落ちる）。**途中で2つ直した**: **探索の割引は間違い**（尽きかけた身体が寝る＝探索は生存の手段であって報酬ではない）／**段階72 の給餌の帳簿が保存されていなかった**。**既定はオフのまま**】
- 段階73：子孫と探索を生存で割り引く。【完了。2026-09-13。**効用式の目的には「窓の長さを知っているもの」と「知らないもの」があった**——**生命は2つの死亡確率の差なので窓が長いほど縮み、子孫と探索は定数なので縮まない**。**後で起きる目的の確率に「そのとき自分がいる見込み」を掛ける**（二重計上ではなく欠けていた項）。**割り引く時間は目的が来るまでの時間**（子は `PairBondDuration` 先。窓まるごとで割ると満腹の身体が求愛をやめる）。**既定の世界では無傷**（+2.50 ± 3.78、`deathRate` −0.14 \*\*）、**先読みの世界では空腹の身体が求愛をやめる**。**それでも先読みは既定にできない**——**3つ目の理由は「決定が何を選ぶかを、その決定の前提に置いている」**（段階72 の upkeep は「自分で食い扶持を賄う」と仮定するが、それはまさに今訊いている当のこと。**手当てには正しく効き、食事には逆に効く**）。**既定はオフ**】
- 段階72：2つ目の窓が身体について仮定していること。【完了。2026-09-13。**先読みを既定にできなかった2つの理由のうち1つを直した**。**予想した「食べない仮定」は半分**——**追い詰められた身体を絶望させていたのは空腹ではなく、`ShockRisk`（消耗していること自体の危険）を2つの窓で2度課していたこと**（1地平ぶんに較正された定数を鎖につないでいた。逃げることの値打ちが 46.23 → 0.63）。**直しは2つ**: **`LookaheadUpkeep`**（自分がこれまで食べてこられたぶんは賄えると仮定する。代謝以上には賄えない）と **`LookaheadWornAgain = false`**（消耗は測られた窓にだけ課す）。**0.75 で追い詰められた身体は再び逃げ、上の端も残る**（1.0 にすると段階67 の効き目が消える）。**個体数は先読み単独 +43.06 \*\*\* に対し upkeep 0.5 で +58.82 \*\*\***。**それでも既定はオフのまま**——**空腹の身体が食べずに求愛するほうは戻らない**。**理由は目的の値付け**（窓が長いほど1食の占める割合は小さくなるのに、子孫も探索も定数で値付けされていて縮まない）＝**次の題目**】
- 段階71：手の枠をやめられるようにし、n個目の値打ちを下げた。【完了。2026-09-13。**枠は価格であるだけでなく、他の2つの規則を死んだコードにしていた**（`CarryDiminishes` も重さの勘定の修正も、枠ありでは `coins` と1ビット同じ）。**「遺伝子が2つ目の枠を買う」は一度も起きていない**（平均の枠数 1.00）。**先読みの世界では枠こそが経済を殺していたもの**——`coinHolders` 0.53 → 0.32 \*\*\*、`sales` 0.12 → **4.38 \*\*\***（段階51 の水準に復帰）、贈与3.7倍、調理 +84%、**個体数の代価は誤差内**。**先が見えない世界では枠は得**（−11〜−15 \*\*）で、**その代価は運ぶ代価のほうであって囲い込みではない**（重さ0の対照で +7.19 \*）。**既定は3つとも据え置き**】
- 段階70：「置く」という語。【完了。2026-09-13。**`ActDrop`＝18 番目の語。既定はオフ**。**値打ちは「運ぶのをやめて浮く代価 ＋ 手が空くことの値打ち − 手放すものの値打ち」で、どれも既存の数字**（手放す側は `willSell` と同じ＝**置くことは誰にも売らずに売ること**）。**世界によって符号が変わる**——**平坦 +7.07 \*\*／経済のある遊ぶ地図 −6.29 \***（**交換はすべて手を通るので、手を空ける語は経済を削る**）。**そして段階51a の「コインで塞がった手」には届かない**（先読みの世界では20000tickに1.88回しか使われない）。**値打ちが0以下の候補は出してはいけない**（門が無い版は置いた回数の2〜8割が評価ノイズの引き当て）】
- 段階51a：コインの値付けの非対称を直す。【完了。2026-09-13。**コインは「競走も重さも無い食料」として食料に厳密に勝っていた**（両方が見えている決定の70%）。**直した形は「2つの条件が両方成り立たなければならない」**（後で困ること × そのとき売る誰かがいること）。**段階51 の世界では世界の数字が動かない**（96シードで個体数も `sales` も 1 SE 以内＝**記録はそのまま読める**）。**先読みの世界では贈与・調理・倉庫が戻る**が、**`coinHolders` も `load` も不動**＝**値段では手は空かない**（コインが手から出る道は売れるか死ぬかの2つだけ）。**`CoinPricedCertain` で旧世界に戻せる**】
- 段階69：本の流通。【完了。2026-09-13。**読んでも消えないことが、この世界で初めて「持ち主には値打ちゼロで他人には正」を作った**〈1冊4.1回 対 消える版0.6回〉。**ただし落ちる場所が無いものは作っても落ちない**——スキルの本は書かれて1回も読まれない。**壁は本の両端にある**〈読む先の枠も、書く技術を置く枠も無い〉。**既定はオフ**】
- 段階68：売ることは手渡しでもある。【完了。2026-09-13。**`sales` +2.88 \*\*\*、断り −18.62 \*\***。**天井は選べない**〈信頼が飽和するので `LoreValue` が上限で、断るときの平均の隔たりに届かない〉。**代価は好意が記憶に残ること**〈対照が62%を取り戻す〉。**既定はオフ**】
- 段階67：効用式に1段だけ先読みを入れる。【完了。2026-09-13。**`pressure` に同じ見積もりをもう1回だけ鎖でつないだ**（`p = p1 + (1−p1)·p2`）。**2段目はその身体自身の代謝だけを進めた状態で、今殴られていることは持ち込まない**＝**持ち込むと戦闘中の全候補が等しく絶望的に見える**（段階55a が裏から見つけた形）。**壁は引き算1つだった**——体力100・空腹40 の身体は `StarveHunger` に達しても体力が尽きるまで1429tick かかり、`PlanHorizon` は700なので `keepValue` はちょうど0。**既定は 0（無効）**】
- 段階0：意思決定の差し込み口（コントローラ／知覚／行動の境界）と血統記録を先に敷く。この上に以降を載せる。【完了】
- 段階1：状態モデルを 体力＋空腹度＋食料 に置換。餓死と回復が目視できること。【完了】
- 段階2：移動の体力コストと effort。動き回ると減り、満腹で休むと回復するのが目視できること。【完了】
- 段階3：継続戦闘と逃走。食料を巡る戦い、劣勢側の逃走、攻撃側有利が目視できること。【完了】
- 段階4：強さ推定と記憶（(A)リスク記憶＋減衰、(B)観戦ベースの強さ推定）。ノードをクリックすると他者への推定が見え、対戦・観戦で収束するのを確認。【完了】
- 段階5：リスク判断と先制排除。強い個体を避ける、競合圧に応じた先制攻撃、もつれが目視できること。【完了】
- 段階6：頭の良さによる戦略選択。賢い血統と愚かな血統で生存率を比較できること。【完了。ただし下記の注意を参照】
- 段階6.5：計測の先行整備（あるべき状態を測れるようにする）。【完了。2026-09-01】
- 段階7a：空間グリッド（挙動を変えない高速化）。【完了。2026-09-01。**既定の個体数では数%の持ち出しで、効き始めるのは500〜600体から**】
- 段階7b：継承則の入れ替え（粒子的遺伝＋まれで大きい変異）。【完了。2026-09-01。**能力の分散が約2倍になり、ここで測定のベースラインがリセットされた**】
- 段階7c：遺伝子ベクトル化と予算制。【完了。実装は 2026-09-01、残っていた6つの決定に数値をつけたのが 2026-09-02。**能力は9本の遺伝子が1つの予算を分け合う。指標は絶対値ではなく予算比で読む。** 予算はもう暴走しない（60000tickで442、最大900に対して）。**初期配分のディリクレ α は「初期条件」ではなくその実行の多様性そのもの**だと分かった】
- 段階8：攻撃・防御・回避のチャネル化。【完了。2026-09-01。**「一番危険」という当初の警告は逆だった**——危険だったのは入れないことのほうで、個体数は 63 → 111 に増えた】
- 段階7d：成長・老い・寿命と育児期間。【完了。2026-09-02。**実効値＝遺伝的素質 × 年齢補正（1本の曲線・掛け算は1か所）。** 子ども期は個体数の4割を払う——死亡率ではなく**世代時間**に乗るコスト。育児が効くのは保護ではなく**凝集**だった】
- 段階9：記憶の容量・帯域・忘却曲線と、好意。【完了。2026-09-02。**好意が入ったので段階12（伝承）の前提が揃った。** 休息の露出は重み 0.05——**露出は `RegenRate` の12倍の単位で入るので、大きくすると「視界に1人いれば休息の価値ゼロ」という崖になる**】
- 段階10：見た目からの学習。【完了。2026-09-03。**初対面の推定は各個体が自分で引いた直線から出る。** 利得は同じ遭遇で測って +3.0（誤差 33.4 → 30.3）だが、**そのうち体つきを読んだぶんは +0.34 だけ**——残りは「世界の平均を知ったこと」。**個体数は35%落ちる。原因は学習ではなく `PriorStrength` の水準**】
- 段階12a：効用式の仮定を個体ごとの値にする。【完了。2026-09-04。**事実（学べる）と好み（学べない）に分けた。** `Retaliation = 0.7` は世界の事実ではなく**実在しない報復への恐れという抑止力**で、真の値 0.15 を知ると個体数が半分になる——しかも増えるのは殺しではなく**餓死**。よって `LearningRate` の既定は 0。好みの側（広がり 0.15）は既定オンで、これだけで**理性への選択圧が負から0へ戻る**】
- 段階12b：伝承（等価交換）。【完了。2026-09-04。**`observe` に相乗りする双方向・同時の歩み寄り。** 好意が効用の正の項になり、他個体が生きていることが得になる2本目の経路ができた。**1回の歩み寄りは差の2%**——「中間で会う」0.5 は段階12a が作った多様性を消して個体数を半減させる。値段は個体数（40000tickで −12.7 \*）で、買えたのは好意のある相手の数（+0.90 \*\*）と集団の持続】
- 段階12c：ヒント（進化し続ける言い伝え）。【完了。2026-09-04。**状況×行動×重みの3つ組を上限4本。効用への加減点としてのみ働き、分岐はどこにもない。** 枠は空でも予算を食う。**手口の多様性を作っているのは重みではなく模倣**——重みを0にしても種類数は変わらず、模倣を切ると伝承なしの水準まで落ちる。**2026-09-05 に再測定して2つ訂正した**: 「頭の良さへの選択圧は下がる」は**予算が下がっていただけ**（予算比では1標準誤差）、「重みだけ0の対照に勝つ」（22.0 対 14.8）は**穴の開いた世界の値で、直った世界では誤差内**（73.3 対 73.5）】
- 段階13：視界をグリッド単位にする。【完了。2026-09-04。**視界は「自セル＋周囲1セル」のブロック**（面積は円と同じになるよう較正）。**「近い」ことが「見える」ことを意味しなくなった**のが本体で、殺しが減る（`killRate` −0.13 \*）。**逃走成功率は動かない**——面積を合わせると逃げ切る距離の平均まで揃い、異方性は分散しか増やさない。**視力は遺伝子にしない**（0リングは致死・2リングはやや悪い＝離散で非線形なので `StrategyDepthUnlock` と同じ轍になる）】
- 段階18：夜型・朝型（体内時計）。【完了。2026-09-04。**時刻で変わるのは休息の回復量だけ。体内時計は予算遺伝子ではない**（量ではなく円周上の位相）。**「眠っている友人は見張れない」という条件1つ**が「たまたま寝る時刻が違う」を「誰かが見張っている」に変える——**見張り役はルールに存在しない**。**これまでで一番世界を良くした**（個体数 +12.6 \*）。**完了条件の指標は最初から飽和していた**】
- 段階17b：毒と警告信号。【実装・測定完了、**既定はオフ**。2026-09-04。**個体数が9分の1**。**`shRationality` が段階9 以来はじめて上がる（0.07 → 0.11 \*\*\*）が、信号を読めなくしても同じだけ上がる**＝上げているのは信号ではなく世界の厳しさ。**正直な警告は進化しない**（毒にも信号にもコストが無いので足場が無い）】
- 段階17a・17c：植物の遺伝と種子散布。【実装・測定完了、**既定はオフ**。2026-09-04。**動かないからではなく世界を壊すから**（個体数 44.2 → 14.9）。**短い散布は地域を空にし（吸収状態）、長い散布は地理を学べなくする**という逃げ道の無い2つの失敗で、パラメータでは解けない。**段階15 の固定版は土台だった。** 17b（毒と警告信号）は別途】
- 段階16：食料の栄養バランス。【完了。2026-09-04。**新しい状態量を作らず、隠さない。** 効果は予想どおり小さい（個体数・`killShare` は誤差内）が**ゼロではなく、群れ狩りを初めて有意に増やした**（+4.7 \*\*）——**一般則（同じものばかりは損）が、専用の仕掛け（肉に独自の栄養）の弱い版として働いた**。**強い罰は逆効果**】
- 段階15：植物食料の地域差と、地域の豊かさの認識・伝承。【完了。2026-09-04。**15a＝食料の総量は変えず分布だけ変える**（`rareTrough` があるべき状態A の帯に復帰、段階14 で起きなかった凝集が起きる）。**15b＝自分が立った地面だけを覚える**。**15c＝行ったことのない場所は聞いて知る**——**集団を1か所に集めたのはこれ**。**`shMemory` への正の選択圧は出なかった**（地域12個では容量が制約にならない、と `PLAN.md` が予告していたとおり）。**性能: `Step` が2.4倍**（原因は既存の再判断トリガーで、`TODO.md` に独立項目）】
- 段階19：人間操作コントローラと血統乗り換え。【完了。2026-09-05。**段階0 で敷いた差し込み口を使うだけで、engine のルールは1つも変わらない**（指紋テストが無変更で通る）。人間は `Controller` のもう1つの実装（`engine/human.go`）で、**命令は「次に訊かれたときの答え」**。**約束は2つ——見せるのは `Perception` の中身だけ、狙えるのは視界の中だけ。** 実際に作ったのは3つ目の規則で、**命令が流れる場合が2つある**（狙いが消えた／身体が変わった）——**どちらも「AIは訊かれれば採点し直すが、据え置きの命令は自力で立ち直れない」という非対称から出た**。血統の乗り換えは同じコントローラを成人した子に付け替えるだけ（`World.Heirs`）】
- 段階23：選択を要求されるゲーム（転機の一手と、節目の性格）。【完了。2026-09-05。**「時々選択を要求される」は新しい仕組みではなく、既存のトリガー機構の目盛りの取り方だった。** 設計の前に頻度を測ったのが要点——1体が訊かれるのは **14.5 tick に1回**（うち53%が「殴られた」）で `-slow` では0.6秒に1回。**トリガーを5つに絞り戦闘を1戦＝1回にまとめると 112 tick に1回＝一生で20〜30回**になり、それがそのままゲームの形になる。**AIは代役ではなく常に動かしている側**（だから誰も答えない誘導ノードは純粋なAIノードと1ビットも違わない）。**メニューは書かず、その個体が実際に採点した候補をそのまま出す**。**順位にはその個体自身の評価ノイズを乗せる**（ノイズ抜きは、その身体の頭の良さ遺伝子が買っていない賢さを渡すことになる）。**答えは1手であって据え置き命令ではない**ので、段階19 が要した「命令が流れる場合」が要らなくなった。節目に人が動かすのは**好みの配分だけで合計は継いだもの**——段階12a が「好みには正解が世界に無い」と切っていたので、人に渡しても何も上書きしていない。**`Config` は1つも増えていない**】
- 段階22：マップエディタv0 とパラメータ調整UI。【完了。2026-09-06。**`cmd/devview` の1つのモード**（`F3`）——別プログラムにしなかったのは、描画もカメラも世界も全部ここにあるから。**engine 側は `engine/edit.go` の4つの操作だけ**で、どれも `Endow` と同じ「シミュレーションからは呼ばれない」立場。**地形と地域は別の地図**（セル単位のコスト／ブロック単位の恵み）で、片方を塗っても他方は動かない。**食料は取り分**なので増やせない。**ルールは6つだけ出し、測定済み範囲を外れたら警告する**（止めない）。**難易度は予算だけ**。**エディタ専用の保存形式は作っていない**（段階21 のまま）】
- 段階21：世界・ノードの保存/読込。【完了。2026-09-06。**3つの要件（他所で育った個体群・進めた世界の配布・天才の配置）は同じ1つの機能**（#48）。**乱数は「種と引いた回数」で保存し、読込時に引き直す**——生成器を差し替えると記録済みの測定値が全部再現できなくなるため。**受け入れ条件は指紋テストと同じ形**（保存→読込→500tick が保存しなかった世界と1ビットも違わない）。**個体群が世界をまたげるのは「その身体が何であるか」だけ**で、他人の名前を含むものは置いていく】
- 段階14：休憩場所。【完了。2026-09-04。**世界に地域が入った**（1種類だけ。段階15 以降も同じ地域に乗る）。**新しい仕組みは作らず、`RestExposureWeight` への掛け算が1つ**。**「良い場所がある」だけでは弱い**（+2%）——個体は今いる場所しか感じられないので、働くのは計画ではなく選別。**凝集は起きなかった**】
- 地形の下ごしらえは 2026-09-04 に完了（上記「現在の機能」）。**残るのは段階15〜18（環境と食料生態系の残り）**：`PLAN.md` にチェックリストとして分解済み。

- 段階98：泳げない身体にとっての水。【完了。2026-09-19。**既定はオフ**（`DrownUnskilledFactor` = 1）。**溺死の確率に「その身体が泳ぎをどれだけ持っているか」の倍率を掛け、既存の軽減（`SkillSwimRelief`）と1本のカーブにした**（無技能で倍、`DrownSkillFull` = 0.5 でちょうど元、そこから軽減が下へ続く）。**実装前の数え上げがカーブの形を決めた**——**水の中の身体の泳ぎは二峰性**（71% が 0、p90 0.525）なので、**マスタリー1でしか戻らない線形では川で育った身体ですら罰の大半を払う**。**規則は完璧に効く**〈溺れた身体の泳ぎ 0.05 → 0.03、一律の腕では 0.05 のまま＝**同じだけ溺れさせても溺れる人が変わった**〉**が、その選り分けには値打ちが無い**〈**一律の対照のほうが個体数 +8.46 \***、**全員3倍で溺死がもっと多い腕はさらに +10.20 \***＝段階95 と同じ形〉。**代価は用量に単調**（−23.32／−40.50／−69.49 \*\*\*、遊ぶ地図は −25.17 \*\*\* で払えない）。**そして誰も水から出ない**〈`onWater` 不動・「知らない」対照と区別がつかない＝**4度目**。**滞在はむしろ延び、死で終わる割合だけが 4.1% → 8.5%**〉。**ただし今回は約分では説明できない**〈候補間の開きは 1.80 → 5.21 と広がっているのに動かない＝**評価ノイズの4分の1しかないから**〉。**泳ぎへの選択圧も出ない**〈`swimReal` −0.08 \*\*、`shVitality` −0.01 \*\* ＝**世界が貧しくなると天井の遺伝子が先に買えなくなる**〉】

- 段階99：水の中にいることが体力を取る。【完了。2026-09-19。**既定はオフ**（`WaterDrain` = 0）。**ユーザの「水では体力を消耗するほうが現実に合う」から**。**それまでの水は「移動への通行料」だけ**で、**課金は「動いた tick」にしかなかった**ので、**川の中で立ち止まっている身体からは1ミリも取っていなかった**〈実測: 水の上の body-tick の 36.9% は静止〉。**足したのはセルの性質1つ**で、**段階85 が寒さのために作った「場所が取るもの」の経路に乗せた**ので、**先読み・休息・食事の見積もりが新しいコードなしに読む**。**この鎖で初めて「知っていること」が行動を変えた**〈感じる腕は水の中で立ち止まる割合が **−0.02 \*\*\***。段階34・86・98 は3度とも区別がつかなかった〉**が、居場所は動かず**〈`onWater` +0.01 \*、感じない腕との差 +0.00〉**世界の数字も動かない**〈個体数の差 +3.40 ± 6.12〉。**場所に紐づけることにも値打ちが無い**〈一律の対照と +4.31 ± 6.75 ＝段階98 と同じ結論の2度目〉。**代価は用量が決める**〈0.02 は **+0.30 ± 6.54 ＝測定上タダ**、0.05 で −17.56 \*\*、0.09 で −43.98 \*\*\*、遊ぶ地図で −12.75 \*\*〉。**そして水の代価の大半はこの目減りではなく通行料**〈`WaterMoveCost` = 1 の腕は同じ目減りを受けながら **+22.76 \*\***、`wetStill` も 0.31 まで落ちる〉】

- 段階100：一歩先の地面を読む。【完了。2026-09-19。**既定はオフ**（`GroundAheadSeen`）。**段階20 以来の「この先も足元と同じ地面」という仮定を、1セルだけ先へずらした**（8方位の読み＝移動コスト倍率と目減り、`(100−理性)/100` の誤差つき。**溺れる確率は読ませない**）。**実装前の数え上げが効かせる先を決めた**——**さまよう手は1tickぶんしか値付けられていない**（0.011＝ノイズの1/500）**ので、重みがあるのは目的地を名指しする手**（`eat` 65tick・`take` 74tick・`court` 34tick・`attack` 73tick ＝ +2〜+14）。**結果: この world で初めて地形が居場所を動かし、情報のない対照と分かれた**〈`onWater` −0.02 \*\*\* 対 対照 −0.00、直接の対で **+0.01 \*\*\***、`bankMoves` −1.20 \*\* 対 +0.06〉。**代価は無く**〈裸の川 +0.65〉**遊ぶ地図では得**〈**+10.20 \***〉、**段階99 の目減りと組むと代価を丸ごと返す**〈単独 −17.56 \*\* → **+6.05**、直接の対で **+22.73 \*\*\***〉＝**情報そのものに付いた最大の値打ち**。**それでも「岸へ向かう」割合は 0.492 で動かない**〈川が2セル幅で、向こう半分からは安い方角が見えない〉＝**効いたのは入る側**。**理性に値打ちを与えるのは誤差のほう**〈誤差を消すと `dRationality` −3.27 \*\*〉。**性能 +7.6%**】

- **2026-09-19：一歩先の地面を読むことを既定にした（ベースラインの3度目のリセット）。**【`GroundAheadSeen` = true。**段階7b（粒子的遺伝）・2026-09-13（先読み）に続く3度目**で、**`HISTORY.md` の 2026-09-19 より前の地形の数値は「足元の地面で値付けた世界」のもの**（`cmd/experiment` の `groundunread` / `playunread`、`cmd/devview -groundunread` で再現できる）。**平坦な世界は地面が無いので何も読まず乱数も引かない**＝指紋テストは無変更で通る。**新旧の対（96シード）: 裸の川は個体数 +0.65（誤差内）で `onWater` −0.02 \*\*\*・`bankMoves` −1.20 \*\*・`killShare` +0.03 \*\*\*、遊ぶ地図は個体数 +10.20 \*・人間 +9.36 \*\*・`rareTrough` +0.06 \*・`onHigh` −0.03 \***。**性能は同じ世界で +7.6%**】

- **2026-09-19：Tiled で描いた地図を読めるようにし、地域を自由形状にした**（`TODO.md` の1番）。【**`engine/tiled.go`（`.tmj` の読み取り）と `engine/regionshape.go`（描かれた地域）、`cmd/devview -tiled`**。**engine はファイルを知らない**＝バイト列を受け取るだけ。**読むのはタイルレイヤーとオブジェクトレイヤーの矩形の2つだけ**で、**タイルの意味はタイルセットのプロパティが言う**（`kind` / `height`）。**地域は割合で持つ矩形**になり、**外側は「それ以外の場所」1つ**、**各矩形は `shelter`/`food`/`special`/`enemies` を持てる**（0 は世界の抽選のまま）。**位置→地域は O(1) のまま**（200×200 の索引表）。**実装前に数えて設計が決まった**〈`regionIndexAt` は1tickに3〜10回＝守るべきはそこではない／地域数に比例するのは `worthOfRegion`（12地域15回→768地域1,900回）と1体あたり48バイト×地域数／**個体数は地域数では動かない**（6シードで30〜53。**1シードでは崩れて見えたが雑音**）〉。**受け入れ条件は段階21 と同じ形**（描いた世界と手で書いた世界が500tick で1ビット同じ）、**保存と読込にも乗る**（索引は読込時に作り直す）。**同じ日に「発生タイル」の層も足した**（`SpawnMap`＝2枚目のタイル層。`p`/`f`/`e`/`F`/`*` を1セル1文字で塗ると、**そこにしか湧かない**。**量は変えない**＝段階15a の保存則が塗られたタイルの中で閉じる。**陸の魚は無視**＝段階42 の不変条件を絵に壊させない）】

---

## 閉じた将来実装（`CLAUDE.md` の「将来実装」から 2026-09-19 に移したもの）

**実装済みのもの、または「作らない」で決着したもの。** 復活の条件つきで閉じたものもここにある。**まだ作っていない項目（配偶者を巡る同性間の戦い・地域ごとの世界リセット）は `CLAUDE.md` に残してある。**

- ~~**見た目からの相手分類。**~~ 2026-09-03 に実装（段階10）。**2026-09-04 に申し送りを閉じた。** 学習そのものは6倍の価値になった（`learnGain` +3.04 → +18.00 \*\*\*）が、**体つきを読んだぶんは 11% → 0% に消えた**（相関 0.22 → 0.08）。「世界が豊かになれば信号が強くなる」という当時の期待は**逆に外れた**。**理由は `Appearance` の定義が予算制と噛み合っていないこと**——体力と速さは「攻撃力を見せないため」に選ばれたが、予算制ではその2本は**攻撃力と同じ財布を奪い合う**ので、隠すために選んだ変数がまさにそのために予測力を失っていた。**見えるものを「総量」にすれば信号は実在する**（相関 0.50、体つきの利得 1.24 \*\*\*。`LooksShowBulk` で試せる。総量は見せても配分は隠れるので攻撃力は隠しパラメータのまま）が、**推定の精度は上がらず個体数を 8.8 \* 払う**ので既定は変えない。
- ~~**仲間・協力。**~~ **2026-09-06 に保留から出した（段階32）。** 外敵は 2026-09-01 に実装済み（`ENEMY.md` 3節）で、**群れ狩りは動機・獲物のサイズ・栄養価をどれだけ振っても現れず**（`packSize` 1.2〜1.5）、段階11 が名指しした残りの欠けもの（狙いを合わせる手段）の前提だった段階9・段階12 はどちらも通った。**作るのは協力性遺伝子ではなく、行動語彙1つ（「誘う」）と好意の使い道1つ**——受けるかどうかは**既存の効用比較が採点**し（閾値は置かない＝段階25 の原則）、**好意は参加ボーナスではなく「土壇場で離脱しない確からしさ」としてリスク見積もりを割り引く**（既存の `AffinityTrust` を戦闘へ広げるだけ）。**AIにも同じ語彙を持たせる**（AI同士で働いて初めて群れ狩りを測れる）。対になる **段階31＝目撃した殺害の伝播**（人間ならリスク推定、外敵なら好意という符号違いの1本。**重みは `ActObserve` 1回分より粗く**＝観察という選択肢を殺さないため）。**順序は 31 → 32 → 29 → 30 → 21 → 22**（`TODO.md`。決定事項#55・#56）。
- ~~**記憶容量に選択圧をかける経路。**~~ 2026-09-04 に決着し、**探すのをやめた**。段階9・12・15b と3度測って `shMemory` は 0.07〜0.08 で動かず、4度目に**掃引して形を見たら平らではなく右下がり**だった（記憶6人で個体数79.9、12人で77.7、24人で73.8、無制限で54.1）。**記憶がこの世界で主に生産するのは「友人」で、好意は休息を安全に見せるので net で損**——`shMemory` 0.07 はバグではなく**正しい値付け**。**「役割があること」と「得であること」は別**という段階9 の観察に、ここで理由がついた。なお段階15b が書いた診断（地域が少なすぎて容量が縛られない）は**決定事項#41 と矛盾しており**（地域の記憶は容量の枠に入れないので、地域を増やしても容量は縛られない）、実測でも否定された。
- ~~**効用式の中の仮定を個体が経験から学ぶこと。**~~ 2026-09-04 に実装（段階12a。下記「現在の機能」を参照）。**仕組みは全部入ったが、信念の学習は既定でオフ**——世界の真の殴り返し率は 0.15 しかなく、`retaliation = 0.7` という思い込みが世界を支えていたため、真実を知ると個体数が半分になる。段階12b で本物の抑止力が入ってから測り直す。
- ~~**種族の汎用化・複数食料種・死体ドロップ。**~~ 2026-09-01 に実装（下記「現在の機能」を参照）。当初の想定と違ったのは、**共通食料A/Bの2種類は作らなかった**こと（AとBを区別するルールが無い）。以下は当時のメモ: 人間・外敵は同じ汎用ノード構造・同じ意思決定エンジンを共有し、パラメータの範囲（種族ごと）と種族固有遺伝子だけで種族差を表現する。食料は3種（共通食料A/B＋死体からの肉ドロップ）。人間は人間由来の肉を食べない（共食い不可）が外敵は種族を問わず肉を食べる。詳細は実装着手時に `NODE.md` / `ENEMY.md` / `PARAMETERS.md` へ書く。
- ~~**遺伝子ベクトルの拡張と学習パラメータ（文化）。**~~ 2026-09-04 に実装（段階12b・12c）。**双方向の等価交換もヒントも狙いどおり入った**が、**1回あたりの歩み寄りは想定よりずっと小さくないと世界が壊れる**（交換は1実行に19000回起きる）。以下は当時のメモ: 遺伝は `Genome []float64`、学習は別ベクトルとして分離する。文化は専用データ構造を作らず、近接個体間の模倣則によるパラメータのクラスタ化として創発させる。模倣は一方向の学習ではなく**双方向の等価交換**とし、伝わる中身は固定名のスカラーだけでなく**個体ごとに可変な「ヒント」**（状況特徴量 × 対象行動 × 重み。効用への加減点としてのみ働き、行動は直接決めない）も含む。「天才」は学習パラメータのどれか1つを通常の突然変異よりずっと大きく飛躍させる特殊イベントとして実装し、**ヒントの構造の変異にも同じ仕組みを流用する**（新しい概念は作らない）。詳細は `PLAN.md` の段階9・段階12。
- ~~**魚を生き物にする（攻撃力の小さい第3の種族）。**~~ **2026-09-13 に「作らない」で決着した。** **水中の脅威は段階63 が別の形で入れ**（`EnemyKind.Water`＝種類の表に1行）、**残っていた目的は「2つの釣りスキルの区別」だけ**になった。**代価が見合わない**——決めることが多く、**段階42・43・34・36 の記録が全部引き直しになる**うえ、**先読みが既定になってベースラインを一度リセットしたばかり**で、同じ記録を短期間に二度引き直すことになる。以下は当時の記述:  **2026-09-10 に「これが段階42・43 の本来の意図だった」と確認したが、保留にした**（`PLAN.md` の #78 と保留の節）。**今の魚は食料アイテム**で、自分では動かず（掴み損ねたときだけ水中のどこかへ跳ぶ）、攻撃力も意思決定も持たない。**そのため2つの釣りスキルは、意図されていた「寄ってきた魚を捕る／こちらから魚に寄って捕る」の区別ではなく、どちらも止まっている的への成功率の掛け算**になっている（**「誰が距離を詰めたか」という区別がコードに無い**）。**種族を足すこと自体は段階11 の汎用化があるので安い**が、**決めることが多く**（魚は何を食べるか／繁殖するか、それとも今の湧き方のまま世界が供給するか／死体は `FoodFish` か `FoodMeat` か／`hunts`・`killShare`・種族ごとの個体数・`dietVariety` の意味が全部変わる）、**段階42・43・34・36 の記録が全部引き直しになる**。**復活の条件は段階50・51 のあとに1段階まるごとの枠を取れるとき**
- ~~**地形（川・高低差・スロープ・穴・崖）。**~~ 2026-09-06 に実装・測定（段階20）。**既定は平坦のまま**（`TerrainMap` が空）で、地形は世界を始める側が渡すもの。詳細は下記「現在の機能」。**続きを2つ立てた**（`PLAN.md`）: **段階29＝地形コストの認識と伝承**（段階15b/15c の地域ごとのベクトルに1本足すだけ。**減衰は0**＝地形は静的な事実。**粒度は12ブロックのまま**＝今のコントローラは経路計画を持たないので、そこが解像度の天井。決定事項#53）と、**段階30＝高台の遮蔽（回避率）**（**攻撃者との高さの差分**を見る初めての規則。**予防的な行動に要る「高い場所は安全」は `engine/lore.go` 側の世界の事実**で、地域ごとの信念とは別枠。**既定オンだと決めつけない**——段階12a の `retaliation` に近い性質だから。決定事項#54）。**順序は 29 → 30 → 21 → 22**（`TODO.md`）。
- **2026-09-20：地域をタイルで塗れるようにした**（`TODO.md` の1番の続き。ユーザの問い「地域レイヤーが『オブジェクト層』である必要は？」から）。【**`Config.RegionMap`（1セル1文字）＋ `RegionShape.Key`（凡例）**。**Tiled では `region` プロパティを持つタイルで塗った層**で、**同じ名前のタイルは離れて塗っても1つの地域**。**塗った地域はどんな形でもよい**（L字・分かれた谷）＝**矩形にできない唯一のこと**。**矩形も残す**（「西の4分の1」を1行で言えるため）が、**同じセルについて両方が言っていたら塗りが勝つ**。**タイル層の役割を順番ではなく中身で決めるようにした**（地形だけが最初の層。**2枚目＝発生タイルのままでは、地域だけ描いた地図が誤読される**）。**索引は 200×200 の表のままで O(1)**＝**矩形が安かったのではなく、索引が最初から形を選ばなかった**。**世界の規則は増えていない**（塗らない世界は1ビットも変わらない）。**あわせて `World.Regions()` の枠を実際の位置から作るようにした**——**描いた世界では格子の枠を返していて嘘だった**（塗られた地域の枠は「囲む箱」で、正確な答えは `regionIndexAt` だけ）。**`cmd/devview` は描かれた世界を格子状に問い合わせて塗る**（`World.DrawnRegions()`）＝**L字が L字として見える**】

- **2026-09-20：材料と加工（`TODO.md` の8番）。**【**`FoodHide`＝この世界で初めての材料**（`HidePerBudget` / `WardNeedsHide` / `EnemyKind.Hide`。**既定はオフ**）。**獣の死体からだけ落ち、防寒着は皮からしか作れない**——**#115 が名指しした最後の梃子「自給自足」を断つ唯一の形**。**値打ちは1か所**（`craftWant` × `CarryValue`＝石が「まだ投げていない投擲」であるのと同じ形）で、**拾う・持つ・売る・買う・贈る・作るが同じ式を読む**。**実装前に数えた**: 外敵の死は1実行に 59.4 回、人間が関与したのは 22.1 回、今の防寒着は 3104着＝**材料にすると供給は桁で落ちる**（だから用量を3つ測った）。**測定: 供給は狙いどおり落ち**（3104 → 144 / 367 / 20.5、着ている割合 0.67 → 0.15）、**市場は開かなかった**（`sales` 2.33 → 1.00 \*\*\*、`coatsSold` 1.75 → 0.33 \*\*\*）。**一番の発見は外れた予想のほう**——**`hidesSpare` 0.53〜0.89 ＝この世界で初めて売り手が余りを持ち、それでも売れた皮は 0.08 枚、贈られた皮は 47〜130 枚**（`HandOverCheapest` を足しても `hidesSold` は12シード全部 0.00）。**段階68 以来の「足りないのは売り手」がここで外れ**、**防寒具を商品にする案を閉じた**（5度目）】

- **2026-09-20：指1本で遊べるようにした**（`TODO.md` の9番）。【**タップで狙い、同じところをもう一度タップで既定の行動、長押し（0.5秒）でメニュー**。**`engine` は1行も変えていない**（入力の層だけ）。**指とマウスは1つの層**（`pointer`）で、**クリックは押した瞬間から離した瞬間に変わった**。**既定の行動は4つ**（食料＝食べる／外敵＝攻撃／人＝観察／地面＝歩く。**人を2タップで殴らない**）。**メニューは質問にも使う**——後継選び・求愛の返事・質問・節目の4つが指で答えられるようになり、**新しい訊き方は足していない**。**判定は時間で、フレーム数ではない**（遅い端末で長押しが1.5秒になる／**ブラウザで1秒に4フレームしか来ない状況を実測した**）。**WASM ＋ 合成タッチイベントでブラウザ確認済み**。**仮想時間ではフレームが来ないので、ページ自身に `requestAnimationFrame` の中で撮らせて POST する**】

- **2026-09-20：円をやめて絵で描くようにした**（`TODO.md` の10番）。【**`cmd/tiles` が絵を焼き、`cmd/devview` が1枚のシートから貼る**。**絵は `cmd/tiles/art.go` に1文字1ピクセル**（地形や天気と同じ書き方）。**灰色で描いて貼るときに `ColorScale` で色を付ける**ので、性別・個体差・物の種類が1枚で足りる。**4状態×2コマ、向きは左右反転、大きさは体力上限**。**円が持っていた読みは全部残し**、**塗り＝残り体力だけ緑の帯に**。**ファイル名に中身のハッシュ、manifest を読んで全部揃ってから開始**。**ネイティブは埋め込み・ブラウザは fetch**（15MB の wasm と 1.4kB の絵は変わる頻度が違う）。**画面の外は描かない**（遊ぶズームで 270 のうち 52）。**測定: ブラウザで 11.0 → 14.4 fps と、絵のほうが速い**（vector は Go 側で三角形に分解するため）。**`-circles` で以前の描き方に戻せる**】

- **2026-09-20：飾り物は満たされる（段階89）。**【**段階16 の食べ飽きと同じ帳簿1本**（`AdornSatiety` / `AdornForgetPerTick`。**既定はオフ**）。**数えるのは「持っていること」**なので**腐敗が需要の再来になる**。**実装前に数えた**: 1人あたり2.71個・43%が0個・一生2475tick に対し品は2000tick。**測定（平坦な世界・12シード）: 保有数 2.58 → 1.89 \*\*\*、作る決定 0.04 → 0.02 \*\*\*、欲しさ 1.00 → 0.62 \*\*\* ＝自己制限は起きた**。**しかし売買は動かず**（+0.25 ± 0.45）、**個体数は下がった**（−9.9 〜 −23.8 \*）——**用量ではなく贈与が減るから**（段階82 の「渡せることで元を取る」の裏返し）。**あわせて分かったこと: 遊ぶ地図は今の既定では 42%（飾り物入りで58%）崩壊するので、この群は平坦な世界で測った**】
