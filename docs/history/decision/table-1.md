# 決定事項の表 1——#1〜#86

**予算制と9遺伝子、継承則、天才、防御と回避、成長と老い、魅力と好意と伝承、あるべき状態、地形、ゲーム化、物と交換。**

**`PLAN.md` から 2026-09-21 に切り出したもので、行は1文字も変えていない**（`docs/history/plan/` と同じやり方）。**表の見出し行（`| # | 決めたこと |`）だけは、切った先でも表として読めるように両方に置いてある。** **索引は `PLAN.md` の「決定事項」。**

**番号はこの表だけの通し番号**で、`#88` のように他の文書から番号で参照される。**`#133` 以降は表に行が無く、`PLAN.md` の各節の中に「決めたこと」として書いてある。**

---

| # | 決めたこと |
| --- | --- |
| 1 | **遺伝子は予算制。9種。** 攻撃力・防御力・最大体力・移動速度・回避率・記憶容量・理性・頭の良さ・**魅力**。強くなるには別の何かを削る |
| 2 | **予算（合計値）は親から継ぐ（2026-08-30 確定）。** 子の予算＝**親の予算 ＋ 正規乱数**（平均0・標準偏差 `BudgetInheritSpread`）。**どちらの親の予算から引くかは 50% のランダム**（両親の平均は取らない。2026-08-30 確定）。初期個体だけ `GeneBudgetMean` ± `GeneBudgetStd` で抽選する。同世代でも「単純に恵まれた個体」が存在し、その恵まれ方が子にも受け継がれる。**当初の「出生ごとに抽選＝予算は遺伝しない」（2026-08-18）から変更**。切替は `BudgetHeritability`（0＝毎回抽選＝旧仕様、1＝親から継ぐ＝既定）で残し、両方回せるようにする |
| 3 | **天才は2段階。** 稀に（10年に一度）、さらに稀に（100年に一度）異常に大きな予算を引く。**#2 の変更により、天才の予算は子孫にも部分的に残る**（親の予算 ± 幅で継がれるため）。維持コスト（価格）を課すかどうかは、**予算の平均が世代とともに単調に上がるかを測ってから決める**（下の帰結と未決を参照）|
| 4 | **継承は染色体式＋幅。** 遺伝子ごとに親のどちらかの値を引き継ぎ、正規乱数を足す。突然変異は `MutationRate`（既定1%）でだけ起き、そのときの幅は大きい（`MutationStd ≈ 40`）。**分配は遺伝子ごとの独立分配（メンデル的独立分配）で確定。性染色体にはしない**（2026-08-30。連鎖群も作らない＝遺伝子どうしは相関しない） |
| 5 | **ラマルク継承を採用。ただし `LamarckRate` で切替可能にする。** 0＝ダーウィン（生まれたときの値を継ぐ）、1＝完全ラマルク（学習後の値を継ぐ）。両方回せないと、測っているのが選択圧なのか学習速度なのか分からなくなるため |
| 6 | **防御と回避は払う資源を変える。** 防御＝ダメージ乗数を下げる／**体力**で払う。回避＝一定確率で完全無効／**行動機会**で払う（回避したtickは攻撃・食事ができない）。同じ資源で払わせると2つを分離して測れない |
| 7 | **effort の配分はプリセット3種**（攻め寄り／守り寄り／逃げ寄り）。連続値にすると候補数が爆発する。戦闘系の行動にだけ掛ける |
| 8 | **記憶容量を有限にする。** 容量は遺伝子（予算の配分の一つ）。容量が有限でないと忘却曲線がただの損になり、0に進化して終わる |
| 9 | **見た目からの推定は各ノードが学習する。** `AgentView` に真値は載せず、遺伝子から作った観測可能な相関値（`Appearance`）＋観測ノイズだけを載せる。「見た目→能力」の写像は engine が持たず、各ノードが自分の経験から学習する |
| 10 | **明示的な「協力」は却下。** 協力という行動も協力性遺伝子も作らない。外敵と「倒した者だけがドロップを得る」という世界側の事実だけを置き、打算は既存の効用式にやらせる。**進化が進んでも一向に協力が現れない場合にだけ、明示的な機構の導入を再検討する** |
| 11 | **空間グリッドを入れる。** ただし「広域探索の高速化（挙動不変）」と「視界をグリッド単位にする（挙動変更）」は別段階に分ける |
| 12 | **魅力を遺伝子にする（2026-08-19）。** 予算を食うが生存には効かない純粋な装飾。**既存の `fitness`（パワー35%＋理性15%＋知能15%＋体力35%）を置き換える**（能力に2役持たせない）。実能力とは**相関させない**（見た目→能力の写像は段階10 で各ノードが学習する。engine が先に答えを書かない）|
| 13 | **「魅力の高い相手に近づく」は行動として書かない（2026-08-19）。** 求愛の効用は既に `OffspringValue × 相手の推定魅力/100` なので、魅力が上がれば接近しやすくなるのは今の効用式のまま起きる。足すのは**再判断トリガー1本**（「魅力の高い個体が視界に入った」）だけ |
| 14 | **集団は「好意」と「伝承」から創発させる（2026-08-19 ／ 2026-08-30 改訂）。** 集団IDも所属行動も作らない。**好意（正の記憶）**を持たせ、そこから学べる（伝承）ことを効用の正の項にする。**集団間の敵対は「よそ者には正の項が無い」ことだけから出す**（憎悪パラメータは作らない＝原則「恨みの状態を持たない」を守る）。**当初あった「無害に共在した時間を好意にする」は 2026-08-30 に不採用**（#19）|
| 15 | **伝えるのは effort そのものではなく、effort を決める前提（2026-08-19）。** effort はプリセット3種＝効用比較の結果（#7）なので、配分を直接コピーさせると効用式と競合する第二の意思決定系ができる。段階12 の学習対象（`retaliation` / `pAccept` / `RiskWeight` / `CompetitionWeight`）を伝承の対象にすれば、「あの集団は守り寄りの effort を選ぶ」は結果として現れる |
| 16 | **「あるべき状態」は受け入れ基準であって仕様ではない（2026-08-19 ／ 2026-08-30 改訂）。** 目標を engine の機構として直接実装したら（集団IDを持たせる等）、「集団があるのは我々がそう書いたから」になり、#10 と同じ理由で世界を測る意味が消える。**目標の数値は段階6.5 で現状を測ったうえで、段階11（外敵）の実装後に書く**（#17）|
| 17 | **段階11 より前は集団が形成されなくてよい（2026-08-30）。** 今の世界には他個体の存在そのものが自分の得になる経路が無く、原理的に集団化しない。よって段階6.5 で目標数値を決めるのは早すぎる。**測定インフラは段階6.5 で先に作り、目標数値の記入は段階11 の後に回す** |
| 18 | **敵意（脅威記憶）は新設しない（2026-08-30）。** 距離・接近ベースの新しい敵意計算は採用しない。**既存のリスク記憶**（自分がその相手に払わされた体力の累積・遅延減衰つき）**をそのまま敵意として扱う**。新しい状態を増やさない |
| 19 | **好意は実イベントからのみ生まれる（2026-08-30）。** 距離・滞在時間ベースの好意計算（視界にいる時間に比例して加点する案）は**不採用**（#17 により急ぐ理由が無くなったため）。発生源は4つだけ: **ペア形成・出産**（既存の `PairBondDuration` 等で成立する関係そのもの）／**血縁**（`ParentIDs` / `ChildIDs` を使った親子の初期ブースト）／**共同戦闘**（段階11。集団化の主エンジン）／**継承**（出産・模倣のどちらでも、実際に何かを受け取ったときに加点）|
| 20 | **深い伝承は既存の `observe` に相乗りさせる（2026-08-30）。** 新しい観察の仕組みは作らない方針を維持する。`observe` が既に持つ体力コストと時間コスト（`observeTicks`）をそのまま流用でき、追加のコスト設計は要らない。排他性はまず `observe` の自然な排他性（1度に1人しか観察できない＝**発信側**の帯域制限）だけに任せる。**受信側（人気者）のハブ化はこれでは防げない**ので、被参照数の分布を測ってから相互占有の追加を判断する |
| 21 | **伝承は等価交換（2026-08-30）。** 一方向の「学ぶ」ではなく**双方向・同時の歩み寄り**にする。学習対象ごとに、値が違えば互いに相手の値へ少しずつ近づく。**好意の加点量は実際に動いた値の大きさに比例**させる。すでに値が近い相手との交換は自動的にほぼ無価値になる（特別な判定を足さない）。副次効果として、学び尽くした相手は交換価値を失うのでハブ化が自然に緩む |
| 22 | **接触だけで減衰の起点をリセットする（2026-08-30）。** 減衰式 `exp(−減衰率×経過tick)` 自体は変えず、**「経過tick」の起点（最後にいつ更新されたか）の扱いだけを変える**。値が動いたときに加えて、**接触した事実だけでも起点を更新する**（等価交換のチェックが走れば、差がゼロで不成立でも更新）。**好意・リスク記憶の両方に適用**する。新しいトリガーは要らない |
| 23 | **伝承される中身は固定パラメータではなく「ヒント」（2026-08-30）。** `retaliation` のような固定名のスカラーだけに限定せず、**個体ごとに可変なヒントの集合**へ拡張する。ヒントは〈状況特徴量 × 対象行動 × 重み〉の3つ組で、**行動を直接決めず、既存の効用比較への加点／減点としてのみ働く**（原則を壊さないことが必須条件）。**枠は遺伝子予算を消費する。** 重みの微調整＝通常の突然変異、構造の変化＝既存の天才イベントの流用 |
| 24 | **初期個体の配分はディリクレ分布で引く（2026-08-30）。** α ≈ 0.5〜1（多少の偏りが出る設定）を出発点にし、値は `cmd/experiment` で振って調整する。**一様乱数を正規化する方式は不採用** — あらゆる配分が等確率にはならず、**1本に予算が集中するような極端な配分が出にくい方向にバイアスがかかる**（中央に寄る）。α で偏りの強さを直接いじれるディリクレのほうが「多少の偏りも生まれる初期集団」に合う |
| 25 | **魅力と実能力は無相関のままにする（2026-08-30 確定）。** ハンディキャップ原理（強い個体しか高い魅力を維持できない）は**今は導入しない**。無相関にしておく狙いは #12 のとおりで、「装飾は嘘をつけるのか」を段階10 の学習に答えさせるため。engine が先に相関を書いてしまうと、その問いの答えを engine が与えたことになる。**ただし性選択圧は `CompetitionWeight` と同格の危険パラメータとして扱う**（下の帰結を参照）。実装後に魅力への配分が暴走して他の遺伝子を圧迫する／個体数が崩れるなら、そこで初めて (a) ハンディキャップ原理側の設計に切り替える か (b) 魅力の効き方（`pAccept` の重み）を弱める、のどちらかを検討する |
| 26 | **好意は既存の「意見」レコードの1フィールドにする。専用の枠は作らない（2026-08-30 確定）。** 記憶容量は**相手ごとに1レコード**（`Opinions(id)` が返す既存のレコード＝リスク記憶＋強さ推定）という単位で管理し、好意はそこへ**新しいフィールドとして足す**。「その相手を覚えている」こと自体が1枠を消費し、リスク記憶・強さ推定・好意はその1枠を共有する（**脅威として覚えても味方として覚えても、消費する枠は同じ1つ**）。容量は既存の9遺伝子の**記憶容量**でそのまま管理し、**好意専用の遺伝子もパラメータも追加しない**。したがって**1ノードが同時に好意を持てる人数＝集団の実質的な最大メンバー数は、記憶容量の遺伝子値がそのまま決める**。好意と脅威を別々の容量で管理する別枠案は、遺伝子とパラメータが増えて測定も複雑になるため不採用。**実測で集団サイズが明らかに不自然（小さすぎる／大きすぎる）と分かった場合にだけ、分離を検討する** |
| 27 | **血縁ブーストは直系の親子（1ホップ）だけ。世代をまたぐ減衰ルールは作らない（2026-08-30 確定）。** `ParentIDs` / `ChildIDs` を1ホップだけ辿り、親子の双方に初期好意を与える。**祖父母・孫・きょうだいは血縁として扱わない** — それらの関係は他の発生源（共同戦闘・継承／模倣・接触による起点リセット）に委ねる。血統を何ホップも辿る処理も、ホップ数に応じた減衰係数も持たない（**新しい状態も新しいパラメータも増やさない**）。近縁者が結果として結びつくなら、それは育児期間ぶん近くにいて実イベントが多く走るからで、engine が血統距離を計算したからではない。**ブーストの大きさは初期値を仮置きし、`cmd/experiment` で調整する** |
| 28 | **成長・老い・自然死を前倒しで実装する。`TicksPerYear` は本決めにする（2026-08-30 の方針転換）。** `CLAUDE.md` では「将来実装・年齢補正は当面つねに1」として先送りしていたが、**育児期間（段階7d）の設計に成長の概念が不可欠**なため、ここで前倒しする。実装の形は当初方針どおり「**遺伝的素質 × 年齢補正**」で、**成長（0→1に伸びる）と老い（1→減衰する）は同じ1本の関数の両端**として扱う（別々の仕組みにしない）。あわせて `TicksPerYear` を「老い実装待ちの暫定プレースホルダー」から**本決めの値**に切り替える（#31 の自立期間と実行長の制約から決める） |
| 29 | **子育て中の食糧確保は「保護」だけ。給餌は作らない（2026-08-30）。** 親が子に食料を渡す新しい仕組み（給餌アクション・食料の受け渡し）は**実装しない**。子は自分で食料を得ようとするが弱いので、**親が近くにいて、既存の戦闘・先制排除の仕組みで競合相手を排除する**という間接支援だけにする。新しい行動もルールも増やさない。**これで足りるかは実装後に測る**（子の餓死率・自立到達率）。不足するようなら、そのとき初めて給餌の追加を検討する |
| 30 | **成長は食事で進む。既存の `eat` に相乗りさせる（2026-08-30）。** 「成長する」という新しいアクションは作らない。食料を摂ると年齢補正の成長側が進み、**満腹（空腹度0＝100%）に達するとそれ以上は成長に乗らない**（頭打ち） |
| 31 | **寿命は過小・過度の両側で削れる。閾値は意図的に別の値にする（2026-08-30）。** **過小**＝空腹が一定時間続くと、既存どおり体力を削るのに加えて**寿命も削る**。**過度**＝摂取が「満腹」ではなく**「腹八分目」（80%程度）を超えた時点**から寿命を削り始める。**成長の頭打ち（満腹＝100%）と過度コストの開始点（腹八分目＝80%）をずらすことが設計の要**で、この差分から「**成長中の個体は多少寿命を削ってでも満腹近くまで食べるほうが得、成長を終えた個体は腹八分目で止めるほうが得**」という行動差が、**特別な場合分けを書かずに効用比較から出る**。「満腹に近いのになぜ食べるのか」も新しい判断閾値ではなく**既存の `rival` の項**（競合相手に渡さないために寿命コストを払ってでも食べる）で説明する。理論的な位置づけは **disposable soma 説の簡易近似**（下の帰結を参照） |
| 32 | **休息を無防備にする。よそ者の近さを効用のリスク項に入れる（2026-08-30）。** 今の `ActRest` は Life の項だけを持ち、リスクがゼロ（＝いつどこで休んでも同じ）。ここに「**対象の近くにいる、好意を持っていない他個体の存在**」によるリスクを足す。**好意は既存の相手ごとの記憶レコード（強さ推定・リスク記憶と同じ枠。#26）を参照するだけ**で、新しい状態も新しい強さ評価器も追加しない（リスクの大きさは既存の `EstStrength` とリスク記憶と距離から作る）。**好意のある個体が近くにいる場合はこのリスクを乗せない、または割り引く。** 実装は**既存の候補生成の走査（`Decide` が `p.Others` を1度なめる処理）に相乗り**させ、**追加の探索コストを発生させない**。重みは `Config` に出して **0 で完全に切れる**こと（今の挙動に戻せる＝A/B の対が取れる） |
| 33 | **慢性的な低体力も寿命を削る（2026-08-31）。** 体力が20%を下回った状態が一定時間続いたら、既存の空腹による寿命コスト（#31 の過小側）とは別に寿命を削る。**`ShockRisk`（今感じられる危険。効用式に載り意思決定に使われる）とは別物**として扱い、この寿命コストは**意思決定に一切関与しない静かな長期コスト**にする。カウントは**体力が20%を上回った時点でリセットする単純な版から始め**、生涯の累積コスト（リセットしない版）にするかは実装後の測定で判断する。**攻撃側有利・休息の無防備さ（#32）と組み合わさると、弱い個体への不利が複合しうる**ため、導入時は必ず `cmd/experiment` で個体数を確認する |
| 34 | **数手先の見積もりはミニマックス探索ではなく役割分担で行う（2026-08-31）。** 将棋のような分岐探索（候補ごとに数手先まで枝を広げる方式）は不採用。理由は2つ：候補数（現状約40）が手数ごとに掛け算で増え `BenchmarkDecide`（現状約8.0µs）が桁違いに重くなること、そしてこの世界は相手も適応的に動く・相手の真の能力は隠しパラメータという二重の不確実性があり、深い事前計画より**トリガー方式による頻繁な考え直し**のほうが動的な多主体環境では有効なことが多いこと。代わりに (a) **閉じた式による数tick先の見積もり**（既存の `PlanHorizon`／`ShockRisk`／`SkirmishTicks`／`fleeExposureTicks`／`recoverable` と同じパターン＝代替案に分岐せず「今の状態がこのまま続いたら数tick先どうなるか」を1本の軌道として見積もる）を、設計者が式を書き下せる新しい場面（休息の無防備化の被害見積もり、育児中に子から離れるリスク、好意のない他個体への接近リスクなど）に広げるのと、(b) **設計者が式を書き下せない・気づいていない関係はヒント（#23）に発見させる**（評価は個体1回の決定ではなく世代を超えた集団規模の生存競争が行う＝探索コストを「今この瞬間の1個体」から「時間をかけた進化」に移す）の2つを役割分担させる。**寿命はこの見積もりの対象に含めない**（ノードは寿命を知覚せず、効用式にも寿命の項を作らないという既存方針を優先する）。両方とも同じ `Utility.Total()` に加算される項として実装し、アーキテクチャを分けない |
| 35 | **深さ・ヒント枠を能力でゲートしない（2026-08-31）。** 頭の良さ・理性が高い個体だけに、より深い見積もり（#34 の(a)）やより多くのヒント枠（#23・#34 の(b)）を解禁する設計はしない。2026-08-14 の測定で `StrategyDepthUnlock`（知能が高いほど戦略の選択肢が広がるゲート）が逆効果（愚かな個体を損な手から保護してしまい、知能への選択圧をむしろ下げた）と分かり既定で無効化した経緯があり（`CLAUDE.md`・`HISTORY.md`）、#34 を実装するときも同じ罠を繰り返さない |
| 36 | **休憩場所は既存の休息露出に相乗りさせる（2026-09-03）。** 新しい仕組みは作らない。実装・較正済みの `RestExposureWeight`（休息中の被害見積もり・既定 0.05）に、**場所ごとの割引率を1つ掛けるだけ**にする。区画は #38 の地域ブロックをそのまま使い、休憩場所専用の区画分けは新設しない。#34 の (a)「閉じた式による数tick先の見積もりを必要な場面に広げる」の延長として扱う |
| 37 | **睡眠を独立した状態として実装しない（2026-09-03）。** 睡眠欲求（疲労度）のような新しい状態量も、強制的な `ActSleep` も作らない。休息はすでに体力回復のために事実上避けられない行為で、`RestExposureWeight` との組で「**休まざるを得ないが休むと危ない**」という緊張はすでに生まれている（段階9 の実測＝休まない世界では消耗死 +45%・`PairBondDuration` の途中でペアが壊れる。`HISTORY.md` 追記26）。**今後の測定でこの緊張が弱すぎると分かった場合にだけ**再検討する（保留欄） |
| 38 | **「地域」は空間分割グリッドを粗くまとめたブロック1種類だけにする（2026-09-03）。** 休憩場所（#36）・植物食料の豊かさ（段階15）・将来の「地域ごとの世界リセット」が、**すべて同じ「地域」という語彙の上に乗る**形にする。用途ごとに別の区画分けを作らない |
| 39 | **「豊かな地域は集団で守られる」という結果を仮定しない（2026-09-03）。** `rival`（先制排除の動機）は周囲の食料希少度に比例するので、**豊かな地域ほど希少度が下がって先制排除の動機が消える**可能性がある。その場合に出てくるのは資源防衛理論どおりの「**内側は平和で、乏しい地域との境界が一番荒れる**」構造で、これも成果として受け取る。判定は段階11 の群れ狩り検証（`PreyValue=0` の対照条件）と同じ要領＝**集団形成に関わる動機だけを切った対照条件**との比較で行う |
| 40 | **地域の豊かさに、新しい知覚も新しい記憶の鮮度管理も作らない（2026-09-03）。** 推定は既存の強さ推定・魅力推定と同じ `(100−理性)/100 × ノイズ`（推定する対象が1つ増えるだけ）。植物の分布は動的になりうる（段階17）ので**静的な地図にはせず**、既存の「読み出し時の遅延減衰」＋「接触で起点をリセット」（#22）をそのまま使う。伝承は等価交換（#21）に乗せ、`retaliation` / `pAccept` / `RiskWeight` / `CompetitionWeight` と並ぶ**学習パラメータの1つ**として地域IDごとのベクトルを足すだけにする。**垂直伝承に「教える」行動は作らない**——育児期間（`RearingRadius` / `ChildRearingTicks`）の共在がそのまま経路になる。子の初期値は粒子的遺伝と同じ形で親の一方からコピーし、そこから先は自分の経験と模倣で更新する |
| 41 | **地域の豊かさの記憶は、相手ごとの記憶容量の枠には入れない。ただし遺伝子は共有する（2026-09-03）。** 相手の人数は理屈上いくらでも増えうるので容量で頭打ちにする意味があるが、**地域の数は地図の広さで決まる小さな固定数**なので同じ理由が当てはまらない。別の小さな構造として持たせる。そのうえで**同じ記憶容量遺伝子が、地域の豊かさの推定精度・更新速度にも効く**ようにする（枠数は共有しないが遺伝子は共有する）。段階9 で記憶容量への選択圧が出なかった（`shMemory` 0.07〜0.08）ので、**この経路が記憶容量への初めての実利的な選択圧になりうる** |
| 42 | **植物は軽量な遺伝子だけを持ち、`Decide()` を持たない（2026-09-03）。** 人間・外敵が共有する9本の予算制アーキテクチャは、戦闘も知覚も意思決定も無い植物には過剰なので**持ち込まない**。繁殖は個体の決定ではなく**既存の食料湧きと同じ確率的なイベント**として扱い、そこに既存の「粒子的遺伝＋まれで大きい変異」だけを相乗りさせる |
| 43 | **毒と警告信号は独立した2遺伝子にし、相関させない（2026-09-03）。** 匂いと目立ちやすさは別々の遺伝子にせず「**警告信号の強さ**」1本にまとめる（現実の apparency theory でも、見つかりやすさ・警告の強さ・防御への投資はセットで語られ、3つの独立した軸として扱われていない）。毒と信号を無相関にするのは #25（魅力と実能力を無相関にした判断）と同じ構造で、**正直な警告が進化するかを世界の側に決めさせる**ため。独立させておけば、毒を持たないのに毒があるふりをする個体（ベイツ型擬態）が本物の毒個体の評判に便乗する余地が開く。**これを狙って作り込まず、独立遺伝子にするだけで可能性を残す。** 毒を食べた側のコストは体力への直接的な損害またはその場での栄養価の低下、毒の有無は**隠しパラメータ**で、食べる側は信号から `(100−理性)/100 × ノイズ` で推定する（理性が高いほど信号を正しく読める） |
| 44 | **種子散布は風散布と動物摂食散布の2つだけ採用する（2026-09-03）。** 現実の5系統（風・動物付着・動物摂食・自己・水）のうち、**風散布**は他個体の行動と無関係に実装できるので最小構成として先に入れる。**動物摂食散布**（食べられて消化管を通り別の場所で出る）は既存の `eat` にそのまま相乗りできる（食べたとき一定確率で種が生き残り、食べた個体が移動した先で新しい株が生える**遅延効果**）ので採り、**動物付着散布は採らない**（「付着」という新しい状態を人間・外敵の側に足す必要があるため。現実の分類でも摂食散布のほうがメジャー）。水散布は川・水場が無いので**地形が入るまで見送る**。摂食散布は段階15・16 とも噛み合う——**人間がよく通う場所ほどその植物の種が運ばれる**ので、行動と分布が互いに影響し合う循環が新しいルール無しに生まれる |
| 45 | **栄養バランスに新しい状態量を作らない。そして隠さない（2026-09-03）。** 栄養素ごとの貯蔵量のような状態は作らず、**直近同じ種類の食料ばかり食べていると、その食料の `FoodNutrition` の効果が一時的に目減りする**という、自分の消費履歴だけを使う軽い割引として実装する。寿命（背景処理・`Perception` に載せない）とは逆に、これは**主観的に感じられる**もの（同じものばかりで飽きる）なので、既存の `Hunger` / `ShockRisk` と同じ「自分についてすでに知っていること」の延長として `Perception` にも効用式にも普通に載せてよい |
| 46 | **夜型・朝型（体内時計）は独立した段階にし、段階12 より後に置く（2026-09-03）。** 狙いは、個体ごとに活動しやすい時間帯が違えば**集団の中で常に誰かが起きている**状態が自然にでき、明示的な見張り役の割り当て無しに夜通しの警戒が生まれること（ハッザ族の睡眠研究＝個体ごとの体内時計のばらつきにより、集団全員が同時に眠っている瞬間がほとんど無かったという報告に根拠がある）。ただし**世界に周期的な時間帯という概念自体が無く、これを新設する必要がある**ので休憩場所・地形よりコストが大きい。休憩場所（#36）や地域差（段階15）とは**束ねない**。何が時間帯によって変わるか・遺伝子をどう割り当てるかは、この段階に着手するときに詳細化する |
| 47 | **人間操作は「もう1種類の `Controller`」として足すだけにする（2026-09-05）。** `AIController` のロジックには触らない。理由は2つ。バランスを測り終えた世界に手を入れないためと、段階0 で敷いた境界（engine は誰が操作しているかを知らない）を保つため。**プレーヤーにも `Perception` の外は見せない**——「相手の真の能力は隠しパラメータ」という原則は人間にも適用する（AI が読めないものを人に見せると、遊んでいるゲームが engine の世界とは別のものになる） |
| 48 | **「他所で育ったパラメータの読込」「進めた世界の配布」「天才の配置」に、別々の機構を作らない（2026-09-05）。** 3つとも「`World` / `Agent` / `Genome` の状態をファイルに出し入れする」の応用として実装できる。**`protocol` のスナップショット形式を机上で先に設計せず**、ここで実際にシリアライズを使ってみてから決める（手戻りが少ない）。ここでの実装は `protocol` そのものではなく、その**前段の実地検証**として位置づける |
| 49 | **地形は、エディタより先にルールを engine に入れて測る（2026-09-05）。** ルールが固まる前に器を作ると、ルールを直すたびに器も作り直すことになる。段階17（種子散布）で「段階15 の固定版は置き換えられる下地ではなく、集団が乗っていた土台だった」と分かったのと同じ轍。**測定はテスト用の決め打ちマップで済ませ、管理者用のエディタ本体はその後** |
| 50 | **`Config` を機械的に全部UIに出さない（2026-09-05）。** `FoodSpawnRate` / `CompetitionWeight` / `SkirmishTicks` / `ShelterSpread` のように、少し動かすだけで世界が崩れる値がある。**出す範囲を絞り、既知の安全域を外れる操作には警告を出す。** ゲームの難易度は予算（`GeneBudgetMean` / `GeneBudgetStd`）で調整し、**測定用の既定値とゲーム用のプリセットは分ける**（既定を動かすと以降の測定のベースラインが動くため） |
| 53 | **地形についての信念は、地域ごとのベクトルに1本足すだけにする（2026-09-06）。** 荒れ地・水・高さを別々の信念に分けず、**その地域の平均移動コスト倍率という1スカラー**にまとめる（毒と警告信号を別々に持たせなかった判断と同じ発想）。**粒度は既存の12ブロックのまま**で、`TerrainMap` 本体の解像度には合わせない——今のコントローラは分岐しない閉じた式で方角の得を見積もる設計で**経路計画を持たない**ので、**地域単位の粗さが今のアーキテクチャの解像度の天井**だから。**減衰は無し**（地形は管理者が書き換えない限り静的な事実）だが、**同じ関数を使い回して減衰率だけ0にする**——将来地形が動的になったとき（浸食・エディタでの書き換え）にすぐ有効化できるように |
| 54 | **【2026-09-08 に閉じた。30b は作らない】** **「高い場所は安全だ」は世界の事実であって場所の値ではない（2026-09-06）。** 高台の遮蔽を予防的な行動につなげるための知識は `engine/lore.go` 側（殴り返される度合い・求愛の受諾率と同じ学習可能な事実のベクトル）に置き、**地域ごとの信念（`engine/regionlore.go`）とは別の枠にする**。そして**既定オンだと決めつけない**——段階12a は「事実を正確に学ばせると個体数が半減した」（`retaliation` の間違った恐れが世界を支えていた）という結果を残しており、**高台の安全性は中立的な資源情報だった地域の豊かさより、戦闘に関わる事実という点で `retaliation` の前例に近い**。遮蔽そのものを単体で測ってから、学習・伝承版を別条件として足す。**——2026-09-08 の判断**: **この置き場所の指定こそが 30b を行き止まりにした**（場所に紐づかない事実は、どこへ向かえばよいかを言えない）。**#59 が「信念は帰結の種類ごと」と改訂したので、作るなら地域の信念の3本目**だが、**同じ形の2本目（危険）が段階35 で何も動かさなかった**ので作らない。再開の条件は `PLAN.md` の段階30 |
| 55 | **目撃した殺害は「観察1回分より粗い1回の観測」として既存の推定に載せる（2026-09-06）。** 新しい状態も新しい記憶の種類も作らない。**重みは通常の `ActObserve` より小さくする**——観察は体力と時間を払い、1度に1人という排他性まで背負っている行動なので、**目撃がただで同じだけ教えると観察という選択肢そのものが死ぬ**。完了条件に `ActObserve` の選択率を入れる。**対象が人間ならリスク側、外敵なら好意側**という符号違いの1本の経路にまとめる（別々の仕組みにしない） |
| 56 | **協力の誘いは「行動語彙1つ＋好意の使い道1つ」に留める（2026-09-06）。** #10 が却下した協力性遺伝子は作らない。**受けるかどうかは既存の効用比較が採点し、ハードコードされた好意の閾値は置かない**（段階25 と同じ原則）。**好意は参加ボーナスではなく信頼**——誘い手が土壇場で離脱しない確からしさとしてリスク見積もりを割り引く（既存の `AffinityTrust` を戦闘の文脈へ広げるだけ）。**プレーヤー専用にしない**——AI同士でも誘いが働いて初めて「群れ狩りが現れるか」を測れる |
| 57 | **地形と食料の相関は「世界の真値どうし」で作り、信念どうしを結ばない（2026-09-06）。** 豊かさの信念（15b/15c）とコストの信念（29）はすでに別々に個体の頭の中にあるので、**世界の側で2つの真値を相関させれば、個体は既存の効用比較を通して自分でそれを見つける**。**相関そのものを覚える・伝える第三の仕組みは作らない。** 総量は段階15a の原則どおり変えず（相関のあとで再正規化し、テストで固定）、粒度は段階29 の地域単位のコストにそのまま乗せる。**符号は混ぜない**——正（荒れた地域ほど痩せている）は段階29 の頭打ちが交絡かどうかに直接答える条件、負（荒れた地域ほど豊か）はリスクとリターンの駆け引きという別の狙いなので、**正で頭打ちが解けることを確認してからの独立した条件**にする |
| 58 | **地形ハザードは継続ダメージではなく「毎tickの確率で即死」にする（2026-09-07）。** 溺死に、体力を削り合う既存の戦闘の形を借りない。理由は3つ——(1) **既存のパワー・防御に新しい掛け算を足さずに済む**（能力の役割を重ねない、という `CLAUDE.md` の原則）、(2) ~~**速い個体は水に浸かっているtick数が少ない＝サイコロを振る回数が少ない**ので、新しい「泳ぎ」の能力を作らなくても既存の速さがそのまま報われる~~ → **2026-09-07 に取り下げ**。実装前に数えたら**個体は川を渡っておらず、川の中で暮らしていた**（平均滞在127tick）ので、サイコロの回数を決めているのは脚の速さではない。実測でも `shSpeed` は**下がる**（−0.01 \*\*）、(3) 川に入るかどうかは**閾値ではなく効用式**が決める（水を渡る候補の期待値に溺死確率を織り込むだけで、「入らない」というハードコードは作らない）。**残る (1)(3) で形は正当化できるので変えていない。** **死因には専用のバケツを新設する**——餓死・戦闘に混ぜると、溺死が他のすべての変更のA/B測定に紛れ込む（`starved` は「他の死因が名乗り出なかったぶん」なので、**排他性まで要る**＝殴られていた身体が溺れたら殺しには数えない） |
| 59 | **場所の危険は、地域ごとの信念の2本目として持つ（2026-09-07。#53 の部分的な取り消し）。** 「この地域で水を渡ることの危険度」を、段階29 の平均移動コスト倍率とは**別の1本**にする。#53 は「荒れ地・水・高さを1スカラーにまとめる」と決めていたが、**水は「渡りにくい」だけでなく「死ぬ」ので、コストに混ぜると命に関わる川とただ高いだけの荒れ地が区別できなくなる**。**保存先と減衰の扱いは #53 のまま**（`engine/regionlore.go`、減衰0、同じ関数を使い回す、粒度は12ブロック）。**最初から地域IDに紐づく**ので、段階30b が行き止まりになった理由（場所に紐づかない事実は、どこへ向かえばよいかを言えない）は繰り返さない |
| 60 | **目撃者スキャンの共通化は、2つ目の実例が出てから行う（2026-09-07）。** 段階31（殺害の周知）と段階35（溺死の周知）は**更新先が違う**（個体の評価と場所の危険）が、**視界内のノードを集める処理は同じ形**になる。ただし**先に抽象を設計しない**——1つの実例では、それが正しい抽象化かどうか検証できない。**31 を具体で書き、35 のときに2つ目の実例として共通部分（視界の定義・粗い重み）だけを切り出す**。同じ理由で、**将来のハザード（崖・底なし沼）のための汎用フィールドも今は作らない**——水を最初の1つとして具体的に持たせ、2つ目が出たときに共通化する |
| 61 | **川による集落の分化は、データ構造ではなく測定で確かめる（2026-09-07）。** 地域グリッド（`engine/region.go`）を川に合わせて描き直さない——**地形と地域が別々の地図であることは段階22 で確認した意図的な設計**なので崩さない。代わりに**既存の集団化指標**（`Clusters` / `ClusterGaps` / `MembershipTracker`）に**新しい2つ**（川をまたいだ遭遇率／両岸の遺伝子と地域信念の乖離）を足し、**川のある世界と無い世界の対**で読む。**これは実装項目ではなく、34〜36 を入れた最終形に対する観測**なので、順序としても最後 |
| 62 | **水辺の豊かさは、#57 の一般式に独立した項として足す（2026-09-07。保留を解く）。** 「水辺に独自の豊かさ」は**一般式と両立しない特別扱い**として保留していたが、段階33 が「食料と地形を同じ地図に乗せる」形を通したので復活させる。**ただしコストの項には混ぜない**——水は「渡りにくいから避けたい」と「近くにいると豊か」を同時に満たす必要があり、1本の移動コスト倍率に混ぜると符号が矛盾する。**地域に占める水の割合を独立した項**として世界生成時の豊かさに足し、**段階15a の原則どおり総量は保存する**（再正規化をテストで固定する） |
| 63 | **スキルは新しい成長軸ではなく、ヒント枠（#23）の中身のもう1種類にする（2026-09-07）。** 「使うほど上手くなる」は作らない——継承・観察・出生地のいずれかで**一度きり習得**し、効果は**本人の遺伝的能力との相関**で決まる。だから**実効値の掛け算は `Agent.Ability` の1か所だけ**という原則を壊さず、防御・回避と同じ「既存の式に乗る因子」として書ける。**表現は1カテゴリにつき連続スカラー1つで、離散の等級テーブルは作らない**（`StrategyDepthUnlock` と視力の離散化で2度踏んだ轍。表示上の初級・中級はUI側のバケット）。**習得・上書き・降格は「新しい値 > 今の値なら置き換える」という1つの比較演算**で3経路すべてを賄う——遺伝相関が実現される恩恵に上限を作るので、**降格に別のルールは要らない**。**習得確率のカーブと大きな跳躍は既存の天才イベント（`GeniusRate` / `GreatGeniusRate`）を流用する** |
| 64 | **採取スキルは競争（レース）ではなく効率（イールド）にする（2026-09-07）。** 「早く見つける・競争に勝つ」方向のボーナスにすると上手い個体が植物食料を独占しうるので、**段階16 の飽きペナルティ（同じものばかり食べると栄養が目減りする）への耐性**として効かせる。**この形なら上手い個体はむしろ少ない量で満たされる**ので、食料経済への負荷はむしろ下がり、独占の余地が構造的に無い。**回避・防御はスキルにしない**（段階8 ですでに独立した予算化された遺伝子＝役割を重ねない）。**戦闘全般もスキルにしない**——やるなら段階24 の「外すと隙ができる」に乗る狭い形（隙を見抜く）に切り出す（新しい候補手を解禁する形は `StrategyDepthUnlock` の再演） |
| 65 | **所持品は、状態量を増やす初めての例外にする（2026-09-09）。** 今の状態量は体力・空腹度・成熟度・寿命の4つで、段階16 の食事履歴すら「帳簿」（読み出し時に遅延減衰する記録）として状態軸を増やさずに済ませた。**所持品は帳簿にできない**——中身が世界のアイテムそのもので、拾う・落とす・渡すという可逆な操作を受けるから。だから5つ目として持つ。**ただし遺伝子は10本目を作らない**——ヒント枠が「空でも予算を食う」ことで9本の水準を一斉に下げた前例（段階12c。`budget` が +42.06 \*\*\* 動いて予算比で読み直しになった）があり、**10本目を足すと段階7c 以降の予算比の測定値が全部読み直しになる**。**容量は体力遺伝子から出す**（体力を買うことが運ぶ力を買うことになる＝予算からはすでに確保されている）。10本目にする腕は `CarryCapacityGene` の切替として残すだけにして、既定にはしない |
| 66 | **運搬の対価は重さで、分母は体力（Vitality）。パワーは使わない（2026-09-09）。** パワーは「体力あたりの戦闘効率」という1つの役割に意図して絞ってあり（`CLAUDE.md` の「能力の役割を重ねない」）、運搬効率にも使うと**選択圧がどちらから来ているか測れなくなる**。体力は量の概念で、泳ぎスキルの天井にもすでに使っている（#63 の適性表）。**効かせ方は移動コストの割り増し（合計の重さ ÷ 体力）**で、速度そのものは落とさない——**速度を落とすのは effort がすでにやっていること**で、二重にすると測定で区別できない（段階20 が地形の倍率をコスト側に掛けたのと同じ理由）。**地形の倍率と掛け算になる**ので、**平坦な世界で単体を先に測り、地形との組み合わせは別条件**にする（38b で「川の地図では採取と泳ぎが同じ枠を奪う」を踏んだのと同じ形）。**お金は重さの対象から外す**（#73） |
| 67 | **運搬の的は餓死で、これまで数えたどの的よりも大きい（2026-09-09）。** 38b の再開条件（打ち消す相手が集団の何%を動かしているかを先に数える）を運搬に当てると、**餓死は死因の34%**——飽きの割引5%（採取＝空振り）・溺死11%（泳ぎ＝唯一動いた）より大きい。**ただし数えるのは死因の割合ではなく「食料を持っていれば助かった死」**＝**餓死した個体のうち、直前の一定tickに食料が視界にあった割合**。これが小さければ運搬の上限もそこで決まる（段階24 で「打撃のうちかわされるのは5%」を数えそこねた失敗の3度目を避ける） |
| 68 | **肉は「量」と「回復量」を別のレバーとして測る（2026-09-09）。** 前者は取引価値（運びきれない余剰が出るか）、後者は栄養価値（効用式の生存見積もりに直接効く）で、**同時に動かすと「群れ狩りが増えたのは肉が美味しいからか高く売れるからか」を切り分けられない**。**全回復は、食料が体力に直接触る初めての経路**——今の世界の食事は空腹度だけを動かし、体力は「満腹で休む」経由でしか戻らない。だから既存の `recoverable`（上限までの差分に切り詰める見積もり）がそのまま**温存行動**を作るはずで、それが「商品としての肉」の在庫時間になる。**所有権のルールは変えない**（戦闘参加ベースのまま）。**余剰の定義は「参加者全員の運搬上限を超えた分」**——新しい調整用の数値を作らずに済み、**非力な集団ほど余剰が増える**という既存の遺伝子と連動した形になる。**肉抽出（保存・解体）のスキルは作らない**（運搬能力がその役目を果たす。#63 の枠を1つ節約する） |
| 69 | **魚は「地域の性質が量を決める食料カテゴリ」という一般形の、最初の実例にする（2026-09-09）。** 3つ目の `FoodKind` として持ち（段階16 の帳簿がそのまま2種類目の実質的な選択肢を得る＝`dietVariety` 0.12 への直接の答え）、**水のセルにしか湧かない**。**段階36（水辺の豊かさ）の項には混ぜない**——36 は「水の割合に応じて**陸の**植物が増える」なので、混ぜると総量保存の再正規化と符号の整合が崩れる。**総量は増やさない**（段階15a の原則。魚は植物の湧きの一部を置き換え、再正規化をテストで固定する）——増やすと「世界が豊かになったから個体数が増えた」と区別できない。**既定では人間だけが取る**（外敵の食性を変えると段階11 以降の個体数が読み直しになる。外敵にも許す腕は対照として持つ） |
| 70 | **特産食料は同じ一般形の2つ目の実例にし、座標の知識（POI）は作らない（2026-09-09）。** 「高台にしかない植物・特定の水域の魚」を座標として覚え伝える案は、**#53 の粒度の天井（地域単位）を超え**、**#41 の前提（地域の記憶は数が少なく固定なので容量を食わない）を壊し**、**新しい移動能力（見えない座標へ向かう）を要求する**——段階4 や段階15 と同格の新しい下部構造になる。代わりに**「その地域に多く、スキルが高いほど多く取れる食料」**にする。**必ず連続の傾斜にし、離散ゲート（スキルが無いと0）にしない**（`StrategyDepthUnlock` と視力の離散化で2度踏んだ轍）。**総量は保存する。** **的の大きさは明示的なパラメータ**（世界の食料の何%を特産にするか）にする——38b の結論「スキルはそれが打ち消す規則の大きさ以上にはならない」により、**この割合がそのままスキルの効き目の上限**になるから |
| 71 | **遠隔攻撃は新しい行動語彙であって、既存の戦闘のスキル化ではない（2026-09-09。#64 と両立する）。** #64 が禁じたのは**既存の近接戦闘をスキルにすること**で、**新しい行動を作ること自体**ではない。**ダメージと被弾は既存の3遺伝子（攻撃力・防御力・回避率）で近接と同じ式を使う**（新しい能力軸を作らない）。**スキルは遠隔だけが持ち、まだ誰も担当していない部分＝距離による命中の目減りにだけ効かせる**（荒れ地耐性が超過分だけ、泳ぎが危険だけを担当しているのと同じ形）。**適性遺伝子にパワーを選ばない**（パワーの代理になり重複感が戻る）——理性か頭の良さ。**順序は「行動を単体で設計・測定 →その上にスキル」**（間合い・コスト・攻撃側有利の非対称が近接と同じ形で成り立つとは限らない）。**そして既定オフで始める**——遠隔は**殴り返されずに殴れる規則**なので、世界の真の報復率（`TrueRetaliation` 0.15）をさらに下げる。段階12a は「実在しない報復への恐れ（`retaliation = 0.7`）が世界を支えていて、真の値を知ると個体数が半分になる」と測っており、**この規則はその支えを直接削る**。`retaliation` の実測値と個体数を必ず対で読む。**段階24 の「外すと隙ができる」は流用しない**（間合いの外にいる身体に隙は要らない） |
| 72 | **弾（小石）は運搬の2つ目の中身にする（2026-09-09）。** 遠隔攻撃を「習得すれば無条件に使える」ではなく「集めて運んで使う」にするための資源制約で、**拾える場所は地形に紐づける**（水・植物と同じ発想で、地図の作者が配置できる）。**運搬（#65）に依存する**ので順序はそのあと。**小石そのものに新しい所有権のルールは作らない**（落ちているものは拾える、持っているものは持っている、で足りる） |
| 73 | **通貨より先に、物々交換が起きるかを測る（2026-09-09）。** 通貨・倉庫・発信はどれも**流通の配管**であって、**「なぜ自分で採らずに買うのか」（比較優位）が無ければ全部空振りする**。**手渡し（バーター）は安い診断**——運搬が入っていれば、行動語彙1つと既存の効用比較だけで書ける。**ここで交換が1度も起きなければ、発信も倉庫も通貨も作らない。** お金を作る場合も、**価値は「将来食料と交換できる見込み」＝割引された期待食料価値として既存の食事の評価の延長に入れ、独立した3つ目の目的軸にしない**（優先順位は生命 > 子孫の2つだけ、という原則と正面から衝突するため）。**コインは1種類・世界に1つのグローバルな仕組み**（地域ごとの信念という今までのパターンには乗せない）。**湧き方の既定は場所に紐づけない**——コインの多い地理を作ることは、「居場所を動かすのは食料だけ」という4度の測定（34・35・36・38a）に反する条件をもう1つ増やすことになる。**半減期・供給上限は既定にしない**（デフレは蓄財そのものに価値を与え、上の原則と衝突する）。どちらも対照としてだけ持つ |
| 74 | **発信は記憶ではなく「見える事実」にする（2026-09-09）。** 売り物を出していることは `AgentView` に載せる——**段階32 の `DeclaredFor`（呼んでいる相手は行動から読む。持つと「呼んでから立ち去った個体が誘い続ける」嘘の状態になる）と同じ形**。**目撃者スキャン（`forEachWitness`）の3つ目の実例にはしない**——あれは殺害・溺死という**一瞬の出来事**のための仕組みで、**売り物を出している身体は出している間ずっと視界にいる**ので `perceive` がすでに配っている。**対価は時間**（発信中はその場に留まる＝`CallTicks` と同じ形）で、**発信するかどうかは効用比較の1候補**（ハードコードされた閾値は作らない）。**範囲は視界内だけ**（地域をまたいだ噂は、狭い範囲で測ってから広げる価値があるか決める） |
| 75 | **倉庫の知識は地域信念の形で持ち、スキル枠を使わない（2026-09-09）。** スキル（#63）は「世界のどこでも通用するカテゴリ・減衰なし」だが、**倉庫の知識は1つの場所に紐づき、離れると忘れる**——これは地域の豊かさの記憶（遅延減衰＋接触リセット）とまったく同じ形なので、そちらを流用する。**習得経路だけをスキルから借りる**（継承・低確率の観察・発信を聞くこと）。**倉庫は建てるものではなくエディタが置く地物**（`engine/edit.go` の操作をもう1つ＝地形・地域と同じ「シミュレーションの側からは誰も呼ばない」立場。建設という行動を作るのは別の話）。**記憶の枠を食わないという #41 の免除は、倉庫の数が地域と同じオーダー（十数個）に収まる限りで成り立つ**ので、上限を持たせる |
| 77 | **「運搬中・販売中は腐らない」は作らない。腐敗の時計は止めず、止めるなら値段をつける（2026-09-09）。** 相談を受けて**先に上限を測った**（48シード）——**地面の肉が一切腐らない世界にしても、世界の数字はほぼ動かない**（個体数 −3.81 で誤差内、`packSize` +0.06 \*、`starveRate` −0.10 \*）。**腐って消えているのは肉の18%で、それを全部救っても買えるものがこれしかない**（38b の「打ち消す規則の大きさ以上にはならない」がそのまま当てはまる）。**逆向きは効く**——300tick で腐る世界は肉の49%が消え、個体数 −13.56 \*\*・`packSize` −0.18 \*\*\*。**つまり今の 900tick はすでに「ほとんど無駄が出ない」側に振れている。** そのうえで**時計を止める形そのものを採らない理由が2つ**: (1) **値段の無い恩恵は上限へ走る**（段階17b で、コストの無い警告信号が正直さの足場を持てず上限に張り付いたのと同じ形）——全員が歩く食料庫になると、食料の希少性という世界の骨格を無料で迂回できる。(2) **「販売中だけ止まる」は冷蔵庫としての出店を招く**（売る気の無い個体が時計を止めるためだけに店を出す）。**やるなら「止める」ではなく「遅くする」で、値段は運搬の重さか、`#63` の天井が自動で効くスキルの側に置く。** **ただし地面の測定は「誰も取りに来なかった肉」の話で、運搬中の肉は「誰かが選んで持った肉」なので同じではない**——`CarriedMeatKeeps` の腕は段階40 のあとに1度だけ回す |
| 78 | **魚は「攻撃力の小さい種族」だったのが本来の意図。今の実装は食料アイテムで、これは意図と違う（2026-09-10・保留）。** 段階42・43 は魚を `FoodKind` の3つ目として作った——**自分では動かず**（掴み損ねたときだけ水中のどこかへ跳ぶ）、**攻撃力も意思決定も持たず**、**2つの釣りスキルはどちらも「止まっている的への成功率の掛け算」**でしかない。**本来の意図は、段階11 の汎用ノードに乗る第3の種族**（水から出られない・攻撃力が最小・倒せば魚の肉になる）で、**2つのスキルはそこで初めて別のことを意味する**——**陸釣り＝寄ってきた魚を捕る**、**水中取り＝こちらから魚に寄って捕る**（**今のコードには「誰が距離を詰めたか」という区別がどこにも無い**）。**保留にした理由は規模**: 種族を1つ足すこと自体は段階11 が汎用化してあるので安いが、**決めることが多く**（魚は何を食べるか／繁殖するのか、それとも今の湧き方のまま世界が供給するのか／死体は `FoodFish` を落とすのか `FoodMeat` か／`hunts`・`killShare`・種族ごとの個体数・`dietVariety` の意味が全部変わる）、**段階42・43・34・36 の記録が全部引き直しになる**。**復活の条件は、段階50・51（倉庫・通貨）を通したあと、1段階まるごとの枠を取れるとき** |
| 76 | **多tickの拘束は既存の前例に乗せ、新しい並行の仕組みを作らない（2026-09-09）。** 「採取に数tickかかる」は段階32 の `CallTicks`・戦闘の `SkirmishTicks`・`observe` の排他性と同じ形で書ける。**ただし拘束時間の導入は食料獲得の効用計算全体に触る**ので、**拘束そのものを単体で先に測り、スキルとの組み合わせはそのあと**（段階33・35 の「混ぜて測らない」）。**運搬の拾う・置くは 1tick で始める**——拘束と運搬を同じ回に入れると、どちらが効いたか分けられない |
| 79 | **段階50 と段階51 が独立に当たった壁は1つで、診断は「この世界のノードは今しか見ていない」（2026-09-11）。** **取っておく価値もコインの値打ちも、効用式が「今の死亡リスクの勾配」から読み**、**その勾配は満腹の身体では平ら**で、**余剰を持っているのは満腹の身体**（倉庫で `ActStore` が312回採点されて勝つのは3回、通貨で断りが売買の2倍）。**したがって備蓄・交換・孤立回避のどれにも値段がつかない。** **ただし根っこ（効用式の計画の形＝先読み）そのものには触らない**——それは段階4・段階15 と同格の下部構造の入れ替えで、**記録済みの測定値が全部引き直しになる**。代わりに**壁を迂回する当たり方を4つ**（52 調理・53 性差・54 感情・55 孤立）用意し、**どれが実際に効くかを測る**。**52 が先頭なのは、4つの中で唯一この壁を構造的に避けられるうえ、55 の必要性そのものを検証する前提になるから** |
| 80 | **調理は新しい食料カテゴリではなく、既存の回復量の枠組みに乗る「値打ちの差」にする（2026-09-11）。** **回復の値打ちは空腹度ではなく「今どれだけ体力が欠けているか」から読まれる**（段階39 の `recoverable` の頭打ち）ので、**満腹でも傷ついている身体には正の値打ちがある**＝**#79 の壁を構造的に避けられる唯一の形**。**新しい状態量を作らない**（`FoodView.Heal` を再利用）、**新しい遺伝子を作らない**（スキルはヒント枠＝#63。10本目は #65 が禁じた）、**4種類目の `FoodKind` にしない**（`dietVariety` の目盛りが動くと段階42 以降が読み直しになる）。**そして生食を禁じない**——**「生では絶対に食べられない」という完全な門は、段階17b（毒に得を作ると個体数が9分の1）と同じ領域**で、離散の門は `StrategyDepthUnlock`・視力・段階44 で3度避けてきた線。**まずは連続の差（調理済みのほうが回復量が高い）だけで測り**、弱すぎると分かったときにだけ**「生食の値打ちがとても低い食料」を段階52d として独立に追加・測定する**（段階44 の `SpecialtyCatch = 0.25` と同じ「連続だが非対称」な値付け。**52 と同じ回に混ぜない**） |
| 81 | **調理の独占は時間で区切り、場所には紐づけない（2026-09-11）。** `ENEMY.md` の分配ルールの表に前例がある——「近くにいる全員が食べられる＝崩壊する（誰も狩らない）」「参加した者だけ＝動く」。**調理にも同じ構造がある**（すぐ誰にでも取られるなら時間を払って調理する理由が消える）ので、**肉の `MeatClaimTicks` と同じ形で調理直後の一定時間だけ優先権を与える**（`CookClaimTicks`。**新しい所有の仕組みは作らない**）。**段階41 の結論をそのまま当てる**（**残すべきは claim、開けるべきは余剰**。claim を全部やめる版は −4.33 で悪かった）。**「身内にしか渡さない」偏りは既存の値付けがすでに罰しているはず**（段階48 の実測＝信頼は飽和するので他人への贈り物が一番高い）**だが、前提であって結論ではない**ので測る。**大量の溜め込みは `CarryCapacity = 1` が構造的に止めている**（段階50 の「入っている合計 0.19 個」）。**焚き火のような世界側の地物を要求しない**——**その場所を占拠する形の独占（縄張り化）が生まれる**ので、段階48・49 と同じ「個体の行動だけで完結する」形で始め、場所に紐づける拡張は段階52e として後回しにする |
| 82 | **性差は発現時（`Agent.Ability`）の掛け算であって、継承には触らない（2026-09-11）。** **コードを読んで確認した**: `tryBirth` は `BirthVitalityCost / 2` で両親が均等に払い、`inheritGene` / `inheritBudget` / `drawGenomeFor` は性別を1度も参照せず、子の性別は遺伝子配分と独立に引かれる。**したがって出産コストの非対称化だけでは表現型の性差は生まれない**（**性染色体的な連鎖が無い**＝#4 で「連鎖群も性染色体も作らない」と確定済み）。**入れるなら年齢補正とまったく同じ位置の掛け算1つ**（実効値 ＝ 遺伝的素質 × 年齢補正 × 性別補正。**掛け算は1か所だけ**という段階7d の原則を守る）で、**`Genome` と継承には触らない**＝両性とも同じ遺伝子プールから生まれる。**範囲を分けず平均だけ緩やかにずらす**（`EnemyBudgetMean` と同じ発想だが**分布の重なりを残す**）。**離散の門は作らない**。**そして設計で入れた差を選択で生まれた差と混同しないため、性別補正なしの対照を必ず対で回す**（段階17b で「上げているのは信号ではなく世界の厳しさだった」を踏んだ形）。**どの遺伝子をどちらの性にどの程度振るかは未決**で、**決まるまで着手しない** |
| 83 | **感情は新しい状態軸ではなく、既存の評価ノイズに乗る履歴依存の偏りにする（2026-09-11）。** **段階12a の一般化**——`retaliation = 0.7` という**実在しない報復への恐れ**が世界を支えており、**真の値 0.15 を知ると個体数が半分になる**（増えるのは殺しではなく餓死）。**問いは「非合理な偏りが集団を安定させるか」。** **形は3つの線で縛る**: (1) **選択の上書きを採らない**——「一定確率で最善手を外して一様ランダムに選ぶ」は **2026-08-14 に自殺的な攻撃を誘発して却下済み**で、**今の `ChoiceNoise` が評価値に正規ノイズを乗せる形なのはそのため**。**感情も同じ安全な形（スコアへの加算バイアス）に乗せる**。(2) **相手ごとに持たない**——**個体の全般的な「今の調子」を表す1〜2個の減衰スカラー**にする。**相手ごとの負の記憶を新設することは `CLAUDE.md` の「恨みの状態を持たない」に正面から反する**（リスク記憶がすでに敵意そのもの）。(3) **新しい検知ロジックを作らない**——トリガーは既存のもの（`attacked` / `TriggerVitalityDrop` / 段階31 の `forEachWitness` / 成立した求愛 / 得をした売買）に相乗りし、**減衰は `Opinion` の `Affinity`/`Risk` と同じ遅延減衰の帳簿**（#65 が所持品を状態軸の唯一の例外にした線を守る）。**既存のノイズ量（`ChoiceNoise` = 20）に対して十分小さく抑え、暴力性が跳ね上がらないかを段階6 と同じ観点で監視する** |
| 84 | **孤立のリスクは、段階52 の結果を見てから判断する（2026-09-11・条件つき保留）。** **客観的な依存関係（調理してくれる相手の近くにいることが得）が集団化を生むなら、孤立ペナルティという人工的な機構は要らない**——保留の「他人の持ち物を奪う行動」が段階48 の結果待ちにされているのと同じ構え。**着手する場合も、先に的を数える**: **「視界内に好意のある相手がゼロ」の状態が全tickの何%か**。**ここが元々小さければリスク項を足しても発火せず、段階30b・31・35・49 で4度続けて出たパターン（規則は絶えず発火するのに世界の数字が動かない）の5度目になる**。**そして予想は2段構えで書く**——**安い実装（`ShockRisk` への乗数）では「留まる」は強まっても「戻る」は生まれず、戻る行動を見たいなら地域ごとの信念（`RegionCostWeight` / `RegionDangerTicks` と同型）に「気心の知れた相手の多い地域」を1本足す必要がある**。**ただし2本目を足しても行動は変わらないほうに賭ける**（段階35 の「正確な信念が伝承で広がっても `onWater` は動かない」＋段階36 の「居場所を動かすのは食料の分布だけ」） |
| 85 | **性差の配分は、抽象的な男女像からではなく、構造として作る役割から逆算する（2026-09-11）。** 先に**母＝ガーディアン**（`RearingRadius` に縛られて動けない）・**父＝広く動いて価値の高い食料を取る側**という非対称を作り、**そこから要る能力だけを振る**——母に理性・防御力・**記憶容量**、父にパワー・速さ・頭の良さ、最大体力と回避率と魅力は中立。**理性を母に寄せたので父は頭の良さ**（役割を重ねない）。**魅力を中立にしたので段階26 の線には触れない**。**本命は記憶容量**——`shMemory` は5度測って一度も正に動いておらず（段階9・12・15b・38b・52）、**53 は「動けず、周りに生死を左右される個体」を世界で初めて作る**ので、**ここで初めて動くかどうかが段階9 の宿題への反証可能な予想**になる。**ただし #4 により性染色体も連鎖群も無いので、片方の性にだけ有利な遺伝子は次の世代で混ぜられる**——**読むのは性別ごとの差（設計で入れたもの）ではなく、集団全体の予算比が対照からどちらへ動いたか** |
| 86 | **ガーディアンを母に固定したら、父の給餌経路を同じ回で開ける（2026-09-11）。** `tryBirth` は今「**どちらでもルール上は関係ない**」として番号の若いほうをガーディアンにしており（実測でも 50.5% が父）、**母に固定するのは「妊娠・出産・育児は女性」を構造にする最小の変更**。**ただし `mouthsToFeed` は `GuardianID` 一致で弾いているので、それだけ入れると父は給餌から完全に締め出される**。**判定を「血縁かつ `RearingRadius` 圏内」に緩める**——**新しい所有も状態も作らず、既存の `share()`（選択でも決定でもない受動的な経路＝段階7d）をそのまま使う**。**`ActGive`（段階48 の贈与）には頼らない**——**実測で配偶者への贈与は 0%** で、**信頼が飽和しているので贈与の値付けでは父から母子へ物が動かない**。**そして 53b は小さな手直しではない**: 実測で**ガーディアンは 0.774 の時間だけ圏内におり、ガーディアンでない親も 0.475 の時間は圏内にいる**＝**給餌の機会がおよそ 1.6 倍になる**ので、**53a 単体・53a+53b・どちらも無し の3つを対で測る** |
