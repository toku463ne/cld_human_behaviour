# 測定の記録 4（2026-09-23）

**この回の問い**: **#148（経験から相関を学ぶ）を作る値打ちがあるか。** **本体を1行も書かずに、読み取り専用の計器だけで数えた**（`TODO.md` の24番）。

**なぜ先に数えるか**: 本体は先読み以来の規模（イベント式の相関学習＋目撃・合成・物々交換の3部構成）で、**#116 が「請求先の枝を何本足しても平らな勾配から読むかぎり同じ」と診断した鎖の6度目の挑戦**にあたる。**空振りなら既定オフの規則がもう1つ増えるだけ**なので、**作る値打ちを先に数字で決める**。

**計器**（`engine/correlate.go`、`Config.Correlate`。**既定はオフ**）: **世界の規則を1つも変えず、乱数を1つも引かない。** 指紋テストはそのまま緑で、**オンとオフの世界が2000tick後に1ビット違わないこと・乱数の引いた回数まで一致することをテストで固定してある**（`TestCountingDoesNotChangeTheWorld`）。

**条件**: 12シード × 20000tick、3つの腕。**`count`** ＝平坦な既定の世界／**`countplay`** ＝遊ぶ地図（`country` ＋ スキル）／**`countmarket`** ＝物が売り買いされ身に着けられる世界（**89b `GiftWorthScaled` の上**。`PLAN.md` の指定）。

---

## 1. 作る値打ちはある——**大きい関係の4分の3は、効用式が書いていない**

| 指標 | `count` | `countplay` | `countmarket` | 何を言っているか |
|---|---:|---:|---:|---|
| `corrNoise` | 10.44 | 11.39 | 10.61 | **1候補を採点するときの誤差の幅**（以下の目盛り） |
| `corrLive` | 444.8 | 480.6 | 583.1 | 〈状況×行動×イベント〉の3つ組のうち、1度でも発火したもの |
| **`corrLoud`** | **194.6** | **181.5** | **208.9** | そのうち**起きたときの値打ちが評価誤差の4分の1を超える**もの |
| **`corrActed`** | **93.1** | **85.8** | **100.0** | **そのうち、起きる頻度を掛けても誤差を超えるもの＝身体が頼れるもの** |
| **`corrUnwritten`** | **38.1** | **31.9** | **40.3** | **そのうち、効用式が半分も期待していなかったもの＝37〜41%** |
| `corrVarying` | 54.2 | 52.0 | 50.4 | 同じ決定の候補どうしを区別できる鍵（相手について読む4つ）|

**目盛りについて1つ訂正しておく。** **`corrNoise` は「1つの候補を採点するときにこの身体が乗せる誤差の標準偏差」**で、走行中に測って 10.4〜11.4（`ChoiceNoise` 20 の世界なので上限20、平均その半分あたり）。**`controller.go` の `addWarmth` のコメントにある「評価ノイズ 139」は別の量**（同じ決定の候補の散らばりの側）なので、**この2つを混ぜて読まないこと。** 以下で「大きい」と言うときの目盛りは前者。

**この項目の門は `corrUnwritten` で、通った。** **判定は「その鍵で決めたときに期待できる値打ち（起きたときの値打ち × 起きる頻度）に対して、効用式がその手に付けていた期待値が半分に満たない」**こと。**二重計上にならない関係が、どの世界でも32〜40本ある。**

**この判定は2度書き直した。そして2度とも、直すたびに数が減った。** **最初に出した 145.6 / 141.0 / 167.3 は、両方の誤りを含んでいる。**

1. **比べる相手が「生命の項」だけだった。** **そのせいで「求愛 → 子」が必ず『書いていない』側に落ちた**——**求愛は体力を払って子を買う手なので生命の項は負**だが、**式は子を `Offspring` でちゃんと値付けている**。**全ゴールの和と比べるように直して 145.6 / 141.0 / 167.3 → 128.9 / 122.8 / 148.5**
2. **桁が揃っていなかった。** **`Value` は「そのイベントが起きたときの値打ち」で、`Pred` は「値打ち × 確率」の期待値。** **うまくいった求愛が子1人ぶんの値打ちを持つのは当たり前**で、**それを「うまくいくかどうか分からない求愛」への期待値と比べれば、世界中のどの関係も『書いていない』ことになる**。**両方を期待値にして 128.9 / 122.8 / 148.5 → 38.1 / 31.9 / 40.3**

**2度目の直しで4分の1になった。** **門は通ったまま**だが、**「大きい関係の75〜80%が未知」と最初に書いたのは間違いで、正しくは「身体が頼れる関係（`corrActed` 86〜100本）のうち37〜41%」。**

**ただし `corrVarying` は4分の1しかない**（50〜54 / 181〜209）。**残りは自分の状態から引く鍵で、同じtickのどの候補にも同じ値が乗る**——**行動どうしは区別するが、同じ行動の相手違いは区別しない**。**段階86 の「全候補を等しく持ち上げる項は誰も動かさない」に半分だけ当たる。**

## 2. **鍵は2度目の崩れ方をした**——今度は「混んでいる」と「乏しい」

`countmarket`・シード1・74027決定の内訳:

| 状況 | 回数 | | 状況 | 回数 |
|---|---:|---|---|---:|
| **乏しさ** | **43510** | | 信頼 | 3047 |
| **人混み** | **20262** | | 近さ | 1521 |
| 空腹 | 2489 | | 別種 | 853 |
| 消耗 | 1790 | | 強さ | 555 |

**86%が「乏しさ」と「人混み」。** **#137 で「死んだ身体の死因特徴は必ず1.0だから連続値は勝てない」と学んだのと同じ形**で、**今度は「たいてい乏しくて混んでいる」から**。**argmax の鍵は、一番よく1に近づく特徴の鍵になる**——**これで2度目**。**本体を作るなら、鍵は argmax ではなく上位2つか、特徴ごとに分けた表にすること。**

## 3. **一番大きい関係は「死」で、これも2度目**

**値打ち×リフトで並べた上位12のうち11が `died`**（値打ち −100 ＝ `LifeValue` そのもの）。**残り1つが「強い相手に求愛 → 子」**（リフト 42.5、値打ち +42.0、遅れ184tick、効用式の予測 −4.98 ＝**効用式は逆符号を付けていた**）。

**つまり教訓（#137）が最初に見つけたものを、一般形も最初に見つける。** **`lesson.go` がこの一般形の最も狭い事例だという `PLAN.md` #148 の位置づけは、測定でも裏が取れた。**

## 4. **時間の窓は、イベントに負ける**

| 窓 | `count` | `countplay` | `countmarket` |
|---|---:|---:|---:|
| 30tick | 0.05 | 0.04 | 0.04 |
| 200tick | 0.05 | 0.08 | 0.06 |
| 700tick | 0.06 | 0.15 | 0.09 |

**「その決定がどの鍵だったか」で説明できる、k tick後の身体の変化のばらつきは4〜15%。** **一番よくて遊ぶ地図の700tickで 0.15**。**固定の窓はほとんど何も言わない**ので、**#148 の (1)「何セルか先はイベントである」は正しい**。**理由は設計のときに書いたとおり**——固定 k の標本は代謝に埋もれる（`corrMended` が 54000〜153000 発火するのに対し `corrFed` は 1900〜4300）。

## 5. 目撃チャネルは、死だけの話ではなかった

**`corrSeen` 0.91〜0.97 / `corrOnlookers` 6.1〜12.5。** **#137 が死について測った 0.99・10.9〜18.4 と同じ桁で、どのイベントにも当てはまる。** **(2) 目撃チャネルを入れる判断は、的の大きさの面では正しい。**

## 6. 貨幣——**鎖は閉じる。ただし #116 の読みを1つ訂正する必要がある**

| 指標 | `countmarket` | 何を言っているか |
|---|---:|---|
| **`corrCoinFed`** | **31244** | **硬貨が手に入ったあと、窓の中で食べる／回復する**が1実行にこれだけ |
| **`corrCoinHold`** | **539.5 tick** | **硬貨が手にとどまる時間**（窓の700tickの中） |
| **`corrCoinStuck`** | **0.22** | **手に入った硬貨のうち、最後まで出ていかなかった割合** |
| `corrCoin` | 1156 | 硬貨が手に入った回数 |
| `corrPairs` | 72.3 / 100 | 〈イベント→イベント〉100マスのうち発火したマス |

**「コインは持たれるだけで使われない」（#116）は、そのままでは正しくない。** **手に入った硬貨の78%は出ていき、平均539tick しかとどまらない。** **#116 が見ていたのは「コインの値打ちを上げても売買が増えない」ことで、「硬貨が動かない」ことではなかった。** **合成の足跡は届く**（窓700tick > 保有539tick）。

**したがって #148 の (3)「合成で鎖を縮める」には材料がある。**

**ただし `corrCoinFed` の 31244 を読みすぎないこと。** **これは生の回数であって、リフト（基準率との比）ではない**——**食べる／治るはもともと頻繁**なので、**「硬貨のあとに食事が来る」が多いこと自体は、硬貨と食事が結びついている証拠にはならない**。**この数字が言っているのは「窓の中で鎖が閉じることが実際にある」ということだけ**で、**結びつきの強さは本体を作るときに、この組についてのリフトを見て決めること。** **そして「出ていく」には贈与も含む**（持ち主が死んだ場合は出ていかなかった側に数えている）。

## 7. 物々交換——**機会はどこにでもあり、賞金は誤差より小さい**

| 指標 | `countmarket` | 何を言っているか |
|---|---:|---|
| **`corrSwap`** | **0.68** | **視界の中に、互いに相手の飾り物のほうを高く評価する相手がいる標本の割合** |
| **`corrGain`** | **4.39** | **その交換で両者が合わせて得るもの**（目盛りは `corrNoise` 10.61） |
| `corrTrinketHands` | 0.89 | 飾り物を持っている身体の割合 |
| **`corrFoodSwap`** | **0.00** | **同じことを食料で訊いたら、1度も立たない** |

**二重の欲求の一致は、この世界では例外ではなく常態**（68%）。**ところが賞金は評価誤差の 0.41倍**しかない。**飾り物の交換は「機会が無いから起きない」のではなく「起きても誤差に埋もれる」**。

**そして食料の側は完全な空振り**（`corrFoodSwap` 0.00、平坦な世界でも 0.01）。**`PLAN.md` が「飽きの割引は5%しかなく評価ノイズに埋もれる恐れがある」と書いたのは、恐れではなく事実だった。** **A案（飾り物の物々交換）を採るなら飾り物側で、食料側には何も無い。**

## 8. **売り手の条件は2万回そろって、売買は1.58回**

**`corrSellerReady` 21918** ——**余りを持ち、視界に相手がいて、その相手が硬貨を持っている**状態が1実行にこれだけ起きる。**同じ実行の `sales` は 1.58。**

**2026-09-20 の「足りないのは売り手ではない」を、買い手と機会の側からも確かめたことになる。** **3つの条件が2万回そろって1.58回しか売れないなら、止めているのは機会ではない。**

## 9. 贈与の往復——**D案には的がある**

| 指標 | `count` | `countplay` | `countmarket` |
|---|---:|---:|---:|
| `corrGifts` | 518 | 195 | 3793 |
| **`corrBack`** | **23.8** | **13.8** | **354.3** |
| `corrBackTicks` | 329 | 408 | 419 |

**物が売り買いされる世界では、贈与の9.3%が逆向きの贈与で返っている**（354 / 3793）、**平均419tick 後**。**平坦な世界でも4.6%。** **「Aに渡した → 後でAから渡された」は新しい語彙なしで学習できる関係として実在する。**

## 9b. 逆伝播——**物に値打ちを戻すと、硬貨と肉が浮き、植物が沈む**（2026-09-23・ユーザの提起）

**ユーザの案**: **合成は中間項を落としてしまう**（「掲げる → 硬貨 → 食べた」を合成すると硬貨が消える）**ので、良い結果に至ったリンクに登場した物のほうに点を配れば、物への値打ちがそこで生まれるのではないか。** **悪い結果には減点する**（**ユーザ自身の「片側だけ学ばせない」という規則をこの案にも適用した**）。

**これは合成の追加ではなく代替**（両方やると同じ食事を2度数える）で、**しかも桁違いに安い**——**鍵空間ではなく物の種類8つ**。**そして合成で消える中間項が戻る**ので、硬貨を拾う・捨てない・渡さない理由がちゃんと式に残る。

**計器に足して測った**（物が手に入ってから窓の中で起きたイベントの値打ちを、符号つきで走る平均。`Gain` は**どのイベントも平均するといくらか**からの差）:

| 物 | 手に入った | 手を離れた | `Gain`（`countmarket`） | `Gain`（30tick窓） | `Gain`（平坦） | 手にとどまる |
|---|---:|---:|---:|---:|---:|---:|
| **硬貨** | 999 | 270 | **+0.89** | **+1.23** | — | 578 |
| **肉** | 129 | 124 | **+0.46** | **+1.76** | **+1.67** | 154 |
| 飾り物 | 4361 | 698 | +0.02 | −0.06 | — | 442 |
| **植物** | 751 | 722 | **−1.03** | **−1.41** | **−0.24** | 298 |
| 皮・魚・本・石 | 0 | 0 | 0 | 0 | 0 | — |

**機構は働く。** **符号も順序も筋が通っている**——**肉は一番大きく**（飢えた身体を一気に戻す＝段階39 の `MeatHeal` がそのまま出ている）、**硬貨がそれに次ぎ**、**植物は負**（持っている＝まだ食べていない＝空腹のまま歩いている）。

**用量を1度読み違えた（2026-09-23・ユーザの問い「防寒着の効用はコイントスと変わらないという意味か」から）。** **最初に「`corrNoise` 10.4 に対して +0.36 だから誤差の3%」と書いたのは、分母の違うものを比べていた**——**`Gain` は「credit された1イベントあたり」の平均で、`corrNoise` は「1候補を採点するときの誤差」**。**コートは平均1383tick 手にあり、その間に16,090件のイベントが credit される**ので、**コートと無関係なイベントで全部割っていた。**

**「1つ手に入れることが何を予告するか」で取り直す**（`Per` ＝ credit された超過の合計 ÷ 手に入れた回数。**基準は「その実行で起きた全イベントの平均」**。目盛りは `LifeValue` 100 と評価誤差 10.4）。**そして交絡を落とす対照を並べる**（`Adj` ＝**同じ状態の身体が普通に得るもの**を基準にした層別推定。**状態は空腹4段 × 消耗4段の16通り**で、**`pressures` が危険を計算するのに使う2つ**）:

| 物 | `counthide` 生 | `counthide` 対照 | `countmarket` 生 | `countmarket` 対照 |
|---|---:|---:|---:|---:|
| **肉** | +46.48 ± 14.53 | **+49.74 ± 12.69** | +98.48 ± 14.29 | **+97.74 ± 14.05** |
| **硬貨** | +59.94 ± 7.67 | **+50.80 ± 7.49** | +133.85 ± 9.75 | **+112.65 ± 8.60** |
| **コート** | +42.94 ± 9.62 | **+18.12 ± 10.03** | — | — |
| **皮** | +39.39 ± 8.41 | **+13.61 ± 8.15** | — | — |
| **ただの飾り物** | +1.98 ± 7.68 | −7.02 ± 6.65 | +67.76 ± 8.92 | +49.20 ± 7.28 |
| 植物 | — | +8.42 ± 8.26 | — | +4.67 ± 5.01 |

**用量は小さくない——評価誤差の2〜10倍**で、**効用式に足せば確実に効く大きさ**。**心配する向きは逆で、大きすぎないかのほう。**

**`Per` は足し合わせられない**（1つのイベントが複数の物に credit されるため）。**物ごとの推定であって、分解ではない。**

**そして交絡がある。** **硬貨を持っている身体は「手が空いていて、飢えていない身体」**でもあるので、**硬貨が良い結果を引き起こしたのか、良い状態の身体が硬貨を持てるのかを、この計器は分けられない。** **本体を作るときは「同じ物を同じだけ拾うが、値打ちは決定に届かない」対照**（`lessonblind` 形）**が要る。**

**皮がゼロなのは規則がオフだから**（`WardNeedsHide`）。**「皮 → 防寒着 → 寒さが止まった」という、この世界で報酬が確実にある鎖はまだ測っていない**——**次に測るならそこ。**

## 9b-2. 皮の鎖——**同じ世界の中で、役に立つ飾り物と役に立たない飾り物が 0.63 離れた**（2026-09-23・ユーザの指定）

**この世界で「遠くの報酬」が確実にあると既に測ってある唯一の鎖**を測った: **獣から皮が落ちる → 皮からコートを作る → コートが寒さの消耗を止める**（段階87a の `warmthValue`）。**腕は `counthide`（`WardNeedsHide` オン）と `counthidefree`（同じ寒い世界だが、コートは何も無くても作れる＝皮は何の役にも立たない）。12シード。**

**最初に、コートを飾り物から分けた。** **コートは `Ward > 0` の `FoodTrinket`**（段階87a が飾り物の上に建てた）なので、**分けないと4千個の装身具の中に埋まる。**

**`counthide`（1つの世界の中）:**

| 物 | 手に入った | `Gain` |
|---|---:|---:|
| 肉 | 63 | **+2.14** |
| 硬貨 | 528 | **+1.75** |
| **皮** | 223 | **+0.59** |
| **コート（`Ward > 0` の飾り物）** | 222 | **+0.36**（12シード平均） |
| 植物 | 132 | −0.24 |
| **ただの飾り物（`Ward` なし）** | 1764 | **−0.27**（12シード平均） |

**これがこの測定で一番きれいな結果です。** **コートとただの飾り物は同じ物**（どちらも `FoodTrinket`、同じ作り方、同じ重さ、同じ手の枠）**で、違うのは「寒さを止めるかどうか」だけ**。**計器はその2つを 0.63 離しました。** **同じ世界の中の比較なので、個体数も気候も交絡しません。**

**対照との対（`counthidefree` 基準・12シード）:**

| 指標 | 差 |
|---|---:|
| **`corrGainHide`** | **+0.32 \*\*** |
| **`corrGainCoat`** | **+0.41 \*\*\*** |
| `corrGainCoin` | +0.09 |
| `corrGainMeat` | +0.22 |
| `corrGotHide` | **+179 \*\*\***（**対照では皮は1枚も手に取られない**——`hidesLying` 140・`hidesHeld` 0.00） |
| `corrGotCoat` | **−3975 \*\*\***（対照ではコートが 4190個。**誰もが着ている物は何も語らない**＝`Gain` −0.04） |
| `pop` | **−40.83 \***（**皮の世界はずっと厳しい**） |

**対照の側は弱い**——**皮が役に立たない世界では、そもそも誰も皮を拾わない**ので、**「計器が無価値な物に値打ちを付けてしまわないか」の検定にはなっていない**（拾われなければ行が立たない）。**その検定をしているのは上の「ただの飾り物 −0.27」のほう。**

**そして個体数が 26.9 対 67.8 と大きく違う**ので、**腕をまたいだ差は世界の違いに汚染されている**。**信用できるのは世界の中の比較のほう。**

**予防はどう見えるか。** **コートの値打ちは「寒さで削られないこと」＝何も起きないこと**で、**イベントの語彙は「起きたこと」しか持たない**。**それでも数字が立つのは、平均の取り方のため**——**コートを持っている間は死（−100）が credit されにくいので、credit されたイベントの平均が押し上がる**。**予防は「悪いイベントが入ってこないこと」として平均に映る。**

**用量は1つあたりで読むこと**（上の `Per`）: **コート +42.94 ± 9.62 対 ただの飾り物 +1.98 ± 7.68 ＝ 約41点の差**。**対照を通しても 18.12 対 −7.02 ＝ 25点残る。** **同じ身体が同じ作り方で作る同じ物で、違うのは寒さを止めるかどうかだけ**なので、**この差は交絡では説明できない。この測定で一番信用できるのはここ。**

## 9b-3. 交絡の対照——**予想は外れ、外れ方が方法の話だった**（2026-09-23・ユーザの指定）

**予想**（測る前に書いた）: **硬貨の数字は交絡でほとんど消える**（この世界で硬貨が何かを買うのは1実行に 1.58回しかないのに、全部の中で最大値が出ていた）。**コートは残る**（同じ身体が作る同じ物との差なので）。

**両方とも外れた。**

| 物 | 生 → 対照 | 残った割合 |
|---|---:|---:|
| **肉** | 46.48 → **49.74** | **106%**（まったく減らない） |
| **硬貨** | 59.94 → **50.80** | **85%** |
| **コート** | 42.94 → **18.12** | **42%** |
| **皮** | 39.39 → **13.61** | **35%** |

**硬貨は15%しか落ちない。** **交絡は実在するが、この数字の6分の1しか説明しない。** **予想は外れた。**

**そしてコートと皮が6割落ちたのは、交絡ではなく過剰な制御だった。** **層別に使った2つは空腹と消耗**で、**コートの仕事はまさに消耗を防ぐこと**。**因果が通っている変数で層別すれば、測りたかった効果そのものが引かれる**（媒介変数での条件付け）。**だからコートと皮の対照値は下限であって推定値ではない。**

**逆に、硬貨の値打ちは空腹や消耗を経由しない**（物を買うことを経由する）**ので、同じ層別が正しい対照になる。** **つまりこの対照は物によって意味が違う**——**これがこの測定で一番学びになった。**

**肉が減らないのは、肉の効果が「食べる」という別のイベントとして出るから**で、**層別の2つでは説明されない。** **機構が本物を捕まえていることの、一番きれいな証拠。**

**合成はこの世界では正直だった**（`corrComposeErr` 1.47 ± 0.22 / 2.26 ± 0.77）。**市場のある世界の8〜10倍とは対照的で、過大申告は物のイベントが束で来る世界の話。**

## 9c. 合成——**素朴にやると桁を間違える。原因は基底率の掛け算**

**（状況, 手）→ B と B → C をつないで（状況, 手）→ C を作り、同じ三つ組を直接観測した値と突き合わせた。** **`Direct` を持っているのがこの検定の全部**で、**合成が直接観測と合わないなら、それは世界に無い規則を身体に持たせることになる。**

**最初の素朴な合成は、直接観測の 13〜30倍を主張した。** 例: `closeness buy got thing safe` が**合成 431.67 に対し直接 0.00**。

**原因は算数で、もっと早く気づくべきだった**——**`pairs[b][e] / events[b]` は「b 1回あたり e が何回続くか」**で、**e が b よりずっと頻繁なら、この比は基底率そのものになる**。**「硬貨1枚のあとに回復が23回続く」は本当だが、それは回復が常に起きているからで、硬貨のせいではない。**

**基底率を超えた分だけを使うように直した**（`excess = 1 - before[b]/rate`）。

| 腕 | `corrComposeErr`（合成 ÷ 直接観測） |
|---|---:|
| `count`（平坦・窓700） | **1.36 ± 0.28** |
| `countmarket`（窓700） | 7.86 ± 5.81 |
| `countnear`（窓30） | 10.11 ± 8.17 |

**平坦な世界では合成は正直**（1.36倍）。**物が売り買いされる世界では8〜10倍に過大申告する**（誤差も大きい）。**理由は物のイベントが束になって来ること**で、**2つのリンクが独立という仮定が崩れる。**

**したがって合成を本体に入れるなら、(a) 基底率を引くこと、(b) 直接観測できる三つ組では直接観測を優先すること、の2つが要る。**

## 10. 値段

- **オフのとき: 変わらない。** `BenchmarkEngineStep` 746 → 745 µs/op、`BenchmarkDecide` 25.1 → 24.7 µs/op（`-count=3`）
- **オンのとき: 1.90倍**（4シード × 20000tick で 14.54秒 → 27.68秒）。**理由は毎tick 全身体の `selfView` を組み直すことと、イベントごとに目撃者を走査すること**
- **1体あたり 638バイト**（構造体312 ＋ 足跡126 ＋ 既見ビット200）、**世界側の表が 59,584バイト**。**鍵152 × イベント10。**

---

## この測定が決めたこと

- **(2) 効用式に書かれていない関係は32〜40本ある**（身体が頼れる関係86〜100本のうち37〜41%）。**本体を作る値打ちはある。**
- **(3) 固定の窓は4〜15%しか説明しない。イベント式で正しい。**
- **(5) 〈イベント→イベント〉は100マス中72マスが発火し、硬貨→食事は31244回。合成に材料がある。**
- **(6) 硬貨の78%は使われ、保有は539tick。足跡は届く。**
- **(7) 二重の欲求の一致は68%で常態だが、賞金は誤差の0.41倍。A案は的が小さい。食料側はゼロ。**
- **(9) 贈与の往復は9.3%。D案にも的がある。**

## 設計を3つ切った

1. **鍵を argmax で取らないこと。** 86%が「乏しさ」と「人混み」に落ちる（#137 と同じ崩れ方の2度目）
2. **食料の物々交換は作らないこと。** `corrFoodSwap` は2つの世界で 0.00 と 0.01
3. **`CorrMended` と `CorrHurt` の切り方は、身体自身の率を目盛りにすること。** 最初は「体力が減った」で切って1実行に92574回発火した（食事は785回）。**代謝と自分で払った努力は「起きたこと」ではない**ので、**殴られたことと、自分の回復率を超えて治ったこと**に切り直した

## 外した予想

**形式的な予想は書いていない**（世界を1ビットも変えない計器なので、`CLAUDE.md` の「選択圧に触れる変更の前に予想を書く」には当たらない）。**設計のときの見立てで外れたものは2つ。**

- **「#116 の『コインは持たれるだけで使われない』が本当なら、硬貨の保有時間は非有界で足跡は永久に閉じない」**——**外れた**。78%は使われ、539tick で出ていく
- **「二重の欲求の一致はゼロ近傍かもしれないので、そのときは D案に落とす」**——**外れた**。68%で常態。**落とす理由は発生率ではなく賞金の小ささのほうだった**
