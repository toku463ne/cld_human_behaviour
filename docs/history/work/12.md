# 変更の記録 12（2026-09-21〜）

**`HISTORY.md` の「変更の記録」の続きです。** `11.md` が 40,000 文字を超えたので番号を繰り上げました（`CLAUDE.md` のメンテナンス方針）。**末尾に追記**し、過去の項目は書き換えません。

---

### 押すことを選ぶ（#139・`TODO.md` の16番。2026-09-21）

**段階13（#136）は「殴られると下がる」を入れた。これはその反対側**——**構えの4つ目（`StanceShoving`）で、その tick の力を「殴る」ではなく「相手を動かす」に注ぐ手**。ユーザの提起は3つあった: **(a) 逃げる確率を上げたいときに押して間合いを切る**／**(b) 相手の背後が水や崖なら押して大きな損を与える**／**(c) その知識が好意を生みながら伝わる**。

**(c) は1行も書かずに済んだ。** `exchangeHints` が写した数は `HintTradeWorth` 倍で `exchangeLore` の `moved` に入り、`moved × AffinityLore` が双方に好意として記録される（既定オン）。**足したのは伝える中身1つだけ。**

**新しい行動語は作らなかった**（決定#139）。`ActShove` を20番目にすると `numActionKinds` が広がり、**ヒントの抽選が全世界で動く**（規則がオフの世界でも指紋が壊れる）。**構えなら語彙は19のまま。** ただし**構えを常に4つ採点すると、候補1つにつき乱数を1つ引くので同じことが起きる**ので、**`ShovePush > 0` のときだけ4つ目を候補に足す**（`numStances`）。

**実装前に数えた（4シード × 20000tick。`shoveProbe` = 5 ＝ `CombatRadius` の3分の1）**

| 地図 | 決定あたり・背後が崖 | 背後が水 | 間合いの内側だけ・崖 | 同・水 |
|---|---:|---:|---:|---:|
| `country` | **5.95%** | 0.25% | **5.40%** | 0.32% |
| `river` | 0%（段差なし） | 0.46% | 0% | 0.28% |
| `countryskill` | **5.87%** | 0.29% | **5.89%** | 0.14% |

**この表が本命を確定させた。** **段階13 の殴打あたり 6.3〜14.6% を「下限」だと思っていたのに、決定あたりは 5.4〜5.9% で、むしろ小さい。** 理由は幾何で、**押す向きは攻撃者→被攻撃者の直線**なので、**決定の時点で崖が背後にある相手と、殴っているうちに崖が背後にある相手は同じ集合**。**「いつ押すか選べる」ことは的を増やさない**——**増やすのは「どこに立つか選べる」ことだけで、それは効用式に無い**（居場所についての6度の測定）。

**形（4つ）**

- **距離は `ShovePush ×（入った押しの力 ÷ AttackDamage）×（MaxVitality ÷ その身体の最大体力）`。** **段階13 と同じ式で、読むチャネルが「殴る」ではなく「押す」だけ**——**`channels` に4本目を足したのは、damage に比例させたままだと押す構えほど押せないという逆向きの式になるから**。押す専用の遺伝子は作っていない（力は攻撃力、重さは体力上限）。
- **関門は押される側のもの2つ**: 回避＝そもそも押されない（かわした打撃は押しも運ばない）、防御＝押される距離が減る（打撃と同じ `1 − 防御 × 落ち着き`）。
- **効用式に入るのは2つだけ。** **(a) `mine` と `theirs` の両方に `1 − away` を掛ける**（`away = 信念 × est/(v+est)`。押すと相手は est 離れ、自分と同じ速さで戻ってくると仮定）。**閾値はどこにも書いていない**——**押しは「勝てない戦いで、入ってくる damage が効いているとき」に勝ち、それ以外では負ける**。**(b) 相手の背後の地面は、相手の残り体力から引く**（落ちるのは1回なので打撃に足さない。水は「tickあたりの溺れ確率 × 小競り合いの長さ」＝1%未満で、**それが正しい大きさ**）。
- **`lore` に3本目の belief（`shoveWorks`）。** 証拠は「押した直後に2体が `CombatRadius` の外に出たか」で、**押すたびに1回**。**入口が2つある belief は世界で初めて**（押した側と押された側が同じ出来事から学ぶ）。

**途中で1つ決め直したことがある**——**`LearningRate` は既定0で、それには理由がある**（「殴り返される確率 0.7」は世界の事実ではなく抑止力で、真値〔約0.15〕を学ばせると個体数が半分になる）。**押しの belief をこの1本に相乗りさせると、規則を「すでに壊れていると分かっている世界」の中でしか測れなくなる**ので、**`ShoveLearnRate` を別に切った**（既定1）。押しの確率には二重の役目が無いので、これで足りる。

**測った（12シード × 20000tick）**

| 指標 | `countryskill` | `shove`（5） | `shovefar`（20） | `shovefixed` | `shoveone` | `shoveblind` | `baseline`（平坦） | `shoveflat`（平坦・20） |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `pop` | 105.6 | **78.7 \*** | **86.9 \*** | 105.6 | **83.2 \*** | 83.9 | 143.8 | 143.2 |
| `births` | 285.7 | **223.6 \*** | **227.4 \*** | 276.9 | **233.8 \*** | **209.4 \*** | 427.1 | 392.6 |
| `killShare` | 0.65 | 0.65 | **0.59 \*\*** | **0.60 \*** | 0.64 | **0.60 \*\*** | 0.81 | 0.79 |
| `fights` | 42299 | **29782 \*** | **27477 \*\*\*** | **36013 \*** | 36021 | **25601 \*\*\*** | 78451 | **69267 \*\*** |
| `killRate` | 1.51 | 1.40 | **1.18 \*** | **1.21 \*\*** | **1.25 \*\*** | **1.15 \*\*\*** | 1.64 | 1.54 |
| `collapsed` | 0.17 | 0.17 | 0.17 | **0.00 \*** | **0.00 \*** | 0.08 | 0.00 | 0.00 |
| `shoved` | 0 | 4942 | 4454 | 5368 | 5896 | 4024 | 0 | 11324 |
| `shoveBroke`（真値） | — | 0.50 | 0.71 | 0.65 | 0.65 | 0.71 | — | 0.67 |
| `shoveWorks`（信念） | 0.50 | 0.48 | 0.59 | 0.50（固定） | 0.54 | 0.57 | 0.50 | 0.57 |
| `shoveErr` | — | **0.04** | 0.13 | 0.15 | 0.11 | 0.14 | — | 0.11 |
| `shoveWait`（tick） | — | 7.8 | 13.3 | 11.2 | 12.4 | 13.8 | — | 10.7 |
| `knockFell` | 0 | 39.5 | 56.3 | 60.5 | 66.0 | 50.7 | 0 | 0 |

**`shovefar` を基準に取り直した対**（同じ12シード）:

| 指標 | `shovefixed` − `shovefar` | `shoveone` − `shovefar` | `shoveblind` − `shovefar` |
|---|---:|---:|---:|
| `pop` | **+18.67 \*** | −3.75 | −3.00 |
| `births` | **+49.50 \*** | +6.33 | −18.00 |
| `collapsed` | **−0.17 \*** | **−0.17 \*** | −0.08 |
| `fightRate` | **+91.14 \*\*** | **+74.85 \*** | −18.74 |
| `killShare` | +0.01 | **+0.05 \*\*** | +0.01 |
| `shoveWorks` | **−0.09 \*\*\*** | **−0.04 \*** | −0.02 |
| `shoveWait` | **−2.16 \*\*** | **−0.89 \*** | +0.43 |
| `starved` | +10.17 | −8.92 | **−11.67 \*** |

**予想は8つ書いて3つ外れた。外れたほうが大きい。**

- **「`fights` は減らない、むしろ増える」が大外れ**（全部の腕で −9184 \*\* 〜 −16699 \*\*\*）。**指標を読み違えていた**——**`fights` は「戦いの数」ではなく「入った打撃の数」**で、**間合いを切る押しは次の tick の打撃をそのまま消す**。**押しは戦いを長くするのではなく、打撃を薄くする。**
- **「`killShare` は段階13 と同じだけ下がる」も外れ。** **平坦な世界では受動ノックバックが 0.81 → 0.68 \*\*\* を動かしたのに、選べる押しは 0.81 → 0.79（誤差内）**。**理由は明快で、受動の押しは打撃のたびに起きるが、選べる押しは打撃を捨てて初めて起きる**——**押している身体は相手を削っていない。** **つまり「選べる」ことは、この規則を強くせず弱くした。**
- **「`ShovePush` 5 で `shoveBroke` < 0.3」も外れ**（0.50）。**戦っている2体は間合いよりずっと近いと思っていたが、実際は縁の近くにいる。**
- **一番大きいのは「`shovefixed` は区別がつかない」が外れたこと。** **信念を世界の値に凍結すると、個体数 +18.67 \*・出生 +49.50 \*・崩壊 0.17 → 0.00 \***。**このプロジェクトで「情報だけ」が世界を動かしたのは、6度の測定でこれが初めて**（段階31・35・49・100・14番・17番はどれも動かなかった）——**しかも動かした向きは悪いほう**。**押しが効くと知っている集団のほうが小さく、崩れやすい。**
- **当たったのは3つ**: 遊ぶ地図で `pop` が下がる（17番と同じ一般則の3度目。**ただし「平坦で上がる」側は2度目も外れ**）／`knockFell` は受動の2倍を超えない（むしろ少ない。112 に対し 40〜66）／見た目（トレースに `attack #71 e1.00` が4行、右パネルに `a push gets me clear 0.50 (8 seen)`）。

**この項目の結論は、2つの情報の非対称**

- **「押すと離れられるか」という信念（距離についての知識）は世界を動かす**（`shovefixed` との差）。**そして両側から学ぶことがその条件**——**片側だけにすると信念は 0.59 → 0.54 までしか行かず、世界はより殺伐とする**（`killShare` +0.05 \*\*）。
- **「相手の背後に崖がある」という知覚（場所についての知識）は1ビットも動かさない**（`shoveblind` はどの指標も誤差内）。**数え上げが先に言っていたとおり**——**的は 5.4〜5.9% しかなく、角度を取りに歩く身体はこの世界にいない。**

**そして belief は真値に届かない**（用量20で 0.59 対 0.71）。**事前分布が引き戻し、交換が「押したことのない身体」と平均するため**で、**用量5では届く**（0.48 対 0.50）。**`shoveErr` を指標に置いたのはこれを見るため。**

**既定はオフのまま**（`ShovePush` 0）。**指紋テストは1つも動いていない。**

**ベンチ。** **平坦な世界は雑音の中**（`BenchmarkEngineStep` 787,132 → 787,395 ns/op、`BenchmarkDecide` 21,488 → 21,708 ns/op）——**地面が無ければ数え上げは即座に戻るため**。**地形のある世界では +1.5%**（`country` の地図で 104,572 → 106,090 ns/op）で、**これは規則ではなく数え上げの値段**（決定ごとに視界の全員ぶん、セルを2つ読む）。**段階24 の「発火しうる回数はその規則が説明できることの上限」を守るために、規則がオフでも数える**と決めているので、**この 1.5% は地形のある全世界が払う**。

### 死に方を覚える（#137・`TODO.md` の15番。2026-09-21）

**エピソード → パターン → 伝承。** **見ていた身体が止まったとき、その死因とそのとき取っていた行動を組にして覚え、2度目に同じ組を見たら枠に入れ、既存の伝承で広める**（`engine/lesson.go`）。**置き場はユーザの判断＝決定#137 の (b)**——**ヒントの枠と共用せず、予算から買う新しい枠**。

**実装前の数え上げが、この項目の前提をひっくり返した**（4シード × 20000tick）

| 指標 | baseline（平坦） | `countryskill`（遊ぶ地図） |
|---|---:|---:|
| **`deathWatched`** | **1.00** | **0.99** |
| **`deathSeen`** | **18.4** | **10.9** |
| **`lessonRipe`** | **3775** | **1287** |
| `deaths`（うち殺し／溺死／餓死） | 341（279 / 0 / 61） | 248（167 / 17 / 63） |

**`PLAN.md` は「目撃はすでに桁で少ない」と書いていた。** **比べていたのは `drownSeen` 8.3〜8.8・`avengeSeen` 4〜13 という「2つの狭い規則の発火回数」で、「死が見られる回数」ではなかった。** **稀なのは目撃ではなく、その2つの規則が要求する条件のほう**（溺死であること／別種を殺した同種であること）。**この項目の的は、桁で少ないどころか最大級だった。**

**2つ目の数え上げが、設計を1つ削った**（2342死・2地図）。**鍵の内訳**:

| 特徴 | baseline | `countryskill` |
|---|---:|---:|
| **under attack** | **0.82** | **0.64** |
| worn down | 0.11 | 0.13 |
| starving | 0.07 | 0.10 |
| in water | 0.00 | 0.13 |
| **in a crowd** | **0.002** | **0.002** |
| **hard ground** | **0.000** | **0.000** |

**「混んでいた」「歩きにくい地面だった」は2342回の死のうち5回しか勝たない。** **理由は算数で、もっと早く気づくべきだった**——**死んだ身体は定義上ちょうど尽きている**ので死因の特徴は読む瞬間に必ず1.0 になり、**1未満の連続値がそれに勝つことはありえない**。**「状況のうち一番際立ったもの」という鍵は、結局「死因」という鍵**だった。**特徴を6つから4つ（＝死因の4つ）に減らし、順序は世界の死因バケツの優先順位と同じにした**（川は、直前に殴られていても川が取る）。**行動の側は健全**（observe 0.25〜0.33 / move 0.20〜0.28 / eat / attack / flee ほか計10〜11種＝生きている鍵はおよそ44で、`hintKinds` 13.7〜25.3 と同じ桁）。

**形（決めたこと4つ）**

- **語彙は専用の4つ**（`DeathFeature`）。**`HintFeature` に足さなかったのは、足すと `drawHint` の抽選が全世界で変わり指紋がリセットされるから**——**`hint.go` は1行も変わっていない。**
- **枠は買う**（`LessonSlots` 既定0、`LessonSlotCost` 2.5 ＝ `HintSlotCost` 5 の半額。教訓は組だけで重みを持たないので）。**学習でただで能力が増えない**（保存則）。
- **覚えているのは「その鍵を前に1度見たか」の1ビットだけ**（`deathMarks` ＝ 4×19 のビット集合）。**出来事の列も、いつ・どこで・誰がも持たない**——**死んだ身体が死んだときの状況をもう持っている**ので、鍵は `kill` の時点で組み立てられる。**全員に輪バッファを持たせる案は要らなかった。**
- **子は枠だけを継ぐ。** 見たものは継がない。

**測った（12シード × 20000tick）**

| 指標 | baseline | `lesson`（2枠） | `lessonblind`（値打ち0） | `lessonquiet`（教えない） | `lessononce`（1度で昇格） | `lessonwide`（4枠） | `lessonhard`（重み3倍） | `countryskill` | `lessonplay` |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `pop` | 143.8 | **118.3 \*** | 129.3 | 130.8 | **120.3 \*** | **112.3 \*** | **119.0 \*** | 105.6 | **45.7 \*\*\*** |
| `births` | 427.1 | **358.2 \*** | 377.1 | 373.8 | **349.0 \*** | **351.5 \*** | 368.3 | 285.7 | **151.2 \*\*\*** |
| `starved` | 75.8 | **59.3 \*** | 70.3 | 69.8 | **61.9 \*** | 79.4 | 76.7 | 78.7 | **64.8 \*** |
| `killShare` | 0.81 | 0.80 | 0.80 | 0.80 | 0.80 | **0.77 \*** | **0.77 \*** | 0.65 | **0.60 \*** |
| `collapsed` | 0.00 | **0.08 \*** | **0.08 \*** | **0.08 \*** | **0.08 \*** | **0.08 \*** | **0.17 \*** | 0.17 | **0.58 \*\*** |
| `lessonTaken` | 0 | 22 | 24 | **353** | 94 | 32 | 20 | 0 | 15 |
| `lessonCopied` | 0 | 369 | 387 | **0** | 323 | 947 | 423 | 0 | 212 |
| `lessonHeld` / `lessonSlots` | — | 0.86 / 0.94 | 0.86 / 0.89 | 0.75 / 1.13 | 0.95 / 1.03 | 2.14 / 2.37 | 0.96 / 1.11 | — | 0.46 / 0.89 |
| `lessonKinds` | 0 | 3.5 | 4.1 | **7.8** | **10.5** | 6.4 | 4.3 | 0 | 1.9 |

**`lessonblind` を基準に取り直した対**（同じ12シード・**同じ枠・同じ予算**で、違うのは値打ちだけ）:

| 指標 | `lesson` − `lessonblind` | `lessonhard` − `lessonblind` |
|---|---:|---:|
| `pop` | **−11.08 \*** | −10.33 |
| `starved` | **−11.00 \*** | +6.33 |
| **`fights`** | **+6089 \*** | **+5799 \*** |
| **`flees`** | **+2.63 \*** | **+4.38 \*\*** |
| `killShare` | +0.01 | **−0.03 \*** |
| `collapsed` | +0.00 | **+0.08 \*** |
| `lessonKinds` | **−0.58 \*** | +0.25 |

**予想7つのうち3つが外れ、そのうち1つは「外れ方が測り方のせいだった」。**

- **一番大きいのは方法論のほう。** **「`lessonblind` と区別がつかない」は baseline の下に2行並べる限り当たっていた**（`pop` −25.6 \* 対 −14.5）**が、`lessonblind` 自体を基準に取り直すとはっきり違う**（`pop` −11.08 \*・`fights` +6089 \*・`flees` +2.63 \*）。**代価を共有する対照は、共通の baseline の下に並べるのではなく、それ自体を基準にしないと答えにならない**——**予算が動かす量のほうが規則が動かす量より大きいときは必ずこうなる。** **この世界で「情報だけでは動かない」と結論した過去の測定は、この形になっていなかったか確かめる価値がある。**
- **個体数の内訳が取れた**: **baseline に対して −25.6 \* のうち、枠の値段が約 −14.5、教訓の中身が −11.1 \***。**両方が同じ向きに払っている。**
- **機序の予想はそのまま当たった**: **鍵の64〜82%が「殴られている最中」で、そこで沈むのは observe と move**なので、**残る手は戦うか逃げるか**——**`fights` +6089 \*・`flees` +2.63 \***、**用量3倍で `killShare` −0.03 \***。
- **「2度目で昇格」は帳簿を縛るが世界を縛らない**: `lessononce` は昇格を4倍にする（22 → 94、種類 3.5 → 10.5）のに、**世界の指標はどれも `lesson` と区別がつかない。**
- **伝承は自力の学習を助けるのではなく置き換える**（予想は「伝承の出番がない」で、逆に外れた）: **通常の腕では枠の94%が「教わって」埋まり**（`lessonCopied` 369 対 `lessonTaken` 22）、**教えるのを止めると自力が全部やる**（0 対 353）。**総量はほぼ同じ**（約370）。**そして教わると種類が減る**（3.5 対 7.8）——**ヒントでは逆だった**（模倣を切ると 7.11 → 4.38 に落ちる）。**同じ経路が、生まれつきの思い込みには多様性を配り、見て覚えた規則からは多様性を奪う。**
- **`starved` は上がるかもしれないと書いたが、減った**（−11.00 \*）。「飢えているときに食べるのをためらう」という害は出ていない。

**既定はオフ**（`LessonSlots` 0）。**理由は「効かないから」ではなく「効いて、世界を小さくするから」**——**遊ぶ地図では `pop` −59.9 \*\*\*・崩壊 0.17 → 0.58 \*\***（**枠を買っても埋められない**＝`lessonHeld` 0.46 対 `lessonSlots` 0.89）。

**ベンチは雑音の中**（`BenchmarkEngineStep` 787,395 → 781,842 ns/op、`BenchmarkDecide` 21,708 → 21,814 ns/op）。**指紋テストは1つも動いていない。**

### 継承と転移——王朝の遊び方（#130・`TODO.md` の6番。2026-09-21）

**段階19・23 で「1ノードを動かす」「転機に訊かれる」まで作ったが、勝ち負けが無かった。** これがそれ——**勝ち＝地図が印を付けたゴール区画すべてに自分の血統が同時に定住していること**、**負け＝死後10年たって世界のどこにも担い手がいないこと**、**死ぬと10年（5000tick）が誰の操作もないまま流れ、そのあと区画ごとに1人の転移先リストが出る**。

**置き場は全部ゲーム側**（`cmd/devview/dynasty.go`）。**engine にはプレイヤーもゲームオーバーもゴールも止まる時計も無い**（段階19 の線）。**engine に足したのは読み取り専用の2本だけ**で、**どちらも「誰が操作しているか」と無関係な身体についての事実**:

- **`World.LineHeirs(line)`** — その血統の、区画ごとに最年長の生きている担い手（区画 → ID）
- **`SettlementTracker.SettledIn(id)`** — 1体が定住している区画（`Result` は集団の平均を答える別の問い）

**実装前に数えた（12シード × 20000tick ＋ 10年の待ち。遊ぶ地図＝`country` ＋ スキル。ゴールは12区画のうち3つ）**

**この数え上げが、`PLAN.md` が選んでいた継承則を否定した。**

| | 木（全員が継ぐ） | 地域ごとに1人 |
|---|---:|---:|
| 担い手のいる区画（12中） | **9.25** | **2.09** |
| 10年の待ちのあと | **9.08** | **0.91** |
| ゴール3区画のうち押さえている数 | **2.08** | **0.36** |
| 3つとも押さえている | 0.42 | **0.00** |
| 少なくとも1つ | **0.83** | **0.36** |
| 10年後に血統が消えている | **0.17** | **0.55** |
| 10年後の血統の大きさ | **×1.20** | **×0.32** |

**「地域ごとに1人」では遊べない**——**転移メニューが空になる確率 0.64、1回の待ちで血統が消える確率 0.55**。**最初の死で半分以上の run が終わる。**

**なぜ前の測定と食い違ったのか**: **`PLAN.md` の 0.58 / 0.40 は平坦な校正世界（12区画・個体数143）のもので、遊ぶ地図は個体数105**。**1区画に担い手を1人しか置かない規則は、個体数が減るとそのまま担い手が減る。** **「きれいな数字が出た地図」と「遊ぶ地図」を取り違えていた**——**この種の取り違えは今回が初めてではない**（14番・17番の「平坦では動かず遊ぶ地図で動く」と同じ根）。

**決着: タグは木のまま。** **「地域ごとに1人」はメニューの形として残った**——`LineHeirs` が区画ごとに最年長を1人ずつ返すので、**選ぶのは身体ではなく国**。

**そして同じ日、ユーザの指摘で、この数え上げが測っていたものが誤解だったと分かった。**

**ユーザの意図**: **ある区画に子が3人生まれ、長男が死んだら次男、次男も死んだら三男。三男に子がいて三男が死んだら、その子。新しい出生を待たない。**

**`LineageRegionChain` の実装**: **タグは出生の瞬間にしか配られない**（`world.go` の `child.Lineage = 0`）。**兄が立っている区画で生まれた弟は永久に担い手にならず、担い手が死んだ区画は次にそこで生まれた子が拾うまで空**（生まれなければ永久に空）。**0.55 の絶滅率はこの差の帰結**であって、「1区画1人という考え方」の帰結ではなかった。

**構造的な理由**: **「今この国の跡継ぎは誰か」は誰かが死んだ瞬間に変わる問いで、出生イベントは関与しない。** **出生時に一度だけ書くタグでは原理的に表現できない**——**表せるのは問い合わせだけ。**

**`LineageRegionChain` は削除した**（2026-09-21・ユーザの判断）。**`Config.LineageRule` は木と鎖の2つ**、`cmd/experiment` の `lineregion` の腕とテスト1本も消した。**意図は `LineHeirs` がそのまま満たしている**——**3兄弟の例を流すと 長男 → 次男 → 三男 → 三男の子** と降りる（**出生を待たない**）。

**`PLAN.md` が残していた「2本目のタグが要るかもしれない」への答えは「要らない。そもそもタグでは書けず、問い合わせで書くものだった」。**

**実装中に見つけた穴**

- **`SettlementTracker` は読みが1回でも「定住」と答える。** **instrument としては正しい**（`settledIn` のコメントが明示している＝世界の最初の1000tick を黙らせないため）**が、勝敗に使うと tick 0 で全員が「今いる場所に定住している」ことになり、run が始まった瞬間に勝つ**。**ゲーム側で「窓1つ分（40読み）が溜まるまで勝たない」と決めた**——**engine の instrument は変えない**（他の読み手がその挙動を前提にしている）。**テストで固定した。**
- **読みの周期をフレームではなく時計に取り直した。** **最初は `Tick() % 25 == 0` で読んでいた**が、**フレームと tick は同じものではない**——**速い速度では1フレームに数tick進むので読みを飛ばし、1/10速度では同じ tick が10フレーム続くので窓が同じ位置の写し10個で埋まる**（**歩いている身体が「定住している」に見える、まさにその読み**）。**「前に読んだ tick から25以上経ったら読む」に直した。** **テストで固定した。**
- **`tiled/samples/001_3division.tmj` は地域レイヤが非表示**（`"visible": false`）。**ローダは見えないレイヤを飛ばす**ので、**このサンプルを読むと地域が0個になり、`goal` を付けた「south」も出てこない**。**今回の変更とは無関係の、前からあるもの**だが、**塗った地域にゴールを付ける経路がこのファイルからは一度も通っていない**ことになる。**目視は `visible` を true にした写しで行った。**

**目視**（`-tiled <goal 付きの地図> -dynasty`）: **死 → `ten years pass with nobody at the wheel` → 10年 → `LOST: ten years after #13 died, your line has nobody left anywhere`** まで通った。**転移メニューは試した2シードでは出ていない**（どちらもその前に血統が絶えた。**このサンプル地図は個体数15〜20と厳しい**）——**メニューの形はテストで固定してある。**

**残った申し送り1つ**: **`-dynasty` が身体を選ばずに始めると `quickestBody`（一番速い成人）を取る**ので、**もう絶えかけている血統を渡されることがある**。**数え上げは「一番大きい血統」で測っているので、既定の始め方はその数字より不利。** **プレイヤーがクリックして選べば済む話**だが、**「どの身体から始めるか」自体が遊びの一部になるべきかは決めていない。**

**ベンチは雑音の中**（`BenchmarkEngineStep` 781,842 → 782,148 ns/op）。**指紋テストは1つも動いていない**（engine に足したのは読み取り専用の2本だけ）。

**通したあとに「ゲームとして成立するか」を測った（12シード × 60000tick）。答えは「まだ」。**

**方法は「誰も操作しない床」**——全員が世界の効用式で動き、「プレイヤー」は転移のときに国を選ぶだけ。**ここで勝ててしまうなら、勝っているのはプレイヤーではない。**

| 条件 | 勝ち | 負け | 転移の回数 |
|---|---:|---:|---:|
| **今の実装のまま**（`quickestBody`・ゴール3） | **0.17** | **0.83** | 0.75 |
| 一番大きい血統から始める・ゴール3 | **0.67** | 0.33 | 1.33 |
| 同・ゴール2 | 0.75 | 0.25 | 1.25 |
| 同・ゴール1 | 0.92 | 0.08 | 0.25 |
| 同・待ちを2年に短縮 | 0.67 | 0.33 | **2.42** |
| 「自分の子孫」に絞る・ゴール3 | 0.75 | 0.25 | 0.92 |
| 同・ゴール2 | 0.75 | 0.25 | 0.92 |

**時間切れは0**（どの run も60000tick 以内に決着する）。**そして「ゴールを狙って転移」と「近い順に転移」で勝率が変わらない**（ゴール2で 0.75 対 0.75、ゴール3で 0.75 対 0.75）。

**3つとも悪い知らせ。**

1. **既定の始め方が壊れている**（0.83 で負ける）。**`quickestBody` は血統の生死を見ない**ので、**一番速い身体がたまたま属している小さな血統**を渡される——**最初の死の10年で消える**。**難易度ではなく、何もしないうちに決まるコイン投げ。** **大きい血統から渡せば 0.17 → 0.67 に動く。**
2. **直すと、今度は何もしなくても勝つ**（0.67〜0.92）。**この測定では誰も操作していないので、勝っているのは世界の漂流。**
3. **選択に意味が無い。** **転移の中心である「どの国で続けるか」が勝率を1ビットも動かさず、転移そのものが1実行に 0.25〜1.33 回しか起きない**（10年の待ちが一生の大半なので2回目の前に決着がつく）。**待ちを2年にすると 2.42 回に増えるが、勝率は動かない。**

**「自分の民族」の定義を狭めても直らなかった**——**木でも自分の子孫の部分木でも勝率は同じ**（部分木も同じ速さで広がる）。**`PLAN.md` の「木は緩すぎる」は正しかったが、狭め方の問題ではなかった。**

**構造の問題として読める**: **勝利条件は集団のふるまい（N区画に定住）についてのもので、プレイヤーが握っているのは1体**。**てこが条件に対して小さすぎ、どう狭めてもN−1区画は世界が勝手に埋める。**

**次に触るときに測るべき目標2つ**: **(a) 誰も操作しない床の勝率が低いこと**（今 0.67〜0.92。0.2 を切るくらい）、**(b) 選択が勝率を動かすこと**（今 0.00）。**候補は「プレイヤー自身の身体が達成に関与しなければならない条件」だが未測定。**

**この測定は engine にもゲームにも残していない**（使い捨ての harness で回した）。**残すなら `cmd/experiment` の腕ではなく、ゲーム側の回帰テストになる**——**測っているのは世界ではなく遊びだから。**

### 人間の行動圏と、1年の待ち（#130 の続き。2026-09-21・ユーザの指定）

**「ゲームとして成立していない」への手当てを2つ試した**——**人間に行動圏を与える**（`HumanHomeCost`、既定0）と、**死後の待ちを10年から1年へ**。**あわせて「10年に1度の天才」という想定を取り下げた**（ユーザの判断。**待ちの長さを10年に縛っていたのはこれだけ**で、**天才はプレイヤーのものか、意図して配備するもの**＝`Inspire`）。

**行動圏は段階64 の `EnemyHomeCost` を人間に広げただけ**（`homeFor` の種族ゲートを外し、`HumanHomeCost` を1本足した）。**`HomeX` / `HomeY` / `HomeRegion` は元から全員に入っている**ので、**新しい状態は0**。**課し方も同じ**（地域の幅あたり・1tickあたりの効用の一項。leash ではない）。

**測った（遊ぶ地図。勝利条件は「その地域で生まれた自分の子孫が、そこに定住している」を全ゴールで同時に。プレイヤーは『満ち足りているときだけ未踏の国へ歩く』方策）**

| 条件（12シード × 120,000tick） | 勝ち | 建国 | 定住 | 世代 | 絶滅 | 訪れた地域 |
|---|---:|---:|---:|---:|---:|---:|
| 10年の待ち・行動圏なし・旅する | 0.08 | 1.58/3 | 1.50 | 1.75 | **0.92** | 7.08/12 |
| 1年の待ち・行動圏なし・旅する | 0.42 | 1.67 | 1.92 | 2.50 | 0.58 | 7.58 |
| 1年・行動圏1・漂流 | 0.33 | 1.33 | 1.58 | 2.83 | 0.67 | 6.75 |
| 1年・行動圏1・旅する | 0.42 | 1.50 | 1.83 | 2.08 | 0.58 | 6.67 |
| 1年・行動圏3・漂流 | 0.08 | 0.75 | 0.92 | 2.58 | **0.92** | 5.42 |
| 1年・行動圏3・旅する | 0.17 | 1.17 | 1.42 | 2.42 | 0.83 | 5.25 |

**そして差が出かけた用量を36シードで引き直した**:

| 36シード | 勝ち | 建国 | 絶滅 | 訪れた地域 |
|---|---:|---:|---:|---:|
| 行動圏1・漂流 | 0.25 | 1.17 | 0.75 | 6.33 |
| 行動圏1・旅する | **0.31** | **1.47** | 0.69 | 7.14 |
| 行動圏なし・漂流 | **0.42** | 1.50 | 0.56 | 7.03 |
| 行動圏なし・旅する | 0.25 | 1.56 | 0.72 | 7.33 |

**わかったこと4つ。**

1. **待ちを1年にしたのは効いた。** **絶滅 0.92 → 0.56〜0.75**、**1実行で遊べる世代が約2から約3.5へ。** **10年は代価ではなくゲームそのものだった**（プレイヤーが何かする前に9割が終わっていた）。
2. **行動圏は狙いどおり徘徊を減らす**（訪れた地域 7.03 → 6.33〈用量1〉→ 5.4〈用量3〉）。**機械としては正しく働いている。**
3. **それでも旅は勝率を動かさない。** **36シードで 0.31 対 0.25**（2シード差＝雑音）。**行動圏なしでは旅のほうが悪い**（0.25 対 0.42）。
4. **旅は「建国」だけは確かに増やす**（1.47 対 1.17、+26%）。**つまりプレイヤーの行為は中間指標には出ていて、勝敗には出ていない。**

**診断はこの (4) と (3) の差にある**: **プレイヤーは新しい国に子孫を置ける。置いた国を同時に保てるだけ血統を生かしておけない。** **どの腕でも絶滅が 0.56〜0.92 を占めており、そこにプレイヤーの手が届いていない**——**操縦しているのは1体で、その1体は他の100体と同じ効用式で生き死にする。**

**用量にトレードオフがある**: **行動圏を上げると旅は際立つが、血統が死ぬ**（用量3で絶滅 0.83〜0.92）。**貧しい土地に生まれた身体がそこに留まって餓える**ためで、**「際立たせること」と「生き延びること」を同じつまみが逆向きに動かしている。**

**この測定の限界を明記する**: **ここでの「プレイヤー」は粗い方策**（満腹かつ健康なときだけ未踏の国へ歩き、それ以外は効用式に任せる）。**人間が食う・戦う・逃げる・口説くを選んだらどうなるかは測れていない**——**この数字は床と「下手なプレイヤー」の推定であって、天井ではない。**

**次に測るべきは「プレイヤーに血統の生存を動かす手があるか」**。**無いなら、血統の状態に書いた勝利条件はどれもゲームにならない**（結果が操作と独立なので）。

### プレイヤーを天才にする、系統に予算を配る（#130 の続き。2026-09-21・ユーザの提案）

**提案**: **プレイヤーは天才**で、**死ぬと既に系統がある地域から生まれ変わる地域を選べ**、**そこで同系統の交配が起きれば天才として生まれる**。**加えて「長男系統は天才まで行かなくとも予算多め」はどうか**、という問い。

**前回の診断が「プレイヤーに血統の生存を動かす手が無い」だった**ので、**そこに効くかを測った**（24シード × 120,000tick・ゴール3・行動圏1・待ち1年。**天才＝その身体の9遺伝子を係数倍に `Endow` し `Inspire` を1回**。**どちらも新しい予算で払う既存の例外**）。

| 腕 | 勝ち | 建国 | 定住 | **世代** | **絶滅** | 訪れた地域 |
|---|---:|---:|---:|---:|---:|---:|
| 素・漂流 | 0.25 | 1.12 | 1.25 | 2.88 | 0.75 | 6.17 |
| 素・旅する | 0.21 | 1.04 | 1.33 | 1.96 | 0.79 | 5.92 |
| **天才（プレイヤーの身体だけ）・漂流** | **0.50** | **1.92** | **1.96** | **7.08** | **0.46** | 7.75 |
| 天才・旅する | 0.29 | 1.50 | 1.67 | 3.38 | 0.71 | 7.04 |
| 系統に+30%・漂流 | 0.38 | 1.38 | 1.67 | 4.96 | 0.62 | 7.29 |
| 系統に+30%・旅する | 0.29 | 1.42 | 1.62 | 2.00 | 0.71 | 7.25 |
| 系統に+15%・旅する | 0.33 | 1.46 | 1.71 | 2.17 | 0.67 | 6.92 |

**3つわかった。**

1. **「プレイヤーは天才」は効く。これまでで一番大きい効果。** **絶滅 0.75 → 0.46、遊べる世代 2.88 → 7.08、勝率 0.25 → 0.50。** **前回「無い」と言った梃子がこれ**——**血統の生存が、プレイヤーに与えたものに反応した。**
2. **系統全体に配るのは、プレイヤー1人に与えるより悪い**（勝率 0.38 対 0.50、世代 4.96 対 7.08）。**用量を半分（+15%）にしても 0.33 で届かない。** **仮説は食料の総量が固定であること**——**大きい身体は空腹の進みが速い**ので、**系統の半分を大きくすると取り分が増えないまま食い扶持だけ増える**。**未検証**（分けて測れば分かる）。
3. **旅は依然としてどの腕でも勝率を下げる**（天才で 0.50 → 0.29、世代 7.08 → 3.38）。**旅は寿命を半分にする**——**歩けば体力を使い、川を渡り、外敵に会い、食べ物と相手から離れる。** **遊びの中心に置かれた行為が、いまは罰になっている。**

**結論**: **「プレイヤーは天才」は採るべき**（既存の `Endow` / `Inspire` で足り、**保存則の例外として文書化済みの経路**）。**「長男系統も予算多め」は採らないほうがよい**（測ると悪い）。**それでもまだ成立しない**——**旅が勝率を下げる限り、「未踏の地域へ行く」を中心に据えた遊びにはならない。**

**限界（前回と同じ）**: **この「プレイヤー」は粗い方策**（満腹かつ健康なときだけ未踏の国へ直線で歩く）。**人間なら食料と危険を見ながら旅する**ので、**「旅は損」は方策の性能の話かもしれない**。**天才の効果（絶滅 0.75 → 0.46）は方策に依らない**ので、そちらは確か。

## 追記: 人間の絵を生成物に替えた（2026-09-21）

**`cmd/tiles` の 16px の灰色の置き絵から、生成した 32px の描かれた絵へ。** 外敵だけは置き絵のまま同じ1枚のシートに同居している（クリップが自分の `w`/`h` を持つので、画面上では同じ大きさに描かれる）。手順と頼み方は `docs/sprites.md`、道具は `tiled/tools/pack_sprites.py`。

**届いた3枚のうち、素材になったのは3枚目だけ**だった。以下は3枚から測ったことで、`docs/sprites.md` の指示はこの測定から出ている。

### 1枚目（`tiled/samples/icons.png`）— 仕様書であって素材ではなかった

1312×1199・RGB（アルファ無し）・**213,696色**・**同じ行も同じ列も1つも繰り返しがない**（キャラ領域で全 run 長が1）。**1ドットの実寸は約 3.7px の非整数。** パネル・枠・日本語のラベルつきの1枚絵で、機械的に切り分けられる格子が存在しない。

**縮尺はパネルごとに違った**: 立ち 61×**118**px、歩き 57×**104**px、戦闘 71×**102** / 64×**98**px。**同じ「32ドットのキャラ」のはずが最大12%差。** いっぽう**パネルの中の A/B フレームはインクの重心差が幅の1%未満**で揃っていた。**「生成は画像単位で一貫する」**という、以降ずっと使うことになる事実がここで出た。

**白背景が使えないことも分かった**: 髪色8種のうち白髪の頭は、**3520画素中1919画素が「ほぼ白」**。明度で背景を抜くと白髪と白肌が背景ごと消える。

**髪と肌は後から色を変えられないことも分かった**: 色相ヒストグラムで茶髪（hue 20〜30、S 0.64）と肌（hue 25〜35、S 0.44）が**完全に重なっていた**。「1体だけ描いてもらって変換側で髪色8種を作る」は不可能で、**髪色は描いてもらうしかない**。服は青（hue 200〜219）とピンク（hue 340〜359）で分離できる。

**32pxに落とすこと自体は通った**（プロトタイプで確認）。44px・88px でよく読め、22px で読め、7〜14px では潰れる——**が、それは今の16px絵でも円でも同じ**で、その倍率ではリングとバーが読みを担っている。

### 2枚目（`tiled/samples/10humans.png`）— 歩きだけが返ってこなかった

マゼンタ背景・文字なし・10行・各行の左端に基準キャラクター。**74体が抜けて 20クリップ・52フレームに詰まった。**

**基準キャラクターは、割り算の分母ではなくドリフト検出器として効いた。** 10本中8本が±2%で一致（97 96 96 96 93 96 95 93）し、外れた2本（82, 83）は**行の中身に引きずられて基準自体が子供として描かれた行**だった（行7の子供 79〜83 と基準 83 が同じ高さ）。**行ごとに割ると子供が大人サイズになる。**

**歩きが立ちと区別できなかった。** 32pxに落としたあとのフレーム差（本体約450画素中）で、ペア間の差101〜181画素は**ペア内の A/B 差と同程度**——脚が振れていない。**「歩行」とだけ頼んだのが原因。**

ほかに、女性の組に青い短パンの男性が1体、1体だけ黒髪（基準は茶髪）、**倒れた体が34〜38px幅で32pxタイルに入らない**（幅で合わせて11%縮めた）。

### 3枚目（`tiled/samples/10humans_new.png`）— 採用

11行。**歩きが直った**: 立ち A/B の差89画素に対し、**歩き A/B が214画素、立ちと歩きの差が347画素**。

**基準キャラクターは2度目も定数にならなかった**（69〜93で幅26%、11本中3本が白いオムツの子供）。**約束されないものとして扱うことにした。**

**そして単純な縮尺ずれではなかった。** 同じ体が **立ち90・歩き83・攻撃81・被弾79** と**戦闘中に12%縮み**、いっぽう**頭の幅は 52→56 と太っていた**。**行が進むほどチビ寄りになる画風のドリフト**で、**頭に合わせると胴の縮みが増幅され、身長に合わせると頭が揃わない**——**どちらか一方に寄せる規則では直らない。** **身長側に寄せた**（地面と他の体に対して目が比べるのは身長のほう）。**しゃがむ・水に入る・子供・倒れるは正規化しない**（高さがそのまま言いたいこと）。

結果、タイル内の高さは **立ち・歩き・攻撃・被弾・髪色・老人が30**、**食事26・水中25・子供25〜26**、**死亡14〜15**。**老婆が31px と成人より高く出たので直した**（原画で老人男性82・老婆92の12%差はドリフト。**老いは白髪と禿頭が言っている**）。

**23クリップ・57フレーム・256×672・79.6 kB**（うち外敵4クリップは 16px の置き絵）。

### 色を掛けるのをやめた

**これがコード側の唯一の設計変更。** 今までは灰色の絵に `ColorScale.ScaleWithColor(性別色 × ID のゆらぎ)` を掛けていた。**描かれた絵に掛けると、性別の青（#2a78d6）で水死体、ピンク（#e87ba4）で赤鬼になる**（実際に掛けて確かめた）。

**掛けるかどうかは `manifest.json` の `tint` がクリップごとに言う。** 灰色の置き絵は掛けないと見えないので掛け、描かれた絵は掛けない。**コードではなくシートが言う**のは、どちらであるかがシートの性質だから。

**そのぶん性別は `clipFor` が言う**（`a.Sex` で分岐し、青い短パンとピンクのワンピースで読む）。**円の塗りだった事実を絵の側へ移しただけ**で、輪の太さ＝攻撃力・輪の色＝状態・尾＝速さ・帯＝空腹・緑の帯＝残り体力は全部そのまま。

**縮小のフィルタも直した**: 拡大は最近傍（遊ぶ倍率では絵が大きくなるので、それ以外だと潰れる）、**縮小は線形**（世界全体を映すと身体は7px で、最近傍だと6画素に5画素を捨てて動くたびにちらつく）。

### まだ無いもの

**外敵の絵は1枚もない**（3枚目のシートの注記自身が「外敵側のスプライトで対応します」と書いていて、実際1体もいない）。今は `enemy.idle/walk/eat/fight` に `cmd/tiles` の置き絵が出ている。**頼み方は `docs/sprites.md` の5節**に書いた。

**シートには使っていないクリップが眠っている**——被弾・水中・死亡・子供・老人・髪色8種。**使うのは `clipFor` に1行足すこと**で、絵をもう一度頼むことではない。

### 手順を再現可能にした

`cmd/tiles` の `-out` を必須にした。**素で回すと `cmd/devview/assets/` の描かれた絵を置き絵で上書きしてしまう**ため。今の手順は2行で、`README.md` と `docs/sprites.md` にある。

### 絵にしてから直した3つ（同じ日）

絵を入れた画面を見て出た3つのうち、2つを直した。**どちらも engine は1行も変わっていない。**

**① 人の大きさを揃えた。** **60人を60通りの大きさで描くと画面が潰れる**——円なら半径がそのまま読めたが、身体の絵になると輪郭がばらばらなだけの雑音になる。**人は `AgeFactor` だけで大きさが変わり**（子供＜大人、老人＜大人）、**予算では変わらない**。**外敵は今までどおり身体の大きさ**（「それがどのくらい大きいか」が外敵について最初に訊かれることで、置き絵の塊が答えられる唯一のこと）。

**捨てた読みは足元の帯へ移した**: **帯の長さ＝体力上限、帯の埋まり＝残り体力**。**「傷ついた大きい身体と健康な小さい身体が同じに見える」のを避けるのが予算の狙いだった**（`main.go` の元のコメント）ので、その差は残っている。**身体・輪・吹き出しの位置の3つが同じ答えを使うよう `bodySize` 1か所にまとめた。**

**② 人の遺体を食べ物として描くのをやめた。** **`dropMeat` は死因を問わず全部の死体に対して呼ばれ**、**肉は `From` に誰の種族から来たかを覚えている**（`Food.From`。**`f.From == a.Species` なら食べられない**＝自分の種族の肉は食べない）。**つまり人の遺体は常に世界に落ちていて、外敵だけが食べられる。** それをビューアが外敵の肉と同じ骨付き肉で描いていたので、**死者の多い世界が誰かの食料庫に見えていた**（実際の画面では死者4416・戦闘死2875）。

**人の遺体は骨にした**（`remains` クリップ。`cmd/tiles/art.go` の置き絵で、絵を頼むまでの繋ぎ）。**規則は1つも変わっていない**——**ビューアが、engine が最初から持っていた区別に追いついただけ**。**`itemClip` がその1か所**で、テスト（`TestAPersonsRemainsAreNotDrawnAsAMeal`）が**人と獣が別の絵になること**と**肉以外がこの分岐に巻き込まれないこと**を押さえている。

**③ 群れを群れとして描くようにした**（手 (A)。**engine は1行も変わっていない**）。

**身体には大きさが無い**——「重なってはいけない」という規則が世界に1つも無いので、**すり抜けているのではなく、そもそも触れていない**。そして**世界は800幅・身体は18幅に描かれる**ので、良い土地に集まった40体は**立っている場所に物理的に入らない**。円のときは輪が重なるだけで読めたが、人の絵になった日に混ざって読めなくなった。

**効いたのは2つのうち「Y順に奥から描く」ほうだった。** 今までは `agents` の順に描いていて、**奥の身体が手前の身体の上に押し出されていた**——それが「重なり」ではなく「混ざり」に見えていた主因で、**並べ替えは嘘を1つもつかない**（誰も1ミリも動かない）。

**もう1つ（近すぎる身体を押し離して描く）は効きが弱い。** 実測すると、**30×30 に40体では全員が上限5.0に張り付き**、11未満の対は 245 → 179 にしか減らない（25体なら 108 → 66、closest 0.3 → 2.4）。**完全に離すには18要るが、それでは身体が居ない地面に描かれる**ので、**上限を上げる気はない**——これは「重なりを無くす」細工ではなく「完全に同じ点に立っている塊をほどく」細工である。

**身体を指すものは全部同じずらした位置を使う**（絆の線・向かっている線・輪・帯・吹き出し・**クリックの当たり判定**）。**描かれた場所と選ばれる場所が違うほうが、混ざって見えることより悪い。** 重なった身体どうしの押し離す向きは **ID から引く**ので、フレームごとに震えない（テストで固定）。

**`-huddle` で元に戻せる。** 絵を曲げている規則は、曲げていない姿と比べられなければ検証できない。

**engine 側で本当にぶつけるか（手 (B)）はまだ決めていない。**
