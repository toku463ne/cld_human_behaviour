# ゲーム化（人が動かす）

**人間コントローラ、訊かれて答える遊び方、性格、贈与。**

**`docs/features.md` から 2026-09-20 に分割したもので、文章は1文字も変えていない。** **索引と書き方の決まりは `docs/features.md`。**

**ここは追記順**（`docs/features.md` にあった順のまま）**なので、探すときは段階番号か `Config` のフィールド名で grep する。**

---

- 人間操作（`engine/human.go`、段階19）: `HumanController` は `Controller` の2つ目の実装で、**UIが置いた「命令」を、訊かれたときにそのまま返すだけ**。engine は誰が操作しているか知らない（`AIController` は1行も変えていない＝**指紋テストが無変更で通ることが証拠**）。**設計上の約束は2つ**——(1) **プレーヤーに見せるのは `Perception` の中身だけ**（真の攻撃力・防御力・回避率は出さない。AIが読めないものを人に見せると別のゲームになる）、(2) **狙えるのは視界の中だけ**（`Order` が視界外の相手を断る。「知識」は縛るが「賢さ」は縛らない＝勝てない喧嘩を売る自由はある）。**engine はいつ訊くかを変えない**ので命令は据え置きで、**即時反映は既存のトリガー機構を通す**（`World.RequestDecision` → `TriggerRequested`）。**命令が流れるのは2か所だけ**: 狙いが消えた（`TriggerTargetLost` で訊かれた）ときと、**身体が変わった**とき——AIは訊かれれば全候補を採点し直すので勝手に立ち直るが、据え置きの命令は消えた相手を永遠に指し続けるため。**血統の乗り換えは同じコントローラを子に付け替えるだけ**（`World.Heirs` ＝ 生きていて成人した子。成人の線は `Agent.IsAdult` ＝ 求愛できるかと同じ線）。**「プレイヤー」も「ゲームオーバー」も engine には無い**（後継の指名も血統の終わりも `cmd/devview` 側）。**「誰として続けるか」は死んだときだけでなく、`t` でいつでも訊ける**（2026-09-06。**engine は最初からこれを許していて**——コントローラは付け替えられるもの——**縛っていたのは devview だけだった**。置いていった身体は**死なず、世界のAIに戻って自分の一生を続ける**。**血統は前に進めるもので、死ぬまで待つ列ではない**）。**死んだときに「誰として続けるか」を訊くのも `cmd/devview` 側**（2026-09-06）——**`World.Heirs`（成人した子）より広い候補を出す**のは、あらかじめ後継を指名する線と、身体が死んだ瞬間の線が別だから: 生まれたばかりの子しかいない血統は `Heirs` では空になり、**子が画面に映ったまま「血統は終わり」と言われていた**。成人前の身体は弱い手であって不可能な手ではないので、**隠さずにラベルを付けて出す**（`its child, still growing (23% grown)`）。`Perception.Trigger`（なぜ訊かれたか）はこの段階で足した——AIは読まない。

- 訊かれて答える遊び方（`engine/guided.go`、段階23）: `GuidedController` は `Controller` の3つ目の実装で、**`AIController` を内包して全部の質問に答えたうえで、転機だけ人に回す**。**AIは留守番ではなく常にその個体を動かしている側**——だから**誰も答えない誘導ノードは純粋なAIノードと1ビットも違わない**（テストで固定）。**訊くのは5つのトリガーだけ**（殴られた・目標消失・配偶者発見・破局・体力急減）で**戦闘は1戦＝1回**。**`TriggerRequested`（人が自分から訊いたとき）も転機に入れてある**——世界はこのトリガーを出さないので、これは「転機と転機の間にできることが見ているだけになる」のを防ぐための、人だけが持つ1つの動詞（実測 112 tick に1回＝一生で20〜30回。全トリガーだと 14.5 tick に1回で、その53%が「殴られた」）。**選択肢はその個体が実際に採点した候補**（`DecisionTrace.Options`）で、**メニューを別に書かない**。**並び順はその個体自身の `ChoiceNoise` を乗せたスコア順**（ノイズ抜きの順位を見せると、その身体の頭の良さ遺伝子が買っていない賢さを渡すことになる）。同じ（行動, 対象）は effort 違いをまとめて**上限12件**（メニューではなく**場**を渡す——「一番安全な手」と「一番大胆な手」はこの順位の中で隣り合わないので、何を見せるかは見せる側が決める）。**絞るときは行動の種類ごとに1つ必ず残す**（`keepAKindOfEach`）——素直な上位N件は**求愛と食事から先に消す**が、満腹なノードがどちらも安く見積もるその瞬間こそ人が口を出したい場面だから。**答えは1手であって据え置き命令ではない**（取り上げられた瞬間に使い切られる＝段階19 の「命令が流れる場合」が要らない）。**猶予60tick**を過ぎた質問には答えられない。狙えるのは視界の中だけで、検査は `World.Answer` が行い `OrderHuman` と同じエラーを返す。**`Config` には1つも足していない**（質問の間隔も猶予も世界のルールではないのでコントローラのフィールド）。

- 節目に決める性格（`engine/disposition.go`、段階23）: 人が動かせるのは**好み3つ**（`RiskWeight` / `CompetitionWeight` / `ShockRisk`）＝**段階12a が「正解が世界に無いので学べない」と切り分けた側**で、人に渡しても何も上書きしていない唯一の量。**読み方は「世界の値の何倍か」**（3つは単位が違うので倍率でしか足せない）。**合計は継いだもので、変えられるのは配分だけ**（`World.SetDisposition` は与えられた比率を継いだ合計に合わせて縮める）——用心深くするぶんは他の2つから出すしかない＝**取引が必ず発生し、最適値に固定して逃げ切れない**。上限（`maxPreferenceFactor` = 4）はそのまま。**何を「節目」と数えるかは engine の外**（`cmd/devview` が 乗り換え・成人・ペア成立・出産 の4つを見る）。

- 贈与（`engine/endow.go`、段階27）: **engine の中で唯一、継承も獲得もしていないものを個体に渡す操作**（`World.Endow(id, gene, value)`）。**シミュレーションの側からは誰も呼ばない**（規則も、コントローラも、出生も）。理由は操作性——予算は固定で配分は遺伝するので、**渡された身体が速さにほとんど使っていないと、人が動かす世界は糖蜜になる**。**世界を速くするのではなく主人公に良い身体を渡す**（世界を速くすると、測定した世界と遊んでいる世界が別物になる）。**新しい予算で払う**（他の遺伝子から取ると、プレーヤーが同意していない取引になり、主人公が静かに脆くなる）。**足した量を返す**ので、UIが「これは世界がやったことではない」と表示できる。**贈与は血統に残る**（予算は遺伝するので主人公の子は増えた予算を継ぐ）——避ける方法は「誰の予算が本物か」という第2の規則を作ることだけで、そのほうが悪い。


- 指1本で遊ぶ（`cmd/devview/touch.go`、`TODO.md` の9番。2026-09-20）: **タップで狙い、同じところをもう一度タップで既定の行動、長押し（0.5秒）でメニュー**。**engine は1行も変わっていない**——ここにあるのは入力の層だけで、命令は今までどおり `OrderHuman` を通り、同じ理由で断られる。**キーもマウスも残る**（開発はこの機械でやる）。**指とマウスは1つの層に入れてある**（`pointer`）ので2つの入力経路が食い違わない。**既定の行動は4つだけ**——食料＝食べる／外敵＝攻撃／人＝観察／地面＝そこまで歩く。**人を2タップで殴らない**のは、取り消せないことを2タップにしないため（攻撃は長押しのメニュー）。**長押しのメニューはその狙いに意味のある命令だけ**を並べ、**数字キーでも選べ、外をタップすれば閉じる**（キーボードの無い端末で閉じられるように）。**そしてメニューは質問にも使う**——後継選び・求愛の返事・訊かれるモードの質問・節目の選択の4つは「短い一覧から1つ選ぶ」で同じ形なので、**どこをタップしてもその選択肢が出る**（**数字キーだけだった4つが指で答えられるようになった**。新しい訊き方は1つも足していない）。**段階19 の2つの約束はそのまま**（見せるのは `Perception` の中身だけ／狙えるのは視界の中だけ。見えていない相手を長押しすると、その地面を狙う）。**判定は時間で、フレーム数ではない**——**フレーム数は2度間違いだった**: 遅い端末では長押しが1.5秒になり、**実測でブラウザが1秒に4フレームしか描かない状況があった**。**押している間に1フレームも来なかった長押しは、離した時点で長押しとして扱う**。**指がずれたら（8px 超）どちらでもない**。**ブラウザで実際に確認した**（WASM + 合成タッチイベント。タップで狙い、2度目で `walking to the ground at 164,560`、長押しで4項目のメニューが出た）


- 円ではなく絵で描く（`cmd/tiles` と `cmd/devview/tiles.go`、`TODO.md` の10番。2026-09-20）: **身体と物は1枚のシートから貼った絵**（16px 四方・透過・灰色で描き、貼るときに `ColorScale` で色を付ける）。**`-circles` で以前の円に戻せる**（2026-09-20 より前のスクリーンショットはその描き方）。**絵は `cmd/tiles/art.go` に1文字1ピクセルで書いてある**——**地形・地域・天気・植物・外敵を文字で描くのと同じ形**（決定#133）で、**この project に絵描きはいないが、文字で描く習慣はある**。**状態は4つ**（立つ・歩く・食べる・戦う）×2コマで、**向きは進行方向に左右反転**、**大きさは体力上限**（円の半径と同じ量）、**色は性別＋ID から引いた小さなばらつき**（遺伝子とは無関係＝群衆が同じ絵の繰り返しに見えないためだけ）。**円が持っていた読みは全部残っている**——**輪の太さ＝攻撃力、輪の色＝状態、尾＝速さ、帯＝空腹**、そして**「円の塗り＝残り体力」だけは絵にできないので緑の帯になった**。**シートは1枚**（同じテクスチャからの描画は何枚貼っても1バッチ）で、**ファイル名に中身のハッシュが入り**、**manifest を読んで全部揃ってからゲームを始める**（1コマ目に穴が空いた世界を見せない）。**ネイティブは埋め込み、ブラウザは `fetch`**——**15MB の wasm と 1.4kB の絵は変わる頻度が違う**ので、絵を直すたびに wasm を配り直さずに済む。**画面の外は描かない**（`onCamera`）——**遊ぶズームでは 270 のうち 52 しか画面に無い**。**測定（同じ wasm・`-circles` と対）: ブラウザで 11.0 → 14.4 fps**（絵のほうが速い。**ebiten の vector は Go 側で三角形に分解するので wasm では高い**）。**ネイティブ（WSLg のソフトウェアGL）は 10 → 11 fps で雑音の中**

- 人間の絵を生成物に替える（`tiled/tools/pack_sprites.py` と `docs/sprites.md`。2026-09-21）: **人間と物は 32px の描かれた絵、外敵だけ 16px の灰色の置き絵**で、**同じ1枚のシートに同居する**（クリップが自分の `w`/`h` を持つので、画面上では同じ大きさに描かれる）。**色を掛けるのはシートが「掛けろ」と言ったクリップだけ**（`manifest.json` の `tint`）——**描かれた身体に性別の青を掛けると水死体になり、ピンクを掛けると赤鬼になる**ので、**灰色の置き絵にだけ掛ける**。**だから性別はクリップが言う**（`clipFor` が `a.Sex` で分岐し、青い短パンとピンクのワンピースで読む）——**円の塗りだった事実を絵の側へ移しただけ**で、輪・尾・帯の読みは全部そのまま。**拡大は最近傍、縮小は線形**（遊ぶ倍率では絵が大きくなるので最近傍でなければ潰れ、世界全体を映すと身体は7px で、最近傍だと6画素に5画素を捨てて動くたびにちらつく）。**シートには使っていないクリップが眠っている**——被弾・水中・死亡・子供・老人・髪色8種で、**使うのは `clipFor` に1行足すこと**（**髪色以外は 2026-09-22 に全部起きた**。下の2項目）。**生成器はドット絵ではなく「ドット絵の絵」を返す**（1体に 32,718 色、同じ行も列も繰り返さない、1ドットが 3.7px の非整数）ので、**切り出しではなく縮小で作る**。**縮尺は同一画像の中でしか揃わない**（行の中は±1.6%、画像をまたぐと12%）ので、**シートは必ず1枚にまとめて頼む**。**基準キャラクターを各行に相乗りさせる約束は2回とも守られなかった**（11行中3行が子供として描かれた）ので、**基準は割り算の分母ではなくドリフト検出器**として使う。**地面に立っているクリップはそれぞれを同じ高さに正規化する**——**同じ体が立ち90・歩き83・攻撃81・被弾79 と戦闘中に12%縮み、頭は52→56 と太っていた**（画風のドリフトで、頭に合わせると胴の縮みが増幅される）。**しゃがむ・水に入る・子供・倒れるは正規化しない**（高さがそのまま言いたいこと）

- 絵にしてから直した3つ（2026-09-21、上の項目と同じ日）: **① 人の大きさを揃えた**——**人は年齢だけで大きさが変わり**（`AgeFactor`。子供＜大人、老人＜大人）、**予算で変わらない**。**外敵は今までどおり身体の大きさで描く**（「それがどのくらい大きいか」が外敵について最初に訊かれることで、絵が答えられる唯一のこと）。**円だった頃は半径＝体力上限で読めていたが、60人を60通りの大きさで描くと画面が潰れる**——**その読みは足元の帯へ移した**（**帯の長さ＝体力上限、帯の埋まり＝残り体力**なので、**傷ついた大きい身体と健康な小さい身体は今も区別できる**＝予算が作りたかった差そのもの）。**3つ（身体・輪・吹き出しの位置）が同じ答えを使うよう `bodySize` 1か所にまとめた**。**② 人の遺体を食べ物として描くのをやめた**——**engine は肉が誰の種族から来たかを最初から覚えている**（`Food.From`。**自分の種族の肉は食べられない**）のに、**ビューアは両方を同じ骨付き肉で描いていた**ので、**死者の多い世界が誰かの食料庫に見えていた**。**人の遺体は骨**（`remains` クリップ。`cmd/tiles` の置き絵）。**ルールは1つも変わっていない**——**絵が規則に追いついただけ**。**③ 群れを群れとして描く**（同じ日。`-huddle` で元に戻せる）——**身体には大きさが無く**、**世界は800幅で身体は18幅に描かれる**ので、**良い土地に40体集まると入りきらない**。**円なら輪が重なるだけだったが、人の絵になると混ざって読めなくなった**。手は2つで、**(a) Y順に奥から描く**（**嘘を1つもつかない**。今までは `agents` の順だったので奥の身体が手前の上に描かれていた＝これが「混ざる」の主因）と、**(b) 近すぎる身体を少し押し離して描く**（**真の位置から `crowdNudge`＝5 world単位を超えて動かさない**。完全に離すには18要るが、それでは身体が居ない地面に描かれる）。**engine は問い合わせも書き込みもされない**（`World.Agents()` は複製を返す）。**身体を指すものは全部同じずらした位置を使う**——絆の線・誰が誰に向かっているかの線・輪・帯・吹き出し、そして**クリックの当たり判定**（描かれた場所と選ばれる場所が違うのは、混ざって見えることより悪い）。**重なった身体どうしの向きは ID から引く**ので、**フレームごとに震えない**

- 外敵も絵で描く（`tiled/tools/pack_sprites.py` の `enemies` の並び。2026-09-22）: **体格4つ**（`big` / `small` / `water` / `fly`）**×4つの状態**（立つ・歩く・食べる・戦う）と、**飛んでいるときの2つ**（滑空・羽ばたき）。**どの体格を出すかは地図が何と名付けたかではなく、世界がその型を何だと言っているかで決まる**（`enemyBuild`。`Flies` → `fly`、`Water` → `water`、`BudgetMean` が世界の `EnemyBudgetMean` より大きい → `big`、それ以外 → `small`）——**名前と場所は地図が、何であるかは行が持つ**（決定#133）ので、**重い獣を別の名前で呼んでも重い絵になり**、**表に無い型の身体が来ても穴ではなく `small` になる**。**大きさはこの選択に入っていない**——**ビューアは予算から大きさを読む**（`bodySize`）ので、**4体格はタイルを同じだけ埋めて描かれ、差は予算が作る**。絵の側でも人の1.5倍にすると同じ事実を二度数える。**空にいる身体は専用のクリップになった**——**浮かせて影を落とすのは前からだが、絵は地上と同じものを持ち上げていた**。**動いていれば羽ばたき、止まっていれば滑空。** **これで灰色の置き絵は1つも残っていない**（人の遺体の骨も描かれた絵に替わった）。**`cmd/tiles` は次に誰も描いていないものが出たときのために残してある**

- 眠っていたクリップを起こす（`clipFor` の `look`。2026-09-22）: **絵は「何をしているか」だけでなく「何が起きているか」でも選ばれる**ようになった。**順番が設計そのもの**で、**目が一番必要とするものから**: **殴っている**（`ActAttack` / `ActThrow`）＞ **殴られている**（`human.hurt`）＞ **水の中にいる**（`human.swim`）＞ **年齢**（`child.idle` / `old.idle`）＞ 行動。**殴るのが殴られるに勝つ**のは、両方やっている身体は「前に出ているほう」として読むほうが読みやすいから。**殴られている判定は engine のダメージではなく「誰が誰に向けて攻撃を選んだか」から作る**——**ビューアが既に赤い線を引いているのと同じ事実**なので、絵と線が食い違えない（そして相手の `Perception` に載っている事実なので、線が許された条件をそのまま満たす）。**水は `TerrainAt().Kind`、年齢は `IsAdult` と `AgeFactor`**——**どれも engine が既に持っている線で、新しい閾値は1つも作っていない**（老いは `AgeFactor < 1`。**世界で老いの規則が切ってあれば誰も老人にならない**）。**子供と老人は「立っているときだけ」**——**シートにあるのは立ち姿だけ**で、歩きにも使うと**大人が歩く横で子供だけ立ち姿のまま滑る**ことになる。**動いている子供は今までどおり大人の絵を小さく描く**（何も失われていない。立ち止まった子供が子供に見えるようになっただけ）。**この制限は同じ日の夕方に消えた**（次の項目）。**殴られると水中は人間だけ**——**獣の被弾の行は頼んだが返ってこず**、**水中は獣にとって体格であって状況ではない**（川を渡る大型は大型のまま。`enemy.water.*` を流用すると「どの型か」という、この画面が唯一間違えてはいけないことを偽る）。**倒れた身体は engine にはもう無い**（`Agents()` は生きているものだけ）ので、**ビューアが最後の位置を覚えて150tickだけ薄れながら描く**——**外敵が着いたときの輪と同じ長さ**で、同じ合図の生涯の反対側だから。**これがこの画面で唯一「世界に無い身体」を描く場所**なので、**短く・輪も帯も付けず・クリックできない**（そこには選ぶものが無い）。**髪の7色はまだ眠っている**——**頭ではなく立ち姿が7体**なので、使うと**立っているときだけ髪の色が変わり、歩いた瞬間に茶色に戻る**。**系統を髪で見分けるには絵を2層（髪の無い身体と髪だけ）で頼む必要があり、それは書くことではなく頼むこと**

- 子供と老人が6つのポーズを持つ（`tiled/tools/pack_sprites.py` の `children` の並びと `clipFor`。2026-09-22）: **`elder_children.png` が4組**——男の子・女の子・老人・老婆——**×立ち・歩行・食事・攻撃・被弾・水中×2コマ**で届いたので、**年齢を訊く場所が動いた**。**それまでは動作の後、しかも「立ち止まっているときだけ」**訊いていた（絵が立ち姿しか無かったため）。**今は性別と同じ場所で、何より先に訊く**——**年齢は状況ではなく、その身体が何であるか**で、**歩き出したからといって子供でなくなるわけではない**。**engine の線は1つも変わっていない**（`IsAdult` と `AgeFactor < 1`。老いの規則を切った世界には今までどおり老人が1人もいない）。**変換の表で2つ決めた**——**老いは成人の背丈に正規化する**（このシートは老いを子供の背丈で描いてきた。老人115・男の子112・基準141。**禿げた子供は、縮まなかった老人より悪い嘘**で、**老いは白髪と禿頭が言っている**）、**子供は描かれた比0.80を保つ**（24pxの絵で「子供」と言っているのはそれだけ）。**残りはドリフトの実測**（同じ身体が歩くと7%高く・食べると6%低く描かれる）。**このシートで初めて、基準キャラクターが人間の絵の全行で守られた**（141〜142px ＝ ±0.4%）

- 絵の幅とタイルの幅（`pack_sprites.py` の `fits` / `wide`。2026-09-22、上と同じシートが強いた）: **クリップのコマは「まとめて縮小する」と書いてあったのに、幅だけが例外だった**——コマごとに幅で縮めていたので、**同じ死体がコマ1で14px・コマ2で15px** になっていた。**今は幅の超過も全コマで測ってクリップごと同じだけ縮める。** **そのとき「身体の幅」と「絵の幅」を分ける**——**殴った線・当たった赤は取り込みのときに身体へ糊付けしてある**（別にすると行の中の体の数が合わなくなる）が、**このシートでは線が身体の倍近く飛ぶ**（身体68に対して絵130）ので、**これに合わせると身体のほうが縮む**（**老人が殴るときだけ1割小さくなる**、というのが実際に出た絵）。**だから幅を測るのは糊付け前のいちばん大きな塊だけ**で、**はみ出した線はタイルの縁で切れる**——**線の先が2px切れていることは目が比べる相手を持たず、身体が1割縮むことは持つ**

- 絵をセットにして入れ替える（`cmd/devview/assets/<名前>/` と `sets.json`、`-art` / F4。2026-09-22）: **絵は生成物なので、描き直すたびに別の絵が返ってくる**——**良くなったかどうかは、同じ世界の同じ瞬間に2つ並べてみるしか判定できない**。だから**1セット = 1ディレクトリ**（シートと manifest）にして、**`-art <名前>` か、走らせたまま F4 で切り替える**（F4 は世界を1tickも進めない）。**一覧と既定は `sets.json`**——**ブラウザはディレクトリを一覧できない**（名前で `fetch` するだけ）ので**ファイルに書くしかなく**、**手で書いたものは腐る**ので**テストが `assets/` の中身と突き合わせる**。**ネイティブは全セットを埋め込み**（1セット数百kBで、実行ファイルは数十MB）、**ブラウザは選ばれたセットだけ取る**。**古いセットは今のコードが訊く絵を全部は持っていない**（`set1` に歩く子供はいない）が、**穴にはならず一番近い絵で描く**——**諦める順は 年齢 → 性別 → ポーズ**。**ポーズが最後なのは、隣の身体と見比べられる唯一の部分だから**（大人が歩く横で子供だけ立ち姿のまま滑ると「子供」ではなく「壊れた絵」に見える）。**年齢を先に諦めても嘘にならないのは、身体を小さく描くのがビューアの仕事だから**（`AgeFactor`）——**大人の絵を小さく描いた子供は 2026-09-22 より前の世界そのもの**。**獣は体格だけは諦めない**（どの型かを偽ることになり、それはこの画面が唯一間違えてはいけないこと）

- 絵の足りないところを数える（`cmd/devview -artgaps`。2026-09-22）: **絵は生成物で、頼んだ行が返ってこないことが実際に2度あった**のに、**画面は何も言わない**——**無い絵は一番近い絵で描かれる**（`standIn`）ので、**遊ぶときには正しく、次に何を頼むか決めるときには最悪**。だから数える。**3つ出る**——**`missing`**（ビューアが訊くのに無い絵。**一覧は誰も書いていない**: `clipFor` / `deadClipFor` / `itemClip` に全組み合わせを訊いて作るので、**規則が新しい絵を訊き始めた日から自動で数に入る**。**テストが「報告器が見落としていないか」を毎回突き合わせる**）、**`wrong`**（**ある**のに測ると間違っている絵。**歩きの2コマが同じ身体の立ち姿より動いていない**＝脚が見えていない／**立っているはずのポーズの背丈が立ち姿と5%以上違う**。**目盛りは同じセットの同じ身体**で、外から持ってきた数字ではない——**何を「動いている」と呼ぶかはその絵の描かれ方で変わる**から。**獣は背丈を測らない**: 獣はタイルを幅で埋めるので、ポーズごとに背丈が違うのが正しい）、**そして「どのセットにも無いので、ビューアが訊きもしないもの」**（**手で書いた一覧**。**無いものをビューアは訊かないので機械には見つけられない**。**届くとテストが「消せ」と言う**）。**世界も窓も作らない**ので、画面の無い端末でも走る。**2026-09-22 の実測では、女性側の歩き3つと獣4体格の歩きが全部「弱い」に出た**（＝手で数えた結果と一致）

- 地面も絵で描く（`cmd/devview/ground.go` と `pack_sprites.py` の `terrain` / `water` の並び、絵のセット `set3`。2026-09-22）: **身体が円をやめた2026-09-20 から、地面だけが4色のべた塗りのまま残っていた**——**そのセルが何を要求するか（水は溺れる・荒地は高い）は言えていたが、そこが何であるかは何も言っていなかった**。**世界観は「もののけ姫の森」**（苔むした照葉樹の森・渓流・岩と巨木の根）で、**規則は1つも足していない**。**4つ決めた**——**① 絵は数字を言わない**（どの段にいるか・豊かさ・寒暖は今までどおり上に重ねる半透明の色。**絵は「そこが何か」を言い、ウォッシュは「いくつか」を言う**）。**② 塊は「面と縁」で描く**（中は面、下を見下ろす側は縁。**縁は3枚**＝辺・外角・入り込んだ角で、**貼るときに90°回す**。**これが「巨木が何マスにもまたがって1本に見える」ことの全部**で、4方向ぶん描いてもらう必要がない）。**③ 高さ3以上は「場所」ではなく「もの」**（`blockLevel`。**新しい規則ではなく既存の規則の読み上げ**——**`canStep` は段差2を拒むので、高さ3に登らせるには坂を3枚並べるしかなく、誰も並べない**。**登れないものは最初から壁だった**ので、**巨木の幹と巨岩として描く**。**高さ5以上は岩**＝`rockLevel`）。**④ 平地・荒地・水はセルごとに8通りの向きで貼る**（座標のハッシュ。**乱数は引かない**＝世界の指紋は1ビットも動かない）。**④が一番効いた**——**3枚の森の床をそのまま敷き詰めると32pxの格子が見え、向きを散らすと地面として読める**。**坂だけは向きを持つ**——**engine は向きを知らない**（`Slope` は真偽値と高さだけ）が、**地図は知っている**（隣のどちらが高いか）ので、**ビューアがそれを読んで段を回す**。**どちらとも言えないセットには向きの無い坂**。**地図の無い世界も森になる**（32単位の格子で森の床を敷く。**以前は真っ白**だった）。**絵を持たないセット（`set1` / `set2`）は今までどおりべた塗り**——**全部揃っているか、1枚も無いかの二択**（`hasGround`）で、**ポーズと違って「一番近い地面」は無い**: **崖の絵で川を描くのは、川が崖を登っていることになる**。**巣も絵になった**（`ground.nest`。囲みの矩形は残す——**矩形は「その型がどこから世界に入るか」という地図の事実で、穴の絵はそれを言わない**）。**フィルタは常に最近傍**（身体は縮小時に線形。**タイルを線形で縮めるとシートの隣の絵を吸い込み、遊ぶ倍率＝セル27pxで世界に1セル1本の淡い格子が出た**）

- 地形のタイルセットも同じ絵にする（`pack_sprites.py --tiled` と `tiled/samples/tilesets/002_mononoke/`、`tiled/samples/002_forest.tmj`。2026-09-22）: **Tiled で地図を描く人が見ていたのは7色の色板で、遊ぶ世界とは何の関係も無かった**。**同じシートから8タイルを書き出す**ようにしたので、**描いている絵と貼られる絵が同じ**になる。**並びは色板と同じ**（`001_simple`）なので、**あちらで描いた地図は意味が変わらない**。**8枚目は巨岩**（`height: 5`）。**高さと絵の対応は地図の作者との約束**なので、**規則ではなく `tiled/properties.md` に書いた**（1〜2は登れる高台、3〜4は巨木、5以上は巨岩、**巨木は3×3以上で塗らないと幹の面が1マスも出ない**＝2×2は全マスが縁になる）

- 地面の色を引き離し、段差に線を引く（`pack_sprites.py` の `PALETTE` と `cmd/devview/ground.go` の `drawDrop`。2026-09-22、上の項目と同じ日の夕方）: **描いた地面を初めて見た人の第一声が「台地と平地、荒地と平地の区別がつかない。高台の輪郭は濃く、色はもっと分けて」だった**——**測ると、家族ごとの平均色の差は平地対荒地で10.6、平地対幹で9.3**（0〜255）**で、明るさは 77.0・78.7・79.1**。**絵は美しく、そしてほとんど一色だった。** **貼るたびに色を掛けるのではなく、変換のときに1度だけ引き離す**（チャンネルごとの倍率なので**テクスチャはそのまま**で、**乾いた光・日陰の光で見ているだけ**になる）。**明暗は1つのことだけを2段階で言う**——**世界の床は暗く、台地の上は明るい。段数に比例させない**（**4段階のグラデーションは4つとも中くらいの緑にしかならなかった**）。**今そこが何段目かを言うのは段差の線のほう**で、**残りは同じ明るさの中で色相が言う**（荒地は乾いて赤・幹は茶でさらに暗く・岩は灰色で冷たく・川は青）。**押さないのは、1枚の中に平地を含むタイル全部**（水際・根の縁・坂・巣・川）——**半分が野原のタイルは隣の野原と同じ緑でないと継ぎ目が出る**。**結果: 明るさ 66/66/124/49/72（床・荒地・台地・幹・岩）、距離は床対荒地 10.6 → 26.6、床対台地 20.5 → 98.2、床対幹 9.3 → 30.1。飽和は1万分の1。** **同じ絵を2つの光で焼くこともした**（`ground.scree.*`＝岩の縁を床の明るさで。**巨岩の足元に台地の縁を使うと岩のまわりだけ明るい輪ができる**）——**新しい行を頼まずに済む**

- 越えられない段差に線を引く（`drawDrop`。2026-09-22、同じ日）: **引くのは `canStep` が断る辺だけ**（**線が言っているのは地面の種類ではなく規則**）。**べた塗りの上では茶色の細線で足りていたのに、葉と石の絵の上では足りなかった**ので、**濃い線1本＋内側に薄い帯2本**（3分の1ずつ薄くする）。**帯が要るのは、線だけだと写真にペンで描いたように浮くから**で、**土手の縁が実際に光に対してしていること**をなぞっている。**帯は必ず高いセルの内側に入れる**——**下は別の場所で、台地に属するものをそこに描いてよい理由が無い**。**坂には引かない**——**坂は高い側に立つので他の高台と同じく下に段差を持つが、階段の足元に線を引くと「唯一登れる場所を塞ぐ縁石」に見える**。**太さはセルの割合**（遊ぶ倍率でセル27px、世界全体を映すと8px）。**これが段数を言う唯一のもの**になったので、**高さに比例する明るさのウォッシュは廃止した**

- 住人を精霊に描き替える（`pack_sprites.py` の `spirits` / `spirit_young` / `spirit_eat` / `spirit_beasts`、絵のセット `set5`＝既定。2026-09-22）: **人・子供・老い・獣・亡骸まで全部が白い精霊になった**。**この cast の1点だけが今までと違う**——**色を持っていない**（彩度の中央値1〜5、ほぼ白60〜72%、暗い輪郭20〜26%）。**白×色＝その色**なので、**貼るときに系統の色を掛けられる**。**これは「髪と肌は後から色を変えられない」（`docs/sprites.md` 1節③）の裏返し**で、**人間の絵では髪色を8種描いてもらうしかなかった**ものが、**精霊ならビューアが無限に作れる**。**代わりに、色が言っていたことを形が言う**——**性別＝頭の形**（丸い耳／炎のような房）、**年齢＝形**（子供は頭が大きい／老いは房が垂れ輪郭がほつれる）。**大きさは絵で言わない**（`AgeFactor` が縮める）。**獣の被弾が2度目の依頼で届いたので、`clipFor` が殴られた獣にもその絵を引くようになった**（それまで人間だけ。**水中は今までどおり人間だけ**——**水の獣は状況ではなく体格**で、流用すると「どの型か」を偽る）。**「髪を2層で」の願いは消えた**——**あれは色を掛けられない絵で系統を示すための回り道**で、**白い身体に掛けるほうが答えだった**（願いの一覧は空になった）。**まだ配線していないのは色そのもの**（`tint` は false のまま。光の輪・粒・波紋の3種も描かれて眠っている）

- 生成物を切り出す機械に足した3つ（`pack_sprites.py`。2026-09-22、上と同じ日）: **① 行の中身を「数」で切る**（`COUNTS`）——**攻撃の線・消滅の粒・光の粒で1行が最大22個に割れる**。**糊（膨張）を強くするのは逆**で、**粒の雲を集められる強さは隣の身体も集める**。**大きい塊を核にして小さいものを近い核へ寄せ**、**核が無い行（光の粒）だけ広い隙間で切る**。**② 遠すぎる欠片は別の行のもの**（核の幅より遠ければ拾わない）——**帯の切れ目で飛ぶ獣の翼の先が下の行に残り、亡骸のクリップが横1052pxに伸びた**。**③ 高さの上限も「身体」で測る**（`tall`）——**幅だけ身体で測っていたのに高さは絵で測っていた**ので、**頭上に衝撃線が飛ぶ被弾で身体が7%縮んでいた**（同じ日の朝に幅で塞いだ穴が高さに残っていた）。**そして `--drop`**——**新しい cast が1行だけ持っていない絵は、古い cast のものを持ち越さない**。**茶色い人間が精霊の横に立つより、大人の精霊の食事を小さく描くほうがまし**（`standIn` がそうする）

- 明るさが「誰の血筋か」を言う（`bodyPaint` と `pack_sprites.py` の `TINTED` / `SHADOW`。2026-09-22、精霊と同じ日）: **プレーヤーの身体と、その血筋が産んだ子は絵に描かれたまま**（世界で一番明るい）、**それ以外の人は暗く塗り、色みは遺伝子から引く**（赤＝戦いと生存に、緑＝動きに、青＝知ることに予算を割いた身体。**明るさは全員同じ**で、**明るさは「誰の血筋か」の答えに取ってある**）。**色は灰色に1段足して作る**——**一定の明るさに揃える作り方は原色になる**（暗いチャンネルが強い混色は、明るさを合わせるために上へ振り切れて飽和する）ので、**足し算だけにして、どのチャンネルも1段しか動かさない**。**子を「少し暗く」しないのが設計**——**血筋は1つのもので、この画面が答えるべき問いは「60体のうちどれが自分のか」**だから。**誰も操作していない世界では全員が明るいまま**（対比する相手がいないのに全体を暗くしても、ただ暗い世界になる）。**性別は失われていない**——**精霊は頭の形で言う**（丸い耳／炎のような房）ので、色は血筋に使える。**獣は色を掛けず、変換のときに裏返す**——**白いところが暗く、暗い輪郭が明るい**（`SHADOW`）。**掛け算では作れない**（明るさを引くことしかできないので、白はどんな色でも一番明るいまま）ので、**貼る前に焼く**。**これで獣は「人と同じ光でできていないもの」として、色を1つも使わずに読める**。**`tint` の旗は絵の大きさではなく並びが決めるようになった**（以前は「16px＝灰色の置き絵」。**精霊は32pxで、かつ色を掛けるために白い**ので大きさでは区別できない）。**`--carry` も旗を持ち越す**——持ち越さなかったせいで**3枚を経由した精霊が灰色で届いた**

- 血筋の光を敷き、獣を人より大きくする（`drawAura` と `minRadius`。2026-09-22）: **明るさが言っていることを、操作している1体にだけもう一度言う**——**プレーヤーの身体は足元の波紋＋身体を囲む輪**（金）、**血筋の子は舞う粒**（淡い金）。**二重に言う価値があるのはここだけ**で、**60体の中で最初に見つけなければならないのが played の身体**であり、**画面外へ出て戻ってくるのもそれ**だから。**3つの絵の役は形から決まる**——**波紋は群衆に強い**（身体が重なっても縁が残る）、**輪は引いた絵に強い**（小さくても輪郭に沿う）、**粒は輪と見間違えようがない**（＝「あなたではない、あなたの血」）。**誰も操作していない世界では1つも出ない**（指す相手がいない）。**そして獣の大きさの下限を人に合わせた**——**`minRadius` は 3.5**（人の3分の1）**だった**: **円だった頃はそれで正しかった**（半径＝体力上限で、軽い獣は本当に身体が小さい）が、**描かれた獣になると「大人に混じった子猫」に見え**、**この画面が唯一間違えてはいけないこと（獣とは自分を食うもの）を偽っていた**。**今は一番軽い獣が大人と同じ「高さ」、重いものはその1.5倍**（`minRadius` 16・`maxRadius` 24）。**半径で揃えると足りません**——**人は幅の0.94倍の高さに描かれ、獣は0.39〜0.58倍**（四つ足だから）なので、**幅を合わせると獣のほうが低く**なり、子猫のまま。**目が比べるのは高さ**です

- 獣を人より大きく、人より粗くなく（`minRadius` / `maxRadius` と `pack_sprites.py` の `CELL`。2026-09-22）: **半径を人に合わせても足りませんでした**——**人は幅の0.94倍の高さに描かれ、獣は0.39〜0.58倍**（四つ足）なので、**幅を揃えると獣のほうが低く**、子猫のままです。**目が比べるのは高さ**なので、**一番軽い獣の高さが大人と同じ**になるところまで上げました（`minRadius` 16・`maxRadius` 24。大型で人の1.5倍＝絵の依頼文と同じ）。**そして獣だけ96pxのセルで切ります**——**クリップは自分の大きさを持ち、ビューアが画面上の大きさに合わせる**ので、**32pxのままだと獣は毎回拡大されて描かれ**（人は縮小される）、**隣に立つ人より目に見えて粗く**なっていました。**元の絵は1体160px幅あり**、足りなかったのはセルのほうです。**96px は遊ぶ倍率から決めました**——**ズーム2.5で人は32pxのクリップから45px に描かれ（1.4倍）、一番重い獣は120px**。**64pxでは2倍近い拡大で縁がブロックになり**、**96pxなら1.25倍**で人より細かくなります
- 地面に落ちたものが光る（`colorItemGlow`。2026-09-22）: **描かれた地面は苔と土で、その上の小さな緑の点＝木の実は、プレイヤーが一番よく探すもので画面で一番見えないもの**でした。**同じ絵を1.7倍に拡大し、蛍光の黄緑で後ろに敷きます**（輪郭を描かずに輪郭を作る。**1pxずつずらした4枚を重ねる方法は、身体に隠れて見えませんでした**）。**色は種類ごとではなく1色**——**緑の植物の下に緑の光では光にならず**、**光が言うべきことは「ここに何か落ちている」**で、**何が落ちているかは中の絵が言います**。**この世界で蛍光色を使うのはここだけ**（身体の色は「誰の血筋か・何に予算を割いたか」という読みなので、くすんだ色に抑えてある）

- 肉になるのは倒された死体だけ（`MeatFromKills`。2026-09-22。**既定でオン＝4度目のベースラインのリセット**）: **画面を見た人の「外敵が自然死で肉になるのはおかしい」から入った規則**で、**stage 11 から死因を問わず全部の死体が肉になっていた**。**もっともらしさの話が、測ったら世界の話だった**——**12シード × 200000tick で 個体数 161 → 215 \*、外敵 41 → 57 \*\*、餓死 −507 \*\*、崩壊 0.08 → 0.00 \***。**予想は4つとも外れた**（供給が15〜30%減る・外敵が減る・人は変わらない・餓死が増える）。**減ったのは供給ではなく無駄**で、**`meatDropped` は動かず**（+594、有意でない）、**腐る割合が 0.21 → 0.03 \*\*\***・**食べられる割合が 0.78 → 0.97 \*\*\***。**自然死の肉は「その身体がたまたま居た場所」＝誰も居ない場所に落ちるから腐り**、**倒された身体の肉は倒した者が立っている場所に落ちるから食べられる**。**そして大きくなった世界はより多くの死体を出すので、殺しから来る肉が自然死のぶんをそのまま埋めた**（`killed` +2200 \*\*\*）。**判定は kill の集計と同じ式**（直前の tick に殴られていて、溺死でない）——**肉になるものと kill と数えるものがズレないため**。**旧世界の腕は `meatalways`**

- 身体の色は生まれた家系（`cmd/devview/tiles.go` の `lineColour` と `cmd/devview/succession.go`。2026-09-22・ユーザの指定）: **人間は自分が生まれた家系の色で描かれ、子は母の色を継ぐ**。**白いのは操作している身体と、その跡を継ぐ者**——**世代ごとに1人、生きている長子**（**長男が死んだ瞬間に次男へ移り、兄弟が尽きたら次の世代へ**）。**`-paint budget` で 2026-09-22 以前の色**（予算の使い道＝赤・緑・青）**に戻せる。**
  - **engine には1ビットも足していない。** **家系の色は `Agent.Lineage` そのもの**——**engine が既に配り、母から子へ継がせ、保存/読込も往復しているタグ**で、**規則は誰も読まない**。**「父か母のどちらかの色」という指定に対して母を選んだのは、engine が既にそう配っているから**で、**どちらかを viewer 側で選ぶと viewer が自前の記憶を持つことになり、同じ世界を見ている2つの画面が違う色を出す。**
  - **跡継ぎは記録ではなく質問。** **毎tick計算し直す**（`successionIn`）ので、**長子が死んだ瞬間に次子へ移る**。**これは `dynasty.go` が一度学んだことの再演**——**「誰が跡継ぎか」は誰かが死んだ瞬間に変わる問いで、出生の瞬間に配るタグでは書けない**（`PLAN.md` が選んでいた `LineageRegionChain` が削除された理由）。
  - **明るさは1つの問いにしか答えない。** **家系の色はどれも同じ明るさ**（Rec.601 の重みで補正してある＝**緑寄りの家系が明るく見えるのを消す**）で、**明るいのは「これはあなたの線か」だけ**。**2つ目の意味を同じチャンネルに乗せると1つ目が壊れる**、というのはこの画面が段階84 以来守っている線。
  - **色相は黄金比で回す**ので、**続けて建国された家系ほど色相環の上で遠い**（隣り合った番号が似た色にならない）。
  - **変わったことが1つある**: **操作していない世界でも色に意味が出た**ので、**「誰も操作していないなら全員を最大の明るさで描く」をやめた**（それまでは、区別する相手がいない世界を暗くしても意味が無いという理由でそうしていた）。
